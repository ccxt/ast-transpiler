"use strict";Object.defineProperty(exports, "__esModule", {value: true}); function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { newObj[key] = obj[key]; } } } newObj.default = obj; return newObj; } } function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; } function _nullishCoalesce(lhs, rhsFn) { if (lhs != null) { return lhs; } else { return rhsFn(); } } function _optionalChain(ops) { let lastAccessLHS = undefined; let value = ops[0]; let i = 1; while (i < ops.length) { const op = ops[i]; const fn = ops[i + 1]; i += 2; if ((op === 'optionalAccess' || op === 'optionalCall') && value == null) { return undefined; } if (op === 'access' || op === 'optionalAccess') { lastAccessLHS = value; value = fn(value); } else if (op === 'call' || op === 'optionalCall') { value = fn((...args) => value.call(lastAccessLHS, ...args)); lastAccessLHS = undefined; } } return value; }var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// ../../ast-transpiler/node_modules/tsup/assets/cjs_shims.js
var init_cjs_shims = __esm({
  "../../ast-transpiler/node_modules/tsup/assets/cjs_shims.js"() {
  }
});

// src/dirname.cjs
var require_dirname = __commonJS({
  "src/dirname.cjs"(exports, module) {
    init_cjs_shims();
    module.exports = __dirname;
  }
});

// src/transpiler.ts
init_cjs_shims();
var import_dirname = __toESM(require_dirname(), 1);
var _typescript = require('typescript'); var _typescript2 = _interopRequireDefault(_typescript);

// src/pythonTranspiler.ts
init_cjs_shims();

// src/baseTranspiler.ts
init_cjs_shims();


// src/types.ts
init_cjs_shims();
var TranspilationError = class extends Error {
  constructor(id, message, nodeText, start, end) {
    const parsedMessage = `Lang: ${id} Error: ${message} at ${start}:${end} node: "${nodeText}"`;
    super(parsedMessage);
    this.name = "TranspilationError";
  }
};

// src/utils.ts
init_cjs_shims();
function regexAll(text, array) {
  for (const i in array) {
    let regex = array[i][0];
    const flags = typeof regex === "string" ? "g" : void 0;
    regex = new RegExp(regex, flags);
    text = text.replace(regex, array[i][1]);
  }
  return text;
}
function unCamelCase(s) {
  return s.match(/[A-Z]/) ? s.replace(/[a-z0-9][A-Z]/g, (x) => x[0] + "_" + x[1]).replace(/[A-Z0-9][A-Z0-9][a-z][^$]/g, (x) => x[0] + "_" + x[1] + x[2] + x[3]).replace(/[a-z][0-9]$/g, (x) => x[0] + "_" + x[1]).toLowerCase() : void 0;
}

// src/logger.ts
init_cjs_shims();
var _colorette = require('colorette');
var Logger = class {
  // static createInstanceIfNeeded(): void {
  //     if (!this._instance) {
  //         this._instance = new Logger();
  //       }
  // }
  static setVerboseMode(verbose) {
    this.verbose = verbose;
  }
  static log(message) {
    if (this.verbose) {
      console.log(message);
    }
  }
  static success(message) {
    this.log(_colorette.green.call(void 0, `[SUCCESS]: ${message}`));
  }
  static warning(message) {
    this.log(_colorette.yellow.call(void 0, `[WARNING]: ${message}`));
  }
  static error(message) {
    this.log(_colorette.red.call(void 0, `[ERROR]: ${message}`));
  }
};
Logger.verbose = true;

// src/baseTranspiler.ts
var NO_CONTEXT_ERROR = "No transpilation context set: the typescript program must be created before printing nodes.";
var BaseTranspiler = class {
  constructor(config) {
    this.NUM_LINES_BETWEEN_CLASS_MEMBERS = 1;
    this.LINES_BETWEEN_FILE_MEMBERS = 0;
    this.NUM_LINES_END_FILE = 1;
    this.SPACE_DEFAULT_PARAM = " ";
    this.BLOCK_OPENING_TOKEN = "{";
    this.BLOCK_CLOSING_TOKEN = "}";
    this.SPACE_BEFORE_BLOCK_OPENING = " ";
    this.CONDITION_OPENING = "(";
    this.CONDITION_CLOSE = ")";
    this.DEFAULT_IDENTATION = "    ";
    this.STRING_QUOTE_TOKEN = '"';
    this.UNDEFINED_TOKEN = "null";
    this.NULL_TOKEN = "null";
    this.IF_TOKEN = "if";
    this.ELSE_TOKEN = "else";
    this.ELSEIF_TOKEN = "else if";
    this.THIS_TOKEN = "this";
    this.SLASH_TOKEN = "/";
    this.ASTERISK_TOKEN = "*";
    this.PLUS_TOKEN = "+";
    this.MINUS_TOKEN = "-";
    this.EQUALS_TOKEN = "=";
    this.EQUALS_EQUALS_TOKEN = "==";
    this.EXCLAMATION_EQUALS_TOKEN = "!=";
    this.EXCLAMATION_EQUALS_EQUALS_TOKEN = "!=";
    this.EQUALS_EQUALS_EQUALS_TOKEN = "==";
    this.AMPERSTAND_APERSAND_TOKEN = "&&";
    this.PLUS_EQUALS = "+=";
    this.BAR_BAR_TOKEN = "||";
    this.PERCENT_TOKEN = "%";
    this.RETURN_TOKEN = "return";
    this.OBJECT_OPENING = "{";
    this.OBJECT_CLOSING = "}";
    this.LEFT_PARENTHESIS = "(";
    this.RIGHT_PARENTHESIS = ")";
    this.ARRAY_OPENING_TOKEN = "[";
    this.ARRAY_CLOSING_TOKEN = "]";
    this.TRUE_KEYWORD = "true";
    this.FALSE_KEYWORD = "false";
    this.NEW_CORRESPODENT = "new";
    this.THROW_TOKEN = "throw";
    this.AWAIT_TOKEN = "await";
    this.STATIC_TOKEN = "static";
    this.CONTINUE_TOKEN = "continue";
    this.EXTENDS_TOKEN = ":";
    this.NOT_TOKEN = "!";
    this.SUPER_TOKEN = "super";
    this.PROPERTY_ACCESS_TOKEN = ".";
    this.TRY_TOKEN = "try";
    this.CATCH_TOKEN = "catch";
    this.CATCH_DECLARATION = "Exception";
    this.BREAK_TOKEN = "break";
    this.IN_TOKEN = "in";
    this.LESS_THAN_TOKEN = "<";
    this.GREATER_THAN_TOKEN = ">";
    this.GREATER_THAN_EQUALS_TOKEN = ">=";
    this.LESS_THAN_EQUALS_TOKEN = "<=";
    this.PLUS_PLUS_TOKEN = "++";
    this.MINUS_MINUS_TOKEN = "--";
    this.CONSTRUCTOR_TOKEN = "def __init__";
    this.SUPER_CALL_TOKEN = "super().__init__";
    this.WHILE_TOKEN = "while";
    this.FOR_TOKEN = "for";
    this.VAR_TOKEN = "";
    this.METHOD_DEFAULT_ACCESS = "public";
    this.PROPERTY_ASSIGNMENT_TOKEN = ":";
    this.PROPERTY_ASSIGNMENT_OPEN = "";
    this.PROPERTY_ASSIGNMENT_CLOSE = "";
    this.LINE_TERMINATOR = ";";
    this.FUNCTION_TOKEN = "function";
    this.METHOD_TOKEN = "function";
    this.ASYNC_TOKEN = "async";
    this.PROMISE_TYPE_KEYWORD = "Task";
    this.NEW_TOKEN = "new";
    this.STRING_LITERAL_KEYWORD = "StringLiteral";
    this.STRING_KEYWORD = "string";
    this.NUMBER_KEYWORD = "float";
    this.PUBLIC_KEYWORD = "public";
    this.PRIVATE_KEYWORD = "private";
    this.VOID_KEYWORD = "void";
    this.BOOLEAN_KEYWORD = "bool";
    this.ARRAY_KEYWORD = "List<object>";
    this.OBJECT_KEYWORD = "Dictionary<string, object>";
    this.INTEGER_KEYWORD = "int";
    this.DEFAULT_RETURN_TYPE = "object";
    this.DEFAULT_PARAMETER_TYPE = "object";
    this.DEFAULT_TYPE = "object";
    this.FALSY_WRAPPER_OPEN = "";
    this.FALSY_WRAPPER_CLOSE = "";
    this.ELEMENT_ACCESS_WRAPPER_OPEN = "";
    this.ELEMENT_ACCESS_WRAPPER_CLOSE = "";
    this.COMPARISON_WRAPPER_OPEN = "";
    this.COMPARISON_WRAPPER_CLOSE = "";
    this.UKNOWN_PROP_WRAPPER_OPEN = "";
    this.UNKOWN_PROP_WRAPPER_CLOSE = "";
    this.UKNOWN_PROP_ASYNC_WRAPPER_OPEN = "";
    this.UNKOWN_PROP_ASYNC_WRAPPER_CLOSE = "";
    this.EQUALS_EQUALS_WRAPPER_OPEN = "";
    this.EQUALS_EQUALS_WRAPPER_CLOSE = "";
    this.DIFFERENT_WRAPPER_OPEN = "";
    this.DIFFERENT_WRAPPER_CLOSE = "";
    this.GREATER_THAN_WRAPPER_OPEN = "";
    this.GREATER_THAN_WRAPPER_CLOSE = "";
    this.LESS_THAN_WRAPPER_OPEN = "";
    this.LESS_THAN_WRAPPER_CLOSE = "";
    this.GREATER_THAN_EQUALS_WRAPPER_OPEN = "";
    this.GREATER_THAN_EQUALS_WRAPPER_CLOSE = "";
    this.LESS_THAN_EQUALS_WRAPPER_OPEN = "";
    this.LESS_THAN_EQUALS_WRAPPER_CLOSE = "";
    this.DIVIDE_WRAPPER_OPEN = "";
    this.DIVIDE_WRAPPER_CLOSE = "";
    this.PLUS_WRAPPER_OPEN = "";
    this.PLUS_WRAPPER_CLOSE = "";
    this.MINUS_WRAPPER_OPEN = "";
    this.MINUS_WRAPPER_CLOSE = "";
    this.MOD_WRAPPER_OPEN = "";
    this.MOD_WRAPPER_CLOSE = "";
    this.ARRAY_LENGTH_WRAPPER_OPEN = "";
    this.ARRAY_LENGTH_WRAPPER_CLOSE = "";
    this.MULTIPLY_WRAPPER_OPEN = "";
    this.MULTIPLY_WRAPPER_CLOSE = "";
    this.INDEXOF_WRAPPER_OPEN = "";
    this.INDEXOF_WRAPPER_CLOSE = "";
    this.PARSEINT_WRAPPER_OPEN = "";
    this.PARSEINT_WRAPPER_CLOSE = "";
    this.DYNAMIC_CALL_OPEN = "";
    this.SPREAD_TOKEN = "...";
    this.INFER_VAR_TYPE = false;
    this.INFER_ARG_TYPE = false;
    this.SupportedKindNames = {};
    this.PostFixOperators = {};
    this.PrefixFixOperators = {};
    this.FunctionDefSupportedKindNames = {};
    this.LeftPropertyAccessReplacements = {};
    this.RightPropertyAccessReplacements = {};
    this.FullPropertyAccessReplacements = {};
    this.StringLiteralReplacements = {};
    this.CallExpressionReplacements = {};
    this.ReservedKeywordsReplacements = {};
    this.ReassignedVars = {};
    this.PropertyAccessRequiresParenthesisRemoval = [];
    this.VariableTypeReplacements = {};
    this.ArgTypeReplacements = {};
    this.FuncModifiers = {};
    this.defaultPropertyAccess = "public";
    this.currentClassName = "";
    this.className = "undefined";
    Object.assign(this, config["parser"] || {});
    this.id = "base";
    this.uncamelcaseIdentifiers = false;
    this.requiresReturnType = false;
    this.requiresParameterType = false;
    this.supportsFalsyOrTruthyValues = true;
    this.requiresCallExpressionCast = false;
    this.removeVariableDeclarationForFunctionExpression = true;
    this.includeFunctionNameInFunctionExpressionDeclaration = true;
    this.initOperators();
  }
  setContext(context) {
    this.context = context;
  }
  getSrc() {
    if (this.context === void 0)
      throw new Error(NO_CONTEXT_ERROR);
    return this.context.src;
  }
  getChecker() {
    if (this.context === void 0)
      throw new Error(NO_CONTEXT_ERROR);
    return this.context.checker;
  }
  // the checker when a transpilation context is set, undefined otherwise (an in-memory
  // program without one keeps every helper the checker would have proven away)
  // true when some node under `scope` (excluding `scope` itself) satisfies `predicate`;
  // the walk stops at the first hit and does not descend below it
  hasNodeWhere(scope, predicate) {
    if (scope === void 0) {
      return false;
    }
    let found = false;
    const visit = (n) => {
      if (found) {
        return;
      }
      if (predicate(n)) {
        found = true;
        return;
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return found;
  }
  checkerOrUndefined() {
    return _optionalChain([this, 'access', _ => _.context, 'optionalAccess', _2 => _2.checker]);
  }
  getProgram() {
    if (this.context === void 0)
      throw new Error(NO_CONTEXT_ERROR);
    return this.context.program;
  }
  initOperators() {
    this.SupportedKindNames = {
      [_typescript2.default.SyntaxKind.StringLiteral]: this.STRING_LITERAL_KEYWORD,
      [_typescript2.default.SyntaxKind.StringKeyword]: this.STRING_KEYWORD,
      // [ts.SyntaxKind.NumberKeyword]: this.NUMBER_KEYWORD,
      [_typescript2.default.SyntaxKind.NumberKeyword]: this.DEFAULT_TYPE,
      [_typescript2.default.SyntaxKind.MinusMinusToken]: this.MINUS_MINUS_TOKEN,
      [_typescript2.default.SyntaxKind.MinusToken]: this.MINUS_TOKEN,
      [_typescript2.default.SyntaxKind.SlashToken]: this.SLASH_TOKEN,
      [_typescript2.default.SyntaxKind.AsteriskToken]: this.ASTERISK_TOKEN,
      [_typescript2.default.SyntaxKind.InKeyword]: this.IN_TOKEN,
      [_typescript2.default.SyntaxKind.PlusToken]: this.PLUS_TOKEN,
      [_typescript2.default.SyntaxKind.PercentToken]: this.PERCENT_TOKEN,
      [_typescript2.default.SyntaxKind.LessThanToken]: this.LESS_THAN_TOKEN,
      [_typescript2.default.SyntaxKind.LessThanEqualsToken]: this.LESS_THAN_EQUALS_TOKEN,
      [_typescript2.default.SyntaxKind.GreaterThanToken]: this.GREATER_THAN_TOKEN,
      [_typescript2.default.SyntaxKind.GreaterThanEqualsToken]: this.GREATER_THAN_EQUALS_TOKEN,
      [_typescript2.default.SyntaxKind.EqualsEqualsToken]: this.EQUALS_EQUALS_TOKEN,
      [_typescript2.default.SyntaxKind.EqualsEqualsEqualsToken]: this.EQUALS_EQUALS_EQUALS_TOKEN,
      [_typescript2.default.SyntaxKind.EqualsToken]: this.EQUALS_TOKEN,
      [_typescript2.default.SyntaxKind.PlusEqualsToken]: this.PLUS_EQUALS,
      [_typescript2.default.SyntaxKind.BarBarToken]: this.BAR_BAR_TOKEN,
      [_typescript2.default.SyntaxKind.AmpersandAmpersandToken]: this.AMPERSTAND_APERSAND_TOKEN,
      [_typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken]: this.EXCLAMATION_EQUALS_EQUALS_TOKEN,
      [_typescript2.default.SyntaxKind.ExclamationEqualsToken]: this.EXCLAMATION_EQUALS_TOKEN,
      [_typescript2.default.SyntaxKind.AsyncKeyword]: this.ASYNC_TOKEN,
      [_typescript2.default.SyntaxKind.AwaitKeyword]: this.AWAIT_TOKEN,
      [_typescript2.default.SyntaxKind.StaticKeyword]: this.STATIC_TOKEN,
      [_typescript2.default.SyntaxKind.PublicKeyword]: this.PUBLIC_KEYWORD,
      [_typescript2.default.SyntaxKind.PrivateKeyword]: this.PRIVATE_KEYWORD,
      [_typescript2.default.SyntaxKind.VoidKeyword]: this.VOID_KEYWORD,
      [_typescript2.default.SyntaxKind.BooleanKeyword]: this.BOOLEAN_KEYWORD
    };
    this.PostFixOperators = {
      [_typescript2.default.SyntaxKind.PlusPlusToken]: this.PLUS_PLUS_TOKEN,
      [_typescript2.default.SyntaxKind.MinusMinusToken]: this.MINUS_MINUS_TOKEN
    };
    this.PrefixFixOperators = {
      [_typescript2.default.SyntaxKind.ExclamationToken]: this.NOT_TOKEN,
      [_typescript2.default.SyntaxKind.MinusToken]: this.MINUS_TOKEN
    };
    this.FunctionDefSupportedKindNames = {
      [_typescript2.default.SyntaxKind.StringKeyword]: this.STRING_KEYWORD
    };
    this.FuncModifiers = {
      [_typescript2.default.SyntaxKind.AsyncKeyword]: this.ASYNC_TOKEN,
      [_typescript2.default.SyntaxKind.PublicKeyword]: this.PUBLIC_KEYWORD,
      [_typescript2.default.SyntaxKind.PrivateKeyword]: this.PRIVATE_KEYWORD,
      [_typescript2.default.SyntaxKind.StaticKeyword]: this.STATIC_TOKEN
    };
  }
  capitalize(str) {
    return str[0].toUpperCase() + str.slice(1);
  }
  applyUserOverrides(config) {
    this.LeftPropertyAccessReplacements = Object.assign({}, this.LeftPropertyAccessReplacements, _nullishCoalesce(config["LeftPropertyAccessReplacements"], () => ( {})));
    this.RightPropertyAccessReplacements = Object.assign({}, this.RightPropertyAccessReplacements, _nullishCoalesce(config["RightPropertyAccessReplacements"], () => ( {})));
    this.FullPropertyAccessReplacements = Object.assign({}, this.FullPropertyAccessReplacements, _nullishCoalesce(config["FullPropertyAccessReplacements"], () => ( {})));
    this.CallExpressionReplacements = Object.assign({}, this.CallExpressionReplacements, _nullishCoalesce(config["CallExpressionReplacements"], () => ( {})));
    this.StringLiteralReplacements = Object.assign({}, this.StringLiteralReplacements, _nullishCoalesce(config["StringLiteralReplacements"], () => ( {})));
  }
  getLineAndCharacterOfNode(node) {
    const { line, character } = this.getSrc().getLineAndCharacterOfPosition(node.getStart());
    return [line + 1, character];
  }
  isComment(line) {
    line = line.trim();
    return line.startsWith("//") || line.startsWith("/*") || line.startsWith("*");
  }
  isStringType(flags) {
    return flags === _typescript2.default.TypeFlags.String || flags === _typescript2.default.TypeFlags.StringLiteral;
  }
  isAnyType(flags) {
    return flags === _typescript2.default.TypeFlags.Any;
  }
  warnIfAnyType(node, flags, variable, target) {
    if (this.isAnyType(flags)) {
      const [line, character] = this.getLineAndCharacterOfNode(node);
      Logger.warning(`[${this.id}] Line: ${line} char: ${character}: ${variable} has any type, ${target} might be incorrectly transpiled`);
    }
  }
  warn(node, target, message) {
    const [line, character] = this.getLineAndCharacterOfNode(node);
    Logger.warning(`[${this.id}] Line: ${line} char: ${character}: ${target} : ${message}`);
  }
  hasAsyncModifier(node) {
    return (_nullishCoalesce(node.modifiers, () => ( []))).some((mod) => mod.kind === _typescript2.default.SyntaxKind.AsyncKeyword);
  }
  isPromiseType(type) {
    return type !== void 0 && this.getTypeFromRawType(type) === this.PROMISE_TYPE_KEYWORD;
  }
  isImplicitAsyncFunction(node) {
    if (!this.implicitAsyncTranspiling || !_typescript2.default.isFunctionLike(node) || this.hasAsyncModifier(node)) {
      return false;
    }
    const signature = this.getChecker().getSignatureFromDeclaration(node);
    return signature !== void 0 && this.isPromiseType(this.getChecker().getReturnTypeOfSignature(signature));
  }
  isAsyncFunction(node) {
    return this.hasAsyncModifier(node) || this.isImplicitAsyncFunction(node);
  }
  getMethodOverride(node) {
    if (node === void 0) {
      return void 0;
    }
    if (!_typescript2.default.isClassDeclaration(node.parent)) {
      return void 0;
    }
    const classDeclaration = node.parent;
    if (!classDeclaration.heritageClauses) {
      return void 0;
    }
    let method = void 0;
    let parentClass = _typescript2.default.getAllSuperTypeNodes(node.parent)[0];
    while (parentClass !== void 0) {
      const parentClassType = this.getChecker().getTypeAtLocation(parentClass);
      const parentClassDecl = _optionalChain([parentClassType, 'optionalAccess', _3 => _3.symbol, 'optionalAccess', _4 => _4.valueDeclaration]);
      if (parentClassDecl === void 0) {
        this.warn(node, "Parent class", "Parent class not found");
        return void 0;
      }
      const parentClassMembers = _nullishCoalesce(parentClassDecl.members, () => ( []));
      parentClassMembers.forEach((elem) => {
        if (_typescript2.default.isMethodDeclaration(elem)) {
          const name = elem.name.getText().trim();
          if (node.name.escapedText === name) {
            method = elem;
          }
        }
      });
      parentClass = _nullishCoalesce(_typescript2.default.getAllSuperTypeNodes(parentClassDecl)[0], () => ( void 0));
    }
    return method;
  }
  getIden(num) {
    return this.DEFAULT_IDENTATION.repeat(parseInt(num));
  }
  getBlockOpen(identation) {
    return this.SPACE_BEFORE_BLOCK_OPENING + this.BLOCK_OPENING_TOKEN + "\n";
  }
  getBlockClose(identation, chainBlock = false) {
    if (chainBlock) {
      return this.BLOCK_CLOSING_TOKEN ? "\n" + this.getIden(identation) + this.BLOCK_CLOSING_TOKEN + this.SPACE_BEFORE_BLOCK_OPENING : "\n" + this.getIden(identation) + this.BLOCK_CLOSING_TOKEN;
    }
    return this.BLOCK_CLOSING_TOKEN ? "\n" + this.getIden(identation) + this.BLOCK_CLOSING_TOKEN : "";
  }
  startsWithUpperCase(str) {
    return str.charAt(0) === str.charAt(0).toUpperCase();
  }
  unCamelCaseIfNeeded(name) {
    if (this.uncamelcaseIdentifiers && !this.startsWithUpperCase(name)) {
      return _nullishCoalesce(unCamelCase(name), () => ( name));
    }
    return name;
  }
  transformIdentifier(node, identifier) {
    return this.unCamelCaseIfNeeded(identifier);
  }
  transformCallExpressionName(name, nameNode = void 0) {
    return name;
  }
  transformPropertyAccessExpressionName(name, nameNode = void 0) {
    return name;
  }
  printIdentifier(node) {
    let idValue = _nullishCoalesce(node.text, () => ( node.escapedText));
    if (this.ReservedKeywordsReplacements[idValue]) {
      idValue = this.ReservedKeywordsReplacements[idValue];
    }
    if (idValue === "undefined") {
      return this.UNDEFINED_TOKEN;
    }
    return this.transformIdentifier(node, idValue);
  }
  shouldRemoveParenthesisFromCallExpression(node) {
    if (node.expression.kind === _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      return this.PropertyAccessRequiresParenthesisRemoval.includes(node.expression.name.text);
    }
    return false;
  }
  printInstanceOfExpression(node, identation) {
    const left = node.left.escapedText;
    const right = node.right.escapedText;
    return this.getIden(identation) + `${left} instanceof ${right}`;
  }
  getCustomOperatorIfAny(left, right, operator) {
    return void 0;
  }
  printCustomBinaryExpressionIfAny(node, identation) {
    return void 0;
  }
  printBinaryExpression(node, identation) {
    const { left, right, operatorToken } = node;
    const customBinaryExp = this.printCustomBinaryExpressionIfAny(node, identation);
    if (customBinaryExp) {
      return customBinaryExp;
    }
    if (operatorToken.kind == _typescript2.default.SyntaxKind.InstanceOfKeyword) {
      return this.printInstanceOfExpression(node, identation);
    }
    let operator = this.SupportedKindNames[operatorToken.kind];
    let leftVar = void 0;
    let rightVar = void 0;
    if (operatorToken.kind === _typescript2.default.SyntaxKind.EqualsEqualsToken || operatorToken.kind === _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken) {
      if (this.COMPARISON_WRAPPER_OPEN) {
        leftVar = this.printNode(left, 0);
        rightVar = this.printNode(right, identation);
        return `${this.COMPARISON_WRAPPER_OPEN}${leftVar}, ${rightVar}${this.COMPARISON_WRAPPER_CLOSE}`;
      }
    }
    let prefixes = "";
    if (operatorToken.kind === _typescript2.default.SyntaxKind.BarBarToken || operatorToken.kind === _typescript2.default.SyntaxKind.AmpersandAmpersandToken) {
      leftVar = this.printCondition(left, 0);
      rightVar = this.printCondition(right, identation);
    } else {
      leftVar = this.printNode(left, 0);
      prefixes = _nullishCoalesce(this.getBinaryExpressionPrefixes(node, identation), () => ( ""));
      rightVar = this.printNode(right, identation);
    }
    const customOperator = this.getCustomOperatorIfAny(left, right, operatorToken);
    operator = customOperator ? customOperator : operator;
    return prefixes + leftVar + " " + operator + " " + rightVar.trim();
  }
  getBinaryExpressionPrefixes(node, identation) {
    return void 0;
  }
  transformPropertyAcessExpressionIfNeeded(node) {
    return void 0;
  }
  transformPropertyAcessRightIdentifierIfNeeded(name) {
    return this.unCamelCaseIfNeeded(name);
  }
  getExceptionalAccessTokenIfAny(node) {
    return void 0;
  }
  printLengthProperty(node, identation, name = void 0) {
    return void 0;
  }
  printPropertyAccessExpression(node, identation) {
    const expression = node.expression;
    const transformedProperty = this.transformPropertyAcessExpressionIfNeeded(node);
    if (transformedProperty) {
      return this.getIden(identation) + transformedProperty;
    }
    let leftSide = node.expression.escapedText;
    let rightSide = node.name.escapedText;
    switch (rightSide) {
      case "length":
        return this.printLengthProperty(node, identation, leftSide);
    }
    let rawExpression = node.getText().trim();
    if (this.FullPropertyAccessReplacements.hasOwnProperty(rawExpression)) {
      return this.FullPropertyAccessReplacements[rawExpression];
    }
    leftSide = this.LeftPropertyAccessReplacements.hasOwnProperty(leftSide) ? this.LeftPropertyAccessReplacements[leftSide] : this.printNode(expression, 0);
    rightSide = this.RightPropertyAccessReplacements.hasOwnProperty(rightSide) ? (
      // eslint-disable-line
      this.RightPropertyAccessReplacements[rightSide]
    ) : _nullishCoalesce(this.transformPropertyAcessRightIdentifierIfNeeded(rightSide), () => ( rightSide));
    const accessToken = _nullishCoalesce(this.getExceptionalAccessTokenIfAny(node), () => ( this.PROPERTY_ACCESS_TOKEN));
    rawExpression = leftSide + accessToken + this.transformPropertyAccessExpressionName(rightSide, node.name);
    return rawExpression;
  }
  printCustomDefaultValueIfNeeded(node) {
    return void 0;
  }
  printParameteCustomName(node, name, defaultValue = true) {
    const initializer = node.initializer;
    let type = this.printParameterType(node);
    type = type ? type + " " : "";
    if (defaultValue) {
      if (initializer) {
        const customDefaultValue = this.printCustomDefaultValueIfNeeded(initializer);
        const defaultValue2 = customDefaultValue ? customDefaultValue : this.printNode(initializer, 0);
        return type + name + this.SPACE_DEFAULT_PARAM + "=" + this.SPACE_DEFAULT_PARAM + defaultValue2;
      }
      return type + name;
    }
    return name;
  }
  printParameter(node, defaultValue = true) {
    const name = this.printNode(node.name, 0);
    const initializer = node.initializer;
    let type = this.printParameterType(node);
    type = type ? type + " " : "";
    if (defaultValue) {
      if (initializer) {
        const customDefaultValue = this.printCustomDefaultValueIfNeeded(initializer);
        const defaultValue2 = customDefaultValue ? customDefaultValue : this.printNode(initializer, 0);
        if (type) {
          type = defaultValue2 === "null" && type !== "object" ? type + "? " : type + " ";
        }
        return type + name + this.SPACE_DEFAULT_PARAM + "=" + this.SPACE_DEFAULT_PARAM + defaultValue2;
      }
      if (type === "") {
        return name;
      }
      return type + " " + name;
    }
    return name;
  }
  printModifiers(node) {
    let modifiers = _nullishCoalesce(node.modifiers, () => ( []));
    modifiers = modifiers.filter((mod) => this.FuncModifiers[mod.kind]);
    if (!this.asyncTranspiling) {
      modifiers = modifiers.filter((mod) => mod.kind !== _typescript2.default.SyntaxKind.AsyncKeyword);
    }
    let res = modifiers.map((modifier) => this.FuncModifiers[modifier.kind]).join(" ");
    if (this.asyncTranspiling && this.ASYNC_TOKEN && this.isImplicitAsyncFunction(node)) {
      res = res ? res + " " + this.ASYNC_TOKEN : this.ASYNC_TOKEN;
    }
    return res;
  }
  transformLeadingComment(comment) {
    return comment;
  }
  transformTrailingComment(comment) {
    return comment;
  }
  printLeadingComments(node, identation) {
    const fullText = this.getSrc().getFullText();
    const commentsRangeList = _typescript2.default.getLeadingCommentRanges(fullText, node.pos);
    const commentsRange = commentsRangeList ? commentsRangeList : void 0;
    let res = "";
    if (commentsRange) {
      for (const commentRange of commentsRange) {
        const commentText = fullText.slice(commentRange.pos, commentRange.end);
        if (commentText !== void 0) {
          const formatted = commentText.split("\n").map((line) => line.trim()).map((line) => !line.trim().startsWith("*") ? this.getIden(identation) + line : this.getIden(identation) + " " + line).join("\n");
          res += this.transformLeadingComment(formatted) + "\n";
        }
      }
    }
    return res;
  }
  printTraillingComment(node, identation) {
    const fullText = this.getSrc().getFullText();
    const commentsRangeList = _typescript2.default.getTrailingCommentRanges(fullText, node.end);
    const commentsRange = commentsRangeList ? commentsRangeList : void 0;
    let res = "";
    if (commentsRange) {
      for (const commentRange of commentsRange) {
        const commentText = fullText.slice(commentRange.pos, commentRange.end);
        if (commentText !== void 0) {
          res += " " + this.transformTrailingComment(commentText);
        }
      }
    }
    return res;
  }
  printNodeCommentsIfAny(node, identation, parsedNode) {
    const leadingComment = this.printLeadingComments(node, identation);
    const trailingComment = this.printTraillingComment(node, identation);
    return leadingComment + parsedNode + trailingComment;
  }
  getType(node) {
    const type = node.type;
    if (type) {
      if (type.kind === _typescript2.default.SyntaxKind.TypeReference) {
        const typeRef = type.typeName.escapedText;
        if (typeRef === "Promise") {
          const typeArgs = type.typeArguments.filter((t) => t.kind !== _typescript2.default.SyntaxKind.VoidKeyword);
          const insideTypes = typeArgs.map((type2) => {
            if (this.SupportedKindNames.hasOwnProperty(type2.kind)) {
              return this.SupportedKindNames[type2.kind];
            } else {
              return type2.escapedText;
            }
          }).join(",");
          if (insideTypes.length > 0) {
            return `${this.PROMISE_TYPE_KEYWORD}<${insideTypes}>`;
          }
          return this.PROMISE_TYPE_KEYWORD;
        }
        return type.typeName.escapedText;
      } else if (this.SupportedKindNames.hasOwnProperty(type.kind)) {
        return this.SupportedKindNames[type.kind];
      }
    }
    const initializer = node.initializer;
    if (initializer) {
      if (_typescript2.default.isArrayLiteralExpression(initializer)) {
        return this.ARRAY_KEYWORD;
      }
      if (_typescript2.default.isBooleanLiteral(initializer)) {
        return this.BOOLEAN_KEYWORD;
      }
      if (_typescript2.default.isObjectLiteralExpression(initializer)) {
        return this.OBJECT_KEYWORD;
      }
      if (_typescript2.default.isNumericLiteral(initializer)) {
        return this.DEFAULT_TYPE;
      }
      if (_typescript2.default.isStringLiteralLike(initializer)) {
        return this.STRING_KEYWORD;
      }
    }
    return void 0;
  }
  getTypeFromRawType(type) {
    if (type.flags === _typescript2.default.TypeFlags.Any) {
      return void 0;
    }
    if (type.flags === _typescript2.default.TypeFlags.Void) {
      return this.VOID_KEYWORD;
    }
    if (type.flags === _typescript2.default.TypeFlags.Number) {
      return this.DEFAULT_TYPE;
    }
    if (type.flags === _typescript2.default.TypeFlags.String) {
      return this.STRING_KEYWORD;
    }
    if (_optionalChain([type, 'optionalAccess', _5 => _5.symbol, 'optionalAccess', _6 => _6.escapedName]) === "Array") {
      return this.ARRAY_KEYWORD;
    }
    if (_optionalChain([type, 'optionalAccess', _7 => _7.symbol, 'optionalAccess', _8 => _8.escapedName]) === "__object") {
      return this.OBJECT_KEYWORD;
    }
    if (_optionalChain([type, 'optionalAccess', _9 => _9.symbol, 'optionalAccess', _10 => _10.escapedName]) === "__type") {
      return this.OBJECT_KEYWORD;
    }
    if (_optionalChain([type, 'optionalAccess', _11 => _11.symbol, 'optionalAccess', _12 => _12.escapedName]) === "Promise") {
      return this.PROMISE_TYPE_KEYWORD;
    }
    if (_optionalChain([type, 'optionalAccess', _13 => _13.intrinsicName]) === "object") {
      return this.OBJECT_KEYWORD;
    }
    if (_optionalChain([type, 'optionalAccess', _14 => _14.intrinsicName]) === "boolean") {
      return this.BOOLEAN_KEYWORD;
    }
    return void 0;
  }
  getFunctionType(node, async = true) {
    const type = this.getChecker().getReturnTypeOfSignature(this.getChecker().getSignatureFromDeclaration(node));
    const parsedTtype = this.getTypeFromRawType(type);
    if (parsedTtype === this.PROMISE_TYPE_KEYWORD) {
      const resolvedTypeArguments = type.resolvedTypeArguments;
      if (resolvedTypeArguments.length === 0) {
        return this.PROMISE_TYPE_KEYWORD;
      }
      if (resolvedTypeArguments.length === 1 && resolvedTypeArguments[0].flags === _typescript2.default.TypeFlags.Void) {
        return this.PROMISE_TYPE_KEYWORD;
      }
      const insideTypes = resolvedTypeArguments.map((type2) => this.getTypeFromRawType(type2)).join(",");
      if (insideTypes.length > 0) {
        if (async) {
          return `${this.PROMISE_TYPE_KEYWORD}<${insideTypes}>`;
        } else {
          return insideTypes;
        }
      }
      return void 0;
    }
    return parsedTtype;
  }
  printFunctionBody(node, identation) {
    return this.printBlock(node.body, identation);
  }
  printParameterType(node) {
    if (!this.requiresParameterType) {
      return "";
    }
    if (!this.INFER_ARG_TYPE) {
      return this.DEFAULT_PARAMETER_TYPE;
    }
    const type = this.getChecker().typeToString(this.getChecker().getTypeAtLocation(node));
    if (this.ArgTypeReplacements[type]) {
      return this.ArgTypeReplacements[type];
    }
    return this.DEFAULT_PARAMETER_TYPE;
  }
  printFunctionType(node) {
    if (!this.requiresReturnType) {
      return "";
    }
    const typeText = this.getFunctionType(node);
    if (typeText === void 0 || typeText !== this.VOID_KEYWORD && typeText !== this.PROMISE_TYPE_KEYWORD) {
      let res = "";
      if (this.isAsyncFunction(node)) {
        res = `${this.PROMISE_TYPE_KEYWORD}<${this.DEFAULT_RETURN_TYPE}>`;
      } else {
        res = this.DEFAULT_RETURN_TYPE;
      }
      this.warn(node, node.name.getText(), "Function return type not found, will default to: " + res);
      return res;
    }
    return typeText;
  }
  printFunctionDefinition(node, identation) {
    let name = _nullishCoalesce(_optionalChain([node, 'access', _15 => _15.name, 'optionalAccess', _16 => _16.escapedText]), () => ( ""));
    name = this.transformFunctionNameIfNeeded(name);
    const parsedArgs = this.printMethodParameters(node);
    let modifiers = this.printModifiers(node);
    modifiers = modifiers ? modifiers + " " : modifiers;
    let returnType = this.printFunctionType(node);
    if (returnType === "java.util.concurrent.CompletableFuture") {
      returnType = "java.util.concurrent.CompletableFuture<Void>";
    }
    returnType = returnType ? returnType + " " : returnType;
    const fnKeyword = this.FUNCTION_TOKEN ? this.FUNCTION_TOKEN + " " : "";
    if (!fnKeyword && _typescript2.default.isFunctionDeclaration(node)) {
      modifiers = modifiers + "public ";
    }
    let functionDef = this.getIden(identation) + modifiers + returnType + fnKeyword;
    if (this.includeFunctionNameInFunctionExpressionDeclaration || !_typescript2.default.isFunctionExpression(node)) {
      functionDef += name;
    }
    functionDef += "(" + parsedArgs + ")";
    return functionDef;
  }
  transformFunctionNameIfNeeded(name) {
    return this.unCamelCaseIfNeeded(name);
  }
  printFunctionDeclaration(node, identation) {
    if (_typescript2.default.isArrowFunction(node)) {
      const parameters = node.parameters.map((param) => this.printParameter(param)).join(", ");
      const body = this.printNode(node.body);
      return `(${parameters}) => ${body}`;
    }
    const funcBody = this.printFunctionBody(node, identation);
    let functionDef = this.printFunctionDefinition(node, identation);
    functionDef += funcBody;
    return this.printNodeCommentsIfAny(node, identation, functionDef);
  }
  printMethodParameters(node) {
    return node.parameters.map((param) => this.printParameter(param)).join(", ");
  }
  transformMethodNameIfNeeded(name) {
    return this.unCamelCaseIfNeeded(name);
  }
  printMethodDefinition(node, identation) {
    let name = node.name.escapedText;
    name = this.transformMethodNameIfNeeded(name);
    let returnType = this.printFunctionType(node);
    let modifiers = this.printModifiers(node);
    const defaultAccess = this.METHOD_DEFAULT_ACCESS ? this.METHOD_DEFAULT_ACCESS + " " : "";
    modifiers = modifiers ? modifiers + " " : defaultAccess;
    const parsedArgs = this.printMethodParameters(node);
    returnType = returnType ? returnType + " " : returnType;
    const methodToken = this.METHOD_TOKEN ? this.METHOD_TOKEN + " " : "";
    const methodDef = this.getIden(identation) + modifiers + returnType + methodToken + name + "(" + parsedArgs + ")";
    return this.printNodeCommentsIfAny(node, identation, methodDef);
  }
  printMethodDeclaration(node, identation) {
    let methodDef = this.printMethodDefinition(node, identation);
    const funcBody = this.printFunctionBody(node, identation);
    methodDef += funcBody;
    return methodDef;
  }
  printStringLiteral(node) {
    const token = this.STRING_QUOTE_TOKEN;
    let text = node.text;
    if (text in this.StringLiteralReplacements) {
      return this.StringLiteralReplacements[text];
    }
    const backslashPlaceholder = "\0";
    text = text.replaceAll("\\", backslashPlaceholder);
    text = text.replaceAll("\b", "\\b");
    text = text.replaceAll("\f", "\\f");
    text = text.replaceAll("\n", "\\n");
    text = text.replaceAll("\r", "\\r");
    text = text.replaceAll("	", "\\t");
    text = text.replaceAll(backslashPlaceholder, "\\\\");
    if (token === "'") {
      text = text.replaceAll("'", "\\'");
    } else if (token === '"') {
      text = text.replaceAll('"', '\\"');
    }
    return token + text + token;
  }
  printNumericLiteral(node) {
    return node.text;
  }
  printArrayLiteralExpression(node, identation) {
    const elements = node.elements.map((e) => {
      return this.printNode(e);
    }).join(", ");
    return this.ARRAY_OPENING_TOKEN + elements + this.ARRAY_CLOSING_TOKEN;
  }
  printVariableDeclarationList(node, identation) {
    const declaration = node.declarations[0];
    const varToken = this.VAR_TOKEN ? this.VAR_TOKEN + " " : "";
    if (this.removeVariableDeclarationForFunctionExpression && _optionalChain([declaration, 'optionalAccess', _17 => _17.initializer]) && (_typescript2.default.isFunctionExpression(declaration.initializer) || _typescript2.default.isArrowFunction(declaration.initializer))) {
      return this.printNode(declaration.initializer, identation).trimEnd();
    }
    const parsedValue = declaration.initializer ? this.printNode(declaration.initializer, identation) : this.NULL_TOKEN;
    return this.getIden(identation) + varToken + this.printNode(declaration.name) + " = " + parsedValue.trim();
  }
  printVariableStatement(node, identation) {
    if (this.isCJSRequireStatement(node)) {
      return "";
    }
    const decList = node.declarationList;
    const varStatement = this.printVariableDeclarationList(decList, identation) + this.LINE_TERMINATOR;
    return this.printNodeCommentsIfAny(node, identation, varStatement);
  }
  printOutOfOrderCallExpressionIfAny(node, identation) {
    return void 0;
  }
  printSuperCallInsideConstructor(node, identation) {
    const args = node.arguments;
    const parsedArgs = args.map((a) => {
      return this.printNode(a, identation).trim();
    }).join(",");
    return this.SUPER_CALL_TOKEN + "(" + parsedArgs + ")";
  }
  isBuiltInFunctionCall(node) {
    const symbol = this.getChecker().getSymbolAtLocation(node);
    const isInLibFiles = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _18 => _18.getDeclarations, 'call', _19 => _19(), 'optionalAccess', _20 => _20.some, 'call', _21 => _21((s) => s.getSourceFile().fileName.includes("/node_modules/typescript/lib/"))]), () => ( false));
    return isInLibFiles;
  }
  getTypesFromCallExpressionParameters(node) {
    const resolvedParams = this.getChecker().getResolvedSignature(node).parameters;
    const parsedTypes = [];
    resolvedParams.forEach((p) => {
      const decl = p.declarations[0];
      const type = this.getChecker().getTypeAtLocation(decl);
      const parsedType = this.getTypeFromRawType(type);
      parsedTypes.push(parsedType);
    });
    return parsedTypes;
  }
  printArgsForCallExpression(node, identation) {
    const args = node.arguments;
    const argsList = args.length > 0 ? args : [];
    const parsedArgs = argsList.map((a) => {
      return this.printNode(a, identation).trim();
    }).join(", ");
    return parsedArgs;
  }
  // builtin functions override
  printArrayIsArrayCall(node, identation, parsedArg = void 0) {
    return void 0;
  }
  printObjectKeysCall(node, identation, parsedArg = void 0) {
    return void 0;
  }
  printObjectValuesCall(node, identation, parsedArg = void 0) {
    return void 0;
  }
  printJsonParseCall(node, identation, parsedArg = void 0) {
    return void 0;
  }
  printJsonStringifyCall(node, identation, parsedArg = void 0) {
    return void 0;
  }
  printPromiseAllCall(node, identation, parsedArg = void 0) {
    return void 0;
  }
  printMathFloorCall(node, identation, parsedArg = void 0) {
    return void 0;
  }
  printMathRoundCall(node, identation, parsedArg = void 0) {
    return void 0;
  }
  printMathCeilCall(node, identation, parsedArg = void 0) {
    return void 0;
  }
  printNumberIsIntegerCall(node, identation, parsedArg = void 0) {
    return void 0;
  }
  printArrayPushCall(node, identation, name = void 0, parsedArg = void 0) {
    return void 0;
  }
  printIncludesCall(node, identation, name = void 0, parsedArg = void 0) {
    return void 0;
  }
  printIndexOfCall(node, identation, name = void 0, parsedArg = void 0) {
    return void 0;
  }
  printStartsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    return void 0;
  }
  printEndsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    return void 0;
  }
  printPadEndCall(node, identation, name, parsedArg, parsedArg2) {
    return void 0;
  }
  printPadStartCall(node, identation, name, parsedArg, parsedArg2) {
    return void 0;
  }
  printTrimCall(node, identation, name = void 0) {
    return void 0;
  }
  printJoinCall(node, identation, name = void 0, parsedArg = void 0) {
    return void 0;
  }
  printSplitCall(node, identation, name = void 0, parsedArg = void 0) {
    return void 0;
  }
  printConcatCall(node, identation, name = void 0, parsedArg = void 0) {
    return void 0;
  }
  printToFixedCall(node, identation, name = void 0, parsedArg = void 0) {
    return void 0;
  }
  printSearchCall(node, identation, name = void 0, parsedArg = void 0) {
    return void 0;
  }
  printSliceCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    return void 0;
  }
  printReplaceCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    return void 0;
  }
  printReplaceAllCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    return void 0;
  }
  printToStringCall(node, identation, name = void 0) {
    return void 0;
  }
  printToUpperCaseCall(node, identation, name = void 0) {
    return void 0;
  }
  printToLowerCaseCall(node, identation, name = void 0) {
    return void 0;
  }
  printShiftCall(node, identation, name = void 0) {
    return void 0;
  }
  printReverseCall(node, identation, name = void 0) {
    return void 0;
  }
  printPopCall(node, identation, name = void 0) {
    return void 0;
  }
  printAssertCall(node, identation, parsedArgs) {
    return `assert(${parsedArgs})`;
  }
  printDateNowCall(node, identation) {
    return void 0;
  }
  printCallExpression(node, identation) {
    const expression = node.expression;
    const parsedArgs = this.printArgsForCallExpression(node, identation);
    const removeParenthesis = this.shouldRemoveParenthesisFromCallExpression(node);
    const finalExpression = this.printOutOfOrderCallExpressionIfAny(node, identation);
    if (finalExpression) {
      return finalExpression;
    }
    if (node.expression.kind === _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      const expressionText = node.expression.getText().trim();
      const args = _nullishCoalesce(node.arguments, () => ( []));
      if (args.length === 0) {
        switch (expressionText) {
          case "Date.now":
            return this.printDateNowCall(node, identation);
        }
      }
      if (args.length === 1) {
        const parsedArg = this.printNode(args[0], 0);
        switch (expressionText) {
          case "JSON.parse":
            return this.printJsonParseCall(node, identation, parsedArg);
          case "JSON.stringify":
            return this.printJsonStringifyCall(node, identation, parsedArg);
          case "Array.isArray":
            return this.printArrayIsArrayCall(node, identation, parsedArg);
          case "Object.keys":
            return this.printObjectKeysCall(node, identation, parsedArg);
          case "Object.values":
            return this.printObjectValuesCall(node, identation, parsedArg);
          case "Promise.all":
            return this.printPromiseAllCall(node, identation, parsedArg);
          case "Math.round":
            return this.printMathRoundCall(node, identation, parsedArg);
          case "Math.floor":
            return this.printMathFloorCall(node, identation, parsedArg);
          case "Math.ceil":
            return this.printMathCeilCall(node, identation, parsedArg);
          case "Number.isInteger":
            return this.printNumberIsIntegerCall(node, identation, parsedArg);
        }
      }
      const rightSide = _optionalChain([node, 'access', _22 => _22.expression, 'access', _23 => _23.name, 'optionalAccess', _24 => _24.escapedText]);
      const leftSide = _optionalChain([node, 'access', _25 => _25.expression, 'optionalAccess', _26 => _26.expression]);
      if (args.length === 0 && rightSide !== void 0 && leftSide !== void 0) {
        const parsedLeftSide = this.printNode(leftSide, 0);
        switch (rightSide) {
          case "toString":
            return this.printToStringCall(node, identation, parsedLeftSide);
          case "toUpperCase":
            return this.printToUpperCaseCall(node, identation, parsedLeftSide);
          case "toLowerCase":
            return this.printToLowerCaseCall(node, identation, parsedLeftSide);
          case "shift":
            return this.printShiftCall(node, identation, parsedLeftSide);
          case "pop":
            return this.printPopCall(node, identation, parsedLeftSide);
          case "reverse":
            return this.printReverseCall(node, identation, parsedLeftSide);
          case "trim":
            return this.printTrimCall(node, identation, parsedLeftSide);
        }
      }
      const arg = args && args.length > 0 ? args[0] : void 0;
      if (leftSide && rightSide && arg) {
        const parsedArg = this.printNode(arg, identation).trimStart();
        const secondParsedArg = args[1] ? this.printNode(args[1], identation).trimStart() : void 0;
        const name = this.printNode(leftSide, 0);
        switch (rightSide) {
          case "push":
            return this.printArrayPushCall(node, identation, name, parsedArg);
          case "includes":
            return this.printIncludesCall(node, identation, name, parsedArg);
          case "indexOf":
            return this.printIndexOfCall(node, identation, name, parsedArg);
          case "join":
            return this.printJoinCall(node, identation, name, parsedArg);
          case "split":
            return this.printSplitCall(node, identation, name, parsedArg);
          case "toFixed":
            return this.printToFixedCall(node, identation, name, parsedArg);
          case "concat":
            return this.printConcatCall(node, identation, name, parsedArg);
          case "search":
            return this.printSearchCall(node, identation, name, parsedArg);
          case "endsWith":
            return this.printEndsWithCall(node, identation, name, parsedArg);
          case "startsWith":
            return this.printStartsWithCall(node, identation, name, parsedArg);
          case "padEnd":
            return this.printPadEndCall(node, identation, name, parsedArg, secondParsedArg);
          case "padStart":
            return this.printPadStartCall(node, identation, name, parsedArg, secondParsedArg);
        }
        if (args.length === 1 || args.length === 2) {
          const parsedArg2 = args[1] ? this.printNode(args[1], identation).trimStart() : void 0;
          switch (rightSide) {
            case "slice":
              return this.printSliceCall(node, identation, name, parsedArg, parsedArg2);
            case "replace":
              return this.printReplaceCall(node, identation, name, parsedArg, parsedArg2);
            case "replaceAll":
              return this.printReplaceAllCall(node, identation, name, parsedArg, parsedArg2);
          }
        }
      }
    } else {
      const args = _nullishCoalesce(node.arguments, () => ( []));
      if (args.length === 2) {
        if (expression.escapedText === "assert") {
          return this.printAssertCall(node, identation, parsedArgs);
        }
        if (expression.escapedText === "padEnd") {
        }
      }
    }
    if (expression.kind === _typescript2.default.SyntaxKind.SuperKeyword) {
      return this.printSuperCallInsideConstructor(node, identation);
    }
    let parsedExpression = void 0;
    if (this.CallExpressionReplacements.hasOwnProperty(expression.getText())) {
      parsedExpression = this.CallExpressionReplacements[expression.getText()];
    } else {
      if (expression.kind === _typescript2.default.SyntaxKind.Identifier) {
        const idValue = _nullishCoalesce(expression.text, () => ( expression.escapedText));
        parsedExpression = this.transformCallExpressionName(this.unCamelCaseIfNeeded(idValue), expression);
      } else {
        parsedExpression = this.printNode(expression, 0);
      }
    }
    let parsedCall = parsedExpression;
    if (!removeParenthesis) {
      parsedCall += "(" + parsedArgs + ")";
    }
    return parsedCall;
  }
  printClassBody(node, identation) {
    const parsedMembers = [];
    node.members.forEach((m, index) => {
      const parsedNode = this.printNode(m, identation + 1);
      if (m.kind === _typescript2.default.SyntaxKind.PropertyDeclaration || index === 0) {
        parsedMembers.push(parsedNode);
      } else {
        parsedMembers.push("\n".repeat(this.NUM_LINES_BETWEEN_CLASS_MEMBERS) + parsedNode);
      }
    });
    return parsedMembers.join("\n");
  }
  getCustomClassName(node) {
    return node.name.escapedText;
  }
  getClassModifier(node) {
    return "";
  }
  printClassDefinition(node, identation) {
    const className = this.getCustomClassName(node);
    this.currentClassName = className;
    const heritageClauses = node.heritageClauses;
    const classModifier = this.getClassModifier(node);
    let classInit = "";
    const classOpening = this.getBlockOpen(identation);
    if (heritageClauses !== void 0) {
      const classExtends = heritageClauses[0].types[0].expression.escapedText;
      classInit = this.getIden(identation) + classModifier + "class " + className + " " + this.EXTENDS_TOKEN + " " + classExtends + classOpening;
    } else {
      classInit = this.getIden(identation) + classModifier + "class " + className + classOpening;
    }
    return classInit;
  }
  printClass(node, identation) {
    const classDefinition = this.printClassDefinition(node, identation);
    const classBody = this.printClassBody(node, identation);
    const classClosing = this.getBlockClose(identation);
    return classDefinition + classBody + classClosing;
  }
  printConstructorDeclaration(node, identation) {
    const args = this.printMethodParameters(node);
    const constructorBody = this.printFunctionBody(node, identation);
    return this.getIden(identation) + this.CONSTRUCTOR_TOKEN + "(" + args + ")" + constructorBody;
  }
  printWhileStatement(node, identation) {
    const loopExpression = node.expression;
    const expression = this.printNode(loopExpression, 0);
    const whileStm = this.getIden(identation) + this.WHILE_TOKEN + " " + this.CONDITION_OPENING + expression + this.CONDITION_CLOSE + this.printBlock(node.statement, identation);
    return this.printNodeCommentsIfAny(node, identation, whileStm);
  }
  printForStatement(node, identation) {
    const initializer = this.printNode(node.initializer, 0);
    const condition = this.printNode(node.condition, 0);
    const incrementor = this.printNode(node.incrementor, 0);
    const forStm = this.getIden(identation) + this.FOR_TOKEN + " " + this.CONDITION_OPENING + initializer + "; " + condition + "; " + incrementor + this.CONDITION_CLOSE + this.printBlock(node.statement, identation);
    return this.printNodeCommentsIfAny(node, identation, forStm);
  }
  printBreakStatement(node, identation) {
    const breakStm = this.getIden(identation) + this.BREAK_TOKEN + this.LINE_TERMINATOR;
    return this.printNodeCommentsIfAny(node, identation, breakStm);
  }
  printPostFixUnaryExpression(node, identation) {
    const { operand, operator } = node;
    return this.getIden(identation) + this.printNode(operand, 0) + this.PostFixOperators[operator];
  }
  printPrefixUnaryExpression(node, identation) {
    const { operand, operator } = node;
    if (operator === _typescript2.default.SyntaxKind.ExclamationToken) {
      return this.getIden(identation) + this.PrefixFixOperators[operator] + this.printCondition(node.operand, 0);
    }
    return this.getIden(identation) + this.PrefixFixOperators[operator] + this.printNode(operand, 0);
  }
  printObjectLiteralBody(node, identation) {
    let body = node.properties.map((p) => this.printNode(p, identation + 1)).join(",\n");
    body = body ? body + "," : body;
    return body;
  }
  printObjectLiteralExpression(node, identation) {
    const objectBody = this.printObjectLiteralBody(node, identation);
    const formattedObjectBody = objectBody ? "\n" + objectBody + "\n" + this.getIden(identation) : objectBody;
    return this.OBJECT_OPENING + formattedObjectBody + this.OBJECT_CLOSING;
  }
  printCustomRightSidePropertyAssignment(node, identation) {
    return void 0;
  }
  printPropertyAssignment(node, identation) {
    const { name, initializer } = node;
    const nameAsString = this.printNode(name, 0);
    const customRightSide = this.printCustomRightSidePropertyAssignment(initializer, identation);
    const valueAsString = customRightSide ? customRightSide : this.printNode(initializer, identation);
    let trailingComment = this.printTraillingComment(node, identation);
    trailingComment = trailingComment ? " " + trailingComment : trailingComment;
    const propOpen = this.PROPERTY_ASSIGNMENT_OPEN ? this.PROPERTY_ASSIGNMENT_OPEN + " " : "";
    const propClose = this.PROPERTY_ASSIGNMENT_CLOSE ? " " + this.PROPERTY_ASSIGNMENT_CLOSE : "";
    return this.getIden(identation) + propOpen + nameAsString + this.PROPERTY_ASSIGNMENT_TOKEN + " " + valueAsString.trim() + propClose + trailingComment;
  }
  printElementAccessExpressionExceptionIfAny(node) {
    return void 0;
  }
  // index WRITE (`x["k"] = v`) whose receiver the C# classifier already declared as a
  // concrete dictionary: the `((IDictionary<string,object>)x)` interface cast the C#
  // branch below emits only exists because the TS type says nothing about the printed
  // declaration, so a printer that knows the declared type can drop it. undefined/false
  // keeps the upstream cast (the untyped emission). See csharpTranspiler.ts.
  csharpDictionaryIndexWriteNeedsNoCast(node) {
    return void 0;
  }
  printElementAccessExpression(node, identation) {
    const { expression, argumentExpression } = node;
    const exception = this.printElementAccessExpressionExceptionIfAny(node);
    if (exception) {
      return exception;
    }
    const isLeftSideOfAssignment = _optionalChain([node, 'access', _27 => _27.parent, 'optionalAccess', _28 => _28.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && (node.parent.operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken || node.parent.operatorToken.kind === _typescript2.default.SyntaxKind.PlusEqualsToken) && _optionalChain([node, 'access', _29 => _29.parent, 'optionalAccess', _30 => _30.left]) === node;
    const expressionAsString = this.printNode(expression, 0);
    const argumentAsString = this.printNode(argumentExpression, 0);
    if (!isLeftSideOfAssignment && this.ELEMENT_ACCESS_WRAPPER_OPEN && this.ELEMENT_ACCESS_WRAPPER_CLOSE) {
      return `${this.ELEMENT_ACCESS_WRAPPER_OPEN}${expressionAsString}, ${argumentAsString}${this.ELEMENT_ACCESS_WRAPPER_CLOSE}`;
    }
    if (isLeftSideOfAssignment && this.ELEMENT_ACCESS_WRAPPER_OPEN && this.ELEMENT_ACCESS_WRAPPER_CLOSE) {
      const type = this.getChecker().getTypeAtLocation(argumentExpression);
      const isString = this.isStringType(type.flags);
      let isUnionString = false;
      if (type.flags === _typescript2.default.TypeFlags.Union) {
        isUnionString = this.isStringType(_optionalChain([type, 'optionalAccess', _31 => _31.types, 'access', _32 => _32[0], 'access', _33 => _33.flags]));
      }
      if (isString || isUnionString || type.flags === _typescript2.default.TypeFlags.Any) {
        if (this.id === "C#") {
          const cast = _typescript2.default.isStringLiteralLike(argumentExpression) ? "" : "(string)";
          if (this.csharpDictionaryIndexWriteNeedsNoCast(node)) {
            return `${expressionAsString}[${cast}${argumentAsString}]`;
          }
          return `((IDictionary<string,object>)${expressionAsString})[${cast}${argumentAsString}]`;
        } else if (this.id === "Java") {
          return `((java.util.HashMap<String, Object>)${expressionAsString}).get(${argumentAsString})`;
        }
      }
      if (this.id === "C#") {
        return `((${this.ARRAY_KEYWORD})${expressionAsString})[Convert.ToInt32(${argumentAsString})]`;
      } else if (this.id === "Java") {
        return `((java.util.List<Object>)${expressionAsString}).get(Helpers.toInt(${argumentAsString}))`;
      }
    }
    return expressionAsString + "[" + argumentAsString + "]";
  }
  printCondition(node, identation) {
    if (this.supportsFalsyOrTruthyValues) {
      return this.printNode(node, identation);
    }
    if (node.kind === _typescript2.default.SyntaxKind.PrefixUnaryExpression && node.operator === _typescript2.default.SyntaxKind.ExclamationToken) {
      return this.printPrefixUnaryExpression(node, identation);
    }
    const expression = `${this.FALSY_WRAPPER_OPEN}${this.printNode(node, 0)}${this.FALSY_WRAPPER_CLOSE}`;
    return `${this.getIden(identation)}${expression}`;
  }
  printIfStatement(node, identation) {
    const expression = this.printCondition(node.expression, 0);
    const elseExists = node.elseStatement !== void 0;
    const isElseIf = node.parent.kind === _typescript2.default.SyntaxKind.IfStatement;
    const needChainBlock = elseExists;
    const ifBody = this.printBlock(node.thenStatement, identation, needChainBlock);
    let ifComplete = this.CONDITION_OPENING + expression + this.CONDITION_CLOSE + ifBody;
    if (isElseIf) {
      ifComplete = this.ELSEIF_TOKEN + " " + ifComplete;
    } else {
      ifComplete = this.getIden(identation) + this.IF_TOKEN + " " + ifComplete;
    }
    const elseStatement = node.elseStatement;
    if (_optionalChain([elseStatement, 'optionalAccess', _34 => _34.kind]) === _typescript2.default.SyntaxKind.Block) {
      const elseBody = this.printBlock(elseStatement, identation);
      const elseBlock = this.ELSE_TOKEN + elseBody;
      ifComplete += elseBlock;
    } else if (_optionalChain([elseStatement, 'optionalAccess', _35 => _35.kind]) === _typescript2.default.SyntaxKind.IfStatement) {
      const elseBody = this.printIfStatement(elseStatement, identation);
      ifComplete += elseBody;
    }
    return this.printNodeCommentsIfAny(node, identation, ifComplete);
  }
  printParenthesizedExpression(node, identation) {
    if (node.expression.kind === _typescript2.default.SyntaxKind.AsExpression) {
      return this.getIden(identation) + this.printNode(node.expression, 0);
    }
    if (node.expression.kind === _typescript2.default.SyntaxKind.ArrowFunction) {
      return "";
    }
    return this.getIden(identation) + this.LEFT_PARENTHESIS + this.printNode(node.expression, 0) + this.RIGHT_PARENTHESIS;
  }
  printBooleanLiteral(node) {
    if (_typescript2.default.SyntaxKind.TrueKeyword === node.kind) {
      return this.TRUE_KEYWORD;
    }
    return this.FALSE_KEYWORD;
  }
  printTryStatement(node, identation) {
    const tryBody = this.printBlock(node.tryBlock, identation, true);
    const catchBody = this.printBlock(node.catchClause.block, identation);
    const catchDeclaration = this.CATCH_DECLARATION + " " + this.printNode(node.catchClause.variableDeclaration.name, 0);
    const catchCondOpen = this.CONDITION_OPENING ? this.CONDITION_OPENING : " ";
    return this.getIden(identation) + this.TRY_TOKEN + tryBody + this.CATCH_TOKEN + catchCondOpen + catchDeclaration + this.CONDITION_CLOSE + catchBody;
  }
  printNewExpression(node, identation) {
    let expression = _optionalChain([node, 'access', _36 => _36.expression, 'optionalAccess', _37 => _37.escapedText]);
    expression = expression ? expression : this.printNode(node.expression);
    const args = node.arguments.map((n) => this.printNode(n, identation)).join(", ");
    const newToken = this.NEW_TOKEN ? this.NEW_TOKEN + " " : "";
    return newToken + expression + this.LEFT_PARENTHESIS + args + this.RIGHT_PARENTHESIS;
  }
  printThrowStatement(node, identation) {
    const expression = this.printNode(node.expression, 0);
    return this.getIden(identation) + this.THROW_TOKEN + " " + expression + this.LINE_TERMINATOR;
  }
  printAwaitExpression(node, identation) {
    const expression = this.printNode(node.expression, identation);
    const awaitToken = this.asyncTranspiling ? this.AWAIT_TOKEN + " " : "";
    return awaitToken + expression;
  }
  wrapSyntheticNode(synthetic, original) {
    synthetic.pos = original.pos;
    synthetic.end = original.end;
    synthetic.parent = original.parent;
    original.parent = synthetic;
    return synthetic;
  }
  wrapImplicitReturnAwait(node) {
    let exp = node.expression;
    if (!exp || _typescript2.default.isAwaitExpression(exp) || !this.isImplicitAsyncFunction(_typescript2.default.findAncestor(node, _typescript2.default.isFunctionLike)) || !this.isPromiseType(this.getChecker().getTypeAtLocation(exp))) {
      return node;
    }
    if (_typescript2.default.isConditionalExpression(exp) || _typescript2.default.isBinaryExpression(exp)) {
      exp = this.wrapSyntheticNode(_typescript2.default.factory.createParenthesizedExpression(exp), exp);
    }
    node.expression = this.wrapSyntheticNode(_typescript2.default.factory.createAwaitExpression(exp), exp);
    return node;
  }
  printConditionalExpression(node, identation) {
    const condition = this.printCondition(node.condition, identation);
    const whenTrue = this.printNode(node.whenTrue, 0);
    const whenFalse = this.printNode(node.whenFalse, 0);
    return condition + " ? " + whenTrue + " : " + whenFalse;
  }
  printAsExpression(node, identation) {
    return this.printNode(node.expression, identation);
  }
  getFunctionNodeFromReturn(node) {
    let parent = node.parent;
    while (parent) {
      if (parent.kind === _typescript2.default.SyntaxKind.FunctionDeclaration || parent.kind === _typescript2.default.SyntaxKind.MethodDeclaration) {
        return parent;
      }
      parent = parent.parent;
    }
    return void 0;
  }
  printReturnStatement(node, identation) {
    const leadingComment = this.printLeadingComments(node, identation);
    let trailingComment = this.printTraillingComment(node, identation);
    trailingComment = trailingComment ? " " + trailingComment : trailingComment;
    const exp = node.expression;
    let rightPart = exp ? " " + this.printNode(exp, identation) : "";
    rightPart = rightPart.trim();
    rightPart = rightPart ? " " + rightPart + this.LINE_TERMINATOR : this.LINE_TERMINATOR;
    return leadingComment + this.getIden(identation) + this.RETURN_TOKEN + rightPart + trailingComment;
  }
  printArrayBindingPattern(node, identation) {
    const elements = node.elements.map((e) => this.printNode(e.name, identation)).join(", ");
    return this.getIden(identation) + this.ARRAY_OPENING_TOKEN + elements + this.ARRAY_CLOSING_TOKEN;
  }
  printBlock(node, identation, chainBlock = false) {
    const blockOpen = this.getBlockOpen(identation);
    const blockClose = this.getBlockClose(identation, chainBlock);
    const statements = node.statements.map((s) => this.printNode(s, identation + 1)).join("\n");
    return blockOpen + statements + blockClose;
  }
  printExpressionStatement(node, identation) {
    if (this.isCJSModuleExportsExpressionStatement(node)) {
      return "";
    }
    const expressionStatementPrefixes = _nullishCoalesce(this.getExpressionStatementPrefixesIfAny(node, identation), () => ( ""));
    const exprStm = this.printNode(node.expression, identation);
    if (exprStm.length === 0) {
      return "";
    }
    const expStatement = expressionStatementPrefixes + this.getIden(identation) + exprStm + this.LINE_TERMINATOR;
    return this.printNodeCommentsIfAny(node, identation, expStatement);
  }
  getExpressionStatementPrefixesIfAny(node, identation) {
    return void 0;
  }
  printPropertyDeclaration(node, identation) {
    const modifiers = this.printPropertyAccessModifiers(node);
    const name = this.printNode(node.name, 0);
    if (node.initializer) {
      const initializer = this.printNode(node.initializer, 0);
      return this.getIden(identation) + modifiers + name + " = " + initializer + this.LINE_TERMINATOR;
    }
    return this.getIden(identation) + modifiers + name + this.LINE_TERMINATOR;
  }
  printPropertyAccessModifiers(node) {
    let modifiers = this.printModifiers(node);
    modifiers = modifiers ? modifiers + " " : modifiers;
    return modifiers;
  }
  printSpreadElement(node, identation) {
    const expression = this.printNode(node.expression, 0);
    return this.getIden(identation) + this.SPREAD_TOKEN + expression;
  }
  printNullKeyword(node, identation) {
    return this.getIden(identation) + this.NULL_TOKEN;
  }
  printContinueStatement(node, identation) {
    return this.getIden(identation) + this.CONTINUE_TOKEN + this.LINE_TERMINATOR;
  }
  printDeleteExpression(node, identation) {
    return void 0;
  }
  printThisKeyword(node, identation) {
    return this.THIS_TOKEN;
  }
  printNode(node, identation = 0) {
    try {
      switch (node.kind) {
        case _typescript2.default.SyntaxKind.ExpressionStatement:
          return this.printExpressionStatement(node, identation);
        case _typescript2.default.SyntaxKind.Block:
          return this.printBlock(node, identation);
        case _typescript2.default.SyntaxKind.FunctionDeclaration:
        case _typescript2.default.SyntaxKind.FunctionExpression:
        case _typescript2.default.SyntaxKind.ArrowFunction:
          return this.printFunctionDeclaration(node, identation);
        case _typescript2.default.SyntaxKind.ClassDeclaration:
          return this.printClass(node, identation);
        case _typescript2.default.SyntaxKind.VariableStatement:
          return this.printVariableStatement(node, identation);
        case _typescript2.default.SyntaxKind.MethodDeclaration:
          return this.printMethodDeclaration(node, identation);
        case _typescript2.default.SyntaxKind.StringLiteral:
          return this.printStringLiteral(node);
        case _typescript2.default.SyntaxKind.NumericLiteral:
          return this.printNumericLiteral(node);
        case _typescript2.default.SyntaxKind.PropertyAccessExpression:
          return this.printPropertyAccessExpression(node, identation);
        case _typescript2.default.SyntaxKind.ArrayLiteralExpression:
          return this.printArrayLiteralExpression(node, identation);
        case _typescript2.default.SyntaxKind.CallExpression:
          return this.printCallExpression(node, identation);
        case _typescript2.default.SyntaxKind.WhileStatement:
          return this.printWhileStatement(node, identation);
        case _typescript2.default.SyntaxKind.BinaryExpression:
          return this.printBinaryExpression(node, identation);
        case _typescript2.default.SyntaxKind.BreakStatement:
          return this.printBreakStatement(node, identation);
        case _typescript2.default.SyntaxKind.ForStatement:
          return this.printForStatement(node, identation);
        case _typescript2.default.SyntaxKind.PostfixUnaryExpression:
          return this.printPostFixUnaryExpression(node, identation);
        case _typescript2.default.SyntaxKind.VariableDeclarationList:
          return this.printVariableDeclarationList(node, identation);
        case _typescript2.default.SyntaxKind.ObjectLiteralExpression:
          return this.printObjectLiteralExpression(node, identation);
        case _typescript2.default.SyntaxKind.PropertyAssignment:
          return this.printPropertyAssignment(node, identation);
        case _typescript2.default.SyntaxKind.Identifier:
          return this.printIdentifier(node);
        case _typescript2.default.SyntaxKind.ElementAccessExpression:
          return this.printElementAccessExpression(node, identation);
        case _typescript2.default.SyntaxKind.IfStatement:
          return this.printIfStatement(node, identation);
        case _typescript2.default.SyntaxKind.ParenthesizedExpression:
          return this.printParenthesizedExpression(node, identation);
        case _typescript2.default.SyntaxKind.TrueKeyword:
        case _typescript2.default.SyntaxKind.FalseKeyword:
          return this.printBooleanLiteral(node);
        case _typescript2.default.SyntaxKind.ThisKeyword:
          return this.printThisKeyword(node, identation);
        case _typescript2.default.SyntaxKind.SuperKeyword:
          return this.SUPER_TOKEN;
        case _typescript2.default.SyntaxKind.TryStatement:
          return this.printTryStatement(node, identation);
        case _typescript2.default.SyntaxKind.PrefixUnaryExpression:
          return this.printPrefixUnaryExpression(node, identation);
        case _typescript2.default.SyntaxKind.ThrowStatement:
          return this.printThrowStatement(node, identation);
        case _typescript2.default.SyntaxKind.NewExpression:
          return this.printNewExpression(node, identation);
        case _typescript2.default.SyntaxKind.AwaitExpression:
          return this.printAwaitExpression(node, identation);
        case _typescript2.default.SyntaxKind.ConditionalExpression:
          return this.printConditionalExpression(node, identation);
        case _typescript2.default.SyntaxKind.AsExpression:
          return this.printAsExpression(node, identation);
        case _typescript2.default.SyntaxKind.ReturnStatement:
          return this.printReturnStatement(this.wrapImplicitReturnAwait(node), identation);
        case _typescript2.default.SyntaxKind.ArrayBindingPattern:
          return this.printArrayBindingPattern(node, identation);
        case _typescript2.default.SyntaxKind.Parameter:
          return this.printParameter(node);
        case _typescript2.default.SyntaxKind.Constructor:
          return this.printConstructorDeclaration(node, identation);
        case _typescript2.default.SyntaxKind.PropertyDeclaration:
          return this.printPropertyDeclaration(node, identation);
        case _typescript2.default.SyntaxKind.SpreadElement:
          return this.printSpreadElement(node, identation);
        case _typescript2.default.SyntaxKind.NullKeyword:
          return this.printNullKeyword(node, identation);
        case _typescript2.default.SyntaxKind.ContinueStatement:
          return this.printContinueStatement(node, identation);
        case _typescript2.default.SyntaxKind.DeleteExpression:
          return this.printDeleteExpression(node, identation);
      }
      if (node.statements) {
        if (_typescript2.default.isSourceFile(node)) {
          this.className = "undefined";
        }
        const transformedStatements = node.statements.map((m) => {
          return this.printNode(m, identation + 1);
        });
        return transformedStatements.filter((st) => st.length > 0).join("\n" + "\n".repeat(this.LINES_BETWEEN_FILE_MEMBERS)) + "\n".repeat(this.NUM_LINES_END_FILE);
      }
      return "";
    } catch (e) {
      if (!(e instanceof TranspilationError)) {
        console.error("[ast-transpiler] underlying error:", e && (e.stack || e.message || e));
      }
      throw new TranspilationError(this.id, _nullishCoalesce((e && (_nullishCoalesce(e.messageText, () => ( e.message)))), () => ( String(e))), node.getFullText(), node.pos, node.end);
    }
  }
  getFileESMImports(node) {
    const result = [];
    const importStatements = node.statements.filter((s) => _typescript2.default.isImportDeclaration(s));
    importStatements.forEach((node2) => {
      const importPath = node2.moduleSpecifier.text;
      const importClause = node2.importClause;
      const namedImports = importClause.namedBindings;
      if (namedImports) {
        if (namedImports.elements) {
          namedImports.elements.forEach((elem) => {
            const name = elem.name.text;
            const fileImport = {
              name,
              path: importPath,
              isDefault: false
            };
            result.push(fileImport);
          });
        } else {
          const name = namedImports.name.escapedText;
          const fileImport = {
            name,
            path: importPath,
            isDefault: false
          };
          result.push(fileImport);
        }
      } else {
        const name = importClause.name.text;
        const fileImport = {
          name,
          path: importPath,
          isDefault: true
        };
        result.push(fileImport);
      }
    });
    return result;
  }
  isCJSRequireStatement(node) {
    const dec = node.declarationList.declarations[0];
    return dec.initializer && _typescript2.default.isCallExpression(dec.initializer) && dec.initializer.expression.getText() === "require";
  }
  isCJSModuleExportsExpressionStatement(node) {
    if (node.expression && node.expression.kind === _typescript2.default.SyntaxKind.BinaryExpression) {
      if (node.expression.left.kind === _typescript2.default.SyntaxKind.PropertyAccessExpression) {
        const left = node.expression.left;
        return left.expression.getText() === "module" && left.name.getText() === "exports";
      }
    }
    return false;
  }
  getCJSImports(node) {
    const result = [];
    const varStatements = node.statements.filter((s) => _typescript2.default.isVariableStatement(s));
    const decList = varStatements.map((s) => s.declarationList);
    const dec = decList.map((d) => d.declarations[0]);
    dec.forEach((decNode) => {
      if (decNode.initializer && decNode.initializer.kind === _typescript2.default.SyntaxKind.CallExpression) {
        const callExpression = decNode.initializer.expression.getText();
        if (callExpression === "require") {
          const isDefault = decNode.name.kind === _typescript2.default.SyntaxKind.Identifier;
          const importPath = decNode.initializer.arguments[0].text;
          if (isDefault) {
            const name = decNode.name.text;
            const fileImport = {
              name,
              path: importPath,
              isDefault
            };
            result.push(fileImport);
          } else {
            const elems = decNode.name.elements;
            elems.forEach((elem) => {
              const name = elem.name.text;
              const fileImport = {
                name,
                path: importPath,
                isDefault: false
              };
              result.push(fileImport);
            });
          }
        }
      }
    });
    return result;
  }
  getFileImports(node) {
    const esmImports = this.getFileESMImports(node);
    if (esmImports.length > 0) {
      return esmImports;
    }
    const cjsImports = this.getCJSImports(node);
    return cjsImports;
  }
  getESMExports(node) {
    const result = [];
    const namedExports = node.statements.filter((s) => _typescript2.default.isExportDeclaration(s));
    const defaultExport = node.statements.filter((s) => _typescript2.default.isExportAssignment(s));
    namedExports.forEach((node2) => {
      const namedExports2 = node2.exportClause;
      if (namedExports2) {
        namedExports2.elements.forEach((elem) => {
          const name = elem.name.text;
          const fileExport = {
            name,
            isDefault: false
          };
          result.push(fileExport);
        });
      }
    });
    defaultExport.forEach((node2) => {
      const name = node2.expression.getText();
      const fileExport = {
        name,
        isDefault: true
      };
      result.push(fileExport);
    });
    return result;
  }
  getCJSExports(node) {
    const result = [];
    const moduleExports = node.statements.filter((s) => this.isCJSModuleExportsExpressionStatement(s)).map((s) => s.expression);
    moduleExports.forEach((node2) => {
      const right = node2.right;
      if (right.kind === _typescript2.default.SyntaxKind.ObjectLiteralExpression) {
        const props = right.properties;
        props.forEach((prop) => {
          const name = prop.name.getText();
          const fileExport = {
            name,
            isDefault: false
          };
          result.push(fileExport);
        });
      }
      if (right.kind === _typescript2.default.SyntaxKind.Identifier) {
        const name = right.getText();
        const fileExport = {
          name,
          isDefault: true
        };
        result.push(fileExport);
      }
    });
    return result;
  }
  getExportDeclarations(node) {
    const result = [];
    const classDeclarations = node.statements.filter((s) => _typescript2.default.isClassDeclaration(s));
    const functionDeclarations = node.statements.filter((s) => _typescript2.default.isFunctionDeclaration(s));
    const both = classDeclarations.concat(functionDeclarations);
    both.forEach((classNode) => {
      const modifiers = classNode.modifiers;
      if (modifiers) {
        const isDefault = modifiers.some((m) => m.kind === _typescript2.default.SyntaxKind.DefaultKeyword);
        if (isDefault) {
          const name = classNode.name.text;
          const fileExport = {
            name,
            isDefault: true
          };
          result.push(fileExport);
        }
      }
    });
    return result;
  }
  getFileExports(node) {
    const defaultClassAndFunctionsExports = this.getExportDeclarations(node);
    const esmExports = this.getESMExports(node).concat(defaultClassAndFunctionsExports);
    if (esmExports.length > 0) {
      return esmExports;
    }
    return this.getCJSExports(node);
  }
  getReturnTypeFromMethod(node) {
    const bType = this.getChecker().getTypeAtLocation(node);
    const func2Type = this.getChecker().getTypeOfSymbolAtLocation(bType.symbol, bType.symbol.valueDeclaration);
    const func2Signature = this.getChecker().getSignaturesOfType(func2Type, _typescript2.default.SignatureKind.Call)[0];
    const rawType = func2Signature.getReturnType();
    const res = this.getChecker().typeToString(rawType);
    if (res === void 0) {
      const name = _optionalChain([node, 'access', _38 => _38.type, 'optionalAccess', _39 => _39.typeName, 'optionalAccess', _40 => _40.escapedText]);
      if (name) {
        return name;
      }
    }
    return res;
  }
  getParameterType(node) {
    const isOptional = node.questionToken !== void 0;
    const result = {
      name: node.name.getText(),
      isOptional,
      type: void 0
    };
    if (node.initializer !== void 0) {
      result.initializer = node.initializer.getText();
    }
    if (node.type === void 0) {
      if (node.initializer !== void 0) {
        const type = this.getChecker().getTypeAtLocation(node.initializer);
        const res = this.getChecker().typeToString(type);
        result.type = _typescript2.default.TypeFlags[type.flags];
        return result;
      }
    }
    const name = _optionalChain([node, 'access', _41 => _41.type, 'optionalAccess', _42 => _42.typeName, 'optionalAccess', _43 => _43.escapedText]);
    if (name) {
      result.type = name;
      if (node.initializer !== void 0) {
        result.initializer = node.initializer.text;
      }
      return result;
    }
    if (node.type != void 0) {
      const type = this.getChecker().getTypeAtLocation(node.type);
      const res = this.getChecker().typeToString(type);
      result.type = res;
      return result;
    }
    return result;
  }
  getMethodTypes(file) {
    const result = [];
    if (!file.statements) {
      return result;
    }
    const classDeclarations = file.statements.filter((s) => _typescript2.default.isClassDeclaration(s));
    classDeclarations.forEach((node) => {
      const methods = node.members.filter((m) => _typescript2.default.isMethodDeclaration(m));
      methods.forEach((m) => {
        const isAsync = this.isAsyncFunction(m);
        const name = m.name.getText();
        const returnType = this.getReturnTypeFromMethod(m);
        const parameters = m.parameters;
        const paramTypes = [];
        parameters.forEach((p) => {
          const res = this.getParameterType(p);
          paramTypes.push(res);
        });
        result.push({
          name,
          async: isAsync,
          returnType,
          parameters: paramTypes
        });
      });
    });
    return result;
  }
};

// src/pythonTranspiler.ts

var SyntaxKind = _typescript2.default.SyntaxKind;
var parserConfig = {
  "STATIC_TOKEN": "",
  // to do static decorator
  "PUBLIC_KEYWORD": "",
  "UNDEFINED_TOKEN": "None",
  "IF_TOKEN": "if",
  "ELSE_TOKEN": "else",
  "ELSEIF_TOKEN": "elif",
  "THIS_TOKEN": "self",
  "AMPERSTAND_APERSAND_TOKEN": "and",
  "BAR_BAR_TOKEN": "or",
  "SPACE_DEFAULT_PARAM": "",
  "BLOCK_OPENING_TOKEN": ":",
  "BLOCK_CLOSING_TOKEN": "",
  "SPACE_BEFORE_BLOCK_OPENING": "",
  "CONDITION_OPENING": "",
  "CONDITION_CLOSE": "",
  "TRUE_KEYWORD": "True",
  "FALSE_KEYWORD": "False",
  "THROW_TOKEN": "raise",
  "NOT_TOKEN": "not ",
  "PLUS_PLUS_TOKEN": " += 1",
  "MINUS_MINUS_TOKEN": " -= 1",
  "CONSTRUCTOR_TOKEN": "def __init__",
  "SUPER_CALL_TOKEN": "super().__init__",
  "PROPERTY_ASSIGNMENT_TOKEN": ":",
  "FUNCTION_TOKEN": "def",
  "SUPER_TOKEN": "super()",
  "NEW_TOKEN": "",
  "STRING_QUOTE_TOKEN": "'",
  "LINE_TERMINATOR": "",
  "METHOD_TOKEN": "def",
  "CATCH_TOKEN": "except",
  "CATCH_DECLARATION": "Exception as",
  "METHOD_DEFAULT_ACCESS": "",
  "SPREAD_TOKEN": "*",
  "NULL_TOKEN": "None"
};
var PythonTranspiler = class extends BaseTranspiler {
  constructor(config = {}) {
    config["parser"] = Object.assign({}, parserConfig, _nullishCoalesce(config["parser"], () => ( {})));
    super(config);
    this.id = "python";
    this.initConfig();
    this.asyncTranspiling = _nullishCoalesce(config["async"], () => ( true));
    this.uncamelcaseIdentifiers = _nullishCoalesce(config["uncamelcaseIdentifiers"], () => ( true));
    this.removeVariableDeclarationForFunctionExpression = _nullishCoalesce(config["removeVariableDeclarationForFunctionExpression"], () => ( true));
    this.includeFunctionNameInFunctionExpressionDeclaration = _nullishCoalesce(config["includeFunctionNameInFunctionExpressionDeclaration"], () => ( true));
    this.applyUserOverrides(config);
  }
  initConfig() {
    this.LeftPropertyAccessReplacements = {
      "this": "self"
    };
    this.RightPropertyAccessReplacements = {
      "push": "append",
      "toUpperCase": "upper",
      "toLowerCase": "lower",
      // 'parseFloat': 'float',
      // 'parseInt': 'int',
      "indexOf": "find",
      "padEnd": "ljust",
      "padStart": "rjust"
    };
    this.FullPropertyAccessReplacements = {
      "console.log": "print",
      "JSON.stringify": "json.dumps",
      "JSON.parse": "json.loads",
      "Math.log": "math.log",
      "Math.abs": "abs",
      "Math.min": "min",
      "Math.max": "max",
      "Math.ceil": "math.ceil",
      "Math.round": "math.round",
      "Math.floor": "math.floor",
      "Math.pow": "math.pow",
      "process.exit": "sys.exit",
      "Number.MAX_SAFE_INTEGER": "float('inf')"
    };
    this.CallExpressionReplacements = {
      "parseInt": "int",
      "parseFloat": "float"
    };
    this.PropertyAccessRequiresParenthesisRemoval = [
      // 'length',
      // 'toString',
    ];
  }
  printArrayIsArrayCall(node, identation, parsedArg = void 0) {
    return `isinstance(${parsedArg}, list)`;
  }
  printObjectKeysCall(node, identation, parsedArg = void 0) {
    return `list(${parsedArg}.keys())`;
  }
  printObjectValuesCall(node, identation, parsedArg = void 0) {
    return `list(${parsedArg}.values())`;
  }
  printPromiseAllCall(node, identation, parsedArg) {
    return `asyncio.gather(*${parsedArg})`;
  }
  printMathFloorCall(node, identation, parsedArg = void 0) {
    return `int(math.floor(${parsedArg}))`;
  }
  printMathCeilCall(node, identation, parsedArg = void 0) {
    return `int(math.ceil(${parsedArg}))`;
  }
  printNumberIsIntegerCall(node, identation, parsedArg = void 0) {
    return `isinstance(${parsedArg}, int)`;
  }
  printMathRoundCall(node, identation, parsedArg = void 0) {
    return `int(round(${parsedArg}))`;
  }
  printIncludesCall(node, identation, name, parsedArg) {
    return `${parsedArg} in ${name}`;
  }
  printJoinCall(node, identation, name, parsedArg) {
    return `${parsedArg}.join(${name})`;
  }
  printSplitCall(node, identation, name, parsedArg) {
    return `${name}.split(${parsedArg})`;
  }
  printConcatCall(node, identation, name, parsedArg) {
    return `${name} + ${parsedArg}`;
  }
  printPopCall(node, identation, name) {
    return `${name}.pop()`;
  }
  printShiftCall(node, identation, name) {
    return `${name}.pop(0)`;
  }
  printReverseCall(node, identation, name = void 0) {
    return `${name}.reverse()`;
  }
  printArrayPushCall(node, identation, name, parsedArg) {
    return `${name}.append(${parsedArg})`;
  }
  printToStringCall(node, identation, name = void 0) {
    return `str(${name})`;
  }
  printIndexOfCall(node, identation, name = void 0, parsedArg = void 0) {
    return `${name}.find(${parsedArg})`;
  }
  printSearchCall(node, identation, name = void 0, parsedArg = void 0) {
    return `${name}.find(${parsedArg})`;
  }
  printStartsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    return `${name}.startswith(${parsedArg})`;
  }
  printEndsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    return `${name}.endswith(${parsedArg})`;
  }
  printPadEndCall(node, identation, name, parsedArg, parsedArg2) {
    return `${name}.ljust(${parsedArg}, ${parsedArg2})`;
  }
  printPadStartCall(node, identation, name, parsedArg, parsedArg2) {
    return `${name}.rjust(${parsedArg}, ${parsedArg2})`;
  }
  printTrimCall(node, identation, name = void 0) {
    return `${name}.strip()`;
  }
  printToUpperCaseCall(node, identation, name = void 0) {
    return `${name}.upper()`;
  }
  printToLowerCaseCall(node, identation, name = void 0) {
    return `${name}.lower()`;
  }
  printJsonParseCall(node, identation, parsedArg) {
    return `json.loads(${parsedArg})`;
  }
  printJsonStringifyCall(node, identation, parsedArg) {
    return `json.dumps(${parsedArg})`;
  }
  printReplaceCall(node, identation, name, parsedArg, parsedArg2) {
    return `${name}.replace(${parsedArg}, ${parsedArg2})`;
  }
  printReplaceAllCall(node, identation, name, parsedArg, parsedArg2) {
    return `${name}.replace(${parsedArg}, ${parsedArg2})`;
  }
  printElementAccessExpressionExceptionIfAny(node) {
    if (node.expression.kind === SyntaxKind.ThisKeyword) {
      return "getattr(self, " + this.printNode(node.argumentExpression, 0) + ")";
    }
  }
  printAssertCall(node, identation, parsedArgs) {
    return `assert ${parsedArgs}`;
  }
  printDateNowCall(node, identation) {
    return "int(time.time() * 1000)";
  }
  printForStatement(node, identation) {
    const varName = node.initializer.declarations[0].name.escapedText;
    const initValue = this.printNode(node.initializer.declarations[0].initializer, 0);
    const roofValue = this.printNode(node.condition.right, 0);
    const forStm = this.getIden(identation) + this.FOR_TOKEN + " " + varName + " in range(" + initValue + ", " + roofValue + "):\n" + node.statement.statements.map((st) => this.printNode(st, identation + 1)).join("\n");
    return this.printNodeCommentsIfAny(node, identation, forStm);
  }
  printPropertyAccessModifiers(node) {
    return "";
  }
  transformLeadingComment(comment) {
    const commentRegex = [
      [/(^|\s)\/\//g, "$1#"],
      // regular comments
      [/\/\*\*/, '"""'],
      // eslint-disable-line
      [/ \*\//, '"""'],
      // eslint-disable-line
      [/\[([^\[\]]*)\]\{@link (.*)\}/g, "`$1 <$2>`"],
      // eslint-disable-line
      [/\s+\* @method/g, ""],
      // docstring @method
      [/(\s+) \* @description (.*)/g, "$1$2"],
      // docstring description
      [/\s+\* @name .*/g, ""],
      // docstring @name
      [/(\s+) \* @see( .*)/g, "$1see$2"],
      // docstring @see
      [/(\s+ \* @(param|returns) {[^}]*)string([^}]*}.*)/g, "$1str$3"],
      // docstring type conversion
      [/(\s+ \* @(param|returns) {[^}]*)object([^}]*}.*)/g, "$1dict$3"],
      // doctstrubg type conversion
      [/(\s+) \* @returns ([^\{])/g, "$1:returns: $2"],
      // eslint-disable-line
      [/(\s+) \* @returns \{(.+)\}/g, "$1:returns $2:"],
      // docstring return
      [/(\s+ \* @param \{[\]\[\|a-zA-Z]+\} )([a-zA-Z0-9_-]+)\.([a-zA-Z0-9_-]+) (.*)/g, "$1$2['$3'] $4"],
      // eslint-disable-line
      [/(\s+) \* @([a-z]+) \{([\]\[a-zA-Z\|]+)\} ([a-zA-Z0-9_\-\.\[\]\']+)/g, "$1:$2 $3 $4:"]
      // eslint-disable-line
    ];
    const transformed = regexAll(comment, commentRegex);
    return transformed;
  }
  transformTrailingComment(comment) {
    const commentRegex = [
      [/(^|\s)\/\//g, "$1#"]
      // regular comments
    ];
    const transformed = regexAll(comment, commentRegex);
    return " " + transformed;
  }
  transformPropertyAcessExpressionIfNeeded(node) {
    const expression = node.expression;
    const leftSide = this.printNode(expression, 0);
    const rightSide = node.name.escapedText;
    let rawExpression = void 0;
    if (rightSide === "length") {
      rawExpression = "len(" + leftSide + ")";
    } else if (rightSide === "toString") {
      rawExpression = "str(" + leftSide + ")";
    }
    return rawExpression;
  }
  printClassDefinition(node, identation) {
    const className = node.name.escapedText;
    const heritageClauses = node.heritageClauses;
    let classInit = "";
    if (heritageClauses !== void 0) {
      const classExtends = heritageClauses[0].types[0].expression.escapedText;
      classInit = this.getIden(identation) + "class " + className + "(" + classExtends + "):\n";
    } else {
      classInit = this.getIden(identation) + "class " + className + ":\n";
    }
    return classInit;
  }
  isFunctionOutSideClass(node) {
    return !(node.parent && node.parent.kind === SyntaxKind.ClassDeclaration);
  }
  printMethodParameters(node) {
    let parsedArgs = super.printMethodParameters(node);
    const shouldAddSelf = !this.isFunctionOutSideClass(node);
    parsedArgs = shouldAddSelf ? parsedArgs ? "self, " + parsedArgs : "self" : parsedArgs;
    return parsedArgs;
  }
  printInstanceOfExpression(node, identation) {
    const left = this.printNode(node.left, 0);
    const right = this.printNode(node.right, 0);
    return this.getIden(identation) + `isinstance(${left}, ${right})`;
  }
  handleTypeOfInsideBinaryExpression(node, identation) {
    const expression = node.left.expression;
    const right = node.right.text;
    const op = node.operatorToken.kind;
    const isDifferentOperator = op === SyntaxKind.ExclamationEqualsEqualsToken || op === SyntaxKind.ExclamationEqualsToken;
    const notOperator = isDifferentOperator ? this.NOT_TOKEN : "";
    switch (right) {
      case "string":
        return this.getIden(identation) + notOperator + "isinstance(" + this.printNode(expression, 0) + ", str)";
      case "number":
        return this.getIden(identation) + notOperator + "isinstance(" + this.printNode(expression, 0) + ", numbers.Real)";
      case "boolean":
        return this.getIden(identation) + notOperator + "isinstance(" + this.printNode(expression, 0) + ", bool)";
      case "object":
        return this.getIden(identation) + notOperator + "isinstance(" + this.printNode(expression, 0) + ", dict)";
      case "undefined":
        return this.getIden(identation) + this.printNode(expression, 0) + " is " + notOperator + "None";
    }
    return void 0;
  }
  printCustomBinaryExpressionIfAny(node, identation) {
    const left = node.left;
    const right = node.right.text;
    const op = node.operatorToken.kind;
    if ((op === _typescript2.default.SyntaxKind.EqualsEqualsToken || op === _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken) && node.right.kind === _typescript2.default.SyntaxKind.TrueKeyword) {
      return this.getIden(identation) + this.printNode(node.left, 0);
    }
    if (left.kind === SyntaxKind.TypeOfExpression) {
      const typeOfExpression = this.handleTypeOfInsideBinaryExpression(node, identation);
      if (typeOfExpression) {
        return typeOfExpression;
      }
    }
    const prop = _optionalChain([node, 'optionalAccess', _44 => _44.left, 'optionalAccess', _45 => _45.expression, 'optionalAccess', _46 => _46.name, 'optionalAccess', _47 => _47.text]);
    if (prop) {
      const args = left.arguments;
      const parsedArg = args && args.length > 0 ? this.printNode(args[0], 0) : void 0;
      const leftSideOfIndexOf = left.expression.expression;
      const leftSide = this.printNode(leftSideOfIndexOf, 0);
      switch (prop) {
        case "indexOf":
          if (op === SyntaxKind.GreaterThanEqualsToken && right === "0") {
            return this.getIden(identation) + `${parsedArg} in ${leftSide}`;
          }
      }
    }
    return void 0;
  }
  printConditionalExpression(node, identation) {
    const condition = this.printNode(node.condition, 0);
    const whenTrue = this.printNode(node.whenTrue, 0);
    const whenFalse = this.printNode(node.whenFalse, 0);
    return this.getIden(identation) + whenTrue + " if " + condition + " else " + whenFalse;
  }
  printDeleteExpression(node, identation) {
    const expression = this.printNode(node.expression);
    return `del ${expression}`;
  }
  getCustomOperatorIfAny(left, right, operator) {
    const rightText = right.getText();
    const isUndefined = rightText === "undefined";
    if (isUndefined) {
      switch (operator.kind) {
        case _typescript2.default.SyntaxKind.EqualsEqualsToken:
          return "is";
        case _typescript2.default.SyntaxKind.ExclamationEqualsToken:
          return "is not";
        case _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken:
          return "is not";
        case _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken:
          return "is";
      }
    }
  }
};

// src/phpTranspiler.ts
init_cjs_shims();

var SyntaxKind2 = _typescript2.default.SyntaxKind;
var parserConfig2 = {
  "ELSEIF_TOKEN": "elseif",
  "THIS_TOKEN": "$this",
  "PROPERTY_ACCESS_TOKEN": "->",
  "UNDEFINED_TOKEN": "null",
  "NOT_TOKEN": "!",
  "LINE_TERMINATOR": ";",
  "ARRAY_OPENING_TOKEN": "[",
  "ARRAY_CLOSING_TOKEN": "]",
  "OBJECT_OPENING": "array(",
  "OBJECT_CLOSING": ")",
  "FUNCTION_TOKEN": "function",
  "ASYNC_TOKEN": "",
  "PROPERTY_ASSIGNMENT_TOKEN": " =>",
  "NEW_TOKEN": "new",
  "THROW_TOKEN": "throw",
  "SUPER_TOKEN": "parent",
  "CONSTRUCTOR_TOKEN": "function __construct",
  "SUPER_CALL_TOKEN": "parent::__construct",
  "CATCH_DECLARATION": "Exception",
  "CATCH_TOKEN": "catch",
  "BLOCK_OPENING_TOKEN": "{",
  "BLOCK_CLOSING_TOKEN": "}",
  "CONDITION_OPENING": "(",
  "CONDITION_CLOSE": ")",
  "PLUS_PLUS_TOKEN": "++",
  "MINUS_MINUS_TOKEN": "--",
  "SPACE_DEFAULT_PARAM": " ",
  "EXCLAMATION_EQUALS_EQUALS_TOKEN": "!==",
  "EQUALS_EQUALS_EQUALS_TOKEN": "===",
  "STRING_QUOTE_TOKEN": "'",
  "EXTENDS_TOKEN": "extends"
};
var PhpTranspiler = class extends BaseTranspiler {
  constructor(config = {}) {
    config["parser"] = Object.assign({}, parserConfig2, _nullishCoalesce(config["parser"], () => ( {})));
    super(config);
    this.ASYNC_FUNCTION_WRAPPER_OPEN = "";
    this.id = "php";
    this.asyncTranspiling = _nullishCoalesce(config["async"], () => ( true));
    this.uncamelcaseIdentifiers = _nullishCoalesce(config["uncamelcaseIdentifiers"], () => ( false));
    this.removeVariableDeclarationForFunctionExpression = _nullishCoalesce(config["removeFunctionAssignToVariable"], () => ( false));
    this.includeFunctionNameInFunctionExpressionDeclaration = _nullishCoalesce(config["includeFunctionNameInFunctionExpressionDeclaration"], () => ( false));
    this.propRequiresScopeResolutionOperator = ["super"] + (_nullishCoalesce(config["ScopeResolutionProps"], () => ( [])));
    this.initConfig();
    this.applyUserOverrides(config);
    this.AWAIT_WRAPPER_OPEN = _nullishCoalesce(config["AWAIT_WRAPPER_OPEN"], () => ( "\\React\\Async\\await("));
    this.AWAIT_WRAPPER_CLOSE = _nullishCoalesce(config["AWAIT_WRAPPER_CLOSE"], () => ( ")"));
  }
  printAwaitExpression(node, identation) {
    const expression = this.printNode(node.expression, identation);
    if (!this.asyncTranspiling) {
      return expression;
    }
    return this.AWAIT_WRAPPER_OPEN + expression + this.AWAIT_WRAPPER_CLOSE;
  }
  transformIdentifier(node, identifier) {
    if (this.uncamelcaseIdentifiers) {
      identifier = this.unCamelCaseIfNeeded(identifier);
    }
    const symbol = this.getChecker().getSymbolAtLocation(node);
    if (symbol && symbol.valueDeclaration) {
      const valueDecl = symbol.valueDeclaration;
      if (_typescript2.default.isFunctionDeclaration(valueDecl) || _typescript2.default.isFunctionExpression(valueDecl) || _typescript2.default.isArrowFunction(valueDecl)) {
        if (node.parent && _typescript2.default.isCallExpression(node.parent) && node.parent.arguments.includes(node)) {
          return `'${identifier}'`;
        }
      }
    }
    if (!this.startsWithUpperCase(identifier)) {
      return "$" + identifier;
    }
    return identifier;
  }
  getCustomOperatorIfAny(left, right, operator) {
    const STRING_CONCAT = ".";
    const PLUS_EQUALS_TOKEN = ".=";
    if (operator.kind == SyntaxKind2.PlusToken || operator.kind == SyntaxKind2.PlusEqualsToken) {
      const TOKEN = operator.kind == SyntaxKind2.PlusToken ? STRING_CONCAT : PLUS_EQUALS_TOKEN;
      if (left.kind == SyntaxKind2.StringLiteral || right.kind == SyntaxKind2.StringLiteral) {
        return TOKEN;
      }
      const leftType = this.getChecker().getTypeAtLocation(left);
      const rightType = this.getChecker().getTypeAtLocation(right);
      if (leftType.flags === _typescript2.default.TypeFlags.String || rightType.flags === _typescript2.default.TypeFlags.String) {
        return TOKEN;
      }
      if (leftType.flags === _typescript2.default.TypeFlags.StringLiteral || rightType.flags === _typescript2.default.TypeFlags.StringLiteral) {
        return TOKEN;
      }
    }
    return void 0;
  }
  printLengthProperty(node, identation, name = void 0) {
    const leftSide = this.printNode(node.expression, 0);
    const type = this.getChecker().getTypeAtLocation(node.expression);
    this.warnIfAnyType(node, type.flags, leftSide, "length");
    return this.isStringType(type.flags) ? `strlen(${leftSide})` : `count(${leftSide})`;
  }
  printPopCall(node, identation, name = void 0) {
    return `array_pop(${name})`;
  }
  printReverseCall(node, identation, name = void 0) {
    return `${name} = array_reverse(${name})`;
  }
  printShiftCall(node, identation, name = void 0) {
    return `array_shift(${name})`;
  }
  printToLowerCaseCall(node, identation, name = void 0) {
    return `strtolower(${name})`;
  }
  printToUpperCaseCall(node, identation, name = void 0) {
    return `strtoupper(${name})`;
  }
  printToStringCall(node, identation, name = void 0) {
    return `((string) ${name})`;
  }
  printArrayIsArrayCall(node, identation, parsedArg = void 0) {
    return `gettype(${parsedArg}) === 'array' && array_is_list(${parsedArg})`;
  }
  printObjectKeysCall(node, identation, parsedArg = void 0) {
    return `is_array(${parsedArg}) ? array_keys(${parsedArg}) : array()`;
  }
  printObjectValuesCall(node, identation, parsedArg = void 0) {
    return `is_array(${parsedArg}) ? array_values(${parsedArg}) : array()`;
  }
  printJsonParseCall(node, identation, parsedArg) {
    return `json_decode(${parsedArg}, $as_associative_array = true)`;
  }
  printJsonStringifyCall(node, identation, parsedArg) {
    return `json_encode(${parsedArg})`;
  }
  printArrayPushCall(node, identation, name = void 0, parsedArg = void 0) {
    return `${name}[] = ${parsedArg}`;
  }
  printPromiseAllCall(node, identation, parsedArg = void 0) {
    return `\\React\\Promise\\all(${parsedArg})`;
  }
  printMathCeilCall(node, identation, parsedArg = void 0) {
    return `((int) ceil(${parsedArg}))`;
  }
  printNumberIsIntegerCall(node, identation, parsedArg) {
    return `is_int(${parsedArg})`;
  }
  printMathRoundCall(node, identation, parsedArg = void 0) {
    return `((int) round(${parsedArg}))`;
  }
  printMathFloorCall(node, identation, parsedArg) {
    return `((int) floor(${parsedArg}))`;
  }
  printReplaceCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    return `str_replace(${parsedArg}, ${parsedArg2}, ${name})`;
  }
  printReplaceAllCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    return `str_replace(${parsedArg}, ${parsedArg2}, ${name})`;
  }
  printIncludesCall(node, identation, name = void 0, parsedArg = void 0) {
    const leftSide = _optionalChain([node, 'access', _48 => _48.expression, 'optionalAccess', _49 => _49.expression]);
    const leftSideText = this.printNode(leftSide, 0);
    const type = this.getChecker().getTypeAtLocation(leftSide);
    this.warnIfAnyType(node, type.flags, leftSideText, "includes");
    this.warnIfAnyType(node, type.flags, leftSideText, "includes");
    if (this.isStringType(type.flags)) {
      return `str_contains(${name}, ${parsedArg})`;
    } else {
      return `in_array(${parsedArg}, ${name})`;
    }
  }
  printIndexOfCall(node, identation, name = void 0, parsedArg = void 0) {
    const leftSide = _optionalChain([node, 'access', _50 => _50.expression, 'optionalAccess', _51 => _51.expression]);
    const leftSideText = this.printNode(leftSide, 0);
    const type = this.getChecker().getTypeAtLocation(leftSide);
    this.warnIfAnyType(node, type.flags, leftSideText, "indexOf");
    if (this.isStringType(type.flags)) {
      return `mb_strpos(${name}, ${parsedArg})`;
    } else {
      return `array_search(${parsedArg}, ${name})`;
    }
  }
  printSearchCall(node, identation, name = void 0, parsedArg = void 0) {
    return `mb_strpos(${name}, ${parsedArg})`;
  }
  printStartsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    return `str_starts_with(${name}, ${parsedArg})`;
  }
  printEndsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    return `str_ends_with(${name}, ${parsedArg})`;
  }
  printTrimCall(node, identation, name = void 0) {
    return `trim(${name})`;
  }
  printJoinCall(node, identation, name = void 0, parsedArg = void 0) {
    return `implode(${parsedArg}, ${name})`;
  }
  printSplitCall(node, identation, name = void 0, parsedArg = void 0) {
    return `explode(${parsedArg}, ${name})`;
  }
  printConcatCall(node, identation, name, parsedArg) {
    return `array_merge(${name}, ${parsedArg})`;
  }
  printPadEndCall(node, identation, name, parsedArg, parsedArg2) {
    return `str_pad(${name}, ${parsedArg}, ${parsedArg2}, STR_PAD_RIGHT)`;
  }
  printPadStartCall(node, identation, name, parsedArg, parsedArg2) {
    return `str_pad(${name}, ${parsedArg}, ${parsedArg2}, STR_PAD_LEFT)`;
  }
  printDateNowCall(node, identation) {
    return "round(microtime(true) * 1000)";
  }
  printInstanceOfExpression(node, identation) {
    const left = node.left.escapedText;
    const right = node.right.escapedText;
    return this.getIden(identation) + "$" + left + " instanceof " + right;
  }
  printDeleteExpression(node, identation) {
    const expression = this.printNode(node.expression, 0);
    return `unset(${expression})`;
  }
  printNewExpression(node, identation) {
    let expression = _optionalChain([node, 'access', _52 => _52.expression, 'optionalAccess', _53 => _53.escapedText]);
    expression = expression ? expression : this.printNode(node.expression);
    if (expression === "Error") {
      expression = "Exception";
    }
    const args = node.arguments.map((n) => this.printNode(n, identation)).join(", ");
    const newToken = this.NEW_TOKEN ? this.NEW_TOKEN + " " : "";
    return newToken + expression + this.LEFT_PARENTHESIS + args + this.RIGHT_PARENTHESIS;
  }
  getExceptionalAccessTokenIfAny(node) {
    const leftSide = _nullishCoalesce(node.expression.escapedText, () => ( node.expression.getFullText().trim()));
    if (!leftSide) {
      return void 0;
    }
    if (this.propRequiresScopeResolutionOperator.includes(leftSide)) {
      return "::";
    }
    return void 0;
  }
  handleTypeOfInsideBinaryExpression(node, identation) {
    const left = node.left;
    const right = node.right.text;
    const op = node.operatorToken.kind;
    const expression = left.expression;
    const isDifferentOperator = op === SyntaxKind2.ExclamationEqualsEqualsToken || op === SyntaxKind2.ExclamationEqualsToken;
    const notOperator = isDifferentOperator ? this.NOT_TOKEN : "";
    const opComp = isDifferentOperator ? this.EXCLAMATION_EQUALS_EQUALS_TOKEN : this.EQUALS_EQUALS_EQUALS_TOKEN;
    switch (right) {
      case "string":
        return this.getIden(identation) + notOperator + "is_string(" + this.printNode(expression, 0) + ")";
      case "number":
        return this.getIden(identation) + notOperator + "(is_int(" + this.printNode(expression, 0) + ") || is_float(" + this.printNode(expression, 0) + "))";
      case "boolean":
        return this.getIden(identation) + notOperator + "is_bool(" + this.printNode(expression, 0) + ")";
      case "object":
        return this.getIden(identation) + notOperator + "is_array(" + this.printNode(expression, 0) + ")";
      case "undefined":
        return this.getIden(identation) + this.printNode(expression, 0) + " " + opComp + " null";
    }
    return void 0;
  }
  printCustomBinaryExpressionIfAny(node, identation) {
    const left = node.left;
    const right = node.right.text;
    const op = node.operatorToken.kind;
    if (left.kind === SyntaxKind2.TypeOfExpression) {
      const typeOfExpression = this.handleTypeOfInsideBinaryExpression(node, identation);
      if (typeOfExpression) {
        return typeOfExpression;
      }
    }
    if (op === _typescript2.default.SyntaxKind.InKeyword) {
      const rightSide = this.printNode(node.right, 0);
      const leftSide = this.printNode(node.left, 0);
      return `${this.getIden(identation)}is_array(${rightSide}) && array_key_exists(${leftSide}, ${rightSide})`;
    }
    const prop = _optionalChain([node, 'optionalAccess', _54 => _54.left, 'optionalAccess', _55 => _55.expression, 'optionalAccess', _56 => _56.name, 'optionalAccess', _57 => _57.text]);
    if (prop) {
      const args = left.arguments;
      const parsedArg = args && args.length > 0 ? this.printNode(args[0], 0) : void 0;
      const leftSideOfIndexOf = left.expression.expression;
      const leftSide = this.printNode(leftSideOfIndexOf, 0);
      const rightType = this.getChecker().getTypeAtLocation(leftSideOfIndexOf);
      switch (prop) {
        case "indexOf":
          if (op === SyntaxKind2.GreaterThanEqualsToken && right === "0") {
            this.warnIfAnyType(node, rightType.flags, leftSide, "indexOf");
            if (this.isStringType(rightType.flags)) {
              return this.getIden(identation) + "mb_strpos(" + leftSide + ", " + parsedArg + ") !== false";
            } else {
              return this.getIden(identation) + "in_array(" + parsedArg + ", " + leftSide + ")";
            }
          }
      }
    }
    return void 0;
  }
  printFunctionDeclaration(node, identation) {
    let functionDef = this.printFunctionDefinition(node, identation);
    const funcBody = this.printFunctionBody(node, identation);
    functionDef += funcBody;
    return this.printNodeCommentsIfAny(node, identation, functionDef);
  }
  printFunctionBody(node, identation) {
    if (this.asyncTranspiling && this.isAsyncFunction(node)) {
      const blockOpen = this.getBlockOpen(identation);
      const blockClose = this.getBlockClose(identation);
      const parsedArgs = node.parameters.map((param) => this.printParameter(param, false)).join(", ");
      const params = parsedArgs ? " use (" + parsedArgs + ")" : "";
      const bodyStms = [...node.body.statements];
      const firstBodyStm = this.printNode(bodyStms[0], identation + 2);
      bodyStms.shift();
      const funcBody = bodyStms.map((s) => this.printNode(s, identation + 2)).join("\n");
      const bodyParts = firstBodyStm.split("\n");
      const commentPart = bodyParts.filter((line) => this.isComment(line));
      const isComment = commentPart.length > 0;
      let header = this.getIden(identation + 1) + "return Async\\async(function ()" + params + " {\n";
      if (isComment) {
        const commentPartString = commentPart.map((c) => this.getIden(identation + 1) + c.trim()).join("\n");
        const firstStmNoComment = bodyParts.filter((line) => !this.isComment(line)).join("\n");
        header = commentPartString + "\n" + header + firstStmNoComment + "\n";
      } else {
        header += firstBodyStm + "\n";
      }
      const result = header + funcBody + "\n" + this.getIden(identation + 1) + "}) ();";
      return blockOpen + result + blockClose;
    }
    return super.printFunctionBody(node, identation);
  }
  printPropertyAccessModifiers(node) {
    const modifiers = super.printPropertyAccessModifiers(node);
    return modifiers ? modifiers : "public ";
  }
  transformLeadingComment(comment) {
    const commentRegex = [
      [/\{([\]\[\|a-zA-Z0-9_-]+?)\}/g, "~$1~"],
      // eslint-disable-line -- resolve the "arrays vs url params" conflict (both are in {}-brackets)
      [/\[([^\]\[]*)\]\{(@link .*)\}/g, "~$2 $1~"],
      // eslint-disable-line -- docstring item with link
      [/\s+\* @method/g, ""],
      // docstring @method
      [/(\s+)\* @description (.*)/g, "$1* $2"],
      // eslint-disable-line
      [/\s+\* @name .*/g, ""],
      // docstring @name
      [/(\s+)\* @returns/g, "$1* @return"],
      // eslint-disable-line
      [/\~([\]\[\|@\.\s+\:\/#\-a-zA-Z0-9_-]+?)\~/g, "{$1}"],
      // eslint-disable-line -- resolve the "arrays vs url params" conflict (both are in {}-brackets)
      [/(\s+ \* @(param|return) {[^}]*)object([^}]*}.*)/g, "$1array$3"]
      // docstring type conversion
    ];
    const transformed = regexAll(comment, commentRegex);
    return transformed;
  }
  initConfig() {
    this.LeftPropertyAccessReplacements = {
      "this": "$this"
    };
    this.RightPropertyAccessReplacements = {};
    this.FullPropertyAccessReplacements = {
      "Number.MAX_SAFE_INTEGER": "PHP_INT_MAX",
      "JSON.stringify": "json_encode",
      "console.log": "var_dump",
      "process.exit": "exit",
      "Math.log": "log",
      "Math.abs": "abs",
      "Math.floor": "(int) floor",
      "Math.ceil": "(int) ceil",
      "Math.round": "(int) round",
      "Math.pow": "pow",
      "Math.min": "min",
      "Math.max": "max"
      // 'Promise.all': '\\React\\Promise\\all',
    };
    this.CallExpressionReplacements = {
      "parseFloat": "floatval",
      "parseInt": "intval"
    };
    this.PropertyAccessRequiresParenthesisRemoval = [
      // 'length',
      // 'toString',
      // 'toUpperCase',
      // 'toLowerCase',
      // 'pop',
      // 'reverse',
      // 'shift',
    ];
  }
};

// src/csharpTranspiler.ts
init_cjs_shims();

var parserConfig3 = {
  "ELSEIF_TOKEN": "else if",
  "OBJECT_OPENING": "new Dictionary<string, object>() {",
  "ARRAY_OPENING_TOKEN": "new List<object>() {",
  "ARRAY_CLOSING_TOKEN": "}",
  "PROPERTY_ASSIGNMENT_TOKEN": ",",
  "VAR_TOKEN": "object",
  // object
  "METHOD_TOKEN": "",
  "PROPERTY_ASSIGNMENT_OPEN": "{",
  "PROPERTY_ASSIGNMENT_CLOSE": "}",
  "SUPER_TOKEN": "base",
  "SUPER_CALL_TOKEN": "base",
  "FALSY_WRAPPER_OPEN": "isTrue(",
  "FALSY_WRAPPER_CLOSE": ")",
  "COMPARISON_WRAPPER_OPEN": "isEqual(",
  "COMPARISON_WRAPPER_CLOSE": ")",
  "UKNOWN_PROP_WRAPPER_OPEN": "this.call(",
  "UNKOWN_PROP_WRAPPER_CLOSE": ")",
  "UKNOWN_PROP_ASYNC_WRAPPER_OPEN": "this.callAsync(",
  "UNKOWN_PROP_ASYNC_WRAPPER_CLOSE": ")",
  "DYNAMIC_CALL_OPEN": "callDynamically(",
  "EQUALS_EQUALS_WRAPPER_OPEN": "isEqual(",
  "EQUALS_EQUALS_WRAPPER_CLOSE": ")",
  "DIFFERENT_WRAPPER_OPEN": "!isEqual(",
  "DIFFERENT_WRAPPER_CLOSE": ")",
  "GREATER_THAN_WRAPPER_OPEN": "isGreaterThan(",
  "GREATER_THAN_WRAPPER_CLOSE": ")",
  "GREATER_THAN_EQUALS_WRAPPER_OPEN": "isGreaterThanOrEqual(",
  "GREATER_THAN_EQUALS_WRAPPER_CLOSE": ")",
  "LESS_THAN_WRAPPER_OPEN": "isLessThan(",
  "LESS_THAN_WRAPPER_CLOSE": ")",
  "LESS_THAN_EQUALS_WRAPPER_OPEN": "isLessThanOrEqual(",
  "LESS_THAN_EQUALS_WRAPPER_CLOSE": ")",
  "PLUS_WRAPPER_OPEN": "add(",
  "PLUS_WRAPPER_CLOSE": ")",
  "MINUS_WRAPPER_OPEN": "subtract(",
  "MINUS_WRAPPER_CLOSE": ")",
  "ARRAY_LENGTH_WRAPPER_OPEN": "getArrayLength(",
  "ARRAY_LENGTH_WRAPPER_CLOSE": ")",
  "DIVIDE_WRAPPER_OPEN": "divide(",
  "DIVIDE_WRAPPER_CLOSE": ")",
  "MULTIPLY_WRAPPER_OPEN": "multiply(",
  "MULTIPLY_WRAPPER_CLOSE": ")",
  "INDEXOF_WRAPPER_OPEN": "getIndexOf(",
  "INDEXOF_WRAPPER_CLOSE": ")",
  "MOD_WRAPPER_OPEN": "mod(",
  "MOD_WRAPPER_CLOSE": ")",
  "FUNCTION_TOKEN": "",
  "INFER_VAR_TYPE": false,
  "INFER_ARG_TYPE": false
};
var CSHARP_METHOD_RETURN_TYPES = {
  "toUpperCase": "string",
  "toLowerCase": "string",
  "toString": "string",
  "trim": "string",
  "join": "string",
  "replace": "string",
  "replaceAll": "string",
  "split": "List<object>",
  "startsWith": "bool",
  "endsWith": "bool",
  "indexOf": "int",
  "search": "int"
};
var CSHARP_STATIC_RETURN_TYPES = {
  "Object.keys": "List<object>",
  "Object.values": "List<object>",
  "Object.entries": "List<object>",
  "JSON.stringify": "string",
  "Math.floor": "double",
  "Math.ceil": "double",
  "Math.round": "double",
  "Array.isArray": "bool",
  "Number.isInteger": "bool"
};
var CSHARP_NATIVE_THIS_DELEGATE_ARITY = {
  "proxyUrlCallback": 3,
  "proxy_url_callback": 3,
  "httpProxyCallback": 4,
  "http_proxy_callback": 4,
  "httpsProxyCallback": 4,
  "https_proxy_callback": 4,
  "socksProxyCallback": 3,
  "socks_proxy_callback": 3
};
var CSHARP_THIS_RETURN_TYPES = {
  "extend": "Dictionary<string, object>",
  "deepExtend": "Dictionary<string, object>",
  "indexBy": "Dictionary<string, object>",
  "groupBy": "Dictionary<string, object>",
  "milliseconds": "Int64",
  "seconds": "Int64",
  "microseconds": "Int64",
  "uuid": "string",
  "hmac": "string",
  "capitalize": "string",
  "ymdhms": "string",
  "yyyymmdd": "string",
  "json": "string",
  "inArray": "bool",
  "valueIsDefined": "bool",
  // the safe* accessor family: cs/ccxt/base already declares these with a concrete
  // return type (`string? safeString(...)`, `Int64? safeInteger(...)`, `bool? safeBool(...)`,
  // `IDictionary<string, object> safeDict(...)`, `List<object> safeList(...)`, ...), so a
  // local initialised by one of them already holds that type inside its `object` box
  "safeString": "string?",
  "safeString2": "string?",
  "safeStringN": "string?",
  "safeStringLower": "string?",
  "safeStringLower2": "string?",
  "safeStringLowerN": "string?",
  "safeStringUpper": "string?",
  "safeStringUpper2": "string?",
  "safeStringUpperN": "string?",
  "safeCurrencyCode": "string?",
  "safeInteger": "Int64?",
  "safeInteger2": "Int64?",
  "safeIntegerN": "Int64?",
  "safeIntegerProduct": "Int64?",
  "safeFloat": "double?",
  "safeFloat2": "double?",
  "safeFloatN": "double?",
  "safeNumberN": "double?",
  "safeBool": "bool?",
  "safeBool2": "bool?",
  "safeBoolN": "bool?",
  "safeDict": "IDictionary<string, object>",
  "safeDict2": "IDictionary<string, object>",
  "safeDictN": "IDictionary<string, object>",
  "safeList": "List<object>",
  "safeList2": "List<object>",
  "safeListN": "List<object>"
};
var CSHARP_NATIVE_NUMERIC_THIS_KINDS = {
  "precisionFromString": "int",
  "parseToInt": "Int64?"
};
var CSHARP_SAFE_ACCESSOR_NAMES = [
  "safeString",
  "safeString2",
  "safeStringN",
  "safeStringLower",
  "safeStringLower2",
  "safeStringLowerN",
  "safeStringUpper",
  "safeStringUpper2",
  "safeStringUpperN",
  "safeCurrencyCode",
  "safeInteger",
  "safeInteger2",
  "safeIntegerN",
  "safeIntegerProduct",
  "safeFloat",
  "safeFloat2",
  "safeFloatN",
  "safeNumberN",
  "safeBool",
  "safeBool2",
  "safeBoolN",
  "safeDict",
  "safeDict2",
  "safeDictN",
  "safeList",
  "safeList2",
  "safeListN"
];
var CSHARP_BOOLEAN_PRINTED_CALLS = [
  "isTrue",
  "isEqual",
  "isGreaterThan",
  "isGreaterThanOrEqual",
  "isLessThan",
  "isLessThanOrEqual",
  "inOp"
];
var CSHARP_TYPE_NAMES = ["string", "bool", "int", "long", "Int64", "double", "object", "List", "IList", "Dictionary", "IDictionary", "var"];
var GUARD_KEY_SEPARATOR = "\0";
var CSHARP_NUMERIC_KINDS = ["int", "Int64", "double"];
var CSHARP_NUMERIC_VALUE_KINDS = ["int", "Int64", "Int64?", "double", "double?"];
var CSHARP_INTEGER_KINDS = ["int", "Int64"];
var CSHARP_NATIVE_COMPARISON_TOKENS = {
  [_typescript2.default.SyntaxKind.LessThanToken]: "<",
  [_typescript2.default.SyntaxKind.GreaterThanToken]: ">",
  [_typescript2.default.SyntaxKind.LessThanEqualsToken]: "<=",
  [_typescript2.default.SyntaxKind.GreaterThanEqualsToken]: ">="
};
var CSHARP_MINMAX_NATIVE_KINDS = ["int", "Int64"];
var CSHARP_ASSIGNMENT_OPERATOR_KINDS = [
  _typescript2.default.SyntaxKind.EqualsToken,
  _typescript2.default.SyntaxKind.PlusEqualsToken,
  _typescript2.default.SyntaxKind.MinusEqualsToken,
  _typescript2.default.SyntaxKind.AsteriskEqualsToken,
  _typescript2.default.SyntaxKind.AsteriskAsteriskEqualsToken,
  _typescript2.default.SyntaxKind.SlashEqualsToken,
  _typescript2.default.SyntaxKind.PercentEqualsToken,
  _typescript2.default.SyntaxKind.LessThanLessThanEqualsToken,
  _typescript2.default.SyntaxKind.GreaterThanGreaterThanEqualsToken,
  _typescript2.default.SyntaxKind.GreaterThanGreaterThanGreaterThanEqualsToken,
  _typescript2.default.SyntaxKind.AmpersandEqualsToken,
  _typescript2.default.SyntaxKind.BarEqualsToken,
  _typescript2.default.SyntaxKind.CaretEqualsToken,
  _typescript2.default.SyntaxKind.BarBarEqualsToken,
  _typescript2.default.SyntaxKind.AmpersandAmpersandEqualsToken,
  _typescript2.default.SyntaxKind.QuestionQuestionEqualsToken
];
var CSHARP_NATIVE_FIELDS = {
  "options": "ConcurrentDictionary<string, object>",
  "has": "Dictionary<string, object>",
  "features": "Dictionary<string, object>",
  "httpExceptions": "Dictionary<string, object>",
  "markets_by_id": "IDictionary<string, object>",
  "symbols": "List<object>",
  "codes": "List<object>",
  "ids": "List<object>"
};
var CSHARP_OBJECT_DICT_FIELDS = ["urls", "tickers", "bidsasks", "orderbooks", "ohlcvs", "trades", "markets", "currencies", "currencies_by_id", "exceptions"];
var CSHARP_REFERENCE_FIELDS_NATIVE = [
  "id",
  "hostname",
  "apiKey",
  "secret",
  "password",
  "uid",
  "accountId",
  "login",
  "privateKey",
  "walletAddress",
  "twofa",
  "proxy",
  "proxyUrl",
  "proxy_url",
  "proxyUrlCallback",
  "proxy_url_callback",
  "last_http_response",
  "markets",
  "markets_by_id",
  "features",
  "tickers",
  "bidsasks",
  "ohlcvs",
  "trades",
  "orders",
  "myTrades",
  "positions",
  "liquidations",
  "balance",
  "accounts",
  "currencies",
  "currencies_by_id",
  "outcomes",
  "outcomes_by_id",
  "events",
  "events_by_slug",
  "clients",
  "ids",
  "tokenBucket"
];
var CSHARP_NATIVE_BOOL_FIELDS = [
  "alias",
  "certified",
  "enableRateLimit",
  "isSandboxModeEnabled",
  "newUpdates",
  "pro",
  "reduceFees",
  "reloadingMarkets",
  "returnResponseHeaders",
  "substituteCommonCurrencyCodes",
  "verbose"
];
var CSHARP_MISSING_KEY_FIELDS_NATIVE = ["has", "options", "urls", "markets", "exceptions"];
var CSHARP_NATIVE_COLLECTION_TYPES = ["List<object>", "IList<object>", "Dictionary<string, object>", "IDictionary<string, object>"];
var CSHARP_SCALAR_ELEMENT_BOOL = 1;
var CSHARP_SCALAR_ELEMENT_STRING = 2;
var CSHARP_NATIVE_DICTIONARY_TYPES = ["Dictionary<string, object>", "IDictionary<string, object>"];
var CSHARP_NATIVE_MARKET_RECEIVERS = ["market", "currency"];
var CSHARP_BOOL_CALLEES_NATIVE = {
  "this.isEmpty": true,
  "this.isJsonEncodedObject": true,
  "this.isBinaryMessage": true,
  "Precise.stringGt": true,
  "Precise.stringGe": true,
  "Precise.stringLt": true,
  "Precise.stringLe": true,
  "Precise.stringEq": true,
  "Precise.stringEquals": true
};
var CSHARP_HANDWRITTEN_CALLEES_NATIVE = ["isDictionary"];
var CSHARP_COUNT_TYPES = ["List<", "IList<", "Dictionary<", "IDictionary<", "ConcurrentDictionary<"];
var CSHARP_DECIMAL_LITERAL = /^[0-9]+(\.[0-9]+)?$/;
function csharpDoubleLiteral(value) {
  if (!Number.isFinite(value)) {
    return void 0;
  }
  const text = String(value);
  return text.indexOf(".") >= 0 || text.indexOf("e") >= 0 ? text : text + ".0";
}
function csharpLiteralNumericValue(node) {
  if (_typescript2.default.isNumericLiteral(node)) {
    return CSHARP_DECIMAL_LITERAL.test(node.text) ? Number(node.text) : void 0;
  }
  if (_typescript2.default.isPrefixUnaryExpression(node) && node.operator === _typescript2.default.SyntaxKind.MinusToken && _typescript2.default.isNumericLiteral(node.operand) && CSHARP_DECIMAL_LITERAL.test(node.operand.text)) {
    return -Number(node.operand.text);
  }
  return void 0;
}
function csharpParseIntLiteralArgument(arg) {
  if (_typescript2.default.isStringLiteral(arg)) {
    if (!/^[0-9]+$/.test(arg.text)) {
      return void 0;
    }
    const value = Number(arg.text);
    return Number.isSafeInteger(value) ? `${value}L` : void 0;
  }
  const numeric = csharpLiteralNumericValue(arg);
  if (numeric === void 0) {
    return void 0;
  }
  const floored = Math.floor(numeric);
  if (!Number.isSafeInteger(floored)) {
    return void 0;
  }
  return floored < 0 ? `(${floored}L)` : `${floored}L`;
}
function csharpParseFloatLiteralArgument(arg) {
  if (_typescript2.default.isStringLiteral(arg)) {
    return CSHARP_DECIMAL_LITERAL.test(arg.text) ? csharpDoubleLiteral(Number(arg.text)) : void 0;
  }
  const numeric = csharpLiteralNumericValue(arg);
  return numeric === void 0 ? void 0 : csharpDoubleLiteral(numeric);
}
var CSHARP_MARKET_RECEIVER_NAMES = ["market", "currency"];
var CSHARP_OVERRIDE_PARAM_TYPES = {
  ethRpc: { 2: "IList<object>" },
  parsePredictionOpenInterest: { 0: "IDictionary<string, object>" },
  signEvmTransaction: { 0: "IDictionary<string, object>" }
};
var CSHARP_LENGTH_CAST = /^\(([A-Za-z_][\w.]*(?:<[^<>]*(?:<[^<>]*>)?[^<>]*>)?)\)/;
var CSHARP_NULL_COMPARISON_REFERENCE_HEADS = ["string", "IDictionary<", "Dictionary<", "IList<", "List<", "ConcurrentDictionary<", "ccxt.pro.", "ArrayCache", "IOrderBook", "Future", "WebSocketClient", "Delegates"];
var CSharpTranspiler = class extends BaseTranspiler {
  constructor(config = {}) {
    config["parser"] = Object.assign({}, parserConfig3, _nullishCoalesce(config["parser"], () => ( {})));
    super(config);
    // method node -> 'bool' | 'bool?' | undefined (see csharpBooleanReturnType)
    this.csharpBooleanReturnTypes = /* @__PURE__ */ new WeakMap();
    // variable declaration -> getCSharpLocalType result, shared by the condition checks
    this.csharpLocalTypes = /* @__PURE__ */ new WeakMap();
    // method node -> `key in recv` guards of that method, keyed by receiver text + key
    this.csharpGuardIndex = /* @__PURE__ */ new WeakMap();
    // variable declaration -> the concrete C# type this printer named for it
    // (getCSharpLocalType): 'List<object>' / 'Dictionary<string, object>' / 'string' / ...
    // Only declarations the printer typed itself are kept: the printed `<type> name = `
    // prefix is final, so every later read of the local is statically that type and its
    // members may replace inOp/getArrayLength
    this.csharpTypedLocals = /* @__PURE__ */ new WeakMap();
    // ws handler `message` parameter -> the typed signature this printer prints for it (D-19)
    this.csharpHandlerMessageTypes = /* @__PURE__ */ new WeakMap();
    // method declaration -> a static call reference exists (a call binds its arguments)
    this.csharpHandlerCalled = /* @__PURE__ */ new WeakMap();
    // class declaration -> the class routes the raw message through a list test
    this.csharpListRouteClasses = /* @__PURE__ */ new WeakMap();
    // source file -> method symbol -> a static call reference exists (built once per file: a
    // sticky batch program is shared by every file of the stage, so a program-keyed index
    // would answer for another venue's class)
    this.csharpHandlerCallIndex = /* @__PURE__ */ new WeakMap();
    // parameter node -> the type the printed signature gives it (printParameterType, e.g.
    // `Dict` -> Dictionary<string, object>), recorded as the signature is printed. Read-only:
    // no printer rule consults it, see csharpPrintedParamType
    this.csharpParamTypes = /* @__PURE__ */ new WeakMap();
    // declaration node -> C# type of the local ('' = the printer prints `object`); see
    // csharpStringReceiverType -- the ccxt classifier asks once per string-method receiver
    this.stringReceiverTypes = /* @__PURE__ */ new WeakMap();
    // declaration node -> 'bool' | 'bool?' | '' (the printer cannot name it); see
    // csharpConditionOperandType — asked once per condition operand
    this.conditionOperandTypes = /* @__PURE__ */ new WeakMap();
    this.csModifiers = {};
    this.requiresParameterType = true;
    this.requiresReturnType = true;
    this.asyncTranspiling = true;
    this.implicitAsyncTranspiling = true;
    this.supportsFalsyOrTruthyValues = false;
    this.requiresCallExpressionCast = true;
    this.id = "C#";
    this.initConfig();
    this.applyUserOverrides(config);
  }
  initConfig() {
    this.LeftPropertyAccessReplacements = {
      // 'this': '$this',
    };
    this.RightPropertyAccessReplacements = {
      "push": "Add",
      // list method
      "indexOf": "IndexOf",
      // list method
      "toUpperCase": "ToUpper",
      "toLowerCase": "ToLower",
      "toString": "ToString"
    };
    this.FullPropertyAccessReplacements = {
      "JSON.parse": "parseJson",
      // custom helper method
      "console.log": "Console.WriteLine",
      "Number.MAX_SAFE_INTEGER": "Int32.MaxValue",
      "Math.min": "Math.Min",
      "Math.max": "Math.Max",
      "Math.log": "Math.Log",
      "Math.abs": "Math.Abs",
      // 'Math.ceil':  'Math.Ceiling', // need cast
      // 'Math.round': 'Math.Round', // need to cast
      "Math.floor": "Math.Floor",
      "Math.pow": "Math.Pow"
      // 'Promise.all': 'Task.WhenAll',
    };
    this.CallExpressionReplacements = {
      // "parseInt": "parseINt",
      // "parseFloat": "float.Parse",
    };
    this.ReservedKeywordsReplacements = {
      "string": "str",
      "object": "obj",
      "params": "parameters",
      "base": "bs",
      "internal": "intern",
      "event": "eventVar",
      "fixed": "fixedVar"
    };
    this.VariableTypeReplacements = {
      "string": "string",
      "Str": "string",
      "number": "double",
      "Int": "Int64",
      "Num": "double",
      "Dict": "Dictionary<string, object>",
      "Strings": "List<string>",
      "List": "List<object>",
      "boolean": "bool"
    };
    this.ArgTypeReplacements = {
      "string": "string",
      "Str": "string",
      "number": "double",
      "Int": "Int64",
      "Num": "double",
      "Dict": "Dictionary<string, object>",
      "Strings": "List<string>",
      "List": "List<object>",
      "boolean": "bool"
    };
    this.binaryExpressionsWrappers = {
      [_typescript2.default.SyntaxKind.EqualsEqualsToken]: [this.EQUALS_EQUALS_WRAPPER_OPEN, this.EQUALS_EQUALS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.EqualsEqualsEqualsToken]: [this.EQUALS_EQUALS_WRAPPER_OPEN, this.EQUALS_EQUALS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.ExclamationEqualsToken]: [this.DIFFERENT_WRAPPER_OPEN, this.DIFFERENT_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken]: [this.DIFFERENT_WRAPPER_OPEN, this.DIFFERENT_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.GreaterThanToken]: [this.GREATER_THAN_WRAPPER_OPEN, this.GREATER_THAN_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.GreaterThanEqualsToken]: [this.GREATER_THAN_EQUALS_WRAPPER_OPEN, this.GREATER_THAN_EQUALS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.LessThanToken]: [this.LESS_THAN_WRAPPER_OPEN, this.LESS_THAN_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.LessThanEqualsToken]: [this.LESS_THAN_EQUALS_WRAPPER_OPEN, this.LESS_THAN_EQUALS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.PlusToken]: [this.PLUS_WRAPPER_OPEN, this.PLUS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.MinusToken]: [this.MINUS_WRAPPER_OPEN, this.MINUS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.AsteriskToken]: [this.MULTIPLY_WRAPPER_OPEN, this.MULTIPLY_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.PercentToken]: [this.MOD_WRAPPER_OPEN, this.MOD_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.SlashToken]: [this.DIVIDE_WRAPPER_OPEN, this.DIVIDE_WRAPPER_CLOSE]
    };
  }
  getBlockOpen(identation) {
    return "\n" + this.getIden(identation) + this.BLOCK_OPENING_TOKEN + "\n";
  }
  printSuperCallInsideConstructor(node, identation) {
    return "";
  }
  printIdentifier(node) {
    let idValue = _nullishCoalesce(node.text, () => ( node.escapedText));
    if (this.ReservedKeywordsReplacements[idValue]) {
      idValue = this.ReservedKeywordsReplacements[idValue];
    }
    if (idValue === "undefined") {
      return this.UNDEFINED_TOKEN;
    }
    const type = this.getChecker().getTypeAtLocation(node);
    const symbol = _optionalChain([type, 'optionalAccess', _58 => _58.symbol]);
    if (symbol !== void 0) {
      const decl = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _59 => _59.declarations]), () => ( []));
      let isBuiltIn = void 0;
      if (decl.length > 0) {
        isBuiltIn = decl[0].getSourceFile().fileName.indexOf("typescript") > -1;
      }
      if (isBuiltIn !== void 0 && !isBuiltIn) {
        const isInsideNewExpression = _optionalChain([node, 'optionalAccess', _60 => _60.parent, 'optionalAccess', _61 => _61.kind]) === _typescript2.default.SyntaxKind.NewExpression;
        const isInsideCatch = _optionalChain([node, 'optionalAccess', _62 => _62.parent, 'optionalAccess', _63 => _63.kind]) === _typescript2.default.SyntaxKind.ThrowStatement;
        const isLeftSide = _optionalChain([node, 'optionalAccess', _64 => _64.parent, 'optionalAccess', _65 => _65.name]) === node || _optionalChain([node, 'optionalAccess', _66 => _66.parent, 'optionalAccess', _67 => _67.left]) === node;
        const isCallOrPropertyAccess = _optionalChain([node, 'optionalAccess', _68 => _68.parent, 'optionalAccess', _69 => _69.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([node, 'optionalAccess', _70 => _70.parent, 'optionalAccess', _71 => _71.kind]) === _typescript2.default.SyntaxKind.ElementAccessExpression;
        if (!isLeftSide && !isCallOrPropertyAccess && !isInsideCatch && !isInsideNewExpression) {
          const symbol2 = this.getChecker().getSymbolAtLocation(node);
          let isClassDeclaration = false;
          if (symbol2) {
            const first = symbol2.declarations[0];
            if (first.kind === _typescript2.default.SyntaxKind.ClassDeclaration) {
              isClassDeclaration = true;
            }
            if (first.kind === _typescript2.default.SyntaxKind.ImportSpecifier) {
              const importedSymbol = this.getChecker().getAliasedSymbol(symbol2);
              if (_optionalChain([importedSymbol, 'optionalAccess', _72 => _72.declarations, 'access', _73 => _73[0], 'optionalAccess', _74 => _74.kind]) === _typescript2.default.SyntaxKind.ClassDeclaration) {
                isClassDeclaration = true;
              }
            }
          }
          if (isClassDeclaration) {
            return `typeof(${idValue})`;
          }
        }
      }
    }
    return this.transformIdentifier(node, idValue);
  }
  printConstructorDeclaration(node, identation) {
    const classNode = node.parent;
    const className = this.printNode(classNode.name, 0);
    const args = this.printMethodParameters(node);
    const constructorBody = this.printFunctionBody(node, identation);
    let superCallParams = "";
    let hasSuperCall = false;
    _optionalChain([node, 'access', _75 => _75.body, 'optionalAccess', _76 => _76.statements, 'access', _77 => _77.forEach, 'call', _78 => _78((statement) => {
      if (_typescript2.default.isExpressionStatement(statement)) {
        const expression = statement.expression;
        if (_typescript2.default.isCallExpression(expression)) {
          const expressionText = expression.expression.getText().trim();
          if (expressionText === "super") {
            hasSuperCall = true;
            superCallParams = expression.arguments.map((a) => {
              return this.printNode(a, identation).trim();
            }).join(", ");
          }
        }
      }
    })]);
    if (hasSuperCall) {
      return this.getIden(identation) + className + `(${args}) : ${this.SUPER_CALL_TOKEN}(${superCallParams})` + constructorBody;
    }
    return this.getIden(identation) + className + "(" + args + ")" + constructorBody;
  }
  printThisElementAccesssIfNeeded(node, identation) {
    const isAsync = true;
    const elementAccess = node.expression;
    if (_optionalChain([elementAccess, 'optionalAccess', _79 => _79.kind]) === _typescript2.default.SyntaxKind.ElementAccessExpression) {
      if (_optionalChain([elementAccess, 'optionalAccess', _80 => _80.expression, 'optionalAccess', _81 => _81.kind]) === _typescript2.default.SyntaxKind.ThisKeyword) {
        let parsedArg = _optionalChain([node, 'access', _82 => _82.arguments, 'optionalAccess', _83 => _83.length]) > 0 ? this.printNode(node.arguments[0], identation).trimStart() : "";
        const propName = this.printNode(elementAccess.argumentExpression, 0);
        const wrapperOpen = isAsync ? this.UKNOWN_PROP_ASYNC_WRAPPER_OPEN : this.UKNOWN_PROP_WRAPPER_OPEN;
        const wrapperClose = isAsync ? this.UNKOWN_PROP_ASYNC_WRAPPER_CLOSE : this.UNKOWN_PROP_WRAPPER_CLOSE;
        parsedArg = parsedArg ? ", " + parsedArg : "";
        return wrapperOpen + propName + parsedArg + wrapperClose;
      }
    }
    return;
  }
  printDynamicCall(node, identation) {
    const isAsync = true;
    const elementAccess = node.expression;
    if (_optionalChain([elementAccess, 'optionalAccess', _84 => _84.kind]) === _typescript2.default.SyntaxKind.ElementAccessExpression) {
      const parsedArg = _optionalChain([node, 'access', _85 => _85.arguments, 'optionalAccess', _86 => _86.length]) > 0 ? node.arguments.map((n) => this.printNode(n, identation).trimStart()).join(", ") : "";
      const target = this.printNode(elementAccess.expression, 0);
      const propName = this.printNode(elementAccess.argumentExpression, 0);
      const argsArray = `new object[] { ${parsedArg} }`;
      const open = this.DYNAMIC_CALL_OPEN;
      let statement = `${open}${target}, ${propName}, ${argsArray})`;
      statement = isAsync ? `((Task<object>)${statement})` : statement;
      return statement;
    }
    return void 0;
  }
  // The C# declared type of an expression's receiver (`request` in `request["k"] = v`), as
  // the consumer's classifier knows it — ccxt's build/csharp-local-types.js overrides this
  // with its scope-aware local map. Undefined on an unpatched printer
  csharpDeclaredReceiverType(node) {
    return void 0;
  }
  // The C# type the printed SIGNATURE gives a parameter (`Dictionary<string, object>` for `Dict`,
  // `object` when unnamed), recorded while printing so a body use can ask. Read-only: no printer
  // rule consults it; only the consumer's csharpDeclaredReceiverType override changes emission.
  csharpPrintedParamType(receiver) {
    const declaration = this.csharpReceiverBinding(receiver);
    if (_optionalChain([declaration, 'optionalAccess', _87 => _87.kind]) !== _typescript2.default.SyntaxKind.Parameter) {
      return void 0;
    }
    const recorded = this.csharpParamTypes.get(declaration);
    return recorded === void 0 || recorded === "" || recorded === "object" ? void 0 : recorded;
  }
  // A receiver whose declared C# type the consumer's classifier names as a concrete dictionary:
  // the `((IDictionary<string,object>)x)` cast around element access is an identity conversion.
  // Gated entirely on the consumer's hook, so an object receiver and untyped runs keep the cast.
  csharpReceiverIsDeclaredDictionary(expression) {
    const declared = this.csharpDeclaredReceiverType(expression);
    return declared === "Dictionary<string, object>" || declared === "IDictionary<string, object>";
  }
  // A dict element WRITE (`request["k"] = v`) on a receiver whose *declared* C# type is already a
  // dictionary needs no `((IDictionary<string,object>)…)` cast: both indexers are the same setter.
  // Gated entirely on csharpDeclaredReceiverType, so object receivers and untyped runs keep the cast.
  csharpDictionaryElementWriteTarget(node) {
    const parent = _optionalChain([node, 'optionalAccess', _88 => _88.parent]);
    const isWrite = _optionalChain([parent, 'optionalAccess', _89 => _89.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && parent.operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken && parent.left === node;
    if (!isWrite || !this.ELEMENT_ACCESS_WRAPPER_OPEN || !this.ELEMENT_ACCESS_WRAPPER_CLOSE) {
      return void 0;
    }
    const { expression, argumentExpression } = node;
    const declared = this.csharpDeclaredReceiverType(expression);
    if (declared !== "Dictionary<string, object>" && declared !== "IDictionary<string, object>") {
      return void 0;
    }
    const keyType = this.getChecker().getTypeAtLocation(argumentExpression);
    const members = keyType.flags === _typescript2.default.TypeFlags.Union ? _nullishCoalesce(keyType.types, () => ( [keyType])) : [keyType];
    const stringKey = keyType.flags === _typescript2.default.TypeFlags.Any || members.some((t) => this.isStringType(t.flags));
    if (!stringKey) {
      return void 0;
    }
    const cast = _typescript2.default.isStringLiteralLike(argumentExpression) ? "" : "(string)";
    return this.printNode(expression, 0) + "[" + cast + this.printNode(argumentExpression, 0) + "]";
  }
  printElementAccessExpressionExceptionIfAny(node) {
  }
  // reads print getValue(recv, key), which yields null for a missing key; a C# indexer
  // throws instead, so the native form is only emitted where the source guarantees the key
  // is there: a dominating `key in recv` guard, or a receiver local built by a literal that
  // declares the key, or a receiver the declared table proves is a dictionary (the native
  // form then keeps the helper's null-for-a-missing-key result). every other read keeps the
  // helper.
  // the single element-access override, gates strongest (declaration-type) proof first:
  // S21 typed dict WRITE (csharpDictionaryElementWriteTarget), S63 typed dict READ twin
  // (printTypedDictElementAccessIfAny), S14 list-cast (csharpElementAccessReceiverIsList),
  // then the native read form proven from the printed source (#82, csharpNativeElementAccess,
  // see the note above). Every other read keeps the helper; a site no gate names keeps the
  // base printing.
  printElementAccessExpression(node, identation) {
    const native = this.csharpNativeElementAccess(node);
    if (native !== void 0) {
      return native;
    }
    const dictWrite = this.csharpDictionaryElementWriteTarget(node);
    if (dictWrite !== void 0) {
      return dictWrite;
    }
    const typedRead = this.printTypedDictElementAccessIfAny(node);
    if (typedRead !== void 0) {
      return typedRead;
    }
    if (this.csharpElementAccessReceiverIsList(node)) {
      const type = this.getChecker().getTypeAtLocation(node.argumentExpression);
      const isUnion = (type.flags & _typescript2.default.TypeFlags.Union) !== 0 && Array.isArray(type.types);
      const isStringOrUnknownKey = this.isStringType(type.flags) || type.flags === _typescript2.default.TypeFlags.Any || isUnion && type.types.some((t) => this.isStringType(t.flags));
      if (!isStringOrUnknownKey) {
        return `${this.printNode(node.expression, 0)}[Convert.ToInt32(${this.printNode(node.argumentExpression, 0)})]`;
      }
    }
    const listRead = this.csharpListIndexRead(node);
    if (listRead !== void 0) {
      return listRead;
    }
    return super.printElementAccessExpression(node, identation);
  }
  csharpNativeElementAccess(node) {
    if (!this.ELEMENT_ACCESS_WRAPPER_OPEN || !this.ELEMENT_ACCESS_WRAPPER_CLOSE) {
      return void 0;
    }
    const exception = this.printElementAccessExpressionExceptionIfAny(node);
    if (exception) {
      return void 0;
    }
    const { expression, argumentExpression } = node;
    const parent = node.parent;
    const isWrite = _optionalChain([parent, 'optionalAccess', _90 => _90.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && (parent.operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken || parent.operatorToken.kind === _typescript2.default.SyntaxKind.PlusEqualsToken) && parent.left === node;
    if (isWrite) {
      return void 0;
    }
    const isStringKey = _typescript2.default.isStringLiteralLike(argumentExpression);
    const isNumberKey = _typescript2.default.isNumericLiteral(argumentExpression);
    if (!isStringKey && !isNumberKey) {
      return this.csharpLoopIndexListRead(expression, argumentExpression);
    }
    const key = argumentExpression.text;
    const builtFromLiteral = this.csharpLiteralDeclaresKey(node, expression, key, isNumberKey);
    const guarded = !builtFromLiteral && this.csharpKeyPresenceGuarded(node, expression, key);
    if (!builtFromLiteral && !guarded) {
      return _nullishCoalesce(_nullishCoalesce(_nullishCoalesce(_nullishCoalesce(this.csharpNativeDeclaredDictionaryRead(expression, argumentExpression), () => ( this.csharpDeclaredLocalResolverRowRead(expression, argumentExpression))), () => ( this.csharpDeclaredCollectionRead(node, expression, argumentExpression, isStringKey, isNumberKey))), () => ( this.csharpProvenDictionaryRead(node, expression, argumentExpression, isStringKey))), () => ( this.csharpMissingKeyFieldRead(expression, argumentExpression, isStringKey)));
    }
    const receiver = this.printNode(expression, 0);
    const printedKey = this.printNode(argumentExpression, 0);
    if (isNumberKey) {
      return `((${this.ARRAY_KEYWORD})${receiver})[${printedKey}]`;
    }
    if (this.csharpReceiverIsDeclaredDictionary(expression)) {
      return `${receiver}[${printedKey}]`;
    }
    return `((IDictionary<string,object>)${receiver})[${printedKey}]`;
  }
  // `getValue (market, "lit")` / `getValue (currency, "lit")` on a row local the declared
  // table proves is a C# dictionary: the native form tests the key, so a missing key still
  // reads null exactly like the helper. An untyped receiver keeps the helper.
  csharpNativeDeclaredDictionaryRead(expression, argumentExpression) {
    if (!_typescript2.default.isIdentifier(expression) || !_typescript2.default.isStringLiteralLike(argumentExpression)) {
      return void 0;
    }
    if (CSHARP_NATIVE_MARKET_RECEIVERS.indexOf(expression.escapedText) < 0) {
      return void 0;
    }
    if (this.csharpDeclaredDictionaryType(expression) === void 0) {
      return void 0;
    }
    const receiver = this.printNode(expression, 0);
    const printedKey = this.printNode(argumentExpression, 0);
    return `(${receiver}.ContainsKey(${printedKey}) ? ${receiver}[${printedKey}] : null)`;
  }
  // the C# dictionary type the declared table names for a local read, or undefined: the
  // printer's own table, then the build layer's read proof (the row a market/currency builder
  // returned; never null on any path)
  csharpDeclaredDictionaryType(node) {
    if (_optionalChain([node, 'optionalAccess', _91 => _91.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const named = _nullishCoalesce(this.csharpTypedLocalType(node), () => ( this.csharpExpressionTypeOf(node)));
    if (named === void 0 || CSHARP_NATIVE_DICTIONARY_TYPES.indexOf(named) < 0) {
      return void 0;
    }
    return named;
  }
  // the type the embedding build layer recorded for the declaration behind a read
  // (csharpDeclaredLocalTypeResolver): the declarations it retyped itself, i.e. the parameters
  // and locals whose printed prefix is no longer what the printer's own tables say
  csharpDeclaredLocalResolverType(node) {
    if (typeof this.csharpDeclaredLocalTypeResolver !== "function") {
      return void 0;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const declaration = _optionalChain([checker, 'access', _92 => _92.getSymbolAtLocation, 'call', _93 => _93(node), 'optionalAccess', _94 => _94.valueDeclaration]);
    if (declaration === void 0) {
      return void 0;
    }
    const recorded = this.csharpDeclaredLocalTypeResolver(declaration);
    return typeof recorded === "string" ? recorded : void 0;
  }
  // the same read for a receiver only the build layer's declared-local resolver names (a
  // parameter B-17 retyped): its box may still be null, so the read carries the helper's
  // own `value2 == null -> null` branch as a null test
  csharpDeclaredLocalResolverRowRead(expression, argumentExpression) {
    if (!_typescript2.default.isIdentifier(expression) || !_typescript2.default.isStringLiteralLike(argumentExpression)) {
      return void 0;
    }
    if (CSHARP_NATIVE_MARKET_RECEIVERS.indexOf(expression.escapedText) < 0) {
      return void 0;
    }
    const recorded = this.csharpDeclaredLocalResolverType(expression);
    if (recorded === void 0 || CSHARP_NATIVE_DICTIONARY_TYPES.indexOf(recorded) < 0) {
      return void 0;
    }
    const receiver = this.printNode(expression, 0);
    const printedKey = this.printNode(argumentExpression, 0);
    return `(${receiver} != null && ${receiver}.ContainsKey(${printedKey}) ? ${receiver}[${printedKey}] : null)`;
  }
  // a read of a hand-written BaseExchange dictionary field whose key may be absent: the key
  // test plus the indexer print what the helper computes, so a missing key still reads null
  // (a bare indexer would throw). Any other receiver or a numeric key keeps the helper
  csharpMissingKeyFieldRead(expression, argumentExpression, isStringKey) {
    if (!isStringKey) {
      return void 0;
    }
    if (!_typescript2.default.isPropertyAccessExpression(expression) || expression.expression.kind !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return void 0;
    }
    const name = _optionalChain([expression, 'access', _95 => _95.name, 'optionalAccess', _96 => _96.escapedText]);
    if (CSHARP_MISSING_KEY_FIELDS_NATIVE.indexOf(name) < 0) {
      return void 0;
    }
    const field = this.printNode(expression, 0);
    const printedKey = this.printNode(argumentExpression, 0);
    if (CSHARP_OBJECT_DICT_FIELDS.indexOf(name) >= 0) {
      const receiver = `((IDictionary<string, object>)${field})`;
      return `(${field} != null && ${receiver}.ContainsKey(${printedKey}) ? ${receiver}[${printedKey}] : null)`;
    }
    return `(${field}.ContainsKey(${printedKey}) ? ${field}[${printedKey}] : null)`;
  }
  // A literal-key read on a local whose C# declaration is already a collection: no cast needed, but
  // the indexer throws where GetValue answers null, so the native form carries the helper's key/index
  // and null-receiver tests. Only an Identifier qualifies (its text is read up to three times).
  csharpDeclaredCollectionRead(node, expression, argumentExpression, isStringKey, isNumberKey) {
    if (!_typescript2.default.isIdentifier(expression)) {
      return void 0;
    }
    if (CSHARP_MARKET_RECEIVER_NAMES.indexOf(expression.escapedText) >= 0) {
      return void 0;
    }
    const csharpType = this.csharpDeclaredCollectionType(expression);
    if (csharpType === void 0) {
      return void 0;
    }
    const func = this.csharpEnclosingFunction(node);
    if (func === void 0 || this.csharpReceiverIsRewritten(func, expression)) {
      return void 0;
    }
    const receiver = this.printNode(expression, 0);
    if (csharpType.indexOf("Dictionary<") >= 0) {
      if (!isStringKey) {
        return void 0;
      }
      const printedKey = this.printNode(argumentExpression, 0);
      return `(${receiver} != null && ${receiver}.ContainsKey(${printedKey}) ? ${receiver}[${printedKey}] : null)`;
    }
    if (isNumberKey) {
      const index = Number(argumentExpression.text);
      if (!Number.isInteger(index) || index < 0) {
        return void 0;
      }
      return `(${receiver} != null && ${index} < ${receiver}.Count ? ${receiver}[${index}] : null)`;
    }
    return void 0;
  }
  // The C# collection type a local read was DECLARED with: printed by this printer, or recorded by
  // embedding build layer for a declaration it retyped (csharpDeclaredLocalTypeResolver). The
  // value-type oracle does not qualify: the declaration may still be `object` and not compile.
  csharpDeclaredCollectionType(expression) {
    const named = this.csharpTypedLocalType(expression);
    if (named !== void 0 && CSHARP_NATIVE_COLLECTION_TYPES.indexOf(named) >= 0) {
      return named;
    }
    if (this.csharpDeclaredLocalTypeResolver === void 0) {
      return void 0;
    }
    const declaration = _optionalChain([this, 'access', _97 => _97.getChecker, 'call', _98 => _98(), 'access', _99 => _99.getSymbolAtLocation, 'call', _100 => _100(expression), 'optionalAccess', _101 => _101.valueDeclaration]);
    if (declaration === void 0) {
      return void 0;
    }
    const recorded = this.csharpDeclaredLocalTypeResolver(declaration);
    return recorded !== void 0 && CSHARP_NATIVE_COLLECTION_TYPES.indexOf(recorded) >= 0 ? recorded : void 0;
  }
  // A literal-key read on a local still declared `object` but whose box the build layer's value-type
  // oracle (build/csharp-local-types.js) proves a dictionary: the cast makes the read compile, and
  // key test plus null guard return exactly what the helper does. Sibling arms are filtered first.
  csharpProvenDictionaryRead(node, expression, argumentExpression, isStringKey) {
    if (!isStringKey || !_typescript2.default.isIdentifier(expression)) {
      return void 0;
    }
    if (CSHARP_MARKET_RECEIVER_NAMES.indexOf(expression.escapedText) >= 0) {
      return void 0;
    }
    if (this.csharpTypedLocalType(expression) !== void 0) {
      return void 0;
    }
    const proven = this.csharpExpressionTypeOf(expression);
    if (proven === void 0 || CSHARP_NATIVE_DICTIONARY_TYPES.indexOf(proven) < 0) {
      return void 0;
    }
    const func = this.csharpEnclosingFunction(node);
    if (func === void 0 || this.csharpReceiverIsRewritten(func, expression)) {
      return void 0;
    }
    const local = this.printNode(expression, 0);
    const receiver = `((IDictionary<string, object>)${local})`;
    const printedKey = this.printNode(argumentExpression, 0);
    return `(${local} != null && ${receiver}.ContainsKey(${printedKey}) ? ${receiver}[${printedKey}] : null)`;
  }
  // the read sits in a branch that a `key in recv` guard admitted: same then-branch as the
  // guard, the else-branch of a negated guard, or after an early-exiting `if (!(key in recv))`
  csharpKeyPresenceGuarded(node, expression, key) {
    const func = this.csharpEnclosingFunction(node);
    if (func === void 0 || this.csharpEnclosingFunction(node) !== func) {
      return false;
    }
    const guards = this.csharpInGuardsOf(func).get(expression.getText() + GUARD_KEY_SEPARATOR + key);
    if (guards === void 0 || !this.csharpReceiverIsDictionaryLike(expression, key)) {
      return false;
    }
    if (this.csharpReceiverIsRewritten(func, expression)) {
      return false;
    }
    if (this.csharpHasKeyRemoval(func, expression, key)) {
      return false;
    }
    for (const guard of guards) {
      if (this.csharpGuardAdmitsRead(guard, node)) {
        return true;
      }
    }
    return false;
  }
  // `recv[i]` where `i` is the counter of an enclosing `for (...; i < recv.length; ...)`: that
  // condition is the range proof, so the helper's out-of-range null branch is unreachable. Both
  // operands must print as the indexer's C# types (object-element list, `int`); else keep the helper.
  csharpLoopIndexListRead(expression, argumentExpression) {
    if (!_typescript2.default.isIdentifier(expression) || !_typescript2.default.isIdentifier(argumentExpression)) {
      return void 0;
    }
    const receiverType = this.csharpExpressionTypeOf(expression);
    if (receiverType === void 0 || !this.csharpTypeIsList(receiverType)) {
      return void 0;
    }
    if (this.csharpExpressionTypeOf(argumentExpression) !== "int") {
      return void 0;
    }
    const loop = this.csharpCounterRangeLoop(argumentExpression, expression);
    if (loop === void 0) {
      return void 0;
    }
    if (!this.csharpCounterUnwrittenIn(loop.statement, argumentExpression) || !this.csharpReceiverIntactIn(loop.statement, expression)) {
      return void 0;
    }
    return `${this.printNode(expression, 0)}[${this.printNode(argumentExpression, 0)}]`;
  }
  // the enclosing `for` whose condition is `<counter> < <recv>.length` and whose header declares
  // that counter: the condition held on entry to this pass and nothing in between moved off it
  csharpCounterRangeLoop(counter, receiver) {
    let node = counter;
    while (node.parent !== void 0) {
      const parent = node.parent;
      if (_typescript2.default.isFunctionLike(parent)) {
        return void 0;
      }
      if (_typescript2.default.isForStatement(parent) && this.csharpContains(parent.statement, counter) && this.csharpForBoundsCounter(parent, counter, receiver)) {
        return parent;
      }
      node = parent;
    }
    return void 0;
  }
  csharpForBoundsCounter(loop, counter, receiver) {
    const condition = this.csharpUnparenthesized(loop.condition);
    if (_optionalChain([condition, 'optionalAccess', _102 => _102.kind]) !== _typescript2.default.SyntaxKind.BinaryExpression || condition.operatorToken.kind !== _typescript2.default.SyntaxKind.LessThanToken) {
      return false;
    }
    const left = this.csharpUnparenthesized(condition.left);
    const right = this.csharpUnparenthesized(condition.right);
    if (!_typescript2.default.isIdentifier(left) || !_typescript2.default.isPropertyAccessExpression(right) || _optionalChain([right, 'access', _103 => _103.name, 'optionalAccess', _104 => _104.escapedText]) !== "length") {
      return false;
    }
    const receiverExpression = this.csharpUnparenthesized(right.expression);
    if (!_typescript2.default.isIdentifier(receiverExpression) || !this.csharpCounterStartsAtZero(loop, counter) || !this.csharpCounterAdvances(loop, counter)) {
      return false;
    }
    const declaration = _optionalChain([this, 'access', _105 => _105.getChecker, 'call', _106 => _106(), 'access', _107 => _107.getSymbolAtLocation, 'call', _108 => _108(counter), 'optionalAccess', _109 => _109.valueDeclaration]);
    const checker = this.getChecker();
    return declaration !== void 0 && _optionalChain([checker, 'access', _110 => _110.getSymbolAtLocation, 'call', _111 => _111(left), 'optionalAccess', _112 => _112.valueDeclaration]) === declaration && _optionalChain([checker, 'access', _113 => _113.getSymbolAtLocation, 'call', _114 => _114(receiverExpression), 'optionalAccess', _115 => _115.valueDeclaration]) === _optionalChain([checker, 'access', _116 => _116.getSymbolAtLocation, 'call', _117 => _117(receiver), 'optionalAccess', _118 => _118.valueDeclaration]);
  }
  // `for (let i = <literal >= 0>; ...)` — a negative start would index below the list
  csharpCounterStartsAtZero(loop, counter) {
    const initializer = loop.initializer;
    if (_optionalChain([initializer, 'optionalAccess', _119 => _119.kind]) !== _typescript2.default.SyntaxKind.VariableDeclarationList || initializer.declarations.length !== 1) {
      return false;
    }
    const declaration = initializer.declarations[0];
    if (!_typescript2.default.isIdentifier(declaration.name) || !_typescript2.default.isNumericLiteral(declaration.initializer)) {
      return false;
    }
    return Number(declaration.initializer.text) >= 0 && _optionalChain([this, 'access', _120 => _120.getChecker, 'call', _121 => _121(), 'access', _122 => _122.getSymbolAtLocation, 'call', _123 => _123(counter), 'optionalAccess', _124 => _124.valueDeclaration]) === declaration;
  }
  // the header moves the counter forward: a decrement could leave a negative index behind
  csharpCounterAdvances(loop, counter) {
    const declaration = _optionalChain([this, 'access', _125 => _125.getChecker, 'call', _126 => _126(), 'access', _127 => _127.getSymbolAtLocation, 'call', _128 => _128(counter), 'optionalAccess', _129 => _129.valueDeclaration]);
    const incrementor = this.csharpUnparenthesized(loop.incrementor);
    if (incrementor === void 0 || declaration === void 0) {
      return false;
    }
    if (incrementor.kind === _typescript2.default.SyntaxKind.PostfixUnaryExpression || incrementor.kind === _typescript2.default.SyntaxKind.PrefixUnaryExpression) {
      return incrementor.operator === _typescript2.default.SyntaxKind.PlusPlusToken && _optionalChain([this, 'access', _130 => _130.getChecker, 'call', _131 => _131(), 'access', _132 => _132.getSymbolAtLocation, 'call', _133 => _133(incrementor.operand), 'optionalAccess', _134 => _134.valueDeclaration]) === declaration;
    }
    if (incrementor.kind === _typescript2.default.SyntaxKind.BinaryExpression && incrementor.operatorToken.kind === _typescript2.default.SyntaxKind.PlusEqualsToken) {
      return _optionalChain([this, 'access', _135 => _135.getChecker, 'call', _136 => _136(), 'access', _137 => _137.getSymbolAtLocation, 'call', _138 => _138(incrementor.left), 'optionalAccess', _139 => _139.valueDeclaration]) === declaration && _typescript2.default.isNumericLiteral(incrementor.right) && Number(incrementor.right.text) >= 0;
    }
    return false;
  }
  // a write to the counter in the body invalidates the bound the condition proved
  csharpCounterUnwrittenIn(range, counter) {
    const declaration = _optionalChain([this, 'access', _140 => _140.getChecker, 'call', _141 => _141(), 'access', _142 => _142.getSymbolAtLocation, 'call', _143 => _143(counter), 'optionalAccess', _144 => _144.valueDeclaration]);
    if (declaration === void 0) {
      return false;
    }
    let written = false;
    this.csharpWalkIdentifiers(range, (identifier) => {
      if (written || !this.csharpIsSameDeclaration(identifier, declaration)) {
        return;
      }
      const parent = identifier.parent;
      if (_typescript2.default.isBinaryExpression(parent) && parent.left === identifier) {
        written = CSHARP_ASSIGNMENT_OPERATOR_KINDS.indexOf(parent.operatorToken.kind) >= 0;
      } else if ((_typescript2.default.isPrefixUnaryExpression(parent) || _typescript2.default.isPostfixUnaryExpression(parent)) && parent.operand === identifier) {
        written = true;
      } else if (_typescript2.default.isDeleteExpression(parent)) {
        written = true;
      }
    });
    return !written;
  }
  // the condition's `<recv>.length` still proves the range only while the list is intact: inside
  // the body the receiver may be read (element reads, its own `.length`) and nothing else
  csharpReceiverIntactIn(range, receiver) {
    const declaration = _optionalChain([this, 'access', _145 => _145.getChecker, 'call', _146 => _146(), 'access', _147 => _147.getSymbolAtLocation, 'call', _148 => _148(receiver), 'optionalAccess', _149 => _149.valueDeclaration]);
    if (declaration === void 0) {
      return false;
    }
    let intact = true;
    this.csharpWalkIdentifiers(range, (identifier) => {
      if (!intact || !this.csharpIsSameDeclaration(identifier, declaration)) {
        return;
      }
      const parent = identifier.parent;
      if (_typescript2.default.isPropertyAccessExpression(parent) && parent.expression === identifier && _optionalChain([parent, 'access', _150 => _150.name, 'optionalAccess', _151 => _151.escapedText]) === "length") {
        return;
      }
      if (_typescript2.default.isElementAccessExpression(parent) && parent.expression === identifier && !this.csharpIsWriteTarget(parent)) {
        return;
      }
      intact = false;
    });
    return intact;
  }
  csharpIsSameDeclaration(identifier, declaration) {
    return _optionalChain([this, 'access', _152 => _152.getChecker, 'call', _153 => _153(), 'access', _154 => _154.getSymbolAtLocation, 'call', _155 => _155(identifier), 'optionalAccess', _156 => _156.valueDeclaration]) === declaration;
  }
  csharpWalkIdentifiers(node, visit) {
    if (node === void 0) {
      return;
    }
    if (_typescript2.default.isIdentifier(node)) {
      visit(node);
    }
    _typescript2.default.forEachChild(node, (child) => this.csharpWalkIdentifiers(child, visit));
  }
  // `x[i] = v` / `x[i]++` / `delete x[i]` change the receiver in place
  csharpIsWriteTarget(node) {
    let value = node;
    while (value.parent !== void 0 && _typescript2.default.isParenthesizedExpression(value.parent)) {
      value = value.parent;
    }
    const parent = value.parent;
    if (parent === void 0) {
      return false;
    }
    if (_typescript2.default.isBinaryExpression(parent) && parent.left === value) {
      return CSHARP_ASSIGNMENT_OPERATOR_KINDS.indexOf(parent.operatorToken.kind) >= 0;
    }
    if ((_typescript2.default.isPrefixUnaryExpression(parent) || _typescript2.default.isPostfixUnaryExpression(parent)) && parent.operand === value) {
      return parent.operator === _typescript2.default.SyntaxKind.PlusPlusToken || parent.operator === _typescript2.default.SyntaxKind.MinusMinusToken;
    }
    return _typescript2.default.isDeleteExpression(parent);
  }
  csharpUnparenthesized(node) {
    let value = node;
    while (value !== void 0 && value.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      value = value.expression;
    }
    return value;
  }
  csharpGuardAdmitsRead(guard, read) {
    const negated = this.csharpGuardIsNegated(guard);
    let statement = guard;
    while (statement !== void 0 && statement.parent !== void 0) {
      const parent = statement.parent;
      if (_typescript2.default.isIfStatement(parent) && this.csharpContains(parent.expression, guard)) {
        if (!negated && this.csharpContains(parent.thenStatement, read)) {
          return true;
        }
        if (negated && parent.elseStatement !== void 0 && this.csharpContains(parent.elseStatement, read)) {
          return true;
        }
        if (negated && this.csharpAlwaysExits(parent.thenStatement) && read.getStart() >= parent.getEnd() && this.csharpContains(parent.parent, read)) {
          return true;
        }
        return false;
      }
      if (_typescript2.default.isWhileStatement(parent) && this.csharpContains(parent.expression, guard)) {
        return !negated && this.csharpContains(parent.statement, read);
      }
      statement = parent;
    }
    return false;
  }
  // `key in recv` guards in the function body, indexed by receiver text + key; nested
  // functions are skipped, their guards cannot dominate a read of the outer function
  csharpInGuardsOf(func) {
    const cached = this.csharpGuardIndex.get(func);
    if (cached !== void 0) {
      return cached;
    }
    const index = /* @__PURE__ */ new Map();
    const collect = (n) => {
      if (n !== func && _typescript2.default.isFunctionLike(n)) {
        return;
      }
      if (_typescript2.default.isBinaryExpression(n) && n.operatorToken.kind === _typescript2.default.SyntaxKind.InKeyword) {
        const keyNode = n.left;
        if (_typescript2.default.isStringLiteralLike(keyNode) || _typescript2.default.isNumericLiteral(keyNode)) {
          const id = n.right.getText() + GUARD_KEY_SEPARATOR + keyNode.text;
          const list = index.get(id);
          if (list === void 0) {
            index.set(id, [n]);
          } else {
            list.push(n);
          }
        }
      }
      _typescript2.default.forEachChild(n, collect);
    };
    collect(func);
    this.csharpGuardIndex.set(func, index);
    return index;
  }
  // the read's receiver is a local whose only initializer is a literal that declares the
  // key, and the local is not reassigned or deleted from afterwards
  csharpLiteralDeclaresKey(node, expression, key, isNumberKey) {
    if (!_typescript2.default.isIdentifier(expression)) {
      return false;
    }
    const symbol = this.getChecker().getSymbolAtLocation(expression);
    const declarations = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _157 => _157.declarations]), () => ( []));
    if (declarations.length !== 1 || !_typescript2.default.isVariableDeclaration(declarations[0])) {
      return false;
    }
    const declaration = declarations[0];
    if (declaration.initializer === void 0 || declaration.getStart() >= node.getStart()) {
      return false;
    }
    const initializer = declaration.initializer;
    let declares = false;
    if (isNumberKey && _typescript2.default.isArrayLiteralExpression(initializer)) {
      const spread = initializer.elements.some((element) => _typescript2.default.isSpreadElement(element));
      declares = !spread && Number(key) < initializer.elements.length;
    } else if (!isNumberKey && _typescript2.default.isObjectLiteralExpression(initializer)) {
      declares = this.csharpObjectLiteralDeclaresKey(initializer, key);
    }
    if (!declares) {
      return false;
    }
    const func = this.csharpEnclosingFunction(node);
    return func !== void 0 && !this.csharpReceiverIsRewritten(func, expression);
  }
  csharpObjectLiteralDeclaresKey(literal, key) {
    for (const property of literal.properties) {
      if (_typescript2.default.isSpreadAssignment(property)) {
        return false;
      }
      const name = property.name;
      if (name !== void 0 && (_typescript2.default.isIdentifier(name) || _typescript2.default.isStringLiteralLike(name) || _typescript2.default.isNumericLiteral(name)) && name.text === key) {
        return true;
      }
    }
    return false;
  }
  // the receiver must be a dictionary at runtime for the IDictionary cast to hold; `any`
  // receivers are rejected because the checker cannot tell what the read reaches
  csharpReceiverIsDictionaryLike(expression, key) {
    const type = this.getChecker().getTypeAtLocation(expression);
    if (type.flags === _typescript2.default.TypeFlags.Any || type.flags === _typescript2.default.TypeFlags.Unknown) {
      return false;
    }
    const checker = this.getChecker();
    return checker.getIndexInfoOfType(type, _typescript2.default.IndexKind.String) !== void 0 || checker.getPropertyOfType(type, key) !== void 0;
  }
  // any assignment to the receiver (or to a same-named binding) in the function makes the
  // object the read evaluates unprovable, so the read falls back to the helper
  csharpReceiverIsRewritten(func, expression) {
    const text = expression.getText();
    const name = _typescript2.default.isIdentifier(expression) ? text : text.split(/[.[]/)[1];
    if (name === void 0) {
      return true;
    }
    let rewritten = false;
    const walk = (n) => {
      if (rewritten) {
        return;
      }
      if (_typescript2.default.isIdentifier(n) && n.text === name) {
        const parent = n.parent;
        if (_typescript2.default.isBinaryExpression(parent) && parent.left === n) {
          rewritten = true;
        } else if ((_typescript2.default.isPrefixUnaryExpression(parent) || _typescript2.default.isPostfixUnaryExpression(parent)) && parent.operand === n) {
          rewritten = true;
        } else if (_typescript2.default.isDeleteExpression(parent)) {
          rewritten = true;
        }
      }
      _typescript2.default.forEachChild(n, walk);
    };
    walk(func);
    return rewritten;
  }
  // ===== ws handler `message` parameter (batch D, D-19) =====
  // A ws handler `handleX (client: Client, message: Dict)` is reached at runtime either
  // through a dispatch-table entry (`{ "k", this.handleX }` -> DynamicInvoker) or a direct
  csharpHandlerMessageType(param) {
    if (this.csharpHandlerMessageTypes.has(param)) {
      return this.csharpHandlerMessageTypes.get(param);
    }
    let result = void 0;
    const method = _optionalChain([param, 'optionalAccess', _158 => _158.parent]);
    if (_typescript2.default.isMethodDeclaration(method) && method.parameters.length >= 2 && method.parameters[1] === param) {
      if (this.csharpCheckerTypeName(method.parameters[0]) === "Client" && this.csharpCheckerTypeName(param) === "Dict" && this.getMethodOverride(method) === void 0 && !this.csharpReceiverIsRewritten(method, param.name) && !this.csharpHandlerIsCalled(method) && !this.csharpClassHasListRoute(method)) {
        result = _nullishCoalesce(this.ArgTypeReplacements["Dict"], () => ( "Dictionary<string, object>"));
      }
    }
    this.csharpHandlerMessageTypes.set(param, result);
    return result;
  }
  // the type name the checker prints for a node's type, or undefined in an in-memory
  // program (no checker answer: the printer keeps its own)
  csharpCheckerTypeName(node) {
    if (node === void 0) {
      return void 0;
    }
    try {
      const checker = this.getChecker();
      const type = checker.getTypeAtLocation(node);
      return type === void 0 ? void 0 : checker.typeToString(type);
    } catch (e) {
      return void 0;
    }
  }
  csharpEnclosingClass(node) {
    let current = _optionalChain([node, 'optionalAccess', _159 => _159.parent]);
    while (current !== void 0) {
      if (_typescript2.default.isClassDeclaration(current) || _typescript2.default.isClassExpression(current)) {
        return current;
      }
      current = current.parent;
    }
    return void 0;
  }
  // the identifier is the raw `message` parameter of a handler-shaped method (a `Client`
  // first parameter, so `handleMessage` with its `any` annotation included): the class can
  // route that same value through its list arm
  csharpIsHandlerMessageIdentifier(node) {
    if (!_typescript2.default.isIdentifier(node)) {
      return false;
    }
    try {
      const declaration = _optionalChain([this, 'access', _160 => _160.getChecker, 'call', _161 => _161(), 'access', _162 => _162.getSymbolAtLocation, 'call', _163 => _163(node), 'optionalAccess', _164 => _164.valueDeclaration]);
      if (declaration === void 0 || !_typescript2.default.isParameter(declaration)) {
        return false;
      }
      const owner = declaration.parent;
      return _typescript2.default.isMethodDeclaration(owner) && owner.parameters.length >= 2 && owner.parameters[1] === declaration && this.csharpCheckerTypeName(owner.parameters[0]) === "Client";
    } catch (e) {
      return false;
    }
  }
  // a class that tests the message (or another dict parameter) for a list can hand the
  // array itself to a dispatch-table entry (binance `'x@arr'`), so its handlers keep the
  // box; a class testing unrelated lists (an array-typed `symbols` parameter, a field of
  csharpClassHasListRoute(method) {
    const cls = this.csharpEnclosingClass(method);
    if (cls === void 0) {
      return true;
    }
    const cached = this.csharpListRouteClasses.get(cls);
    if (cached !== void 0) {
      return cached;
    }
    let found = false;
    const walk = (n) => {
      if (found) {
        return;
      }
      if (_typescript2.default.isCallExpression(n) && _typescript2.default.isPropertyAccessExpression(n.expression) && _optionalChain([n, 'access', _165 => _165.expression, 'access', _166 => _166.expression, 'optionalAccess', _167 => _167.escapedText]) === "Array" && _optionalChain([n, 'access', _168 => _168.expression, 'access', _169 => _169.name, 'optionalAccess', _170 => _170.escapedText]) === "isArray") {
        const argument = n.arguments[0];
        if (this.csharpIsHandlerMessageIdentifier(argument)) {
          found = true;
          return;
        }
      }
      _typescript2.default.forEachChild(n, walk);
    };
    walk(cls);
    this.csharpListRouteClasses.set(cls, found);
    return found;
  }
  // a static call reference (`this.handleX (…)`) binds the argument to the printed
  // signature; a method-group value (a dispatch-table entry) does not
  csharpHandlerIsCalled(method) {
    const cached = this.csharpHandlerCalled.get(method);
    if (cached !== void 0) {
      return cached;
    }
    let called = true;
    const cls = this.csharpEnclosingClass(method);
    let file;
    try {
      file = method.getSourceFile();
    } catch (e) {
      file = void 0;
    }
    if (cls !== void 0 && file !== void 0) {
      try {
        const symbol = this.getChecker().getSymbolAtLocation(method.name);
        if (symbol !== void 0) {
          called = this.csharpHandlerCallIndexFor(file).get(symbol) === true;
        }
      } catch (e) {
        called = true;
      }
    }
    this.csharpHandlerCalled.set(method, called);
    return called;
  }
  // every identifier of the FILE that resolves to one of its classes' own method names,
  // recorded as a call when it is the callee of a call expression; keyed by symbol, so a
  // same-named method of another class never marks this one
  csharpHandlerCallIndexFor(file) {
    const cached = this.csharpHandlerCallIndex.get(file);
    if (cached !== void 0) {
      return cached;
    }
    const names = /* @__PURE__ */ new Set();
    const collect = (n) => {
      if (_typescript2.default.isClassDeclaration(n) || _typescript2.default.isClassExpression(n)) {
        for (const member of n.members) {
          const name = _optionalChain([member, 'access', _171 => _171.name, 'optionalAccess', _172 => _172.escapedText]);
          if (_typescript2.default.isMethodDeclaration(member) && name !== void 0) {
            names.add(name);
          }
        }
      }
      _typescript2.default.forEachChild(n, collect);
    };
    collect(file);
    const index = /* @__PURE__ */ new Map();
    if (names.size > 0) {
      let checker;
      try {
        checker = this.getChecker();
      } catch (e) {
        checker = void 0;
      }
      if (checker !== void 0) {
        const walk = (n) => {
          if (_typescript2.default.isIdentifier(n) && names.has(n.escapedText)) {
            let symbol;
            try {
              symbol = checker.getSymbolAtLocation(n);
            } catch (e) {
              symbol = void 0;
            }
            if (symbol !== void 0) {
              const parent = n.parent;
              const isCall = _typescript2.default.isPropertyAccessExpression(parent) && parent.name === n && _typescript2.default.isCallExpression(parent.parent) && parent.parent.expression === parent || _typescript2.default.isCallExpression(parent) && parent.expression === n;
              if (isCall) {
                index.set(symbol, true);
              } else if (index.get(symbol) === void 0) {
                index.set(symbol, false);
              }
            }
          }
          _typescript2.default.forEachChild(n, walk);
        };
        walk(file);
      }
    }
    this.csharpHandlerCallIndex.set(file, index);
    return index;
  }
  csharpHasKeyRemoval(func, expression, key) {
    const text = expression.getText();
    let removed = false;
    const walk = (n) => {
      if (removed) {
        return;
      }
      if (_typescript2.default.isDeleteExpression(n) && _typescript2.default.isElementAccessExpression(n.expression) && n.expression.expression.getText() === text && n.expression.argumentExpression.getText().replace(/['"]/g, "") === key.replace(/['"]/g, "")) {
        removed = true;
      }
      _typescript2.default.forEachChild(n, walk);
    };
    walk(func);
    return removed;
  }
  csharpGuardIsNegated(guard) {
    let node = guard;
    while (node.parent !== void 0 && _typescript2.default.isParenthesizedExpression(node.parent)) {
      node = node.parent;
    }
    return node.parent !== void 0 && _typescript2.default.isPrefixUnaryExpression(node.parent) && node.parent.operator === _typescript2.default.SyntaxKind.ExclamationToken;
  }
  csharpAlwaysExits(statement) {
    if (statement === void 0) {
      return false;
    }
    const exits = (n) => _typescript2.default.isReturnStatement(n) || _typescript2.default.isThrowStatement(n) || _typescript2.default.isContinueStatement(n) || _typescript2.default.isBreakStatement(n);
    if (exits(statement)) {
      return true;
    }
    if (_typescript2.default.isBlock(statement) && statement.statements.length > 0) {
      return exits(statement.statements[statement.statements.length - 1]);
    }
    return false;
  }
  csharpContains(outer, inner) {
    if (outer === void 0 || inner === void 0) {
      return false;
    }
    return inner.getStart() >= outer.getStart() && inner.getEnd() <= outer.getEnd();
  }
  // cs-strict S14: `((List<object>)x)[i] = v` needs no cast when the PRINTED declaration is
  // `List<object>` (identity conversion; not IList<object>, a checked downcast). Type comes via the
  // `csharpLocalTypeOf` hook; `x[i] += v` and reads stay on the base path (third override layer).
  csharpElementAccessReceiverIsList(node) {
    if (_optionalChain([node, 'optionalAccess', _173 => _173.kind]) !== _typescript2.default.SyntaxKind.ElementAccessExpression) {
      return false;
    }
    const parent = node.parent;
    if (_optionalChain([parent, 'optionalAccess', _174 => _174.kind]) !== _typescript2.default.SyntaxKind.BinaryExpression || _optionalChain([parent, 'access', _175 => _175.operatorToken, 'optionalAccess', _176 => _176.kind]) !== _typescript2.default.SyntaxKind.EqualsToken || parent.left !== node) {
      return false;
    }
    const receiver = node.expression;
    if (_optionalChain([receiver, 'optionalAccess', _177 => _177.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const localTypeOf = this.csharpLocalTypeOf;
    return typeof localTypeOf === "function" && localTypeOf.call(this, receiver) === "List<object>";
  }
  // The C# declared type pair of a list index READ (`x[i]`): receiver (`List<object>` /
  // `IList<object>`) and index (`int`). Consumer-installed (build/csharp-local-types.js);
  // undefined by default, so an unpatched printer keeps `getValue (x, i)` for every read.
  csharpListIndexReadTypes(node) {
    return void 0;
  }
  // `x[i]` read on a list receiver: the helper answers null off the end while the C# indexer throws,
  // so the native read is only emitted where the source proves the index in range. Types come from
  // the consumer hook above, so the untyped emission is byte-identical.
  csharpListIndexRead(node) {
    if (_optionalChain([node, 'optionalAccess', _178 => _178.kind]) !== _typescript2.default.SyntaxKind.ElementAccessExpression) {
      return void 0;
    }
    const { expression, argumentExpression } = node;
    if (_optionalChain([expression, 'optionalAccess', _179 => _179.kind]) !== _typescript2.default.SyntaxKind.Identifier || _optionalChain([argumentExpression, 'optionalAccess', _180 => _180.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const parent = node.parent;
    const isWrite = _optionalChain([parent, 'optionalAccess', _181 => _181.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && (parent.operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken || parent.operatorToken.kind === _typescript2.default.SyntaxKind.PlusEqualsToken) && parent.left === node;
    if (isWrite) {
      return void 0;
    }
    const types = typeof this.csharpListIndexReadTypes === "function" ? this.csharpListIndexReadTypes(node) : void 0;
    if (_optionalChain([types, 'optionalAccess', _182 => _182.index]) !== "int") {
      return void 0;
    }
    if (types.receiver !== "List<object>" && types.receiver !== "IList<object>") {
      return void 0;
    }
    if (!this.csharpIndexIsLoopBounded(node, expression, argumentExpression)) {
      return void 0;
    }
    return this.printNode(expression, 0) + "[" + this.printNode(argumentExpression, 0) + "]";
  }
  // the read sits in the body of a `for` that re-tests `index < receiver.length`, both
  // resolved to the very symbols the read uses, and nothing in the body can move either
  csharpIndexIsLoopBounded(read, receiver, index) {
    const receiverSymbol = this.csharpIdentifierSymbol(receiver);
    const indexSymbol = this.csharpIdentifierSymbol(index);
    if (receiverSymbol === void 0 || indexSymbol === void 0) {
      return false;
    }
    let node = read;
    while (_optionalChain([node, 'optionalAccess', _183 => _183.parent]) !== void 0) {
      const parent = node.parent;
      if (_typescript2.default.isFunctionLike(parent)) {
        return false;
      }
      if (_typescript2.default.isForStatement(parent) && this.csharpForBoundsIndex(parent, read, receiverSymbol, indexSymbol)) {
        return true;
      }
      node = parent;
    }
    return false;
  }
  csharpIdentifierSymbol(node) {
    if (_optionalChain([node, 'optionalAccess', _184 => _184.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    return _optionalChain([this, 'access', _185 => _185.checkerOrUndefined, 'call', _186 => _186(), 'optionalAccess', _187 => _187.getSymbolAtLocation, 'call', _188 => _188(node)]);
  }
  csharpForBoundsIndex(forStatement, read, receiverSymbol, indexSymbol) {
    if (!this.csharpContains(forStatement.statement, read)) {
      return false;
    }
    const condition = forStatement.condition;
    if (_optionalChain([condition, 'optionalAccess', _189 => _189.kind]) !== _typescript2.default.SyntaxKind.BinaryExpression || _optionalChain([condition, 'access', _190 => _190.operatorToken, 'optionalAccess', _191 => _191.kind]) !== _typescript2.default.SyntaxKind.LessThanToken) {
      return false;
    }
    const length = condition.right;
    if (_optionalChain([length, 'optionalAccess', _192 => _192.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([length, 'access', _193 => _193.name, 'optionalAccess', _194 => _194.escapedText]) !== "length") {
      return false;
    }
    if (this.csharpIdentifierSymbol(condition.left) !== indexSymbol) {
      return false;
    }
    if (this.csharpIdentifierSymbol(this.csharpLengthReceiverIdentifier(length.expression)) !== receiverSymbol) {
      return false;
    }
    return !this.csharpIndexBoundIsVoided(forStatement.statement, receiverSymbol, indexSymbol);
  }
  // The identifier a `.length` receiver reads through wrappers whose C# print is the bare text:
  // parentheses, and `as T` where printAsExpression does not cast (`any`/`string`/`T[]` print a cast,
  // so the bound would not be the receiver's Count). `(response as List).length` -> `?.Count ?? 0`
  csharpLengthReceiverIdentifier(node) {
    let current = node;
    while (current !== void 0) {
      if (current.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
        current = current.expression;
        continue;
      }
      if (current.kind === _typescript2.default.SyntaxKind.AsExpression && current.type !== void 0 && current.type.kind !== _typescript2.default.SyntaxKind.AnyKeyword && current.type.kind !== _typescript2.default.SyntaxKind.StringKeyword && current.type.kind !== _typescript2.default.SyntaxKind.ArrayType) {
        current = current.expression;
        continue;
      }
      break;
    }
    return current;
  }
  // the condition ran before the body did: a write to the index, or any use of the receiver
  // other than an element access / a property read (a method call, an argument, a bare read —
  // anything that could hand the list to something that shrinks it) voids the bound
  csharpIndexBoundIsVoided(body, receiverSymbol, indexSymbol) {
    let voided = false;
    const visit = (n) => {
      if (voided || n === void 0) {
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.Identifier) {
        const symbol = this.csharpIdentifierSymbol(n);
        if (symbol === indexSymbol && this.csharpIdentifierIsWritten(n)) {
          voided = true;
          return;
        }
        if (symbol === receiverSymbol && !this.csharpReceiverUseKeepsBound(n)) {
          voided = true;
          return;
        }
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(body, visit);
    return voided;
  }
  // a use that cannot move the value the condition tested: the expression of an element
  // access (`recv[i]`, a read or an element write — neither changes the length) or of a
  // property READ (`recv.Count`); a property write or a call on the receiver is not one
  csharpReceiverUseKeepsBound(node) {
    const parent = node.parent;
    if (parent === void 0) {
      return false;
    }
    if (parent.kind === _typescript2.default.SyntaxKind.ElementAccessExpression && parent.expression === node) {
      return true;
    }
    if (parent.kind === _typescript2.default.SyntaxKind.PropertyAccessExpression && parent.expression === node) {
      if (this.csharpIdentifierIsWritten(node)) {
        return false;
      }
      const grand = parent.parent;
      return !(_optionalChain([grand, 'optionalAccess', _195 => _195.kind]) === _typescript2.default.SyntaxKind.CallExpression && grand.expression === parent);
    }
    return false;
  }
  // the identifier is a write target: `x = v`, `x += v`, `x++` / `x--`, a destructuring
  // element, or a property/element write through it (`x.length = 0`)
  csharpIdentifierIsWritten(node) {
    const parent = node.parent;
    if (parent === void 0) {
      return false;
    }
    if (parent.kind === _typescript2.default.SyntaxKind.BinaryExpression && parent.left === node && this.csharpIsAssignmentOperator(parent.operatorToken.kind)) {
      return true;
    }
    if ((parent.kind === _typescript2.default.SyntaxKind.PrefixUnaryExpression || parent.kind === _typescript2.default.SyntaxKind.PostfixUnaryExpression) && (parent.operator === _typescript2.default.SyntaxKind.PlusPlusToken || parent.operator === _typescript2.default.SyntaxKind.MinusMinusToken)) {
      return true;
    }
    if (parent.kind === _typescript2.default.SyntaxKind.ArrayLiteralExpression || parent.kind === _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      const grand = parent.parent;
      return _optionalChain([grand, 'optionalAccess', _196 => _196.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && grand.left === parent && this.csharpIsAssignmentOperator(grand.operatorToken.kind);
    }
    return false;
  }
  csharpIsAssignmentOperator(kind) {
    return kind >= _typescript2.default.SyntaxKind.FirstAssignment && kind <= _typescript2.default.SyntaxKind.LastAssignment;
  }
  // S22: an index WRITE (`x["k"] = v`) needs no `((IDictionary<string,object>)x)` cast when the
  // receiver's printed C# declaration already IS a dictionary. The ccxt classifier
  // (build/csharp-local-types.js) installs this predicate; undefined/false keeps the upstream cast.
  csharpDictionaryIndexWriteNeedsNoCast(node) {
    return void 0;
  }
  // the consumer's classifier may prove the declared C# type of an element-access receiver
  // (`Dictionary<string, object>` / `IDictionary<string, object>`); consumer-installed, so an
  // answer of undefined keeps the untyped emission byte-identical
  csharpElementAccessTypedReceiver(node) {
    return void 0;
  }
  // `recv[key]` read with a consumer-proven string-keyed dict receiver and a literal key: the typed
  // twin GetValue(IDictionary<string, object>, string) runs the same dictionary read as the object
  // overload's dict branch without its runtime sniffing. Every other read keeps the wrapper.
  printTypedDictElementAccessIfAny(node) {
    if (this.csharpElementAccessTypedReceiver(node) === void 0) {
      return void 0;
    }
    const { expression, argumentExpression } = node;
    if (!_typescript2.default.isStringLiteralLike(argumentExpression)) {
      return void 0;
    }
    const parent = node.parent;
    const isLeftSideOfAssignment = _optionalChain([parent, 'optionalAccess', _197 => _197.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && (parent.operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken || parent.operatorToken.kind === _typescript2.default.SyntaxKind.PlusEqualsToken) && parent.left === node;
    if (isLeftSideOfAssignment) {
      return void 0;
    }
    return "GetValue(" + this.printNode(expression, 0) + ", " + this.printNode(argumentExpression, 0) + ")";
  }
  printWrappedUnknownThisProperty(node) {
    const type = this.getChecker().getResolvedSignature(node);
    if (_optionalChain([type, 'optionalAccess', _198 => _198.declaration]) === void 0) {
      let parsedArguments = _optionalChain([node, 'access', _199 => _199.arguments, 'optionalAccess', _200 => _200.map, 'call', _201 => _201((a) => this.printNode(a, 0)), 'access', _202 => _202.join, 'call', _203 => _203(", ")]);
      parsedArguments = parsedArguments ? parsedArguments : "";
      const propName = _optionalChain([node, 'access', _204 => _204.expression, 'optionalAccess', _205 => _205.name, 'access', _206 => _206.escapedText]);
      const nativeDelegateCall = this.csharpNativeDelegateCall(node, propName);
      if (nativeDelegateCall !== void 0) {
        return nativeDelegateCall;
      }
      const isAsyncDecl = _optionalChain([node, 'optionalAccess', _207 => _207.parent, 'optionalAccess', _208 => _208.kind]) === _typescript2.default.SyntaxKind.AwaitExpression;
      const argsArray = `new object[] { ${parsedArguments} }`;
      const open = this.DYNAMIC_CALL_OPEN;
      let statement = `${open}this, "${propName}", ${argsArray})`;
      statement = isAsyncDecl ? `((Task<object>)${statement})` : statement;
      return statement;
    }
    return void 0;
  }
  // A `this.<name>(...)` call the checker cannot resolve on a function-valued property is a
  // delegate in the hand-written C# base, which `ResolveMethod` never finds (the helper throws
  // there); matching arity prints the same property invocation directly.
  csharpNativeDelegateCall(node, propName) {
    const arity = CSHARP_NATIVE_THIS_DELEGATE_ARITY[propName];
    if (arity === void 0) {
      return void 0;
    }
    if (_optionalChain([node, 'optionalAccess', _209 => _209.parent, 'optionalAccess', _210 => _210.kind]) === _typescript2.default.SyntaxKind.AwaitExpression) {
      return void 0;
    }
    const args = _nullishCoalesce(node.arguments, () => ( []));
    if (args.length !== arity) {
      return void 0;
    }
    return `this.${propName}(${args.map((a) => this.printNode(a, 0)).join(", ")})`;
  }
  printOutOfOrderCallExpressionIfAny(node, identation) {
    if (node.expression.kind === _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      const expressionText = node.expression.getText().trim();
      const args = node.arguments;
      if (args.length === 1) {
        const parsedArg = this.printNode(args[0], 0);
        switch (expressionText) {
          case "Math.abs":
            return `Math.Abs(Convert.ToDouble(${parsedArg}))`;
        }
      } else if (args.length === 2) {
        const parsedArg1 = this.printNode(args[0], 0);
        const parsedArg2 = this.printNode(args[1], 0);
        switch (expressionText) {
          case "Math.min":
            return _nullishCoalesce(this.csharpNativeMathMinMax(node, "Min", parsedArg1, parsedArg2), () => ( `mathMin(${parsedArg1}, ${parsedArg2})`));
          case "Math.max":
            return _nullishCoalesce(this.csharpNativeMathMinMax(node, "Max", parsedArg1, parsedArg2), () => ( `mathMax(${parsedArg1}, ${parsedArg2})`));
          case "Math.pow":
            return `Math.Pow(Convert.ToDouble(${parsedArg1}), Convert.ToDouble(${parsedArg2}))`;
        }
      }
      const leftSide = _optionalChain([node, 'access', _211 => _211.expression, 'optionalAccess', _212 => _212.expression]);
      const leftSideText = leftSide ? this.printNode(leftSide, 0) : void 0;
      if (leftSideText === this.THIS_TOKEN || leftSide.getFullText().indexOf("(this as any)") > -1) {
        const res = this.printWrappedUnknownThisProperty(node);
        if (res) {
          return res;
        }
      }
    }
    if (node.expression.kind === _typescript2.default.SyntaxKind.ElementAccessExpression) {
      return this.printDynamicCall(node, identation);
    }
    return void 0;
  }
  handleTypeOfInsideBinaryExpression(node, identation) {
    const left = node.left;
    const right = node.right.text;
    const op = node.operatorToken.kind;
    const expression = left.expression;
    const isDifferentOperator = op === _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsToken;
    const notOperator = isDifferentOperator ? this.NOT_TOKEN : "";
    const target = this.printNode(expression, 0);
    switch (right) {
      case "string":
        return notOperator + `(${target} is string)`;
      case "number":
        return notOperator + `(${target} is Int64 || ${target} is int || ${target} is float || ${target} is double)`;
      case "boolean":
        return notOperator + `(${target} is bool)`;
      case "object":
        return notOperator + `(${target} is IDictionary<string, object>)`;
      case "function":
        return notOperator + `(${target} is Delegate)`;
    }
    return void 0;
  }
  // The C# type of an operand of `==` / `!=` as the printer emits it, or undefined when
  // the printer only knows `object`: an `object` operand takes the reference-comparing
  // `operator ==`, so those keep the isEqual helper.
  csharpEqualityOperandType(node) {
    if (node === void 0) {
      return void 0;
    }
    switch (node.kind) {
      case _typescript2.default.SyntaxKind.NullKeyword:
        return "null";
      case _typescript2.default.SyntaxKind.TrueKeyword:
      case _typescript2.default.SyntaxKind.FalseKeyword:
        return "bool";
      case _typescript2.default.SyntaxKind.NumericLiteral:
        return this.csharpNumericLiteralKind(node);
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
        return this.csharpEqualityOperandType(node.expression);
      case _typescript2.default.SyntaxKind.Identifier:
        if (node.escapedText === "undefined") {
          return "null";
        }
        return _nullishCoalesce(this.csharpDeclaredTypeOfBinding(node), () => ( this.csharpParameterOperandType(node)));
      case _typescript2.default.SyntaxKind.PropertyAccessExpression: {
        const fieldType = this.csharpReferenceFieldType(node);
        return fieldType === void 0 ? this.csharpTypeOfInitializer(node) : fieldType;
      }
    }
    if (_typescript2.default.isStringLiteralLike(node)) {
      return "string";
    }
    return this.csharpTypeOfInitializer(node);
  }
  // 'object' for a `this.<field>` read of a hand-written field the table proves a reference box;
  // undefined otherwise. Only printInlineEquality's null branch accepts 'object', so the exact
  // field type is never claimed and the value-equality branches keep the helper.
  csharpReferenceFieldType(node) {
    if (!_typescript2.default.isPropertyAccessExpression(node) || _optionalChain([node, 'access', _213 => _213.expression, 'optionalAccess', _214 => _214.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return void 0;
    }
    const name = _optionalChain([node, 'access', _215 => _215.name, 'optionalAccess', _216 => _216.escapedText]);
    if (CSHARP_REFERENCE_FIELDS_NATIVE.indexOf(name) < 0) {
      return void 0;
    }
    return this.csharpOperandIsValueTyped(node) ? void 0 : "object";
  }
  // the declaration behind an identifier read that is a plain method parameter; a
  // destructured or rest parameter prints a different declaration shape
  csharpParameterDeclaration(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const symbol = checker.getSymbolAtLocation(node);
    const declaration = _optionalChain([symbol, 'optionalAccess', _217 => _217.valueDeclaration]);
    if (declaration === void 0 || !_typescript2.default.isParameter(declaration)) {
      return void 0;
    }
    if (!_typescript2.default.isIdentifier(declaration.name) || declaration.name.escapedText !== node.escapedText || declaration.dotDotDotToken !== void 0) {
      return void 0;
    }
    return declaration;
  }
  // `object` for a parameter operand a null comparison compiles on, else undefined. An optional
  // parameter prints `object` / `string` / `Int64?` / `double?` / `bool?`; any other keeps the
  // helper unless its checker type holds no number/boolean member (a reference box).
  csharpParameterOperandType(node) {
    const declaration = this.csharpParameterDeclaration(node);
    if (declaration === void 0) {
      return void 0;
    }
    if (this.csharpDeclarationPrintsNullComparable(declaration)) {
      return "object";
    }
    return this.csharpOperandIsValueTyped(node) ? void 0 : "object";
  }
  // Whether this parameter's printed C# type is a reference or nullable value, so `x == null`
  // compiles and is isEqual's null branch. optionalScalarCsharpType gives a nullable scalar only
  // when optional with no initializer or `= undefined`; required or real-default ones are non-null.
  csharpDeclarationPrintsNullComparable(declaration) {
    const initializer = declaration.initializer;
    const optional = declaration.questionToken !== void 0 || initializer !== void 0;
    if (optional && (initializer === void 0 || initializer.kind === _typescript2.default.SyntaxKind.Identifier && initializer.escapedText === "undefined")) {
      return true;
    }
    return !this.csharpDeclarationHasValueScalar(declaration);
  }
  csharpDeclarationHasValueScalar(declaration) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return true;
    }
    const type = checker.getTypeAtLocation(declaration);
    return this.csharpTypeHasValueScalar(type);
  }
  // A numeric literal prints as an untyped C# constant that adapts to the operand on the
  // other side. isEqual's integer branches round-trip through Convert.ToInt64, which an
  // integer literal beyond 2^53 does not survive, so those stay on the helper.
  csharpNumericLiteralKind(node) {
    const value = Number(node.text);
    if (!Number.isFinite(value) || Number.isInteger(value) && !Number.isSafeInteger(value)) {
      return void 0;
    }
    return "number";
  }
  // the C# type the declaration behind an identifier was printed with ('object' when the
  // printer named none), or undefined when the identifier is not a printed local
  csharpDeclaredTypeOfBinding(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const symbol = checker.getSymbolAtLocation(node);
    const declaration = _optionalChain([symbol, 'optionalAccess', _218 => _218.valueDeclaration]);
    if (declaration === void 0) {
      return void 0;
    }
    if (declaration.kind === _typescript2.default.SyntaxKind.VariableDeclaration) {
      return this.getCSharpLocalType(declaration);
    }
    if (declaration.kind === _typescript2.default.SyntaxKind.BindingElement) {
      return "var";
    }
    return void 0;
  }
  // the C# operator family an operand belongs to: `==` compares two operands of one family
  // by value, exactly like the isEqual branches those types take
  csharpValueEqualityKind(csharpType) {
    switch (csharpType) {
      case "string":
      case "string?":
        return "string";
      case "bool":
      case "bool?":
        return "bool";
      case "double":
      case "double?":
        return "double";
      case "Int64":
      case "long":
      case "Int64?":
      case "long?":
        return "Int64";
      case "int":
      case "int?":
        return "int";
      case "number":
        return "number";
    }
    return void 0;
  }
  // `x == null` compiles — and matches isEqual — for reference types and nullable value
  // types, but not for a non-nullable value type (double / bool / Int64 / int). Lists,
  // dictionaries, class instances and `var` are references, and are the only types left
  // once csharpValueEqualityKind has claimed the value-typed names above.
  csharpIsNullComparableType(csharpType) {
    if (csharpType === void 0 || csharpType === "" || csharpType === "null") {
      return false;
    }
    if (csharpType.endsWith("?")) {
      return true;
    }
    if (csharpType === "object" || csharpType === "string") {
      return true;
    }
    return this.csharpValueEqualityKind(csharpType) === void 0;
  }
  // `x == null` compiles and matches isEqual's null branch when x's printed type is a
  // reference box or a nullable value type: the operand table's declared types, an optional
  // parameter, or a box the checker proves holds no number/boolean scalar.
  csharpOperandIsNullComparable(node) {
    const declaration = this.csharpParameterDeclaration(node);
    if (declaration !== void 0 && this.csharpDeclarationPrintsNullComparable(declaration)) {
      return true;
    }
    return !this.csharpOperandIsValueTyped(node);
  }
  // U55: the null-comparison rule's gate, stricter than csharpIsNullComparableType above:
  // only a type the hook names (the emitted declaration's own text) is accepted, `object` /
  // `var` included in the refusal because those are the boxes the classifier did not name.
  csharpNullComparisonTypeIsProvable(csharpType) {
    if (csharpType === void 0 || csharpType === "" || csharpType === "object" || csharpType === "var" || csharpType === "null") {
      return false;
    }
    if (csharpType.endsWith("?")) {
      return true;
    }
    return CSHARP_NULL_COMPARISON_REFERENCE_HEADS.some((head) => csharpType.startsWith(head));
  }
  // TypeScript numbers and booleans are C# value types in this port (double / bool /
  // Int64 / int), and the ccxt build script retypes some `object` declarations to exactly
  // those from its own tables (precisionFromString -> int, milliseconds -> Int64,
  // isEmpty -> bool). A null comparison against one of them would not compile, and the
  // printer's `object` cannot rule it out, so these always keep the helper.
  csharpOperandIsValueTyped(node) {
    if (node === void 0) {
      return true;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return true;
    }
    const type = checker.getTypeAtLocation(node);
    return this.csharpTypeHasValueScalar(type);
  }
  csharpTypeHasValueScalar(type) {
    if (type === void 0) {
      return true;
    }
    const flags = type.flags;
    if (flags & _typescript2.default.TypeFlags.Union) {
      const members = _nullishCoalesce(type.types, () => ( []));
      return members.some((member) => this.csharpTypeHasValueScalar(member));
    }
    return (flags & (_typescript2.default.TypeFlags.Number | _typescript2.default.TypeFlags.NumberLiteral | _typescript2.default.TypeFlags.Boolean | _typescript2.default.TypeFlags.BooleanLiteral)) !== 0;
  }
  // `x == "lit"` / `x != "lit"` prints natively when x is a read of a local whose printed
  // declaration is a C# string: the printer's declared-local table, then the embedding build
  // layer's proof for the declarations it retypes itself (ccxt's csharp-local-types.js).
  csharpDeclaredStringLiteralComparison(left, right, leftText, rightText, isEquality) {
    const ident = _typescript2.default.isIdentifier(left) ? left : _typescript2.default.isIdentifier(right) ? right : void 0;
    if (ident === void 0 || !_typescript2.default.isStringLiteralLike(ident === left ? right : left)) {
      return void 0;
    }
    const declared = this.csharpDeclaredReadType(ident);
    if (declared === void 0 || !this.csharpTypeIsStringType(declared)) {
      return void 0;
    }
    return isEquality ? `(${leftText} == ${rightText})` : `(${leftText} != ${rightText})`;
  }
  // the C# type the declaration behind a local read was printed with, or undefined: the
  // printer's own declared-local table first, then the embedding build layer's answer for a
  // declaration it retyped after the printer had left it `object`
  csharpDeclaredReadType(node) {
    let named;
    try {
      named = this.csharpTypedLocalType(node);
    } catch (e) {
      named = void 0;
    }
    if (named !== void 0) {
      return named;
    }
    const resolver = this.csharpExpressionTypeResolver;
    if (typeof resolver !== "function") {
      return void 0;
    }
    let resolved;
    try {
      resolved = resolver(node);
    } catch (e) {
      return void 0;
    }
    return typeof resolved === "string" ? resolved : void 0;
  }
  // the declared type of an identifier read, including this printer's own local table
  csharpDeclaredLocalType(node) {
    const provided = this.csharpExpressionTypeResolver ? this.csharpExpressionTypeResolver(node) : void 0;
    if (provided !== void 0) {
      return provided;
    }
    const declaration = _optionalChain([this, 'access', _219 => _219.getChecker, 'call', _220 => _220(), 'access', _221 => _221.getSymbolAtLocation, 'call', _222 => _222(node), 'optionalAccess', _223 => _223.valueDeclaration]);
    if (declaration === void 0 || !_typescript2.default.isVariableDeclaration(declaration) || _optionalChain([declaration, 'access', _224 => _224.name, 'optionalAccess', _225 => _225.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    if (!this.csharpLocalTypes.has(declaration)) {
      this.csharpLocalTypes.set(declaration, this.getCSharpLocalType(declaration));
    }
    const type = this.csharpLocalTypes.get(declaration);
    return type === void 0 || type === this.VAR_TOKEN ? void 0 : type;
  }
  // `a === b` / `a !== b` between two plain reads: a local or parameter prints as a bare
  // name and both operands are read once by the printed expression
  csharpOperandsAreDeclaredReads(left, right) {
    const isRead = (node) => _optionalChain([node, 'optionalAccess', _226 => _226.kind]) === _typescript2.default.SyntaxKind.Identifier && node.escapedText !== "undefined";
    return isRead(left) && isRead(right);
  }
  // the C# type the embedding build layer declares for a read the printer can only call
  // `object`: that layer retypes the declaration so its recorded type IS the printed one
  csharpDeclaredReadEqualityType(node, printerType) {
    if (printerType !== void 0 && printerType !== this.VAR_TOKEN && printerType !== "var") {
      return printerType;
    }
    const resolver = this.csharpExpressionTypeResolver;
    if (typeof resolver !== "function") {
      return printerType;
    }
    let resolved;
    try {
      resolved = resolver(node);
    } catch (e) {
      return printerType;
    }
    return typeof resolved === "string" ? resolved : printerType;
  }
  // `==` / `!=` in place of the isEqual wrapper when both operands are C# values of one
  // family, or one side is null/undefined against a type `== null` compiles for. Both
  // operands are printed once, so neither is evaluated twice.
  printInlineEquality(left, right, leftText, rightText, isEquality) {
    const stringComparison = this.csharpDeclaredStringLiteralComparison(left, right, leftText, rightText, isEquality);
    if (stringComparison !== void 0) {
      return stringComparison;
    }
    let leftType = this.csharpEqualityOperandType(left);
    let rightType = this.csharpEqualityOperandType(right);
    if (this.csharpOperandsAreDeclaredReads(left, right)) {
      leftType = this.csharpDeclaredReadEqualityType(left, leftType);
      rightType = this.csharpDeclaredReadEqualityType(right, rightType);
    }
    if (leftType === "null") {
      if (!this.csharpIsNullComparableType(rightType) || !this.csharpOperandIsNullComparable(right)) {
        return void 0;
      }
      return this.csharpNullComparison(rightText, isEquality);
    }
    if (rightType === "null") {
      if (!this.csharpIsNullComparableType(leftType) || !this.csharpOperandIsNullComparable(left)) {
        return void 0;
      }
      return this.csharpNullComparison(leftText, isEquality);
    }
    const leftKind = this.csharpValueEqualityKind(leftType);
    const rightKind = this.csharpValueEqualityKind(rightType);
    const leftReadKind = leftKind !== void 0 ? leftKind : this.csharpDeclaredReadEqualityKind(left, leftType);
    const rightReadKind = rightKind !== void 0 ? rightKind : this.csharpDeclaredReadEqualityKind(right, rightType);
    if (leftReadKind === void 0 || rightReadKind === void 0) {
      return void 0;
    }
    const numericKinds = ["double", "Int64", "int"];
    const sameKind = leftReadKind === rightReadKind;
    const literalVsNumeric = leftReadKind === "number" && numericKinds.indexOf(rightReadKind) >= 0 || rightReadKind === "number" && numericKinds.indexOf(leftReadKind) >= 0;
    if (!sameKind && !literalVsNumeric) {
      return void 0;
    }
    return isEquality ? `(${leftText} == ${rightText})` : `(${leftText} != ${rightText})`;
  }
  // the value kind of an operand the printer could only call `object`: the type the read's
  // declaration was PRINTED with (printer table -> build layer's read-type oracle -> the
  // declared-type registry for the declarations its own passes retyped). A collection/class
  csharpDeclaredReadEqualityKind(node, operandType) {
    if (operandType !== void 0 && operandType !== "" && operandType !== "object") {
      return void 0;
    }
    if (_optionalChain([node, 'optionalAccess', _227 => _227.kind]) !== _typescript2.default.SyntaxKind.Identifier || node.escapedText === "undefined") {
      return void 0;
    }
    const declared = _nullishCoalesce(this.csharpDeclaredReadType(node), () => ( this.csharpDeclaredLocalResolverType(node)));
    return declared === void 0 ? void 0 : this.csharpValueEqualityKind(declared);
  }
  csharpNullComparison(text, isEquality) {
    return isEquality ? `(${text} == null)` : `(${text} != null)`;
  }
  // `isEqual(<numeric call>, N)` / `isEqual(N, <numeric call>)` -> `==` / `!=`: the call prints a
  // concrete numeric kind, the literal adapts to it, and the operator matches isEqual's integer and
  // double branches. String/bool/collection calls and every `object` box keep the helper.
  csharpNativeNumericCallEquality(left, right, leftText, rightText, isEquality) {
    const leftKind = this.csharpNumericCallKind(left);
    const rightKind = this.csharpNumericCallKind(right);
    const leftLiteral = leftKind === void 0 ? this.csharpIntegerLiteralKind(left) : void 0;
    const rightLiteral = rightKind === void 0 ? this.csharpIntegerLiteralKind(right) : void 0;
    const callOnLeft = leftKind !== void 0 && rightLiteral !== void 0 && this.csharpNumericKindHoldsLiteral(leftKind, rightLiteral);
    const callOnRight = rightKind !== void 0 && leftLiteral !== void 0 && this.csharpNumericKindHoldsLiteral(rightKind, leftLiteral);
    if (!callOnLeft && !callOnRight) {
      return void 0;
    }
    return isEquality ? `(${leftText} == ${rightText})` : `(${leftText} != ${rightText})`;
  }
  // The C# value kind of a call operand (or `x.length`) when its printed signature is numeric:
  // `.indexOf`/`.length` (int) and safe* accessors from the printer's tables, `this.<name>()` from
  // CSHARP_NATIVE_NUMERIC_THIS_KINDS. undefined keeps the helper (an `object` box has no kind).
  csharpNumericCallKind(node) {
    const expression = _optionalChain([node, 'optionalAccess', _228 => _228.expression]);
    if (_optionalChain([node, 'optionalAccess', _229 => _229.kind]) === _typescript2.default.SyntaxKind.CallExpression && _optionalChain([expression, 'optionalAccess', _230 => _230.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression && _optionalChain([expression, 'access', _231 => _231.expression, 'optionalAccess', _232 => _232.kind]) === _typescript2.default.SyntaxKind.ThisKeyword) {
      const named2 = CSHARP_NATIVE_NUMERIC_THIS_KINDS[_optionalChain([expression, 'access', _233 => _233.name, 'optionalAccess', _234 => _234.escapedText])];
      if (named2 !== void 0 && this.csharpCalleeResolves(node)) {
        return named2;
      }
    }
    const named = this.csharpCallReturnType(node);
    return named === void 0 || CSHARP_NUMERIC_VALUE_KINDS.indexOf(named) < 0 ? void 0 : named;
  }
  // The C# kind of an integer literal operand (`N` / `-N`), or undefined when the text is not an
  // integer the literal holds exactly. isEqual round-trips through Convert.ToInt64 and truncates
  // via `(int)a == (int)b`, so only a safe integer literal keeps both comparisons identical.
  csharpIntegerLiteralKind(node) {
    let value;
    if (_optionalChain([node, 'optionalAccess', _235 => _235.kind]) === _typescript2.default.SyntaxKind.PrefixUnaryExpression) {
      if (node.operator !== _typescript2.default.SyntaxKind.MinusToken || _optionalChain([node, 'access', _236 => _236.operand, 'optionalAccess', _237 => _237.kind]) !== _typescript2.default.SyntaxKind.NumericLiteral) {
        return void 0;
      }
      value = -Number(node.operand.text);
    } else if (_optionalChain([node, 'optionalAccess', _238 => _238.kind]) === _typescript2.default.SyntaxKind.NumericLiteral) {
      value = Number(node.text);
    } else {
      return void 0;
    }
    if (!Number.isSafeInteger(value)) {
      return void 0;
    }
    return value >= -2147483648 && value <= 2147483647 ? "int" : "long";
  }
  // a `long` literal has no implicit conversion to an `int` operand; every other pair
  // converts the literal exactly, which is what isEqual's Convert.ToInt64 /
  // Convert.ToDouble branches do with the same two boxes
  csharpNumericKindHoldsLiteral(callKind, literalKind) {
    if (callKind === "int") {
      return literalKind === "int";
    }
    return callKind.indexOf("Int64") === 0 || callKind.indexOf("double") === 0;
  }
  // the concrete C# type of an expression the printer can name, or undefined: the embedding
  // build layer's proof wins (it retypes locals the printer leaves `object`), then the
  // printer's own tables and the literals whose C# type is fixed by their text
  csharpExpressionTypeOf(node) {
    const provided = this.csharpExpressionTypeResolver ? this.csharpExpressionTypeResolver(node) : void 0;
    if (provided !== void 0) {
      return provided;
    }
    if (_typescript2.default.isNumericLiteral(node)) {
      const value = Number(node.text);
      return Number.isInteger(value) && Math.abs(value) <= 2147483647 ? "int" : void 0;
    }
    if (_typescript2.default.isPrefixUnaryExpression(node) && node.operator === _typescript2.default.SyntaxKind.MinusToken && _typescript2.default.isNumericLiteral(node.operand)) {
      const value = Number(node.operand.text);
      return Number.isInteger(value) && value <= 2147483647 ? "int" : void 0;
    }
    return this.csharpTypeOfInitializer(node);
  }
  // the TypeScript checker must see two plain numbers: `any` (could be a string box) and a
  // nullable union (the helper orders null, C# would throw) both keep the runtime helper
  csharpOperandsAreNumbers(node) {
    return this.csharpOperandIsPlainNumber(node.left) && this.csharpOperandIsPlainNumber(node.right);
  }
  csharpOperandIsPlainNumber(operand) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const flags = _optionalChain([checker, 'access', _239 => _239.getTypeAtLocation, 'call', _240 => _240(operand), 'optionalAccess', _241 => _241.flags]);
    return flags === _typescript2.default.TypeFlags.Number || flags === _typescript2.default.TypeFlags.NumberLiteral;
  }
  // `<`, `>`, `<=`, `>=` on two operands whose printed C# kind this printer can name (declaration,
  // literal, known call signature — never the JS type) print natively: the helper applies the same
  // conversions as the C# operator; an int/Int64 pair compares the same. See CSHARP_NUMERIC_KINDS.
  csharpNativeNumericComparison(node, identation) {
    const token = CSHARP_NATIVE_COMPARISON_TOKENS[node.operatorToken.kind];
    if (token === void 0) {
      return void 0;
    }
    const leftKind = this.csharpExpressionTypeOf(node.left);
    const rightKind = this.csharpExpressionTypeOf(node.right);
    if (leftKind === void 0 || rightKind === void 0) {
      return void 0;
    }
    const integerPair = CSHARP_INTEGER_KINDS.indexOf(leftKind) >= 0 && CSHARP_INTEGER_KINDS.indexOf(rightKind) >= 0;
    if (!integerPair && (leftKind !== rightKind || CSHARP_NUMERIC_KINDS.indexOf(leftKind) < 0)) {
      return void 0;
    }
    if (leftKind === "double" && (token === "<" || token === "<=")) {
      return void 0;
    }
    const leftText = this.printNode(node.left, 0).trim();
    const rightText = this.printNode(node.right, 0).trim();
    return leftText + " " + token + " " + rightText;
  }
  // `Math.min/max (a, b)`: mathMin/mathMax read both boxes via Convert.ToDouble, return an ORIGINAL
  // one (null if either is null); Math.Min/Max returns one numeric kind. Native only when both are
  // declared integers of the same kind and the checker sees two plain numbers; else keep the helper.
  csharpNativeMathMinMax(node, name, parsedArg1, parsedArg2) {
    const args = node.arguments;
    if (_optionalChain([args, 'optionalAccess', _242 => _242.length]) !== 2 || !this.csharpMinMaxResultIsPlainValue(node)) {
      return void 0;
    }
    const leftKind = this.csharpExpressionTypeOf(args[0]);
    if (leftKind === void 0 || leftKind !== this.csharpExpressionTypeOf(args[1]) || CSHARP_MINMAX_NATIVE_KINDS.indexOf(leftKind) < 0) {
      return void 0;
    }
    if (!this.csharpOperandIsPlainNumber(args[0]) || !this.csharpOperandIsPlainNumber(args[1])) {
      return void 0;
    }
    return "Math." + name + "(" + parsedArg1 + ", " + parsedArg2 + ")";
  }
  // the positions that take the primitive as the helper's box: assignment, argument,
  // dictionary/list value, return and ternary arm all box an int the same way. A receiver, an
  // `as`/`!` wrapper and the printer's hard `(string)` casts (throw / `delete`) do not.
  csharpMinMaxResultIsPlainValue(node) {
    if (this.csharpIsClassThrowArgument(node) || this.csharpIsDeleteKey(node)) {
      return false;
    }
    let parent = node.parent;
    while (parent !== void 0 && _typescript2.default.isParenthesizedExpression(parent)) {
      parent = parent.parent;
    }
    if (parent === void 0) {
      return false;
    }
    if ((_typescript2.default.isPropertyAccessExpression(parent) || _typescript2.default.isElementAccessExpression(parent) || _typescript2.default.isCallExpression(parent)) && parent.expression === node) {
      return false;
    }
    return parent.kind !== _typescript2.default.SyntaxKind.AsExpression && parent.kind !== _typescript2.default.SyntaxKind.TypeAssertionExpression && parent.kind !== _typescript2.default.SyntaxKind.NonNullExpression;
  }
  // `parseInt(...)` / `parseFloat(...)` print the runtime helper call; the folds below replace
  // it with the exact box the helper returns, everything else falls through to the printer
  printCallExpression(node, identation) {
    const nativeParse = this.csharpNativeParseCall(node);
    if (nativeParse !== void 0) {
      return nativeParse;
    }
    return super.printCallExpression(node, identation);
  }
  csharpNativeParseCall(node) {
    if (_optionalChain([node, 'optionalAccess', _243 => _243.kind]) !== _typescript2.default.SyntaxKind.CallExpression || !_typescript2.default.isIdentifier(node.expression)) {
      return void 0;
    }
    const callee = node.expression.escapedText;
    if (callee !== "parseInt" && callee !== "parseFloat") {
      return void 0;
    }
    const args = node.arguments;
    if (args === void 0 || args.length !== 1) {
      return void 0;
    }
    if (!this.csharpCalleeIsGlobalFunction(node.expression)) {
      return void 0;
    }
    const literal = callee === "parseInt" ? csharpParseIntLiteralArgument(args[0]) : csharpParseFloatLiteralArgument(args[0]);
    if (literal !== void 0) {
      return literal;
    }
    return this.csharpNativeParseCallOnDeclaredLocal(callee, args[0]);
  }
  // the global `parseInt` / `parseFloat` live in the TS lib chain; a declaration anywhere else
  // means the call prints a different function than the runtime helper
  csharpCalleeIsGlobalFunction(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return true;
    }
    const declarations = _nullishCoalesce(_optionalChain([checker, 'access', _244 => _244.getSymbolAtLocation, 'call', _245 => _245(node), 'optionalAccess', _246 => _246.declarations]), () => ( []));
    return declarations.every((declaration) => declaration.getSourceFile().fileName.indexOf("typescript") > -1);
  }
  // `parseFloat(x)` / `parseInt(x)` on a local declared numeric: Convert.ToDouble is an identity for
  // `double` and a widening for integer kinds. An Int64 parseInt operand (rounded above 2^53), a
  // double one (NaN/overflow -> null) and nullable or `object` locals (null -> 0) keep the helper.
  csharpNativeParseCallOnDeclaredLocal(callee, arg) {
    if (_optionalChain([arg, 'optionalAccess', _247 => _247.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const kind = this.csharpExpressionTypeOf(arg);
    if (CSHARP_NUMERIC_KINDS.indexOf(kind) < 0) {
      return void 0;
    }
    const text = this.printNode(arg, 0);
    if (callee === "parseFloat") {
      return kind === "double" ? text : `((double)${text})`;
    }
    return kind === "int" ? `((Int64)${text})` : void 0;
  }
  // `a % b` prints `mod(a, b)`: the helper takes the double remainder and converts back to Int64. An
  // Int32 dividend with a nonzero integer literal divisor is exact as double, so the native Int64
  // remainder matches. Int64 dividends (rounded above 2^53) and possibly-zero divisors keep helper.
  csharpNativeModExpression(left, right, leftText) {
    if (_optionalChain([left, 'optionalAccess', _248 => _248.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    if (this.csharpExpressionTypeOf(left) !== "int") {
      return void 0;
    }
    const divisor = csharpLiteralNumericValue(right);
    if (divisor === void 0 || !Number.isSafeInteger(divisor) || divisor === 0) {
      return void 0;
    }
    return `((Int64)${leftText} % ${divisor}L)`;
  }
  // `-x` prints `prefixUnaryNeg(ref x)`, whose typed overloads negate in place and return the same
  // box (`a = -a; return a;`): a local declared int / Int64 / double binds one, so the assignment is
  // the identical emission. Every other operand keeps the helper's runtime dispatch and null answer.
  csharpNativeNegatedLocal(operand, leftSide) {
    if (_optionalChain([operand, 'optionalAccess', _249 => _249.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    if (CSHARP_NUMERIC_KINDS.indexOf(this.csharpExpressionTypeOf(operand)) < 0) {
      return void 0;
    }
    return `(${leftSide} = -${leftSide})`;
  }
  // The printed receiver whose C# static type is a known collection: a local this printer declared
  // with a concrete type (csharpTypedLocals), or a hand-written BaseExchange field. undefined keeps
  // the runtime helper, since the printer cannot name the operand's type.
  csharpNativeReceiver(node) {
    if (_optionalChain([node, 'optionalAccess', _250 => _250.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      const inner = this.csharpNativeReceiver(node.expression);
      return inner === void 0 ? void 0 : { text: `(${inner.text})`, type: inner.type };
    }
    if (_typescript2.default.isIdentifier(node)) {
      const named = this.csharpTypedLocalType(node);
      return named === void 0 || CSHARP_NATIVE_COLLECTION_TYPES.indexOf(named) < 0 ? void 0 : { text: this.printNode(node, 0), type: named };
    }
    if (_typescript2.default.isPropertyAccessExpression(node) && _optionalChain([node, 'access', _251 => _251.expression, 'optionalAccess', _252 => _252.kind]) === _typescript2.default.SyntaxKind.ThisKeyword) {
      const name = _optionalChain([node, 'access', _253 => _253.name, 'optionalAccess', _254 => _254.escapedText]);
      if (CSHARP_OBJECT_DICT_FIELDS.indexOf(name) >= 0) {
        return { text: `((IDictionary<string, object>)${this.printNode(node, 0)})`, type: "IDictionary<string, object>" };
      }
      return CSHARP_NATIVE_FIELDS[name] === void 0 ? void 0 : { text: this.printNode(node, 0), type: CSHARP_NATIVE_FIELDS[name] };
    }
    const callType = this.csharpCallReturnType(node);
    if (callType !== void 0 && CSHARP_NATIVE_COLLECTION_TYPES.indexOf(callType) >= 0) {
      const printed = this.printNode(node, 0);
      return { text: printed.startsWith("new ") ? `(${printed})` : printed, type: callType };
    }
    return void 0;
  }
  // the C# types this printer can name on a local whose members replace the helpers:
  // the collection types (Count/ContainsKey) and string (Length/ContainsKey keys)
  csharpTypeIsNative(csharpType) {
    return CSHARP_NATIVE_COLLECTION_TYPES.indexOf(csharpType) >= 0 || csharpType.indexOf("string") === 0;
  }
  // the C# type this printer declared for a local read, or undefined
  csharpTypedLocalType(node) {
    const declaration = _optionalChain([this, 'access', _255 => _255.getChecker, 'call', _256 => _256(), 'access', _257 => _257.getSymbolAtLocation, 'call', _258 => _258(node), 'optionalAccess', _259 => _259.valueDeclaration]);
    return declaration === void 0 ? void 0 : this.csharpTypedLocals.get(declaration);
  }
  // the member that replaces getArrayLength on a declared C# type: Count counts the same
  // elements the helper's IList / ICollection branches count, Length is its string branch.
  // Any other declared type keeps the helper
  csharpCountMemberOf(csharpType) {
    if (csharpType === "string" || csharpType === "string?") {
      return "Length";
    }
    return CSHARP_COUNT_TYPES.some((prefix) => csharpType.indexOf(prefix) === 0) ? "Count" : void 0;
  }
  // `getArrayLength(x)` -> `(x?.Count ?? 0)` for a local whose printed declaration carries a C#
  // collection / string type (printer's declared-local table, then build/csharp-local-types.js).
  // The null-conditional is the helper's `null -> 0`; a plain member read would throw instead.
  csharpDeclaredLengthExpression(node) {
    const printed = this.printNode(node, 0).trim();
    const cast = CSHARP_LENGTH_CAST.exec(printed);
    if (cast !== null) {
      const castMember = this.csharpCountMemberOf(cast[1]);
      if (castMember !== void 0) {
        return `(${_typescript2.default.isIdentifier(node) ? printed : `(${printed})`}?.${castMember} ?? 0)`;
      }
    }
    const receiver = this.csharpLengthReceiverIdentifier(node);
    if (receiver === void 0) {
      return void 0;
    }
    const named = this.csharpTypedLocalType(receiver);
    const csharpType = named !== void 0 ? named : _nullishCoalesce((this.csharpExpressionTypeResolver ? this.csharpExpressionTypeResolver(receiver) : void 0), () => ( this.csharpLengthReceiverType(receiver)));
    const member = csharpType === void 0 ? void 0 : this.csharpCountMemberOf(csharpType);
    if (member === void 0) {
      return void 0;
    }
    const receiverText = this.printNode(receiver, 0);
    if (this.printNode(node, 0).trim() !== receiverText.trim()) {
      return void 0;
    }
    return `(${receiverText}?.${member} ?? 0)`;
  }
  // the printed key of ContainsKey must itself be a C# string: a literal, a local this
  // printer declared `string`, a call it types as string, or its own `((string)x)` cast
  csharpNativeStringKey(key) {
    if (_optionalChain([key, 'optionalAccess', _260 => _260.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.csharpNativeStringKey(key.expression);
    }
    if (_typescript2.default.isStringLiteralLike(key)) {
      return this.printNode(key, 0);
    }
    if (_typescript2.default.isIdentifier(key)) {
      const named = this.csharpTypedLocalType(key);
      if (named === "string") {
        return this.printNode(key, 0);
      }
      if (named === void 0 || named.indexOf("string") !== 0) {
        return void 0;
      }
      return this.isStringType(this.getChecker().getTypeAtLocation(key).flags) ? this.printNode(key, 0) : void 0;
    }
    if (this.csharpCallReturnType(key) === "string") {
      return this.isStringType(this.getChecker().getTypeAtLocation(key).flags) ? this.printNode(key, 0) : void 0;
    }
    const printed = this.printNode(key, 0);
    if (!printed.startsWith("((string)")) {
      return void 0;
    }
    return this.isStringType(this.getChecker().getTypeAtLocation(key).flags) ? printed : void 0;
  }
  // the checker's view of an `in` / `.length` operand: a dictionary carries a string
  // index signature, an array is the Array reference type. `any` proves nothing
  csharpIsDictionaryType(type) {
    if (type === void 0 || this.isAnyType(type.flags)) {
      return false;
    }
    return this.getChecker().getIndexTypeOfType(type, _typescript2.default.IndexKind.String) !== void 0;
  }
  csharpIsArrayType(type) {
    if (type === void 0 || this.isAnyType(type.flags)) {
      return false;
    }
    return _optionalChain([type, 'optionalAccess', _261 => _261.symbol, 'optionalAccess', _262 => _262.escapedName]) === "Array";
  }
  // a union of dictionary members and nullish ones (what a `Dict | undefined` signature
  // widens to): a C# reference that happens to be null is a nullish arm, not a scalar one.
  // Any other member (arrays, classes, scalars) keeps the helper
  csharpNullableDictionaryType(type) {
    if (type === void 0 || (type.flags & _typescript2.default.TypeFlags.Union) === 0) {
      return false;
    }
    let dictionaries = 0;
    for (const member of _nullishCoalesce(type.types, () => ( []))) {
      if ((member.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null)) !== 0) {
        continue;
      }
      if (!this.csharpIsDictionaryType(member)) {
        return false;
      }
      dictionaries++;
    }
    return dictionaries > 0;
  }
  // a dictionary whose values are `any` — the shape the transpiled C# boxes as
  // Dictionary<string, object>. A `Dictionary<Future>` / `Dictionary<Currency>` keeps the
  // helper: the C# cast target is invariant, so `IDictionary<string, Future>` would throw
  csharpIsAnyValuedDictionaryType(type) {
    if (!this.csharpIsDictionaryType(type)) {
      return false;
    }
    const value = this.getChecker().getIndexTypeOfType(type, _typescript2.default.IndexKind.String);
    return value !== void 0 && this.isAnyType(value.flags);
  }
  // the parameter printFunctionBody gives a `??= new Dictionary<string, object>()` line
  // (its initializer is an object literal): from the first statement on its box is a
  // dictionary whatever the caller passed, so a null check would be dead code
  csharpDictionaryParamsBag(node) {
    const declaration = _optionalChain([this, 'access', _263 => _263.getChecker, 'call', _264 => _264(), 'access', _265 => _265.getSymbolAtLocation, 'call', _266 => _266(node), 'optionalAccess', _267 => _267.valueDeclaration]);
    const initializer = _optionalChain([declaration, 'optionalAccess', _268 => _268.kind]) === _typescript2.default.SyntaxKind.Parameter ? declaration.initializer : void 0;
    return _optionalChain([initializer, 'optionalAccess', _269 => _269.kind]) === _typescript2.default.SyntaxKind.ObjectLiteralExpression;
  }
  // D2: the name is rewritten before this read, so the box it holds there is not the one its
  // declaration started with (the scan is a name match, so any earlier write bails)
  csharpNameWrittenBefore(node, name) {
    const scope = this.csharpEnclosingFunction(node);
    if (scope === void 0) {
      return true;
    }
    let written = false;
    const visit = (n) => {
      if (written || n.pos >= node.pos) {
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.Identifier && n.escapedText === name) {
        const parent = n.parent;
        const assignment = _optionalChain([parent, 'optionalAccess', _270 => _270.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && parent.left === n;
        const increment = (_optionalChain([parent, 'optionalAccess', _271 => _271.kind]) === _typescript2.default.SyntaxKind.PostfixUnaryExpression || _optionalChain([parent, 'optionalAccess', _272 => _272.kind]) === _typescript2.default.SyntaxKind.PrefixUnaryExpression) && (parent.operator === _typescript2.default.SyntaxKind.PlusPlusToken || parent.operator === _typescript2.default.SyntaxKind.MinusMinusToken);
        if (assignment || increment) {
          written = true;
          return;
        }
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return written;
  }
  // The C# dictionary a ContainsKey receiver reads through, or undefined: printer tables, then the
  // build layer's declared-type table, then an `object` parameter proven a dictionary by TS or an
  // emitted `??= new Dictionary<string, object>()`. That case applies the helper's cast + null test.
  csharpNativeDictionaryReceiver(obj) {
    const checker = this.getChecker();
    const type = checker.getTypeAtLocation(obj);
    const nullable = this.csharpNullableDictionaryType(type);
    const receiver = this.csharpNativeReceiver(obj);
    if (receiver !== void 0) {
      if (receiver.type.indexOf("Dictionary<") < 0) {
        return void 0;
      }
      return nullable ? { text: receiver.text, nullTest: receiver.text } : { text: receiver.text };
    }
    const declared = this.csharpExpressionTypeOf(obj);
    if (declared !== void 0) {
      return declared.indexOf("Dictionary<") >= 0 ? { text: this.printNode(obj, 0) } : void 0;
    }
    if (!_typescript2.default.isIdentifier(obj) || this.csharpTypedLocalType(obj) !== void 0) {
      return void 0;
    }
    const name = obj.escapedText;
    if (this.csharpNameWrittenBefore(obj, name)) {
      return void 0;
    }
    const declaration = _optionalChain([checker, 'access', _273 => _273.getSymbolAtLocation, 'call', _274 => _274(obj), 'optionalAccess', _275 => _275.valueDeclaration]);
    if (_optionalChain([declaration, 'optionalAccess', _276 => _276.kind]) !== _typescript2.default.SyntaxKind.Parameter) {
      return void 0;
    }
    const printed = this.printNode(obj, 0);
    const text = `((IDictionary<string, object>)${printed})`;
    if (this.csharpDictionaryParamsBag(obj)) {
      return { text };
    }
    if (!this.csharpIsDictionaryType(type) && !nullable) {
      return void 0;
    }
    if (!this.csharpIsAnyValuedDictionaryType(type)) {
      return void 0;
    }
    return { text, nullTest: printed };
  }
  // `key in obj` -> `obj.ContainsKey(key)`, only when both the printed key and the printed
  // operand are already C# string / dictionary values. Every other shape keeps the inOp helper
  csharpNativeInExpression(key, obj) {
    const printedKey = this.csharpNativeStringKey(key);
    if (printedKey === void 0) {
      return this.csharpDeclaredDictInExpression(key, obj);
    }
    const receiver = this.csharpNativeDictionaryReceiver(obj);
    if (receiver !== void 0) {
      const call = `${receiver.text}.ContainsKey(${printedKey})`;
      return receiver.nullTest === void 0 ? call : `(${receiver.nullTest} != null && ${call})`;
    }
    return this.csharpDeclaredDictInExpression(key, obj);
  }
  // `key in obj` on a local whose EMITTED declaration is a dictionary the printer cannot name itself
  // (build/csharp-local-types.js retypes it, answers csharpDeclaredDictReceiverType): inOp's dict
  // branch is exactly `obj?.ContainsKey(key) == true` for a key that is already a C# string.
  csharpDeclaredDictInExpression(key, obj) {
    if (_optionalChain([obj, 'optionalAccess', _277 => _277.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const declared = typeof this.csharpDeclaredDictReceiverType === "function" ? this.csharpDeclaredDictReceiverType(obj) : void 0;
    if (declared === void 0 || declared.indexOf("Dictionary<") < 0) {
      return void 0;
    }
    const printedKey = _nullishCoalesce(this.csharpNativeStringKey(key), () => ( this.csharpDeclaredStringKey(key)));
    if (printedKey === void 0) {
      return void 0;
    }
    const receiver = this.printNode(obj, 0);
    if (_typescript2.default.isStringLiteralLike(key)) {
      return `(${receiver}?.ContainsKey(${printedKey}) == true)`;
    }
    if (_typescript2.default.isIdentifier(key)) {
      return `((${printedKey} != null) && (${receiver}?.ContainsKey(${printedKey}) == true))`;
    }
    return void 0;
  }
  // the key of the rule above, when the printer's own tables name no string for it but the
  // embedding build layer's record does (the same hook the isEqual twin reads)
  csharpDeclaredStringKey(key) {
    if (_optionalChain([key, 'optionalAccess', _278 => _278.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const type = typeof this.csharpLocalTypeOf === "function" ? this.csharpLocalTypeOf(key) : void 0;
    return type === "string" || type === "string?" ? this.printNode(key, 0) : void 0;
  }
  // `x.length` -> `x.Count`, same proof for the checker's array operands; strings keep
  // the `((string)x).Length` branch and every unproven operand keeps getArrayLength
  csharpNativeLengthExpression(expression) {
    if (this.csharpIsArrayType(this.getChecker().getTypeAtLocation(expression))) {
      const receiver = this.csharpNativeReceiver(expression);
      if (receiver !== void 0) {
        return `${receiver.text}.Count`;
      }
    }
    return this.csharpDeclaredLengthExpression(expression);
  }
  // the C# type of a dictionary element read is `object`: isEqual compares the boxed
  // element, which `as` + the operator reproduce exactly (any other box reads as null,
  // where isEqual also answers false)
  csharpNativeElementLiteralEquality(left, right, leftText, rightText, isEquality) {
    let element;
    let literal;
    if (this.csharpStringKeyedElementAccess(left)) {
      element = left;
      literal = right;
    } else if (this.csharpStringKeyedElementAccess(right)) {
      element = right;
      literal = left;
    } else {
      return void 0;
    }
    const isBoolLiteral = literal.kind === _typescript2.default.SyntaxKind.TrueKeyword || literal.kind === _typescript2.default.SyntaxKind.FalseKeyword;
    const isStringLiteral = _typescript2.default.isStringLiteralLike(literal);
    if (!isBoolLiteral && !isStringLiteral) {
      return void 0;
    }
    if (this.csharpDictionaryReceiverType(element.expression) === void 0) {
      return void 0;
    }
    const wanted = isBoolLiteral ? CSHARP_SCALAR_ELEMENT_BOOL : CSHARP_SCALAR_ELEMENT_STRING;
    if ((this.csharpScalarElementKinds(element) & wanted) === 0) {
      return void 0;
    }
    const token = isEquality ? "==" : "!=";
    if (isStringLiteral && this.csharpNumericStringLiteral(literal)) {
      return void 0;
    }
    const cast = isBoolLiteral ? "bool?" : "string";
    const elementIsLeft = element === left;
    const castElement = `(${elementIsLeft ? leftText : rightText} as ${cast})`;
    const otherText = elementIsLeft ? rightText : leftText;
    return elementIsLeft ? `(${castElement} ${token} ${otherText})` : `(${otherText} ${token} ${castElement})`;
  }
  // `x["k"]` — the element read of a dictionary key this printer prints as getValue(x, "k")
  csharpStringKeyedElementAccess(node) {
    return _optionalChain([node, 'optionalAccess', _279 => _279.kind]) === _typescript2.default.SyntaxKind.ElementAccessExpression && _typescript2.default.isStringLiteralLike(node.argumentExpression);
  }
  // isEqual compares a boxed number with a numeric string by converting both, which the
  // string cast of the native form cannot reproduce: a numeric-looking literal stays on
  // the helper (the element proof says the box is a string, and a number box would differ)
  csharpNumericStringLiteral(node) {
    const text = String(node.text).trim();
    return text !== "" && !isNaN(Number(text));
  }
  // the declared C# type of a dictionary receiver: a local the embedding build layer retypes
  // (csharpExpressionTypeResolver) or a hand-written BaseExchange field; undefined keeps the
  // runtime helper, since the printer cannot name the box the key lives in
  csharpDictionaryReceiverType(node) {
    const native = this.csharpNativeReceiver(node);
    const declared = native !== void 0 ? native.type : this.csharpExpressionTypeOf(node);
    if (declared === void 0 || declared.indexOf("Dictionary<") < 0) {
      return void 0;
    }
    return declared;
  }
  // the scalar branches isEqual can compare an element with: every member of the element's
  // TypeScript type must be a boolean, a string or undefined, or the helper stays
  csharpScalarElementKinds(node) {
    try {
      const type = this.getChecker().getTypeAtLocation(node);
      const members = (type.flags & _typescript2.default.TypeFlags.Union) !== 0 ? _nullishCoalesce(type.types, () => ( [])) : [type];
      let kinds = 0;
      for (const member of members) {
        const flags = member.flags;
        if (flags & (_typescript2.default.TypeFlags.Boolean | _typescript2.default.TypeFlags.BooleanLiteral)) {
          kinds |= CSHARP_SCALAR_ELEMENT_BOOL;
        } else if (flags & (_typescript2.default.TypeFlags.String | _typescript2.default.TypeFlags.StringLiteral | _typescript2.default.TypeFlags.TemplateLiteral)) {
          kinds |= CSHARP_SCALAR_ELEMENT_STRING;
        } else if (!(flags & _typescript2.default.TypeFlags.Undefined)) {
          return 0;
        }
      }
      return kinds;
    } catch (e) {
      return 0;
    }
  }
  // the C# type the emitted declaration gives a `.length` receiver, or undefined when this
  // printer names none. Installed by build/csharp-local-types.js; undefined by default, so
  // the untyped emission is byte-identical without the override
  csharpLengthReceiverType(expression) {
    return void 0;
  }
  // `isEqual (x, "lit")` -> `x == "lit"` (and `!isEqual` -> `!=`) when the operand's emitted
  // declaration is a string: isEqual's string branch is the same ordinal comparison; a null operand
  // is false in both. Gated on csharpLocalTypeOf; an unnamed operand keeps the helper call.
  csharpStringLiteralEquality(op, left, right, leftText, rightText) {
    const equality = op === _typescript2.default.SyntaxKind.EqualsEqualsToken || op === _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken;
    const inequality = op === _typescript2.default.SyntaxKind.ExclamationEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken;
    if (!equality && !inequality) {
      return void 0;
    }
    const leftLiteral = _typescript2.default.isStringLiteral(left);
    const rightLiteral = _typescript2.default.isStringLiteral(right);
    if (leftLiteral === rightLiteral) {
      return void 0;
    }
    const operand = leftLiteral ? right : left;
    if (_optionalChain([operand, 'optionalAccess', _280 => _280.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const operandType = this.csharpLocalTypeOf(operand);
    if (operandType !== "string" && operandType !== "string?") {
      return void 0;
    }
    return `${leftText} ${inequality ? "!=" : "=="} ${rightText}`;
  }
  // U55: `isEqual (x, null)` -> `x == null` (`!isEqual` -> `!=`) when the emitted declaration is
  // a typed reference or nullable scalar: the helper's null guards return exactly the C# null test,
  // never reach a typed branch. Gated on csharpNullComparisonTypeOf; unnamed operands keep helper.
  csharpNullLiteralEquality(op, left, right, leftText, rightText) {
    const equality = op === _typescript2.default.SyntaxKind.EqualsEqualsToken || op === _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken;
    const inequality = op === _typescript2.default.SyntaxKind.ExclamationEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken;
    if (!equality && !inequality) {
      return void 0;
    }
    const leftNull = this.csharpOperandIsNullLiteral(left);
    const rightNull = this.csharpOperandIsNullLiteral(right);
    if (leftNull === rightNull) {
      return void 0;
    }
    let operand = leftNull ? right : left;
    while (_optionalChain([operand, 'optionalAccess', _281 => _281.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      operand = operand.expression;
    }
    if (_optionalChain([operand, 'optionalAccess', _282 => _282.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const operandType = typeof this.csharpNullComparisonTypeOf === "function" ? this.csharpNullComparisonTypeOf(operand) : void 0;
    if (!this.csharpNullComparisonTypeIsProvable(operandType)) {
      return void 0;
    }
    return this.csharpNullComparison(leftNull ? rightText : leftText, equality);
  }
  // `null` and the `undefined` identifier both print as the C# null literal
  csharpOperandIsNullLiteral(node) {
    return _optionalChain([node, 'optionalAccess', _283 => _283.kind]) === _typescript2.default.SyntaxKind.NullKeyword || _optionalChain([node, 'optionalAccess', _284 => _284.kind]) === _typescript2.default.SyntaxKind.Identifier && node.escapedText === "undefined";
  }
  printCustomBinaryExpressionIfAny(node, identation) {
    const left = node.left;
    const right = node.right;
    const op = node.operatorToken.kind;
    if (left.kind === _typescript2.default.SyntaxKind.TypeOfExpression) {
      const typeOfExpression = this.handleTypeOfInsideBinaryExpression(node, identation);
      if (typeOfExpression) {
        return typeOfExpression;
      }
    }
    if (op === _typescript2.default.SyntaxKind.EqualsToken && left.kind === _typescript2.default.SyntaxKind.ArrayLiteralExpression) {
      const arrayBindingPatternElements = left.elements;
      const parsedArrayBindingElements = arrayBindingPatternElements.map((e) => this.printNode(e, 0));
      const syntheticName = parsedArrayBindingElements.join("") + "Variable";
      const tempType = this.csharpDestructuringTempType(right);
      const tempExpression = this.printNode(right, 0);
      let arrayBindingStatement = tempType ? `${tempType} ${syntheticName} = (${tempType})${tempExpression};
` : `var ${syntheticName} = ${tempExpression};
`;
      parsedArrayBindingElements.forEach((e, index) => {
        const leftElement = arrayBindingPatternElements[index];
        const leftType = this.getChecker().getTypeAtLocation(leftElement);
        const parsedType = this.getTypeFromRawType(leftType);
        const castExp = parsedType ? `(${parsedType})` : "";
        const statement = this.getIden(identation) + (tempType ? `${e} = ${syntheticName}[${index}]` : `${e} = ((IList<object>)${syntheticName})[${index}]`);
        if (index < parsedArrayBindingElements.length - 1) {
          arrayBindingStatement += statement + ";\n";
        } else {
          arrayBindingStatement += statement;
        }
      });
      return arrayBindingStatement;
    }
    if (op === _typescript2.default.SyntaxKind.InKeyword) {
      const nativeIn = this.csharpNativeInExpression(left, right);
      if (nativeIn !== void 0) {
        return nativeIn;
      }
      return `inOp(${this.printNode(right, 0)}, ${this.printNode(left, 0)})`;
    }
    if (op === _typescript2.default.SyntaxKind.PlusEqualsToken || op === _typescript2.default.SyntaxKind.MinusEqualsToken || op in this.binaryExpressionsWrappers) {
      const nativeComparison = this.csharpNativeNumericComparison(node, identation);
      if (nativeComparison !== void 0) {
        return nativeComparison;
      }
      const leftText = this.printNode(left, 0);
      const rightText = this.printNode(right, 0);
      if (op === _typescript2.default.SyntaxKind.PlusEqualsToken) {
        return `${leftText} = add(${leftText}, ${rightText})`;
      }
      if (op === _typescript2.default.SyntaxKind.MinusEqualsToken) {
        return `${leftText} = subtract(${leftText}, ${rightText})`;
      }
      const isEquality = op === _typescript2.default.SyntaxKind.EqualsEqualsToken || op === _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken;
      const isDifference = op === _typescript2.default.SyntaxKind.ExclamationEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken;
      if (isEquality || isDifference) {
        const nativeEquality = this.csharpStringLiteralEquality(op, left, right, leftText, rightText);
        if (nativeEquality !== void 0) {
          return nativeEquality;
        }
        const nativeNullEquality = this.csharpNullLiteralEquality(op, left, right, leftText, rightText);
        if (nativeNullEquality !== void 0) {
          return nativeNullEquality;
        }
        const inlined = this.printInlineEquality(left, right, leftText, rightText, isEquality);
        if (inlined !== void 0) {
          return inlined;
        }
        const nativeElement = this.csharpNativeElementLiteralEquality(left, right, leftText, rightText, isEquality);
        if (nativeElement !== void 0) {
          return nativeElement;
        }
        const numericCall = this.csharpNativeNumericCallEquality(left, right, leftText, rightText, isEquality);
        if (numericCall !== void 0) {
          return numericCall;
        }
      }
      if (op === _typescript2.default.SyntaxKind.PlusToken) {
        const nativeConcat = this.csharpNativeStringConcat(left, right, leftText, rightText);
        if (nativeConcat !== void 0) {
          return nativeConcat;
        }
      }
      const wrapper = this.binaryExpressionsWrappers[op];
      const open = wrapper[0];
      const close = wrapper[1];
      if (op === _typescript2.default.SyntaxKind.PercentToken) {
        const nativeMod = this.csharpNativeModExpression(left, right, leftText);
        if (nativeMod !== void 0) {
          return nativeMod;
        }
      }
      return `${open}${leftText}, ${rightText}${close}`;
    }
    return void 0;
  }
  // castVariableAssignmentIfNeeded(left, right, identation) {
  //     const leftType = this.getChecker().getTypeAtLocation(left);
  //     const rightType = this.getChecker().getTypeAtLocation(right);
  //     const leftText = this.printNode(left, 0);
  //     const rightText = this.printNode(right, 0);
  //     if (this.isAnyType(rightType.flags) && !this.isAnyType(leftType.flags)) {
  //         const parsedType = this.getTypeFromRawType(leftType);
  //         return `${this.getIden(identation)}${leftText} = (${parsedType})${rightText}`;
  //     }
  //     return undefined;
  // }
  // the C# type of a whole-call / whole-property initializer, keyed on the AST so
  // the printer's cast wrappers (`((string)x).ToUpper()`) do not hide the callee
  csharpCallReturnType(initializer) {
    if (_optionalChain([initializer, 'optionalAccess', _285 => _285.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      return _optionalChain([initializer, 'access', _286 => _286.name, 'optionalAccess', _287 => _287.escapedText]) === "length" ? "int" : void 0;
    }
    if (_optionalChain([initializer, 'optionalAccess', _288 => _288.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return void 0;
    }
    const expression = initializer.expression;
    if (_optionalChain([expression, 'optionalAccess', _289 => _289.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      return void 0;
    }
    const methodName = _optionalChain([expression, 'access', _290 => _290.name, 'optionalAccess', _291 => _291.escapedText]);
    const target = expression.expression;
    if (_optionalChain([target, 'optionalAccess', _292 => _292.kind]) === _typescript2.default.SyntaxKind.ThisKeyword) {
      if (!this.csharpCalleeResolves(initializer)) {
        return void 0;
      }
      return CSHARP_THIS_RETURN_TYPES[methodName];
    }
    if (_optionalChain([target, 'optionalAccess', _293 => _293.kind]) === _typescript2.default.SyntaxKind.Identifier) {
      const full = target.escapedText + "." + methodName;
      if (CSHARP_STATIC_RETURN_TYPES[full] !== void 0) {
        return CSHARP_STATIC_RETURN_TYPES[full];
      }
    }
    return CSHARP_METHOD_RETURN_TYPES[methodName];
  }
  // mirrors printWrappedUnknownThisProperty: a `this.<name>(...)` call whose callee the
  // checker cannot resolve is printed as `callDynamically(this, "<name>", ...)`, whose C#
  // signature returns `object` whatever the name says
  csharpCalleeResolves(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const signature = checker.getResolvedSignature(node);
    return _optionalChain([signature, 'optionalAccess', _294 => _294.declaration]) !== void 0;
  }
  // the concrete C# type the initializer already produces, or undefined when the
  // printer cannot name it (this.safeString, getValue, add, parseInt, ... return object)
  csharpTypeOfInitializer(initializer) {
    switch (_optionalChain([initializer, 'optionalAccess', _295 => _295.kind])) {
      case _typescript2.default.SyntaxKind.StringLiteral:
      case _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral:
        return "string";
      case _typescript2.default.SyntaxKind.TrueKeyword:
      case _typescript2.default.SyntaxKind.FalseKeyword:
        return "bool";
      case _typescript2.default.SyntaxKind.PrefixUnaryExpression:
        return initializer.operator === _typescript2.default.SyntaxKind.ExclamationToken ? "bool" : void 0;
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
        return this.csharpTypeOfInitializer(initializer.expression);
      case _typescript2.default.SyntaxKind.BinaryExpression: {
        const op = initializer.operatorToken.kind;
        switch (op) {
          case _typescript2.default.SyntaxKind.BarBarToken:
          case _typescript2.default.SyntaxKind.AmpersandAmpersandToken:
          case _typescript2.default.SyntaxKind.EqualsEqualsToken:
          case _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken:
          case _typescript2.default.SyntaxKind.ExclamationEqualsToken:
          case _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken:
          case _typescript2.default.SyntaxKind.GreaterThanToken:
          case _typescript2.default.SyntaxKind.GreaterThanEqualsToken:
          case _typescript2.default.SyntaxKind.LessThanToken:
          case _typescript2.default.SyntaxKind.LessThanEqualsToken:
          case _typescript2.default.SyntaxKind.InKeyword:
            return "bool";
        }
        return void 0;
      }
    }
    const knownType = this.csharpCallReturnType(initializer);
    if (knownType !== void 0) {
      return knownType;
    }
    return this.csharpBoolCallTyped(initializer) ? "bool" : void 0;
  }
  // A `this.<name>(...)` call to a method the enclosing class declares with a plain `bool` return
  // (csharpBooleanReturnType prints that `bool`) hands back an unboxed bool, so a local holding it is
  // `bool`. Base helpers, names the table answers and overloaded families keep their box.
  csharpBoolCallTyped(node) {
    if (_optionalChain([node, 'optionalAccess', _296 => _296.kind]) !== _typescript2.default.SyntaxKind.CallExpression || !this.csharpIsCheckedBoolean(node)) {
      return false;
    }
    const expression = node.expression;
    if (_optionalChain([expression, 'optionalAccess', _297 => _297.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([expression, 'access', _298 => _298.expression, 'optionalAccess', _299 => _299.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return false;
    }
    let declaration;
    let checker;
    try {
      checker = this.getChecker();
      declaration = _optionalChain([checker, 'access', _300 => _300.getResolvedSignature, 'call', _301 => _301(node), 'optionalAccess', _302 => _302.declaration]);
    } catch (e) {
      return false;
    }
    if (!_typescript2.default.isMethodDeclaration(declaration) || this.csharpBooleanReturnType(declaration) !== "bool") {
      return false;
    }
    const owner = _typescript2.default.findAncestor(declaration, _typescript2.default.isClassLike);
    if (owner === void 0 || owner !== _typescript2.default.findAncestor(node, _typescript2.default.isClassLike)) {
      return false;
    }
    return _optionalChain([checker, 'access', _303 => _303.getSymbolAtLocation, 'call', _304 => _304(declaration.name), 'optionalAccess', _305 => _305.declarations, 'optionalAccess', _306 => _306.length]) === 1;
  }
  csharpEnclosingFunction(node) {
    let current = _optionalChain([node, 'optionalAccess', _307 => _307.parent]);
    while (current) {
      switch (current.kind) {
        case _typescript2.default.SyntaxKind.MethodDeclaration:
        case _typescript2.default.SyntaxKind.FunctionDeclaration:
        case _typescript2.default.SyntaxKind.FunctionExpression:
        case _typescript2.default.SyntaxKind.ArrowFunction:
        case _typescript2.default.SyntaxKind.Constructor:
        case _typescript2.default.SyntaxKind.SourceFile:
          return current;
      }
      current = current.parent;
    }
    return void 0;
  }
  csharpTypeNameIsShadowed(scope, csharpType) {
    const names = _nullishCoalesce(csharpType.match(/[A-Za-z_]\w*/g), () => ( []));
    const relevant = names.filter((n) => CSHARP_TYPE_NAMES.indexOf(n) >= 0);
    if (relevant.length === 0 || scope === void 0) {
      return false;
    }
    return this.hasNodeWhere(scope, (n) => {
      const isBinding = n.kind === _typescript2.default.SyntaxKind.Parameter || n.kind === _typescript2.default.SyntaxKind.VariableDeclaration;
      if (isBinding && _optionalChain([n, 'access', _308 => _308.name, 'optionalAccess', _309 => _309.kind]) === _typescript2.default.SyntaxKind.Identifier) {
        const printed = this.printNode(n.name, 0);
        if (relevant.indexOf(printed) >= 0) {
          return true;
        }
      }
      return false;
    });
  }
  // reject the refinement when something downstream needs the local to stay `object`:
  // `x.push(v)` prints `((IList<object>)x).Add(v)` on a value that must be boxed, a
  // later assignment of another concrete type would stop compiling, and `x++` prints
  // `postFixIncrement(ref x)` whose parameter is `ref object`
  csharpLocalIsSafeToType(scope, declaration, varName, csharpType, safeAccessor = false) {
    if (scope === void 0) {
      return false;
    }
    const safe = !this.hasNodeWhere(scope, (n) => {
      if (n.kind === _typescript2.default.SyntaxKind.Identifier && n.escapedText === varName && n !== declaration.name) {
        const parent = n.parent;
        if (_optionalChain([parent, 'optionalAccess', _310 => _310.kind]) === _typescript2.default.SyntaxKind.VariableDeclaration && parent.name === n) {
          return;
        }
        if (_optionalChain([parent, 'optionalAccess', _311 => _311.kind]) === _typescript2.default.SyntaxKind.PostfixUnaryExpression || _optionalChain([parent, 'optionalAccess', _312 => _312.kind]) === _typescript2.default.SyntaxKind.PrefixUnaryExpression) {
          const op = parent.operator;
          if (op === _typescript2.default.SyntaxKind.PlusPlusToken || op === _typescript2.default.SyntaxKind.MinusMinusToken) {
            return true;
          }
          if (safeAccessor && op !== _typescript2.default.SyntaxKind.ExclamationToken && csharpType !== "int" && csharpType !== "Int64" && csharpType !== "double") {
            return true;
          }
        }
        if (_optionalChain([parent, 'optionalAccess', _313 => _313.kind]) === _typescript2.default.SyntaxKind.SpreadElement) {
          return true;
        }
        if (_optionalChain([parent, 'optionalAccess', _314 => _314.kind]) === _typescript2.default.SyntaxKind.ArrayLiteralExpression && _optionalChain([parent, 'access', _315 => _315.parent, 'optionalAccess', _316 => _316.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && parent.parent.left === parent && parent.parent.operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken) {
          return true;
        }
        if (_optionalChain([parent, 'optionalAccess', _317 => _317.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression && parent.expression === n) {
          const method = _optionalChain([parent, 'access', _318 => _318.name, 'optionalAccess', _319 => _319.escapedText]);
          if (method === "push" || method === "reverse" || method === "sort") {
            return true;
          }
          if (safeAccessor && !this.csharpTypeIsList(csharpType) && (method === "join" || method === "shift" || method === "pop")) {
            return true;
          }
        }
        if (safeAccessor) {
          if (_optionalChain([parent, 'optionalAccess', _320 => _320.kind]) === _typescript2.default.SyntaxKind.VariableDeclaration && _optionalChain([parent, 'access', _321 => _321.name, 'optionalAccess', _322 => _322.kind]) === _typescript2.default.SyntaxKind.ArrayBindingPattern && !this.csharpTypeIsList(csharpType)) {
            return true;
          }
          if (!this.csharpTypeIsStringType(csharpType) && this.csharpIsClassThrowArgument(n)) {
            return true;
          }
          if (!this.csharpTypeIsStringType(csharpType) && this.csharpIsDeleteKey(n)) {
            return true;
          }
        }
        if (_optionalChain([parent, 'optionalAccess', _323 => _323.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && parent.left === n) {
          const op = parent.operatorToken.kind;
          if (op === _typescript2.default.SyntaxKind.EqualsToken) {
            if (this.csharpTypeOfInitializer(parent.right) !== csharpType) {
              return true;
            }
          } else if (op >= _typescript2.default.SyntaxKind.FirstCompoundAssignment && op <= _typescript2.default.SyntaxKind.LastCompoundAssignment) {
            return true;
          }
        }
        if (safeAccessor && csharpType === "string?" && this.csharpIsLeftPlusOperand(n)) {
          return true;
        }
      }
      return false;
    });
    return safe;
  }
  // a `((string)x)` wrapper is redundant as soon as the receiver's static C# type is
  // already string: the cast names the box the value is in, it never changes the value
  getCSharpLocalType(declaration) {
    const csharpType = this.csharpTypeOfInitializer(declaration.initializer);
    if (csharpType === void 0) {
      return this.VAR_TOKEN;
    }
    const sourceName = _optionalChain([declaration, 'access', _324 => _324.name, 'optionalAccess', _325 => _325.escapedText]);
    if (sourceName === void 0) {
      return this.VAR_TOKEN;
    }
    const scope = this.csharpEnclosingFunction(declaration);
    const safeAccessor = this.csharpIsSafeAccessorCall(declaration.initializer);
    if (this.csharpTypeNameIsShadowed(scope, csharpType) || !this.csharpLocalIsSafeToType(scope, declaration, sourceName, csharpType, safeAccessor)) {
      return this.VAR_TOKEN;
    }
    return csharpType;
  }
  // `this.safeString2 (...)`: the printed call binds the concrete C# signature of the
  // same-name base helper (see CSHARP_THIS_RETURN_TYPES). Used to gate the extra sinks.
  csharpIsSafeAccessorCall(initializer) {
    if (_optionalChain([initializer, 'optionalAccess', _326 => _326.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return false;
    }
    const expression = initializer.expression;
    if (_optionalChain([expression, 'optionalAccess', _327 => _327.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([expression, 'access', _328 => _328.expression, 'optionalAccess', _329 => _329.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return false;
    }
    return CSHARP_SAFE_ACCESSOR_NAMES.indexOf(_optionalChain([expression, 'access', _330 => _330.name, 'optionalAccess', _331 => _331.escapedText])) >= 0;
  }
  csharpTypeIsList(csharpType) {
    return csharpType === "List<object>" || csharpType === "IList<object>";
  }
  csharpTypeIsStringType(csharpType) {
    return csharpType === "string" || csharpType === "string?";
  }
  // `throw new ExchangeError (x)`: the printer wraps the argument in a hard `(string)`
  csharpIsClassThrowArgument(node) {
    let current = node;
    while (current.parent) {
      const parent = current.parent;
      if (parent.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression || parent.kind === _typescript2.default.SyntaxKind.AsExpression) {
        current = parent;
        continue;
      }
      if (parent.kind === _typescript2.default.SyntaxKind.NewExpression && _optionalChain([parent, 'access', _332 => _332.arguments, 'optionalAccess', _333 => _333.indexOf, 'call', _334 => _334(current)]) >= 0) {
        current = parent;
        continue;
      }
      return parent.kind === _typescript2.default.SyntaxKind.ThrowStatement;
    }
    return false;
  }
  // `delete obj[x]` prints `.Remove((string)x)`
  csharpIsDeleteKey(node) {
    let value = node;
    while (value.parent && (value.parent.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression || value.parent.kind === _typescript2.default.SyntaxKind.AsExpression)) {
      value = value.parent;
    }
    const access = value.parent;
    return _optionalChain([access, 'optionalAccess', _335 => _335.kind]) === _typescript2.default.SyntaxKind.ElementAccessExpression && access.argumentExpression === value && _optionalChain([access, 'access', _336 => _336.parent, 'optionalAccess', _337 => _337.kind]) === _typescript2.default.SyntaxKind.DeleteExpression;
  }
  // `(x) + y` / `x + y` prints `add(x, y)`: the parentheses keep x's static type
  csharpIsLeftPlusOperand(node) {
    let value = node;
    while (value.parent && value.parent.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      value = value.parent;
    }
    const parent = value.parent;
    if (_optionalChain([parent, 'optionalAccess', _338 => _338.kind]) !== _typescript2.default.SyntaxKind.BinaryExpression || parent.left !== value) {
      return false;
    }
    const op = parent.operatorToken.kind;
    return op === _typescript2.default.SyntaxKind.PlusToken || op === _typescript2.default.SyntaxKind.PlusEqualsToken;
  }
  // the binding a bare identifier resolves to (a local, a parameter, ...) or undefined
  // for every other receiver shape (`this.x`, a call result, a destructured target)
  csharpReceiverBinding(receiver) {
    if (_optionalChain([receiver, 'optionalAccess', _339 => _339.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const symbol = this.getChecker().getSymbolAtLocation(receiver);
    return _optionalChain([symbol, 'optionalAccess', _340 => _340.valueDeclaration]);
  }
  // The static C# type the emitted declaration gives a string-method receiver, or undefined when the
  // printer cannot name it (parameters, call results, ...). Only printer-typed locals are provable
  // here; the ccxt classifier overrides this hook to add the locals and parameters ITS tables retype.
  csharpStringReceiverType(receiver) {
    const declaration = this.csharpReceiverBinding(receiver);
    if (_optionalChain([declaration, 'optionalAccess', _341 => _341.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration) {
      return void 0;
    }
    if (_optionalChain([declaration, 'access', _342 => _342.parent, 'optionalAccess', _343 => _343.declarations, 'optionalAccess', _344 => _344.length]) !== 1) {
      return void 0;
    }
    const cached = this.stringReceiverTypes.get(declaration);
    if (cached !== void 0) {
      return cached === "" ? void 0 : cached;
    }
    const type = this.getCSharpLocalType(declaration);
    this.stringReceiverTypes.set(declaration, _nullishCoalesce(type, () => ( "")));
    return type;
  }
  // the receiver of `x.Split/.ToUpper/.ToLower/.Replace/.Trim/.Length`, already printed:
  // bare when its static C# type is a string, wrapped in `((string)...)` otherwise (the
  // untyped emission is unchanged)
  csharpStringMethodReceiver(node, name) {
    const expression = _optionalChain([node, 'optionalAccess', _345 => _345.expression]);
    const receiver = _optionalChain([node, 'optionalAccess', _346 => _346.kind]) === _typescript2.default.SyntaxKind.CallExpression ? _optionalChain([expression, 'optionalAccess', _347 => _347.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression ? expression.expression : void 0 : expression;
    const type = this.csharpStringReceiverType(receiver);
    return type === "string" || type === "string?" ? name : `((string)${name})`;
  }
  // The declared C# type of the `<a><b>Variable` holder of a destructuring block, or undefined
  // (keeps a `var` holder read via `((IList<object>)holder)[i]`). The ccxt classifier overrides this
  // for the tuple-returning `handle*AndParams` family: holder is `IList<object>`, read directly.
  csharpDestructuringTempType(initializer) {
    return void 0;
  }
  // `isTrue (x)` is the identity on a C# `bool`, and `x == true` is what it computes for a `bool?`
  // (null -> false), so in a condition the wrapper adds nothing. The hook answers the emitted
  // declaration's type (getCSharpLocalType, plus classifier retypes); unnamed operands keep isTrue.
  csharpConditionOperandType(node) {
    if (_optionalChain([node, 'optionalAccess', _348 => _348.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const symbol = this.getChecker().getSymbolAtLocation(node);
    const declaration = _optionalChain([symbol, 'optionalAccess', _349 => _349.valueDeclaration]);
    if (_optionalChain([declaration, 'optionalAccess', _350 => _350.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration) {
      return void 0;
    }
    if (_optionalChain([declaration, 'access', _351 => _351.parent, 'optionalAccess', _352 => _352.declarations, 'optionalAccess', _353 => _353.length]) !== 1) {
      return void 0;
    }
    const cached = this.conditionOperandTypes.get(declaration);
    if (cached !== void 0) {
      return cached === "" ? void 0 : cached;
    }
    const type = this.getCSharpLocalType(declaration);
    const result = type === "bool" || type === "bool?" ? type : void 0;
    this.conditionOperandTypes.set(declaration, _nullishCoalesce(result, () => ( "")));
    return result;
  }
  // the native spelling of a condition operand, or undefined when it needs `isTrue`.
  // `!x == true` would parse as `(!x) == true`, so the `bool?` form takes parentheses
  // under the `!` operator; every other allowed position binds `==` tighter already.
  csharpNativeCondition(node, identation) {
    const type = this.csharpConditionOperandType(node);
    if (type === void 0) {
      return void 0;
    }
    const text = this.printNode(node, 0);
    if (type === "bool") {
      return this.getIden(identation) + text;
    }
    const equalsTrue = text + " == true";
    const parent = node.parent;
    const negated = _optionalChain([parent, 'optionalAccess', _354 => _354.kind]) === _typescript2.default.SyntaxKind.PrefixUnaryExpression && parent.operator === _typescript2.default.SyntaxKind.ExclamationToken;
    return this.getIden(identation) + (negated ? `(${equalsTrue})` : equalsTrue);
  }
  // `!x` on the `bool?` the hook names prints `x != true`: exactly `!(x == true)` (null -> true)
  // and the value isTrue computes for the box. A `bool` operand keeps the base `!x`; an operand
  // the hook cannot name keeps `!isTrue(x)` (the untyped emission is untouched).
  csharpNegatedConditionOperand(operand) {
    if (_optionalChain([operand, 'optionalAccess', _355 => _355.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    if (this.csharpConditionOperandType(operand) !== "bool?") {
      return void 0;
    }
    return `${this.printNode(operand, 0)} != true`;
  }
  // only the if / while / && / || / ! condition positions print natively through this gate; the
  // ternary condition has its own hook-driven fold (csharpTernaryConditionOperand, U56) because
  // its operand is a plain value position the base printCondition path never asks about.
  csharpConditionPositionAllowsNative(node) {
    const parent = _optionalChain([node, 'optionalAccess', _356 => _356.parent]);
    switch (_optionalChain([parent, 'optionalAccess', _357 => _357.kind])) {
      case _typescript2.default.SyntaxKind.IfStatement:
        return parent.expression === node;
      case _typescript2.default.SyntaxKind.WhileStatement:
        return parent.expression === node;
      case _typescript2.default.SyntaxKind.PrefixUnaryExpression:
        return parent.operator === _typescript2.default.SyntaxKind.ExclamationToken && parent.operand === node;
      case _typescript2.default.SyntaxKind.BinaryExpression: {
        const op = _optionalChain([parent, 'access', _358 => _358.operatorToken, 'optionalAccess', _359 => _359.kind]);
        return (op === _typescript2.default.SyntaxKind.AmpersandAmpersandToken || op === _typescript2.default.SyntaxKind.BarBarToken) && (parent.left === node || parent.right === node);
      }
    }
    return false;
  }
  printVariableDeclarationList(node, identation) {
    const declaration = node.declarations[0];
    if (this.removeVariableDeclarationForFunctionExpression && _optionalChain([declaration, 'optionalAccess', _360 => _360.initializer]) && _typescript2.default.isFunctionExpression(declaration.initializer)) {
      return this.printNode(declaration.initializer, identation).trimEnd();
    }
    if (_optionalChain([declaration, 'optionalAccess', _361 => _361.name, 'access', _362 => _362.kind]) === _typescript2.default.SyntaxKind.ArrayBindingPattern) {
      const arrayBindingPattern = declaration.name;
      const arrayBindingPatternElements = arrayBindingPattern.elements;
      const parsedArrayBindingElements = arrayBindingPatternElements.map((e) => this.printNode(e.name, 0));
      const syntheticName = parsedArrayBindingElements.join("") + "Variable";
      const tempType = this.csharpDestructuringTempType(declaration.initializer);
      const tempExpression = this.printNode(declaration.initializer, 0);
      const tempDeclaration = tempType ? `${tempType} ${syntheticName} = (${tempType})${tempExpression}` : `var ${syntheticName} = ${tempExpression}`;
      let arrayBindingStatement = `${this.getIden(identation)}${tempDeclaration};
`;
      parsedArrayBindingElements.forEach((e, index) => {
        const statement = this.getIden(identation) + `var ${e} = ` + (tempType ? `${syntheticName}[${index}]` : `((IList<object>) ${syntheticName})[${index}]`);
        if (index < parsedArrayBindingElements.length - 1) {
          arrayBindingStatement += statement + ";\n";
        } else {
          arrayBindingStatement += statement;
        }
      });
      return arrayBindingStatement;
    }
    const isNew = _optionalChain([declaration, 'optionalAccess', _363 => _363.initializer]) && declaration.initializer.kind === _typescript2.default.SyntaxKind.NewExpression;
    const varToken = isNew ? "var " : this.VAR_TOKEN + " ";
    if (_optionalChain([declaration, 'optionalAccess', _364 => _364.initializer]) && declaration.initializer === void 0) {
      return this.getIden(identation) + varToken + this.printNode(declaration.name) + " = " + this.UNDEFINED_TOKEN;
    } else if (!declaration.initializer) {
      return this.getIden(identation) + "object " + this.printNode(declaration.name) + " = " + this.UNDEFINED_TOKEN;
    }
    const parsedValue = this.printNode(declaration.initializer, identation).trimStart();
    if (parsedValue === this.UNDEFINED_TOKEN) {
      let specificVarToken = "object";
      if (this.INFER_VAR_TYPE) {
        const variableType = this.getChecker().typeToString(this.getChecker().getTypeAtLocation(declaration));
        if (this.VariableTypeReplacements[variableType]) {
          specificVarToken = this.VariableTypeReplacements[variableType] + "?";
        }
      }
      return this.getIden(identation) + specificVarToken + " " + this.printNode(declaration.name) + " = " + parsedValue;
    }
    const declaredType = isNew ? "var" : this.getCSharpLocalType(declaration);
    if (!isNew && node.declarations.length === 1 && this.csharpTypeIsNative(declaredType)) {
      this.csharpTypedLocals.set(declaration, declaredType);
    }
    return this.getIden(identation) + declaredType + " " + this.printNode(declaration.name) + " = " + parsedValue;
  }
  transformPropertyAcessExpressionIfNeeded(node) {
    const expression = node.expression;
    const leftSide = this.printNode(expression, 0);
    const rightSide = node.name.escapedText;
    let rawExpression = void 0;
    switch (rightSide) {
      case "length":
        const type = this.getChecker().getTypeAtLocation(expression);
        this.warnIfAnyType(node, type.flags, leftSide, "length");
        rawExpression = this.isStringType(type.flags) ? `${this.csharpStringMethodReceiver(node, leftSide)}.Length` : _nullishCoalesce(this.csharpNativeLengthExpression(expression), () => ( `${this.ARRAY_LENGTH_WRAPPER_OPEN}${leftSide}${this.ARRAY_LENGTH_WRAPPER_CLOSE}`));
        break;
      case "push":
        rawExpression = `((IList<object>)${leftSide}).Add`;
        break;
    }
    return rawExpression;
  }
  printCustomDefaultValueIfNeeded(node) {
    if (_typescript2.default.isArrayLiteralExpression(node) || _typescript2.default.isObjectLiteralExpression(node) || _typescript2.default.isStringLiteral(node) || _typescript2.default.isBooleanLiteral(node)) {
      return this.UNDEFINED_TOKEN;
    }
    if (_typescript2.default.isNumericLiteral(node)) {
      return this.UNDEFINED_TOKEN;
    }
    if (_optionalChain([node, 'optionalAccess', _365 => _365.escapedText]) === "undefined" && _optionalChain([this, 'access', _366 => _366.getChecker, 'call', _367 => _367(), 'access', _368 => _368.getTypeAtLocation, 'call', _369 => _369(_optionalChain([node, 'optionalAccess', _370 => _370.parent])), 'optionalAccess', _371 => _371.flags]) === _typescript2.default.TypeFlags.Number) {
      return this.UNDEFINED_TOKEN;
    }
    return void 0;
  }
  printFunctionBody(node, identation) {
    const funcParams = node.parameters;
    const initParams = [];
    if (funcParams.length > 0) {
      const body = node.body.statements;
      const first = body.length > 0 ? body[0] : [];
      const remaining = body.length > 0 ? body.slice(1) : [];
      let firstStatement = this.printNode(first, identation + 1);
      const remainingString = remaining.map((statement) => this.printNode(statement, identation + 1)).join("\n");
      funcParams.forEach((param) => {
        const initializer = param.initializer;
        if (initializer) {
          if (_typescript2.default.isArrayLiteralExpression(initializer)) {
            initParams.push(`${this.printNode(param.name, 0)} ??= new List<object>();`);
          }
          if (_typescript2.default.isObjectLiteralExpression(initializer)) {
            initParams.push(`${this.printNode(param.name, 0)} ??= new Dictionary<string, object>();`);
          }
          if (_typescript2.default.isNumericLiteral(initializer)) {
            initParams.push(`${this.printNode(param.name, 0)} ??= ${this.printNode(initializer, 0)};`);
          }
          if (_typescript2.default.isStringLiteral(initializer)) {
            initParams.push(`${this.printNode(param.name, 0)} ??= ${this.printNode(initializer, 0)};`);
          }
          if (_typescript2.default.isBooleanLiteral(initializer)) {
            initParams.push(`${this.printNode(param.name, 0)} ??= ${this.printNode(initializer, 0)};`);
          }
        }
      });
      if (initParams.length > 0) {
        const defaultInitializers = initParams.map((l) => this.getIden(identation + 1) + l).join("\n") + "\n";
        const bodyParts = firstStatement.split("\n");
        const commentPart = bodyParts.filter((line) => this.isComment(line));
        const isComment = commentPart.length > 0;
        if (isComment) {
          const commentPartString = commentPart.map((c) => this.getIden(identation + 1) + c.trim()).join("\n");
          const firstStmNoComment = bodyParts.filter((line) => !this.isComment(line)).join("\n");
          firstStatement = commentPartString + "\n" + defaultInitializers + firstStmNoComment;
        } else {
          firstStatement = defaultInitializers + firstStatement;
        }
      }
      const blockOpen = this.getBlockOpen(identation);
      const blockClose = this.getBlockClose(identation);
      firstStatement = remainingString.length > 0 ? firstStatement + "\n" : firstStatement;
      return blockOpen + firstStatement + remainingString + blockClose;
    }
    return super.printFunctionBody(node, identation);
  }
  printInstanceOfExpression(node, identation) {
    const left = node.left.escapedText;
    const right = node.right.escapedText;
    return this.getIden(identation) + `${left} is ${right}`;
  }
  printAsExpression(node, identation) {
    const type = node.type;
    if (type.kind === _typescript2.default.SyntaxKind.AnyKeyword) {
      return `((object)${this.printNode(node.expression, identation)})`;
    }
    if (type.kind === _typescript2.default.SyntaxKind.StringKeyword) {
      return `((string)${this.printNode(node.expression, identation)})`;
    }
    if (type.kind === _typescript2.default.SyntaxKind.ArrayType) {
      if (type.elementType.kind === _typescript2.default.SyntaxKind.AnyKeyword) {
        return `(IList<object>)(${this.printNode(node.expression, identation)})`;
      }
      if (type.elementType.kind === _typescript2.default.SyntaxKind.StringKeyword) {
        return this.printNode(node.expression, identation);
      }
    }
    return this.printNode(node.expression, identation);
  }
  // A parameter of a method that participates in an override relation prints the spelling its
  // base member and every sibling override agree on (CSHARP_OVERRIDE_PARAM_TYPES; D8): C# is
  // invariant on override parameter types, so a half-retyped name is CS0115, while a name the
  printParameterType(node) {
    if (node === void 0 || node.kind !== _typescript2.default.SyntaxKind.Parameter) {
      return super.printParameterType(node);
    }
    const method = _typescript2.default.findAncestor(node, (n) => _typescript2.default.isMethodDeclaration(n));
    const name = method === void 0 || method.name === void 0 ? void 0 : method.name.getText().trim();
    const row = name === void 0 ? void 0 : CSHARP_OVERRIDE_PARAM_TYPES[name];
    const wanted = row === void 0 ? void 0 : row[method.parameters.indexOf(node)];
    if (wanted === void 0 || this.csharpOverrideParamSpelling(node) !== wanted) {
      return super.printParameterType(node);
    }
    return wanted;
  }
  // checker spelling of a parameter the override table covers: a dictionary-shaped type (string
  // index signature, never a class instance or a callable) or an array of any/dictionary cells
  csharpOverrideParamSpelling(node) {
    const type = this.getChecker().getTypeAtLocation(node);
    const rest = type === void 0 || !type.isUnion() ? type : type.types.filter((m) => !(m.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null)))[0];
    return this.csharpOverrideParamSpellingOfType(rest, type !== void 0 && type.isUnion() ? type.types.filter((m) => !(m.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null))).length : 1);
  }
  csharpOverrideParamSpellingOfType(type, unionArms = 1) {
    if (type === void 0 || unionArms !== 1) {
      return void 0;
    }
    if (type.flags & (_typescript2.default.TypeFlags.TypeParameter | _typescript2.default.TypeFlags.Any | _typescript2.default.TypeFlags.Unknown | _typescript2.default.TypeFlags.EnumLike)) {
      return void 0;
    }
    const checker = this.getChecker();
    if (checker.isArrayType(type) || checker.isTupleType(type)) {
      const el = (checker.getTypeArguments(type) || [])[0];
      if (el === void 0) {
        return void 0;
      }
      if (el.flags & (_typescript2.default.TypeFlags.Any | _typescript2.default.TypeFlags.Unknown)) {
        return "IList<object>";
      }
      return this.csharpOverrideParamSpellingOfType(el) === "IDictionary<string, object>" ? "IList<object>" : void 0;
    }
    if (checker.getIndexTypeOfType(type, _typescript2.default.IndexKind.String) === void 0) {
      return void 0;
    }
    const declarations = type.symbol && type.symbol.declarations ? type.symbol.declarations : [];
    if (declarations.some((d) => d.kind === _typescript2.default.SyntaxKind.ClassDeclaration)) {
      return void 0;
    }
    if (type.getCallSignatures && type.getCallSignatures().length > 0) {
      return void 0;
    }
    return "IDictionary<string, object>";
  }
  printParameter(node, defaultValue = true) {
    const name = this.printNode(node.name, 0);
    const initializer = node.initializer;
    const handlerType = this.csharpHandlerMessageType(node);
    if (handlerType !== void 0) {
      this.csharpTypedLocals.set(node, handlerType);
    }
    let type = handlerType !== void 0 ? handlerType : this.printParameterType(node);
    type = type ? type : "";
    this.csharpParamTypes.set(node, type);
    if (defaultValue) {
      if (initializer) {
        const customDefaultValue = this.printCustomDefaultValueIfNeeded(initializer);
        const defaultValue2 = customDefaultValue ? customDefaultValue : this.printNode(initializer, 0);
        type = defaultValue2 === "null" && type !== "object" ? type + "? " : type + " ";
        return type + name + this.SPACE_DEFAULT_PARAM + "=" + this.SPACE_DEFAULT_PARAM + defaultValue2;
      }
      return type + " " + name;
    }
    return name;
  }
  printArrayLiteralExpression(node) {
    let arrayOpen = this.ARRAY_OPENING_TOKEN;
    const elems = node.elements;
    const elements = node.elements.map((e) => {
      return this.printNode(e);
    }).join(", ");
    if (elems.length > 0) {
      const first = elems[0];
      if (first.kind === _typescript2.default.SyntaxKind.CallExpression) {
        let type = this.getFunctionType(first);
        if (type === void 0 || elements.indexOf(this.UKNOWN_PROP_ASYNC_WRAPPER_OPEN) > -1) {
          arrayOpen = "new List<object> {";
        } else {
          type = "object";
          arrayOpen = `new List<${type}> {`;
        }
      }
    }
    return arrayOpen + elements + this.ARRAY_CLOSING_TOKEN;
  }
  // A method declared `: boolean` / `: boolean | undefined` (or an alias of either) returns a
  // C# `bool` / `bool?` instead of `object`. Only non-async methods with an explicit annotation
  // qualify: `undefined`/`null` union members make the result nullable, any other member (or an
  // inferred type) keeps the upstream `object`. The nullable spelling is what keeps a missing
  // key a missing key — plain `bool` would turn it into `false`.
  csharpBooleanReturnType(node) {
    if (_optionalChain([node, 'optionalAccess', _372 => _372.kind]) !== _typescript2.default.SyntaxKind.MethodDeclaration || this.isAsyncFunction(node)) {
      return void 0;
    }
    if (this.csharpBooleanReturnTypes.has(node)) {
      return this.csharpBooleanReturnTypes.get(node);
    }
    let result = void 0;
    if (node.type) {
      const type = this.getChecker().getTypeFromTypeNode(node.type);
      const members = type.isUnion() ? type.types : [type];
      let nullable = false;
      let sawBoolean = false;
      let sawOther = false;
      for (const member of members) {
        if (member.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null)) {
          nullable = true;
        } else if (member.flags & _typescript2.default.TypeFlags.BooleanLike) {
          sawBoolean = true;
        } else {
          sawOther = true;
        }
      }
      if (sawBoolean && !sawOther) {
        result = nullable ? this.BOOLEAN_KEYWORD + "?" : this.BOOLEAN_KEYWORD;
      }
    } else {
      result = this.csharpBooleanReturnType(this.getMethodOverride(node));
    }
    this.csharpBooleanReturnTypes.set(node, result);
    return result;
  }
  printFunctionType(node) {
    const booleanType = this.csharpBooleanReturnType(node);
    if (booleanType !== void 0) {
      return booleanType;
    }
    return super.printFunctionType(node);
  }
  // `return x;` inside a bool/bool? method: the printed expression is still the `object`
  // box the rest of the printer produces, so unbox it through `object`. The nullable
  // spelling `(bool?)((object)(x))` accepts null; the non-nullable one needs the
  // null-forgiving `!` on the box (CS8605 under TreatWarningsAsErrors otherwise) — which is
  // exactly the runtime NullReferenceException a `: boolean` method returning null deserves
  printReturnStatement(node, identation) {
    const booleanType = this.csharpBooleanReturnType(_typescript2.default.findAncestor(node.parent, _typescript2.default.isFunctionLike));
    if (booleanType === void 0 || !node.expression) {
      return super.printReturnStatement(node, identation);
    }
    const leadingComment = this.printLeadingComments(node, identation);
    let trailingComment = this.printTraillingComment(node, identation);
    trailingComment = trailingComment ? " " + trailingComment : trailingComment;
    const value = this.printNode(node.expression, identation).trim();
    const forgiving = booleanType.endsWith("?") ? "" : "!";
    return leadingComment + this.getIden(identation) + this.RETURN_TOKEN + ` ((${booleanType})((object)(${value}))${forgiving})` + this.LINE_TERMINATOR + trailingComment;
  }
  printMethodDefinition(node, identation) {
    let name = node.name.escapedText;
    name = this.transformMethodNameIfNeeded(name);
    let returnType = this.printFunctionType(node);
    let modifiers = this.printModifiers(node);
    const defaultAccess = this.METHOD_DEFAULT_ACCESS ? this.METHOD_DEFAULT_ACCESS + " " : "";
    modifiers = modifiers ? modifiers + " " : defaultAccess;
    modifiers = modifiers.indexOf("public") === -1 && modifiers.indexOf("private") === -1 && modifiers.indexOf("protected") === -1 ? defaultAccess + modifiers : modifiers;
    let parsedArgs = void 0;
    const methodOverride = this.getMethodOverride(node);
    const isOverride = methodOverride !== void 0;
    modifiers = isOverride ? modifiers + "override " : modifiers + "virtual ";
    if (isOverride && (returnType === "object" || returnType === "Task<object>")) {
      returnType = this.printFunctionType(methodOverride);
    }
    if (isOverride && node.parameters.length > 0) {
      const first = node.parameters[0];
      const firstType = this.getType(first);
      if (firstType === void 0) {
        const currentArgs = node.parameters;
        const parentArgs = methodOverride.parameters;
        parsedArgs = "";
        parentArgs.forEach((param, index) => {
          const originalName = this.printNode(currentArgs[index].name, 0);
          const parsedArg = this.printParameteCustomName(param, originalName);
          parsedArgs += parsedArg;
          if (index < parentArgs.length - 1) {
            parsedArgs += ", ";
          }
        });
      }
    }
    parsedArgs = parsedArgs ? parsedArgs : this.printMethodParameters(node);
    returnType = returnType ? returnType + " " : returnType;
    const methodToken = this.METHOD_TOKEN ? this.METHOD_TOKEN + " " : "";
    const methodDef = this.getIden(identation) + modifiers + returnType + methodToken + name + "(" + parsedArgs + ")";
    return this.printNodeCommentsIfAny(node, identation, methodDef);
  }
  printArgsForCallExpression(node, identation) {
    const args = node.arguments;
    let parsedArgs = "";
    if (false) {
      const parsedTypes = this.getTypesFromCallExpressionParameters(node);
      const tmpArgs = [];
      args.forEach((arg, index) => {
        const parsedType = parsedTypes[index];
        let cast = "";
        if (parsedType !== "object" && parsedType !== "float" && parsedType !== "int") {
          cast = parsedType ? `(${parsedType})` : "";
        }
        tmpArgs.push(cast + this.printNode(arg, identation).trim());
      });
      parsedArgs = tmpArgs.join(",");
      return parsedArgs;
    }
    return super.printArgsForCallExpression(node, identation);
  }
  // check this out later
  printArrayIsArrayCall(node, identation, parsedArg = void 0) {
    return `((${parsedArg} is IList<object>) || (${parsedArg}.GetType().IsGenericType && ${parsedArg}.GetType().GetGenericTypeDefinition().IsAssignableFrom(typeof(List<>))))`;
  }
  printObjectKeysCall(node, identation, parsedArg = void 0) {
    if (_optionalChain([node, 'optionalAccess', _373 => _373.arguments, 'optionalAccess', _374 => _374.length]) === 1 && this.csharpReceiverIsDeclaredDictionary(node.arguments[0])) {
      return `new List<object>(${parsedArg}.Keys)`;
    }
    return `new List<object>(((IDictionary<string,object>)${parsedArg}).Keys)`;
  }
  printObjectValuesCall(node, identation, parsedArg = void 0) {
    if (_optionalChain([node, 'optionalAccess', _375 => _375.arguments, 'optionalAccess', _376 => _376.length]) === 1 && this.csharpReceiverIsDeclaredDictionary(node.arguments[0])) {
      return `new List<object>(${parsedArg}.Values)`;
    }
    return `new List<object>(((IDictionary<string,object>)${parsedArg}).Values)`;
  }
  printJsonParseCall(node, identation, parsedArg = void 0) {
    return `parseJson(${parsedArg})`;
  }
  printJsonStringifyCall(node, identation, parsedArg = void 0) {
    return `json(${parsedArg})`;
  }
  printPromiseAllCall(node, identation, parsedArg = void 0) {
    return `promiseAll(${parsedArg})`;
  }
  printMathFloorCall(node, identation, parsedArg = void 0) {
    return `(Math.Floor(Double.Parse((${parsedArg}).ToString())))`;
  }
  printMathRoundCall(node, identation, parsedArg = void 0) {
    return `Math.Round(Convert.ToDouble(${parsedArg}))`;
  }
  printMathCeilCall(node, identation, parsedArg = void 0) {
    return `Math.Ceiling(Convert.ToDouble(${parsedArg}))`;
  }
  printNumberIsIntegerCall(node, identation, parsedArg) {
    return `((${parsedArg} is int) || (${parsedArg} is long) || (${parsedArg} is Int32) || (${parsedArg} is Int64))`;
  }
  printArrayPushCall(node, identation, name = void 0, parsedArg = void 0) {
    if (this.csharpReceiverIsDeclaredList(_optionalChain([node, 'optionalAccess', _377 => _377.expression, 'optionalAccess', _378 => _378.expression]))) {
      return `${name}.Add(${parsedArg})`;
    }
    return `((IList<object>)${name}).Add(${parsedArg})`;
  }
  // The C# type the emitted declaration gives a local / operand read (`string`, `List<object>`,
  // `Dictionary<string, object>`, ...), or undefined. Installed by build/csharp-local-types.js, which
  // retypes the declaration AFTER printing. Undefined by default: callers keep the helper form.
  csharpLocalTypeOf(node) {
    return void 0;
  }
  // U55: the emitted C# type of an identifier operand of a null comparison, installed by
  // build/csharp-local-types.js from the PRINTED declaration lines (retyped after printing, so the
  // printer cannot see it). Undefined by default: every null comparison keeps isEqual unchanged.
  csharpNullComparisonTypeOf(node) {
    return void 0;
  }
  // `add (x, y)` -> `(x + y)` when the classifier proves the LEFT operand's emitted declaration is a
  // string (U57): add(string, string) / add(string, object) compute exactly C# concatenation, nulls
  // included. Undefined keeps the helper; parenthesised because `+` binds looser in embedded text.
  csharpNativeStringConcat(left, right, leftText, rightText) {
    return void 0;
  }
  // The emitted declaration type of a local read the embedding build layer retypes AFTER
  // printing (ccxt: build/csharp-local-types.js): the dict type for the `key in x` rule,
  // undefined for every receiver it does not retype. Undefined by default.
  csharpDeclaredDictReceiverType(node) {
    return void 0;
  }
  // is this receiver expression, as printed, a local declared List<object> /
  // IList<object>? Only a bare identifier reads the type hook; a parameter (printed
  // `object`), a member read and every composite expression keep the cast.
  csharpReceiverIsDeclaredList(receiver) {
    if (_optionalChain([receiver, 'optionalAccess', _379 => _379.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const type = typeof this.csharpLocalTypeOf === "function" ? this.csharpLocalTypeOf(receiver) : void 0;
    return type === "List<object>" || type === "IList<object>";
  }
  printIncludesCall(node, identation, name = void 0, parsedArg = void 0) {
    return `${name}.Contains(${parsedArg})`;
  }
  // `x.indexOf (y)` prints `x`'s own member when the printer proves the receiver holds a
  // string or a list at this read and can name that C# type; every other receiver keeps the
  // runtime getIndexOf helper, which is the only form that answers -1 for a null box.
  printIndexOfCall(node, identation, name = void 0, parsedArg = void 0) {
    const native = this.csharpNativeIndexOfCall(node, name, parsedArg);
    if (native !== void 0) {
      return native;
    }
    return `${this.INDEXOF_WRAPPER_OPEN}${name}, ${parsedArg}${this.INDEXOF_WRAPPER_CLOSE}`;
  }
  // the helper's `(string)` branch spelled natively: `((string)x).IndexOf(needle, Ordinal)` is
  // the ordinal scan TS indexOf performs (not-found stays -1, an empty needle stays 0); a
  // `List<object>` local takes its own IndexOf, a possibly-null box keeps the helper
  csharpNativeIndexOfCall(node, name = void 0, parsedArg = void 0) {
    const receiver = _optionalChain([node, 'optionalAccess', _380 => _380.expression, 'optionalAccess', _381 => _381.expression]);
    if (receiver === void 0 || name === void 0) {
      return void 0;
    }
    const declared = this.csharpExpressionTypeOf(receiver);
    if (this.csharpIndexOfReceiverHoldsString(node, receiver, declared)) {
      const needle = this.csharpNativeIndexOfNeedle(_optionalChain([node, 'access', _382 => _382.arguments, 'optionalAccess', _383 => _383[0]]), parsedArg);
      if (needle === void 0) {
        return void 0;
      }
      const casted = name.startsWith("((string)") ? name : `((string)${name})`;
      return `${casted}.IndexOf(${needle}, StringComparison.Ordinal)`;
    }
    if (this.csharpIndexOfReceiverIsDeclaredList(declared)) {
      return `((${declared})${name}).IndexOf(${parsedArg})`;
    }
    return void 0;
  }
  // the receiver is a string the helper would scan and no null can reach the read: a C#
  // `string` declaration, a checker `string`/literal whose own declaration is a plain
  // non-optional `string` (`as string` included), or a value a `!== undefined` test admits
  csharpIndexOfReceiverHoldsString(node, receiver, declared) {
    if (declared === "string") {
      return true;
    }
    if (declared !== void 0 && declared !== "string?") {
      return false;
    }
    if (!this.csharpIndexOfReceiverIsCheckedString(receiver)) {
      return false;
    }
    return this.csharpNullGuardAdmitsRead(node, receiver) || declared === void 0 && this.csharpReceiverDeclaredNonNullString(receiver);
  }
  // the checker's view of the receiver: exactly `string` / a string literal. `any` (could box
  // anything) and `string[]` (boxes a List<string> where the helper casts its target) are not
  csharpIndexOfReceiverIsCheckedString(receiver) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const type = checker.getTypeAtLocation(receiver);
    return this.isStringType(_optionalChain([type, 'optionalAccess', _384 => _384.flags]));
  }
  // `List<object>` / `IList<object>` locals (split, Object.keys, the retyped collection
  // returns) hold exactly the box the helper's own IList<object> branch scans
  csharpIndexOfReceiverIsDeclaredList(declared) {
    return declared === "List<object>" || declared === "IList<object>";
  }
  // the receiver's own declaration says non-optional `string` (params, fields, locals), or the
  // source pinned a string itself (a literal, an `as string` assertion). The `Str` alias and
  // `= undefined` fields hold the undefined the helper answers -1 for: they need the guard
  csharpReceiverDeclaredNonNullString(receiver) {
    const kind = _optionalChain([receiver, 'optionalAccess', _385 => _385.kind]);
    if (kind === _typescript2.default.SyntaxKind.StringLiteral || kind === _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral) {
      return true;
    }
    if (kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.csharpReceiverDeclaredNonNullString(receiver.expression);
    }
    if (kind === _typescript2.default.SyntaxKind.AsExpression && _optionalChain([receiver, 'access', _386 => _386.type, 'optionalAccess', _387 => _387.kind]) === _typescript2.default.SyntaxKind.StringKeyword) {
      return true;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const symbol = checker.getSymbolAtLocation(receiver);
    const declarations = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _388 => _388.declarations]), () => ( []));
    return declarations.length > 0 && declarations.every((declaration) => this.csharpDeclarationIsNonNullString(declaration));
  }
  csharpDeclarationIsNonNullString(declaration) {
    const annotation = _optionalChain([declaration, 'optionalAccess', _389 => _389.type]);
    if (_optionalChain([declaration, 'optionalAccess', _390 => _390.kind]) === _typescript2.default.SyntaxKind.Parameter) {
      return declaration.questionToken === void 0 && declaration.dotDotDotToken === void 0 && (_optionalChain([annotation, 'optionalAccess', _391 => _391.kind]) === _typescript2.default.SyntaxKind.StringKeyword || annotation === void 0 && _typescript2.default.isStringLiteralLike(declaration.initializer));
    }
    const isField = _typescript2.default.isPropertyDeclaration(declaration) || _typescript2.default.isPropertySignature(declaration);
    if (_typescript2.default.isVariableDeclaration(declaration) || isField) {
      if (declaration.questionToken !== void 0) {
        return false;
      }
      if (annotation !== void 0) {
        return annotation.kind === _typescript2.default.SyntaxKind.StringKeyword && !this.csharpInitializerIsUndefined(declaration.initializer);
      }
      return _typescript2.default.isStringLiteralLike(declaration.initializer);
    }
    return false;
  }
  csharpInitializerIsUndefined(initializer) {
    if (initializer === void 0) {
      return false;
    }
    return initializer.kind === _typescript2.default.SyntaxKind.NullKeyword || initializer.kind === _typescript2.default.SyntaxKind.Identifier && initializer.escapedText === "undefined";
  }
  // a `x !== undefined` / `x !== null` (or `!= null`) test in a branch that admits the read:
  // inside the right operand of its `&&`, inside the then-branch of its `if`, or after an
  // early-exiting `if (x === undefined) { return/throw/continue/break }`
  csharpNullGuardAdmitsRead(node, receiver) {
    if (!_typescript2.default.isIdentifier(receiver)) {
      return false;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const symbol = checker.getSymbolAtLocation(receiver);
    if (symbol === void 0) {
      return false;
    }
    const scope = this.csharpEnclosingFunction(node);
    let current = node;
    while (current !== void 0) {
      const parent = current.parent;
      if (parent === void 0) {
        return false;
      }
      if (parent.kind === _typescript2.default.SyntaxKind.BinaryExpression && parent.operatorToken.kind === _typescript2.default.SyntaxKind.AmpersandAmpersandToken && this.csharpContains(parent.right, current) && this.csharpTestIsNonNullCheck(parent.left, symbol, true)) {
        return true;
      }
      if (parent.kind === _typescript2.default.SyntaxKind.IfStatement) {
        if (this.csharpContains(parent.thenStatement, current) && this.csharpTestIsNonNullCheck(parent.expression, symbol, true)) {
          return true;
        }
        if (this.csharpContains(parent.elseStatement, current) && this.csharpTestIsNonNullCheck(parent.expression, symbol, false)) {
          return true;
        }
      }
      if (_typescript2.default.isBlock(parent) || _typescript2.default.isSourceFile(parent)) {
        if (this.csharpEarlyExitNonNullGuarded(parent, current, symbol, scope)) {
          return true;
        }
      }
      if (this.csharpIsFunctionLike(parent)) {
        return false;
      }
      current = parent;
    }
    return false;
  }
  csharpIsFunctionLike(node) {
    switch (_optionalChain([node, 'optionalAccess', _392 => _392.kind])) {
      case _typescript2.default.SyntaxKind.MethodDeclaration:
      case _typescript2.default.SyntaxKind.FunctionDeclaration:
      case _typescript2.default.SyntaxKind.FunctionExpression:
      case _typescript2.default.SyntaxKind.ArrowFunction:
      case _typescript2.default.SyntaxKind.Constructor:
        return true;
    }
    return false;
  }
  // `if (x === undefined) { return/throw/... }` (or the same test with an exiting else) in the
  // same block, with no write to x in between: every statement after it runs with x bound
  csharpEarlyExitNonNullGuarded(block, statement, symbol, scope) {
    for (const sibling of block.statements) {
      if (sibling.getStart() >= statement.getStart()) {
        return false;
      }
      if (!_typescript2.default.isIfStatement(sibling)) {
        continue;
      }
      const exitsOnNull = this.csharpTestIsNonNullCheck(sibling.expression, symbol, false) && this.csharpAlwaysExits(sibling.thenStatement);
      const continuesOnNonNull = this.csharpTestIsNonNullCheck(sibling.expression, symbol, true) && this.csharpAlwaysExits(sibling.elseStatement);
      if ((exitsOnNull || continuesOnNonNull) && !this.csharpReceiverWrittenBetween(scope, sibling.getEnd(), statement.getStart(), symbol)) {
        return true;
      }
    }
    return false;
  }
  // the test proves the binding non-null when it evaluates to `truthy`: `x != null`, `x !==
  // undefined` (either operand order), and both sides of an `&&` (a false `||`). Everything
  // else — `any` comparisons, a different binding — proves nothing
  csharpTestIsNonNullCheck(test, symbol, truthy) {
    switch (_optionalChain([test, 'optionalAccess', _393 => _393.kind])) {
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
        return this.csharpTestIsNonNullCheck(test.expression, symbol, truthy);
      case _typescript2.default.SyntaxKind.PrefixUnaryExpression:
        return test.operator === _typescript2.default.SyntaxKind.ExclamationToken && this.csharpTestIsNonNullCheck(test.operand, symbol, !truthy);
      case _typescript2.default.SyntaxKind.BinaryExpression: {
        const op = test.operatorToken.kind;
        if (op === _typescript2.default.SyntaxKind.AmpersandAmpersandToken) {
          return truthy && (this.csharpTestIsNonNullCheck(test.left, symbol, true) || this.csharpTestIsNonNullCheck(test.right, symbol, true));
        }
        if (op === _typescript2.default.SyntaxKind.BarBarToken) {
          return !truthy && (this.csharpTestIsNonNullCheck(test.left, symbol, false) || this.csharpTestIsNonNullCheck(test.right, symbol, false));
        }
        const inequality = op === _typescript2.default.SyntaxKind.ExclamationEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken;
        const isNullTest = op === _typescript2.default.SyntaxKind.EqualsEqualsToken || op === _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken || inequality;
        if (!isNullTest || truthy !== inequality) {
          return false;
        }
        const tested = _typescript2.default.isIdentifier(test.left) ? test.left : test.right;
        const literal = _typescript2.default.isIdentifier(test.left) ? test.right : test.left;
        return _typescript2.default.isIdentifier(tested) && this.csharpInitializerIsUndefined(literal) && this.csharpSameBinding(tested, symbol);
      }
    }
    return false;
  }
  csharpSameBinding(node, symbol) {
    try {
      const resolved = this.getChecker().getSymbolAtLocation(node);
      return resolved !== void 0 && resolved === symbol;
    } catch (e) {
      return false;
    }
  }
  // any write to the same binding inside the window voids a dominance proof
  csharpReceiverWrittenBetween(scope, from, to, symbol) {
    if (scope === void 0) {
      return true;
    }
    let written = false;
    const visit = (n) => {
      if (written || n.getStart() >= to || n.getEnd() <= from) {
        return;
      }
      if (_typescript2.default.isIdentifier(n) && this.csharpIsWriteTarget(n) && this.csharpSameBinding(n, symbol)) {
        written = true;
        return;
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return written;
  }
  // the C# string the native `IndexOf` takes as its needle: a value the printer types a C#
  // string prints as-is, an `object`-printed operand takes the helper's own `(string)` cast.
  // A value of any other named C# type (number, bool, collection) keeps the helper
  csharpNativeIndexOfNeedle(key, printed = void 0) {
    if (key === void 0 || printed === void 0) {
      return void 0;
    }
    const keyType = this.csharpExpressionTypeOf(key);
    if (keyType === void 0) {
      return `((string)${printed})`;
    }
    if (keyType === "string") {
      return printed;
    }
    return keyType === "string?" ? `((string)${printed})` : void 0;
  }
  printSearchCall(node, identation, name = void 0, parsedArg = void 0) {
    return `((string)${name}).IndexOf(${parsedArg})`;
  }
  printStartsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    return `((string)${name}).StartsWith(((string)${parsedArg}))`;
  }
  printEndsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    return `((string)${name}).EndsWith(((string)${parsedArg}))`;
  }
  printTrimCall(node, identation, name = void 0) {
    return `${this.csharpStringMethodReceiver(node, name)}.Trim()`;
  }
  printJoinCall(node, identation, name = void 0, parsedArg = void 0) {
    return `String.Join(${parsedArg}, ((IList<object>)${name}).ToArray())`;
  }
  printSplitCall(node, identation, name = void 0, parsedArg = void 0) {
    return `${this.csharpStringMethodReceiver(node, name)}.Split(new [] {((string)${parsedArg})}, StringSplitOptions.None).ToList<object>()`;
  }
  printConcatCall(node, identation, name = void 0, parsedArg = void 0) {
    return `concat(${name}, ${parsedArg})`;
  }
  printToFixedCall(node, identation, name = void 0, parsedArg = void 0) {
    return `toFixed(${name}, ${parsedArg})`;
  }
  printToStringCall(node, identation, name = void 0) {
    return `((object)${name}).ToString()`;
  }
  printToUpperCaseCall(node, identation, name = void 0) {
    return `${this.csharpStringMethodReceiver(node, name)}.ToUpper()`;
  }
  printToLowerCaseCall(node, identation, name = void 0) {
    return `${this.csharpStringMethodReceiver(node, name)}.ToLower()`;
  }
  printShiftCall(node, identation, name = void 0) {
    return `((IList<object>)${name}).First()`;
  }
  printReverseCall(node, identation, name = void 0) {
    return `${name} = (${name} as IList<object>).Reverse().ToList()`;
  }
  printPopCall(node, identation, name = void 0) {
    return `((IList<object>)${name}).Last()`;
  }
  printAssertCall(node, identation, parsedArgs) {
    return `assert(${parsedArgs})`;
  }
  printSliceCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    const nativeCall = this.csharpNativeSliceCall(node, name);
    if (nativeCall !== void 0) {
      return nativeCall;
    }
    if (parsedArg2 === void 0) {
      parsedArg2 = "null";
    }
    return `slice(${name}, ${parsedArg}, ${parsedArg2})`;
  }
  // `x.slice (a, b)` -> Substring / GetRange when x is a checker-proven string (or a printer-declared
  // `List<object>`) and every bound is an integer literal: JS clamps bounds into [0, length] where C#
  // throws, so literals are clamped with Math.Min / Math.Max. Anything unproven keeps the helper.
  csharpNativeSliceCall(node, name) {
    const args = _nullishCoalesce(_optionalChain([node, 'optionalAccess', _394 => _394.arguments]), () => ( []));
    if (args.length < 1 || args.length > 2) {
      return void 0;
    }
    const start = this.csharpSliceLiteralBound(args[0]);
    if (start === void 0) {
      return void 0;
    }
    const hasEnd = args.length === 2;
    const end = hasEnd ? this.csharpSliceLiteralBound(args[1]) : void 0;
    if (hasEnd && end === void 0) {
      return void 0;
    }
    const receiver = _typescript2.default.isPropertyAccessExpression(_optionalChain([node, 'optionalAccess', _395 => _395.expression])) ? node.expression.expression : void 0;
    if (!this.csharpSliceReceiverIsSideEffectFree(receiver)) {
      return void 0;
    }
    const kind = this.csharpSliceReceiverKind(receiver);
    if (kind === void 0) {
      return void 0;
    }
    const isString = kind === "string";
    const cast = isString && name.startsWith("((string)") ? name : `((${isString ? "string" : "List<object>"})${name})`;
    const length = `${cast}.${isString ? "Length" : "Count"}`;
    const from = this.csharpSliceBoundExpression(start, length);
    const guard = `(${this.csharpNullComparison(name, true)} ? null : `;
    const method = isString ? "Substring" : "GetRange";
    if (!hasEnd) {
      const open = isString ? `Substring(${from})` : `GetRange(${from}, ${from === "0" ? length : `${length} - ${from}`})`;
      return `${guard}${cast}.${open})`;
    }
    const to = this.csharpSliceBoundExpression(end, length);
    const ordered = start === 0 || start >= 0 && end >= 0 && start <= end || start < 0 && end < 0 && start <= end;
    let count;
    if (from === to) {
      count = "0";
    } else if (from === "0") {
      count = to;
    } else {
      count = ordered ? `${to} - ${from}` : `Math.Max(${to} - ${from}, 0)`;
    }
    return `${guard}${cast}.${method}(${from}, ${count}))`;
  }
  // Integer value of a slice bound that is an integer literal (`18`, `-64`); anything
  // else (expression, float, exponent, out of int range) keeps the helper — the bounds
  // are clamped with the integer Math.Min / Math.Max of the native form.
  csharpSliceLiteralBound(node) {
    if (node === void 0) {
      return void 0;
    }
    if (_typescript2.default.isNumericLiteral(node)) {
      const text = String(node.text);
      if (text.indexOf(".") !== -1 || text.indexOf("e") !== -1 || text.indexOf("E") !== -1) {
        return void 0;
      }
      const value = Number(text);
      return value <= 2147483647 ? value : void 0;
    }
    if (_typescript2.default.isPrefixUnaryExpression(node) && node.operator === _typescript2.default.SyntaxKind.MinusToken) {
      const inner = this.csharpSliceLiteralBound(node.operand);
      return inner === void 0 ? void 0 : -inner;
    }
    return void 0;
  }
  // JS slice clamps a literal bound into [0, length]: a non-negative bound is min
  // (bound, length), a negative one counts from the end (max (length - |bound|, 0)).
  // `0` stays `0` because the length of a string / list is never negative.
  csharpSliceBoundExpression(value, length) {
    if (value === 0) {
      return "0";
    }
    return value > 0 ? `Math.Min(${value}, ${length})` : `Math.Max(${length} - ${-value}, 0)`;
  }
  // The receiver of a native slice: a `List<object>` the printer / build layer declared (the only
  // receiver with GetRange), or a CHECKER-proven string (`string`, a literal, or a union with
  // null / undefined such as ccxt's `Str`); `any`, Dict and every other type keep the helper.
  csharpSliceReceiverKind(expression) {
    const declared = _nullishCoalesce((_typescript2.default.isIdentifier(expression) ? this.csharpTypedLocalType(expression) : void 0), () => ( this.csharpExpressionTypeOf(expression)));
    if (declared === "List<object>") {
      return "list";
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const type = checker.getTypeAtLocation(expression);
    return this.csharpSliceStringType(type) ? "string" : void 0;
  }
  // the checker's string view of a slice receiver: a string type, or a union of string
  // members and nullish ones only. `boolean | string`, `any` and every unproven shape fail
  csharpSliceStringType(type) {
    if (this.isStringType(_optionalChain([type, 'optionalAccess', _396 => _396.flags]))) {
      return true;
    }
    if (_optionalChain([type, 'optionalAccess', _397 => _397.flags]) !== _typescript2.default.TypeFlags.Union) {
      return false;
    }
    const members = _nullishCoalesce(type.types, () => ( []));
    return members.length > 0 && members.every((member) => this.isStringType(member.flags) || this.csharpSliceNullishType(member.flags));
  }
  csharpSliceNullishType(flags) {
    return flags === _typescript2.default.TypeFlags.Undefined || flags === _typescript2.default.TypeFlags.Null;
  }
  // True for receivers that read a value without calling anything: `x`, `x.y`, `this.x`,
  // `(x as string)` and parenthesised forms of those. Guards the repeated receiver read —
  // the clamp reads `Length` / `Count` and the call itself reads the receiver again.
  csharpSliceReceiverIsSideEffectFree(expression) {
    if (expression === void 0) {
      return false;
    }
    if (_typescript2.default.isParenthesizedExpression(expression) || _typescript2.default.isAsExpression(expression) || _typescript2.default.isTypeAssertionExpression(expression)) {
      return this.csharpSliceReceiverIsSideEffectFree(expression.expression);
    }
    if (_typescript2.default.isIdentifier(expression) || expression.kind === _typescript2.default.SyntaxKind.ThisKeyword) {
      return true;
    }
    if (_typescript2.default.isPropertyAccessExpression(expression)) {
      return this.csharpSliceReceiverIsSideEffectFree(expression.expression);
    }
    return false;
  }
  printReplaceCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    return `${this.csharpStringMethodReceiver(node, name)}.Replace((string)${parsedArg}, (string)${parsedArg2})`;
  }
  printReplaceAllCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    return `${this.csharpStringMethodReceiver(node, name)}.Replace((string)${parsedArg}, (string)${parsedArg2})`;
  }
  printPadEndCall(node, identation, name, parsedArg, parsedArg2) {
    return `(${name} as String).PadRight(Convert.ToInt32(${parsedArg}), Convert.ToChar(${parsedArg2}))`;
  }
  printPadStartCall(node, identation, name, parsedArg, parsedArg2) {
    return `(${name} as String).PadLeft(Convert.ToInt32(${parsedArg}), Convert.ToChar(${parsedArg2}))`;
  }
  printDateNowCall(node, identation) {
    return "(new DateTimeOffset(DateTime.UtcNow)).ToUnixTimeMilliseconds()";
  }
  printLengthProperty(node, identation, name = void 0) {
    const leftSide = this.printNode(node.expression, 0);
    const type = this.getChecker().getTypeAtLocation(node.expression);
    this.warnIfAnyType(node, type.flags, leftSide, "length");
    return this.isStringType(type.flags) ? `${this.csharpStringMethodReceiver(node, leftSide)}.Length` : _nullishCoalesce(this.csharpNativeLengthExpression(node.expression), () => ( `${this.ARRAY_LENGTH_WRAPPER_OPEN}${leftSide}${this.ARRAY_LENGTH_WRAPPER_CLOSE}`));
  }
  // A for-header incrementor discards the postfix value, so an operand whose printed C# type is `int`
  // takes the native operator: the same unchecked +1 / -1 as the (ref int) helper twin. Any other
  // operand keeps the helper, whose overload set binds an `object` counter.
  csharpNativePostFixIncrement(node) {
    if (_optionalChain([node, 'access', _398 => _398.operand, 'optionalAccess', _399 => _399.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const parent = node.parent;
    if (_optionalChain([parent, 'optionalAccess', _400 => _400.kind]) !== _typescript2.default.SyntaxKind.ForStatement || parent.incrementor !== node) {
      return false;
    }
    return this.csharpExpressionTypeOf(node.operand) === "int";
  }
  printPostFixUnaryExpression(node, identation) {
    const { operand, operator } = node;
    if (operand.kind === _typescript2.default.SyntaxKind.NumericLiteral) {
      return super.printPostFixUnaryExpression(node, identation);
    }
    const leftSide = this.printNode(operand, 0);
    const op = this.PostFixOperators[operator];
    if (this.csharpNativePostFixIncrement(node)) {
      return `${leftSide}${op}`;
    }
    if (op === "--") {
      return `postFixDecrement(ref ${leftSide})`;
    }
    return `postFixIncrement(ref ${leftSide})`;
  }
  printPrefixUnaryExpression(node, identation) {
    const { operand, operator } = node;
    if (operand.kind === _typescript2.default.SyntaxKind.NumericLiteral) {
      return super.printPrefixUnaryExpression(node, identation);
    }
    if (operator === _typescript2.default.SyntaxKind.ExclamationToken) {
      const nativeNot = this.csharpNegatedConditionOperand(operand);
      if (nativeNot !== void 0) {
        return nativeNot;
      }
      return this.PrefixFixOperators[operator] + this.printCondition(node.operand, 0);
    }
    const leftSide = this.printNode(operand, 0);
    if (operator === _typescript2.default.SyntaxKind.PlusToken) {
      return `prefixUnaryPlus(ref ${leftSide})`;
    }
    const nativeNegation = this.csharpNativeNegatedLocal(operand, leftSide);
    if (nativeNegation !== void 0) {
      return nativeNegation;
    }
    return `prefixUnaryNeg(ref ${leftSide})`;
  }
  // `isTrue(x)` is the identity function on a C# bool (`isTrue` returns a bool unchanged),
  // so the wrapper is only needed for values the printer leaves boxed as `object`. Every
  // shape below is rendered as a C# bool by the printer itself; anything else keeps the helper.
  csharpConditionPrintsBool(node) {
    switch (_optionalChain([node, 'optionalAccess', _401 => _401.kind])) {
      case _typescript2.default.SyntaxKind.TrueKeyword:
      case _typescript2.default.SyntaxKind.FalseKeyword:
        return true;
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
        return this.csharpConditionPrintsBool(node.expression);
      case _typescript2.default.SyntaxKind.PrefixUnaryExpression:
        return node.operator === _typescript2.default.SyntaxKind.ExclamationToken;
      case _typescript2.default.SyntaxKind.BinaryExpression:
        return this.csharpBinaryExpressionPrintsBool(node);
      case _typescript2.default.SyntaxKind.Identifier:
        return this.csharpIdentifierPrintsBool(node);
      case _typescript2.default.SyntaxKind.PropertyAccessExpression:
        return this.csharpFieldPrintsBool(node);
      case _typescript2.default.SyntaxKind.CallExpression:
        return this.csharpCallPrintsBool(node);
    }
    return false;
  }
  // `this.<field>` reads of the hand-written BaseExchange bool fields: the field is declared
  // `bool` there, so a checker-typed plain boolean read is a C# bool the condition can use bare
  csharpFieldPrintsBool(node) {
    if (_optionalChain([node, 'access', _402 => _402.expression, 'optionalAccess', _403 => _403.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return false;
    }
    return CSHARP_NATIVE_BOOL_FIELDS.indexOf(_optionalChain([node, 'access', _404 => _404.name, 'optionalAccess', _405 => _405.escapedText])) >= 0 && this.csharpIsCheckedBoolean(node);
  }
  // isEqual / !isEqual / isGreaterThan / ... / inOp all have a C# `bool` signature, a
  // `&&` / `||` prints both operands through printCondition, i.e. as bool themselves, and
  // `x instanceof T` prints the C# type test `<x> is <T>`, itself a bool
  csharpBinaryExpressionPrintsBool(node) {
    switch (node.operatorToken.kind) {
      case _typescript2.default.SyntaxKind.EqualsEqualsToken:
      case _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken:
      case _typescript2.default.SyntaxKind.ExclamationEqualsToken:
      case _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken:
      case _typescript2.default.SyntaxKind.GreaterThanToken:
      case _typescript2.default.SyntaxKind.GreaterThanEqualsToken:
      case _typescript2.default.SyntaxKind.LessThanToken:
      case _typescript2.default.SyntaxKind.LessThanEqualsToken:
      case _typescript2.default.SyntaxKind.InKeyword:
      case _typescript2.default.SyntaxKind.InstanceOfKeyword:
      case _typescript2.default.SyntaxKind.BarBarToken:
      case _typescript2.default.SyntaxKind.AmpersandAmpersandToken:
        return true;
    }
    return false;
  }
  // the checker sees a value that is exactly boolean; `boolean | undefined` is a
  // TypeFlags.Union here and is therefore rejected (`bool?` is no condition in C#)
  csharpIsCheckedBoolean(node) {
    const type = this.getChecker().getTypeAtLocation(node);
    return ((_nullishCoalesce(_optionalChain([type, 'optionalAccess', _406 => _406.flags]), () => ( 0))) & _typescript2.default.TypeFlags.BooleanLike) !== 0;
  }
  // `bool name = ...` is only declared when getCSharpLocalType resolved that exact
  // declaration to `bool`, so ask the same function: the condition and the declaration
  // cannot disagree. Parameters, members and demoted locals return `object` there. The
  // embedding layer's resolver (ccxt: build/csharp-local-types.js) retypes locals the
  // printer cannot name itself and reports the type the printed declaration carries, so a
  // `bool` there is the same proof for a declaration this printer did not write
  csharpIdentifierPrintsBool(node) {
    if (!this.csharpIsCheckedBoolean(node)) {
      return false;
    }
    if (this.csharpExpressionTypeOf(node) === "bool") {
      return true;
    }
    const declaration = _optionalChain([this, 'access', _407 => _407.getChecker, 'call', _408 => _408(), 'access', _409 => _409.getSymbolAtLocation, 'call', _410 => _410(node), 'optionalAccess', _411 => _411.valueDeclaration]);
    if (declaration === void 0 || !_typescript2.default.isVariableDeclaration(declaration) || _optionalChain([declaration, 'access', _412 => _412.name, 'optionalAccess', _413 => _413.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    if (!this.csharpLocalTypes.has(declaration)) {
      this.csharpLocalTypes.set(declaration, this.getCSharpLocalType(declaration));
    }
    return this.csharpLocalTypes.get(declaration) === "bool";
  }
  // the printed callee text of a method call the printer can name statically: `this.<name>` and
  // `<Ident>.<name>`. A deeper receiver (`a.b.c(...)`) or a plain function call stays undefined —
  // the hand-written-callee proof must know exactly which callee the emitted text binds
  csharpCalleeName_Native(node) {
    const expression = _optionalChain([node, 'optionalAccess', _414 => _414.expression]);
    if (_optionalChain([expression, 'optionalAccess', _415 => _415.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      return void 0;
    }
    const receiver = expression.expression;
    const name = _optionalChain([expression, 'access', _416 => _416.name, 'optionalAccess', _417 => _417.escapedText]);
    if (_optionalChain([receiver, 'optionalAccess', _418 => _418.kind]) === _typescript2.default.SyntaxKind.ThisKeyword) {
      return "this." + name;
    }
    if (_optionalChain([receiver, 'optionalAccess', _419 => _419.kind]) === _typescript2.default.SyntaxKind.Identifier) {
      return receiver.escapedText + "." + name;
    }
    return void 0;
  }
  // cs-14: `isTrue(<call>)` goes bare when the call's own C# signature is non-nullable `bool`: (a)
  // hand-written cs/ccxt/base callees (CSHARP_BOOL_CALLEES_NATIVE), (b) `this.<name>(...)` printed by
  // the same csharpBooleanReturnType. Hand-written overrides and `callDynamically` keep the wrapper.
  csharpBoolCall_Native(node) {
    const callee = this.csharpCalleeName_Native(node);
    if (callee !== void 0 && CSHARP_BOOL_CALLEES_NATIVE[callee] === true) {
      return true;
    }
    if (callee === void 0 || callee.indexOf("this.") !== 0) {
      return false;
    }
    if (CSHARP_HANDWRITTEN_CALLEES_NATIVE.indexOf(callee.substring("this.".length)) > -1) {
      return false;
    }
    if (!this.csharpCalleeResolves(node)) {
      return false;
    }
    const signature = this.getChecker().getResolvedSignature(node);
    const declaration = _optionalChain([signature, 'optionalAccess', _420 => _420.declaration]);
    if (_optionalChain([declaration, 'optionalAccess', _421 => _421.kind]) !== _typescript2.default.SyntaxKind.MethodDeclaration || declaration.body === void 0) {
      return false;
    }
    return this.csharpBooleanReturnType(declaration) === "bool";
  }
  // calls the printer gives a concrete bool signature (inArray, valueIsDefined, startsWith,
  // Array.isArray, ...); safeBool and friends are `bool?` / `object` and keep the wrapper
  csharpCallPrintsBool(node) {
    return this.csharpIsCheckedBoolean(node) && (this.csharpCallReturnType(node) === "bool" || this.csharpBoolCall_Native(node));
  }
  // The declared type of an identifier read: build layer's proof first, then this printer's table;
  // `var`/`object` mean "no type". `isTrue (x)` answers false for a null box, exactly the lifted
  // `x == true` on a `bool?`. Only reads declared `bool?` qualify; every other shape keeps helper.
  csharpNullableBoolCondition(node) {
    let value = node;
    while (_optionalChain([value, 'optionalAccess', _422 => _422.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      value = value.expression;
    }
    if (_optionalChain([value, 'optionalAccess', _423 => _423.kind]) === _typescript2.default.SyntaxKind.Identifier) {
      if (this.csharpDeclaredLocalType(value) === "bool?") {
        return `(${this.printNode(value, 0)} == true)`;
      }
      if (this.csharpDeclaredLocalResolverType(value) === "bool?") {
        return `(${this.printNode(value, 0)} == true)`;
      }
      return void 0;
    }
    if (_optionalChain([value, 'optionalAccess', _424 => _424.kind]) === _typescript2.default.SyntaxKind.CallExpression && this.csharpCallPrintsNullableBool(value)) {
      return `(${this.printNode(value, 0)} == true)`;
    }
    return void 0;
  }
  // a call whose printed C# signature is `bool?`: the `bool? safeBool(...)` family of the
  // hand-written base (CSHARP_THIS_RETURN_TYPES, which names that signature), and a
  // `this.<name>(...)` whose TS declaration the generator itself prints — definition and call
  csharpCallPrintsNullableBool(node) {
    if (this.csharpCallReturnType(node) === "bool?") {
      return true;
    }
    const callee = this.csharpCalleeName_Native(node);
    if (callee === void 0 || callee.indexOf("this.") !== 0) {
      return false;
    }
    if (CSHARP_HANDWRITTEN_CALLEES_NATIVE.indexOf(callee.substring("this.".length)) > -1) {
      return false;
    }
    if (!this.csharpCalleeResolves(node)) {
      return false;
    }
    const declaration = _optionalChain([this, 'access', _425 => _425.getChecker, 'call', _426 => _426(), 'access', _427 => _427.getResolvedSignature, 'call', _428 => _428(node), 'optionalAccess', _429 => _429.declaration]);
    if (_optionalChain([declaration, 'optionalAccess', _430 => _430.kind]) !== _typescript2.default.SyntaxKind.MethodDeclaration || declaration.body === void 0) {
      return false;
    }
    return this.csharpBooleanReturnType(declaration) === "bool?";
  }
  // same emission as the base implementation except for the bare-bool branch: the node is
  // printed once and only wrapped in isTrue(...) when the printer did not already render a bool
  //
  // the single `printCondition` override, rules layered strongest-proof-first over the base
  // body (the untyped fallback at the bottom):
  // S61 -- bare `isTrue (x)` in an if / while / && / || / ! condition: the operand is already
  // the `bool` (or `bool?`) the helper computes, so the wrapper adds nothing (the gate is the
  // csharpConditionPositionAllowsNative / csharpNativeCondition pair, never a name shape).
  // S62 -- a condition whose printed text is already a C# `bool` helper call
  // (csharpPrintedConditionIsBoolean) needs no wrapper either; the same identity proven from
  // the printed form rather than from the declaration.
  // #82 -- csharpConditionPrintsBool folds the wrapper on the node shapes the printer itself
  // renders as a bool, with csharpConditionParensIfNeeded keeping the `&&` / `||` precedence.
  printCondition(node, identation) {
    if (this.supportsFalsyOrTruthyValues) {
      return this.printNode(node, identation);
    }
    if (_optionalChain([node, 'optionalAccess', _431 => _431.kind]) === _typescript2.default.SyntaxKind.PrefixUnaryExpression && node.operator === _typescript2.default.SyntaxKind.ExclamationToken) {
      return this.printPrefixUnaryExpression(node, identation);
    }
    const nullableBool = this.csharpNullableBoolCondition(node);
    if (nullableBool !== void 0) {
      return `${this.getIden(identation)}${nullableBool}`;
    }
    const native = this.csharpConditionPositionAllowsNative(node) ? this.csharpNativeCondition(node, identation) : void 0;
    if (native !== void 0) {
      return native;
    }
    const printed = this.printNode(node, 0);
    if (this.csharpConditionPrintsBool(node)) {
      return `${this.getIden(identation)}${this.csharpConditionParensIfNeeded(node, printed)}`;
    }
    if (this.csharpPrintedConditionIsBoolean(printed)) {
      return `${this.getIden(identation)}${printed}`;
    }
    return `${this.getIden(identation)}${this.FALSY_WRAPPER_OPEN}${printed}${this.FALSY_WRAPPER_CLOSE}`;
  }
  // dropping the wrapper exposes the `&&` / `||` / `is` of the node, so the bare text needs its own
  // parentheses where C# binds tighter than the JS it replaces: under `!` (which binds tighter
  // than both), and a `||` that becomes an operand of a `&&` (`(a || b) && c` must not flatten
  // to `a || b && c`). A type test additionally needs them under the `(bool)` cast the
  // conditional-expression printer prepends (`(bool) x is T` would bind as `((bool) x) is T`).
  // Source parentheses, when present, already come out in `printed`.
  csharpConditionParensIfNeeded(node, printed) {
    if (_optionalChain([node, 'optionalAccess', _432 => _432.kind]) !== _typescript2.default.SyntaxKind.BinaryExpression) {
      return printed;
    }
    const op = node.operatorToken.kind;
    const parent = node.parent;
    if (op === _typescript2.default.SyntaxKind.InstanceOfKeyword) {
      const underNot2 = _optionalChain([parent, 'optionalAccess', _433 => _433.kind]) === _typescript2.default.SyntaxKind.PrefixUnaryExpression && parent.operator === _typescript2.default.SyntaxKind.ExclamationToken;
      const underCast = _optionalChain([parent, 'optionalAccess', _434 => _434.kind]) === _typescript2.default.SyntaxKind.ConditionalExpression && parent.condition === node;
      return underNot2 || underCast ? `(${printed})` : printed;
    }
    if (op !== _typescript2.default.SyntaxKind.BarBarToken && op !== _typescript2.default.SyntaxKind.AmpersandAmpersandToken) {
      return printed;
    }
    const underNot = _optionalChain([parent, 'optionalAccess', _435 => _435.kind]) === _typescript2.default.SyntaxKind.PrefixUnaryExpression && parent.operator === _typescript2.default.SyntaxKind.ExclamationToken;
    const underAnd = op === _typescript2.default.SyntaxKind.BarBarToken && _optionalChain([parent, 'optionalAccess', _436 => _436.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && parent.operatorToken.kind === _typescript2.default.SyntaxKind.AmpersandAmpersandToken;
    return underNot || underAnd ? `(${printed})` : printed;
  }
  printConditionalExpression(node, identation) {
    const condition = this.printTernaryCondition(node.condition);
    const whenTrue = this.printNode(node.whenTrue, 0);
    const whenFalse = this.printNode(node.whenFalse, 0);
    return condition + " ? " + whenTrue + " : " + whenFalse;
  }
  // The ternary condition is a condition position the native gate above never sees: the same hook
  // answer prints natively there — `bool` bare, `bool?` as `x == true` (null -> false), source parens
  // unwrapped. Undefined keeps the base path, so an unnamed operand is unchanged.
  csharpTernaryConditionOperand(node) {
    let operand = node;
    while (_optionalChain([operand, 'optionalAccess', _437 => _437.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      operand = operand.expression;
    }
    if (_optionalChain([operand, 'optionalAccess', _438 => _438.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const type = this.csharpConditionOperandType(operand);
    if (type === "bool") {
      return this.printNode(operand, 0);
    }
    if (type === "bool?") {
      return `${this.printNode(operand, 0)} == true`;
    }
    return void 0;
  }
  // printCondition already yields a C# `bool` (`isTrue(x)` / `!isTrue(x)`), so a `((bool) <cond>)`
  // wrapper is a cast on a bool. The hook fold above answers the emitted declaration's type first;
  // anything it cannot name keeps the printer's `bool` fold (getCSharpLocalType) and then `isTrue`.
  printTernaryCondition(node) {
    const native = this.csharpTernaryConditionOperand(node);
    if (native !== void 0) {
      return native;
    }
    if (this.csharpConditionPrintsAsBool(node)) {
      return this.printNode(node, 0);
    }
    return this.printCondition(node, 0);
  }
  csharpConditionPrintsAsBool(node) {
    if (_optionalChain([node, 'optionalAccess', _439 => _439.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const checker = this.getChecker();
    const declaration = _optionalChain([checker, 'access', _440 => _440.getSymbolAtLocation, 'call', _441 => _441(node), 'optionalAccess', _442 => _442.valueDeclaration]);
    if (_optionalChain([declaration, 'optionalAccess', _443 => _443.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration) {
      return false;
    }
    return this.getCSharpLocalType(declaration) === "bool";
  }
  // index of the `)` matching the `(` at `start` inside printed C# text, or -1; string
  // and char literals are skipped so a `(` inside a literal cannot shift the depth
  csharpMatchingParenIndex(text, start) {
    let depth = 0;
    for (let i = start; i < text.length; i++) {
      const char = text[i];
      if (char === '"' || char === "'") {
        const quote = char;
        i++;
        while (i < text.length) {
          if (text[i] === "\\") {
            i += 2;
            continue;
          }
          if (text[i] === quote) {
            break;
          }
          i++;
        }
      } else if (char === "(") {
        depth++;
      } else if (char === ")") {
        depth--;
        if (depth === 0) {
          return i;
        }
      }
    }
    return -1;
  }
  // S62: a `?` at paren/bracket/brace depth 0 in printed condition text is a C# conditional; its type
  // is the branches' common type (`object`), never `bool`; without this guard the falsy wrapper would
  // be folded into an invalid `if (object)`. `?` inside parens/brackets and in literals is ignored.
  csharpTextHasTopLevelConditional(text) {
    let depth = 0;
    for (let i = 0; i < text.length; i++) {
      const char = text[i];
      if (char === '"' || char === "'") {
        const quote = char;
        i++;
        while (i < text.length) {
          if (text[i] === "\\") {
            i += 2;
            continue;
          }
          if (text[i] === quote) {
            break;
          }
          i++;
        }
      } else if (char === "(" || char === "[" || char === "{") {
        depth++;
      } else if (char === ")" || char === "]" || char === "}") {
        depth--;
      } else if (char === "?" && depth === 0) {
        return true;
      }
    }
    return false;
  }
  // S62: is this printed condition text already a C# `bool`? Only `isTrue`/`isEqual`/`isGreaterThan
  // (OrEqual)`/`isLessThan(OrEqual)`/`inOp` calls qualify (CSHARP_BOOLEAN_PRINTED_CALLS). A `!`,
  // whole-text parens and `&&`/`||` composites (via the leading operand) keep the property.
  csharpPrintedConditionIsBoolean(text) {
    const trimmed = text.trim();
    if (trimmed.length === 0) {
      return false;
    }
    if (this.csharpTextHasTopLevelConditional(trimmed)) {
      return false;
    }
    if (trimmed.startsWith("(") && this.csharpMatchingParenIndex(trimmed, 0) === trimmed.length - 1) {
      return this.csharpPrintedConditionIsBoolean(trimmed.substring(1, trimmed.length - 1));
    }
    if (trimmed.startsWith("!")) {
      return this.csharpPrintedConditionIsBoolean(trimmed.substring(1));
    }
    const printedCall = /^([A-Za-z_]\w*)\(/.exec(trimmed);
    return printedCall !== null && CSHARP_BOOLEAN_PRINTED_CALLS.indexOf(printedCall[1]) >= 0;
  }
  printDeleteExpression(node, identation) {
    const receiver = node.expression.expression;
    const object = this.printNode(receiver, 0);
    const key = this.printNode(node.expression.argumentExpression, 0);
    if (this.csharpReceiverIsDeclaredDictionary(receiver)) {
      return `${object}.Remove((string)${key})`;
    }
    return `((IDictionary<string,object>)${object}).Remove((string)${key})`;
  }
  printNewExpression(node, identation) {
    let expression = _optionalChain([node, 'access', _444 => _444.expression, 'optionalAccess', _445 => _445.escapedText]);
    expression = expression ? expression : this.printNode(node.expression);
    if (expression === "Error") {
      expression = "Exception";
    }
    const args = node.arguments.map((n) => this.printNode(n, identation)).join(", ");
    const newToken = this.NEW_TOKEN ? this.NEW_TOKEN + " " : "";
    return newToken + expression + this.LEFT_PARENTHESIS + args + this.RIGHT_PARENTHESIS;
  }
  printThrowStatement(node, identation) {
    if (node.expression.kind === _typescript2.default.SyntaxKind.Identifier) {
      return this.getIden(identation) + this.THROW_TOKEN + " " + this.printNode(node.expression, 0) + this.LINE_TERMINATOR;
    }
    if (node.expression.kind === _typescript2.default.SyntaxKind.NewExpression) {
      const expression = node.expression;
      const argumentsExp = _nullishCoalesce(_optionalChain([expression, 'optionalAccess', _446 => _446.arguments]), () => ( []));
      const parsedArg = _nullishCoalesce(argumentsExp.map((n) => this.printNode(n, 0)).join(","), () => ( ""));
      const newExpression = this.printNode(expression.expression, 0);
      if (expression.expression.kind === _typescript2.default.SyntaxKind.Identifier) {
        const id = expression.expression;
        const idName = id.escapedText === "Error" ? "Exception" : id.escapedText;
        const symbol = this.getChecker().getSymbolAtLocation(expression.expression);
        if (symbol) {
          const declarations = _nullishCoalesce(_optionalChain([this, 'access', _447 => _447.getChecker, 'call', _448 => _448(), 'access', _449 => _449.getDeclaredTypeOfSymbol, 'call', _450 => _450(symbol), 'access', _451 => _451.symbol, 'optionalAccess', _452 => _452.declarations]), () => ( []));
          const isClassDeclaration = declarations.find((l) => l.kind === _typescript2.default.SyntaxKind.InterfaceDeclaration || l.kind === _typescript2.default.SyntaxKind.ClassDeclaration);
          if (isClassDeclaration) {
            return this.getIden(identation) + `${this.THROW_TOKEN} ${this.NEW_TOKEN} ${idName} ((string)${parsedArg}) ${this.LINE_TERMINATOR}`;
          } else {
            return this.getIden(identation) + `throwDynamicException(${idName}, ${parsedArg});return null;`;
          }
        }
        return this.getIden(identation) + `${this.THROW_TOKEN} ${this.NEW_TOKEN} ${idName === id.escapedText ? newExpression : idName} (${parsedArg}) ${this.LINE_TERMINATOR}`;
      } else if (expression.expression.kind === _typescript2.default.SyntaxKind.ElementAccessExpression) {
        return this.getIden(identation) + `throwDynamicException(${newExpression}, ${parsedArg});`;
      }
      return super.printThrowStatement(node, identation);
    }
  }
  printPropertyAccessModifiers(node) {
    let modifiers = this.printModifiers(node);
    if (modifiers === "") {
      modifiers = this.defaultPropertyAccess;
    }
    let typeText = "object";
    if (node.type) {
      typeText = this.getType(node);
      if (!typeText) {
        if (node.type.kind === _typescript2.default.SyntaxKind.AnyKeyword) {
          typeText = this.OBJECT_KEYWORD + " ";
        }
      }
    }
    return modifiers + " " + typeText + " ";
  }
  // printLeadingComments(node, identation) {
  //     const fullText = this.getSrc().getFullText();
  //     const commentsRangeList = ts.getLeadingCommentRanges(fullText, node.pos);
  //     const commentsRange = commentsRangeList ? commentsRangeList : undefined;
  //     let res = "";
  //     if (commentsRange) {
  //         for (const commentRange of commentsRange) {
  //             const commentText = fullText.slice(commentRange.pos, commentRange.end);
  //             if (commentText !== undefined) {
  //                 const formatted = commentText
  //                     .split("\n")
  //                     .map(line=>line.trim())
  //                     .map(line => !(line.trim().startsWith("*")) ? this.getIden(identation) + line : this.getIden(identation) + " " + line) .join("\n");
  //                 // res+= this.transformLeadingComment(formatted) + "\n";
  //             }
  //         }
  //     }
  //     return res;
  // }
};

// src/transpiler.ts
var _path = require('path'); var path2 = _interopRequireWildcard(_path); var path = _interopRequireWildcard(_path);
var _fs = require('fs'); var fs2 = _interopRequireWildcard(_fs); var fs = _interopRequireWildcard(_fs);

// src/goTranspiler.ts
init_cjs_shims();



var SyntaxKind3 = _typescript2.default.SyntaxKind;
var parserConfig4 = {
  "ELSEIF_TOKEN": "else if",
  "OBJECT_OPENING": "map[string]any{",
  "ARRAY_OPENING_TOKEN": "[]any{",
  "ARRAY_CLOSING_TOKEN": "}",
  "PROPERTY_ASSIGNMENT_TOKEN": ":",
  "VAR_TOKEN": "object",
  // object
  "METHOD_TOKEN": "func",
  "PROPERTY_ASSIGNMENT_OPEN": "",
  "PROPERTY_ASSIGNMENT_CLOSE": "",
  "SUPER_TOKEN": "base",
  "SUPER_CALL_TOKEN": "base",
  "FALSY_WRAPPER_OPEN": "EvalTruthy(",
  "FALSY_WRAPPER_CLOSE": ")",
  "COMPARISON_WRAPPER_OPEN": "IsEqual(",
  "COMPARISON_WRAPPER_CLOSE": ")",
  "UKNOWN_PROP_WRAPPER_OPEN": "this.call(",
  "UNKOWN_PROP_WRAPPER_CLOSE": ")",
  "UKNOWN_PROP_ASYNC_WRAPPER_OPEN": "this.callAsync(",
  "UNKOWN_PROP_ASYNC_WRAPPER_CLOSE": ")",
  "DYNAMIC_CALL_OPEN": "callDynamically(",
  "EQUALS_EQUALS_WRAPPER_OPEN": "IsEqual(",
  "EQUALS_EQUALS_WRAPPER_CLOSE": ")",
  "DIFFERENT_WRAPPER_OPEN": "!IsEqual(",
  "DIFFERENT_WRAPPER_CLOSE": ")",
  "GREATER_THAN_WRAPPER_OPEN": "IsGreaterThan(",
  "GREATER_THAN_WRAPPER_CLOSE": ")",
  "GREATER_THAN_EQUALS_WRAPPER_OPEN": "IsGreaterThanOrEqual(",
  "GREATER_THAN_EQUALS_WRAPPER_CLOSE": ")",
  "LESS_THAN_WRAPPER_OPEN": "IsLessThan(",
  "LESS_THAN_WRAPPER_CLOSE": ")",
  "LESS_THAN_EQUALS_WRAPPER_OPEN": "IsLessThanOrEqual(",
  "LESS_THAN_EQUALS_WRAPPER_CLOSE": ")",
  "PLUS_WRAPPER_OPEN": "Add(",
  "PLUS_WRAPPER_CLOSE": ")",
  "MINUS_WRAPPER_OPEN": "Subtract(",
  "MINUS_WRAPPER_CLOSE": ")",
  "ARRAY_LENGTH_WRAPPER_OPEN": "GetArrayLength(",
  "ARRAY_LENGTH_WRAPPER_CLOSE": ")",
  "DIVIDE_WRAPPER_OPEN": "Divide(",
  "DIVIDE_WRAPPER_CLOSE": ")",
  "MULTIPLY_WRAPPER_OPEN": "Multiply(",
  "MULTIPLY_WRAPPER_CLOSE": ")",
  "INDEXOF_WRAPPER_OPEN": "GetIndexOf(",
  "INDEXOF_WRAPPER_CLOSE": ")",
  "MOD_WRAPPER_OPEN": "Mod(",
  "MOD_WRAPPER_CLOSE": ")",
  "FUNCTION_TOKEN": "func",
  "DEFAULT_RETURN_TYPE": "any",
  "BLOCK_OPENING_TOKEN": "{",
  "DEFAULT_PARAMETER_TYPE": "any",
  "LINE_TERMINATOR": "",
  "CONDITION_OPENING": "",
  "CONDITION_CLOSE": "",
  "AWAIT_TOKEN": "",
  "NULL_TOKEN": "nil",
  "UNDEFINED_TOKEN": "nil",
  "WHILE_TOKEN": "for",
  "ELEMENT_ACCESS_WRAPPER_OPEN": "GetValue(",
  "ELEMENT_ACCESS_WRAPPER_CLOSE": ")"
};
var GO_NATIVE_CALL_RETURN_TYPES = {
  "strings.Index": "int"
};
var GO_HELPER_RETURN_TYPES = {
  "GetArrayLength": "int",
  "GetLength": "int",
  // the printInlineArrayLength emission of `.length` on a slice
  "len": "int",
  "GetIndexOf": "int",
  "ToString": "string",
  // hand-written exchange_encode.go helper, a plain Go `string` on its only return path
  "this.UrlencodeNested": "string",
  "ToLower": "string",
  "ToUpper": "string",
  "JsonStringify": "string",
  "Capitalize": "string",
  "this.Uuid": "string",
  "this.Hmac": "string",
  "this.Ymdhms": "string",
  "this.Yyyymmdd": "string",
  "this.Ymd": "string",
  "Split": "[]string",
  "ObjectKeys": "[]string",
  "this.Extend": "map[string]any",
  "this.DeepExtend": "map[string]any",
  "this.Keysort": "map[string]any",
  "this.IndexBy": "map[string]any",
  "this.GroupBy": "map[string]any",
  "this.Milliseconds": "int64",
  "this.Seconds": "int64",
  "this.Microseconds": "int64",
  "ParseInt": "int64",
  "MathFloor": "float64",
  "MathCeil": "float64",
  "MathRound": "float64",
  "MathAbs": "float64",
  "MathPow": "float64",
  "ToFloat64": "float64",
  "EvalTruthy": "bool",
  "IsEqual": "bool",
  "IsGreaterThan": "bool",
  "IsLessThan": "bool",
  "IsGreaterThanOrEqual": "bool",
  "IsLessThanOrEqual": "bool",
  "InOp": "bool",
  "IsArray": "bool",
  "IsString": "bool",
  "IsInt": "bool",
  "IsBool": "bool",
  "IsNumber": "bool",
  "IsObject": "bool",
  "IsDictionary": "bool",
  "StartsWith": "bool",
  "EndsWith": "bool",
  // the native string operations (emitted with a proven Go `string` receiver) return exactly
  // the type of the helper they replace, so a local initialised by one keeps its declared type
  "strings.Split": "[]string",
  "strings.Join": "string",
  "strings.ToUpper": "string",
  "strings.ToLower": "string",
  "strings.Replace": "string",
  "strings.ReplaceAll": "string",
  "strings.HasPrefix": "bool",
  "strings.HasSuffix": "bool",
  "IsInstance": "bool",
  "IsInteger": "bool",
  "this.InArray": "bool",
  "this.ValueIsDefined": "bool",
  "Precise.StringGt": "bool",
  "Precise.StringGe": "bool",
  "Precise.StringLt": "bool",
  "Precise.StringLe": "bool",
  "Precise.StringEq": "bool",
  "Precise.StringEquals": "bool",
  // the base Safe* accessors return a typed pointer so that an absent value is a
  // nil pointer, distinct from a present zero value ("" / 0 / false)
  "this.SafeString": "*string",
  "this.SafeString2": "*string",
  "this.SafeStringN": "*string",
  "this.SafeStringLower": "*string",
  "this.SafeStringLower2": "*string",
  "this.SafeStringLowerN": "*string",
  "this.SafeStringUpper": "*string",
  "this.SafeStringUpper2": "*string",
  "this.SafeStringUpperN": "*string",
  "this.SafeInteger": "*int64",
  "this.SafeInteger2": "*int64",
  "this.SafeIntegerN": "*int64",
  "this.SafeIntegerProduct": "*int64",
  "this.SafeIntegerProduct2": "*int64",
  "this.SafeIntegerProductN": "*int64",
  "this.SafeTimestamp": "*int64",
  "this.SafeTimestamp2": "*int64",
  "this.SafeTimestampN": "*int64",
  "this.SafeFloat": "*float64",
  "this.SafeFloat2": "*float64",
  "this.SafeFloatN": "*float64",
  // absent flag → nil pointer, present flag → its value, mirroring the string/number accessors
  "this.SafeBool": "*bool",
  "this.SafeBool2": "*bool",
  "this.SafeBoolN": "*bool",
  // SafeDict*/SafeList* stay untyped: the Go accessors return `any` because the value
  // may be a *sync.Map, a Dict or an order-book side, none of which is a map[string]any / []any
  // Precise arithmetic returns a numeric string, or nil when an operand is
  // absent, so it carries the same *string shape as the Safe* string accessors
  "Precise.StringMul": "*string",
  "Precise.StringDiv": "*string",
  "Precise.StringSub": "*string",
  "Precise.StringAdd": "*string",
  "Precise.StringOr": "*string",
  "Precise.StringMax": "*string",
  "Precise.StringMin": "*string",
  "Precise.StringAbs": "*string",
  "Precise.StringNeg": "*string",
  "Precise.StringMod": "*string"
};
var GO_JSON_PARSE_CALLS = ["Json", "JsonParse", "ParseJson", "ParseJSON"];
var GO_BOOL_FIELDS = /* @__PURE__ */ new Set([
  "this.Verbose",
  "this.EnableRateLimit",
  "this.ReduceFees",
  "this.SubstituteCommonCurrencyCodes",
  "this.IsSandboxModeEnabled",
  // `public newUpdates: boolean` in ts/src/base/Exchange.ts; the hand-written
  // struct field is the `NewUpdates bool` read by every WS loop
  "this.NewUpdates"
]);
var GO_BOOL_CALL_NAMES_NATIVE = [
  "this.IsEmpty",
  "this.IsJsonEncodedObject",
  "this.IsBinaryMessage"
];
var GO_DEREF_WRAPPED_CALLS = ["NumberToString", "Parse8601", "Iso8601"];
var GO_ANY_BOX_CALLS = [
  "GetValue",
  "Ternary",
  "SafeValue",
  "this.SafeValue",
  "SafeDict",
  "this.SafeDict",
  "SafeList",
  "this.SafeList",
  "SafeNumber",
  "this.SafeNumber"
];
var GO_PURE_STRING_ACCESSORS = [
  "this.SafeString",
  "this.SafeString2",
  "this.SafeStringN",
  "this.SafeStringLower",
  "this.SafeStringLower2",
  "this.SafeStringLowerN",
  "this.SafeStringUpper",
  "this.SafeStringUpper2",
  "this.SafeStringUpperN",
  "this.NumberToString",
  "this.SafeCurrencyCode"
];
var GO_PURE_BOOL_ACCESSORS = [
  "this.SafeBool",
  "this.SafeBool2",
  "this.SafeBoolN"
];
var GO_NIL_EQUIVALENT_POINTER_TYPES_Native = /* @__PURE__ */ new Set([
  "*sync.Map",
  "*string",
  "*int64",
  "*float64",
  "*bool",
  "*int",
  "*[]string",
  "*[]any",
  "*map[string]any"
]);
var GO_NILABLE_FIELDS_Typed = {
  "this.Options": "*sync.Map",
  "this.Markets": "*sync.Map",
  "this.Markets_by_id": "*sync.Map",
  "this.MarketsById": "*sync.Map",
  "this.Currencies": "*sync.Map",
  "this.Currencies_by_id": "*sync.Map",
  "this.CurrenciesById": "*sync.Map",
  "this.BaseCurrencies": "*sync.Map",
  "this.QuoteCurrencies": "*sync.Map",
  "this.Tickers": "*sync.Map",
  "this.Orderbooks": "*sync.Map",
  "this.Bidsasks": "*sync.Map",
  "this.Transactions": "*sync.Map"
};
var GO_TYPE_NAMES = ["string", "int", "int64", "float64", "bool", "any"];
var GO_SAFE_DICT_LOCAL_TYPE = "map[string]any";
var GO_SAFE_DICT_READ_HELPERS = ["GetValue", "InOp", "ObjectKeys", "IsDictionary"];
var GO_MARKET_LOCAL_TYPE = "map[string]any";
var GO_MARKET_READ_COMPARISON_OPERATORS = [
  _typescript2.default.SyntaxKind.EqualsEqualsToken,
  _typescript2.default.SyntaxKind.ExclamationEqualsToken,
  _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken,
  _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken,
  _typescript2.default.SyntaxKind.LessThanToken,
  _typescript2.default.SyntaxKind.GreaterThanToken,
  _typescript2.default.SyntaxKind.LessThanEqualsToken,
  _typescript2.default.SyntaxKind.GreaterThanEqualsToken,
  _typescript2.default.SyntaxKind.AmpersandAmpersandToken,
  _typescript2.default.SyntaxKind.BarBarToken
];
var GO_SAFE_LIST_LOCAL_TYPE = "[]any";
var GO_NUMERIC_KINDS = ["int", "int64", "float64"];
var ORDERED_COMPARISON_OPERATORS = {
  [_typescript2.default.SyntaxKind.GreaterThanToken]: ">",
  [_typescript2.default.SyntaxKind.GreaterThanEqualsToken]: ">=",
  [_typescript2.default.SyntaxKind.LessThanToken]: "<",
  [_typescript2.default.SyntaxKind.LessThanEqualsToken]: "<="
};
var GO_STRING_FIELD_NAMES = ["Id", "Name", "Version", "Url", "Hostname", "UserAgent"];
var GO_DEFAULTED_SAFE_STRING_ARITY = {
  "safeString": 3,
  "safeStringLower": 3,
  "safeStringUpper": 3,
  "safeString2": 4,
  "safeStringLower2": 4,
  "safeStringUpper2": 4,
  "safeStringN": 3,
  "safeStringLowerN": 3,
  "safeStringUpperN": 3
};
var GO_NON_NIL_DEFAULT_KINDS = [
  _typescript2.default.SyntaxKind.StringLiteral,
  _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral,
  _typescript2.default.SyntaxKind.NumericLiteral,
  _typescript2.default.SyntaxKind.TrueKeyword,
  _typescript2.default.SyntaxKind.FalseKeyword
];
var GO_WRITE_OPERATOR_KINDS = [
  _typescript2.default.SyntaxKind.EqualsToken,
  _typescript2.default.SyntaxKind.PlusEqualsToken,
  _typescript2.default.SyntaxKind.MinusEqualsToken,
  _typescript2.default.SyntaxKind.AsteriskEqualsToken,
  _typescript2.default.SyntaxKind.SlashEqualsToken,
  _typescript2.default.SyntaxKind.PercentEqualsToken
];
var GO_FIELD_CONTAINER_TYPES_NATIVE = {
  "Has": "map[string]any",
  "Api": "map[string]any",
  "TransformedApi": "map[string]any",
  "RequiredCredentials": "map[string]any",
  "HttpExceptions": "map[string]any",
  "Timeframes": "map[string]any",
  "Features": "map[string]any",
  "Exceptions": "map[string]any",
  "Precision": "map[string]any",
  "UserAgents": "map[string]any",
  "TokenBucket": "map[string]any",
  "CommonCurrencies": "map[string]any",
  "ProxyDictionaries": "map[string]any",
  "WsClients": "map[string]any",
  "Clients": "map[string]any",
  "Limits": "map[string]any",
  "Fees": "map[string]any",
  "Status": "map[string]any",
  "Countries": "map[string]any",
  "Options": "*sync.Map",
  "Markets": "*sync.Map",
  "Markets_by_id": "*sync.Map",
  "MarketsById": "*sync.Map",
  "Currencies": "*sync.Map",
  "Currencies_by_id": "*sync.Map",
  "CurrenciesById": "*sync.Map",
  "BaseCurrencies": "*sync.Map",
  "QuoteCurrencies": "*sync.Map",
  "Tickers": "*sync.Map",
  "Orderbooks": "*sync.Map",
  "Transactions": "*sync.Map"
};
var GO_ARITHMETIC_KINDS = [
  _typescript2.default.SyntaxKind.PlusToken,
  _typescript2.default.SyntaxKind.MinusToken,
  _typescript2.default.SyntaxKind.AsteriskToken,
  _typescript2.default.SyntaxKind.SlashToken,
  _typescript2.default.SyntaxKind.PercentToken
];
var GO_COMMENT_BREAK_END = /(?:[({[:]|[+\-*/%&|^<>=!])$/;
function goBracketBalance(code) {
  let depth = 0;
  let i = 0;
  while (i < code.length) {
    const ch = code[i];
    if (ch === '"' || ch === "'") {
      const quote = ch;
      i += 1;
      while (i < code.length) {
        if (code[i] === "\\") {
          i += 2;
          continue;
        }
        if (code[i] === quote) {
          i += 1;
          break;
        }
        i += 1;
      }
      continue;
    }
    if (ch === "`") {
      i += 1;
      while (i < code.length && code[i] !== "`") {
        i += 1;
      }
      i += 1;
      continue;
    }
    if (ch === "(" || ch === "[" || ch === "{") {
      depth += 1;
    } else if (ch === ")" || ch === "]" || ch === "}") {
      depth -= 1;
    }
    i += 1;
  }
  return depth;
}
function goFuncLiteralReturnType(value) {
  const literal = /^func\(\) ([\w.[\]]*) \{/.exec(value);
  if (literal === null || !value.endsWith("}()") || literal[1] === "any") {
    return void 0;
  }
  return literal[1];
}
function goTrailingCommentIndex(line, state) {
  let i = 0;
  while (i < line.length) {
    const ch = line[i];
    if (state.block) {
      if (ch === "*" && line[i + 1] === "/") {
        state.block = false;
        i += 2;
        continue;
      }
      i += 1;
      continue;
    }
    if (state.raw) {
      if (ch === "`") {
        state.raw = false;
      }
      i += 1;
      continue;
    }
    if (ch === "`") {
      state.raw = true;
      i += 1;
      continue;
    }
    if (ch === '"' || ch === "'") {
      const quote = ch;
      i += 1;
      while (i < line.length) {
        if (line[i] === "\\") {
          i += 2;
          continue;
        }
        if (line[i] === quote) {
          i += 1;
          break;
        }
        i += 1;
      }
      continue;
    }
    if (ch === "/" && line[i + 1] === "/") {
      return i;
    }
    if (ch === "/" && line[i + 1] === "*") {
      state.block = true;
      i += 2;
      continue;
    }
    i += 1;
  }
  return -1;
}
function goRuneWidth(text) {
  let width = 0;
  for (const _rune of text) {
    width += 1;
  }
  return width;
}
function alignGoTrailingComments(content) {
  const lines = content.split("\n");
  const entries = [];
  const state = { "block": false, "raw": false };
  for (let index = 0; index < lines.length; index++) {
    const commentIndex = goTrailingCommentIndex(lines[index], state);
    if (commentIndex < 0) {
      continue;
    }
    const code = lines[index].slice(0, commentIndex).replace(/[ \t]+$/, "");
    if (!code.trim()) {
      continue;
    }
    const indent = code.match(/^[ \t]*/)[0];
    entries.push({ "index": index, indent, code, "comment": lines[index].slice(commentIndex) });
  }
  const groups = [];
  let group = [];
  for (const entry of entries) {
    const previous = group[group.length - 1];
    const continues = previous && entry.index === previous.index + 1 && entry.indent === previous.indent && goBracketBalance(previous.code) === 0 && !GO_COMMENT_BREAK_END.test(previous.code);
    if (continues) {
      group.push(entry);
    } else {
      if (group.length) {
        groups.push(group);
      }
      group = [entry];
    }
  }
  if (group.length) {
    groups.push(group);
  }
  for (const members of groups) {
    let width = 0;
    for (const member of members) {
      width = Math.max(width, goRuneWidth(member.code));
    }
    width += 1;
    for (const member of members) {
      const pad = width - goRuneWidth(member.code);
      lines[member.index] = member.code + " ".repeat(pad) + member.comment;
    }
  }
  return lines.join("\n");
}
var GO_TS_SRC_STRING_PRODUCERS = [
  /^this\s*\.\s*safeString\s*\(/,
  /^this\s*\.\s*safeString2\s*\(/,
  /^this\s*\.\s*safeString3\s*\(/,
  /^this\s*\.\s*safeStringN\s*\(/,
  /^this\s*\.\s*safeStringLower\s*\(/,
  /^this\s*\.\s*safeStringLower2\s*\(/,
  /^this\s*\.\s*safeStringUpper\s*\(/,
  /^this\s*\.\s*safeStringUpper2\s*\(/,
  /^this\s*\.\s*safeCurrencyCode\s*\(/,
  /^this\s*\.\s*safeSymbol\s*\(/
];
var GO_TS_SRC_MAP_PRODUCERS = [
  /^this\s*\.\s*extend\s*\(/,
  /^this\s*\.\s*deepExtend\s*\(/,
  /^this\s*\.\s*keysort\s*\(/,
  /^this\s*\.\s*indexBy\s*\(/,
  /^this\s*\.\s*groupBy\s*\(/
];
function goBalancedCallArgs(text, open) {
  const args = [];
  let depth = 0;
  let current = "";
  let quote;
  for (let i = open; i < text.length; i++) {
    const ch = text[i];
    if (quote !== void 0) {
      current += ch;
      if (ch === "\\") {
        current += _nullishCoalesce(text[i + 1], () => ( ""));
        i++;
        continue;
      }
      if (ch === quote) {
        quote = void 0;
      }
      continue;
    }
    if (ch === '"' || ch === "'" || ch === "`") {
      quote = ch;
      current += ch;
      continue;
    }
    if (ch === "/" && text[i + 1] === "/") {
      while (i < text.length && text[i] !== "\n") {
        i++;
      }
      continue;
    }
    if (ch === "(") {
      depth++;
      if (depth > 1) {
        current += ch;
      }
      continue;
    }
    if (ch === ")") {
      depth--;
      if (depth === 0) {
        if (current.trim().length > 0) {
          args.push(current.trim());
        }
        return args;
      }
      current += ch;
      continue;
    }
    if (ch === "," && depth === 1) {
      args.push(current.trim());
      current = "";
      continue;
    }
    current += ch;
  }
  return void 0;
}
var GO_GETARG_EXCLUDED_POSITIONS = {
  "fetch2": [1, 3],
  "request": [1, 3],
  "sign": [1, 3]
};
var GO_PARAMS_TUPLE_HELPERS = {
  "handleUntilOption": 1,
  "handleNetworkCodeAndParams": 1,
  "handleWithdrawTagAndParams": 1,
  "handleTriggerAndParams": 1,
  "handleTriggerDirectionAndParams": 1,
  "handlePostOnly": 1,
  "handleTriggerPricesAndParams": 3
};
var GO_GETARG_NIL_MAP_READERS = [
  "GetValue",
  "InOp",
  "ObjectKeys",
  "SafeValue",
  "SafeValue2",
  "SafeDict",
  "SafeList",
  "SafeString",
  "SafeString2",
  "SafeStringN",
  "SafeStringUpper",
  "SafeStringLower",
  "SafeInteger",
  "SafeInteger2",
  "SafeNumber",
  "SafeNumber2",
  "SafeFloat",
  "SafeBool",
  "SafeTimestamp"
];
var GoTranspiler = class extends BaseTranspiler {
  // stdlib packages the printed source file references. A Go import must precede the first
  // declaration, so the file-level print (printSourceFileStatements) collects names here and
  // prepends `import "..."`. Nested prints keep their own list; memo of goStdlibImportIsPlaceable().
  constructor(config = {}) {
    config["parser"] = Object.assign({}, parserConfig4, _nullishCoalesce(config["parser"], () => ( {})));
    super(config);
    this.wrapCallMethods = [];
    // declarations whose Go local type is being resolved right now (see goLocalStaticType)
    this.goLocalTypeResolution = /* @__PURE__ */ new Set();
    // appended to every async (channel returning) Go method/function name and to each
    // checker-resolved call site of one; '' disables the rename
    this.asyncMethodSuffix = "";
    this.DEFAULT_RETURN_TYPE = "any";
    // suffix of the sibling body method an async trampoline hands its work to
    this.ASYNC_BODY_SUFFIX = "Body";
    // gofmt indents every nesting level with exactly one tab; the printer emits the
    // same bytes so the generated tree needs no `gofmt` pass (campaign go-gofmt F01)
    this.DEFAULT_IDENTATION = "	";
    // `var x any = this.SafeDict(container, key)` -> `var x map[string]any = SafeMapTyped(...)`:
    // same member read, and every later use reads the local; with the absent case a nil map,
    // GetValue/InOp/ObjectKeys/IsDictionary and Safe* normalise a nil receiver. Else keep the box.
    this.goSafeDictLocalUnboxCache = /* @__PURE__ */ new Map();
    // `var market any = this.Market(symbol)` -> `var market map[string]any = MapTyped(...)`.
    // The box holds the dictionary the checker proved and every later use reads it (SafeDict's scan),
    // so this is a pure refinement. A value position, nil test or map write keeps the box.
    this.goMarketLocalUnboxCache = /* @__PURE__ */ new Map();
    // `var x any = this.SafeList(container, key)` -> `var x []any = SafeListTyped(container, key)`.
    // SafeListTyped reads the same member and every later use reads it as a list: a nil slice counts
    // 0 and indexes to nil, matching the box's absent case. Anything else keeps the box.
    this.goSafeListLocalUnboxCache = /* @__PURE__ */ new Map();
    // true when an `any`-typed local can hold a *T helper result: its initializer or a
    // later `x = …` write is a `this.safeX(…)` call whose Go signature returns a pointer
    this.goAnyLocalHoldsPointerCache = /* @__PURE__ */ new Map();
    // true when every value written into an `any` local is a non-pointer source: the
    // box holds a container, a scalar or nil, so IsEqual(x, nil) is exactly `x == nil`.
    // A write of any other shape in the enclosing function (D2 scan) keeps the helper
    this.goAnyLocalHoldsNonPointerCache = /* @__PURE__ */ new Map();
    // B-02: a parameter of a nullable ccxt alias (`Str`) prints as the native Go type (`*string`) only
    // when the method is internal (not async/override, no inherited member), every call site passes
    // that type (checker here, textual for siblings), and the body never writes another type (D2).
    this.goNativeParameterTypeCache = /* @__PURE__ */ new Map();
    this.goSameFileCallCache = /* @__PURE__ */ new Map();
    this.goTsSrcTreeCache = /* @__PURE__ */ new Map();
    // the Go type this identifier is actually *declared* with, or undefined when it
    // stays `any`. It goes through getGoLocalType, not goTypeOfInitializer, so a
    // declaration the reject filters demoted back to `any` is reported as `any` here
    // too — otherwise we would emit `*x` against an `any` box.
    // A parameter resolves to its declared Go type only when goNativeParameterType
    // proved it (see the B-02 block above); everything else stays `any`.
    //
    // getGoLocalType re-prints every reassignment's right-hand side, and printing a
    // ternary re-enters printCondition, which lands back here: `x = (x === 'a') ? …`
    // would recurse forever. The in-progress set breaks that cycle by answering
    // `any` for the declaration currently being classified, and the cache keeps the
    // per-occurrence cost at one scope scan per declaration.
    this.goDeclaredTypeCache = /* @__PURE__ */ new Map();
    this.goDeclaredTypeInProgress = /* @__PURE__ */ new Set();
    // `x.length` on a value the printer declares as a slice is `len(x)`: Go's len is
    // 0 for a nil slice, exactly like GetArrayLength's `[]T` cases. On a map or an
    // `any` box GetArrayLength answers 0, so only a `[]`-typed value may inline —
    // and only for the slice types the helper itself counts (a []byte would answer 0).
    this.sliceLengthTypes = ["[]any", "[]string", "[]int64", "[]float64", "[]bool", "[]int", "[][]any", "[]map[string]any"];
    // hand-written BaseExchange fields (go/v4/exchange.go) whose Go type is one of the
    // slice types above: the struct value is never a nil pointer and len(field) is the
    // very value the helper's type switch returns for it.
    this.GO_NATIVE_LENGTH_FIELDS = ["Symbols"];
    // comparison helpers that normalize int/int64/float64 against each other, so a
    // literal operand's Go default type (int) behaves like the int64 OpNeg produces
    this.comparisonHelpers = ["IsEqual", "IsGreaterThan", "IsLessThan", "IsGreaterThanOrEqual", "IsLessThanOrEqual"];
    // level of the statement being printed: a multi-line composite literal is laid out
    // relative to it (go/printer), whatever level the expression printers hand down
    this.goStatementLevel = 0;
    // stdlib packages this file's native emissions call; printSourceFileStatements turns them
    // into the import declarations the emitted Go needs (an occurrence outside a file print,
    // e.g. the intellisense body, only marks a set that the next file print replaces)
    this.goFileStdlibImports = /* @__PURE__ */ new Set();
    // check this out later
    // IsArray(v) answers true exactly for the slice types its runtime type switch lists
    // and false for every other box, a nil box included. On an operand whose Go type the
    // printer can name the predicate is that constant.
    this.isArraySliceTypes = [
      "[]any",
      "[][]any",
      "[]map[string]any",
      "[]string",
      "[]bool",
      "[]int",
      "[]int8",
      "[]int16",
      "[]int32",
      "[]int64",
      "[]float32",
      "[]float64",
      "[]uint",
      "[]uint8",
      "[]uint16",
      "[]uint32",
      "[]uint64"
    ];
    // the other Go types the printer can name: a map, a string, a bool, a number, or a
    // pointer to a scalar — none of them is a slice the switch matches
    this.isArrayNonSliceTypes = [
      "map[string]any",
      "string",
      "bool",
      "int",
      "int64",
      "float64",
      "*string",
      "*int64",
      "*float64",
      "*bool",
      "*int"
    ];
    // -----------------------------------------------------------------------
    // gofmt-compatible spacing of the binary expressions this printer emits
    // -----------------------------------------------------------------------
    // go/printer (nodes.go) prints a binary expression with blanks around the
    // operator unless the expression sits deeper than the top level of a
    // statement: binaryExpr() asks cutoff() - which inspects the operator tree
    // through walkBinary() - and drops *both* blanks when the operator
    // precedence is below that cutoff. Level 4/5 operators (`+ - * / % & | ^
    // << >>`) therefore print as `a + b` at the top level but as `a+b`, `a[i+1]`
    // one level down; comparisons and `&&`/`||` (level 3 and below) always keep
    // their blanks.
    //
    // goExprDepth mirrors the depth go/printer tracks over the Go AST it is
    // about to emit: 1 at the start of every statement, +1 for an argument list
    // with more than one argument, +1 for an index expression, +1 for the right
    // operand of a binary expression, -1 inside parentheses (never below 1), and
    // back to 1 for composite literal elements.
    this.goExprDepth = 1;
    this.requiresParameterType = true;
    this.requiresReturnType = true;
    this.asyncTranspiling = false;
    this.implicitAsyncTranspiling = true;
    this.supportsFalsyOrTruthyValues = false;
    this.requiresCallExpressionCast = true;
    this.wrapThisCalls = false;
    this.id = "Go";
    this.className = "undefined";
    this.classNameMap = _nullishCoalesce(config["classNameMap"], () => ( {}));
    this.initConfig();
    this.applyUserOverrides(config);
    this.wrapThisCalls = _nullishCoalesce(config["wrapThisCalls"], () => ( false));
    this.wrapCallMethods = _nullishCoalesce(config["wrapCallMethods"], () => ( []));
    this.asyncMethodSuffix = _nullishCoalesce(config["asyncMethodSuffix"], () => ( ""));
  }
  initConfig() {
    this.LeftPropertyAccessReplacements = {
      // 'this': '$this',
    };
    this.RightPropertyAccessReplacements = {
      "push": "Add",
      // list method
      "indexOf": "IndexOf",
      // list method
      "toUpperCase": "ToUpper",
      "toLowerCase": "ToLower",
      "toString": "ToString"
    };
    this.FullPropertyAccessReplacements = {
      "JSON.parse": "parseJson",
      // custom helper method
      "console.log": "fmt.Println",
      "Number.MAX_SAFE_INTEGER": "Int32.MaxValue",
      "Math.min": "Math.Min",
      "Math.max": "Math.Max",
      "Math.log": "Math.Log",
      "Math.abs": "Math.Abs",
      // 'Math.ceil':  'Math.Ceiling', // need cast
      // 'Math.round': 'Math.Round', // need to cast
      "Math.floor": "Math.Floor",
      "Math.pow": "Math.Pow"
      // 'Promise.all': 'Task.WhenAll',
    };
    this.CallExpressionReplacements = {
      // "parseInt": "parseINt",
      // "parseFloat": "float.Parse",
    };
    this.ReservedKeywordsReplacements = {
      // 'string': 'str',
      // 'params': 'parameters',
      "type": "typeVar"
      // 'internal': 'intern',
      // 'event': 'eventVar',
      // 'fixed': 'fixedVar',
    };
    this.binaryExpressionsWrappers = {
      [_typescript2.default.SyntaxKind.EqualsEqualsToken]: [this.EQUALS_EQUALS_WRAPPER_OPEN, this.EQUALS_EQUALS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.EqualsEqualsEqualsToken]: [this.EQUALS_EQUALS_WRAPPER_OPEN, this.EQUALS_EQUALS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.ExclamationEqualsToken]: [this.DIFFERENT_WRAPPER_OPEN, this.DIFFERENT_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken]: [this.DIFFERENT_WRAPPER_OPEN, this.DIFFERENT_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.GreaterThanToken]: [this.GREATER_THAN_WRAPPER_OPEN, this.GREATER_THAN_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.GreaterThanEqualsToken]: [this.GREATER_THAN_EQUALS_WRAPPER_OPEN, this.GREATER_THAN_EQUALS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.LessThanToken]: [this.LESS_THAN_WRAPPER_OPEN, this.LESS_THAN_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.LessThanEqualsToken]: [this.LESS_THAN_EQUALS_WRAPPER_OPEN, this.LESS_THAN_EQUALS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.PlusToken]: [this.PLUS_WRAPPER_OPEN, this.PLUS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.MinusToken]: [this.MINUS_WRAPPER_OPEN, this.MINUS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.AsteriskToken]: [this.MULTIPLY_WRAPPER_OPEN, this.MULTIPLY_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.PercentToken]: [this.MOD_WRAPPER_OPEN, this.MOD_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.SlashToken]: [this.DIVIDE_WRAPPER_OPEN, this.DIVIDE_WRAPPER_CLOSE]
    };
  }
  // getBlockOpen(identation){
  //     return this.getIden(identation)  + this.BLOCK_OPENING_TOKEN;
  // }
  printSuperCallInsideConstructor(node, identation) {
    return "";
  }
  printStringLiteral(node) {
    const token = this.STRING_QUOTE_TOKEN;
    let text = node.text;
    if (text in this.StringLiteralReplacements) {
      return this.StringLiteralReplacements[text];
    }
    if (/[\\"\b\f\n\r\t]/.test(text)) {
      const backslashPlaceholder = "\0";
      text = text.replaceAll("\\", backslashPlaceholder);
      text = text.replaceAll("\b", "\\b");
      text = text.replaceAll("\f", "\\f");
      text = text.replaceAll("\n", "\\n");
      text = text.replaceAll("\r", "\\r");
      text = text.replaceAll("	", "\\t");
      text = text.replaceAll(backslashPlaceholder, "\\\\");
      text = text.replaceAll('"', '\\"');
    }
    return token + text + token;
  }
  transformFunctionNameIfNeeded(name) {
    return this.capitalize(name);
  }
  // The cells of one struct field in the shape gofmt's fieldList() prints them: a named
  // field is `Name Type [Tag]` (the name cell — and, when the field carries a tag, the type
  // cell too — is a tab-terminated column cell) and an embedded field is a single cell.
  // printStruct() lays those cells out; printPropertyDeclaration() joins them with spaces.
  getStructFieldCells(node) {
    const name = this.capitalize(this.printNode(node.name, 0));
    let type = "any";
    if (node.type === void 0) {
      type = "any";
    } else if (node.type.kind === SyntaxKind3.StringKeyword) {
      type = "string";
    } else if (node.type.kind === SyntaxKind3.NumberKeyword) {
      type = "int";
    } else if (node.type.kind === SyntaxKind3.BooleanKeyword || _typescript2.default.isBooleanLiteral(node)) {
      type = "bool";
    } else if (node.type.kind === SyntaxKind3.ArrayType) {
      type = "[]any";
    }
    const cells = [name, type];
    if (node.initializer) {
      let initializer = this.printNode(node.initializer, 0);
      initializer = initializer.replaceAll('"', "");
      cells.push(`\`default:"${initializer}"\``);
    }
    return cells;
  }
  printPropertyDeclaration(node, identation) {
    return this.getIden(identation) + this.getStructFieldCells(node).join(" ") + this.LINE_TERMINATOR;
  }
  printStruct(node, indentation) {
    const rows = [];
    if (_optionalChain([node, 'optionalAccess', _453 => _453.heritageClauses, 'optionalAccess', _454 => _454.length]) > 0) {
      const heritage = node.heritageClauses[0];
      const heritageType = heritage.types[0];
      let heritageEscapedText = heritageType.expression.escapedText;
      if (this.classNameMap[heritageEscapedText]) {
        heritageEscapedText = this.classNameMap[heritageEscapedText];
      }
      rows.push([heritageEscapedText]);
    }
    const propDeclarations = node.members.filter((member) => member.kind === SyntaxKind3.PropertyDeclaration);
    propDeclarations.forEach((member) => rows.push(this.getStructFieldCells(member)));
    const lines = rows.map((cells, row) => {
      let line = cells[0];
      for (let column = 0; column < cells.length - 1; column++) {
        let width = 0;
        for (let previous = row; previous >= 0 && rows[previous].length > column + 1; previous--) {
          width = Math.max(width, rows[previous][column].length);
        }
        for (let next = row + 1; next < rows.length && rows[next].length > column + 1; next++) {
          width = Math.max(width, rows[next][column].length);
        }
        line += " ".repeat(width + 1 - cells[column].length) + cells[column + 1];
      }
      return this.getIden(indentation + 1) + line;
    });
    const body = lines.length ? "\n" + lines.join("\n") + "\n" : "\n";
    return `type ${this.className} struct {${body}}`;
  }
  printNewStructMethod(node) {
    return `
func New${this.capitalize(this.className)}() *${this.className} {
	p := &${this.className}{}
	setDefaults(p)
	return p
}
`;
  }
  printClass(node, identation) {
    this.className = node.name.escapedText;
    if (this.classNameMap[this.className]) {
      this.className = this.classNameMap[this.className];
    }
    const struct = this.printStruct(node, identation);
    const newMethod = this.printNewStructMethod(node);
    const methods = node.members.filter((member) => member.kind === SyntaxKind3.MethodDeclaration);
    const classMethods = this.joinTopLevelDecls(methods.map((method) => this.printMethodDeclaration(method, identation)));
    return struct + "\n" + newMethod + "\n" + classMethods;
  }
  /**
   * gofmt's declaration-list rule (go/printer nodes.go `declList`): a top-level
   * declaration that carries a doc comment is separated from the previous declaration
   * by exactly one blank line (`min = 2` linebreaks), while a declaration without one
   * keeps the source's own separation (the printer emits members adjacent to the
   * closing brace above them). `printClass` used to join every member with a bare
   * "\n", so a method whose leading `/** ... *` + `/` comment follows the previous
   * method's closing brace came out as `}\n/**` and gofmt re-inserted the blank line.
   */
  joinTopLevelDecls(decls) {
    return decls.map((decl, index) => {
      if (index === 0 || !this.startsWithComment(decl)) {
        return (index === 0 ? "" : "\n") + decl;
      }
      return "\n\n" + decl;
    }).join("");
  }
  /**
   * True when the emitted declaration text opens with its doc comment - the comment
   * group gofmt attaches to the declaration (`getDoc(d) != nil` in go/printer).
   */
  startsWithComment(decl) {
    return this.isComment(decl.split("\n")[0]);
  }
  /**
   * Indent every non-blank line of `lines` by `identation` levels. gofmt trims trailing
   * whitespace, so an indented *blank* line (only the indentation of a blank source
   * line) must stay empty instead of becoming whitespace-only text.
   */
  indentLines(lines, identation) {
    return lines.map((line) => line.trim().length === 0 ? "" : this.getIden(identation) + line);
  }
  printPropertyAccessModifiers(node) {
    return "";
  }
  printSpreadElement(node, identation) {
    const expression = this.printNode(node.expression, 0);
    return this.getIden(identation) + expression + this.SPREAD_TOKEN;
  }
  printMethodDeclaration(node, identation) {
    let methodDef = this.printMethodDefinition(node, identation);
    const isAsync = this.isAsyncFunction(node);
    const funcBody = this.printFunctionBody(node, identation, isAsync);
    if (!isAsync) {
      methodDef += funcBody;
      return methodDef;
    }
    const goName = this.transformMethodNameIfNeeded(node.name.escapedText);
    const bodyName = this.getAsyncBodyName(node, goName);
    const trampoline = methodDef + this.printAsyncTrampolineBlock(node, identation, `${this.THIS_TOKEN}.${bodyName}`);
    const bodyDef = `${this.getIden(identation)}func (${this.THIS_TOKEN} *${this.className}) ${bodyName}(${this.printAsyncBodyParameters(node)}) ${this.DEFAULT_RETURN_TYPE} `;
    return trampoline + "\n" + bodyDef + funcBody;
  }
  printFunctionDeclaration(node, identation) {
    if (_typescript2.default.isArrowFunction(node)) {
      const parameters = node.parameters.map((param) => this.printParameter(param)).join(", ");
      const body = this.printNode(node.body);
      return `(${parameters}) => ${body}`;
    }
    const isAsync = this.isAsyncFunction(node);
    const functionDef = this.printFunctionDefinition(node, identation);
    const funcBody = this.printFunctionBody(node, identation, isAsync);
    if (!isAsync) {
      return functionDef + funcBody;
    }
    const goName = this.transformMethodNameIfNeeded(node.name.escapedText);
    const bodyName = this.getAsyncBodyName(node, goName);
    const trampoline = functionDef + this.printAsyncTrampolineBlock(node, identation, bodyName);
    const bodyDef = `${this.getIden(identation)}func ${bodyName}(${this.printAsyncBodyParameters(node)}) ${this.DEFAULT_RETURN_TYPE} `;
    return trampoline + "\n" + bodyDef + funcBody;
  }
  /**
   * Name of the sibling *body* method/function an async core hands its work to.
   *
   * `FetchTicker` -> `fetchTickerBody`. Deliberately UNEXPORTED: the body is an
   * implementation detail of the trampoline, so it must not show up on the generated
   * interfaces (ICoreExchange) nor on the typed `*_wrapper.go` facades, and it stays
   * invisible to the reflection based `callInternal`/`callDynamically` dispatch.
   *
   * If that name is already taken by a real declaration (a hand written
   * `fetchTickerBody`), a numeric suffix is appended instead of silently clobbering it.
   */
  getAsyncBodyName(node, goName) {
    const taken = /* @__PURE__ */ new Set();
    const remember = (raw) => {
      if (!raw) {
        return;
      }
      const name2 = String(raw);
      taken.add(name2);
      try {
        taken.add(this.transformMethodNameIfNeeded(name2));
      } catch (e2) {
      }
    };
    try {
      const parent = _optionalChain([node, 'optionalAccess', _455 => _455.parent]);
      if (parent && _typescript2.default.isClassDeclaration(parent)) {
        parent.members.forEach((member) => remember(_optionalChain([member, 'optionalAccess', _456 => _456.name, 'optionalAccess', _457 => _457.escapedText])));
      } else if (parent && _typescript2.default.isSourceFile(parent)) {
        parent.statements.forEach((statement) => {
          if (_typescript2.default.isFunctionDeclaration(statement)) {
            remember(_optionalChain([statement, 'optionalAccess', _458 => _458.name, 'optionalAccess', _459 => _459.escapedText]));
          }
        });
      }
    } catch (e3) {
    }
    const base = goName.charAt(0).toLowerCase() + goName.slice(1) + this.ASYNC_BODY_SUFFIX;
    let name = base;
    let suffix = 0;
    while (taken.has(name)) {
      suffix++;
      name = `${base}${suffix}`;
    }
    return name;
  }
  /**
   * Parameter list of the body: the channel it must fill, then the original parameters
   * verbatim (including the `optionalArgs ...any` tail), so the trampoline can forward
   * its own arguments unchanged.
   */
  printAsyncBodyParameters(node) {
    const params = this.printMethodParameters(node);
    const channelParam = `ch chan ${this.DEFAULT_RETURN_TYPE}`;
    return params ? `${channelParam}, ${params}` : channelParam;
  }
  /**
   * Arguments the trampoline forwards to its body, matching printMethodParameters:
   * the declared parameters in order, plus the variadic `optionalArgs...` tail when
   * the function has any defaulted parameter.
   */
  printAsyncTrampolineArgs(node) {
    const args = [];
    let hasOptionalParameter = false;
    (_nullishCoalesce(_optionalChain([node, 'optionalAccess', _460 => _460.parameters]), () => ( []))).forEach((param) => {
      if (param.initializer) {
        hasOptionalParameter = true;
        return;
      }
      args.push(this.printNode(param.name, 0));
    });
    if (hasOptionalParameter) {
      args.push("optionalArgs...");
    }
    return args.join(", ");
  }
  /**
   * The trampoline: an async core hands back a *hot handle*.
   *
   *     func (this *Exchange) FetchTicker(symbol any) <-chan any {
   *         ch := make(chan any, 1)
   *         go this.fetchTickerBody(ch, symbol)
   *         return ch
   *     }
   *
   *   - `ch` is buffered (cap 1): the body's single `ch <- value` never blocks, so a
   *     result nobody ever receives still lets the goroutine finish and run
   *     `defer close(ch)` (no leak for abandoned calls).
   *   - the body runs on its own goroutine, so the call expression returns immediately
   *     with work already in flight. That is what makes
   *     `const a = this.fetchA (); const b = this.fetchB (); await Promise.all([a,b])`
   *     overlap, exactly like the C#/Java ports, with no call-site wrapper.
   *   - the result stays UNNAMED (`<-chan any`): `return ch` is the trampoline's only
   *     statement and it always runs, because the recover (`defer ReturnPanicError(ch)`)
   *     lives on the body, not here.
   */
  printAsyncTrampolineBlock(node, identation, callee) {
    const args = this.printAsyncTrampolineArgs(node);
    const argList = args ? `, ${args}` : "";
    return [
      // F04: the signature above ends WITHOUT a trailing space, so the block opener
      // carries the one space before `{` (same contract as getBlockOpen)
      " {",
      `${this.getIden(identation + 1)}ch := make(chan ${this.DEFAULT_RETURN_TYPE}, 1)`,
      `${this.getIden(identation + 1)}go ${callee}(ch${argList})`,
      `${this.getIden(identation + 1)}return ch`,
      `${this.getIden(identation)}}`
    ].join("\n");
  }
  /**
   * Go name of an async (channel returning) declaration: `fetchTicker` -> `FetchTickerAsync`.
   * Empty `asyncMethodSuffix` (the default) keeps the plain name, so the suffix is opt-in.
   */
  printAsyncDeclarationName(node, goName) {
    if (!this.asyncMethodSuffix || !this.isAsyncFunction(node)) {
      return goName;
    }
    return goName + this.asyncMethodSuffix;
  }
  /**
   * Resolve the declaration a call/property access refers to and append `asyncMethodSuffix`
   * when it is an async function. Uses the checker, so `this.x()`, `super.x()`, `obj.x()` and
   * bare `x()` all agree with the declaration site. Unresolvable or non-function symbols
   * (properties, `any` receivers, JS builtins) keep the plain name.
   */
  applyAsyncSuffixToCallee(nameNode, goName) {
    if (!this.asyncMethodSuffix || !nameNode) {
      return goName;
    }
    let decls;
    try {
      let symbol = this.getChecker().getSymbolAtLocation(nameNode);
      if (symbol && symbol.flags & _typescript2.default.SymbolFlags.Alias) {
        symbol = this.getChecker().getAliasedSymbol(symbol);
      }
      decls = _optionalChain([symbol, 'optionalAccess', _461 => _461.declarations]);
    } catch (e4) {
      return goName;
    }
    if (!decls || decls.length === 0) {
      return goName;
    }
    const isAsyncDecl = decls.some((d) => (_typescript2.default.isMethodDeclaration(d) || _typescript2.default.isFunctionDeclaration(d)) && d.body !== void 0 && this.isAsyncFunction(d));
    return isAsyncDecl ? goName + this.asyncMethodSuffix : goName;
  }
  printMethodDefinition(node, identation) {
    let name = node.name.escapedText;
    name = this.printAsyncDeclarationName(node, this.transformMethodNameIfNeeded(name));
    const returnType = this.printFunctionType(node).trim();
    const parsedArgs = this.printMethodParameters(node);
    const methodToken = this.METHOD_TOKEN ? this.METHOD_TOKEN + " " : " ";
    const structReceiver = `(${this.THIS_TOKEN} *${this.className})`;
    const returnSignature = returnType ? " " + returnType : "";
    const methodDef = this.getIden(identation) + methodToken + structReceiver + " " + name + "(" + parsedArgs + ")" + returnSignature;
    return this.printNodeCommentsIfAny(node, identation, methodDef);
  }
  printFunctionDefinition(node, identation) {
    let name = node.name.escapedText;
    name = this.printAsyncDeclarationName(node, this.transformMethodNameIfNeeded(name));
    const returnType = this.printFunctionType(node).trim();
    const parsedArgs = this.printMethodParameters(node);
    const methodToken = this.METHOD_TOKEN ? this.METHOD_TOKEN + " " : " ";
    const returnSignature = returnType ? " " + returnType : "";
    const methodDef = this.getIden(identation) + methodToken + name + "(" + parsedArgs + ")" + returnSignature;
    return this.printNodeCommentsIfAny(node, identation, methodDef);
  }
  printMethodParameters(node) {
    const params = node.parameters.map((param) => this.printParameter(param));
    const hasOptionalParameter = params.some((p) => p === "optional");
    if (!hasOptionalParameter) {
      return params.join(", ");
    }
    const paramsWithOptional = params.filter((param) => param !== "optional");
    paramsWithOptional.push("optionalArgs ...any");
    return paramsWithOptional.join(", ");
  }
  printParameter(node, defaultValue = true) {
    const name = this.printNode(node.name, 0);
    const initializer = node.initializer;
    const type = this.printParameterType(node);
    if (defaultValue) {
      if (initializer) {
        return "optional";
      }
      return name + " " + type;
    }
    return name + " " + type;
  }
  printParameterType(node) {
    const nativeType = this.goNativeParameterType(node);
    if (nativeType !== void 0) {
      return nativeType;
    }
    const typeText = this.getType(node);
    return "any";
    if (typeText === this.STRING_KEYWORD) {
      return "string";
    }
    if (typeText === this.NUMBER_KEYWORD) {
      return "float64";
    }
    if (typeText === this.BOOLEAN_KEYWORD) {
      return "bool";
    }
    return this.DEFAULT_PARAMETER_TYPE;
    if (typeText === void 0 || typeText === this.STRING_KEYWORD) {
      this.warn(node, node.getText(), "Parameter type not found, will default to: " + this.DEFAULT_PARAMETER_TYPE);
      return this.DEFAULT_PARAMETER_TYPE;
    }
    return typeText;
  }
  printFunctionType(node) {
    const typeText = this.getFunctionType(node);
    if (typeText === "void") {
      return "";
    }
    if (typeText === void 0 || typeText !== this.VOID_KEYWORD && typeText !== this.PROMISE_TYPE_KEYWORD) {
      let res = "";
      if (this.isAsyncFunction(node)) {
        res = `<-chan ${this.DEFAULT_RETURN_TYPE}`;
      } else {
        res = this.DEFAULT_RETURN_TYPE;
      }
      this.warn(node, node.name.getText(), "Function return type not found, will default to: " + res);
      return res;
    }
    if (typeText === this.PROMISE_TYPE_KEYWORD) {
      return `<-chan any`;
    }
    if (typeText && typeText.endsWith("[]")) {
      const core = typeText.substring(0, typeText.length - 2);
      const lastBracketPos = core.lastIndexOf("]");
      if (lastBracketPos !== -1) {
        return core.substring(0, lastBracketPos + 1) + "[]" + core.substring(lastBracketPos + 1);
      }
    }
    return typeText;
  }
  // true when the printed expression is a single call `Callee(...)` covering the
  // whole string, so its Go type is the callee's return type and nothing else
  isWholePrintedCall(value, open) {
    let depth = 0;
    let inString = false;
    let escaped = false;
    for (let i = open; i < value.length; i++) {
      const c = value[i];
      if (inString) {
        if (escaped) {
          escaped = false;
        } else if (c === "\\") {
          escaped = true;
        } else if (c === '"') {
          inString = false;
        }
        continue;
      }
      if (c === '"') {
        inString = true;
        continue;
      }
      if (c === "(") {
        depth++;
        continue;
      }
      if (c === ")") {
        depth--;
        if (depth === 0) {
          return i === value.length - 1;
        }
      }
    }
    return false;
  }
  // the concrete Go type the initializer already produces, or undefined when the
  // printer cannot name it (GetValue, Ternary, Add, ... return any)
  goTypeOfInitializer(initializer, printedValue) {
    if (this.goIsNativeSliceCall(initializer)) {
      return "string";
    }
    switch (_optionalChain([initializer, 'optionalAccess', _462 => _462.kind])) {
      case _typescript2.default.SyntaxKind.StringLiteral:
      case _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral:
        return "string";
      case _typescript2.default.SyntaxKind.TrueKeyword:
      case _typescript2.default.SyntaxKind.FalseKeyword:
        return "bool";
      case _typescript2.default.SyntaxKind.ObjectLiteralExpression:
        return "map[string]any";
      case _typescript2.default.SyntaxKind.ArrayLiteralExpression:
        return "[]any";
      case _typescript2.default.SyntaxKind.PrefixUnaryExpression:
        return initializer.operator === _typescript2.default.SyntaxKind.ExclamationToken ? "bool" : void 0;
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
        return this.goTypeOfInitializer(initializer.expression, printedValue);
      case _typescript2.default.SyntaxKind.BinaryExpression: {
        const op = initializer.operatorToken.kind;
        if (op === _typescript2.default.SyntaxKind.BarBarToken || op === _typescript2.default.SyntaxKind.AmpersandAmpersandToken) {
          return "bool";
        }
        if (op === _typescript2.default.SyntaxKind.EqualsEqualsToken || op === _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken || ORDERED_COMPARISON_OPERATORS[op] !== void 0) {
          return "bool";
        }
        if (op === _typescript2.default.SyntaxKind.PlusToken && _optionalChain([this, 'access', _463 => _463.goNativeArithmetic, 'call', _464 => _464(initializer), 'optionalAccess', _465 => _465.goType]) === "string") {
          return "string";
        }
        break;
      }
      case _typescript2.default.SyntaxKind.CallExpression: {
        const property = initializer.expression;
        if (_optionalChain([property, 'optionalAccess', _466 => _466.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression && _optionalChain([property, 'access', _467 => _467.name, 'optionalAccess', _468 => _468.escapedText]) === "toString" && _optionalChain([initializer, 'access', _469 => _469.arguments, 'optionalAccess', _470 => _470.length]) === 0 && this.goOperandStaticType(property.expression, printedValue) === "string") {
          return "string";
        }
        break;
      }
    }
    let value = printedValue.trim();
    while (value.startsWith("(") && this.isWholePrintedCall(value, 0)) {
      value = value.substring(1, value.length - 1).trim();
    }
    const open = value.indexOf("(");
    const literalType = goFuncLiteralReturnType(value);
    if (literalType !== void 0 && GO_TYPE_NAMES.indexOf(literalType) >= 0) {
      return literalType;
    }
    if (value.startsWith("func() int {") && value.endsWith("}()")) {
      return "int";
    }
    if (open <= 0 || !this.isWholePrintedCall(value, open)) {
      return void 0;
    }
    const callee = value.substring(0, open);
    if (!/^[A-Za-z_][\w.]*$/.test(callee)) {
      return void 0;
    }
    return _nullishCoalesce(GO_NATIVE_CALL_RETURN_TYPES[callee], () => ( GO_HELPER_RETURN_TYPES[callee]));
  }
  // strips the wrapping parentheses the source (or an operand) printed around a
  // whole expression, so the inner text can be classified
  goUnwrapPrintedParens(printedText) {
    let value = (_nullishCoalesce(printedText, () => ( ""))).trim();
    while (value.startsWith("(") && this.isWholePrintedCall(value, 0)) {
      value = value.substring(1, value.length - 1).trim();
    }
    return value;
  }
  // the concrete Go type of a `var x T = <init>` local, undefined for `any`
  goLocalStaticType(node) {
    const declaration = _optionalChain([this, 'access', _471 => _471.getChecker, 'call', _472 => _472(), 'access', _473 => _473.getSymbolAtLocation, 'call', _474 => _474(node), 'optionalAccess', _475 => _475.valueDeclaration]);
    if (_optionalChain([declaration, 'optionalAccess', _476 => _476.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration || declaration.initializer === void 0) {
      return void 0;
    }
    if (_optionalChain([declaration, 'access', _477 => _477.parent, 'optionalAccess', _478 => _478.parent, 'optionalAccess', _479 => _479.kind]) !== _typescript2.default.SyntaxKind.FirstStatement) {
      return void 0;
    }
    if (this.goLocalTypeResolution.has(declaration)) {
      return void 0;
    }
    this.goLocalTypeResolution.add(declaration);
    try {
      return this.getGoLocalType(declaration, this.printNode(declaration.initializer, 0));
    } finally {
      this.goLocalTypeResolution.delete(declaration);
    }
  }
  // `x := <init>` takes the Go type of the printed initializer, and a `for` initializer carries no
  // annotation, so only decidable literal shapes are named: untyped integer constant is `int`,
  // floating-point is `float64`. Integers not fitting `int` and other initializers keep the helper.
  goInferredLocalStaticType(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const declaration = _optionalChain([checker, 'access', _480 => _480.getSymbolAtLocation, 'call', _481 => _481(node), 'optionalAccess', _482 => _482.valueDeclaration]);
    if (_optionalChain([declaration, 'optionalAccess', _483 => _483.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration || declaration.initializer === void 0) {
      return void 0;
    }
    const declarationList = declaration.parent;
    if (_optionalChain([declarationList, 'optionalAccess', _484 => _484.kind]) !== _typescript2.default.SyntaxKind.VariableDeclarationList || _optionalChain([declarationList, 'access', _485 => _485.parent, 'optionalAccess', _486 => _486.kind]) === _typescript2.default.SyntaxKind.FirstStatement) {
      return void 0;
    }
    const kind = this.goNumericLiteralKind(declaration.initializer);
    if (kind === "int" && !this.goLiteralFitsKind(declaration.initializer, "int")) {
      return void 0;
    }
    return kind;
  }
  // `this.<field>` read of a hand-written BaseExchange string field
  goStringFieldStaticType(node, printedText) {
    const match = /^this\.([A-Za-z_]\w*)$/.exec(this.goUnwrapPrintedParens(printedText));
    if (match === null || GO_STRING_FIELD_NAMES.indexOf(match[1]) < 0) {
      return void 0;
    }
    return "string";
  }
  // a parameter the printer's own signature printer types with a concrete Go scalar:
  // the emitted Go parameter holds that type at every use, so the operator rule can
  // consume it (the typed-param families re-type `Str`/`Int`/`Num` params this way)
  goDeclaredParamStaticType(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const declaration = _optionalChain([checker, 'access', _487 => _487.getSymbolAtLocation, 'call', _488 => _488(node), 'optionalAccess', _489 => _489.valueDeclaration]);
    if (_optionalChain([declaration, 'optionalAccess', _490 => _490.kind]) !== _typescript2.default.SyntaxKind.Parameter) {
      return void 0;
    }
    const bound = this.goGetArgParameterType(declaration);
    if (bound !== void 0 && GO_TYPE_NAMES.indexOf(bound) >= 0 && ["string", "int64", "float64"].includes(bound)) {
      return bound;
    }
    let type;
    try {
      type = this.printParameterType(declaration);
    } catch (e) {
      return void 0;
    }
    if (typeof type !== "string" || type === "any" || GO_TYPE_NAMES.indexOf(type) < 0) {
      return void 0;
    }
    return this.goTypeNameIsShadowed(this.goEnclosingFunction(declaration), type) ? void 0 : type;
  }
  // a `*string` local prints a nilable Go pointer; it may only be dereferenced
  // where the checker narrowed it to a non-nilable string (a guard that always
  // exits, an `if (x !== undefined)` block). TypeScript `undefined` == Go nil here.
  goNilProvenStringDeref(node) {
    if (_optionalChain([node, 'optionalAccess', _491 => _491.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    if (this.goDeclaredTypeOfIdentifier(node) !== "*string") {
      return false;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const type = checker.getTypeAtLocation(node);
    return (type.flags & _typescript2.default.TypeFlags.StringLike) !== 0;
  }
  // a `*string` local every write of which is a SafeString-family call with a literal
  // default: that Go method can only return a fresh non-nil pointer, so the local may
  // be deref'd even where the checker offers no narrowing. One write of any other
  goDefaultedSafeStringLocal(node) {
    if (_optionalChain([node, 'optionalAccess', _492 => _492.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    let declaration;
    try {
      declaration = _optionalChain([this, 'access', _493 => _493.getChecker, 'call', _494 => _494(), 'access', _495 => _495.getSymbolAtLocation, 'call', _496 => _496(node), 'optionalAccess', _497 => _497.valueDeclaration]);
    } catch (e) {
      return false;
    }
    if (_optionalChain([declaration, 'optionalAccess', _498 => _498.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration || _optionalChain([declaration, 'access', _499 => _499.name, 'optionalAccess', _500 => _500.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    if (this.goDeclaredTypeOfIdentifier(node) !== "*string") {
      return false;
    }
    if (!this.goDefaultedSafeStringCall(declaration.initializer)) {
      return false;
    }
    const scope = this.goEnclosingFunction(declaration);
    if (scope === void 0) {
      return false;
    }
    const name = declaration.name.escapedText;
    let everyWriteDefaulted = true;
    const visit = (n) => {
      if (!everyWriteDefaulted) {
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.Identifier && n.escapedText === name && n !== declaration.name) {
        const parent = n.parent;
        if (_optionalChain([parent, 'optionalAccess', _501 => _501.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && parent.left === n && GO_WRITE_OPERATOR_KINDS.indexOf(_optionalChain([parent, 'access', _502 => _502.operatorToken, 'optionalAccess', _503 => _503.kind])) >= 0) {
          everyWriteDefaulted = parent.operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken && this.goDefaultedSafeStringCall(parent.right);
          return;
        }
        if (_optionalChain([parent, 'optionalAccess', _504 => _504.kind]) === _typescript2.default.SyntaxKind.PrefixUnaryExpression || _optionalChain([parent, 'optionalAccess', _505 => _505.kind]) === _typescript2.default.SyntaxKind.PostfixUnaryExpression) {
          everyWriteDefaulted = false;
          return;
        }
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return everyWriteDefaulted;
  }
  // a call to one of the hand-written SafeString-family methods that passes enough
  // arguments for the default and whose default is a literal (never the absent case)
  goDefaultedSafeStringCall(node) {
    const call = node;
    if (_optionalChain([call, 'optionalAccess', _506 => _506.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return false;
    }
    const callee = call.expression;
    if (_optionalChain([callee, 'optionalAccess', _507 => _507.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([callee, 'access', _508 => _508.expression, 'optionalAccess', _509 => _509.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return false;
    }
    const arity = GO_DEFAULTED_SAFE_STRING_ARITY[_optionalChain([callee, 'access', _510 => _510.name, 'optionalAccess', _511 => _511.escapedText])];
    if (arity === void 0 || (_nullishCoalesce(_optionalChain([call, 'access', _512 => _512.arguments, 'optionalAccess', _513 => _513.length]), () => ( 0))) < arity) {
      return false;
    }
    const last = call.arguments[call.arguments.length - 1];
    return GO_NON_NIL_DEFAULT_KINDS.indexOf(_optionalChain([last, 'optionalAccess', _514 => _514.kind])) >= 0;
  }
  // a `*string` leaf the concat may deref: the checker narrowed it to a non-nilable
  // string at that use, a local whose every write is a defaulted SafeString-family
  // call, or such a call inline
  goDerefableStringOperand(node) {
    if (this.goNilProvenStringDeref(node)) {
      return true;
    }
    if (_optionalChain([node, 'optionalAccess', _515 => _515.kind]) === _typescript2.default.SyntaxKind.Identifier) {
      return this.goDefaultedSafeStringLocal(node);
    }
    return this.goDefaultedSafeStringCall(node);
  }
  // the printed text of a `+` operand the concat rule may use: the printer's own
  // text for a proven string, or the deref goNativeBinaryText adds for a derefable
  // `*string` leaf. undefined keeps the helper call.
  goStringConcatOperandType(node, printedText) {
    if (this.goDerefableStringOperand(node)) {
      return "string";
    }
    return this.goOperandStaticType(node, printedText) === "string" ? "string" : void 0;
  }
  // `Add(Add(a, "lit"), b)` and every other `+` chain whose leaves are all non-nil
  // Go strings: the runtime Add only ever takes its string branch for those, so the
  // chain prints as the Go operator (`a + "lit" + b`). Anything else keeps the call.
  goNativeStringConcat(node, leftText, rightText) {
    if (this.goStringConcatOperandType(node.left, leftText) === void 0 || this.goStringConcatOperandType(node.right, rightText) === void 0) {
      return void 0;
    }
    return { "goType": "string", "text": this.goNativeBinaryText(node, "+", leftText, rightText) };
  }
  // Go static type of an operand's printed form: 'string', 'int', 'int64',
  // 'float64', or 'const-int' / 'const-float' (an untyped literal). undefined when
  // the printer cannot name it — a nilable/`any` operand keeps the helper call.
  goOperandStaticType(node, printedText) {
    switch (_optionalChain([node, 'optionalAccess', _516 => _516.kind])) {
      case _typescript2.default.SyntaxKind.StringLiteral:
      case _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral:
        return "string";
      case _typescript2.default.SyntaxKind.NumericLiteral:
        return /^[0-9]+$/.test(node.text) ? "const-int" : this.goConstFloatStaticType(node);
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
        return this.goOperandStaticType(node.expression, this.goUnwrapPrintedParens(printedText));
      case _typescript2.default.SyntaxKind.BinaryExpression:
        return _optionalChain([this, 'access', _517 => _517.goNativeArithmetic, 'call', _518 => _518(node), 'optionalAccess', _519 => _519.goType]);
      case _typescript2.default.SyntaxKind.Identifier:
        return _nullishCoalesce(_nullishCoalesce(this.goLocalStaticType(node), () => ( this.goInferredLocalStaticType(node))), () => ( this.goDeclaredParamStaticType(node)));
      case _typescript2.default.SyntaxKind.PropertyAccessExpression:
        return _nullishCoalesce(this.goStringFieldStaticType(node, printedText), () => ( this.goStringCallStaticType(node, printedText)));
    }
    return this.goStringCallStaticType(node, printedText);
  }
  // the Go type the printed expression already produces; '*string' / '*int64'
  // helpers box a nilable pointer, so those keep the helper call as well
  goStringCallStaticType(node, printedText) {
    if (GO_ANY_BOX_CALLS.indexOf(this.goPrintedCallee(printedText)) >= 0) {
      return void 0;
    }
    const goType = this.goTypeOfInitializer(node, printedText);
    return "string" === goType || "int" === goType || "int64" === goType || "float64" === goType ? goType : void 0;
  }
  // an untyped Go floating-point constant: the helper boxes it as float64 and the
  // compiler converts the operand to float64 the same way
  goConstFloatStaticType(node) {
    const text = _optionalChain([node, 'optionalAccess', _520 => _520.text]);
    if (typeof text !== "string" || !/^[0-9][.eE]/.test(text)) {
      return void 0;
    }
    return Number.isFinite(Number(text.replaceAll("_", ""))) ? "const-float" : void 0;
  }
  isNonZeroIntegerLiteral(node) {
    if (_optionalChain([node, 'optionalAccess', _521 => _521.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.isNonZeroIntegerLiteral(node.expression);
    }
    return _optionalChain([node, 'optionalAccess', _522 => _522.kind]) === _typescript2.default.SyntaxKind.NumericLiteral && /^[1-9][0-9]*$/.test(node.text);
  }
  isNonZeroFloatLiteral(node) {
    if (_optionalChain([node, 'optionalAccess', _523 => _523.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.isNonZeroFloatLiteral(node.expression);
    }
    return this.goConstFloatStaticType(node) !== void 0 && Number(node.text.replaceAll("_", "")) !== 0;
  }
  // the exact value of a constant-only integer expression, undefined otherwise. Go folds `a * b`
  // over literals in arbitrary precision, so it must fit `int` and stay exact for the helper's
  // float64 path — the bound the caller checks.
  goConstantIntValue(node) {
    if (_optionalChain([node, 'optionalAccess', _524 => _524.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.goConstantIntValue(node.expression);
    }
    if (_optionalChain([node, 'optionalAccess', _525 => _525.kind]) === _typescript2.default.SyntaxKind.NumericLiteral) {
      return /^[0-9]+$/.test(node.text) ? Number(node.text) : void 0;
    }
    if (_optionalChain([node, 'optionalAccess', _526 => _526.kind]) !== _typescript2.default.SyntaxKind.BinaryExpression || this.goConstantIntValue(node.left) === void 0 || this.goConstantIntValue(node.right) === void 0) {
      return void 0;
    }
    const left = this.goConstantIntValue(node.left);
    const right = this.goConstantIntValue(node.right);
    switch (node.operatorToken.kind) {
      case _typescript2.default.SyntaxKind.PlusToken:
        return left + right;
      case _typescript2.default.SyntaxKind.MinusToken:
        return left - right;
      case _typescript2.default.SyntaxKind.AsteriskToken:
        return left * right;
      case _typescript2.default.SyntaxKind.SlashToken:
        return right === 0 ? void 0 : Math.trunc(left / right);
      case _typescript2.default.SyntaxKind.PercentToken:
        return right === 0 ? void 0 : left % right;
    }
    return void 0;
  }
  // an arithmetic expression that is the whole initializer of a printed `var x T = ...`: the
  // declared-local table owns that position (naming int64 when its int-kind proof holds, with the
  // forced unbox), so the operator rule leaves the call to it.
  goInsideTypedDeclarationInitializer(node) {
    let current = node;
    while (current !== void 0) {
      const parent = current.parent;
      if (_optionalChain([parent, 'optionalAccess', _527 => _527.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression && parent.expression === current) {
        current = parent;
        continue;
      }
      if (_optionalChain([parent, 'optionalAccess', _528 => _528.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && (parent.left === current || parent.right === current) && GO_ARITHMETIC_KINDS.indexOf(parent.operatorToken.kind) >= 0) {
        current = parent;
        continue;
      }
      break;
    }
    const declaration = _optionalChain([current, 'optionalAccess', _529 => _529.parent]);
    if (_optionalChain([declaration, 'optionalAccess', _530 => _530.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration || declaration.initializer !== current) {
      return false;
    }
    return _optionalChain([declaration, 'access', _531 => _531.parent, 'optionalAccess', _532 => _532.parent, 'optionalAccess', _533 => _533.kind]) === _typescript2.default.SyntaxKind.FirstStatement;
  }
  // The bare Go operator must yield what the runtime helper returns (go/v4/exchange_helpers.go):
  // Add keeps int/int64, while Subtract/Multiply/Divide/Mod take an exact int64 path or a float64
  // path, so only the rows below are equivalent.
  goNativeNumericResultType(op, leftType, rightType, node) {
    const isIntKind = (kind) => kind === "int" || kind === "int64" || kind === "const-int";
    const isFloatKind = (kind) => kind === "float64" || kind === "const-float";
    const leftNode = node.left;
    const rightNode = node.right;
    if (isFloatKind(leftType) !== isFloatKind(rightType)) {
      return void 0;
    }
    if (isFloatKind(leftType) && isFloatKind(rightType)) {
      if (op === _typescript2.default.SyntaxKind.PercentToken) {
        return void 0;
      }
      if (leftType === "const-float" && rightType === "const-float") {
        return void 0;
      }
      if (op === _typescript2.default.SyntaxKind.SlashToken && !this.isNonZeroFloatLiteral(rightNode)) {
        return void 0;
      }
      return "float64";
    }
    if (!isIntKind(leftType) || !isIntKind(rightType)) {
      return void 0;
    }
    if (leftType === "int64" && rightType === "int" || leftType === "int" && rightType === "int64") {
      return void 0;
    }
    if (leftType === "const-int" && rightType === "const-int") {
      const value = this.goConstantIntValue(node);
      if (value === void 0 || Math.abs(value) > Number.MAX_SAFE_INTEGER) {
        return void 0;
      }
    }
    const operands = leftType === "int64" || rightType === "int64" ? "int64" : "int";
    if (op === _typescript2.default.SyntaxKind.PlusToken) {
      return operands;
    }
    if (operands === "int" && this.goInsideTypedDeclarationInitializer(node)) {
      return void 0;
    }
    if (op === _typescript2.default.SyntaxKind.PercentToken) {
      return this.isNonZeroIntegerLiteral(rightNode) ? operands : void 0;
    }
    if (op === _typescript2.default.SyntaxKind.SlashToken && !this.isNonZeroIntegerLiteral(rightNode)) {
      return void 0;
    }
    return operands;
  }
  // an operand that is itself a bare operator expression needs parens under a
  // tighter parent operator; a whole helper call is already delimited
  goNativeOperandText(node, printedText) {
    const text = printedText.trim();
    if (_optionalChain([node, 'optionalAccess', _534 => _534.kind]) !== _typescript2.default.SyntaxKind.BinaryExpression || text.startsWith("(")) {
      return text;
    }
    const open = text.indexOf("(");
    if (open > 0 && this.isWholePrintedCall(text, open)) {
      return text;
    }
    return this.goOperandStaticType(node, text) === "string" ? text : "(" + text + ")";
  }
  // `Add(a, b)` & co. become the Go operator when both printed operands already
  // hold a concrete Go type the helper would return unchanged; undefined keeps the
  // helper call (nil branches, `any` boxes, strings passed to Subtract, ...).
  goNativeArithmetic(node, leftText = void 0, rightText = void 0) {
    const op = node.operatorToken.kind;
    if (GO_ARITHMETIC_KINDS.indexOf(op) < 0) {
      return void 0;
    }
    leftText = _nullishCoalesce(leftText, () => ( this.printNode(node.left, 0)));
    rightText = _nullishCoalesce(rightText, () => ( this.printNode(node.right, 0)));
    if (op === _typescript2.default.SyntaxKind.PlusToken) {
      const concat = this.goNativeStringConcat(node, leftText, rightText);
      if (concat !== void 0) {
        return concat;
      }
    }
    const leftType = this.goOperandStaticType(node.left, leftText);
    const rightType = this.goOperandStaticType(node.right, rightText);
    if (leftType === void 0 || rightType === void 0) {
      return void 0;
    }
    if (leftType === "string" || rightType === "string") {
      if (leftType !== "string" || rightType !== "string" || op !== _typescript2.default.SyntaxKind.PlusToken) {
        return void 0;
      }
      return { "goType": "string", "text": this.goNativeBinaryText(node, "+", leftText, rightText) };
    }
    const goType = this.goNativeNumericResultType(op, leftType, rightType, node);
    if (goType === void 0) {
      return void 0;
    }
    return { goType, "text": this.goNativeBinaryText(node, this.SupportedKindNames[op], leftText, rightText) };
  }
  // the operator line gofmt prints for a natively emitted arithmetic expression: the
  // blanks follow go/printer's cutoff at the current depth, and a binary operand is
  // re-printed at the expression's own depth (a same-precedence left operand, or the
  // parentheses the printer wraps it in, which undo the one level the operand adds)
  goNativeBinaryText(node, symbol, leftText, rightText) {
    const operandText = (operand, printed) => {
      const isBinary = _optionalChain([operand, 'optionalAccess', _535 => _535.kind]) === _typescript2.default.SyntaxKind.BinaryExpression;
      const text = isBinary ? this.goWithExprDepth(this.goExprDepth, () => this.printNode(operand, 0)) : printed;
      const leaf = this.goDerefableStringOperand(operand) ? "*" + text.trim() : text;
      return this.goNativeOperandText(operand, leaf);
    };
    const left = operandText(node.left, leftText);
    const right = operandText(node.right, rightText);
    const separator = this.goBinarySeparator(symbol, right, node.left, node.right);
    return left + separator + symbol + separator + right;
  }
  // `x += y` prints `x = Add(x, y)`; the compound operator is equivalent while
  // both sides hold a concrete Go type that can never make the helper return nil
  goNativeCompoundAssignment(op, leftNode, leftText, rightNode, rightText) {
    const leftType = this.goOperandStaticType(leftNode, leftText);
    const rightType = this.goOperandStaticType(rightNode, rightText);
    if (leftType === void 0 || rightType === void 0) {
      return void 0;
    }
    const isAdd = op === _typescript2.default.SyntaxKind.PlusEqualsToken;
    const isSubtract = op === _typescript2.default.SyntaxKind.MinusEqualsToken;
    if (!isAdd && !isSubtract) {
      return void 0;
    }
    if (leftType === "string" && isAdd && rightType === "string") {
      return `${leftText.trim()} += ${rightText.trim()}`;
    }
    if (leftType !== "int64") {
      return void 0;
    }
    if (isAdd && (rightType === "int64" || rightType === "const-int")) {
      return `${leftText.trim()} += ${rightText.trim()}`;
    }
    if (isSubtract && (rightType === "int64" || rightType === "const-int")) {
      return `${leftText.trim()} -= ${rightText.trim()}`;
    }
    return void 0;
  }
  goEnclosingFunction(node) {
    let current = _optionalChain([node, 'optionalAccess', _536 => _536.parent]);
    while (current) {
      switch (current.kind) {
        case _typescript2.default.SyntaxKind.MethodDeclaration:
        case _typescript2.default.SyntaxKind.FunctionDeclaration:
        case _typescript2.default.SyntaxKind.FunctionExpression:
        case _typescript2.default.SyntaxKind.ArrowFunction:
        case _typescript2.default.SyntaxKind.Constructor:
        case _typescript2.default.SyntaxKind.SourceFile:
          return current;
      }
      current = current.parent;
    }
    return void 0;
  }
  // a transpiled parameter or local can literally be named `string`, which would
  // turn `var x string = ...` into a reference to that value instead of the type
  goTypeNameIsShadowed(scope, goType) {
    const names = _nullishCoalesce(goType.match(/[A-Za-z_]\w*/g), () => ( []));
    const relevant = names.filter((n) => GO_TYPE_NAMES.indexOf(n) >= 0);
    if (relevant.length === 0 || scope === void 0) {
      return false;
    }
    return this.hasNodeWhere(scope, (n) => {
      const isBinding = n.kind === _typescript2.default.SyntaxKind.Parameter || n.kind === _typescript2.default.SyntaxKind.VariableDeclaration;
      if (isBinding && _optionalChain([n, 'access', _537 => _537.name, 'optionalAccess', _538 => _538.kind]) === _typescript2.default.SyntaxKind.Identifier) {
        if (relevant.indexOf(n.name.escapedText) >= 0) {
          return true;
        }
      }
      return false;
    });
  }
  // the shape `x.push(v)` that both the native emission and the declaration's safety
  // scan accept: the printed `x = append(x, v)` is a statement, so a value position or
  // a multi-argument/spread call still needs the helper
  goIsNativeAppendShape(receiverNode, pushNode) {
    if (_optionalChain([receiverNode, 'optionalAccess', _539 => _539.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    if (_optionalChain([pushNode, 'optionalAccess', _540 => _540.parent, 'optionalAccess', _541 => _541.kind]) !== _typescript2.default.SyntaxKind.ExpressionStatement) {
      return false;
    }
    if (pushNode.questionDotToken !== void 0 || _optionalChain([pushNode, 'access', _542 => _542.expression, 'optionalAccess', _543 => _543.questionDotToken]) !== void 0) {
      return false;
    }
    const access = pushNode.expression;
    if (_optionalChain([access, 'optionalAccess', _544 => _544.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || access.expression !== receiverNode || _optionalChain([access, 'access', _545 => _545.name, 'optionalAccess', _546 => _546.escapedText]) !== "push") {
      return false;
    }
    const args = pushNode.arguments;
    return _optionalChain([args, 'optionalAccess', _547 => _547.length]) === 1 && args[0].kind !== _typescript2.default.SyntaxKind.SpreadElement;
  }
  // `x.push(v)` on a local declared `[]any` prints `x = append(x, v)`; undefined keeps
  // AppendToArray(&x, v). An `any` box keeps the helper: `&x` is a *any there, while a declared
  // []any local would pass a *[]any the helper's parameter does not accept.
  goNativeAppendReceiver(pushNode) {
    const receiver = _optionalChain([pushNode, 'optionalAccess', _548 => _548.expression, 'optionalAccess', _549 => _549.expression]);
    if (!this.goIsNativeAppendShape(receiver, pushNode)) {
      return void 0;
    }
    if (this.goDeclaredTypeOfIdentifier(receiver) !== "[]any") {
      return void 0;
    }
    return this.printNode(receiver, 0);
  }
  // reject the refinement when something downstream needs the local to stay `any`:
  // `x.push(v)` prints `AppendToArray(&x, v)` (a *T is not a *any) and a later
  // assignment of a value with another concrete type would stop compiling
  goLocalIsSafeToType(scope, declaration, varName, goType) {
    if (scope === void 0) {
      return false;
    }
    const safe = !this.hasNodeWhere(scope, (n) => {
      if (n.kind === _typescript2.default.SyntaxKind.Identifier && n.escapedText === varName && n !== declaration.name) {
        const parent = n.parent;
        if (_optionalChain([parent, 'optionalAccess', _550 => _550.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression && parent.expression === n && _optionalChain([parent, 'access', _551 => _551.name, 'optionalAccess', _552 => _552.escapedText]) === "push") {
          if (goType !== "[]any" || !this.goIsNativeAppendShape(n, parent.parent)) {
            return true;
          }
        }
        if (_optionalChain([parent, 'optionalAccess', _553 => _553.kind]) === _typescript2.default.SyntaxKind.VariableDeclaration && parent.name === n) {
          return;
        }
        if (_optionalChain([parent, 'optionalAccess', _554 => _554.kind]) === _typescript2.default.SyntaxKind.PostfixUnaryExpression || _optionalChain([parent, 'optionalAccess', _555 => _555.kind]) === _typescript2.default.SyntaxKind.PrefixUnaryExpression) {
          const op = parent.operator;
          if (op === _typescript2.default.SyntaxKind.PlusPlusToken || op === _typescript2.default.SyntaxKind.MinusMinusToken) {
            return true;
          }
        }
        if (_optionalChain([parent, 'optionalAccess', _556 => _556.kind]) === _typescript2.default.SyntaxKind.SpreadElement) {
          return true;
        }
        if (_optionalChain([parent, 'optionalAccess', _557 => _557.kind]) === _typescript2.default.SyntaxKind.ArrayLiteralExpression && _optionalChain([parent, 'access', _558 => _558.parent, 'optionalAccess', _559 => _559.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && parent.parent.left === parent && parent.parent.operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken && !(goType === "map[string]any" && this.goGetArgTupleWriteIsDict(declaration, parent.parent.right, parent.elements.indexOf(n)))) {
          return true;
        }
        if (_optionalChain([parent, 'optionalAccess', _560 => _560.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && parent.left === n) {
          const op = parent.operatorToken.kind;
          if (op === _typescript2.default.SyntaxKind.EqualsToken) {
            if (this.goTypeOfInitializer(parent.right, this.printNode(parent.right, 0)) !== goType && !(declaration.kind === _typescript2.default.SyntaxKind.VariableDeclaration && this.goPointerWriteConversion(parent.right, goType) !== void 0)) {
              return true;
            }
          } else if (op >= _typescript2.default.SyntaxKind.FirstCompoundAssignment && op <= _typescript2.default.SyntaxKind.LastCompoundAssignment) {
            return true;
          }
        }
      }
      return false;
    });
    return safe;
  }
  // How a write of another shape reaches a pointer-typed local: 'nil' (undefined/null prints nil),
  // 'wrap' (a Go string becomes SafeStringPtr(v), never nil), or undefined (not convertible).
  // Shared by goLocalIsSafeToType (admission) and goPointerWriteText (emission).
  goPointerWriteConversion(right, goType) {
    while (_optionalChain([right, 'optionalAccess', _561 => _561.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      right = right.expression;
    }
    if (!["*string", "*int64", "*float64"].includes(goType) || right === void 0) {
      return void 0;
    }
    if (right.kind === _typescript2.default.SyntaxKind.NullKeyword || right.kind === _typescript2.default.SyntaxKind.UndefinedKeyword || right.kind === _typescript2.default.SyntaxKind.Identifier && right.escapedText === "undefined") {
      return "nil";
    }
    if (goType === "*string" && this.goTypeOfInitializer(right, this.printNode(right, 0)) === "string") {
      return "wrap";
    }
    return void 0;
  }
  // `x = "limit"` on a *string local prints `x = SafeStringPtr("limit")`; undefined otherwise
  goPointerWriteText(node, identation) {
    const { left, right } = node;
    if (_optionalChain([left, 'optionalAccess', _562 => _562.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const decl = _optionalChain([this, 'access', _563 => _563.checkerOrUndefined, 'call', _564 => _564(), 'optionalAccess', _565 => _565.getSymbolAtLocation, 'call', _566 => _566(left), 'optionalAccess', _567 => _567.valueDeclaration]);
    if (_optionalChain([decl, 'optionalAccess', _568 => _568.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration) {
      return void 0;
    }
    const goType = this.goDeclaredTypeOfIdentifier(left);
    if (goType === void 0 || this.goPointerWriteConversion(right, goType) !== "wrap") {
      return void 0;
    }
    const value = this.goWithExprDepth(this.goExprDepth + 1, () => this.printNode(right, identation)).trim();
    return `${this.printNode(left, 0)} = SafeStringPtr(${value})`;
  }
  // the container/key argument nodes of a whole `this.SafeDict(container, key)` call, or undefined
  // for another shape. A third argument is droppable only when it is the empty map literal
  // (`safeDict(x, k, {})`), which no whitelisted read could observe.
  goSafeDictLocalArgs(initializer) {
    if (_optionalChain([initializer, 'optionalAccess', _569 => _569.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return void 0;
    }
    const callee = initializer.expression;
    if (_optionalChain([callee, 'optionalAccess', _570 => _570.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([callee, 'access', _571 => _571.name, 'optionalAccess', _572 => _572.escapedText]) !== "safeDict") {
      return void 0;
    }
    if (_optionalChain([callee, 'access', _573 => _573.expression, 'optionalAccess', _574 => _574.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return void 0;
    }
    const args = initializer.arguments;
    if (args.length === 3) {
      const fallback = args[2];
      if (_optionalChain([fallback, 'optionalAccess', _575 => _575.kind]) !== _typescript2.default.SyntaxKind.ObjectLiteralExpression || fallback.properties.length !== 0) {
        return void 0;
      }
    } else if (args.length !== 2) {
      return void 0;
    }
    return { container: args[0], key: args[1] };
  }
  // one later use of a dict local: it must read the value as a dictionary, never hand the
  // box out. Only the receiver position of the deref-aware read helpers, the `this.Safe*`
  // accessors and a plain `x[k]` element read qualify.
  goSafeDictUseReadsTheMap(node) {
    const parent = node.parent;
    if (parent === void 0) {
      return false;
    }
    switch (parent.kind) {
      case _typescript2.default.SyntaxKind.ElementAccessExpression: {
        if (parent.expression !== node) {
          return false;
        }
        const grandparent = parent.parent;
        if (_optionalChain([grandparent, 'optionalAccess', _576 => _576.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && grandparent.left === parent) {
          return false;
        }
        if (_optionalChain([grandparent, 'optionalAccess', _577 => _577.kind]) === _typescript2.default.SyntaxKind.PostfixUnaryExpression || _optionalChain([grandparent, 'optionalAccess', _578 => _578.kind]) === _typescript2.default.SyntaxKind.PrefixUnaryExpression) {
          return false;
        }
        if (_optionalChain([grandparent, 'optionalAccess', _579 => _579.kind]) === _typescript2.default.SyntaxKind.DeleteExpression) {
          return false;
        }
        return true;
      }
      case _typescript2.default.SyntaxKind.BinaryExpression: {
        return _optionalChain([parent, 'access', _580 => _580.operatorToken, 'optionalAccess', _581 => _581.kind]) === _typescript2.default.SyntaxKind.InKeyword && parent.right === node;
      }
      case _typescript2.default.SyntaxKind.CallExpression: {
        if (parent.expression === node) {
          return false;
        }
        if (parent.arguments.indexOf(node) !== 0) {
          return false;
        }
        const callee = this.goPrintedCallee(this.printNode(parent, 0));
        if (callee === void 0) {
          return false;
        }
        if (GO_SAFE_DICT_READ_HELPERS.indexOf(callee) >= 0) {
          return true;
        }
        return /^(?:this\.)?Safe[A-Z]/.test(callee) || callee === "this.IsDictionary";
      }
      default:
        return false;
    }
  }
  goSafeDictLocalUnbox(declaration) {
    if (_optionalChain([declaration, 'optionalAccess', _582 => _582.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration || _optionalChain([declaration, 'access', _583 => _583.name, 'optionalAccess', _584 => _584.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    if (_optionalChain([declaration, 'access', _585 => _585.parent, 'optionalAccess', _586 => _586.parent, 'optionalAccess', _587 => _587.kind]) !== _typescript2.default.SyntaxKind.FirstStatement) {
      return void 0;
    }
    if (this.goSafeDictLocalUnboxCache.has(declaration)) {
      return this.goSafeDictLocalUnboxCache.get(declaration);
    }
    this.goSafeDictLocalUnboxCache.set(declaration, void 0);
    let result;
    try {
      result = this.goSafeDictLocalUnboxUncached(declaration);
    } finally {
      this.goSafeDictLocalUnboxCache.set(declaration, result);
    }
    return result;
  }
  // every read of `declaration`'s name inside its function must keep the boxed value's meaning for
  // the local to take a named Go type: a rebinding mixes two values, and a use `readsTheValue`
  // rejects re-boxes the local. `skipUse` drops nodes that are not references at all.
  goDeclaredLocalTypeIfSafe(declaration, goType, readsTheValue, skipUse) {
    const sourceName = declaration.name.escapedText;
    const scope = this.goEnclosingFunction(declaration);
    if (scope === void 0) {
      return void 0;
    }
    const unsafe = this.hasNodeWhere(scope, (n) => {
      if ((n.kind === _typescript2.default.SyntaxKind.VariableDeclaration || n.kind === _typescript2.default.SyntaxKind.Parameter) && n !== declaration && _optionalChain([n, 'access', _588 => _588.name, 'optionalAccess', _589 => _589.kind]) === _typescript2.default.SyntaxKind.Identifier && n.name.escapedText === sourceName) {
        return true;
      }
      if (n.kind !== _typescript2.default.SyntaxKind.Identifier || n.escapedText !== sourceName || n === declaration.name) {
        return false;
      }
      return skipUse !== void 0 && skipUse(n) ? false : !readsTheValue(n);
    });
    if (unsafe || this.goTypeNameIsShadowed(scope, goType)) {
      return void 0;
    }
    return goType;
  }
  goSafeDictLocalUnboxUncached(declaration) {
    if (this.goSafeDictLocalArgs(declaration.initializer) === void 0) {
      return void 0;
    }
    return this.goDeclaredLocalTypeIfSafe(declaration, GO_SAFE_DICT_LOCAL_TYPE, (n) => this.goSafeDictUseReadsTheMap(n));
  }
  // the initializer a typed dict local is declared with: the accessor call is replaced by the
  // typed reader, which reads the same member (and converts a sync.Map) but names the result
  goSafeDictUnboxValue(declaration, identation) {
    if (this.goSafeDictLocalUnbox(declaration) !== GO_SAFE_DICT_LOCAL_TYPE) {
      return void 0;
    }
    const args = this.goSafeDictLocalArgs(declaration.initializer);
    const container = this.printNode(args.container, identation);
    const key = this.printNode(args.key, 0);
    return `SafeMapTyped(${container}, ${key})`;
  }
  // the TypeScript return type proves the boxed value is the market dictionary: the checker reports
  // the `MarketInterface` interface for `this.Market(...)` / `this.SafeMarket(...)` (the `Market`
  // alias is the same interface unioned with undefined). Read from the checker, never a printed name.
  goMarketCallReturnsDict(initializer) {
    if (_optionalChain([initializer, 'optionalAccess', _590 => _590.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return false;
    }
    const callee = initializer.expression;
    if (_optionalChain([callee, 'optionalAccess', _591 => _591.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      return false;
    }
    const name = _optionalChain([callee, 'access', _592 => _592.name, 'optionalAccess', _593 => _593.escapedText]);
    if (name === void 0) {
      return false;
    }
    const receiver = callee.expression;
    const onThis = _optionalChain([receiver, 'optionalAccess', _594 => _594.kind]) === _typescript2.default.SyntaxKind.ThisKeyword;
    const onDerived = _optionalChain([receiver, 'optionalAccess', _595 => _595.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression && _optionalChain([receiver, 'access', _596 => _596.expression, 'optionalAccess', _597 => _597.kind]) === _typescript2.default.SyntaxKind.ThisKeyword && _optionalChain([receiver, 'access', _598 => _598.name, 'optionalAccess', _599 => _599.escapedText]) === "DerivedExchange";
    if (!onThis && !onDerived) {
      return false;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const type = checker.getTypeAtLocation(initializer);
    if (type === void 0) {
      return false;
    }
    const isMarketInterface = (t) => {
      const names = [_nullishCoalesce(_optionalChain([t, 'optionalAccess', _600 => _600.symbol, 'optionalAccess', _601 => _601.getName, 'optionalCall', _602 => _602()]), () => ( _optionalChain([t, 'optionalAccess', _603 => _603.symbol, 'optionalAccess', _604 => _604.escapedName]))), _optionalChain([t, 'optionalAccess', _605 => _605.aliasSymbol, 'optionalAccess', _606 => _606.getName, 'optionalCall', _607 => _607()])];
      return names.indexOf("MarketInterface") >= 0 || names.indexOf("CurrencyInterface") >= 0;
    };
    if (isMarketInterface(type)) {
      return true;
    }
    if (typeof type.isUnion === "function" && type.isUnion()) {
      return type.types.some((t) => isMarketInterface(t));
    }
    return false;
  }
  goMarketLocalUnbox(declaration) {
    if (_optionalChain([declaration, 'optionalAccess', _608 => _608.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration || _optionalChain([declaration, 'access', _609 => _609.name, 'optionalAccess', _610 => _610.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    if (_optionalChain([declaration, 'access', _611 => _611.parent, 'optionalAccess', _612 => _612.parent, 'optionalAccess', _613 => _613.kind]) !== _typescript2.default.SyntaxKind.FirstStatement) {
      return void 0;
    }
    if (this.goMarketLocalUnboxCache.has(declaration)) {
      return this.goMarketLocalUnboxCache.get(declaration);
    }
    this.goMarketLocalUnboxCache.set(declaration, void 0);
    let result;
    try {
      result = this.goMarketLocalUnboxUncached(declaration);
    } finally {
      this.goMarketLocalUnboxCache.set(declaration, result);
    }
    return result;
  }
  // true when this identifier resolves to the declaration being typed (a property name or a
  // binding of the same name in another scope is not a use of the local). Without checker
  // information the name match stands.
  goIdentifierRefersToDeclaration(node, declaration) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return true;
    }
    const symbol = checker.getSymbolAtLocation(node);
    if (symbol === void 0) {
      return true;
    }
    const valueDeclaration = _nullishCoalesce(symbol.valueDeclaration, () => ( _optionalChain([symbol, 'access', _614 => _614.declarations, 'optionalAccess', _615 => _615[0]])));
    return valueDeclaration === void 0 || valueDeclaration === declaration;
  }
  // one later use of the market local: the dictionary read shapes SafeDict proves (element read,
  // `in`, read-helper argument), plus — only when the accessor throws rather than answers absent —
  // a plain argument position, since Go re-boxes the map into the callee's `any` as the box did.
  goMarketUseReadsTheValue(node, throwingAccessor) {
    if (this.goSafeDictUseReadsTheMap(node)) {
      return true;
    }
    const parent = node.parent;
    if (_optionalChain([parent, 'optionalAccess', _616 => _616.kind]) === _typescript2.default.SyntaxKind.ElementAccessExpression && parent.expression === node) {
      let operand = parent;
      let above = parent.parent;
      while (_optionalChain([above, 'optionalAccess', _617 => _617.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
        operand = above;
        above = above.parent;
      }
      if (_optionalChain([above, 'optionalAccess', _618 => _618.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && (above.left === operand || above.right === operand) && GO_MARKET_READ_COMPARISON_OPERATORS.indexOf(_optionalChain([above, 'access', _619 => _619.operatorToken, 'optionalAccess', _620 => _620.kind])) >= 0) {
        return true;
      }
    }
    if (!throwingAccessor) {
      return false;
    }
    return _optionalChain([parent, 'optionalAccess', _621 => _621.kind]) === _typescript2.default.SyntaxKind.CallExpression && parent.expression !== node && parent.arguments.indexOf(node) >= 0;
  }
  goMarketLocalUnboxUncached(declaration) {
    if (!this.goMarketCallReturnsDict(declaration.initializer)) {
      return void 0;
    }
    const accessorName = _optionalChain([declaration, 'access', _622 => _622.initializer, 'access', _623 => _623.expression, 'optionalAccess', _624 => _624.name, 'optionalAccess', _625 => _625.escapedText]);
    const throwingAccessor = accessorName === "market" || accessorName === "currency";
    return this.goDeclaredLocalTypeIfSafe(
      declaration,
      GO_MARKET_LOCAL_TYPE,
      (n) => this.goMarketUseReadsTheValue(n, throwingAccessor),
      (n) => _optionalChain([n, 'access', _626 => _626.parent, 'optionalAccess', _627 => _627.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression && n.parent.name === n || !this.goIdentifierRefersToDeclaration(n, declaration)
    );
  }
  // the initializer a typed market local is declared with: the same call, its boxed result
  // converted to the map the checker proved (nil when the call answered a non-map)
  goMarketUnboxValue(declaration, parsedValue) {
    if (this.goMarketLocalUnbox(declaration) !== GO_MARKET_LOCAL_TYPE) {
      return void 0;
    }
    return `MapTyped(${parsedValue.trimStart()})`;
  }
  // the variable declaration an identifier binds to, through the checker (undefined for every
  // other shape) — the market rule resolves its own locals this way, never by printed name
  goDeclarationOfIdentifier(node) {
    try {
      const symbol = this.getChecker().getSymbolAtLocation(node);
      const declaration = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _628 => _628.valueDeclaration]), () => ( _optionalChain([symbol, 'optionalAccess', _629 => _629.declarations, 'optionalAccess', _630 => _630[0]])));
      if (_optionalChain([declaration, 'optionalAccess', _631 => _631.kind]) === _typescript2.default.SyntaxKind.VariableDeclaration) {
        return declaration;
      }
    } catch (e) {
      return void 0;
    }
    return void 0;
  }
  // a compared element of a market/currency local keeps the GetValue helper: its deref is what
  // the boxed local's read used to apply, and a parseMarket may have stored a `*bool`/`*string`
  // under the key. Only the operands the use scan admits this way are affected; every other
  goMarketComparisonElementRead(node) {
    const base = _optionalChain([node, 'optionalAccess', _632 => _632.expression]);
    if (_optionalChain([base, 'optionalAccess', _633 => _633.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const declaration = this.goDeclarationOfIdentifier(base);
    if (declaration === void 0 || this.goMarketLocalUnbox(declaration) !== GO_MARKET_LOCAL_TYPE) {
      return false;
    }
    let operand = node;
    let above = node.parent;
    while (_optionalChain([above, 'optionalAccess', _634 => _634.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      operand = above;
      above = above.parent;
    }
    return _optionalChain([above, 'optionalAccess', _635 => _635.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && (above.left === operand || above.right === operand) && GO_MARKET_READ_COMPARISON_OPERATORS.indexOf(_optionalChain([above, 'access', _636 => _636.operatorToken, 'optionalAccess', _637 => _637.kind])) >= 0;
  }
  // the container/key argument nodes of a whole `this.SafeList(container, key)` call, or
  // undefined when the initializer is another shape. A third argument is only droppable when
  // it is the empty array literal the TS call sites pass (`safeList(x, k, [])`): the typed
  goSafeListLocalArgs(initializer) {
    if (_optionalChain([initializer, 'optionalAccess', _638 => _638.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return void 0;
    }
    const callee = initializer.expression;
    if (_optionalChain([callee, 'optionalAccess', _639 => _639.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([callee, 'access', _640 => _640.name, 'optionalAccess', _641 => _641.escapedText]) !== "safeList") {
      return void 0;
    }
    if (_optionalChain([callee, 'access', _642 => _642.expression, 'optionalAccess', _643 => _643.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return void 0;
    }
    const args = initializer.arguments;
    if (args.length === 3) {
      const fallback = args[2];
      if (_optionalChain([fallback, 'optionalAccess', _644 => _644.kind]) !== _typescript2.default.SyntaxKind.ArrayLiteralExpression || fallback.elements.length !== 0) {
        return void 0;
      }
    } else if (args.length !== 2) {
      return void 0;
    }
    return { container: args[0], key: args[1] };
  }
  // one later use of a list local: it must read the value as a list, never hand the box out.
  // A length read, an element read and the statement-shaped push all answer for a []any what
  // the accessors answered for the box; every other use keeps the box.
  goSafeListUseReadsTheList(node) {
    const parent = node.parent;
    if (parent === void 0) {
      return false;
    }
    switch (parent.kind) {
      case _typescript2.default.SyntaxKind.PropertyAccessExpression: {
        if (parent.expression !== node) {
          return false;
        }
        if (_optionalChain([parent, 'access', _645 => _645.name, 'optionalAccess', _646 => _646.escapedText]) === "length") {
          return true;
        }
        return _optionalChain([parent, 'access', _647 => _647.name, 'optionalAccess', _648 => _648.escapedText]) === "push" && this.goIsNativeAppendShape(node, parent.parent);
      }
      case _typescript2.default.SyntaxKind.ElementAccessExpression: {
        if (parent.expression !== node) {
          return false;
        }
        return !this.isGoElementAccessAssignmentTarget(parent);
      }
      case _typescript2.default.SyntaxKind.CallExpression: {
        if (parent.expression === node) {
          return false;
        }
        if (parent.arguments.indexOf(node) !== 0) {
          return false;
        }
        const callee = this.goPrintedCallee(this.printNode(parent, 0));
        return callee === "GetValue" || callee === "GetArrayLength";
      }
      default:
        return false;
    }
  }
  goSafeListLocalUnbox(declaration) {
    if (_optionalChain([declaration, 'optionalAccess', _649 => _649.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration || _optionalChain([declaration, 'access', _650 => _650.name, 'optionalAccess', _651 => _651.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    if (_optionalChain([declaration, 'access', _652 => _652.parent, 'optionalAccess', _653 => _653.parent, 'optionalAccess', _654 => _654.kind]) !== _typescript2.default.SyntaxKind.FirstStatement) {
      return void 0;
    }
    if (this.goSafeListLocalUnboxCache.has(declaration)) {
      return this.goSafeListLocalUnboxCache.get(declaration);
    }
    this.goSafeListLocalUnboxCache.set(declaration, void 0);
    let result;
    try {
      result = this.goSafeListLocalUnboxUncached(declaration);
    } finally {
      this.goSafeListLocalUnboxCache.set(declaration, result);
    }
    return result;
  }
  goSafeListLocalUnboxUncached(declaration) {
    if (this.goSafeListLocalArgs(declaration.initializer) === void 0) {
      return void 0;
    }
    return this.goDeclaredLocalTypeIfSafe(declaration, GO_SAFE_LIST_LOCAL_TYPE, (n) => this.goSafeListUseReadsTheList(n));
  }
  // the initializer a typed list local is declared with: the accessor call is replaced by the
  // typed reader, which reads the same member but names the result
  goSafeListUnboxValue(declaration, identation) {
    if (this.goSafeListLocalUnbox(declaration) !== GO_SAFE_LIST_LOCAL_TYPE) {
      return void 0;
    }
    const args = this.goSafeListLocalArgs(declaration.initializer);
    const container = this.printNode(args.container, identation);
    const key = this.printNode(args.key, 0);
    return `SafeListTyped(${container}, ${key})`;
  }
  getGoLocalType(declaration, parsedValue) {
    const goType = this.goTypeOfInitializer(declaration.initializer, parsedValue);
    if (goType === void 0) {
      return _nullishCoalesce(_nullishCoalesce(_nullishCoalesce(this.goSafeDictLocalUnbox(declaration), () => ( this.goSafeListLocalUnbox(declaration))), () => ( this.goMarketLocalUnbox(declaration))), () => ( "any"));
    }
    const sourceName = _optionalChain([declaration, 'access', _655 => _655.name, 'optionalAccess', _656 => _656.escapedText]);
    if (sourceName === void 0) {
      return "any";
    }
    const scope = this.goEnclosingFunction(declaration);
    if (this.goTypeNameIsShadowed(scope, goType) || !this.goLocalIsSafeToType(scope, declaration, sourceName, goType)) {
      return "any";
    }
    return goType;
  }
  // Typed async receive: an extension that knows the core's channel element type replaces
  // `x := (<-this.FooAsync(..))` + `PanicOnError(x)` with a typed declaration that runs PanicOnError
  // first (same frame and message). The default returns undefined and changes nothing.
  goAwaitReceiveUnbox(awaitNode, printedInitializer) {
    return void 0;
  }
  printVariableDeclarationList(node, identation) {
    const declaration = node.declarations[0];
    if (_optionalChain([declaration, 'optionalAccess', _657 => _657.name, 'access', _658 => _658.kind]) === _typescript2.default.SyntaxKind.ArrayBindingPattern) {
      const arrayBindingPattern = declaration.name;
      const arrayBindingPatternElements = arrayBindingPattern.elements;
      const parsedArrayBindingElements = arrayBindingPatternElements.map((e) => this.printNode(e.name, 0));
      const syntheticName = parsedArrayBindingElements.join("") + "Variable";
      let arrayBindingStatement = `${this.getIden(identation)}${syntheticName} := ${this.printNode(declaration.initializer, 0)}
`;
      parsedArrayBindingElements.forEach((e, index) => {
        const statement = this.getIden(identation) + `${e} := GetValue(${syntheticName}, ${index})`;
        if (index < parsedArrayBindingElements.length - 1) {
          arrayBindingStatement += statement + "\n";
        } else {
          arrayBindingStatement += statement;
        }
      });
      return arrayBindingStatement;
    }
    if (_optionalChain([declaration, 'optionalAccess', _659 => _659.initializer, 'optionalAccess', _660 => _660.kind]) === _typescript2.default.SyntaxKind.AwaitExpression) {
      const parsedName = this.printNode(declaration.name, 0);
      const parsedInitializer = this.printNode(declaration.initializer, identation);
      const awaitUnbox = this.goAwaitReceiveUnbox(declaration.initializer, parsedInitializer);
      if (awaitUnbox !== void 0) {
        return `
${this.getIden(identation)}var ${parsedName} ${awaitUnbox.goType} = ${awaitUnbox.wrap(parsedInitializer)}`;
      }
      return `
${this.getIden(identation)}${parsedName} := ${parsedInitializer}
${this.getIden(identation)}PanicOnError(${parsedName})`;
    }
    const isNew = declaration.initializer && declaration.initializer.kind === _typescript2.default.SyntaxKind.NewExpression;
    const parsedValue = declaration.initializer ? this.printNode(declaration.initializer, identation) : this.NULL_TOKEN;
    if (parsedValue === this.UNDEFINED_TOKEN) {
      return this.getIden(identation) + "var " + this.printNode(declaration.name) + " any = " + parsedValue;
    }
    if (_optionalChain([node, 'optionalAccess', _661 => _661.parent, 'optionalAccess', _662 => _662.kind]) === _typescript2.default.SyntaxKind.FirstStatement) {
      if (isNew) {
        return this.getIden(identation) + this.printNode(declaration.name) + " := " + parsedValue;
      }
      const varName = this.printNode(declaration.name);
      const declaredType = this.getGoLocalType(declaration, parsedValue);
      const unboxValue = _nullishCoalesce(this.goMarketUnboxValue(declaration, parsedValue), () => ( (declaredType === GO_SAFE_DICT_LOCAL_TYPE ? this.goSafeDictUnboxValue(declaration, identation) : declaredType === GO_SAFE_LIST_LOCAL_TYPE ? this.goSafeListUnboxValue(declaration, identation) : void 0)));
      const declaredValue = unboxValue !== void 0 ? unboxValue : parsedValue.trimStart();
      const stm = this.getIden(identation) + "var " + varName + " " + declaredType + " = " + declaredValue;
      if (parsedValue.startsWith("<-this.callInternal(")) {
        return `
${stm}
${this.getIden(identation)}PanicOnError(${varName})`;
      }
      return stm;
    }
    return this.getIden(identation) + this.printNode(declaration.name) + " := " + parsedValue.trim();
  }
  // printObjectLiteralExpression(node, identation) {
  //     const objectCreation = 'make(map[string]any) {';
  //     let formattedObjectBody = '{}';
  //     if (node.properties?.length > 0) {
  //         const objectBody = this.printObjectLiteralBody(node, identation);
  //         formattedObjectBody = objectBody ? "\n" + objectBody + "\n" + this.getIden(identation) : objectBody;
  //     }
  //     // return  this.OBJECT_OPENING + formattedObjectBody + this.OBJECT_CLOSING;
  //     return objectCreation + formattedObjectBody;
  // }
  // printObjectLiteralBody(node, identation) {
  //     let objectName = node.parent?.name?.escapedText;
  //     if (objectName === undefined) {
  //         objectName = "object";
  //     }
  //     const body =  node.properties.map((p) => `${this.getIden(identation)}${objectName}["${node.properties[0].name.text}"] = ${p.initializer.text}` ).join("\n");
  //     return body;
  // }
  // F09 — gofmt aligns the key/value columns inside multi-line composite literals.
  //
  // go/printer's exprList prints a single-line `key: value` entry as `key:` + vtab, so
  // every consecutive single-line entry of a literal body lands in one text/tabwriter
  // column block: the value starts after the widest key cell of that block (`"key":`,
  // the key plus its colon) and one space of padding. An entry whose value spans lines
  // carries no vtab cell, so it ends the block on both sides, exactly like a blank line
  // does. exprList also writes a formfeed — turned into a plain newline by the trimmer,
  // so it never shows up in the output — before an entry that opens a new alignment
  // section: that happens when the entry or its predecessor does not fit on a single
  // line, and when the key size ratio against the geometric mean of the previous key
  // sizes of the section reaches r = 2.5 (or drops to 1/r) while at least one of the
  // two keys is larger than smallSize = 40 bytes; keys of at most 40 bytes always keep
  // the section aligned. A trailing comment is one more tabwriter cell, so comments
  // line up after the widest `value,` cell of the run of consecutive commented entries.
  printObjectLiteralBody(node, identation) {
    const previousLevel = this.goStatementLevel;
    this.goStatementLevel = identation + 1;
    try {
      const entries = node.properties.map((p) => this.goWithExprDepth(1, () => this.printNode(p, identation + 1)));
      return this.alignGoCompositeEntries(entries).join("\n");
    } finally {
      this.goStatementLevel = previousLevel;
    }
  }
  // Applies the gofmt column alignment to already-printed `key: value` entries (the
  // entries must not carry the separating comma). Reused by the hand-written composite
  // literals in ccxt's build/goTranspiler.ts, which do not go through this printer.
  alignGoCompositeEntries(entries) {
    const parsedEntries = entries.map((entry) => this.parseGoCompositeEntry(entry));
    const paddings = this.getGoCompositePaddings(parsedEntries);
    return entries.map((entry, index) => this.renderGoCompositeEntry(entry, parsedEntries[index], paddings[index]));
  }
  // `        "key": value, // comment` -> the pieces gofmt's tabwriter aligns.
  // Returns undefined for anything that is not a plain `key: value` entry (a spread, a
  // method, a computed key): the caller then leaves that entry alone and ends the block.
  parseGoCompositeEntry(entry) {
    const newlineIndex = entry.indexOf("\n");
    const firstLine = newlineIndex === -1 ? entry : entry.slice(0, newlineIndex);
    const keyMatch = /^([ \t]*)("(?:[^"\\]|\\.)*"): /.exec(firstLine);
    if (keyMatch === null) {
      return void 0;
    }
    const singleLine = newlineIndex === -1;
    const tail = singleLine ? entry.slice(keyMatch[0].length) : entry.slice(entry.lastIndexOf("\n") + 1);
    const commentIndex = this.findGoTrailingCommentStart(tail);
    return {
      "indent": keyMatch[1],
      "key": keyMatch[2],
      // nodeSize() measures the printed key; a value that spans lines gets size 0
      "size": singleLine ? this.getGoByteLength(keyMatch[2]) : 0,
      "singleLine": singleLine,
      "value": singleLine ? (commentIndex === -1 ? tail : tail.slice(0, commentIndex)).trimEnd() : void 0,
      "comment": commentIndex === -1 ? void 0 : tail.slice(commentIndex).trimEnd()
    };
  }
  // Index of the trailing comment of a printed line, or -1. `//` or `/*` inside a string
  // or a rune literal (e.g. a "https://…" value) is not a comment.
  findGoTrailingCommentStart(line) {
    let quote;
    for (let index = 0; index < line.length; ++index) {
      const character = line[index];
      if (quote !== void 0) {
        if (character === "\\" && quote !== "`") {
          index += 1;
        } else if (character === quote) {
          quote = void 0;
        }
        continue;
      }
      if (character === '"' || character === "`" || character === "'") {
        quote = character;
      } else if (character === "/" && (line[index + 1] === "/" || line[index + 1] === "*")) {
        return index;
      }
    }
    return -1;
  }
  // {key, comment} space counts per entry, i.e. the padding gofmt's tabwriter inserts.
  // Only entries that are part of a block get a padding; every other entry is rendered
  // as printed (gofmt leaves single-line and multi-line sections untouched).
  getGoCompositePaddings(parsedEntries) {
    const smallSize = 40;
    const ratio = 2.5;
    const paddings = parsedEntries.map(() => void 0);
    let block = [];
    let previousSize = 0;
    let size = 0;
    let lnSum = 0;
    let count = 0;
    const flushBlock = () => {
      if (block.length === 0) {
        return;
      }
      let keyWidth = 1;
      for (const index of block) {
        keyWidth = Math.max(keyWidth, this.getGoRuneLength(parsedEntries[index].key) + 2);
      }
      for (const index of block) {
        paddings[index] = { "key": keyWidth - this.getGoRuneLength(parsedEntries[index].key) - 1, "comment": 1 };
      }
      let run = [];
      const flushRun = () => {
        if (run.length === 0) {
          return;
        }
        let runWidth = 1;
        for (const index of run) {
          runWidth = Math.max(runWidth, this.getGoRuneLength(parsedEntries[index].value) + 2);
        }
        for (const index of run) {
          paddings[index].comment = runWidth - this.getGoRuneLength(parsedEntries[index].value) - 1;
        }
        run = [];
      };
      for (const index of block) {
        if (parsedEntries[index].comment !== void 0) {
          run.push(index);
        } else {
          flushRun();
        }
      }
      flushRun();
      block = [];
    };
    for (let index = 0; index < parsedEntries.length; ++index) {
      const entry = parsedEntries[index];
      previousSize = size;
      size = entry !== void 0 ? entry.size : 0;
      let sectionBreak = true;
      if (previousSize > 0 && size > 0) {
        if (count === 0 || previousSize <= smallSize && size <= smallSize) {
          sectionBreak = false;
        } else {
          const geomean = Math.exp(lnSum / count);
          const sizeRatio = size / geomean;
          sectionBreak = ratio * sizeRatio <= 1 || ratio <= sizeRatio;
        }
      }
      const alignable = entry !== void 0 && entry.singleLine;
      if (index > 0 && sectionBreak) {
        lnSum = 0;
        count = 0;
      }
      if (!alignable || sectionBreak) {
        flushBlock();
      }
      if (alignable) {
        block.push(index);
      }
      if (size > 0) {
        lnSum += Math.log(size);
        count += 1;
      }
    }
    flushBlock();
    return paddings;
  }
  renderGoCompositeEntry(entry, parsed, padding) {
    if (parsed === void 0 || !parsed.singleLine || padding === void 0) {
      return this.appendGoTrailingComma(entry);
    }
    const comment = parsed.comment === void 0 ? "" : " ".repeat(padding.comment) + parsed.comment;
    return parsed.indent + parsed.key + ":" + " ".repeat(padding.key) + parsed.value + "," + comment;
  }
  // gofmt prints the comma of an entry before its trailing comment (`value, // comment`),
  // the entry text carries the comment last, so move the comma in front of it
  appendGoTrailingComma(entry) {
    const newlineIndex = entry.lastIndexOf("\n");
    const lastLine = newlineIndex === -1 ? entry : entry.slice(newlineIndex + 1);
    const commentIndex = this.findGoTrailingCommentStart(lastLine);
    if (commentIndex === -1) {
      return entry + ",";
    }
    const offset = entry.length - lastLine.length + commentIndex;
    return entry.slice(0, offset).trimEnd() + ", " + entry.slice(offset).trimEnd();
  }
  // text/tabwriter sizes cells in runes, go/printer's nodeSize counts bytes
  getGoRuneLength(text) {
    return [...text].length;
  }
  getGoByteLength(text) {
    let length = 0;
    for (const character of text) {
      const codePoint = character.codePointAt(0);
      length += codePoint < 128 ? 1 : codePoint < 2048 ? 2 : codePoint < 65536 ? 3 : 4;
    }
    return length;
  }
  printConstructorDeclaration(node, identation) {
    const classNode = node.parent;
    const className = this.printNode(classNode.name, 0);
    const args = this.printMethodParameters(node);
    const constructorBody = this.printFunctionBody(node, identation);
    let superCallParams = "";
    let hasSuperCall = false;
    _optionalChain([node, 'access', _663 => _663.body, 'optionalAccess', _664 => _664.statements, 'access', _665 => _665.forEach, 'call', _666 => _666((statement) => {
      if (_typescript2.default.isExpressionStatement(statement)) {
        const expression = statement.expression;
        if (_typescript2.default.isCallExpression(expression)) {
          const expressionText = expression.expression.getText().trim();
          if (expressionText === "super") {
            hasSuperCall = true;
            superCallParams = expression.arguments.map((a) => {
              return this.printNode(a, identation).trim();
            }).join(", ");
          }
        }
      }
    })]);
    if (hasSuperCall) {
      return this.getIden(identation) + className + `(${args}) : ${this.SUPER_CALL_TOKEN}(${superCallParams})` + constructorBody;
    }
    return this.getIden(identation) + className + "(" + args + ")" + constructorBody;
  }
  printThisElementAccesssIfNeeded(node, identation) {
    const isAsync = true;
    const elementAccess = node.expression;
    if (_optionalChain([elementAccess, 'optionalAccess', _667 => _667.kind]) === _typescript2.default.SyntaxKind.ElementAccessExpression) {
      if (_optionalChain([elementAccess, 'optionalAccess', _668 => _668.expression, 'optionalAccess', _669 => _669.kind]) === _typescript2.default.SyntaxKind.ThisKeyword) {
        let parsedArg = _optionalChain([node, 'access', _670 => _670.arguments, 'optionalAccess', _671 => _671.length]) > 0 ? this.printNode(node.arguments[0], identation).trimStart() : "";
        const propName = this.printNode(elementAccess.argumentExpression, 0);
        const wrapperOpen = isAsync ? this.UKNOWN_PROP_ASYNC_WRAPPER_OPEN : this.UKNOWN_PROP_WRAPPER_OPEN;
        const wrapperClose = isAsync ? this.UNKOWN_PROP_ASYNC_WRAPPER_CLOSE : this.UNKOWN_PROP_WRAPPER_CLOSE;
        parsedArg = parsedArg ? ", " + parsedArg : "";
        return wrapperOpen + propName + parsedArg + wrapperClose;
      }
    }
    return;
  }
  printDynamicCall(node, identation) {
    const elementAccess = node.expression;
    if (_optionalChain([elementAccess, 'optionalAccess', _672 => _672.kind]) === _typescript2.default.SyntaxKind.ElementAccessExpression) {
      const argumentDepth = this.goExprDepth + (_optionalChain([node, 'access', _673 => _673.arguments, 'optionalAccess', _674 => _674.length]) > 0 ? 1 : 0);
      const parsedArg = _optionalChain([node, 'access', _675 => _675.arguments, 'optionalAccess', _676 => _676.length]) > 0 ? node.arguments.map((n) => this.goWithExprDepth(argumentDepth, () => this.printNode(n, identation).trimStart())).join(", ") : "";
      const propName = this.goWithExprDepth(argumentDepth, () => this.printNode(elementAccess.argumentExpression, 0));
      const argsArray = `${parsedArg}`;
      const open = this.DYNAMIC_CALL_OPEN;
      const statement = `${open}${propName}, ${argsArray})`;
      return statement;
    }
    return void 0;
  }
  printElementAccessExpressionExceptionIfAny(node) {
    const tsKind = _typescript2.default.SyntaxKind;
    if (node.expression.kind === tsKind.CallExpression) {
      const callExp = node.expression;
      const calleeText = callExp.expression.getText();
      if (calleeText.endsWith(".split") || calleeText.toLowerCase().includes("split")) {
        let splitCall = this.printNode(callExp, 0).trim();
        if (!splitCall.endsWith(")")) {
          splitCall += ")";
        }
        const idxArg = this.printNode(node.argumentExpression, 0);
        return `GetValue(${splitCall}, ${idxArg})`;
      }
    }
    return void 0;
  }
  printWrappedUnknownThisProperty(node, identation = 0) {
    const type = this.getChecker().getResolvedSignature(node);
    if (_optionalChain([type, 'optionalAccess', _677 => _677.declaration]) === void 0) {
      const argumentDepth = this.goExprDepth + (_optionalChain([node, 'access', _678 => _678.arguments, 'optionalAccess', _679 => _679.length]) > 0 ? 1 : 0);
      let parsedArguments = _optionalChain([node, 'access', _680 => _680.arguments, 'optionalAccess', _681 => _681.map, 'call', _682 => _682((a) => this.goWithExprDepth(argumentDepth, () => this.printNode(a, identation).trimStart())), 'access', _683 => _683.join, 'call', _684 => _684(", ")]);
      parsedArguments = parsedArguments ? parsedArguments : "";
      const propName = _optionalChain([node, 'access', _685 => _685.expression, 'optionalAccess', _686 => _686.name, 'access', _687 => _687.escapedText]);
      const argsArray = `${parsedArguments}`;
      const open = this.DYNAMIC_CALL_OPEN;
      const statement = `${open}"${propName}", ${argsArray})`;
      return statement;
    }
    return void 0;
  }
  transformMethodNameIfNeeded(name) {
    const res = this.unCamelCaseIfNeeded(name);
    return this.capitalize(res);
  }
  transformCallExpressionName(name, nameNode = void 0) {
    return this.applyAsyncSuffixToCallee(nameNode, this.capitalize(name));
  }
  transformPropertyAccessExpressionName(name, nameNode = void 0) {
    return this.applyAsyncSuffixToCallee(nameNode, this.capitalize(name));
  }
  printOutOfOrderCallExpressionIfAny(node, identation) {
    if (node.expression.kind === _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      const args = node.arguments;
      if (node.expression.expression.kind === _typescript2.default.SyntaxKind.ThisKeyword) {
        const methodName = this.printNode(node.expression.name, 0);
        if (this.wrapThisCalls || this.wrapCallMethods.includes(methodName)) {
          let argsParsed = "";
          if (args.length > 0) {
            argsParsed = args.map((a) => this.printNode(a, 0)).join(", ");
            return `<-this.callInternal("${methodName}", ${argsParsed})`;
          }
          return `<-this.callInternal("${methodName}")`;
        }
      }
      const expressionText = node.expression.getText().trim();
      if (args.length === 1) {
        const parsedArg = this.printNode(args[0], 0);
        switch (expressionText) {
          case "Math.abs":
            return `mathAbs(${parsedArg})`;
        }
      } else if (args.length === 2) {
        const parsedArg1 = this.printNode(args[0], 0);
        const parsedArg2 = this.printNode(args[1], 0);
        switch (expressionText) {
          case "Math.min":
            return `mathMin(${parsedArg1}, ${parsedArg2})`;
          case "Math.max":
            return `mathMax(${parsedArg1}, ${parsedArg2})`;
          case "Math.pow":
            return `MathPow(${parsedArg1}, ${parsedArg2})`;
        }
      }
      const leftSide = _optionalChain([node, 'access', _688 => _688.expression, 'optionalAccess', _689 => _689.expression]);
      const leftSideText = leftSide ? this.printNode(leftSide, 0) : void 0;
      if (leftSideText === this.THIS_TOKEN || leftSide.getFullText().indexOf("(this as any)") > -1) {
        const res = this.printWrappedUnknownThisProperty(node, identation);
        if (res) {
          return res;
        }
      }
    }
    if (node.expression.kind === _typescript2.default.SyntaxKind.ElementAccessExpression) {
      return this.printDynamicCall(node, identation);
    }
    return void 0;
  }
  handleTypeOfInsideBinaryExpression(node, identation) {
    const left = node.left;
    const right = node.right.text;
    const op = node.operatorToken.kind;
    const expression = left.expression;
    const isDifferentOperator = op === _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsToken;
    const notOperator = isDifferentOperator ? this.NOT_TOKEN : "";
    const target = this.printNode(expression, 0);
    switch (right) {
      case "string":
        return notOperator + `IsString(${target})`;
      case "number":
        return notOperator + `IsNumber(${target})`;
      case "boolean":
        return notOperator + `IsBool(${target})`;
      case "object":
        return notOperator + `IsObject(${target})`;
      case "function":
        return notOperator + `IsFunction(${target})`;
    }
    return void 0;
  }
  printCustomBinaryExpressionIfAny(node, identation) {
    const left = node.left;
    const right = node.right;
    const op = node.operatorToken.kind;
    if (op === _typescript2.default.SyntaxKind.EqualsToken && left.kind === _typescript2.default.SyntaxKind.ArrayLiteralExpression) {
      const arrayBindingPatternElements = left.elements;
      const parsedArrayBindingElements = arrayBindingPatternElements.map((e) => this.printNode(e, 0));
      const syntheticName = parsedArrayBindingElements.join("") + "Variable";
      let arrayBindingStatement = `${syntheticName} := ${this.printNode(right, 0)}
`;
      parsedArrayBindingElements.forEach((e, index) => {
        const statement = this.getIden(identation) + (this.goGetArgBindsDictElement(arrayBindingPatternElements[index], right, index) ? `${e} = MapTyped(GetValue(${syntheticName}, ${index}))` : `${e} = GetValue(${syntheticName}, ${index})`);
        if (index < parsedArrayBindingElements.length - 1) {
          arrayBindingStatement += statement + "\n";
        } else {
          arrayBindingStatement += statement;
        }
      });
      return arrayBindingStatement;
    }
    if (op === _typescript2.default.SyntaxKind.EqualsToken && left.kind === _typescript2.default.SyntaxKind.ElementAccessExpression) {
      const keys = [];
      let baseExpr = null;
      let cur = left;
      while (_typescript2.default.isElementAccessExpression(cur)) {
        keys.unshift(cur.argumentExpression);
        const expr = cur.expression;
        if (!_typescript2.default.isElementAccessExpression(expr)) {
          baseExpr = expr;
          break;
        }
        cur = expr;
      }
      const containerStr = this.printNode(baseExpr, 0);
      const keyStrs = keys.map((k) => this.printNode(k, 0));
      const acc = this.goElementWriteChain(baseExpr, containerStr, keys, keyStrs);
      const lastKey = keyStrs[keyStrs.length - 1];
      const rhs = this.goWithExprDepth(this.goExprDepth + 1, () => this.printNode(right, identation)).trimStart();
      const nativeRhs = right.kind === _typescript2.default.SyntaxKind.BinaryExpression ? this.goWithExprDepth(this.goExprDepth, () => this.printNode(right, identation)).trimStart() : rhs;
      const native = keyStrs.length === 1 ? this.printNativeElementAssignment(baseExpr, containerStr, keys[0], lastKey, nativeRhs) : void 0;
      if (native !== void 0) {
        return native;
      }
      return `AddElementToObject(${acc}, ${lastKey}, ${rhs})`;
    }
    if (op === _typescript2.default.SyntaxKind.PlusEqualsToken && left.kind === _typescript2.default.SyntaxKind.ElementAccessExpression) {
      const keys = [];
      let baseExpr = null;
      let cur = left;
      while (_typescript2.default.isElementAccessExpression(cur)) {
        keys.unshift(cur.argumentExpression);
        const expr = cur.expression;
        if (!_typescript2.default.isElementAccessExpression(expr)) {
          baseExpr = expr;
          break;
        }
        cur = expr;
      }
      const containerStr = this.printNode(baseExpr, 0);
      const keyStrs = keys.map((k) => this.printNode(k, 0));
      const acc = this.goElementWriteChain(baseExpr, containerStr, keys, keyStrs);
      const lastKey = keyStrs[keyStrs.length - 1];
      const rhs = this.printNode(right, 0);
      const currentValue = `${this.ELEMENT_ACCESS_WRAPPER_OPEN}${acc}, ${lastKey}${this.ELEMENT_ACCESS_WRAPPER_CLOSE}`;
      const native = keyStrs.length === 1 ? this.printNativeElementAssignment(baseExpr, containerStr, keys[0], lastKey, `Add(${containerStr}[${lastKey}], ${rhs})`, true) : void 0;
      if (native !== void 0) {
        return native;
      }
      const result = `AddElementToObject(${acc}, ${lastKey}, Add(${currentValue}, ${rhs}))`;
      return result;
    }
    if (left.kind === _typescript2.default.SyntaxKind.TypeOfExpression) {
      const typeOfExpression = this.handleTypeOfInsideBinaryExpression(node, identation);
      if (typeOfExpression) {
        return typeOfExpression;
      }
    }
    if (op === _typescript2.default.SyntaxKind.InKeyword) {
      const dictText = this.printNode(right, 0);
      const keyText = this.printNode(left, 0);
      const inlined = this.printInlineInOp(right, left, dictText, keyText);
      if (inlined !== void 0) {
        return inlined;
      }
      return `InOp(${dictText}, ${keyText})`;
    }
    if (op === _typescript2.default.SyntaxKind.PlusEqualsToken || op === _typescript2.default.SyntaxKind.MinusEqualsToken || op in this.binaryExpressionsWrappers) {
      const operandDepth = this.goExprDepth + 1;
      const leftText = this.goWithExprDepth(operandDepth, () => this.printNode(left, 0));
      const rightText = this.goWithExprDepth(operandDepth, () => this.printNode(right, 0));
      const nativeAssignment = this.goNativeCompoundAssignment(op, left, leftText, right, rightText);
      if (nativeAssignment !== void 0) {
        return nativeAssignment;
      }
      if (op === _typescript2.default.SyntaxKind.PlusEqualsToken) {
        return `${leftText} = Add(${leftText}, ${rightText})`;
      }
      if (op === _typescript2.default.SyntaxKind.MinusEqualsToken) {
        return `${leftText} = Subtract(${leftText}, ${rightText})`;
      }
      const isEquality = op === _typescript2.default.SyntaxKind.EqualsEqualsToken || op === _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken;
      const isDifference = op === _typescript2.default.SyntaxKind.ExclamationEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken;
      if (isEquality || isDifference) {
        const inlined = this.printInlineEquality(left, right, leftText, rightText, isEquality);
        if (inlined !== void 0) {
          return inlined;
        }
      }
      if (ORDERED_COMPARISON_OPERATORS[op] !== void 0) {
        const inlined = this.printInlineOrderedComparison(left, right, leftText, rightText, op);
        if (inlined !== void 0) {
          return inlined;
        }
      }
      const wrapper = this.binaryExpressionsWrappers[op];
      const open = wrapper[0];
      const close = wrapper[1];
      const nativeArithmetic = this.goNativeArithmetic(node, leftText, rightText);
      if (nativeArithmetic !== void 0) {
        return nativeArithmetic.text;
      }
      return `${open}${leftText}, ${rightText}${close}`;
    }
    return void 0;
  }
  // the scalar family the TypeScript type of an operand belongs to: 'string',
  // 'int', 'float', 'bool', 'nil' for the undefined/null literals, or undefined
  // when the type is any/unknown/a union of several families
  goScalarFamily(node) {
    if (_optionalChain([node, 'optionalAccess', _690 => _690.kind]) === _typescript2.default.SyntaxKind.Identifier && this.goDeclaredTypeOfIdentifier(node) === void 0) {
      let decl;
      const checker2 = this.checkerOrUndefined();
      if (checker2 === void 0) {
        decl = void 0;
      }
      decl = _optionalChain([checker2, 'access', _691 => _691.getSymbolAtLocation, 'call', _692 => _692(node), 'optionalAccess', _693 => _693.valueDeclaration]);
      if (this.goAnyLocalHoldsPointer(decl)) {
        return void 0;
      }
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const type = checker.getTypeAtLocation(node);
    return this.goScalarFamilyOfType(type);
  }
  // the scalar family the TypeScript type of an operand belongs to, where a
  // `string | undefined` union still counts as 'string': the Go box holds that
  // scalar or nil, and both `x == nil` and `x == "lit"` are then the same
  // predicate as the helper. Numbers are excluded by the caller.
  goScalarFamilyWithNil(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const type = checker.getTypeAtLocation(node);
    return this.goScalarFamilyOfType(type, true);
  }
  // true when this operand is a *parameter* boxed as `any` whose TypeScript type is an array or
  // object (`Strings`, `Market`, `NullableDict`, `object[]`, `object`): a Go map/slice, never a
  // pointer, so a nil test needs no helper. Locals may box a nil `*sync.Map`, which is not `== nil`.
  goObjectBoxParameter(node) {
    if (_optionalChain([node, 'optionalAccess', _694 => _694.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    if (_optionalChain([checker, 'access', _695 => _695.getSymbolAtLocation, 'call', _696 => _696(node), 'optionalAccess', _697 => _697.valueDeclaration, 'optionalAccess', _698 => _698.kind]) !== _typescript2.default.SyntaxKind.Parameter) {
      return false;
    }
    return this.goTypeIsNilComparableObject(checker.getTypeAtLocation(node));
  }
  // an object type whose Go value is a map/slice, or a union of such a type with
  // undefined/null. `any`, functions and class instances are excluded: their Go
  // value may be an identity-bearing pointer
  goTypeIsNilComparableObject(type) {
    if (type === void 0) {
      return false;
    }
    if (type.flags & _typescript2.default.TypeFlags.Union) {
      let seen = false;
      for (const member of type.types) {
        if (member.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.Void)) {
          continue;
        }
        if (!this.goTypeIsNilComparableObject(member)) {
          return false;
        }
        seen = true;
      }
      return seen;
    }
    if (!(type.flags & (_typescript2.default.TypeFlags.Object | _typescript2.default.TypeFlags.NonPrimitive))) {
      return false;
    }
    if (type.getCallSignatures().length > 0 || type.getConstructSignatures().length > 0) {
      return false;
    }
    const declaration = _nullishCoalesce(_optionalChain([type, 'access', _699 => _699.symbol, 'optionalAccess', _700 => _700.valueDeclaration]), () => ( _optionalChain([type, 'access', _701 => _701.symbol, 'optionalAccess', _702 => _702.declarations, 'optionalAccess', _703 => _703[0]])));
    return _optionalChain([declaration, 'optionalAccess', _704 => _704.kind]) !== _typescript2.default.SyntaxKind.ClassDeclaration;
  }
  // the callee name of a printed call, e.g. `this.SafeDict(x, 0, {})` → `this.SafeDict`
  goPrintedCallee(printedValue) {
    let value = printedValue.trim();
    while (value.startsWith("(") && this.isWholePrintedCall(value, 0)) {
      value = value.substring(1, value.length - 1).trim();
    }
    const open = value.indexOf("(");
    if (open <= 0 || !this.isWholePrintedCall(value, open)) {
      return void 0;
    }
    const callee = value.substring(0, open);
    return /^[A-Za-z_][\w.]*$/.test(callee) ? callee : void 0;
  }
  // true when this expression prints to an interface (`any`) box: a parameter, a
  // local the printer left `any`, or one of the helpers whose Go signature returns
  // `any`. A *T / scalar local or call is not a box and keeps its own rule.
  goIsAnyBoxExpression(node, printedText) {
    if (_optionalChain([node, 'optionalAccess', _705 => _705.kind]) === _typescript2.default.SyntaxKind.Identifier) {
      const checker = this.checkerOrUndefined();
      if (checker === void 0) {
        return false;
      }
      const symbol = checker.getSymbolAtLocation(node);
      const decl = _optionalChain([symbol, 'optionalAccess', _706 => _706.valueDeclaration]);
      const isBinding = _optionalChain([decl, 'optionalAccess', _707 => _707.kind]) === _typescript2.default.SyntaxKind.Parameter || _optionalChain([decl, 'optionalAccess', _708 => _708.kind]) === _typescript2.default.SyntaxKind.VariableDeclaration;
      if (!isBinding) {
        return false;
      }
      if (this.goDeclaredTypeOfIdentifier(node) !== void 0) {
        return false;
      }
      return !this.goAnyLocalHoldsPointer(decl);
    }
    if (_optionalChain([node, 'optionalAccess', _709 => _709.kind]) === _typescript2.default.SyntaxKind.CallExpression) {
      if (this.goTypeOfInitializer(node, printedText) !== void 0) {
        return false;
      }
      return GO_ANY_BOX_CALLS.indexOf(this.goPrintedCallee(printedText)) >= 0;
    }
    return false;
  }
  // an element read prints either a native map[string]any index or the
  // GetValue(container, key) helper call — an `any` box in both cases
  goBoxedElementRead(node, printedText) {
    while (_optionalChain([node, 'optionalAccess', _710 => _710.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      node = node.expression;
    }
    if (_optionalChain([node, 'optionalAccess', _711 => _711.kind]) !== _typescript2.default.SyntaxKind.ElementAccessExpression) {
      return false;
    }
    if (GO_ANY_BOX_CALLS.indexOf(this.goPrintedCallee(printedText)) >= 0) {
      return true;
    }
    let base = node.expression;
    while (_typescript2.default.isElementAccessExpression(base)) {
      base = base.expression;
    }
    return this.goIndexableTypeOf(base, "") === "map[string]any";
  }
  goAnyLocalHoldsPointer(decl) {
    if (_optionalChain([decl, 'optionalAccess', _712 => _712.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration || _optionalChain([decl, 'access', _713 => _713.name, 'optionalAccess', _714 => _714.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    if (this.goAnyLocalHoldsPointerCache.has(decl)) {
      return this.goAnyLocalHoldsPointerCache.get(decl);
    }
    const isPointerInit = (expr) => {
      while (_optionalChain([expr, 'optionalAccess', _715 => _715.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
        expr = expr.expression;
      }
      if (_optionalChain([expr, 'optionalAccess', _716 => _716.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
        return false;
      }
      const callee = expr.expression;
      if (_optionalChain([callee, 'optionalAccess', _717 => _717.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([callee, 'access', _718 => _718.expression, 'optionalAccess', _719 => _719.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
        return false;
      }
      const name = _optionalChain([callee, 'access', _720 => _720.name, 'optionalAccess', _721 => _721.escapedText]);
      if (typeof name !== "string" || name.length === 0) {
        return false;
      }
      const goType = GO_HELPER_RETURN_TYPES["this." + name.charAt(0).toUpperCase() + name.substring(1)];
      return typeof goType === "string" && goType.startsWith("*");
    };
    let holds = isPointerInit(decl.initializer);
    if (!holds) {
      const name = decl.name.escapedText;
      const scope = this.goEnclosingFunction(decl);
      const visit = (n) => {
        if (holds) {
          return;
        }
        if (n.kind === _typescript2.default.SyntaxKind.BinaryExpression && n.operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken && _optionalChain([n, 'access', _722 => _722.left, 'optionalAccess', _723 => _723.kind]) === _typescript2.default.SyntaxKind.Identifier && n.left.escapedText === name && isPointerInit(n.right)) {
          holds = true;
          return;
        }
        _typescript2.default.forEachChild(n, visit);
      };
      if (scope !== void 0) {
        _typescript2.default.forEachChild(scope, visit);
      }
    }
    this.goAnyLocalHoldsPointerCache.set(decl, holds);
    return holds;
  }
  // the callee name of a call, read from the AST and capitalised the way the
  // printer names Go functions (`this.parseJson` -> `this.ParseJson`). Printing the
  // callee would re-enter the equality classifier that asks this question.
  goAstCalleeName(call) {
    const callee = _optionalChain([call, 'optionalAccess', _724 => _724.expression]);
    if (_optionalChain([callee, 'optionalAccess', _725 => _725.kind]) === _typescript2.default.SyntaxKind.Identifier) {
      const name = callee.escapedText;
      return typeof name === "string" && name.length > 0 ? name.charAt(0).toUpperCase() + name.substring(1) : void 0;
    }
    if (_optionalChain([callee, 'optionalAccess', _726 => _726.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression && _optionalChain([callee, 'access', _727 => _727.expression, 'optionalAccess', _728 => _728.kind]) === _typescript2.default.SyntaxKind.ThisKeyword) {
      const name = _optionalChain([callee, 'access', _729 => _729.name, 'optionalAccess', _730 => _730.escapedText]);
      return typeof name === "string" && name.length > 0 ? "this." + name.charAt(0).toUpperCase() + name.substring(1) : void 0;
    }
    return void 0;
  }
  // true when every declaration of the awaited callee is a bodyless method signature:
  // TS never implements it, the endpoint generator does, and the Go body is the
  // `<-chan any` wrapper over callEndpointAsync (decoded JSON / "panic: " / nil)
  goAwaitedCallIsImplicitEndpoint(expression) {
    let call = expression;
    while (_optionalChain([call, 'optionalAccess', _731 => _731.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      call = call.expression;
    }
    if (_optionalChain([call, 'optionalAccess', _732 => _732.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return false;
    }
    const callee = call.expression;
    if (_optionalChain([callee, 'optionalAccess', _733 => _733.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([callee, 'access', _734 => _734.expression, 'optionalAccess', _735 => _735.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return false;
    }
    const nameNode = callee.name;
    if (_optionalChain([nameNode, 'optionalAccess', _736 => _736.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const symbol = checker.getSymbolAtLocation(nameNode);
    const declarations = _optionalChain([symbol, 'optionalAccess', _737 => _737.declarations]);
    if (!declarations || declarations.length === 0) {
      return false;
    }
    return declarations.every((d) => d.kind === _typescript2.default.SyntaxKind.MethodSignature && d.body === void 0 && _optionalChain([d, 'access', _738 => _738.parent, 'optionalAccess', _739 => _739.kind]) === _typescript2.default.SyntaxKind.InterfaceDeclaration);
  }
  // true when the printed value of this expression is never a *T whose nil
  // derefScalar folds to nil (nor a *sync.Map): null/undefined, an object/array
  // literal, an endpoint await or a JSON decode. Everything else stays unproven.
  goIsNonPointerValueSource(expr) {
    while (_optionalChain([expr, 'optionalAccess', _740 => _740.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      expr = expr.expression;
    }
    if (expr === void 0) {
      return true;
    }
    switch (expr.kind) {
      case _typescript2.default.SyntaxKind.NullKeyword:
        return true;
      case _typescript2.default.SyntaxKind.Identifier:
        return expr.escapedText === "undefined";
      case _typescript2.default.SyntaxKind.ObjectLiteralExpression:
      case _typescript2.default.SyntaxKind.ArrayLiteralExpression:
        return true;
      case _typescript2.default.SyntaxKind.AwaitExpression:
        return this.goAwaitedCallIsImplicitEndpoint(expr.expression);
      case _typescript2.default.SyntaxKind.CallExpression: {
        const name = this.goAstCalleeName(expr);
        if (name === void 0) {
          return false;
        }
        return GO_JSON_PARSE_CALLS.indexOf(name.replace(/^this\./, "")) >= 0;
      }
    }
    return false;
  }
  goAnyLocalHoldsNonPointer(decl) {
    if (_optionalChain([decl, 'optionalAccess', _741 => _741.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration || _optionalChain([decl, 'access', _742 => _742.name, 'optionalAccess', _743 => _743.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    if (this.goAnyLocalHoldsNonPointerCache.has(decl)) {
      return this.goAnyLocalHoldsNonPointerCache.get(decl);
    }
    let holds = !this.goAnyLocalHoldsPointer(decl) && this.goIsNonPointerValueSource(decl.initializer);
    if (holds) {
      const name = decl.name.escapedText;
      const scope = this.goEnclosingFunction(decl);
      if (scope === void 0) {
        holds = false;
      } else {
        const visit = (n) => {
          if (!holds) {
            return;
          }
          if (n.kind === _typescript2.default.SyntaxKind.BinaryExpression && _optionalChain([n, 'access', _744 => _744.operatorToken, 'optionalAccess', _745 => _745.kind]) === _typescript2.default.SyntaxKind.EqualsToken && this.goAssignmentWritesName(n.left, name) && !this.goIsNonPointerValueSource(n.right)) {
            holds = false;
            return;
          }
          _typescript2.default.forEachChild(n, visit);
        };
        _typescript2.default.forEachChild(scope, visit);
      }
    }
    this.goAnyLocalHoldsNonPointerCache.set(decl, holds);
    return holds;
  }
  // true when this assignment target binds the named local: a plain identifier, or a
  // destructuring element (`[ a, b ] = …` prints GetValue(<tuple>, i) writes)
  goAssignmentWritesName(left, name) {
    if (_optionalChain([left, 'optionalAccess', _746 => _746.kind]) === _typescript2.default.SyntaxKind.Identifier) {
      return left.escapedText === name;
    }
    if (_optionalChain([left, 'optionalAccess', _747 => _747.kind]) === _typescript2.default.SyntaxKind.ArrayLiteralExpression) {
      return left.elements.some((e) => _optionalChain([e, 'optionalAccess', _748 => _748.kind]) === _typescript2.default.SyntaxKind.Identifier && e.escapedText === name);
    }
    return false;
  }
  // the variable declaration an identifier resolves to, when it is one
  goAnyBoxLocalDeclaration(node) {
    if (_optionalChain([node, 'optionalAccess', _749 => _749.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const symbol = checker.getSymbolAtLocation(node);
    return _optionalChain([symbol, 'optionalAccess', _750 => _750.valueDeclaration]);
  }
  // true when this identifier is a parameter bound by `x := GetArg(optionalArgs, i, default)`:
  // GetArg runs derefScalar and folds a typed nil pointer (and nil []string/[]any) into the untyped
  // default, so the box holds a plain scalar or an untyped nil, never a nil *T.
  goGetArgBoundParameter(node) {
    if (_optionalChain([node, 'optionalAccess', _751 => _751.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const symbol = checker.getSymbolAtLocation(node);
    const decl = _optionalChain([symbol, 'optionalAccess', _752 => _752.valueDeclaration]);
    if (_optionalChain([decl, 'optionalAccess', _753 => _753.kind]) !== _typescript2.default.SyntaxKind.Parameter || decl.initializer === void 0) {
      return false;
    }
    const owner = _optionalChain([decl, 'access', _754 => _754.parent, 'optionalAccess', _755 => _755.kind]);
    if (owner !== _typescript2.default.SyntaxKind.MethodDeclaration && owner !== _typescript2.default.SyntaxKind.FunctionDeclaration && owner !== _typescript2.default.SyntaxKind.Constructor) {
      return false;
    }
    return !this.goParameterLaterWritesPointerBox(decl);
  }
  // D2 for a GetArg-bound parameter: a later `x = …` write whose printed value is a typed
  // pointer puts a nil *T back in the box, where IsEqual(x, nil) is true but `x == nil` is not
  goParameterLaterWritesPointerBox(decl) {
    const name = _optionalChain([decl, 'optionalAccess', _756 => _756.name, 'optionalAccess', _757 => _757.kind]) === _typescript2.default.SyntaxKind.Identifier ? decl.name.escapedText : void 0;
    if (typeof name !== "string") {
      return true;
    }
    const scope = this.goEnclosingFunction(decl);
    if (scope === void 0) {
      return true;
    }
    let pointerWrite = false;
    const visit = (n) => {
      if (pointerWrite) {
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.BinaryExpression && _optionalChain([n, 'access', _758 => _758.operatorToken, 'optionalAccess', _759 => _759.kind]) === _typescript2.default.SyntaxKind.EqualsToken && this.goAssignmentWritesName(n.left, name) && this.goWritePrintsPointerBox(n.right)) {
        pointerWrite = true;
        return;
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return pointerWrite;
  }
  // true when the printed value of an assignment's right-hand side is a typed pointer: a
  // `this.safeX(…)`/`this.Parse8601(…)` accessor (GO_HELPER_RETURN_TYPES), an identifier the
  // printer declared `*T`, or a hand-written *sync.Map field. Read from the AST, never printed.
  goWritePrintsPointerBox(expr) {
    while (_optionalChain([expr, 'optionalAccess', _760 => _760.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      expr = expr.expression;
    }
    if (expr === void 0) {
      return false;
    }
    if (expr.kind === _typescript2.default.SyntaxKind.CallExpression) {
      const name = this.goAstCalleeName(expr);
      if (typeof name === "string") {
        const goType = GO_HELPER_RETURN_TYPES[name];
        if (typeof goType === "string" && goType.startsWith("*")) {
          return true;
        }
        return GO_DEREF_WRAPPED_CALLS.indexOf(name.replace(/^this\./, "")) >= 0;
      }
      return false;
    }
    if (expr.kind === _typescript2.default.SyntaxKind.Identifier) {
      const declared = this.goDeclaredTypeOfIdentifier(expr);
      return typeof declared === "string" && declared.startsWith("*");
    }
    if (expr.kind === _typescript2.default.SyntaxKind.PropertyAccessExpression && _optionalChain([expr, 'access', _761 => _761.expression, 'optionalAccess', _762 => _762.kind]) === _typescript2.default.SyntaxKind.ThisKeyword) {
      const fieldType = GO_NILABLE_FIELDS_Typed["this." + _optionalChain([expr, 'access', _763 => _763.name, 'optionalAccess', _764 => _764.escapedText])];
      return typeof fieldType === "string" && GO_NIL_EQUIVALENT_POINTER_TYPES_Native.has(fieldType);
    }
    return false;
  }
  goScalarFamilyOfType(type, allowNil = false) {
    if (type === void 0) {
      return void 0;
    }
    const alias = _optionalChain([type, 'access', _765 => _765.aliasSymbol, 'optionalAccess', _766 => _766.escapedName]);
    if (!allowNil) {
      switch (alias) {
        case "Str":
        case "Int":
        case "Num":
        case "Bool":
          return void 0;
      }
    }
    const flags = type.flags;
    if (flags & _typescript2.default.TypeFlags.Union) {
      const families = /* @__PURE__ */ new Set();
      for (const member of type.types) {
        const family = this.goScalarFamilyOfType(member, allowNil);
        if (family === void 0) {
          return void 0;
        }
        if (family === "nil") {
          if (allowNil) {
            continue;
          }
          return void 0;
        }
        families.add(family);
      }
      if (families.size !== 1) {
        return void 0;
      }
      return families.values().next().value;
    }
    if (flags & (_typescript2.default.TypeFlags.String | _typescript2.default.TypeFlags.StringLiteral)) {
      return "string";
    }
    if (flags & (_typescript2.default.TypeFlags.Number | _typescript2.default.TypeFlags.NumberLiteral)) {
      return "number";
    }
    if (flags & (_typescript2.default.TypeFlags.Boolean | _typescript2.default.TypeFlags.BooleanLiteral)) {
      return "bool";
    }
    if (flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.Void)) {
      return "nil";
    }
    return void 0;
  }
  goNativeParameterType(param) {
    if (_optionalChain([param, 'optionalAccess', _767 => _767.kind]) !== _typescript2.default.SyntaxKind.Parameter) {
      return void 0;
    }
    if (this.goNativeParameterTypeCache.has(param)) {
      return this.goNativeParameterTypeCache.get(param);
    }
    this.goNativeParameterTypeCache.set(param, void 0);
    const result = this.goNativeParameterTypeOf(param);
    this.goNativeParameterTypeCache.set(param, result);
    return result;
  }
  goNativeParameterTypeOf(param) {
    if (param.initializer !== void 0 || param.dotDotDotToken !== void 0) {
      return void 0;
    }
    if (_optionalChain([param, 'access', _768 => _768.name, 'optionalAccess', _769 => _769.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const fn = param.parent;
    if (_optionalChain([fn, 'optionalAccess', _770 => _770.kind]) !== _typescript2.default.SyntaxKind.MethodDeclaration || fn.body === void 0 || _optionalChain([fn, 'access', _771 => _771.name, 'optionalAccess', _772 => _772.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const parseParam = fn.name.escapedText.startsWith("parse");
    const handlerParam = !parseParam && this.goIsProHandlerMethod(fn);
    if (!parseParam && !handlerParam) {
      return void 0;
    }
    if (this.isAsyncFunction(fn) || this.goMethodKeepsBaseSignature(fn)) {
      return void 0;
    }
    const index = fn.parameters.indexOf(param);
    for (const goType of this.goNativeParameterTypeCandidates(param, handlerParam)) {
      if (this.goParameterCallSitesPassType(fn, index, goType) && this.goLocalIsSafeToType(fn.body, param, param.name.escapedText, goType) && this.goParameterKeepsNilCompareNative(fn.body, param, goType)) {
        return goType;
      }
    }
    return void 0;
  }
  // The boxed object parameter prints `x === undefined` as a native `x == nil`
  // (goObjectBoxParameter); a parameter the printer typed as a Go map/slice would fall
  // through to IsEqual(x, nil) instead. Both predicates answer false for every value a
  goParameterKeepsNilCompareNative(body, param, goType) {
    if (goType === "*string") {
      return true;
    }
    const name = param.name.escapedText;
    let keeps = true;
    const visit = (n) => {
      if (!keeps) {
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.Identifier && n.escapedText === name) {
        let symbol;
        try {
          symbol = this.getChecker().getSymbolAtLocation(n);
        } catch (e) {
          symbol = void 0;
        }
        const binary = n.parent;
        if (_optionalChain([symbol, 'optionalAccess', _773 => _773.valueDeclaration]) === param && _optionalChain([binary, 'optionalAccess', _774 => _774.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && (binary.left === n || binary.right === n)) {
          const op = _optionalChain([binary, 'access', _775 => _775.operatorToken, 'optionalAccess', _776 => _776.kind]);
          if (op === _typescript2.default.SyntaxKind.EqualsEqualsToken || op === _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken) {
            const other = binary.left === n ? binary.right : binary.left;
            if (_optionalChain([other, 'optionalAccess', _777 => _777.kind]) === _typescript2.default.SyntaxKind.NullKeyword || _optionalChain([other, 'optionalAccess', _778 => _778.kind]) === _typescript2.default.SyntaxKind.Identifier && other.escapedText === "undefined") {
              keeps = false;
              return;
            }
          }
        }
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(body, visit);
    return keeps;
  }
  // ---- D-03: pro handler frame parameters ------------------------------------
  // A `handle*` method of a pro exchange class receives the raw frame from the WS
  // client. When the checker types that parameter as `Dict`, and every call site
  goIsProHandlerMethod(fn) {
    const fileName = String(_nullishCoalesce(fn.getSourceFile().fileName, () => ( "")));
    if (!/(^|[\\/])ts[\\/]src[\\/]pro[\\/]/.test(fileName)) {
      return false;
    }
    const name = fn.name.escapedText;
    return name.length > 6 && name.startsWith("handle") && name[6] === name[6].toUpperCase();
  }
  // `Dict` is the checker's structural `{[key: string]: any}`: a string index
  // signature is the proof. Class instances (OrderBook, ArrayCache*, Client, Future),
  // named-member interfaces (Market, Order, Ticker) and arrays carry none of them, so
  goParameterTypeIsDict(type) {
    try {
      return typeof type.getStringIndexType === "function" && type.getStringIndexType() !== void 0;
    } catch (e) {
      return false;
    }
  }
  // the Go types the declared TypeScript type can carry. `Dict`/`Market`/`Currency`
  // and the other dict-shaped object types print as the `map[string]any` the Go side
  // builds them from, `List` (= any[]) as its `[]any`; pro `handle*` frames (isHandler)
  goNativeParameterTypeCandidates(param, isHandler = false) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return [];
    }
    const type = checker.getTypeAtLocation(param);
    if (type === void 0) {
      return [];
    }
    let parts = typeof type.isUnion === "function" && type.isUnion() ? type.types.slice() : [type];
    parts = parts.filter((p) => !(p.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null)));
    if (parts.length !== 1) {
      return [];
    }
    const inner = parts[0];
    if (inner.flags & _typescript2.default.TypeFlags.String) {
      return isHandler ? [] : ["*string"];
    }
    if (isHandler && this.goParameterTypeIsDict(inner)) {
      return ["map[string]any"];
    }
    if (!(inner.flags & _typescript2.default.TypeFlags.Object)) {
      return [];
    }
    if (checker.isArrayType(inner)) {
      const element = checker.getIndexTypeOfType(inner, _typescript2.default.IndexKind.Number);
      if (element !== void 0 && element.flags & (_typescript2.default.TypeFlags.Any | _typescript2.default.TypeFlags.Unknown)) {
        return ["[]any"];
      }
      return [];
    }
    if (this.goTypeIsNilComparableObject(inner)) {
      return ["map[string]any"];
    }
    return [];
  }
  // true when the method overrides (or shadows) a member of the class it extends, or carries an
  // explicit `override`: those print the base signature so the generated base classes and
  // IDerivedExchange keep compiling. The abstract base (ts/src/base/**) is never retyped.
  goMethodKeepsBaseSignature(fn) {
    if ((_nullishCoalesce(fn.modifiers, () => ( []))).some((m) => m.kind === _typescript2.default.SyntaxKind.OverrideKeyword)) {
      return true;
    }
    if (/(^|\/)ts\/src\/base\//.test(fn.getSourceFile().fileName)) {
      return true;
    }
    const name = fn.name.escapedText;
    let cls = fn.parent;
    while (cls !== void 0 && cls.kind !== _typescript2.default.SyntaxKind.ClassDeclaration && cls.kind !== _typescript2.default.SyntaxKind.ClassExpression) {
      cls = cls.parent;
    }
    if (cls === void 0) {
      return true;
    }
    const clauses = _nullishCoalesce(cls.heritageClauses, () => ( []));
    if (!clauses.some((clause) => clause.token === _typescript2.default.SyntaxKind.ExtendsKeyword)) {
      return true;
    }
    for (const clause of clauses) {
      if (clause.token !== _typescript2.default.SyntaxKind.ExtendsKeyword) {
        continue;
      }
      for (const expr of _nullishCoalesce(clause.types, () => ( []))) {
        let baseType;
        const checker = this.checkerOrUndefined();
        if (checker === void 0) {
          baseType = void 0;
        }
        baseType = checker.getTypeAtLocation(expr);
        if (_optionalChain([baseType, 'optionalAccess', _779 => _779.getProperty, 'optionalCall', _780 => _780(name)]) !== void 0) {
          return true;
        }
      }
    }
    return false;
  }
  // every call site of `fn` in the whole tree must pass exactly `goType` at `index`
  goParameterCallSitesPassType(fn, index, goType) {
    const name = fn.name.escapedText;
    for (const call of this.goSameFileCallsOf(fn, name)) {
      const arg = _optionalChain([call, 'access', _781 => _781.arguments, 'optionalAccess', _782 => _782[index]]);
      if (arg === void 0 || arg.kind === _typescript2.default.SyntaxKind.SpreadElement) {
        return false;
      }
      if (this.goPrintedArgType(arg) !== goType) {
        return false;
      }
    }
    const tree = this.goTsSrcTree(fn.getSourceFile());
    if (tree !== void 0) {
      const myClass = this.goEnclosingClassName(fn);
      const myFile = tree.relativeOf.get(fn.getSourceFile().fileName);
      for (const site of _nullishCoalesce(tree.callIndex.get(name), () => ( []))) {
        if (site.file === myFile) {
          continue;
        }
        if (!this.goTsSrcFileDerivesFrom(tree, site.file, myClass)) {
          continue;
        }
        const arg = site.args[index];
        if (arg === void 0 || !this.goTextArgMatchesType(arg, goType, site.file, tree)) {
          return false;
        }
      }
    }
    return true;
  }
  goEnclosingClassName(fn) {
    let cls = fn.parent;
    while (cls !== void 0 && cls.kind !== _typescript2.default.SyntaxKind.ClassDeclaration && cls.kind !== _typescript2.default.SyntaxKind.ClassExpression) {
      cls = cls.parent;
    }
    return _optionalChain([cls, 'optionalAccess', _783 => _783.name, 'optionalAccess', _784 => _784.kind]) === _typescript2.default.SyntaxKind.Identifier ? cls.name.escapedText : void 0;
  }
  // the printed Go type of a call-site argument, or undefined when the printer
  // cannot name it (then the call site does not prove anything)
  goPrintedArgType(arg) {
    if (arg.kind === _typescript2.default.SyntaxKind.Identifier) {
      return this.goDeclaredTypeOfIdentifier(arg);
    }
    return this.goTypeOfInitializer(arg, this.printNode(arg, 0));
  }
  // every `this.<name>(...)` of this file whose resolved signature is `fn`
  goSameFileCallsOf(fn, name) {
    const file = fn.getSourceFile();
    let index = this.goSameFileCallCache.get(file);
    if (index === void 0) {
      index = /* @__PURE__ */ new Map();
      const visit = (node) => {
        if (node.kind === _typescript2.default.SyntaxKind.CallExpression) {
          const callee = node.expression;
          const calleeName = _optionalChain([callee, 'optionalAccess', _785 => _785.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression ? _optionalChain([callee, 'access', _786 => _786.name, 'optionalAccess', _787 => _787.escapedText]) : void 0;
          if (typeof calleeName === "string") {
            const list = _nullishCoalesce(index.get(calleeName), () => ( []));
            list.push(node);
            index.set(calleeName, list);
          }
        }
        _typescript2.default.forEachChild(node, visit);
      };
      visit(file);
      this.goSameFileCallCache.set(file, index);
    }
    const checker = this.getChecker();
    const result = [];
    for (const call of _nullishCoalesce(index.get(name), () => ( []))) {
      let declaration;
      try {
        declaration = _optionalChain([checker, 'access', _788 => _788.getResolvedSignature, 'call', _789 => _789(call), 'optionalAccess', _790 => _790.declaration]);
      } catch (e) {
        declaration = void 0;
      }
      if (declaration === fn) {
        result.push(call);
      }
    }
    return result;
  }
  // Lazily read the ts/src tree this file belongs to: the call sites of every
  // `this.x(...)`, each file's text and its class -> base map. A scoped run's
  // program holds one exchange, so the sibling files are only provable textually.
  goTsSrcTree(file) {
    const fileName = file.fileName;
    const marker = "/ts/src/";
    const at = fileName.lastIndexOf(marker);
    if (at < 0) {
      return void 0;
    }
    const root = fileName.substring(0, at + marker.length - 1);
    if (!this.goTsSrcTreeCache.has(root)) {
      this.goTsSrcTreeCache.set(root, this.goTsSrcTreeBuild(root));
    }
    return this.goTsSrcTreeCache.get(root);
  }
  goTsSrcTreeBuild(root) {
    const callIndex = /* @__PURE__ */ new Map();
    const fileText = /* @__PURE__ */ new Map();
    const classBases = /* @__PURE__ */ new Map();
    const relativeOf = /* @__PURE__ */ new Map();
    const walk = (dir, rel) => {
      let entries;
      try {
        entries = fs.readdirSync(dir, { withFileTypes: true });
      } catch (e) {
        return;
      }
      for (const entry of entries) {
        if (entry.isDirectory()) {
          if (entry.name !== "node_modules") {
            walk(path.join(dir, entry.name), rel + entry.name + "/");
          }
          continue;
        }
        if (!entry.name.endsWith(".ts") || entry.name.endsWith(".d.ts")) {
          continue;
        }
        const relPath = rel + entry.name;
        let text;
        try {
          text = fs.readFileSync(path.join(dir, entry.name), "utf8");
        } catch (e) {
          continue;
        }
        fileText.set(relPath, text);
        relativeOf.set(path.join(dir, entry.name), relPath);
        const classRe = /\bclass\s+([A-Za-z_$][\w$]*)\s+extends\s+([A-Za-z_$][\w$]*)/g;
        let match;
        while ((match = classRe.exec(text)) !== null) {
          classBases.set(match[1], match[2]);
        }
        const callRe = /this\s*\.\s*([A-Za-z_$][\w$]*)\s*\(/g;
        while ((match = callRe.exec(text)) !== null) {
          const args = goBalancedCallArgs(text, callRe.lastIndex - 1);
          if (args === void 0) {
            continue;
          }
          const list = _nullishCoalesce(callIndex.get(match[1]), () => ( []));
          list.push({ file: relPath, args });
          callIndex.set(match[1], list);
        }
      }
    };
    walk(root, "");
    return { callIndex, fileText, classBases, relativeOf };
  }
  goTsSrcFileDerivesFrom(tree, file, className) {
    if (className === void 0) {
      return true;
    }
    const text = tree.fileText.get(file);
    if (text === void 0) {
      return true;
    }
    const classRe = /\bclass\s+([A-Za-z_$][\w$]*)/g;
    let match;
    while ((match = classRe.exec(text)) !== null) {
      let current = match[1];
      for (let hops = 0; hops < 12 && current !== void 0; hops++) {
        if (current === className) {
          return true;
        }
        current = tree.classBases.get(current);
      }
    }
    return false;
  }
  // the textual call-site proof. `*string` is the only pointer-shaped native type
  // with siblings the printer can name from text; a map/slice parameter is proven for
  // the argument shapes whose printed Go form IS that type: the object/array literals
  goTextArgMatchesType(argText, goType, file, tree) {
    let text = (_nullishCoalesce(argText, () => ( ""))).trim();
    while (text.startsWith("(") && text.endsWith(")")) {
      text = text.substring(1, text.length - 1).trim();
    }
    const isStringProducer = (candidate) => GO_TS_SRC_STRING_PRODUCERS.some((rx) => rx.test(candidate));
    const localDeclaredFrom = (name, accept) => {
      const fileText = _nullishCoalesce(tree.fileText.get(file), () => ( ""));
      const declRe = new RegExp("(?:const|let|var)\\s+" + name + "\\s*(?::[^=]*)?=\\s*([^;\\n]+)");
      const match = declRe.exec(fileText);
      return match !== null && accept(match[1].trim());
    };
    if (goType === "*string") {
      if (isStringProducer(text)) {
        return true;
      }
      const identifier = /^([A-Za-z_$][\w$]*)$/.exec(text);
      if (identifier !== null) {
        return localDeclaredFrom(identifier[1], isStringProducer);
      }
    }
    if (goType === "map[string]any" || goType === "[]any") {
      const isMap = goType === "map[string]any";
      const literal = isMap ? "{" : "[";
      const producers = isMap ? GO_TS_SRC_MAP_PRODUCERS : [];
      return text.startsWith(literal) || producers.some((rx) => rx.test(text));
    }
    return false;
  }
  goDeclaredTypeOfIdentifier(node) {
    if (_optionalChain([node, 'optionalAccess', _791 => _791.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const symbol = checker.getSymbolAtLocation(node);
    const decl = _optionalChain([symbol, 'optionalAccess', _792 => _792.valueDeclaration]);
    if (decl === void 0) {
      return void 0;
    }
    if (decl.kind === _typescript2.default.SyntaxKind.Parameter) {
      const bound = this.goGetArgParameterType(decl);
      if (bound !== void 0 && bound.startsWith("*")) {
        return bound;
      }
      return this.goNativeParameterType(decl);
    }
    if (decl.kind !== _typescript2.default.SyntaxKind.VariableDeclaration) {
      return void 0;
    }
    if (decl.initializer === void 0 || _optionalChain([decl, 'access', _793 => _793.name, 'optionalAccess', _794 => _794.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    if (this.goDeclaredTypeCache.has(decl)) {
      return this.goDeclaredTypeCache.get(decl);
    }
    if (this.goDeclaredTypeInProgress.has(decl)) {
      return void 0;
    }
    this.goDeclaredTypeInProgress.add(decl);
    let goType;
    try {
      goType = this.getGoLocalType(decl, this.printNode(decl.initializer, 0));
    } finally {
      this.goDeclaredTypeInProgress.delete(decl);
    }
    const result = goType === "any" ? void 0 : goType;
    this.goDeclaredTypeCache.set(decl, result);
    return result;
  }
  // true when this identifier's Go type is a pointer we can deref (*string / *int64 / …)
  goIsPointerIdentifier(node) {
    const goType = this.goDeclaredTypeOfIdentifier(node);
    return typeof goType === "string" && goType.startsWith("*");
  }
  // the Go pointer type an *expression* evaluates to, or undefined. Covers both a
  // local declared `var x *string = …` and a direct `this.SafeString(...)` call,
  // whose Go signature returns a pointer even though TypeScript says `string`.
  goPointerTypeOfExpression(node, printedText) {
    const declared = this.goDeclaredTypeOfIdentifier(node);
    if (typeof declared === "string" && declared.startsWith("*")) {
      return declared;
    }
    if (_optionalChain([node, 'optionalAccess', _795 => _795.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      const fieldType = GO_NILABLE_FIELDS_Typed[printedText];
      if (typeof fieldType === "string" && GO_NIL_EQUIVALENT_POINTER_TYPES_Native.has(fieldType)) {
        return fieldType;
      }
    }
    if (_optionalChain([node, 'optionalAccess', _796 => _796.kind]) === _typescript2.default.SyntaxKind.CallExpression) {
      const goType = this.goTypeOfInitializer(node, printedText);
      if (typeof goType === "string" && goType.startsWith("*")) {
        return goType;
      }
    }
    return void 0;
  }
  // the concrete Go type a `x[k] = v` receiver is declared with, when the printer
  // can name it: a local it typed itself, or a whole call whose Go return type it
  // knows. Everything else is an `any` box, and indexing an `any` in Go needs a
  // type assertion, so those keep the runtime helper.
  goElementAssignmentContainerType(node, printedText) {
    const declared = this.goDeclaredTypeOfIdentifier(node);
    if (declared === "map[string]any" || declared === "[]any") {
      return declared;
    }
    if (_optionalChain([node, 'optionalAccess', _797 => _797.kind]) === _typescript2.default.SyntaxKind.CallExpression) {
      const known = this.goTypeOfInitializer(node, printedText);
      if (known === "map[string]any" || known === "[]any") {
        return known;
      }
    }
    return void 0;
  }
  // the Go container type of a hand-written `this.<field>` receiver, undefined for every other
  // shape. Generated structs embed BaseExchange, so `this.<field>` is the only property access the
  // field table types — a local or parameter of the same name is a different declaration.
  goFieldContainerTypeNative(node) {
    if (_optionalChain([node, 'optionalAccess', _798 => _798.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([node, 'access', _799 => _799.expression, 'optionalAccess', _800 => _800.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return void 0;
    }
    const name = _optionalChain([node, 'access', _801 => _801.name, 'optionalAccess', _802 => _802.escapedText]);
    if (typeof name !== "string") {
      return void 0;
    }
    return GO_FIELD_CONTAINER_TYPES_NATIVE[this.transformPropertyAccessExpressionName(name, node.name)];
  }
  // the printed key is a Go string when the printer knows it: a string literal, or
  // an identifier declared `string`. Params, GetValue(...) and string concatenation
  // all print as `any`, which Go refuses as a map key.
  goIsStringKeyExpression(node) {
    if (node.kind === _typescript2.default.SyntaxKind.StringLiteral || node.kind === _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral) {
      return true;
    }
    return this.goDeclaredTypeOfIdentifier(node) === "string";
  }
  // `[]any` receivers only inline when the index is a literal the slice's own
  // literal initializer covers and nothing rebinds the local: the helper silently
  // ignores an out-of-range index, while Go panics on the assignment.
  goSliceIndexProvablyInRange(node, indexNode) {
    if (_optionalChain([node, 'optionalAccess', _803 => _803.kind]) !== _typescript2.default.SyntaxKind.Identifier || _optionalChain([indexNode, 'optionalAccess', _804 => _804.kind]) !== _typescript2.default.SyntaxKind.NumericLiteral) {
      return false;
    }
    const index = Number(indexNode.text);
    if (!Number.isInteger(index) || index < 0) {
      return false;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const symbol = checker.getSymbolAtLocation(node);
    const decl = _optionalChain([symbol, 'optionalAccess', _805 => _805.valueDeclaration]);
    if (decl === void 0 || decl.kind !== _typescript2.default.SyntaxKind.VariableDeclaration || _optionalChain([decl, 'access', _806 => _806.name, 'optionalAccess', _807 => _807.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const initializer = decl.initializer;
    if (_optionalChain([initializer, 'optionalAccess', _808 => _808.kind]) !== _typescript2.default.SyntaxKind.ArrayLiteralExpression) {
      return false;
    }
    if (initializer.elements.length <= index) {
      return false;
    }
    return !this.goLocalIsRebound(this.goEnclosingFunction(decl), decl.name);
  }
  // true when the local is rebound anywhere in its function; an element write
  // (`x[k] = v`) leaves the local itself bound, so only the slice header is at stake
  goLocalIsRebound(scope, nameNode) {
    if (scope === void 0) {
      return true;
    }
    const name = nameNode.escapedText;
    let rebound = false;
    const visit = (n) => {
      if (rebound) {
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.Identifier && n.escapedText === name && n !== nameNode) {
        if (this.goRebindingTargetOf(n) !== void 0) {
          rebound = true;
          return;
        }
      }
      _typescript2.default.forEachChild(n, visit);
    };
    visit(scope);
    return rebound;
  }
  // walks up from the identifier to the assignment it targets: `x = …` / `x += …` /
  // `[x, y] = …` / `for (x of …)` rebind the local, `x[k] = v` does not
  goRebindingTargetOf(identifier) {
    let node = identifier;
    let parent = node.parent;
    while (_optionalChain([parent, 'optionalAccess', _809 => _809.kind]) === _typescript2.default.SyntaxKind.ArrayLiteralExpression) {
      node = parent;
      parent = parent.parent;
    }
    if (_optionalChain([parent, 'optionalAccess', _810 => _810.kind]) === _typescript2.default.SyntaxKind.ForOfStatement && parent.initializer === node) {
      return parent;
    }
    if (_optionalChain([parent, 'optionalAccess', _811 => _811.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && parent.left === node) {
      const op = parent.operatorToken.kind;
      if (op === _typescript2.default.SyntaxKind.EqualsToken || op >= _typescript2.default.SyntaxKind.FirstCompoundAssignment && op <= _typescript2.default.SyntaxKind.LastCompoundAssignment) {
        return parent;
      }
    }
    return void 0;
  }
  // native `container[key] = value` when the receiver's Go type is proved by the
  // printer, otherwise undefined and the caller keeps the runtime helper
  printNativeElementAssignment(containerNode, containerStr, keyNode, keyStr, valueStr, compound = false) {
    const containerType = this.goElementAssignmentContainerType(containerNode, containerStr);
    const fieldType = this.goFieldContainerTypeNative(containerNode);
    if (fieldType !== void 0) {
      if (!this.goIsStringKeyExpression(keyNode)) {
        return void 0;
      }
      if (fieldType === "*sync.Map") {
        return compound ? void 0 : `${containerStr}.Store(${keyStr}, ${valueStr})`;
      }
      return `${containerStr}[${keyStr}] = ${valueStr}`;
    }
    if (containerType === "map[string]any") {
      if (!this.goIsStringKeyExpression(keyNode)) {
        return void 0;
      }
      return `${containerStr}[${keyStr}] = ${valueStr}`;
    }
    if (containerType === "[]any") {
      if (!this.goSliceIndexProvablyInRange(containerNode, keyNode)) {
        return void 0;
      }
      return `${containerStr}[${keyStr}] = ${valueStr}`;
    }
    return void 0;
  }
  // the concrete Go type an *expression* is printed as: a local's declared type,
  // the return type of a runtime helper call, or a literal. undefined when the value
  // stays inside an `any` box — a native Go operation on an `any` box would not
  // compile, so every rule below falls back to its helper in that case.
  goPrintedTypeOfExpression(node, printedText) {
    const inner = _optionalChain([node, 'optionalAccess', _812 => _812.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression ? node.expression : node;
    if (_optionalChain([inner, 'optionalAccess', _813 => _813.kind]) === _typescript2.default.SyntaxKind.Identifier) {
      return this.goDeclaredTypeOfIdentifier(inner);
    }
    return this.goTypeOfInitializer(inner, printedText);
  }
  printInlineArrayLength(expression, printedText, lengthNode) {
    if (printedText.includes("\n")) {
      return void 0;
    }
    const goType = this.goPrintedTypeOfExpression(expression, printedText);
    if (typeof goType === "string" && this.sliceLengthTypes.indexOf(goType) >= 0) {
      return `len(${printedText})`;
    }
    if (this.goLengthFeedsArithmeticClassifier(lengthNode)) {
      return void 0;
    }
    if (goType === "string") {
      return `len(${printedText})`;
    }
    if (this.goNativeLengthFieldType(printedText) !== void 0) {
      return `len(${printedText})`;
    }
    return void 0;
  }
  // the hand-written struct type of a `this.<field>` read, for fields declared as a slice the
  // length helpers count. Any other type (map / *sync.Map / interface{}) keeps the helper:
  // GetArrayLength answers 0 for those, while len would answer the real count.
  goNativeLengthFieldType(printedText) {
    const match = /^this\.([A-Za-z_]\w*)$/.exec(this.goUnwrapPrintedParens(printedText));
    if (match === null || this.GO_NATIVE_LENGTH_FIELDS.indexOf(match[1]) < 0) {
      return void 0;
    }
    return "[]string";
  }
  // true when the `.length` sits inside a `-`, `*`, `/` or `%` chain. The ccxt-side classifier
  // (build/go-local-types.js, CCXT_GO_INT_OPERAND_CALLEES) names `GetArrayLength(`/`GetLength(` an
  // `int` operand, so inlining `len` would declassify the chain's int64 local and `.(int64)` unbox.
  goLengthFeedsArithmeticClassifier(lengthNode) {
    let current = _optionalChain([lengthNode, 'optionalAccess', _814 => _814.parent]);
    for (let i = 0; i < 16 && current !== void 0; i++) {
      if (current.kind === _typescript2.default.SyntaxKind.BinaryExpression) {
        const op = _optionalChain([current, 'access', _815 => _815.operatorToken, 'optionalAccess', _816 => _816.kind]);
        if (op === _typescript2.default.SyntaxKind.MinusToken || op === _typescript2.default.SyntaxKind.AsteriskToken || op === _typescript2.default.SyntaxKind.SlashToken || op === _typescript2.default.SyntaxKind.PercentToken) {
          return true;
        }
      }
      switch (current.kind) {
        case _typescript2.default.SyntaxKind.ExpressionStatement:
        case _typescript2.default.SyntaxKind.VariableStatement:
        case _typescript2.default.SyntaxKind.ReturnStatement:
        case _typescript2.default.SyntaxKind.IfStatement:
        case _typescript2.default.SyntaxKind.Block:
        case _typescript2.default.SyntaxKind.ForStatement:
        case _typescript2.default.SyntaxKind.WhileStatement:
        case _typescript2.default.SyntaxKind.MethodDeclaration:
        case _typescript2.default.SyntaxKind.FunctionDeclaration:
        case _typescript2.default.SyntaxKind.ArrowFunction:
          return false;
      }
      current = current.parent;
    }
    return false;
  }
  // Go has no ternary operator. The func literal returns the same branch value the
  // helper would and prints the condition the same way; it evaluates only the branch
  // TypeScript would take, while Ternary receives both already evaluated. Both arms
  // printing as one and the same Go scalar names it (`func() string`), so the literal
  // carries the value itself instead of an `any` box.
  printInlineTernary(condition, whenTrue, whenFalse, resultType = void 0) {
    if (condition.includes("\n")) {
      return void 0;
    }
    const level = this.goStatementLevel;
    const body = this.getIden(level + 1);
    const branch = this.getIden(level + 2);
    return `func() ${_nullishCoalesce(resultType, () => ( "any"))} {
${body}if ${this.goStripControlClauseParens(condition)} {
${branch}return ${whenTrue}
${body}}
${body}return ${whenFalse}
${this.getIden(level)}}()`;
  }
  // the Go scalar an arm already prints a VALUE of, or undefined while the arm is an `any` box.
  // Only the printed text counts: a literal, a local declared with that type, or a call whose Go
  // signature returns it. A classifier-named box (`Subtract(...)`) cannot be carried by `return`.
  goTernaryArmType(node, printedText) {
    const text = this.goUnwrapPrintedParens(printedText);
    const inner = _optionalChain([node, 'optionalAccess', _817 => _817.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression ? node.expression : node;
    switch (_optionalChain([inner, 'optionalAccess', _818 => _818.kind])) {
      case _typescript2.default.SyntaxKind.StringLiteral:
      case _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral:
        return "string";
      case _typescript2.default.SyntaxKind.TrueKeyword:
      case _typescript2.default.SyntaxKind.FalseKeyword:
        return "bool";
      case _typescript2.default.SyntaxKind.NumericLiteral:
        if (/^[0-9]+$/.test(inner.text)) {
          return "int";
        }
        return /^[0-9]*\.[0-9]+([eE][+-]?[0-9]+)?$/.test(inner.text) || /^[0-9]+[eE][+-]?[0-9]+$/.test(inner.text) ? "float64" : void 0;
      case _typescript2.default.SyntaxKind.Identifier:
        return this.goLocalStaticType(inner);
    }
    const literalType = goFuncLiteralReturnType(text);
    if (literalType !== void 0) {
      return literalType;
    }
    const open = text.indexOf("(");
    if (open <= 0 || !this.isWholePrintedCall(text, open)) {
      return void 0;
    }
    const goType = GO_HELPER_RETURN_TYPES[text.substring(0, open)];
    return goType === void 0 || goType.startsWith("*") ? void 0 : goType;
  }
  // the one Go scalar both arms print as, or undefined when the literal keeps the `any`
  // box: only GO_TYPE_NAMES are nameable, and a pointer/`any` arm (or two different
  // types) is not a type the literal could return
  goTernaryResultType(whenTrueNode, whenTrue, whenFalseNode, whenFalse) {
    const whenTrueType = this.goTernaryArmType(whenTrueNode, whenTrue);
    if (typeof whenTrueType !== "string" || whenTrueType === "any" || GO_TYPE_NAMES.indexOf(whenTrueType) < 0) {
      return void 0;
    }
    return this.goTernaryArmType(whenFalseNode, whenFalse) === whenTrueType ? whenTrueType : void 0;
  }
  // stripParens() applied to a printed condition text (see goControlClauseParens)
  goStripControlClauseParens(text) {
    for (; ; ) {
      const inner = this.goEnclosedExpression(text);
      if (inner === void 0) {
        return text;
      }
      text = inner;
    }
  }
  // a multi-line branch (a nested ternary, a composite literal) is laid out relative to
  // the `return` statement that holds it: inside the `if` for whenTrue (two levels below
  // the statement), the literal's body for whenFalse (one level)
  goPrintTernaryBranch(node, levels) {
    const previousLevel = this.goStatementLevel;
    this.goStatementLevel = previousLevel + levels;
    try {
      return this.goWithExprDepth(1, () => this.printNode(node, 0));
    } finally {
      this.goStatementLevel = previousLevel;
    }
  }
  // `key in obj` on a Go map[string]any with a Go string key: the two-value read is a statement,
  // hence the func literal; present-but-nil is ok=true like InOp. A `*string` key derefs, nil
  // answering false. InOp also covers nil/number keys and sync.Map/orderbook receivers: else keep it.
  printInlineInOp(dictNode, keyNode, dictText, keyText) {
    if (dictText.includes("\n") || keyText.includes("\n")) {
      return void 0;
    }
    if (this.goPrintedTypeOfExpression(dictNode, dictText) !== "map[string]any") {
      return void 0;
    }
    if (this.goPrintedTypeOfExpression(keyNode, keyText) !== "string") {
      if (_optionalChain([keyNode, 'optionalAccess', _819 => _819.kind]) !== _typescript2.default.SyntaxKind.Identifier || this.goDeclaredTypeOfIdentifier(keyNode) !== "*string") {
        return void 0;
      }
      const level2 = this.goStatementLevel;
      const body = this.getIden(level2 + 1);
      return `func() bool {
${body}if ${keyText} == nil {
${this.getIden(level2 + 2)}return false
${body}}
${body}_, ok := ${dictText}[*${keyText}]
${body}return ok
${this.getIden(level2)}}()`;
    }
    const read = `_, ok := ${dictText}[${keyText}]`;
    if (11 + read.length + 2 + "return ok".length <= 100) {
      return `func() bool { ${read}; return ok }()`;
    }
    const level = this.goStatementLevel;
    return `func() bool {
${this.getIden(level + 1)}${read}
${this.getIden(level + 1)}return ok
${this.getIden(level)}}()`;
  }
  // OpNeg boxes `-val.Int()` / `-val.Float()`, i.e. an int64 or a float64, and nil
  // for anything else. `-x` reproduces that exactly only for an operand that already
  // prints as float64/int64; a Go `int` (or an integer literal) boxes as int instead.
  printInlineOpNeg(node, printedText) {
    if (printedText.includes("\n")) {
      return void 0;
    }
    const goType = this.goPrintedTypeOfExpression(node.operand, printedText);
    if (goType === "float64" || goType === "int64") {
      return `-${printedText}`;
    }
    if (/^\d+(\.\d+)?([eE][+-]?\d+)?$/.test(printedText.trim())) {
      if (printedText.includes(".") || /[eE]/.test(printedText)) {
        return `-${printedText}`;
      }
      const parent = node.parent;
      if (_optionalChain([parent, 'optionalAccess', _820 => _820.kind]) === _typescript2.default.SyntaxKind.CallExpression) {
        const callee = this.printNode(parent.expression, 0);
        if (this.comparisonHelpers.indexOf(callee) >= 0) {
          return `-${printedText}`;
        }
      }
      if (_optionalChain([parent, 'optionalAccess', _821 => _821.kind]) === _typescript2.default.SyntaxKind.BinaryExpression) {
        const op = parent.operatorToken.kind;
        if (op === _typescript2.default.SyntaxKind.GreaterThanToken || op === _typescript2.default.SyntaxKind.GreaterThanEqualsToken || op === _typescript2.default.SyntaxKind.LessThanToken || op === _typescript2.default.SyntaxKind.LessThanEqualsToken) {
          return `-${printedText}`;
        }
      }
    }
    return void 0;
  }
  // JS truthiness of an operand whose Go type the printer knows, expressed with
  // plain Go instead of boxing the value into `EvalTruthy(any)`. Each arm mirrors the
  // matching `EvalTruthy` case exactly, including nil (a nil *T and a nil map are
  // both falsy) — so this is the same predicate, minus the interface round-trip.
  printInlineTruthy(node) {
    if (_optionalChain([node, 'optionalAccess', _822 => _822.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const goType = this.goDeclaredTypeOfIdentifier(node);
    if (goType === void 0) {
      return void 0;
    }
    const text = this.printNode(node, 0);
    switch (goType) {
      case "bool":
        return text;
      case "string":
        return `(${text} != "")`;
      case "int":
      case "int64":
      case "float64":
        return `(${text} != 0)`;
      case "*bool":
        return `(${text} != nil && *${text})`;
      case "*string":
        return `(${text} != nil && *${text} != "")`;
      case "*int":
      case "*int64":
      case "*float64":
        return `(${text} != nil && *${text} != 0)`;
      case "[]string":
      case "[]any":
      case "map[string]any":
        return `(len(${text}) > 0)`;
    }
    return void 0;
  }
  // an `any` box the checker proves can only hold a bool or nil — a `boolean`
  // local whose Go declaration stayed `any` (a later write the printer cannot
  // type, e.g. a tuple read) — is what `EvalTruthy` reduces to: nil and `false`
  printInlineBoolBoxTruthy(node) {
    if (this.goScalarFamilyWithNil(node) !== "bool") {
      return void 0;
    }
    let declaration;
    try {
      declaration = _optionalChain([this, 'access', _823 => _823.getChecker, 'call', _824 => _824(), 'access', _825 => _825.getSymbolAtLocation, 'call', _826 => _826(node), 'optionalAccess', _827 => _827.valueDeclaration]);
    } catch (e) {
      return void 0;
    }
    if (_optionalChain([declaration, 'optionalAccess', _828 => _828.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration && !this.goGetArgBoundParameter(node)) {
      return void 0;
    }
    const text = this.printNode(node, 0);
    if (!this.goIsAnyBoxExpression(node, text)) {
      return void 0;
    }
    return `(${text} == true)`;
  }
  // `EvalTruthy(this.SafeBool(…))`: the accessor's Go signature returns a `*bool`,
  // nil for an absent flag and the flag otherwise — exactly the two states
  // `derefScalar` hands the helper — so the predicate is `x != nil && *x`.
  printInlineBoolPointerTruthy(node, printedText) {
    if (_optionalChain([node, 'optionalAccess', _829 => _829.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return void 0;
    }
    if (GO_PURE_BOOL_ACCESSORS.indexOf(this.goPrintedCallee(printedText)) < 0) {
      return void 0;
    }
    if (!node.arguments.every((argument) => this.goIsRepeatSafePointerArgument(argument))) {
      return void 0;
    }
    return `(${printedText} != nil && *${printedText})`;
  }
  // an argument the two halves of a pointer deref may read twice: a plain
  // identifier/literal read, or a `this.<field>` read of the exchange instance
  goIsRepeatSafePointerArgument(node) {
    while (_optionalChain([node, 'optionalAccess', _830 => _830.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      node = node.expression;
    }
    if (this.goIsReadOnlyCallArgument(node)) {
      return true;
    }
    return _optionalChain([node, 'optionalAccess', _831 => _831.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression && _optionalChain([node, 'access', _832 => _832.expression, 'optionalAccess', _833 => _833.kind]) === _typescript2.default.SyntaxKind.ThisKeyword && _optionalChain([node, 'access', _834 => _834.name, 'optionalAccess', _835 => _835.kind]) === _typescript2.default.SyntaxKind.Identifier;
  }
  // the native Go text for a condition operand the printer can type, or undefined
  // when the operand has to go through the truthiness helper. `(x)` is decided on
  // its operand and keeps the source parentheses, so the surrounding operator still
  // parses exactly the same way.
  goNativeCondition(node) {
    if (_optionalChain([node, 'optionalAccess', _836 => _836.kind]) === _typescript2.default.SyntaxKind.Identifier) {
      return _nullishCoalesce(this.printInlineTruthy(node), () => ( this.printInlineBoolBoxTruthy(node)));
    }
    if (_optionalChain([node, 'optionalAccess', _837 => _837.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      const inner = this.goNativeCondition(node.expression);
      if (inner === void 0) {
        return void 0;
      }
      return inner.startsWith("(") && this.isWholePrintedCall(inner, 0) ? inner : `(${inner})`;
    }
    const printed = this.printNode(node, 0);
    const pointerTruthy = this.printInlineBoolPointerTruthy(node, printed);
    if (pointerTruthy !== void 0) {
      return pointerTruthy;
    }
    if (this.goTypeOfInitializer(node, printed) === "bool") {
      return printed;
    }
    if (_optionalChain([node, 'optionalAccess', _838 => _838.kind]) === _typescript2.default.SyntaxKind.CallExpression && (printed === "true" || printed === "false")) {
      return printed;
    }
    if (GO_BOOL_FIELDS.has(printed)) {
      return printed;
    }
    const callee = this.goPrintedCallee(printed);
    return callee !== void 0 && GO_BOOL_CALL_NAMES_NATIVE.indexOf(callee) >= 0 ? printed : void 0;
  }
  // gofmt prints the condition of every `if`/`for`/`switch` through
  // go/printer/nodes.go controlClause() -> stripParens(): the single outermost,
  // fully enclosing parentheses pair is dropped, and the rule applies again to the
  // enclosed expression while that one is parenthesized as well
  // (`if (x == 1) {` -> `if x == 1 {`, `if ((x == 1)) {` -> `if x == 1 {`).
  // Parentheses survive when the enclosed expression holds an unparenthesized
  // composite literal whose type is a type name, because `if T{} == x {` does not
  // parse. The printer emits text instead of an ast.Expr, so stripParens runs over
  // the printed condition text here.
  goControlClauseParens(node, expression) {
    if (!this.goIsControlClauseCondition(node)) {
      return expression;
    }
    let text = expression;
    for (; ; ) {
      const inner = this.goEnclosedExpression(text);
      if (inner === void 0) {
        return text;
      }
      text = inner;
    }
  }
  // the expression inside the outermost parentheses pair of `text`, or undefined
  // when `text` is not one fully enclosing pair or gofmt keeps that pair
  goEnclosedExpression(text) {
    const trimmed = text.trim();
    if (!trimmed.startsWith("(") || !trimmed.endsWith(")")) {
      return void 0;
    }
    if (this.goSkipBalanced(trimmed, 0, "(", ")") !== trimmed.length) {
      return void 0;
    }
    const inner = trimmed.substring(1, trimmed.length - 1).trim();
    if (this.goHasTypeNameCompositeLiteral(inner)) {
      return void 0;
    }
    return inner;
  }
  // the expression a Go `if`/`for`/`switch` statement tests, the only positions
  // gofmt's controlClause() rewrites
  goIsControlClauseCondition(node) {
    const parent = _optionalChain([node, 'optionalAccess', _839 => _839.parent]);
    switch (_optionalChain([parent, 'optionalAccess', _840 => _840.kind])) {
      case _typescript2.default.SyntaxKind.IfStatement:
      case _typescript2.default.SyntaxKind.WhileStatement:
      case _typescript2.default.SyntaxKind.SwitchStatement:
        return parent.expression === node;
      case _typescript2.default.SyntaxKind.ForStatement:
        return parent.condition === node;
    }
    return false;
  }
  // stripParens' ast.Inspect stops at nested parentheses, which protect whatever
  // they enclose, and reports a composite literal whenever its type is a type name
  goHasTypeNameCompositeLiteral(text) {
    let index = 0;
    while (index < text.length) {
      const char = text[index];
      if (char === '"' || char === "`" || char === "'") {
        index = this.goSkipQuoted(text, index);
        continue;
      }
      if (char === "(") {
        const next = this.goSkipBalanced(text, index, "(", ")");
        if (next < 0) {
          return false;
        }
        index = next;
        continue;
      }
      if (char === "{") {
        if (this.goCompositeLitHasTypeName(text, index)) {
          return true;
        }
        const next = this.goSkipBalanced(text, index, "{", "}");
        if (next < 0) {
          return false;
        }
        index = next;
        continue;
      }
      index += 1;
    }
    return false;
  }
  // `{` opens a composite literal whose type is a type name when the text in front
  // of it is an ident or a selector chain of idents; `map[string]any{` and `[]any{`
  // are type literals and do not count (isTypeName in go/printer/nodes.go)
  goCompositeLitHasTypeName(text, braceIndex) {
    let start = braceIndex;
    while (start > 0 && /[A-Za-z0-9_.[\]]/.test(text[start - 1])) {
      start -= 1;
    }
    const typeText = text.substring(start, braceIndex).trim();
    if (["map", "struct", "interface", "func", "chan"].indexOf(typeText) >= 0) {
      return false;
    }
    return /^[A-Za-z_]\w*(\.[A-Za-z_]\w*)*$/.test(typeText);
  }
  // the index right after the bracket closing the one at `start`, or -1 when the
  // brackets are unbalanced (the printer sees statement fragments, not whole files)
  goSkipBalanced(text, start, open, close) {
    let depth = 0;
    let index = start;
    while (index < text.length) {
      const char = text[index];
      if (char === '"' || char === "`" || char === "'") {
        index = this.goSkipQuoted(text, index);
        continue;
      }
      if (char === open) {
        depth += 1;
      } else if (char === close) {
        depth -= 1;
        if (depth === 0) {
          return index + 1;
        }
      }
      index += 1;
    }
    return -1;
  }
  // the index right after the string, rune or raw string literal opening at `start`
  goSkipQuoted(text, start) {
    const quote = text[start];
    let index = start + 1;
    while (index < text.length) {
      const char = text[index];
      if (char === "\\" && quote !== "`") {
        index += 2;
        continue;
      }
      if (char === quote) {
        return index + 1;
      }
      index += 1;
    }
    return text.length;
  }
  // gofmt keeps a comment group that the source separates from the following
  // declaration by a blank line as a free-standing comment; joined to it, it becomes
  // the declaration's doc comment and its indented lines are re-laid out as a code
  // block. The blank line is preserved so the emitted text keeps the source's shape.
  printLeadingComments(node, identation) {
    const printed = super.printLeadingComments(node, identation);
    if (printed.length === 0) {
      return printed;
    }
    const fullText = this.getSrc().getFullText();
    const ranges = _nullishCoalesce(_typescript2.default.getLeadingCommentRanges(fullText, node.pos), () => ( []));
    const last = ranges[ranges.length - 1];
    if (last === void 0) {
      return printed;
    }
    const gap = fullText.slice(last.end, node.getStart());
    const detached = (_nullishCoalesce(gap.match(/\n/g), () => ( []))).length > 1;
    return detached ? printed + "\n" : printed;
  }
  // gofmt separates a top-level declaration that carries a comment from the previous
  // declaration by a blank line (go/printer declList: min = 2 when the decl has a doc
  // comment); the file members are joined with a bare newline otherwise
  printSourceFileStatements(node, identation) {
    const outerImports = this.goFileStdlibImports;
    const fileImports = /* @__PURE__ */ new Set();
    this.goFileStdlibImports = fileImports;
    let body = "";
    try {
      const printed = node.statements.map((m) => this.printNode(m, identation + 1)).filter((st) => st.length > 0);
      body = printed.map((st, index) => index > 0 && /^\s*(\/\/|\/\*)/.test(st) ? "\n" + st : st).join("\n") + "\n".repeat(this.NUM_LINES_END_FILE);
    } finally {
      this.goFileStdlibImports = outerImports;
    }
    if (fileImports.size === 0) {
      return body;
    }
    return [...fileImports].sort().map((path3) => `import "${path3}"`).join("\n") + "\n\n" + body;
  }
  printNode(node, identation = 0) {
    if (node !== void 0 && _typescript2.default.isSourceFile(node)) {
      this.className = "undefined";
      return this.printSourceFileStatements(node, identation);
    }
    const isStatement = node !== void 0 && _typescript2.default.isStatement(node) && node.kind !== _typescript2.default.SyntaxKind.Block;
    const previousLevel = this.goStatementLevel;
    if (isStatement) {
      this.goStatementLevel = identation;
    }
    try {
      const printed = super.printNode(node, identation);
      return this.goControlClauseParens(node, printed);
    } finally {
      this.goStatementLevel = previousLevel;
    }
  }
  // composite literal body one level deeper than the statement, closing brace at the
  // statement's level; the leading indentation belongs to the enclosing printer
  printObjectLiteralExpression(node, identation) {
    const level = this.goStatementLevel;
    const objectBody = this.printObjectLiteralBody(node, level);
    const formattedObjectBody = objectBody ? "\n" + objectBody + "\n" + this.getIden(level) : objectBody;
    return this.OBJECT_OPENING + formattedObjectBody + this.OBJECT_CLOSING;
  }
  printCondition(node, identation) {
    const native = this.goNativeCondition(node);
    if (native !== void 0) {
      return `${this.getIden(identation)}${this.goControlClauseParens(node, native)}`;
    }
    const inNode = _optionalChain([node, 'optionalAccess', _841 => _841.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression ? node.expression : node;
    if (_optionalChain([inNode, 'optionalAccess', _842 => _842.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && inNode.operatorToken.kind === _typescript2.default.SyntaxKind.InKeyword) {
      const inlined = this.printInlineInOp(inNode.right, inNode.left, this.printNode(inNode.right, 0), this.printNode(inNode.left, 0));
      if (inlined !== void 0) {
        const text = inNode === node ? inlined : `(${inlined})`;
        return `${this.getIden(identation)}${this.goControlClauseParens(node, text)}`;
      }
    }
    return super.printCondition(node, identation);
  }
  // === / !== inlined to plain Go operators when both sides are concrete Go
  // values or real pointers. Everything else — in particular anything that is
  // still `any` in Go — falls through to the existing IsEqual helper.
  // `(a == b || *a == *b)` is rejected: a nil *T panics on the second clause.
  // Go has no implicit numeric conversion: `*limit == length` does not compile
  // when limit is *int64 and length is int, even though TypeScript calls both
  // `number`. Dereferencing is therefore only safe against an untyped constant
  // (a literal, which adapts to the pointee) or an operand of the very same Go
  // type. Anything else — including any `any` operand — keeps IsEqual.
  goDerefComparableWith(ptrNode, ptrText, otherNode) {
    const pointee = _optionalChain([this, 'access', _843 => _843.goPointerTypeOfExpression, 'call', _844 => _844(ptrNode, ptrText), 'optionalAccess', _845 => _845.substring, 'call', _846 => _846(1)]);
    if (pointee === void 0) {
      return false;
    }
    switch (_optionalChain([otherNode, 'optionalAccess', _847 => _847.kind])) {
      case _typescript2.default.SyntaxKind.StringLiteral:
      case _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral:
        return pointee === "string";
      case _typescript2.default.SyntaxKind.NumericLiteral:
        return pointee === "int" || pointee === "int64" || pointee === "float64";
      case _typescript2.default.SyntaxKind.TrueKeyword:
      case _typescript2.default.SyntaxKind.FalseKeyword:
        return pointee === "bool";
    }
    const otherType = this.goDeclaredTypeOfIdentifier(otherNode);
    return otherType === pointee;
  }
  // a node the printer prints as a Go string constant: both literal forms are
  // emitted with the printer's string quote token, so the comparison is a plain
  // Go string comparison
  goIsStringLiteralNode(node) {
    if (node === void 0) {
      return false;
    }
    return node.kind === _typescript2.default.SyntaxKind.StringLiteral || node.kind === _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral;
  }
  // the deref arms print their operand twice, so it must read the same value both times: an
  // identifier is a plain read, and a string accessor call whose arguments are all
  // identifiers/literals re-reads them (no statement runs between the two evaluations).
  goDerefRepeatableOperand(node, printedText) {
    if (_optionalChain([node, 'optionalAccess', _848 => _848.kind]) === _typescript2.default.SyntaxKind.Identifier) {
      return true;
    }
    if (_optionalChain([node, 'optionalAccess', _849 => _849.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return false;
    }
    if (GO_PURE_STRING_ACCESSORS.indexOf(this.goPrintedCallee(printedText)) < 0) {
      return false;
    }
    return node.arguments.every((argument) => this.goIsReadOnlyCallArgument(argument));
  }
  // an argument whose evaluation is a plain read: a value the printer already
  // holds, never a call that could observe or cause a change
  goIsReadOnlyCallArgument(node) {
    while (_optionalChain([node, 'optionalAccess', _850 => _850.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      node = node.expression;
    }
    switch (_optionalChain([node, 'optionalAccess', _851 => _851.kind])) {
      case _typescript2.default.SyntaxKind.Identifier:
      case _typescript2.default.SyntaxKind.StringLiteral:
      case _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral:
      case _typescript2.default.SyntaxKind.NumericLiteral:
      case _typescript2.default.SyntaxKind.TrueKeyword:
      case _typescript2.default.SyntaxKind.FalseKeyword:
      case _typescript2.default.SyntaxKind.NullKeyword:
        return true;
    }
    return false;
  }
  // true when the Go value of this operand is a bare `string`: a local declared `string`, or a
  // parameter the signature printer emits as `string`. A Go string holds neither nil nor a pointer,
  // so `x == "lit"` is exactly IsEqual(x, "lit") — the pointer arms never see this type.
  goIsBareStringOperand(node) {
    if (this.goDeclaredTypeOfIdentifier(node) === "string") {
      return true;
    }
    if (_optionalChain([node, 'optionalAccess', _852 => _852.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const symbol = checker.getSymbolAtLocation(node);
    const declaration = _optionalChain([symbol, 'optionalAccess', _853 => _853.valueDeclaration]);
    if (_optionalChain([declaration, 'optionalAccess', _854 => _854.kind]) !== _typescript2.default.SyntaxKind.Parameter) {
      return false;
    }
    return this.printParameterType(declaration) === "string";
  }
  printInlineEquality(left, right, leftText, rightText, isEq) {
    const lPtr = this.goPointerTypeOfExpression(left, leftText) !== void 0;
    const rPtr = this.goPointerTypeOfExpression(right, rightText) !== void 0;
    const lRepeatable = this.goDerefRepeatableOperand(left, leftText);
    const rRepeatable = this.goDerefRepeatableOperand(right, rightText);
    const lFam = this.goScalarFamily(left);
    const rFam = this.goScalarFamily(right);
    if (lFam === "nil" && rPtr) {
      return isEq ? `(${rightText} == nil)` : `(${rightText} != nil)`;
    }
    if (rFam === "nil" && lPtr) {
      return isEq ? `(${leftText} == nil)` : `(${leftText} != nil)`;
    }
    if (lPtr && rPtr) {
      const lPointee = this.goPointerTypeOfExpression(left, leftText);
      if (!lRepeatable || !rRepeatable || lPointee !== this.goPointerTypeOfExpression(right, rightText)) {
        return void 0;
      }
      if (isEq) {
        return `(${leftText} == ${rightText} || (${leftText} != nil && ${rightText} != nil && *${leftText} == *${rightText}))`;
      }
      return `(${leftText} != ${rightText} && (${leftText} == nil || ${rightText} == nil || *${leftText} != *${rightText}))`;
    }
    if (lPtr && rFam !== void 0 && rFam !== "nil") {
      if (!lRepeatable || !this.goDerefComparableWith(left, leftText, right)) {
        return void 0;
      }
      return isEq ? `(${leftText} != nil && *${leftText} == ${rightText})` : `(${leftText} == nil || *${leftText} != ${rightText})`;
    }
    if (rPtr && lFam !== void 0 && lFam !== "nil") {
      if (!rRepeatable || !this.goDerefComparableWith(right, rightText, left)) {
        return void 0;
      }
      return isEq ? `(${rightText} != nil && *${rightText} == ${leftText})` : `(${rightText} == nil || *${rightText} != ${leftText})`;
    }
    if (!lPtr && !rPtr && lFam !== void 0 && rFam !== void 0 && lFam !== "nil" && rFam !== "nil" && lFam === rFam) {
      return isEq ? `(${leftText} == ${rightText})` : `(${leftText} != ${rightText})`;
    }
    if (!lPtr && !rPtr) {
      if (this.goIsBareStringOperand(left) && this.goIsStringLiteralNode(right)) {
        return isEq ? `(${leftText} == ${rightText})` : `(${leftText} != ${rightText})`;
      }
      if (this.goIsBareStringOperand(right) && this.goIsStringLiteralNode(left)) {
        return isEq ? `(${leftText} == ${rightText})` : `(${leftText} != ${rightText})`;
      }
    }
    const lNilFam = this.goScalarFamilyWithNil(left);
    const rNilFam = this.goScalarFamilyWithNil(right);
    const lBox = !lPtr && this.goIsAnyBoxExpression(left, leftText);
    const rBox = !rPtr && this.goIsAnyBoxExpression(right, rightText);
    if (lBox && rFam === "nil" && lNilFam !== void 0 && lNilFam !== "number") {
      return isEq ? `(${leftText} == nil)` : `(${leftText} != nil)`;
    }
    if (rBox && lFam === "nil" && rNilFam !== void 0 && rNilFam !== "number") {
      return isEq ? `(${rightText} == nil)` : `(${rightText} != nil)`;
    }
    const lObjParam = lNilFam === void 0 && lBox && this.goObjectBoxParameter(left);
    const rObjParam = rNilFam === void 0 && rBox && this.goObjectBoxParameter(right);
    if (lObjParam && rFam === "nil") {
      return isEq ? `(${leftText} == nil)` : `(${leftText} != nil)`;
    }
    if (rObjParam && lFam === "nil") {
      return isEq ? `(${rightText} == nil)` : `(${rightText} != nil)`;
    }
    if (lBox && rFam === "nil" && this.goAnyLocalHoldsNonPointer(this.goAnyBoxLocalDeclaration(left))) {
      return isEq ? `(${leftText} == nil)` : `(${leftText} != nil)`;
    }
    if (rBox && lFam === "nil" && this.goAnyLocalHoldsNonPointer(this.goAnyBoxLocalDeclaration(right))) {
      return isEq ? `(${rightText} == nil)` : `(${rightText} != nil)`;
    }
    if (lNilFam === "number" && rFam === "nil" && this.goGetArgBoundParameter(left)) {
      return isEq ? `(${leftText} == nil)` : `(${leftText} != nil)`;
    }
    if (rNilFam === "number" && lFam === "nil" && this.goGetArgBoundParameter(right)) {
      return isEq ? `(${rightText} == nil)` : `(${rightText} != nil)`;
    }
    const isLiteral = (node) => {
      switch (_optionalChain([node, 'optionalAccess', _855 => _855.kind])) {
        case _typescript2.default.SyntaxKind.StringLiteral:
        case _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral:
        case _typescript2.default.SyntaxKind.TrueKeyword:
        case _typescript2.default.SyntaxKind.FalseKeyword:
          return true;
      }
      return false;
    };
    const literalMatchesBox = (boxFam, litNode, litFam) => (litFam === "string" || litFam === "bool") && isLiteral(litNode) && (boxFam === litFam || boxFam === void 0);
    if (lBox && literalMatchesBox(lNilFam, right, rFam)) {
      return isEq ? `(${leftText} == ${rightText})` : `(${leftText} != ${rightText})`;
    }
    if (rBox && literalMatchesBox(rNilFam, left, lFam)) {
      return isEq ? `(${leftText} == ${rightText})` : `(${leftText} != ${rightText})`;
    }
    const isBoolLiteral = (node) => _optionalChain([node, 'optionalAccess', _856 => _856.kind]) === _typescript2.default.SyntaxKind.TrueKeyword || _optionalChain([node, 'optionalAccess', _857 => _857.kind]) === _typescript2.default.SyntaxKind.FalseKeyword;
    if (!lPtr && isBoolLiteral(right) && this.goScalarFamilyWithNil(left) === "bool" && this.goBoxedElementRead(left, leftText)) {
      return isEq ? `(${leftText} == ${rightText})` : `(${leftText} != ${rightText})`;
    }
    if (!rPtr && isBoolLiteral(left) && this.goScalarFamilyWithNil(right) === "bool" && this.goBoxedElementRead(right, rightText)) {
      return isEq ? `(${leftText} == ${rightText})` : `(${leftText} != ${rightText})`;
    }
    if (lBox && rBox && lNilFam !== void 0 && lNilFam !== "number" && lNilFam === rNilFam) {
      return isEq ? `(${leftText} == ${rightText})` : `(${leftText} != ${rightText})`;
    }
    return void 0;
  }
  // the Go numeric kind an operand's static type is, or undefined when it stays
  // `any` (unknown helper result, union, pointer box): only a concrete kind can
  // join a comparison the Go compiler accepts
  goOperandNumericKind(node, printedText) {
    if (node === void 0) {
      return void 0;
    }
    if (node.kind === _typescript2.default.SyntaxKind.Identifier) {
      const declared = this.goDeclaredTypeOfIdentifier(node);
      if (declared !== void 0) {
        return GO_NUMERIC_KINDS.indexOf(declared) >= 0 ? declared : void 0;
      }
      return this.goLiteralTypedLocalKind(node);
    }
    if (node.kind === _typescript2.default.SyntaxKind.NumericLiteral) {
      return this.goNumericLiteralKind(node);
    }
    if (this.goIsSignedNumericLiteral(node)) {
      return this.goNumericLiteralKind(node);
    }
    if (node.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.goOperandNumericKind(node.expression, this.printNode(node.expression, 0));
    }
    const goType = this.goTypeOfInitializer(node, printedText);
    return goType !== void 0 && GO_NUMERIC_KINDS.indexOf(goType) >= 0 ? goType : void 0;
  }
  // a local bound by `:=` takes its Go type from the initializer: an untyped
  // integer constant is `int`, a floating-point one is `float64`. A variable
  // statement prints `var x <type> = …` instead, where the printer already
  // decided the type, so only the `:=` form may be trusted here.
  goLiteralTypedLocalKind(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const symbol = checker.getSymbolAtLocation(node);
    const declaration = _optionalChain([symbol, 'optionalAccess', _858 => _858.valueDeclaration]);
    if (_optionalChain([declaration, 'optionalAccess', _859 => _859.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration || declaration.initializer === void 0) {
      return void 0;
    }
    const declarationList = declaration.parent;
    if (_optionalChain([declarationList, 'optionalAccess', _860 => _860.kind]) !== _typescript2.default.SyntaxKind.VariableDeclarationList || _optionalChain([declarationList, 'access', _861 => _861.parent, 'optionalAccess', _862 => _862.kind]) === _typescript2.default.SyntaxKind.FirstStatement) {
      return void 0;
    }
    return this.goNumericLiteralKind(declaration.initializer);
  }
  // an untyped Go constant: the integer forms adopt any numeric kind, the
  // floating-point ones only fit float64. A signed literal is the same constant:
  // the printer writes `-1` as text, never through OpNeg's box.
  goNumericLiteralKind(node) {
    if (this.goIsSignedNumericLiteral(node)) {
      return this.goNumericLiteralKind(node.operand);
    }
    const text = _optionalChain([node, 'optionalAccess', _863 => _863.text]);
    if (text === void 0) {
      return void 0;
    }
    if (/^[0-9][0-9_]*$/.test(text) || /^0[xXoObB][0-9a-fA-F_]+$/.test(text)) {
      return "int";
    }
    if (/^[0-9][.eE]/.test(text)) {
      return "float64";
    }
    return void 0;
  }
  // `-1` / `+1.5` as written: a sign on a numeric literal, the only prefix-unary
  // shape that reaches a comparison as a constant rather than as OpNeg(...)
  goIsSignedNumericLiteral(node) {
    if (_optionalChain([node, 'optionalAccess', _864 => _864.kind]) !== _typescript2.default.SyntaxKind.PrefixUnaryExpression) {
      return false;
    }
    if (node.operator !== _typescript2.default.SyntaxKind.MinusToken && node.operator !== _typescript2.default.SyntaxKind.PlusToken) {
      return false;
    }
    return _optionalChain([node, 'access', _865 => _865.operand, 'optionalAccess', _866 => _866.kind]) === _typescript2.default.SyntaxKind.NumericLiteral;
  }
  // a numeric constant operand: an integer/float literal, with an optional sign
  goIsNumericConstant(node) {
    return _optionalChain([node, 'optionalAccess', _867 => _867.kind]) === _typescript2.default.SyntaxKind.NumericLiteral || this.goIsSignedNumericLiteral(node);
  }
  // the constant's value with its sign; NaN for anything but a numeric constant
  goNumericConstantValue(node) {
    if (this.goIsSignedNumericLiteral(node)) {
      const value = Number(node.operand.text.replaceAll("_", ""));
      return node.operator === _typescript2.default.SyntaxKind.MinusToken ? -value : value;
    }
    if (_optionalChain([node, 'optionalAccess', _868 => _868.kind]) !== _typescript2.default.SyntaxKind.NumericLiteral) {
      return Number.NaN;
    }
    return Number(node.text.replaceAll("_", ""));
  }
  // a constant only joins a comparison when its value is representable in the
  // other operand's kind: `0.5` is not an int, and neither is 1e400 (infinity)
  goLiteralFitsKind(node, kind) {
    const value = this.goNumericConstantValue(node);
    if (!Number.isFinite(value)) {
      return false;
    }
    if (kind === "float64") {
      return true;
    }
    return Number.isInteger(value) && Math.abs(value) <= 2147483647;
  }
  // the kind both operands are compared in, or undefined when Go would need a
  // conversion (two different concrete kinds) or the constant does not fit
  goComparisonKind(left, leftKind, right, rightKind) {
    if (leftKind === rightKind) {
      return leftKind;
    }
    if (this.goIsNumericConstant(left) && !this.goIsNumericConstant(right)) {
      return this.goLiteralFitsKind(left, rightKind) ? rightKind : void 0;
    }
    if (this.goIsNumericConstant(right) && !this.goIsNumericConstant(left)) {
      return this.goLiteralFitsKind(right, leftKind) ? leftKind : void 0;
    }
    return void 0;
  }
  // `<` `>` `<=` `>=` between two operands the checker proves to be numbers of the
  // same Go kind is exactly the comparison the helper performs, minus the interface
  // round-trip, so the call carries no information. A `*int64` / `*float64` operand
  // joins them with its deref written out, and the helper's nil predicate with it.
  // Everything else — `any` boxes, mixed kinds, strings — keeps the helper. float64
  // keeps IsLessThan/IsLessThanOrEqual: the helper answers true whenever an operand
  // is NaN while Go (and JS) answer false, and only those two operators differ.
  printInlineOrderedComparison(left, right, leftText, rightText, op) {
    const operator = ORDERED_COMPARISON_OPERATORS[op];
    if (operator === void 0) {
      return void 0;
    }
    const leftPointee = this.goOrderedComparisonPointerKind(left);
    const rightPointee = this.goOrderedComparisonPointerKind(right);
    if (leftPointee !== void 0 || rightPointee !== void 0) {
      return this.printPointerOrderedComparison(left, right, leftText, rightText, operator, leftPointee, rightPointee);
    }
    const leftKind = this.goOperandNumericKind(left, leftText);
    const rightKind = this.goOperandNumericKind(right, rightText);
    if (leftKind === void 0 || rightKind === void 0) {
      return void 0;
    }
    const kind = this.goComparisonKind(left, leftKind, right, rightKind);
    if (kind === void 0) {
      return void 0;
    }
    if (kind === "float64" && (operator === "<" || operator === "<=")) {
      return void 0;
    }
    return `(${leftText} ${operator} ${rightText})`;
  }
  // the pointee kind of a `*int64` / `*float64` **identifier** operand. Only a local
  // the printer itself declared as that pointer qualifies: a call would be repeated by
  // the nil test, and an `any` box holds a value the printer cannot name.
  goOrderedComparisonPointerKind(node) {
    if (_optionalChain([node, 'optionalAccess', _869 => _869.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const declared = this.goDeclaredTypeOfIdentifier(node);
    if (declared !== "*int64" && declared !== "*float64") {
      return void 0;
    }
    return declared.substring(1);
  }
  // an ordered comparison with a `*int64` / `*float64` operand on at least one side. The helper
  // derefs both sides and answers its own predicate when one is nil, so the native form spells that
  // out; an enclosing `x !== undefined` guard (printed as `x != nil`) removes it for that side.
  printPointerOrderedComparison(left, right, leftText, rightText, operator, leftPointee, rightPointee) {
    const leftKind = leftPointee === void 0 ? this.goOperandNumericKind(left, leftText) : leftPointee;
    const rightKind = rightPointee === void 0 ? this.goOperandNumericKind(right, rightText) : rightPointee;
    if (leftKind === void 0 || rightKind === void 0) {
      return void 0;
    }
    const kind = this.goComparisonKind(left, leftKind, right, rightKind);
    if (kind === void 0) {
      return void 0;
    }
    if (kind === "float64" && (operator === "<" || operator === "<=")) {
      return void 0;
    }
    const leftNil = leftPointee !== void 0 && !this.goHasEnclosingNilGuard(left);
    const rightNil = rightPointee !== void 0 && !this.goHasEnclosingNilGuard(right);
    if (leftNil && rightPointee === void 0 && !this.goIsPureComparisonOperand(right)) {
      return void 0;
    }
    if (rightNil && leftPointee === void 0 && !this.goIsPureComparisonOperand(left)) {
      return void 0;
    }
    const lv = leftPointee === void 0 ? leftText : `*${leftText}`;
    const rv = rightPointee === void 0 ? rightText : `*${rightText}`;
    if (!leftNil && !rightNil) {
      return `(${lv} ${operator} ${rv})`;
    }
    if (leftNil && !rightNil) {
      if (operator === ">") {
        return `(${leftText} != nil && ${lv} > ${rv})`;
      }
      if (operator === ">=") {
        return `(${leftText} != nil && ${lv} >= ${rv})`;
      }
      const sign = operator === "<" ? "<" : "<=";
      return `(${leftText} == nil || ${lv} ${sign} ${rv})`;
    }
    if (!leftNil && rightNil) {
      if (operator === ">") {
        return `(${rightText} == nil || ${lv} > ${rv})`;
      }
      if (operator === ">=") {
        return `(${rightText} == nil || ${lv} >= ${rv})`;
      }
      const sign = operator === "<" ? "<" : "<=";
      return `(${rightText} != nil && ${lv} ${sign} ${rv})`;
    }
    if (operator === ">") {
      return `(${leftText} != nil && (${rightText} == nil || ${lv} > ${rv}))`;
    }
    if (operator === ">=") {
      return `(${rightText} == nil || (${leftText} != nil && ${lv} >= ${rv}))`;
    }
    if (operator === "<") {
      return `(${rightText} != nil && (${leftText} == nil || ${lv} < ${rv}))`;
    }
    return `(${leftText} == nil || (${rightText} != nil && ${lv} <= ${rv}))`;
  }
  // `x !== undefined` / `x != null` on that identifier: the printer writes the Go
  // `x != nil` test for it, so an enclosing one proves the pointer present
  goIsNonNilTestOf(node, ident) {
    if (_optionalChain([node, 'optionalAccess', _870 => _870.kind]) !== _typescript2.default.SyntaxKind.BinaryExpression) {
      return false;
    }
    const op = _optionalChain([node, 'access', _871 => _871.operatorToken, 'optionalAccess', _872 => _872.kind]);
    if (op !== _typescript2.default.SyntaxKind.ExclamationEqualsToken && op !== _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken) {
      return false;
    }
    return this.goIsSameSymbol(node.left, ident) && this.goIsNilLiteral(node.right) || this.goIsSameSymbol(node.right, ident) && this.goIsNilLiteral(node.left);
  }
  // a null/undefined literal, or any expression the checker types as one
  goIsNilLiteral(node) {
    if (_optionalChain([node, 'optionalAccess', _873 => _873.kind]) === _typescript2.default.SyntaxKind.NullKeyword || _optionalChain([node, 'optionalAccess', _874 => _874.kind]) === _typescript2.default.SyntaxKind.UndefinedKeyword) {
      return true;
    }
    return _optionalChain([node, 'optionalAccess', _875 => _875.kind]) === _typescript2.default.SyntaxKind.Identifier && this.goScalarFamily(node) === "nil";
  }
  goIsSameSymbol(a, b) {
    if (_optionalChain([a, 'optionalAccess', _876 => _876.kind]) !== _typescript2.default.SyntaxKind.Identifier || _optionalChain([b, 'optionalAccess', _877 => _877.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    try {
      const checker = this.getChecker();
      const leftSymbol = checker.getSymbolAtLocation(a);
      return leftSymbol !== void 0 && leftSymbol === checker.getSymbolAtLocation(b);
    } catch (e) {
      return false;
    }
  }
  // true when the enclosing control flow already proves the identifier non-nil at this node: an
  // earlier conjunct of an `&&` chain, or the condition of a wrapping if/loop/ternary containing the
  // node. A local rebound anywhere in its function never counts — the guard may be dead by then.
  goHasEnclosingNilGuard(ident) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const declaration = _optionalChain([checker, 'access', _878 => _878.getSymbolAtLocation, 'call', _879 => _879(ident), 'optionalAccess', _880 => _880.valueDeclaration]);
    if (this.goLocalIsRebound(this.goEnclosingFunction(_nullishCoalesce(declaration, () => ( ident))), ident)) {
      return false;
    }
    let child = ident;
    let parent = _optionalChain([ident, 'optionalAccess', _881 => _881.parent]);
    while (parent !== void 0) {
      const kind = parent.kind;
      if (kind === _typescript2.default.SyntaxKind.ParenthesizedExpression || kind === _typescript2.default.SyntaxKind.VariableStatement || kind === _typescript2.default.SyntaxKind.ExpressionStatement || kind === _typescript2.default.SyntaxKind.Block || kind === _typescript2.default.SyntaxKind.ReturnStatement || kind === _typescript2.default.SyntaxKind.ElementAccessExpression || kind === _typescript2.default.SyntaxKind.CallExpression || kind === _typescript2.default.SyntaxKind.PropertyAccessExpression || kind === _typescript2.default.SyntaxKind.ObjectLiteralExpression || kind === _typescript2.default.SyntaxKind.PropertyAssignment || kind === _typescript2.default.SyntaxKind.ArrayLiteralExpression || kind === _typescript2.default.SyntaxKind.AwaitExpression || kind === _typescript2.default.SyntaxKind.CaseClause || kind === _typescript2.default.SyntaxKind.SwitchStatement || kind === _typescript2.default.SyntaxKind.DoStatement || kind === _typescript2.default.SyntaxKind.LabeledStatement) {
        child = parent;
        parent = parent.parent;
        continue;
      }
      if (kind === _typescript2.default.SyntaxKind.BinaryExpression) {
        const op = _optionalChain([parent, 'access', _882 => _882.operatorToken, 'optionalAccess', _883 => _883.kind]);
        if (op === _typescript2.default.SyntaxKind.AmpersandAmpersandToken && child === parent.right && this.goConditionProvesNonNil(parent.left, ident)) {
          return true;
        }
        child = parent;
        parent = parent.parent;
        continue;
      }
      if (kind === _typescript2.default.SyntaxKind.IfStatement) {
        if (child === parent.thenStatement && this.goConditionProvesNonNil(parent.expression, ident)) {
          return true;
        }
        child = parent;
        parent = parent.parent;
        continue;
      }
      if (kind === _typescript2.default.SyntaxKind.WhileStatement) {
        if (child === parent.statement && this.goConditionProvesNonNil(parent.expression, ident)) {
          return true;
        }
        child = parent;
        parent = parent.parent;
        continue;
      }
      if (kind === _typescript2.default.SyntaxKind.ForStatement) {
        if (child === parent.statement && this.goConditionProvesNonNil(parent.condition, ident)) {
          return true;
        }
        child = parent;
        parent = parent.parent;
        continue;
      }
      if (kind === _typescript2.default.SyntaxKind.ConditionalExpression) {
        if (child === parent.whenTrue && this.goConditionProvesNonNil(parent.condition, ident)) {
          return true;
        }
        child = parent;
        parent = parent.parent;
        continue;
      }
      return false;
    }
    return false;
  }
  // the whole condition, or any conjunct of its `&&` chain, is the non-nil test
  goConditionProvesNonNil(condition, ident) {
    const inner = this.goUnwrapParenthesizedNode(condition);
    if (inner === void 0) {
      return false;
    }
    if (this.goIsNonNilTestOf(inner, ident)) {
      return true;
    }
    if (inner.kind !== _typescript2.default.SyntaxKind.BinaryExpression) {
      return false;
    }
    if (_optionalChain([inner, 'access', _884 => _884.operatorToken, 'optionalAccess', _885 => _885.kind]) !== _typescript2.default.SyntaxKind.AmpersandAmpersandToken) {
      return false;
    }
    return this.goConditionProvesNonNil(inner.left, ident) || this.goConditionProvesNonNil(inner.right, ident);
  }
  goUnwrapParenthesizedNode(node) {
    let current = node;
    while (_optionalChain([current, 'optionalAccess', _886 => _886.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      current = current.expression;
    }
    return current;
  }
  // an operand a short-circuit may skip without any observable difference: no calls,
  // no assignments, nothing the helper would have evaluated exactly once
  goIsPureComparisonOperand(node) {
    switch (_optionalChain([node, 'optionalAccess', _887 => _887.kind])) {
      case _typescript2.default.SyntaxKind.Identifier:
      case _typescript2.default.SyntaxKind.NumericLiteral:
      case _typescript2.default.SyntaxKind.StringLiteral:
      case _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral:
      case _typescript2.default.SyntaxKind.PropertyAccessExpression:
        return true;
      case _typescript2.default.SyntaxKind.PrefixUnaryExpression:
        return node.operator === _typescript2.default.SyntaxKind.MinusToken && this.goIsPureComparisonOperand(node.operand);
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
        return this.goIsPureComparisonOperand(node.expression);
    }
    return false;
  }
  // Go's printer never wraps an already parenthesised expression: gofmt prints a
  // ParenExpr whose child is itself a ParenExpr without its own parentheses
  // (`((x))` prints as `(x)`), because the text it is handed is re-parsed that way.
  // Our output is re-parsed exactly like that, so a source parenthesis around an
  // expression that already prints parenthesised -- an inlined comparison, a nested
  // parenthesised expression, an EvalTruthy(...) arm -- must emit the single pair
  // gofmt keeps instead of doubling it.
  printParenthesizedExpression(node, identation) {
    const expression = node.expression;
    if (_optionalChain([expression, 'optionalAccess', _888 => _888.kind]) === _typescript2.default.SyntaxKind.AsExpression) {
      return this.getIden(identation) + this.printNode(expression, 0);
    }
    if (_optionalChain([expression, 'optionalAccess', _889 => _889.kind]) === _typescript2.default.SyntaxKind.ArrowFunction) {
      return "";
    }
    const printed = this.goWithExprDepth(this.goExprDepth - 1, () => this.printNode(expression, 0));
    if (this.goIsParenthesizedExpression(printed)) {
      return this.getIden(identation) + printed;
    }
    return this.getIden(identation) + this.LEFT_PARENTHESIS + printed + this.RIGHT_PARENTHESIS;
  }
  // true when the printed text is exactly one parenthesised expression: its first
  // `(` closes on the last non-space character. Literals and comments are skipped
  // so a parenthesis inside them cannot unbalance the scan.
  goIsParenthesizedExpression(printed) {
    const text = printed.trimStart();
    if (text[0] !== "(") {
      return false;
    }
    let depth = 0;
    for (let i = 0; i < text.length; i++) {
      const c = text[i];
      if (c === "/" && text[i + 1] === "/") {
        return false;
      }
      if (c === "/" && text[i + 1] === "*") {
        const end = text.indexOf("*/", i + 2);
        if (end < 0) {
          return false;
        }
        i = end + 1;
        continue;
      }
      if (c === '"' || c === "'" || c === "`") {
        i = this.goSkipGoLiteral(text, i);
        if (i < 0) {
          return false;
        }
        continue;
      }
      if (c === "(") {
        depth += 1;
      } else if (c === ")") {
        depth -= 1;
        if (depth === 0) {
          return text.substring(i + 1).trim().length === 0;
        }
      }
    }
    return false;
  }
  // index of the quote closing the Go string/rune literal that starts at `start`, -1 when unterminated
  goSkipGoLiteral(text, start) {
    const quote = text[start];
    for (let i = start + 1; i < text.length; i++) {
      const c = text[i];
      if (c === "\\") {
        i += 1;
        continue;
      }
      if (c === quote) {
        return i;
      }
    }
    return -1;
  }
  // castVariableAssignmentIfNeeded(left, right, identation) {
  //     const leftType = this.getChecker().getTypeAtLocation(left);
  //     const rightType = this.getChecker().getTypeAtLocation(right);
  //     const leftText = this.printNode(left, 0);
  //     const rightText = this.printNode(right, 0);
  //     if (this.isAnyType(rightType.flags) && !this.isAnyType(leftType.flags)) {
  //         const parsedType = this.getTypeFromRawType(leftType);
  //         return `${this.getIden(identation)}${leftText} = (${parsedType})${rightText}`;
  //     }
  //     return undefined;
  // }
  transformPropertyAcessExpressionIfNeeded(node) {
    const expression = node.expression;
    const leftSide = this.printNode(expression, 0);
    const rightSide = node.name.escapedText;
    let rawExpression = void 0;
    switch (rightSide) {
      case "length":
        const type = this.getChecker().getTypeAtLocation(expression);
        rawExpression = _nullishCoalesce(this.printInlineArrayLength(expression, leftSide, node), () => ( (this.isStringType(type.flags) ? `GetLength(${leftSide})` : `${this.ARRAY_LENGTH_WRAPPER_OPEN}${leftSide}${this.ARRAY_LENGTH_WRAPPER_CLOSE}`)));
        break;
      case "push":
        rawExpression = `((IList<object>)${leftSide}).Add`;
        break;
    }
    return rawExpression;
  }
  printCustomDefaultValueIfNeeded(node) {
    return void 0;
  }
  // The Go type of a GetArg-bound optional local (from the printed default, or the declared type
  // for a nil default), or undefined to keep `any`; the retype needs goLocalIsSafeToType,
  // goParameterKeepsNilCompareNative and goGetArgConsumersAreSafe to agree.
  goGetArgLocalType(body, param, printedDefault) {
    const method = _optionalChain([param, 'optionalAccess', _890 => _890.parent]);
    const excluded = GO_GETARG_EXCLUDED_POSITIONS[_optionalChain([method, 'optionalAccess', _891 => _891.name, 'optionalAccess', _892 => _892.escapedText])];
    if (excluded !== void 0 && excluded.includes(method.parameters.indexOf(param))) {
      return void 0;
    }
    if (_optionalChain([param, 'optionalAccess', _893 => _893.type, 'optionalAccess', _894 => _894.kind]) === _typescript2.default.SyntaxKind.AnyKeyword && !this.goGetArgBaseParamIsUnannotated(param)) {
      return void 0;
    }
    const shape = (_nullishCoalesce(printedDefault, () => ( ""))).trim();
    const byDefault = this.goGetArgTypeOfShape(shape);
    if (byDefault !== void 0) {
      if (_optionalChain([param, 'optionalAccess', _895 => _895.type]) !== void 0 && param.type.kind !== _typescript2.default.SyntaxKind.AnyKeyword && !this.goGetArgDeclaredTypeCandidates(param).some((t) => t.replace(/^\*/, "") === byDefault)) {
        return void 0;
      }
      return this.goGetArgLocalIsSafe(body, param, byDefault) ? byDefault : void 0;
    }
    if (shape !== "nil" && shape !== "undefined") {
      return void 0;
    }
    for (const goType of this.goGetArgDeclaredTypeCandidates(param)) {
      if (this.goGetArgLocalIsSafe(body, param, goType, true)) {
        return goType;
      }
    }
    return void 0;
  }
  // true when the method overrides a base method whose parameter at the same position has no
  // type annotation and a default of the same syntax kind (the base binds it through its twin)
  goGetArgBaseParamIsUnannotated(param) {
    const method = _optionalChain([param, 'optionalAccess', _896 => _896.parent]);
    const name = _optionalChain([method, 'optionalAccess', _897 => _897.name, 'optionalAccess', _898 => _898.escapedText]);
    const index = _nullishCoalesce(_optionalChain([method, 'optionalAccess', _899 => _899.parameters, 'optionalAccess', _900 => _900.indexOf, 'call', _901 => _901(param)]), () => ( -1));
    const cls = _optionalChain([method, 'optionalAccess', _902 => _902.parent]);
    const checker = this.checkerOrUndefined();
    if (name === void 0 || index < 0 || checker === void 0 || param.initializer === void 0) {
      return false;
    }
    for (const clause of _nullishCoalesce(_optionalChain([cls, 'optionalAccess', _903 => _903.heritageClauses]), () => ( []))) {
      if (clause.token !== _typescript2.default.SyntaxKind.ExtendsKeyword) {
        continue;
      }
      for (const expr of _nullishCoalesce(clause.types, () => ( []))) {
        const baseDecl = _optionalChain([checker, 'access', _904 => _904.getTypeAtLocation, 'call', _905 => _905(expr), 'optionalAccess', _906 => _906.getProperty, 'optionalCall', _907 => _907(name), 'optionalAccess', _908 => _908.valueDeclaration]);
        const baseParam = _optionalChain([baseDecl, 'optionalAccess', _909 => _909.parameters, 'optionalAccess', _910 => _910[index]]);
        if (baseParam === void 0) {
          return false;
        }
        return baseParam.type === void 0 && baseParam.initializer !== void 0 && baseParam.initializer.kind === param.initializer.kind;
      }
    }
    return false;
  }
  // a use of a nil-defaulted map local that only reads it: an element read, `k in x`, or the
  // receiver of a helper that treats a nil map like an absent value (no IsDictionary/IsEqual)
  goGetArgNilMapUseOnlyReads(n) {
    const parent = n.parent;
    if (_optionalChain([parent, 'optionalAccess', _911 => _911.kind]) === _typescript2.default.SyntaxKind.ElementAccessExpression || _optionalChain([parent, 'optionalAccess', _912 => _912.kind]) === _typescript2.default.SyntaxKind.BinaryExpression) {
      return this.goSafeDictUseReadsTheMap(n);
    }
    if (_optionalChain([parent, 'optionalAccess', _913 => _913.kind]) !== _typescript2.default.SyntaxKind.CallExpression || parent.arguments.indexOf(n) !== 0) {
      return false;
    }
    const callee = this.goPrintedCallee(this.printNode(parent, 0));
    return callee !== void 0 && GO_GETARG_NIL_MAP_READERS.indexOf(callee.replace(/^this\./, "")) >= 0;
  }
  // the Go type the printed default names, or undefined when it names none
  goGetArgTypeOfShape(shape) {
    if (/^map\[string\]any\{/.test(shape)) {
      return "map[string]any";
    }
    if (/^\[map\[string\]any\]\{/.test(shape)) {
      return "[]map[string]any";
    }
    if (/^\[\]string\{/.test(shape)) {
      return "[]string";
    }
    if (/^\[\]any\{/.test(shape)) {
      return "[]any";
    }
    if (/^"/.test(shape)) {
      return "string";
    }
    if (shape === "true" || shape === "false") {
      return "bool";
    }
    if (/^-?[0-9]/.test(shape) || /^math\./.test(shape)) {
      return "int64";
    }
    return void 0;
  }
  // Go type candidates for a nil-defaulted parameter, in try order: the annotation text
  // (`Int` vs `Num` exist only there), then goNativeParameterTypeCandidates.
  goGetArgDeclaredTypeCandidates(param) {
    const out = [];
    const declared = _optionalChain([param, 'optionalAccess', _914 => _914.type]) !== void 0 ? String(param.type.getText()).replace(/\s+/g, " ") : void 0;
    const alias = _nullishCoalesce(this.CCXT_GO_GETARG_DECLARED_TYPES, () => ( {}));
    if (declared !== void 0 && alias[declared] !== void 0) {
      out.push(alias[declared]);
    } else if (declared !== void 0 && this.goGetArgPrimitiveType(declared) !== void 0) {
      out.push("*" + this.goGetArgPrimitiveType(declared));
    }
    for (const goType of this.goNativeParameterTypeCandidates(param)) {
      if (out.indexOf(goType) < 0) {
        out.push(goType);
      }
    }
    return out;
  }
  goGetArgPrimitiveType(declared) {
    if (declared === "Int" || declared === "Integer" || declared === "int") {
      return "int64";
    }
    if (declared === "Num" || declared === "number" || declared === "Float") {
      return "float64";
    }
    if (declared === "Bool" || declared === "boolean") {
      return "bool";
    }
    if (declared === "Str" || declared === "string" || declared === "String") {
      return "string";
    }
    return void 0;
  }
  // the twin of a declared Go type (go/v4/exchange_helpers.go); undefined when the twin is
  // missing, in which case the parameter keeps the `any` box (an `any` return must never be
  // assigned to a typed local)
  goGetArgTwinName(goType) {
    switch (goType) {
      case "map[string]any":
        return "GetArgMap";
      case "[]map[string]any":
        return "GetArgMapSlice";
      case "[]string":
        return "GetArgStringSlice";
      case "[]any":
        return "GetArgAnySlice";
      case "string":
        return "GetArgString";
      case "bool":
        return "GetArgBool";
      case "int64":
        return "GetArgInt64";
      case "float64":
        return "GetArgFloat64";
      case "*string":
        return "GetArgStringPtr";
      case "*int64":
        return "GetArgInt64Ptr";
      case "*float64":
        return "GetArgFloat64Ptr";
      case "*bool":
        return "GetArgBoolPtr";
    }
    return void 0;
  }
  goGetArgIsValueType(goType) {
    return goType === "string" || goType === "bool" || goType === "int64" || goType === "float64";
  }
  goGetArgLocalIsSafe(body, param, goType, nilable = false) {
    const name = param.name.escapedText;
    if (this.goGetArgTwinName(goType) === void 0) {
      return false;
    }
    if (!this.goLocalIsSafeToType(body, param, name, goType)) {
      return false;
    }
    if (this.goGetArgIsValueType(goType)) {
      return this.goParameterKeepsNilCompareNative(body, param, goType);
    }
    return this.goGetArgConsumersAreSafe(body, param, goType, nilable);
  }
  // Every later use must read the typed local as it read the `any` box: pointers only reach
  // audited deref consumers (fail-closed); containers are the same map/list, and nil-sensitive
  // consumers are tabled as `container` (goGetArgPassesIntoContainerDefault covers call chains).
  goGetArgConsumersAreSafe(body, param, goType, nilable) {
    const name = param.name.escapedText;
    const table = _nullishCoalesce(this.CCXT_GO_GETARG_SAFE_CONSUMERS, () => ( {}));
    const pointer = goType.startsWith("*");
    let safe = true;
    const verdictOf = (callee, argIndex) => {
      const goName = callee.charAt(0).toUpperCase() + callee.slice(1);
      const entry = _nullishCoalesce(table[callee], () => ( (pointer ? _nullishCoalesce(table[goName], () => ( table[goName + "Async"])) : void 0)));
      if (entry === void 0) {
        return pointer || nilable ? "unknown" : "deref";
      }
      if (typeof entry === "string") {
        return entry;
      }
      return _nullishCoalesce(_nullishCoalesce(entry[String(argIndex)], () => ( entry["*"])), () => ( (pointer ? "unknown" : "deref")));
    };
    const visit = (n) => {
      if (!safe) {
        return;
      }
      if (_optionalChain([n, 'optionalAccess', _915 => _915.kind]) === _typescript2.default.SyntaxKind.Identifier && n.escapedText === name) {
        let symbol;
        try {
          symbol = this.getChecker().getSymbolAtLocation(n);
        } catch (e) {
          symbol = void 0;
        }
        if (_optionalChain([symbol, 'optionalAccess', _916 => _916.valueDeclaration]) === param) {
          const parent = n.parent;
          if (parent === param) {
            return;
          }
          if (nilable && goType === "map[string]any") {
            const assigned = _optionalChain([parent, 'optionalAccess', _917 => _917.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && parent.left === n && _optionalChain([parent, 'access', _918 => _918.operatorToken, 'optionalAccess', _919 => _919.kind]) === _typescript2.default.SyntaxKind.EqualsToken;
            if (assigned || this.goGetArgNilMapUseOnlyReads(n)) {
              return;
            }
          }
          if (pointer && _optionalChain([parent, 'optionalAccess', _920 => _920.kind]) === _typescript2.default.SyntaxKind.BinaryExpression) {
            const op = _optionalChain([parent, 'access', _921 => _921.operatorToken, 'optionalAccess', _922 => _922.kind]);
            if (parent.left === n && op === _typescript2.default.SyntaxKind.EqualsToken || parent.left === n && op === _typescript2.default.SyntaxKind.InKeyword) {
              return;
            }
            const other = parent.left === n ? parent.right : parent.left;
            const equality = [_typescript2.default.SyntaxKind.EqualsEqualsEqualsToken, _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken].includes(op);
            if (equality && (_optionalChain([other, 'optionalAccess', _923 => _923.kind]) === _typescript2.default.SyntaxKind.StringLiteral || _optionalChain([other, 'optionalAccess', _924 => _924.kind]) === _typescript2.default.SyntaxKind.NumericLiteral)) {
              return;
            }
          }
          if (_optionalChain([parent, 'optionalAccess', _925 => _925.kind]) === _typescript2.default.SyntaxKind.BinaryExpression) {
            const other = parent.left === n ? parent.right : parent.left;
            const isNullTest = _optionalChain([other, 'optionalAccess', _926 => _926.kind]) === _typescript2.default.SyntaxKind.NullKeyword || _optionalChain([other, 'optionalAccess', _927 => _927.kind]) === _typescript2.default.SyntaxKind.Identifier && other.escapedText === "undefined";
            if (isNullTest) {
              safe = true;
              return;
            }
          }
          if (_optionalChain([parent, 'optionalAccess', _928 => _928.kind]) === _typescript2.default.SyntaxKind.CallExpression) {
            const args = _nullishCoalesce(parent.arguments, () => ( []));
            const argIndex = args.indexOf(n);
            const callee = parent.expression;
            let calleeName = _optionalChain([callee, 'optionalAccess', _929 => _929.name]) !== void 0 ? callee.name.escapedText : _optionalChain([callee, 'optionalAccess', _930 => _930.escapedText]) !== void 0 ? callee.escapedText : void 0;
            if (_optionalChain([callee, 'optionalAccess', _931 => _931.expression, 'optionalAccess', _932 => _932.escapedText]) === "Math" && (calleeName === "min" || calleeName === "max")) {
              calleeName = "math" + calleeName.charAt(0).toUpperCase() + calleeName.slice(1);
            }
            if (calleeName === void 0) {
              safe = !pointer && !nilable;
              return;
            }
            const verdict = verdictOf(calleeName, argIndex);
            if (verdict === "unsafe") {
              safe = false;
              return;
            }
            if (verdict === "container" && nilable) {
              safe = false;
              return;
            }
            if (calleeName === "IsEqual" && nilable) {
              const other = argIndex === 0 ? args[1] : args[0];
              const otherIsNil = other === void 0 || _optionalChain([other, 'optionalAccess', _933 => _933.kind]) === _typescript2.default.SyntaxKind.NullKeyword || _optionalChain([other, 'optionalAccess', _934 => _934.kind]) === _typescript2.default.SyntaxKind.Identifier && other.escapedText === "undefined";
              if (otherIsNil) {
                safe = false;
                return;
              }
            }
            if (verdict === "unknown" && nilable && goType === "map[string]any" && this.goGetArgPositionIsDefaulted(callee, argIndex)) {
              safe = true;
              return;
            }
            if (verdict === "unknown" && pointer && this.goGetArgPositionIsDefaulted(callee, argIndex)) {
              safe = true;
              return;
            }
            if (verdict === "unknown") {
              safe = false;
              return;
            }
            if (nilable && !pointer && this.goGetArgPassesIntoContainerDefault(callee, argIndex)) {
              safe = false;
              return;
            }
            safe = true;
            return;
          }
          if (_optionalChain([parent, 'optionalAccess', _935 => _935.kind]) === _typescript2.default.SyntaxKind.ExpressionStatement) {
            safe = true;
            return;
          }
          if (pointer && this.goGetArgPointerStoredAsValue(n, param)) {
            safe = true;
            return;
          }
          if (pointer && this.goGetArgPointerInHelperArithmetic(n)) {
            safe = true;
            return;
          }
          safe = !pointer && !nilable;
          return;
        }
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(body, visit);
    return safe;
  }
  // `x / 1000`, `x - 1`, `x > 0`: printed through Divide/Subtract/…/IsGreaterThan, which
  // derefScalar both operands at entry (checked on the printed text by the caller's diff)
  goGetArgPointerInHelperArithmetic(n) {
    const parent = n.parent;
    if (_optionalChain([parent, 'optionalAccess', _936 => _936.kind]) !== _typescript2.default.SyntaxKind.BinaryExpression) {
      return false;
    }
    const ops = [
      _typescript2.default.SyntaxKind.SlashToken,
      _typescript2.default.SyntaxKind.MinusToken,
      _typescript2.default.SyntaxKind.AsteriskToken,
      _typescript2.default.SyntaxKind.PercentToken,
      _typescript2.default.SyntaxKind.GreaterThanToken,
      _typescript2.default.SyntaxKind.LessThanToken,
      _typescript2.default.SyntaxKind.GreaterThanEqualsToken,
      _typescript2.default.SyntaxKind.LessThanEqualsToken
    ];
    if (!ops.includes(_optionalChain([parent, 'access', _937 => _937.operatorToken, 'optionalAccess', _938 => _938.kind]))) {
      return false;
    }
    const printed = this.printNode(parent, 0).trim();
    return /^(?:\(\s*)*(?:Divide|Subtract|Multiply|Mod|IsGreaterThan|IsLessThan|IsGreaterThanOrEqual|IsLessThanOrEqual)\(/.test(printed);
  }
  // `request[k] = x` / `{ k: x }`: the pointer lands in an `any` dictionary whose readers
  // (GetValue, Urlencode, Json, IsEqual) derefScalar it; a `*Request` builder returns that
  // dictionary to its caller, so it keeps the box
  goGetArgPointerStoredAsValue(n, param) {
    const parent = n.parent;
    const stored = _optionalChain([parent, 'optionalAccess', _939 => _939.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && parent.right === n && _optionalChain([parent, 'access', _940 => _940.operatorToken, 'optionalAccess', _941 => _941.kind]) === _typescript2.default.SyntaxKind.EqualsToken && _optionalChain([parent, 'access', _942 => _942.left, 'optionalAccess', _943 => _943.kind]) === _typescript2.default.SyntaxKind.ElementAccessExpression || _optionalChain([parent, 'optionalAccess', _944 => _944.kind]) === _typescript2.default.SyntaxKind.PropertyAssignment && parent.initializer === n;
    const methodName = String(_nullishCoalesce(_optionalChain([param, 'optionalAccess', _945 => _945.parent, 'optionalAccess', _946 => _946.name, 'optionalAccess', _947 => _947.escapedText]), () => ( "")));
    return stored && !methodName.endsWith("Request");
  }
  // element `index` of a tuple-typed call result is `Dict` (`[T, Dict]`); the Go tuple holds
  // that map, so MapTyped reads the same dictionary back
  goTupleElementIsDict(right, index) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0 || index < 0) {
      return false;
    }
    const expr = _optionalChain([right, 'optionalAccess', _948 => _948.kind]) === _typescript2.default.SyntaxKind.AwaitExpression ? right.expression : right;
    if (_optionalChain([expr, 'optionalAccess', _949 => _949.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return false;
    }
    const type = checker.getTypeAtLocation(expr);
    if (type === void 0 || !checker.isTupleType(type)) {
      return this.goParamsTupleHelperIndex(expr) === index;
    }
    const element = _optionalChain([checker, 'access', _950 => _950.getTypeArguments, 'call', _951 => _951(type), 'optionalAccess', _952 => _952[index]]);
    return element !== void 0 && !(element.flags & (_typescript2.default.TypeFlags.Any | _typescript2.default.TypeFlags.Unknown)) && this.goParameterTypeIsDict(element);
  }
  // `this.handleXxx(…, params, …)` of a base helper typed `any[]` whose element holds the params
  // map it was given after omit/extend; only when that element is the same map-typed local
  goParamsTupleHelperIndex(call) {
    const callee = _optionalChain([call, 'optionalAccess', _953 => _953.expression]);
    if (_optionalChain([callee, 'optionalAccess', _954 => _954.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([callee, 'access', _955 => _955.expression, 'optionalAccess', _956 => _956.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return -1;
    }
    const index = GO_PARAMS_TUPLE_HELPERS[_optionalChain([callee, 'access', _957 => _957.name, 'optionalAccess', _958 => _958.escapedText])];
    if (index === void 0) {
      return -1;
    }
    const target = _optionalChain([call, 'access', _959 => _959.parent, 'optionalAccess', _960 => _960.left, 'optionalAccess', _961 => _961.elements, 'optionalAccess', _962 => _962[index]]);
    const passesTarget = _optionalChain([target, 'optionalAccess', _963 => _963.kind]) === _typescript2.default.SyntaxKind.Identifier && (_nullishCoalesce(call.arguments, () => ( []))).some((a) => a.kind === _typescript2.default.SyntaxKind.Identifier && a.escapedText === target.escapedText);
    return passesTarget ? index : -1;
  }
  // the safety check of a map-typed GetArg local: a Dict tuple element written into it is printed
  // through MapTyped once the local is bound as a map (no recursion into goGetArgLocalType)
  goGetArgTupleWriteIsDict(declaration, right, index) {
    return _optionalChain([declaration, 'optionalAccess', _964 => _964.kind]) === _typescript2.default.SyntaxKind.Parameter && declaration.initializer !== void 0 && this.goTupleElementIsDict(right, index);
  }
  // `[x, params] = f()` writes a GetArg local bound as map[string]any: unbox the element
  goGetArgBindsDictElement(leftElement, right, index) {
    if (_optionalChain([leftElement, 'optionalAccess', _965 => _965.kind]) !== _typescript2.default.SyntaxKind.Identifier || !this.goTupleElementIsDict(right, index)) {
      return false;
    }
    let decl;
    try {
      decl = _optionalChain([this, 'access', _966 => _966.checkerOrUndefined, 'call', _967 => _967(), 'optionalAccess', _968 => _968.getSymbolAtLocation, 'call', _969 => _969(leftElement), 'optionalAccess', _970 => _970.valueDeclaration]);
    } catch (e) {
      decl = void 0;
    }
    if (_optionalChain([decl, 'optionalAccess', _971 => _971.kind]) !== _typescript2.default.SyntaxKind.Parameter || decl.initializer === void 0 || _optionalChain([decl, 'access', _972 => _972.parent, 'optionalAccess', _973 => _973.body]) === void 0) {
      return false;
    }
    return this.goGetArgParameterType(decl) === "map[string]any";
  }
  // the Go type a defaulted parameter's GetArg twin binds (undefined: the `any` GetArg); the one
  // predicate shared by the binding line and every consumer's printing
  goGetArgParameterType(decl) {
    if (_optionalChain([decl, 'optionalAccess', _974 => _974.kind]) !== _typescript2.default.SyntaxKind.Parameter || decl.initializer === void 0 || decl.dotDotDotToken !== void 0 || _optionalChain([decl, 'access', _975 => _975.name, 'optionalAccess', _976 => _976.kind]) !== _typescript2.default.SyntaxKind.Identifier || _optionalChain([decl, 'access', _977 => _977.parent, 'optionalAccess', _978 => _978.body]) === void 0 || ![_typescript2.default.SyntaxKind.MethodDeclaration, _typescript2.default.SyntaxKind.FunctionDeclaration].includes(decl.parent.kind)) {
      return void 0;
    }
    this.goGetArgTypeCache ??= /* @__PURE__ */ new WeakMap();
    if (!this.goGetArgTypeCache.has(decl)) {
      this.goGetArgTypeCache.set(decl, void 0);
      const goType = this.goGetArgLocalType(decl.parent.body, decl, this.printNode(decl.initializer, 0));
      this.goGetArgTypeCache.set(decl, goType !== void 0 && this.goGetArgTwinName(goType) !== void 0 ? goType : void 0);
    }
    return this.goGetArgTypeCache.get(decl);
  }
  // argument `argIndex` of the callee binds a parameter with a TypeScript default (GetArg-bound)
  goGetArgPositionIsDefaulted(callee, argIndex) {
    if (argIndex < 0) {
      return false;
    }
    let decl;
    try {
      decl = _optionalChain([this, 'access', _979 => _979.getChecker, 'call', _980 => _980(), 'access', _981 => _981.getSymbolAtLocation, 'call', _982 => _982(callee), 'optionalAccess', _983 => _983.valueDeclaration]);
    } catch (e) {
      decl = void 0;
    }
    const kinds = [_typescript2.default.SyntaxKind.MethodDeclaration, _typescript2.default.SyntaxKind.FunctionDeclaration];
    const param = decl !== void 0 && kinds.includes(decl.kind) ? _optionalChain([decl, 'access', _984 => _984.parameters, 'optionalAccess', _985 => _985[argIndex]]) : void 0;
    return _optionalChain([param, 'optionalAccess', _986 => _986.initializer]) !== void 0 && param.dotDotDotToken === void 0;
  }
  // the callee's own GetArg with a container default returns def for an untyped nil box but the
  // nil map for a nil map box (nil slices collapse to def), so only map shapes differ
  goGetArgPassesIntoContainerDefault(callee, argIndex) {
    if (argIndex < 0) {
      return false;
    }
    let symbol;
    try {
      symbol = this.getChecker().getSymbolAtLocation(callee);
    } catch (e) {
      symbol = void 0;
    }
    const decl = _optionalChain([symbol, 'optionalAccess', _987 => _987.valueDeclaration]);
    const params = _nullishCoalesce(_optionalChain([decl, 'optionalAccess', _988 => _988.parameters]), () => ( []));
    const param = params[argIndex];
    const initializer = _optionalChain([param, 'optionalAccess', _989 => _989.initializer]);
    if (initializer === void 0) {
      return false;
    }
    return initializer.kind === _typescript2.default.SyntaxKind.ObjectLiteralExpression;
  }
  printFunctionBody(node, identation, wrapInChannel = false) {
    let functionBody;
    const funcParams = node.parameters;
    const initParams = [];
    if (funcParams.length > 0) {
      const body = node.body.statements;
      const first = body.length > 0 ? body[0] : [];
      const remaining = body.length > 0 ? body.slice(1) : [];
      let firstStatement = this.printNode(first, identation + 1);
      const remainingString = remaining.map((statement) => this.printNode(statement, identation + 1)).join("\n");
      let offSetIndex = 0;
      funcParams.forEach((param, i) => {
        const initializer = param.initializer;
        if (initializer) {
          const index = i + offSetIndex;
          const paramName = this.printNode(param.name, 0);
          const printedDefault = this.printNode(initializer, 0);
          const goType = this.goGetArgParameterType(param);
          const twinName = goType !== void 0 ? this.goGetArgTwinName(goType) : void 0;
          if (goType !== void 0 && twinName !== void 0) {
            initParams.push(`var ${paramName} ${goType} = ${twinName}(optionalArgs, ${index}, ${printedDefault})`);
          } else {
            initParams.push(`${paramName} := GetArg(optionalArgs, ${index}, ${printedDefault})`);
          }
          initParams.push(`_ = ${paramName}`);
        } else {
          offSetIndex--;
        }
      });
      if (initParams.length > 0) {
        const defaultInitializers = initParams.map((l) => this.getIden(identation + 1) + l).join("\n") + "\n";
        const bodyParts = firstStatement.split("\n");
        const commentPart = bodyParts.filter((line) => this.isComment(line));
        const isComment = commentPart.length > 0;
        if (isComment) {
          const commentPartString = commentPart.map((c) => {
            const line = c.trim();
            return this.getIden(identation + 1) + (line.startsWith("*") ? " " + line : line);
          }).join("\n");
          const firstStmNoComment = bodyParts.filter((line) => !this.isComment(line)).join("\n");
          firstStatement = commentPartString + "\n" + defaultInitializers + firstStmNoComment;
        } else {
          firstStatement = defaultInitializers + firstStatement;
        }
      }
      const blockOpen = this.getBlockOpen(identation);
      const blockClose = this.getBlockClose(identation);
      firstStatement = remainingString.length > 0 ? firstStatement + "\n" : firstStatement;
      if (!wrapInChannel) {
        functionBody = blockOpen + firstStatement + remainingString + blockClose;
      } else {
        functionBody = firstStatement + remainingString;
      }
    } else {
      if (!wrapInChannel) {
        functionBody = super.printFunctionBody(node, identation);
      } else {
        functionBody = node.body.statements.map((statement) => {
          return this.printNode(statement, identation + 1);
        }).join("\n");
      }
    }
    if (wrapInChannel) {
      const functionBodySplit = functionBody.split("\n");
      const bodyWithIndentationExtraAndNoReturn = functionBodySplit.join("\n");
      let shouldAddLastReturn = true;
      const bodySplit = functionBodySplit;
      const lastLine = bodySplit[bodySplit.length - 1];
      if (lastLine.trim().startsWith("return") || lastLine.trim().startsWith("panic")) {
        shouldAddLastReturn = false;
      }
      if (node.body && this.blockEndsWithConditionalReturn(node.body.statements)) {
        shouldAddLastReturn = false;
      }
      const lastReturn = shouldAddLastReturn ? this.getIden(identation + 1) + "return nil" : "";
      const lines = [
        "{",
        `${this.getIden(identation + 1)}defer close(ch)`,
        `${this.getIden(identation + 1)}defer ReturnPanicError(ch)`,
        bodyWithIndentationExtraAndNoReturn
      ];
      if (lastReturn) {
        lines.push(lastReturn);
      }
      lines.push(`${this.getIden(identation)}}`);
      functionBody = lines.join("\n");
      functionBody = functionBody.replaceAll(/(^\s*)ch\s<-\snil\s+return\snil(\s*\})/gm, "$1return nil$2");
    }
    return functionBody;
  }
  printAwaitExpression(node, identation) {
    const expression = this.printNode(node.expression, identation);
    if (expression.startsWith("<-")) {
      return expression;
    }
    return `(<-${expression})`;
  }
  printInstanceOfExpression(node, identation) {
    const left = this.printNode(node.left);
    const right = this.printNode(node.right);
    return this.getIden(identation) + `IsInstance(${left}, ${right})`;
  }
  getRandomNameSuffix() {
    return Math.floor(Math.random() * 1e6).toString();
  }
  getLineBasedSuffix(node) {
    const { line, character } = this.getSrc().getLineAndCharacterOfPosition(node.getStart());
    return `${line}${character}`;
  }
  printExpressionStatement(node, identation) {
    if (_optionalChain([node, 'optionalAccess', _990 => _990.expression, 'optionalAccess', _991 => _991.kind]) === _typescript2.default.SyntaxKind.AsExpression) {
      node = node.expression;
    }
    if (node.expression.kind !== _typescript2.default.SyntaxKind.AwaitExpression) {
      return this.stripWhitespaceOnlyLines(super.printExpressionStatement(node, identation));
    }
    const exprStm = this.printNode(node.expression, identation);
    const returnRandName = "retRes" + this.getLineBasedSuffix(node);
    const stmtUnbox = this.goAwaitReceiveUnbox(node.expression, exprStm);
    const expStatement = stmtUnbox !== void 0 ? `
${this.getIden(identation)}var ${returnRandName} ${stmtUnbox.goType} = ${stmtUnbox.wrap(exprStm)}` : `
${this.getIden(identation)}PanicOnError(${exprStm.trim()})`;
    return this.printNodeCommentsIfAny(node, identation, expStatement);
  }
  isInsideAsyncFunction(returnStatementNode) {
    let currentNode = returnStatementNode;
    while (currentNode) {
      if (_typescript2.default.isFunctionDeclaration(currentNode) || _typescript2.default.isFunctionExpression(currentNode) || _typescript2.default.isArrowFunction(currentNode) || _typescript2.default.isMethodDeclaration(currentNode)) {
        return this.isAsyncFunction(currentNode);
      }
      currentNode = currentNode.parent;
    }
    return false;
  }
  /**
   * Statement that terminates an async (channel returning) function body.
   *
   * The body is the trampoline's sibling method (`go this.fetchTickerBody(ch, ...)`),
   * and the synthetic try/catch closures nest inside it: in both cases `return` leaves
   * a function whose result is a plain `any`, never the channel. The trampoline itself
   * owns the single `return ch`, emitted by printFunctionBody.
   */
  getAsyncReturnStatement(node) {
    return "return nil";
  }
  printReturnStatement(node, identation) {
    const isAsyncFunction = this.isInsideAsyncFunction(node);
    if (!isAsyncFunction) {
      return super.printReturnStatement(node, identation);
    }
    const leadingComment = this.printLeadingComments(node, identation);
    let trailingComment = this.printTraillingComment(node, identation);
    trailingComment = trailingComment ? " " + trailingComment : trailingComment;
    const exp = node.expression;
    let rightPart = exp ? " " + this.printNode(exp, identation) : "";
    rightPart = rightPart.trim();
    const returnStatement = this.getAsyncReturnStatement(node);
    if (_optionalChain([node, 'optionalAccess', _992 => _992.expression, 'optionalAccess', _993 => _993.kind]) === _typescript2.default.SyntaxKind.AsExpression) {
      node = node.expression;
    }
    if (_optionalChain([node, 'optionalAccess', _994 => _994.expression, 'optionalAccess', _995 => _995.kind]) === _typescript2.default.SyntaxKind.AwaitExpression) {
      const returnRandName = "retRes" + this.getLineBasedSuffix(node.expression);
      const printedExpr = rightPart;
      rightPart = rightPart ? rightPart + this.LINE_TERMINATOR : this.LINE_TERMINATOR;
      const retUnbox = this.goAwaitReceiveUnbox(node.expression, printedExpr);
      if (retUnbox !== void 0) {
        return `
${this.getIden(identation)}var ${returnRandName} ${retUnbox.goType} = ${retUnbox.wrap(printedExpr)}
${leadingComment}${this.getIden(identation)}ch <- ${returnRandName}${trailingComment}
${this.getIden(identation)}${returnStatement}`;
      }
      return `
${this.getIden(identation)}${returnRandName} := ${rightPart}
${this.getIden(identation)}PanicOnError(${returnRandName})
${leadingComment}${this.getIden(identation)}ch <- ${returnRandName}${trailingComment}
${this.getIden(identation)}${returnStatement}`;
    }
    if (rightPart.length === 0) {
      return `
${this.getIden(identation)}${returnStatement}`;
    }
    return `
${leadingComment}${this.getIden(identation)}ch <- ${rightPart}${trailingComment}
${this.getIden(identation)}${returnStatement}`;
  }
  printAsExpression(node, identation) {
    const type = node.type;
    if (type.kind === _typescript2.default.SyntaxKind.AnyKeyword) {
    }
    if (type.kind === _typescript2.default.SyntaxKind.StringKeyword) {
    }
    if (type.kind === _typescript2.default.SyntaxKind.ArrayType) {
    }
    return this.printNode(node.expression, identation);
  }
  printArrayLiteralExpression(node, identation = 0) {
    let arrayOpen = this.ARRAY_OPENING_TOKEN;
    const elems = node.elements;
    const elements = node.elements.map((e) => this.goWithExprDepth(1, () => this.printNode(e, identation)).trim()).join(", ");
    if (elems.length > 0) {
      const first = elems[0];
      if (first.kind === _typescript2.default.SyntaxKind.CallExpression) {
        const type = this.getFunctionType(first);
        if (type === void 0 || elements.indexOf(this.UKNOWN_PROP_ASYNC_WRAPPER_OPEN) > -1) {
          arrayOpen = "[]any{";
        } else {
          arrayOpen = `[]any{`;
        }
      }
    }
    return arrayOpen + elements + this.ARRAY_CLOSING_TOKEN;
  }
  printArgsForCallExpression(node, identation) {
    const args = node.arguments;
    let parsedArgs = "";
    if (false) {
      const parsedTypes = this.getTypesFromCallExpressionParameters(node);
      const tmpArgs = [];
      args.forEach((arg, index) => {
        const parsedType = parsedTypes[index];
        let cast = "";
        if (parsedType !== "object" && parsedType !== "float" && parsedType !== "int") {
          cast = parsedType ? `(${parsedType})` : "";
        }
        tmpArgs.push(cast + this.printNode(arg, identation).trim());
      });
      parsedArgs = tmpArgs.join(",");
      return parsedArgs;
    }
    if (node.arguments && node.arguments.length > 1) {
      return this.goWithExprDepth(this.goExprDepth + 1, () => super.printArgsForCallExpression(node, identation));
    }
    return super.printArgsForCallExpression(node, identation);
  }
  // true when the identifier keeps a reference besides this one: the constant fold
  // drops the call's reference to the operand, and Go rejects a local that ends up
  // unused, so an operand without another use keeps the helper
  goIdentifierUsedElsewhere(nameNode) {
    const scope = this.goEnclosingFunction(nameNode);
    if (scope === void 0) {
      return false;
    }
    const name = nameNode.escapedText;
    let used = false;
    const visit = (n) => {
      if (used) {
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.Identifier && n.escapedText === name && n !== nameNode) {
        const parent = n.parent;
        const isDeclarationName = _optionalChain([parent, 'optionalAccess', _996 => _996.kind]) === _typescript2.default.SyntaxKind.VariableDeclaration && parent.name === n;
        const isPropertyName = _optionalChain([parent, 'optionalAccess', _997 => _997.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression && parent.name === n;
        if (!isDeclarationName && !isPropertyName) {
          used = true;
          return;
        }
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return used;
  }
  // the local is printed `any` yet every value that reaches it is a []any: it starts
  // from a slice initializer and no later statement rebinds it (a push only appends
  // to the same slice), so the box holds a []any at every use
  goLocalHoldsOnlyArrays(nameNode) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const symbol = checker.getSymbolAtLocation(nameNode);
    const declaration = _optionalChain([symbol, 'optionalAccess', _998 => _998.valueDeclaration]);
    if (_optionalChain([declaration, 'optionalAccess', _999 => _999.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration || declaration.initializer === void 0) {
      return false;
    }
    if (this.goTypeOfInitializer(declaration.initializer, this.printNode(declaration.initializer, 0)) !== "[]any") {
      return false;
    }
    const scope = this.goEnclosingFunction(declaration);
    if (scope === void 0) {
      return false;
    }
    const name = declaration.name.escapedText;
    let safe = true;
    const visit = (n) => {
      if (!safe) {
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.Identifier && n.escapedText === name && n !== declaration.name && n !== nameNode) {
        const parent = n.parent;
        if (_optionalChain([parent, 'optionalAccess', _1000 => _1000.kind]) === _typescript2.default.SyntaxKind.VariableDeclaration && parent.name === n) {
          return;
        }
        if (this.goRebindingTargetOf(n) !== void 0) {
          safe = false;
          return;
        }
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return safe;
  }
  // native IsArray: a constant for an operand whose Go type the printer proves, and
  // the two-value assertion on an `any` box that only ever holds a []any
  printNativeIsArray(node, parsedArg) {
    if (typeof parsedArg !== "string" || parsedArg.includes("\n")) {
      return void 0;
    }
    const argNode = _optionalChain([node, 'access', _1001 => _1001.arguments, 'optionalAccess', _1002 => _1002[0]]);
    if (_optionalChain([argNode, 'optionalAccess', _1003 => _1003.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const goType = this.goPrintedTypeOfExpression(argNode, parsedArg);
    if (goType !== void 0) {
      const isSlice = this.isArraySliceTypes.indexOf(goType) >= 0;
      if (isSlice || this.isArrayNonSliceTypes.indexOf(goType) >= 0) {
        return this.goIdentifierUsedElsewhere(argNode) ? isSlice ? "true" : "false" : void 0;
      }
      return void 0;
    }
    if (!this.goLocalHoldsOnlyArrays(argNode)) {
      return void 0;
    }
    const read = `_, ok := ${parsedArg}.([]any)`;
    if (11 + read.length + 2 + "return ok".length <= 100) {
      return `func() bool { ${read}; return ok }()`;
    }
    const level = this.goStatementLevel;
    return `func() bool {
${this.getIden(level + 1)}${read}
${this.getIden(level + 1)}return ok
${this.getIden(level)}}()`;
  }
  printArrayIsArrayCall(node, identation, parsedArg = void 0) {
    return _nullishCoalesce(this.printNativeIsArray(node, parsedArg), () => ( `IsArray(${parsedArg})`));
  }
  printObjectKeysCall(node, identation, parsedArg = void 0) {
    return `ObjectKeys(${parsedArg})`;
  }
  printObjectValuesCall(node, identation, parsedArg = void 0) {
    return `ObjectValues(${parsedArg})`;
  }
  printJsonParseCall(node, identation, parsedArg = void 0) {
    return `JsonParse(${parsedArg})`;
  }
  printJsonStringifyCall(node, identation, parsedArg = void 0) {
    return `JsonStringify(${parsedArg})`;
  }
  printPromiseAllCall(node, identation, parsedArg = void 0) {
    return `promiseAll(${parsedArg})`;
  }
  printMathFloorCall(node, identation, parsedArg = void 0) {
    return `MathFloor(${parsedArg})`;
  }
  printMathRoundCall(node, identation, parsedArg = void 0) {
    return `MathRound(${parsedArg})`;
  }
  printMathCeilCall(node, identation, parsedArg = void 0) {
    return `MathCeil(${parsedArg})`;
  }
  printNumberIsIntegerCall(node, identation, parsedArg) {
    return `IsInt(${parsedArg})`;
  }
  // the base printer prints a method-call argument at the statement's depth; the
  // emitted Go call has two or more arguments, which go/printer lays out one level
  // deeper (a native `a + b` argument then drops its blanks)
  goPrintCallArgument(argument, printedText) {
    if (_optionalChain([argument, 'optionalAccess', _1004 => _1004.kind]) !== _typescript2.default.SyntaxKind.BinaryExpression || printedText === void 0) {
      return printedText;
    }
    return this.goWithExprDepth(this.goExprDepth + 1, () => this.printNode(argument, 0)).trimStart();
  }
  printArrayPushCall(node, identation, name = void 0, parsedArg = void 0) {
    let returnValue = "";
    let returnRandName = name;
    parsedArg = this.goPrintCallArgument(_optionalChain([node, 'access', _1005 => _1005.arguments, 'optionalAccess', _1006 => _1006[0]]), parsedArg);
    const nativeReceiver = this.goNativeAppendReceiver(node);
    if (nativeReceiver !== void 0) {
      return `${nativeReceiver} = append(${nativeReceiver}, ${parsedArg})`;
    }
    if (_optionalChain([name, 'optionalAccess', _1007 => _1007.startsWith, 'call', _1008 => _1008("GetValue")]) || /[\])]$/.test(_nullishCoalesce(name, () => ( "")))) {
      returnRandName = "retRes" + this.getLineBasedSuffix(node);
      returnValue = `${returnRandName} := ${name}
${this.getIden(identation)}`;
    }
    return `${returnValue}AppendToArray(&${returnRandName}, ${parsedArg})`;
  }
  printIncludesCall(node, identation, name = void 0, parsedArg = void 0) {
    return `Contains(${name}, ${parsedArg})`;
  }
  // the Go type an `indexOf` operand is printed as, or undefined for anything Go cannot
  // hand to strings.Index: an `any` box (a parameter, a GetValue/Ternary result), a
  // number, a slice. The receiver and the needle both have to be Go strings
  goIndexOfOperandType(node, printedText) {
    if (this.goIsAnyBoxExpression(node, printedText)) {
      return void 0;
    }
    if (_optionalChain([node, 'optionalAccess', _1009 => _1009.kind]) === _typescript2.default.SyntaxKind.Identifier) {
      return this.goDeclaredTypeOfIdentifier(node);
    }
    if (GO_ANY_BOX_CALLS.indexOf(this.goPrintedCallee(printedText)) >= 0) {
      return void 0;
    }
    return _nullishCoalesce(this.goStringCallStaticType(node, printedText), () => ( this.goStringFieldStaticType(node, printedText)));
  }
  // The ccxt writers assemble four outputs from a *slice* of the printer output (base-class files
  // after their TRANSPILED marker, ws cache tests after their first separator), which would cut off
  // the leading file-level import; they keep the helper until their writer adds the import itself.
  goFileKeepsFileLevelImports() {
    const fileName = _nullishCoalesce(_optionalChain([this, 'access', _1010 => _1010.getSrc, 'call', _1011 => _1011(), 'optionalAccess', _1012 => _1012.fileName]), () => ( ""));
    return !/(?:base\/Exchange(?:\.nooverloads\.\d+)?|base\/PredictionExchange|base\/test\.orderBook|base\/test\.cache)\.ts$/.test(fileName);
  }
  // `GetIndexOf(s, t)` is `strings.Index(s, t)` for a receiver declared `string` once `t` is a
  // string too (`target.(string)` succeeds; -1 is both not-found answers); a `*string` receiver
  // answers -1 when nil, reproduced by the nil guard. Slices and `any` boxes keep the helper.
  goNativeIndexOf(node, name, parsedArg) {
    if (typeof name !== "string" || typeof parsedArg !== "string") {
      return void 0;
    }
    if (name.includes("\n") || parsedArg.includes("\n")) {
      return void 0;
    }
    if (!this.goFileKeepsFileLevelImports()) {
      return void 0;
    }
    const receiver = _optionalChain([node, 'optionalAccess', _1013 => _1013.expression, 'optionalAccess', _1014 => _1014.expression]);
    const target = _optionalChain([node, 'optionalAccess', _1015 => _1015.arguments, 'optionalAccess', _1016 => _1016[0]]);
    if (this.goIndexOfOperandType(target, parsedArg) !== "string") {
      return void 0;
    }
    const receiverType = this.goIndexOfOperandType(receiver, name);
    if (receiverType === "string") {
      this.goFileStdlibImports.add("strings");
      return `strings.Index(${name}, ${parsedArg})`;
    }
    if (receiverType === "*string" && _optionalChain([receiver, 'optionalAccess', _1017 => _1017.kind]) === _typescript2.default.SyntaxKind.Identifier) {
      this.goFileStdlibImports.add("strings");
      const level = this.goStatementLevel;
      const body = this.getIden(level + 1);
      const inner = this.getIden(level + 2);
      return `func() int {
${body}if ${name} == nil {
${inner}return -1
${body}}
${body}return strings.Index(*${name}, ${parsedArg})
${this.getIden(level)}}()`;
    }
    return void 0;
  }
  printIndexOfCall(node, identation, name = void 0, parsedArg = void 0) {
    const native = this.goNativeIndexOf(node, name, parsedArg);
    if (native !== void 0) {
      return native;
    }
    return `${this.INDEXOF_WRAPPER_OPEN}${name}, ${parsedArg}${this.INDEXOF_WRAPPER_CLOSE}`;
  }
  // A native string operation needs every operand to be a printed Go `string` — the helper takes
  // `any` and re-derives the same string, so a proven operand cannot change the result. A regex
  // literal is never a Go string (a pattern, not the helper's ToString value) and keeps the helper.
  goNativeStringOperands(operands, texts, expected) {
    for (let i = 0; i < expected.length; i++) {
      const operand = operands[i];
      if (operand === void 0 || operand.kind === _typescript2.default.SyntaxKind.RegularExpressionLiteral) {
        return false;
      }
      if (this.goOperandStaticType(operand, texts[i]) !== expected[i]) {
        return false;
      }
    }
    return true;
  }
  // the emission entry point: undefined when the file's stdlib import could not be placed
  // (see goStdlibImportIsPlaceable), else the native call text, with the file-level import
  // recorded for printSourceFileStatements
  goNativeStringCall(nativeCall) {
    if (!this.goStdlibImportIsPlaceable()) {
      return void 0;
    }
    this.goFileStdlibImports.add("strings");
    return nativeCall;
  }
  // The native string calls need `import "strings"` before the file's first declaration. Every ccxt
  // consumer splices the body at file head, except build/goTranspiler.ts#transpileBaseMethods and
  // #transpilePredictionBaseMethods; those keep the helper until getGoImports(file) declares it.
  goStdlibImportIsPlaceable() {
    return this.goFileKeepsFileLevelImports();
  }
  printStartsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    if (parsedArg !== void 0 && this.goNativeStringOperands([_optionalChain([node, 'access', _1018 => _1018.expression, 'optionalAccess', _1019 => _1019.expression]), _optionalChain([node, 'access', _1020 => _1020.arguments, 'optionalAccess', _1021 => _1021[0]])], [name, parsedArg], ["string", "string"])) {
      const native = this.goNativeStringCall(`strings.HasPrefix(${name}, ${parsedArg})`);
      if (native !== void 0) {
        return native;
      }
    }
    return `StartsWith(${name}, ${parsedArg})`;
  }
  printEndsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    if (parsedArg !== void 0 && this.goNativeStringOperands([_optionalChain([node, 'access', _1022 => _1022.expression, 'optionalAccess', _1023 => _1023.expression]), _optionalChain([node, 'access', _1024 => _1024.arguments, 'optionalAccess', _1025 => _1025[0]])], [name, parsedArg], ["string", "string"])) {
      const native = this.goNativeStringCall(`strings.HasSuffix(${name}, ${parsedArg})`);
      if (native !== void 0) {
        return native;
      }
    }
    return `EndsWith(${name}, ${parsedArg})`;
  }
  printTrimCall(node, identation, name = void 0) {
    return `Trim(${name})`;
  }
  printJoinCall(node, identation, name = void 0, parsedArg = void 0) {
    if (parsedArg !== void 0 && this.goNativeStringOperands([_optionalChain([node, 'access', _1026 => _1026.expression, 'optionalAccess', _1027 => _1027.expression]), _optionalChain([node, 'access', _1028 => _1028.arguments, 'optionalAccess', _1029 => _1029[0]])], [name, parsedArg], ["[]string", "string"])) {
      const native = this.goNativeStringCall(`strings.Join(${name}, ${parsedArg})`);
      if (native !== void 0) {
        return native;
      }
    }
    return `Join(${name}, ${parsedArg})`;
  }
  printSplitCall(node, identation, name = void 0, parsedArg = void 0) {
    if (parsedArg !== void 0 && this.goNativeStringOperands([_optionalChain([node, 'access', _1030 => _1030.expression, 'optionalAccess', _1031 => _1031.expression]), _optionalChain([node, 'access', _1032 => _1032.arguments, 'optionalAccess', _1033 => _1033[0]])], [name, parsedArg], ["string", "string"])) {
      const native = this.goNativeStringCall(`strings.Split(${name}, ${parsedArg})`);
      if (native !== void 0) {
        return native;
      }
    }
    return `Split(${name}, ${parsedArg})`;
  }
  printToFixedCall(node, identation, name = void 0, parsedArg = void 0) {
    return `toFixed(${name}, ${parsedArg})`;
  }
  // ToString is the identity on a Go string (exchange_helpers.go: derefScalar and `case string`
  // return it unchanged), so a receiver declared `string` prints as itself. An `any` box, a
  // *string (derefScalar answers nil), an int64 or a float64 keeps the helper's runtime formatting.
  printToStringCall(node, identation, name = void 0) {
    if (name !== void 0 && name.indexOf("\n") < 0) {
      const receiver = _optionalChain([node, 'optionalAccess', _1034 => _1034.expression, 'optionalAccess', _1035 => _1035.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression ? node.expression.expression : void 0;
      if (receiver !== void 0 && this.goOperandStaticType(receiver, name) === "string") {
        return name;
      }
    }
    return `ToString(${name})`;
  }
  printConcatCall(node, identation, name = void 0, parsedArg = void 0) {
    return `Concat(${name}, ${parsedArg})`;
  }
  printToUpperCaseCall(node, identation, name = void 0) {
    if (this.goNativeStringOperands([_optionalChain([node, 'access', _1036 => _1036.expression, 'optionalAccess', _1037 => _1037.expression])], [name], ["string"])) {
      const native = this.goNativeStringCall(`strings.ToUpper(${name})`);
      if (native !== void 0) {
        return native;
      }
    }
    return `ToUpper(${name})`;
  }
  printToLowerCaseCall(node, identation, name = void 0) {
    if (this.goNativeStringOperands([_optionalChain([node, 'access', _1038 => _1038.expression, 'optionalAccess', _1039 => _1039.expression])], [name], ["string"])) {
      const native = this.goNativeStringCall(`strings.ToLower(${name})`);
      if (native !== void 0) {
        return native;
      }
    }
    return `ToLower(${name})`;
  }
  printShiftCall(node, identation, name = void 0) {
    return `Shift(${name})`;
  }
  printReverseCall(node, identation, name = void 0) {
    return `Reverse(${name})`;
  }
  printPopCall(node, identation, name = void 0) {
    return `Pop(${name}))`;
  }
  printAssertCall(node, identation, parsedArgs) {
    return `assert(${parsedArgs})`;
  }
  // `x.slice(a)` / `x.slice(a, b)` prints `Slice(x, a, b)`, a JS slice on a string: a
  // negative bound counts from the end, a start-only call clamps its start to 0 and an
  // end past len is clamped to len, where Go's native slicing panics
  goSliceLiteralBound(node) {
    if (_optionalChain([node, 'optionalAccess', _1040 => _1040.kind]) === _typescript2.default.SyntaxKind.NumericLiteral) {
      const text = node.text;
      if (!/^\d+$/.test(text)) {
        return void 0;
      }
      const value = Number(text);
      return value > 2147483647 ? void 0 : value;
    }
    if (_optionalChain([node, 'optionalAccess', _1041 => _1041.kind]) === _typescript2.default.SyntaxKind.PrefixUnaryExpression && node.operator === _typescript2.default.SyntaxKind.MinusToken) {
      const operand = this.goSliceLiteralBound(node.operand);
      return operand === void 0 ? void 0 : -operand;
    }
    return void 0;
  }
  // the subscript for a string value, with the helper's own index arithmetic
  goSliceSubscript(value, start, hasEnd, end) {
    const length = `len(${value})`;
    let startText;
    if (start >= 0) {
      startText = `${start}`;
    } else if (hasEnd) {
      startText = `${length} - ${-start}`;
    } else {
      startText = `max(${length} - ${-start}, 0)`;
    }
    if (!hasEnd) {
      return `${value}[${startText}:]`;
    }
    const endText = end >= 0 ? `min(${end}, ${length})` : `${length} - ${-end}`;
    return `${value}[${startText}:${endText}]`;
  }
  // receiver of an inlinable `.slice(...)`: `string` prints a plain subscript and
  // `*string` a nil-guarded one, while anything else (an `any` box) keeps the helper.
  // A TS cast (`(id as string).slice(...)`) prints nothing, so it is transparent here.
  goSliceReceiverType(node) {
    let receiverNode = _optionalChain([node, 'optionalAccess', _1042 => _1042.expression, 'optionalAccess', _1043 => _1043.expression]);
    while (_optionalChain([receiverNode, 'optionalAccess', _1044 => _1044.kind]) === _typescript2.default.SyntaxKind.AsExpression || _optionalChain([receiverNode, 'optionalAccess', _1045 => _1045.kind]) === _typescript2.default.SyntaxKind.NonNullExpression || _optionalChain([receiverNode, 'optionalAccess', _1046 => _1046.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      receiverNode = receiverNode.expression;
    }
    if (_optionalChain([receiverNode, 'optionalAccess', _1047 => _1047.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const goType = this.goPrintedTypeOfExpression(receiverNode, "");
    return goType === "string" || goType === "*string" ? goType : void 0;
  }
  // the integer-literal bounds of a `.slice(a, b)` call; undefined when a bound is an
  // expression, whose Go value is not provably a non-negative int the subscript could take
  goSliceLiteralBounds(node) {
    const args = _nullishCoalesce(_optionalChain([node, 'optionalAccess', _1048 => _1048.arguments]), () => ( []));
    const start = this.goSliceLiteralBound(args[0]);
    if (start === void 0) {
      return void 0;
    }
    const hasEnd = args.length > 1;
    const end = hasEnd ? this.goSliceLiteralBound(args[1]) : void 0;
    if (hasEnd && end === void 0) {
      return void 0;
    }
    return { start, hasEnd, end };
  }
  // true when this `.slice(a, b)` call prints native Go slicing of a string value
  goIsNativeSliceCall(node) {
    if (_optionalChain([node, 'optionalAccess', _1049 => _1049.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return false;
    }
    const callee = node.expression;
    if (_optionalChain([callee, 'optionalAccess', _1050 => _1050.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([callee, 'access', _1051 => _1051.name, 'optionalAccess', _1052 => _1052.escapedText]) !== "slice") {
      return false;
    }
    if (this.goSliceReceiverType(node) === void 0) {
      return false;
    }
    return this.goSliceLiteralBounds(node) !== void 0;
  }
  // `x.slice(a, b)` -> `x[a:b]` when the receiver is a local the printer declares
  // `string` (or `*string`, which keeps the helper's nil -> "" branch as a guard) and
  // both bounds are integer literals. Any other bound or receiver keeps the helper.
  printInlineSlice(node, receiverText) {
    if ((_nullishCoalesce(receiverText, () => ( ""))).includes("\n")) {
      return void 0;
    }
    const goType = this.goSliceReceiverType(node);
    if (goType === void 0) {
      return void 0;
    }
    const bounds = this.goSliceLiteralBounds(node);
    if (bounds === void 0) {
      return void 0;
    }
    const { start, hasEnd, end } = bounds;
    if (goType === "string") {
      return this.goSliceSubscript(receiverText, start, hasEnd, end);
    }
    const level = this.goStatementLevel;
    const body = this.getIden(level + 1);
    const branch = this.getIden(level + 2);
    const subscript = this.goSliceSubscript("str", start, hasEnd, end);
    return `func() string {
${body}if ${receiverText} == nil {
${branch}return ""
${body}}
${body}str := *${receiverText}
${body}return ${subscript}
${this.getIden(level)}}()`;
  }
  printSliceCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    parsedArg = this.goPrintCallArgument(_optionalChain([node, 'access', _1053 => _1053.arguments, 'optionalAccess', _1054 => _1054[0]]), parsedArg);
    parsedArg2 = this.goPrintCallArgument(_optionalChain([node, 'access', _1055 => _1055.arguments, 'optionalAccess', _1056 => _1056[1]]), parsedArg2);
    const nativeSlice = this.printInlineSlice(node, name);
    if (nativeSlice !== void 0) {
      return nativeSlice;
    }
    if (parsedArg2 === void 0) {
      parsedArg2 = "nil";
    }
    return `Slice(${name}, ${parsedArg}, ${parsedArg2})`;
  }
  printReplaceCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    if (parsedArg !== void 0 && parsedArg2 !== void 0 && this.goNativeStringOperands([_optionalChain([node, 'access', _1057 => _1057.expression, 'optionalAccess', _1058 => _1058.expression]), _optionalChain([node, 'access', _1059 => _1059.arguments, 'optionalAccess', _1060 => _1060[0]]), _optionalChain([node, 'access', _1061 => _1061.arguments, 'optionalAccess', _1062 => _1062[1]])], [name, parsedArg, parsedArg2], ["string", "string", "string"])) {
      const native = this.goNativeStringCall(`strings.Replace(${name}, ${parsedArg}, ${parsedArg2}, 1)`);
      if (native !== void 0) {
        return native;
      }
    }
    return `Replace(${name}, ${parsedArg}, ${parsedArg2})`;
  }
  printReplaceAllCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    if (parsedArg !== void 0 && parsedArg2 !== void 0 && this.goNativeStringOperands([_optionalChain([node, 'access', _1063 => _1063.expression, 'optionalAccess', _1064 => _1064.expression]), _optionalChain([node, 'access', _1065 => _1065.arguments, 'optionalAccess', _1066 => _1066[0]]), _optionalChain([node, 'access', _1067 => _1067.arguments, 'optionalAccess', _1068 => _1068[1]])], [name, parsedArg, parsedArg2], ["string", "string", "string"])) {
      const native = this.goNativeStringCall(`strings.ReplaceAll(${name}, ${parsedArg}, ${parsedArg2})`);
      if (native !== void 0) {
        return native;
      }
    }
    return `Replace(${name}, ${parsedArg}, ${parsedArg2})`;
  }
  printPadEndCall(node, identation, name, parsedArg, parsedArg2) {
    return `PadEnd(${name}, ${parsedArg}, ${parsedArg2})`;
  }
  printPadStartCall(node, identation, name, parsedArg, parsedArg2) {
    return `PadStart(${name}, ${parsedArg}, ${parsedArg2})`;
  }
  printDateNowCall(node, identation) {
    return "DateNow()";
  }
  printLengthProperty(node, identation, name = void 0) {
    const leftSide = this.printNode(node.expression, 0);
    return `GetLength(${leftSide})`;
  }
  // printPostFixUnaryExpression(node, identation) {
  //     const {operand, operator} = node;
  //     if (operand.kind === ts.SyntaxKind.NumericLiteral) {
  //         return super.printPostFixUnaryExpression(node, identation);
  //     }
  //     const leftSide = this.printNode(operand, 0);
  //     const op = this.PostFixOperators[operator]; // todo: handle --
  //     if (op === '--') {
  //         return `postFixDecrement(ref ${leftSide})`;
  //     }
  //     return `postFixIncrement(ref ${leftSide})`;
  // }
  // printPrefixUnaryExpression(node, identation) {
  //     const {operand, operator} = node;
  //     if (operand.kind === ts.SyntaxKind.NumericLiteral) {
  //         return super.printPrefixUnaryExpression(node, identation);
  //     }
  //     if (operator === ts.SyntaxKind.ExclamationToken) {
  //         // not branch check falsy/turthy values if needed;
  //         return  this.PrefixFixOperators[operator] + this.printCondition(node.operand, 0);
  //     }
  //     const leftSide = this.printNode(operand, 0);
  //     if (operator === ts.SyntaxKind.PlusToken) {
  //         return `prefixUnaryPlus(ref ${leftSide})`;
  //     } else {
  //         return `prefixUnaryNeg(ref ${leftSide})`;
  //     }
  // }
  printConditionalExpression(node, identation) {
    const condition = this.goWithExprDepth(1, () => this.printCondition(node.condition, 0));
    if (!condition.includes("\n")) {
      const whenTrue2 = this.goPrintTernaryBranch(node.whenTrue, 2);
      const whenFalse2 = this.goPrintTernaryBranch(node.whenFalse, 1);
      const inlined = this.printInlineTernary(condition, whenTrue2, whenFalse2, this.goTernaryResultType(node.whenTrue, whenTrue2, node.whenFalse, whenFalse2));
      if (inlined !== void 0) {
        return inlined;
      }
    }
    const whenTrue = this.printNode(node.whenTrue, 0);
    const whenFalse = this.printNode(node.whenFalse, 0);
    return `Ternary(${condition}, ${whenTrue}, ${whenFalse})`;
  }
  printDeleteExpression(node, identation) {
    const object = this.printNode(node.expression.expression, 0);
    const key = this.printNode(node.expression.argumentExpression, 0);
    return `Remove(${object}, ${key})`;
  }
  printThrowStatement(node, identation) {
    if (node.expression.kind === _typescript2.default.SyntaxKind.Identifier) {
      return this.getIden(identation) + "panic(" + this.printNode(node.expression, 0) + ")" + this.LINE_TERMINATOR;
    }
    if (node.expression.kind === _typescript2.default.SyntaxKind.NewExpression) {
      const expression = node.expression;
      const argumentsExp = _nullishCoalesce(_optionalChain([expression, 'optionalAccess', _1069 => _1069.arguments]), () => ( []));
      const parsedArg = _nullishCoalesce(argumentsExp.map((n) => this.printNode(n, 0)).join(","), () => ( ""));
      const newExpression = this.printNode(expression.expression, 0);
      if (expression.expression.kind === _typescript2.default.SyntaxKind.Identifier) {
        const id = expression.expression;
        const symbol = this.getChecker().getSymbolAtLocation(expression.expression);
        if (symbol) {
          const declarations = _nullishCoalesce(_optionalChain([this, 'access', _1070 => _1070.getChecker, 'call', _1071 => _1071(), 'access', _1072 => _1072.getDeclaredTypeOfSymbol, 'call', _1073 => _1073(symbol), 'access', _1074 => _1074.symbol, 'optionalAccess', _1075 => _1075.declarations]), () => ( []));
          const isClassDeclaration = declarations.find((l) => l.kind === _typescript2.default.SyntaxKind.InterfaceDeclaration || l.kind === _typescript2.default.SyntaxKind.ClassDeclaration);
          if (isClassDeclaration) {
          } else {
            return this.getIden(identation) + `throwDynamicException(${id.escapedText}, ${parsedArg})
${this.getIden(identation)}return nil`;
          }
        }
        return this.getIden(identation) + `panic(${id.escapedText}(${parsedArg}))${this.LINE_TERMINATOR}`;
      } else if (expression.expression.kind === _typescript2.default.SyntaxKind.ElementAccessExpression) {
        return this.getIden(identation) + `throwDynamicException(${newExpression}, ${parsedArg})`;
      }
      return super.printThrowStatement(node, identation);
    }
  }
  goWithExprDepth(depth, callback) {
    const previous = this.goExprDepth;
    this.goExprDepth = depth < 1 ? 1 : depth;
    try {
      return callback();
    } finally {
      this.goExprDepth = previous;
    }
  }
  // go/token precedence of the operators this printer can print natively
  // (5 `* / % << >> & &^`, 4 `+ - | ^`, 3 comparisons, 2 `&&`, 1 `||`)
  goOperatorPrecedence(operator) {
    switch (operator) {
      case "*":
      case "/":
      case "%":
      case "<<":
      case ">>":
      case "&":
      case "&^":
        return 5;
      case "+":
      case "-":
      case "|":
      case "^":
        return 4;
      case "==":
      case "!=":
      case "<":
      case "<=":
      case ">":
      case ">=":
        return 3;
      case "&&":
        return 2;
      case "||":
        return 1;
    }
    return 0;
  }
  // the operator string a node is printed as when it stays a Go binary
  // expression, or undefined when the node becomes a helper call or is not
  // binary at all - a primary expression, which walkBinary() never looks into
  goNativeBinaryOperator(node) {
    if (!node || !_typescript2.default.isBinaryExpression(node)) {
      return void 0;
    }
    const kind = node.operatorToken.kind;
    if (kind === _typescript2.default.SyntaxKind.EqualsToken || kind === _typescript2.default.SyntaxKind.PlusEqualsToken || kind === _typescript2.default.SyntaxKind.MinusEqualsToken || kind === _typescript2.default.SyntaxKind.InKeyword || kind === _typescript2.default.SyntaxKind.InstanceOfKeyword || kind in this.binaryExpressionsWrappers) {
      return void 0;
    }
    const operator = this.SupportedKindNames[kind];
    return this.goOperatorPrecedence(operator) > 0 ? operator : void 0;
  }
  // walkBinary(): has4 / has5 / maxProblem of the operator tree that is about
  // to be printed. Operands that stay binary expressions are walked, every
  // other operand is a primary expression and stops the walk - the same
  // boundary go/printer draws for parens and calls.
  goWalkBinary(operator, left, right, rightText) {
    const precedence = this.goOperatorPrecedence(operator);
    let has4 = precedence === 4;
    let has5 = precedence === 5;
    let maxProblem = 0;
    const leftOperator = this.goNativeBinaryOperator(left);
    if (leftOperator !== void 0 && this.goOperatorPrecedence(leftOperator) >= precedence) {
      const info = this.goWalkBinary(leftOperator, left.left, left.right, "");
      has4 = has4 || info.has4;
      has5 = has5 || info.has5;
      maxProblem = Math.max(maxProblem, info.maxProblem);
    }
    const rightOperator = this.goNativeBinaryOperator(right);
    if (rightOperator !== void 0 && this.goOperatorPrecedence(rightOperator) > precedence) {
      const info = this.goWalkBinary(rightOperator, right.left, right.right, "");
      has4 = has4 || info.has4;
      has5 = has5 || info.has5;
      maxProblem = Math.max(maxProblem, info.maxProblem);
    } else if (rightOperator === void 0) {
      const pair = operator + rightText.replace(/^[ \t]+/, "").slice(0, 1);
      if (pair === "/*" || pair === "&&" || pair === "&^") {
        maxProblem = 5;
      } else if (pair === "++" || pair === "--") {
        maxProblem = Math.max(maxProblem, 4);
      }
    }
    return { has4, has5, maxProblem };
  }
  // the separator gofmt puts around a natively printed operator: `' '` keeps
  // the blanks, `''` drops them (go/printer cutoff())
  goBinarySeparator(operator, rightText, left, right) {
    const precedence = this.goOperatorPrecedence(operator);
    if (precedence < 4) {
      return " ";
    }
    const { has4, has5, maxProblem } = this.goWalkBinary(operator, left, right, rightText);
    let cutoff;
    if (maxProblem > 0) {
      cutoff = maxProblem + 1;
    } else if (has4 && has5) {
      cutoff = this.goExprDepth === 1 ? 5 : 4;
    } else {
      cutoff = this.goExprDepth === 1 ? 6 : 4;
    }
    return precedence < cutoff ? " " : "";
  }
  printBinaryExpression(node, identation) {
    const { left, right, operatorToken } = node;
    const customBinaryExp = this.printCustomBinaryExpressionIfAny(node, identation);
    if (customBinaryExp) {
      return customBinaryExp;
    }
    if (operatorToken.kind == _typescript2.default.SyntaxKind.InstanceOfKeyword) {
      return this.printInstanceOfExpression(node, identation);
    }
    if (operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken) {
      const pointerWrite = this.goPointerWriteText(node, identation);
      if (pointerWrite !== void 0) {
        return pointerWrite;
      }
      const elementAccess = left;
      const rightSide = this.goWithExprDepth(this.goExprDepth + 1, () => this.printNode(right, 0));
      if (left.kind === _typescript2.default.SyntaxKind.ElementAccessExpression) {
        const leftSide = this.printNode(elementAccess.expression, 0);
        const propName = this.printNode(elementAccess.argumentExpression, 0);
        const value = this.goWithExprDepth(this.goExprDepth + 1, () => this.printNode(right, identation)).trimStart();
        const native = this.printNativeElementAssignment(elementAccess.expression, leftSide, elementAccess.argumentExpression, propName, value);
        if (native !== void 0) {
          return native;
        }
        return `AddElementToObject(${leftSide}, ${propName}, ${value})`;
      }
      if (_optionalChain([right, 'optionalAccess', _1076 => _1076.kind]) === _typescript2.default.SyntaxKind.AwaitExpression || rightSide.startsWith("<-this.callInternal")) {
        const leftParsed = this.printNode(left, 0);
        const awaited = _optionalChain([right, 'optionalAccess', _1077 => _1077.kind]) === _typescript2.default.SyntaxKind.AwaitExpression ? this.printNode(right, identation) : rightSide;
        return `
${this.getIden(identation)}${leftParsed} = ${awaited}
${this.getIden(identation)}PanicOnError(${leftParsed})`;
      }
    }
    const op = operatorToken.kind;
    if (op === _typescript2.default.SyntaxKind.EqualsToken && left.kind === _typescript2.default.SyntaxKind.ArrayLiteralExpression) {
      const arrayBindingPatternElements = left.elements;
      const parsedArrayBindingElements = arrayBindingPatternElements.map((e) => this.printNode(e, 0));
      const syntheticName = parsedArrayBindingElements.join("") + "Variable";
      let arrayBindingStatement = `${syntheticName} := ${this.printNode(right, 0)}
`;
      parsedArrayBindingElements.forEach((e, index) => {
        const leftElement = arrayBindingPatternElements[index];
        const leftType = this.getChecker().getTypeAtLocation(leftElement);
        const parsedType = this.getTypeFromRawType(leftType);
        const castExp = parsedType ? `(${parsedType})` : "";
        const statement = this.getIden(identation) + (this.goGetArgBindsDictElement(leftElement, right, index) ? `${e} = MapTyped(GetValue(${syntheticName}, ${index}))` : `${e} = GetValue(${syntheticName}, ${index})`);
        if (index < parsedArrayBindingElements.length - 1) {
          arrayBindingStatement += statement + "\n";
        } else {
          arrayBindingStatement += statement;
        }
      });
      return arrayBindingStatement;
    }
    let operator = this.SupportedKindNames[operatorToken.kind];
    let leftVar = void 0;
    let rightVar = void 0;
    if (operatorToken.kind === _typescript2.default.SyntaxKind.EqualsEqualsToken || operatorToken.kind === _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken) {
      if (this.COMPARISON_WRAPPER_OPEN) {
        leftVar = this.printNode(left, 0);
        rightVar = this.printNode(right, identation);
        return `${this.COMPARISON_WRAPPER_OPEN}${leftVar}, ${rightVar}${this.COMPARISON_WRAPPER_CLOSE}`;
      }
    }
    if (operatorToken.kind === _typescript2.default.SyntaxKind.BarBarToken || operatorToken.kind === _typescript2.default.SyntaxKind.AmpersandAmpersandToken) {
      leftVar = this.printCondition(left, 0);
      rightVar = this.printCondition(right, identation);
      if (operatorToken.kind === _typescript2.default.SyntaxKind.AmpersandAmpersandToken) {
        const collapsed = this.goDropRedundantNilGuard(leftVar.trim(), rightVar.trim());
        if (collapsed !== void 0) {
          return collapsed;
        }
      }
    } else {
      const precedence = this.goOperatorPrecedence(operator);
      if (precedence > 0) {
        const leftOperator = this.goNativeBinaryOperator(left);
        const samePrecedence = leftOperator !== void 0 && this.goOperatorPrecedence(leftOperator) === precedence;
        const leftDepth = samePrecedence ? this.goExprDepth : this.goExprDepth + 1;
        leftVar = this.goWithExprDepth(leftDepth, () => this.printNode(left, 0));
        rightVar = this.goWithExprDepth(this.goExprDepth + 1, () => this.printNode(right, identation));
      } else {
        leftVar = this.printNode(left, 0);
        rightVar = this.printNode(right, identation);
      }
    }
    const customOperator = this.getCustomOperatorIfAny(left, right, operatorToken);
    operator = customOperator ? customOperator : operator;
    const separator = this.goBinarySeparator(operator, rightVar.trim(), left, right);
    return leftVar + separator + operator + separator + rightVar.trim();
  }
  // `(x != nil) && (x != nil && …)` -> `(x != nil && …)`. Only fires when the
  // right operand opens with the very same nil guard the left operand *is*, so
  // the left is implied and dropping it cannot change the result.
  goDropRedundantNilGuard(leftVar, rightVar) {
    const guard = /^\(([A-Za-z_]\w*) != nil\)$/.exec(leftVar);
    if (guard === null) {
      return void 0;
    }
    if (rightVar.startsWith(`(${guard[1]} != nil && `)) {
      return rightVar;
    }
    return void 0;
  }
  printTryStatement(node, identation) {
    let tryBody = node.tryBlock.statements.map((s) => {
      return this.printNode(s, identation + 1);
    }).join("\n");
    tryBody = tryBody.replaceAll(/(\s*)break\s*$/gm, '$1panic("break")');
    const catchBody = node.catchClause.block.statements.map((s) => this.printNode(s, identation + 1)).join("\n");
    const catchLines = catchBody.split("\n").map((l) => l.trim()).filter(Boolean);
    const catchLastLine = catchLines.length ? catchLines[catchLines.length - 1] : "";
    const catchBodyEndsWithReturn = catchLastLine.startsWith("return") || catchLastLine.startsWith("panic") || catchLastLine.startsWith("throw new") || this.blockEndsWithConditionalReturn(node.catchClause.block.statements);
    const tryLines = tryBody.split("\n").map((l) => l.trim()).filter(Boolean);
    const tryLastLine = tryLines.length ? tryLines[tryLines.length - 1] : "";
    const tryBodyEndsWithReturn = tryLastLine.startsWith("return") || tryLastLine.startsWith("panic") || tryLastLine.startsWith("throw new") || this.blockEndsWithConditionalReturn(node.tryBlock.statements);
    const returNil = "return nil";
    const isVoid = this.isInsideVoidFunction(node);
    const nodeEndsWithReturn = tryBodyEndsWithReturn && catchBodyEndsWithReturn && !isVoid;
    const errorName = node.catchClause.variableDeclaration.name.escapedText;
    const classPrefix = this.className !== "undefined" ? `(this *${this.className})` : "()";
    const thisWord = this.className !== "undefined" ? "this" : "";
    const catchBodyBlock = this.indentBlock(catchBody, "					");
    const tryBodyBlock = this.indentBlock(tryBody, "		");
    const catchBlock = `
{
	${nodeEndsWithReturn ? "ret__ := " : ""}func${classPrefix} (ret_ any) {
		defer func() {
			if ${errorName} := recover(); ${errorName} != nil {
				if ${errorName} == "break" {
					return
				}
				ret_ = func${classPrefix} any {
					// catch block:
${catchBodyBlock}
					${catchBodyEndsWithReturn ? "" : returNil}
				}(${thisWord})
			}
		}()
		// try block:
${tryBodyBlock}
		${tryBodyEndsWithReturn ? "" : returNil}
	}(${thisWord})
	${nodeEndsWithReturn ? `if ret__ != nil {
		return ret__
	}
	return nil` : ""}
}`;
    const indentedBlock = catchBlock.split("\n").map((line) => line.trim().length ? this.getIden(identation) + line : "").join("\n");
    return indentedBlock;
  }
  /**
   * Strip the printer's own leading indentation from every line of a printed
   * statement block so the caller can re-place it at an explicit level. Only the
   * common prefix goes away: relative nesting (one tab per level) is preserved.
   */
  dedentBlock(block) {
    const lines = block.split("\n");
    const indents = lines.filter((line) => line.trim().length > 0).map((line) => line.match(/^[\t ]*/)[0].length);
    const common = indents.length ? Math.min(...indents) : 0;
    return lines.map((line) => line.slice(common)).join("\n");
  }
  /**
   * Re-place a printed statement block at `level` (a run of tabs): the block's own
   * leading indentation is dropped and every non-blank line is prefixed with `level`,
   * so relative nesting (one tab per level) survives the move.
   */
  indentBlock(block, level) {
    return this.dedentBlock(block).split("\n").map((line) => line.trim().length ? level + line : "").join("\n");
  }
  /**
   * gofmt writes blank lines with no whitespace at all. A multi-line statement template
   * opens on a fresh line, so the inherited `getIden(identation) + <statement>` prefix
   * lands on a line that carries nothing else: drop that prefix instead of leaving a
   * whitespace-only line behind. Only blank lines are touched, never printed content.
   */
  stripWhitespaceOnlyLines(block) {
    return block.split("\n").map((line) => line.trim().length ? line : "").join("\n");
  }
  printPrefixUnaryExpression(node, identation) {
    const { operand, operator } = node;
    if (operator === _typescript2.default.SyntaxKind.ExclamationToken) {
      return this.getIden(identation) + this.PrefixFixOperators[operator] + this.printCondition(node.operand, 0);
    }
    if (operator === _typescript2.default.SyntaxKind.MinusToken) {
      const printed = this.printNode(node.operand, 0);
      const inlined = this.printInlineOpNeg(node, printed);
      if (inlined !== void 0) {
        return this.getIden(identation) + inlined;
      }
      return this.getIden(identation) + `OpNeg(${printed})`;
    }
    return this.getIden(identation) + this.PrefixFixOperators[operator] + this.printNode(operand, 0);
  }
  printNewExpression(node, identation) {
    let expression = _optionalChain([node, 'access', _1078 => _1078.expression, 'optionalAccess', _1079 => _1079.escapedText]);
    expression = expression ? expression : this.printNode(node.expression);
    if (node.arguments.length === 0) {
      return `New${this.capitalize(expression)}()`;
    }
    const args = node.arguments.map((n) => this.printNode(n, identation).trim()).join(", ");
    if (expression.endsWith("Error")) {
      return expression + this.LEFT_PARENTHESIS + args + this.RIGHT_PARENTHESIS;
    }
    return "New" + this.capitalize(expression) + this.LEFT_PARENTHESIS + args + this.RIGHT_PARENTHESIS;
  }
  /**
   * Override the default element-access printer with a version that walks the
   * entire `x[y][z]` chain and builds a properly nested sequence of helper
   * calls.  This removes the root cause of the unbalanced-parenthesis bug
   * without any post-processing or regex hacks.
   */
  // The Go static type of `printed` when the printer can name it as a native Go
  // map or slice, undefined while the value stays boxed in `any` (GetValue,
  // Ternary, a parameter, an untyped struct field). Only initializer shapes the
  // printer itself types — object/array literals, helper calls whose Go return
  // type it knows (GO_HELPER_RETURN_TYPES + the ccxt extension), and locals whose
  // declaration got that same concrete type — are reported.
  goIndexableTypeOf(node, printed) {
    if (node === void 0) {
      return void 0;
    }
    switch (node.kind) {
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
      case _typescript2.default.SyntaxKind.AsExpression:
        return this.goIndexableTypeOf(node.expression, printed);
      case _typescript2.default.SyntaxKind.ObjectLiteralExpression:
        return "map[string]any";
      case _typescript2.default.SyntaxKind.ArrayLiteralExpression:
        return "[]any";
      case _typescript2.default.SyntaxKind.CallExpression:
        return this.goTypeOfInitializer(node, printed);
      case _typescript2.default.SyntaxKind.Identifier:
        return this.goDeclaredTypeOfIdentifier(node);
    }
    return void 0;
  }
  // the leftover chain after the first (native) step, still helper-wrapped:
  // `m["a"]["b"]["c"]` prints `GetValue(GetValue(m["a"], "b"), "c")`
  goElementAccessChain(containerStr, keyStrs) {
    let acc = containerStr;
    for (let i = 1; i < keyStrs.length; i++) {
      acc = `${this.ELEMENT_ACCESS_WRAPPER_OPEN}${acc}, ${keyStrs[i]}${this.ELEMENT_ACCESS_WRAPPER_CLOSE}`;
    }
    return acc;
  }
  // the container of a nested `m["a"]["b"] = v` write, for all but the last key: its first step is a
  // plain read, so a receiver typed as a map indexes natively and only `any` steps keep the helper.
  // A missing key or nil map reads nil in both forms; a non-map container is a no-op either way.
  goElementWriteChain(baseExpr, containerStr, keyNodes, keyStrs) {
    let acc = containerStr;
    let first = 0;
    if (keyStrs.length > 1 && this.goIndexableTypeOf(baseExpr, containerStr) === "map[string]any" && this.goKeyIsString(keyNodes[0], keyStrs[0])) {
      acc = `${containerStr}[${keyStrs[0]}]`;
      first = 1;
    }
    for (let i = first; i < keyStrs.length - 1; i++) {
      acc = `${this.ELEMENT_ACCESS_WRAPPER_OPEN}${acc}, ${keyStrs[i]}${this.ELEMENT_ACCESS_WRAPPER_CLOSE}`;
    }
    return acc;
  }
  // true when the printed key is a Go string, so `m[key]` reads the map with the
  // same key GetValue resolves for a string operand (GetValue parses a non-string
  // key, which on map[string]any just yields nil)
  goKeyIsString(node, printed) {
    if (node === void 0) {
      return false;
    }
    switch (node.kind) {
      case _typescript2.default.SyntaxKind.StringLiteral:
      case _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral:
        return true;
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
      case _typescript2.default.SyntaxKind.AsExpression:
        return this.goKeyIsString(node.expression, printed);
      case _typescript2.default.SyntaxKind.Identifier:
        return this.goDeclaredTypeOfIdentifier(node) === "string";
      case _typescript2.default.SyntaxKind.CallExpression:
        return this.goTypeOfInitializer(node, printed) === "string";
    }
    return false;
  }
  // true when the printed key is a Safe*-boxed string: an identifier declared `*string`,
  // i.e. a nilable Go pointer. The native read needs GetValue's `*` deref and its nil
  // answer, neither of which an index expression expresses.
  goIsDerefStringKeyExpression(node) {
    return _optionalChain([node, 'optionalAccess', _1080 => _1080.kind]) === _typescript2.default.SyntaxKind.Identifier && this.goDeclaredTypeOfIdentifier(node) === "*string";
  }
  // the nil-guarded native read of a declared map with a `*string` key, reproducing GetValue's key
  // deref: a nil key reads nil, otherwise the map index (missing key is the `any` nil). gofmt keeps
  // a func literal holding an `if` on its own lines, so the guard sits at the statement's level.
  printNilGuardedMapIndex(containerStr, keyStr) {
    const level = this.goStatementLevel;
    const body = this.getIden(level + 1);
    return `func() any {
${body}if ${keyStr} == nil {
${this.getIden(level + 2)}return nil
${body}}
${body}return ${containerStr}[*${keyStr}]
${this.getIden(level)}}()`;
  }
  // true for `this.<field>` — the one property-access shape whose Go type the
  // printer itself cannot name (the fields live in the hand-written Go structs)
  isGoThisPropertyAccessExpression(node) {
    return _optionalChain([node, 'optionalAccess', _1081 => _1081.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression && _optionalChain([node, 'access', _1082 => _1082.expression, 'optionalAccess', _1083 => _1083.kind]) === _typescript2.default.SyntaxKind.ThisKeyword;
  }
  // true when the element access is the target of an assignment: the binary
  // expression printer owns that shape (AddElementToObject / rewritten GetValue
  // chains), a native `x[k]` index there would drop the write
  isGoElementAccessAssignmentTarget(node) {
    const parent = node.parent;
    return _optionalChain([parent, 'optionalAccess', _1084 => _1084.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && parent.left === node && (parent.operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken || parent.operatorToken.kind === _typescript2.default.SyntaxKind.PlusEqualsToken);
  }
  // the key of a native list read: its printed Go type must be `int` for the guard's two
  // comparisons to compile, and the key expression is printed twice (the bounds test and the
  // index), so a call operand — evaluated twice — is rejected. Every other key keeps GetValue.
  goIntIndexExpression(node) {
    switch (_optionalChain([node, 'optionalAccess', _1085 => _1085.kind])) {
      case _typescript2.default.SyntaxKind.NumericLiteral: {
        const text = `${_nullishCoalesce(node.text, () => ( ""))}`;
        return /^\d+$/.test(text);
      }
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
        return this.goIntIndexExpression(node.expression);
      case _typescript2.default.SyntaxKind.PrefixUnaryExpression:
        return node.operator === _typescript2.default.SyntaxKind.MinusToken && this.goIntIndexExpression(node.operand);
      case _typescript2.default.SyntaxKind.Identifier:
        return this.goIntOperandIdentifier(node);
      case _typescript2.default.SyntaxKind.BinaryExpression: {
        const op = _optionalChain([node, 'access', _1086 => _1086.operatorToken, 'optionalAccess', _1087 => _1087.kind]);
        if (op === _typescript2.default.SyntaxKind.PlusToken || op === _typescript2.default.SyntaxKind.MinusToken || op === _typescript2.default.SyntaxKind.AsteriskToken || op === _typescript2.default.SyntaxKind.SlashToken || op === _typescript2.default.SyntaxKind.PercentToken) {
          return this.goIntIndexExpression(node.left) && this.goIntIndexExpression(node.right);
        }
        return false;
      }
    }
    return false;
  }
  // true for an identifier the printer emits as a Go int: a local it declared `int`, and a
  // `for (let i = 0; …)` counter, which prints `for i := 0` and is inferred as `int`
  goIntOperandIdentifier(node) {
    if (this.goDeclaredTypeOfIdentifier(node) === "int") {
      return true;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const symbol = checker.getSymbolAtLocation(node);
    const declaration = _optionalChain([symbol, 'optionalAccess', _1088 => _1088.valueDeclaration]);
    if (_optionalChain([declaration, 'optionalAccess', _1089 => _1089.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration || declaration.initializer === void 0) {
      return false;
    }
    const list = declaration.parent;
    const loop = _optionalChain([list, 'optionalAccess', _1090 => _1090.parent]);
    if (_optionalChain([list, 'optionalAccess', _1091 => _1091.kind]) !== _typescript2.default.SyntaxKind.VariableDeclarationList || _optionalChain([list, 'access', _1092 => _1092.declarations, 'optionalAccess', _1093 => _1093.length]) !== 1 || _optionalChain([loop, 'optionalAccess', _1094 => _1094.kind]) !== _typescript2.default.SyntaxKind.ForStatement || loop.initializer !== list) {
      return false;
    }
    const text = `${_nullishCoalesce(declaration.initializer.text, () => ( ""))}`;
    return declaration.initializer.kind === _typescript2.default.SyntaxKind.NumericLiteral && /^\d+$/.test(text);
  }
  // true when this identifier is a local the printer declared []any because it was unboxed
  // from a `this.SafeList` accessor. Only that family carried the first guarded element read;
  // the declared-type arm below is the D-06 widening.
  goSafeListUnboxIdentifier(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const symbol = checker.getSymbolAtLocation(node);
    const declaration = _optionalChain([symbol, 'optionalAccess', _1095 => _1095.valueDeclaration]);
    return _optionalChain([declaration, 'optionalAccess', _1096 => _1096.kind]) === _typescript2.default.SyntaxKind.VariableDeclaration && this.goSafeListLocalUnbox(declaration) === GO_SAFE_LIST_LOCAL_TYPE;
  }
  // true when the identifier's printed Go declaration is a `[]any` slice: the SafeList family
  // above, or any other local/param the declared-type table proved a slice (a slice literal,
  // a `[]any`-returning accessor, a slice param the typed-param family registers). All of them
  goDeclaredListIdentifier(node) {
    return this.goDeclaredTypeOfIdentifier(node) === GO_SAFE_LIST_LOCAL_TYPE || this.goSafeListUnboxIdentifier(node);
  }
  // `x[k]` on a local the printer declared []any is a slice index: GetValue answered nil for an
  // absent index (negative or out of range) and derefs a pointer element, so the native read is
  // the same index behind that guard. Only a key printed as an `int` carries it; every other
  goNativeListElementRead(node, containerStr, keyNode, keyStr) {
    if (containerStr.includes("\n") || keyStr.includes("\n")) {
      return void 0;
    }
    if (this.isGoElementAccessAssignmentTarget(node) || !this.goIntIndexExpression(keyNode)) {
      return void 0;
    }
    const level = this.goStatementLevel;
    const body = this.getIden(level + 1);
    return `func() any {
${body}if ${keyStr} >= 0 && ${keyStr} < len(${containerStr}) {
${this.getIden(level + 2)}return DerefScalar(${containerStr}[${keyStr}])
${body}}
${body}return nil
${this.getIden(level)}}()`;
  }
  printElementAccessExpression(node, identation) {
    const special = this.printElementAccessExpressionExceptionIfAny(node);
    if (special) {
      return special;
    }
    const keys = [];
    let baseExpr = null;
    let current = node;
    while (_typescript2.default.isElementAccessExpression(current)) {
      keys.unshift(current.argumentExpression);
      const expr = current.expression;
      if (!_typescript2.default.isElementAccessExpression(expr)) {
        baseExpr = expr;
        break;
      }
      current = expr;
    }
    const indexDepth = this.goExprDepth + 1;
    const containerStr = this.goWithExprDepth(indexDepth, () => this.printNode(baseExpr, 0));
    const keyStrs = keys.map((k) => this.goWithExprDepth(indexDepth, () => this.printNode(k, 0)));
    if (this.goIndexableTypeOf(baseExpr, containerStr) === "map[string]any") {
      if (this.goKeyIsString(keys[0], keyStrs[0]) && !this.isGoElementAccessAssignmentTarget(node) && !this.goMarketComparisonElementRead(node)) {
        return this.goElementAccessChain(`${containerStr}[${keyStrs[0]}]`, keyStrs);
      }
      if (this.goIsDerefStringKeyExpression(keys[0]) && !containerStr.includes("\n") && !this.isGoElementAccessAssignmentTarget(node)) {
        return this.goElementAccessChain(this.printNilGuardedMapIndex(containerStr, keyStrs[0]), keyStrs);
      }
    }
    let acc = containerStr;
    keyStrs.forEach((k) => {
      acc = `${this.ELEMENT_ACCESS_WRAPPER_OPEN}${acc}, ${k}${this.ELEMENT_ACCESS_WRAPPER_CLOSE}`;
    });
    if (_optionalChain([baseExpr, 'optionalAccess', _1097 => _1097.kind]) === _typescript2.default.SyntaxKind.Identifier && this.goDeclaredListIdentifier(baseExpr)) {
      const nativeRead = this.goNativeListElementRead(node, containerStr, keys[0], keyStrs[0]);
      if (nativeRead !== void 0) {
        return this.goElementAccessChain(nativeRead, keyStrs);
      }
    }
    return acc;
  }
  isInsideVoidFunction(node) {
    for (let cur = node.parent; cur; cur = cur.parent) {
      if (_typescript2.default.isFunctionLike(cur)) {
        return cur.type === void 0 || cur.type.kind === _typescript2.default.SyntaxKind.VoidKeyword;
      }
    }
    return true;
  }
  /**
   * Check if a block or statement contains a return statement or throws an error
   */
  hasReturnInBlock(statement) {
    if (_typescript2.default.isBlock(statement)) {
      if (statement.statements.length === 0) {
        return false;
      }
      return this.hasReturnInBlock(statement.statements[statement.statements.length - 1]);
    } else if (_typescript2.default.isReturnStatement(statement)) {
      return true;
    } else if (_typescript2.default.isThrowStatement(statement)) {
      return true;
    } else if (_typescript2.default.isIfStatement(statement)) {
      const ifHasReturn = this.hasReturnInBlock(statement.thenStatement);
      if (statement.elseStatement) {
        const elseHasReturn = this.hasReturnInBlock(statement.elseStatement);
        return ifHasReturn && elseHasReturn;
      }
      return false;
    } else if (_typescript2.default.isTryStatement(statement)) {
      const tryHasReturn = this.hasReturnInBlock(statement.tryBlock);
      const catchHasReturn = this.hasReturnInBlock(statement.catchClause.block);
      return tryHasReturn && catchHasReturn;
    }
    return false;
  }
  /**
   * Check if the last statement in a block is a conditional with returns in all branches
   */
  blockEndsWithConditionalReturn(statements) {
    if (statements.length === 0) {
      return false;
    }
    const lastStatement = statements[statements.length - 1];
    if (_typescript2.default.isIfStatement(lastStatement)) {
      const ifHasReturn = this.hasReturnInBlock(lastStatement.thenStatement);
      if (lastStatement.elseStatement) {
        const elseHasReturn = this.hasReturnInBlock(lastStatement.elseStatement);
        return ifHasReturn && elseHasReturn;
      }
    }
    if (_typescript2.default.isTryStatement(lastStatement)) {
      const tryHasReturn = this.hasReturnInBlock(lastStatement.tryBlock);
      const catchHasReturn = this.hasReturnInBlock(lastStatement.catchClause.block);
      return tryHasReturn && catchHasReturn;
    }
    return false;
  }
};

// src/javaTranspiler.ts
init_cjs_shims();

var parserConfig5 = {
  EXTENDS_TOKEN: "extends",
  PROMISE_TYPE_KEYWORD: "java.util.concurrent.CompletableFuture",
  ARRAY_KEYWORD: "java.util.List<Object>",
  OBJECT_KEYWORD: "java.util.Map<String, Object>",
  STRING_KEYWORD: "String",
  BOOLEAN_KEYWORD: "boolean",
  DEFAULT_PARAMETER_TYPE: "Object",
  DEFAULT_RETURN_TYPE: "Object",
  DEFAULT_TYPE: "Object",
  ELSEIF_TOKEN: "else if",
  // Objects in Java: we'll use double-brace initialization so property puts work
  OBJECT_OPENING: "new java.util.HashMap<String, Object>() {{",
  OBJECT_CLOSING: "}}",
  // Arrays in Java: emit Arrays.asList(...) wrapped by ArrayList
  ARRAY_OPENING_TOKEN: "new java.util.ArrayList<Object>(java.util.Arrays.asList(",
  ARRAY_CLOSING_TOKEN: "))",
  // For object literal properties we'll emit: put(key, value);
  PROPERTY_ASSIGNMENT_TOKEN: ",",
  VAR_TOKEN: "Object",
  // Java 10+ local var
  METHOD_TOKEN: "",
  PROPERTY_ASSIGNMENT_OPEN: "put(",
  PROPERTY_ASSIGNMENT_CLOSE: ");",
  SUPER_TOKEN: "super",
  SUPER_CALL_TOKEN: "super",
  FALSY_WRAPPER_OPEN: "Helpers.isTrue(",
  FALSY_WRAPPER_CLOSE: ")",
  COMPARISON_WRAPPER_OPEN: "Helpers.isEqual(",
  COMPARISON_WRAPPER_CLOSE: ")",
  UKNOWN_PROP_WRAPPER_OPEN: "this.call(",
  UNKOWN_PROP_WRAPPER_CLOSE: ")",
  UKNOWN_PROP_ASYNC_WRAPPER_OPEN: "this.callAsync(",
  UNKOWN_PROP_ASYNC_WRAPPER_CLOSE: ")",
  DYNAMIC_CALL_OPEN: "Helpers.callDynamically(",
  EQUALS_EQUALS_WRAPPER_OPEN: "Helpers.isEqual(",
  EQUALS_EQUALS_WRAPPER_CLOSE: ")",
  DIFFERENT_WRAPPER_OPEN: "!Helpers.isEqual(",
  DIFFERENT_WRAPPER_CLOSE: ")",
  GREATER_THAN_WRAPPER_OPEN: "Helpers.isGreaterThan(",
  GREATER_THAN_WRAPPER_CLOSE: ")",
  GREATER_THAN_EQUALS_WRAPPER_OPEN: "Helpers.isGreaterThanOrEqual(",
  GREATER_THAN_EQUALS_WRAPPER_CLOSE: ")",
  LESS_THAN_WRAPPER_OPEN: "Helpers.isLessThan(",
  LESS_THAN_WRAPPER_CLOSE: ")",
  LESS_THAN_EQUALS_WRAPPER_OPEN: "Helpers.isLessThanOrEqual(",
  LESS_THAN_EQUALS_WRAPPER_CLOSE: ")",
  PLUS_WRAPPER_OPEN: "Helpers.add(",
  PLUS_WRAPPER_CLOSE: ")",
  MINUS_WRAPPER_OPEN: "Helpers.subtract(",
  MINUS_WRAPPER_CLOSE: ")",
  ARRAY_LENGTH_WRAPPER_OPEN: "Helpers.getArrayLength(",
  ARRAY_LENGTH_WRAPPER_CLOSE: ")",
  DIVIDE_WRAPPER_OPEN: "Helpers.divide(",
  DIVIDE_WRAPPER_CLOSE: ")",
  MULTIPLY_WRAPPER_OPEN: "Helpers.multiply(",
  MULTIPLY_WRAPPER_CLOSE: ")",
  INDEXOF_WRAPPER_OPEN: "Helpers.getIndexOf(",
  INDEXOF_WRAPPER_CLOSE: ")",
  MOD_WRAPPER_OPEN: "Helpers.mod(",
  MOD_WRAPPER_CLOSE: ")",
  FUNCTION_TOKEN: "",
  ELEMENT_ACCESS_WRAPPER_OPEN: "Helpers.GetValue(",
  ELEMENT_ACCESS_WRAPPER_CLOSE: ")",
  INFER_VAR_TYPE: false,
  INFER_ARG_TYPE: false
};
var JAVA_THIS_RETURN_TYPES = {
  "milliseconds": "long"
};
var JAVA_THIS_RETURN_TYPES_BASE_FILE = /(^|[\\/])ts[\\/]src[\\/]base[\\/]Exchange(\.nooverloads\.\d+)?\.ts$/;
var JAVA_THIS_RETURN_TYPES_LIB_FILE = /(^|[\\/])node_modules[\\/](?:[^\\/]+[\\/]node_modules[\\/])?typescript6?[\\/]lib[\\/]lib\.[^\\/]*\.d\.ts$/;
var JAVA_ASSIGNMENT_OPERATOR_KINDS = (() => {
  const kinds = _typescript2.default.SyntaxKind;
  const names = Object.keys(kinds).filter((name) => name.endsWith("EqualsToken") && !/^Equals|^Exclamation|^LessThan|^GreaterThan/.test(name));
  return new Set(["EqualsToken"].concat(names).map((name) => kinds[name]).filter((kind) => kind !== void 0));
})();
var JAVA_SCALAR_TYPE_FLAGS = _typescript2.default.TypeFlags.String | _typescript2.default.TypeFlags.StringLiteral | _typescript2.default.TypeFlags.Number | _typescript2.default.TypeFlags.NumberLiteral | _typescript2.default.TypeFlags.Boolean | _typescript2.default.TypeFlags.BooleanLiteral | _typescript2.default.TypeFlags.BigInt | _typescript2.default.TypeFlags.BigIntLiteral | _typescript2.default.TypeFlags.Enum | _typescript2.default.TypeFlags.EnumLiteral | _typescript2.default.TypeFlags.ESSymbol | _typescript2.default.TypeFlags.UniqueESSymbol;
var JAVA_NULLISH_TYPE_FLAGS = _typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.Void | _typescript2.default.TypeFlags.Never;
var JAVA_BOOLEAN_BASE_FIELDS = /* @__PURE__ */ new Set([
  "this.alias",
  "this.verbose",
  "this.validateServerSsl",
  "this.enableRateLimit",
  "this.pro",
  "this.certified",
  "this.reloadingMarkets",
  "this.marketsLoaded",
  "this.reduceFees",
  "this.substituteCommonCurrencyCodes",
  "this.isSandboxModeEnabled",
  "this.returnResponseHeaders",
  "this.newUpdates",
  "this.syncSleep",
  // PredictionExchange.java
  "this.reloadingEvents"
]);
var JAVA_FIELD_TYPES = {
  "ohlcvs": { map: true, nullable: false },
  "orderbooks": { map: true, nullable: false },
  "balance": { map: true, nullable: true },
  "options": { map: true, nullable: true },
  "markets": { map: true, nullable: true }
};
var JAVA_BOOLEAN_BASE_CALLS = /* @__PURE__ */ new Set([
  "valueIsDefined",
  "inArray",
  "isEmpty",
  "isJsonEncodedObject",
  "isBinaryMessage"
]);
var JAVA_BOOLEAN_OPERATOR_KINDS = (() => {
  const k = _typescript2.default.SyntaxKind;
  return /* @__PURE__ */ new Set([
    k.AmpersandAmpersandToken,
    k.BarBarToken,
    k.EqualsEqualsToken,
    k.EqualsEqualsEqualsToken,
    k.ExclamationEqualsToken,
    k.ExclamationEqualsEqualsToken,
    k.LessThanToken,
    k.LessThanEqualsToken,
    k.GreaterThanToken,
    k.GreaterThanEqualsToken,
    k.InKeyword,
    k.InstanceOfKeyword
  ]);
})();
var JAVA_PRECISE_BOOLEAN_STATICS = /* @__PURE__ */ new Set([
  "stringEq",
  "stringEquals",
  "stringGt",
  "stringGe",
  "stringLt",
  "stringLe"
]);
var JAVA_THIS_BOOLEAN_METHODS = /* @__PURE__ */ new Set([
  "inArray",
  // public boolean inArray (Object elem, Object list2)
  "isArray",
  // public boolean isArray (Object a)
  "isEmpty",
  // public boolean isEmpty (Object a)
  "valueIsDefined",
  // public boolean valueIsDefined (Object value)
  "isJsonEncodedObject",
  // public boolean isJsonEncodedObject (Object str)
  "isBinaryMessage"
  // public boolean isBinaryMessage (Object message)
]);
var JAVA_THIS_BOOLEAN_BOX_METHODS = {
  "safeBool": 2,
  "safeBool2": 3,
  "safeBoolN": 2
};
var JAVA_DECLARED_MAP_TYPES = /^(java\.util\.)?(Map|HashMap)\s*<\s*String\s*,\s*Object\s*>$/;
var JAVA_DECLARED_LIST_TYPES = /^(java\.util\.)?(List|ArrayList)\s*<[^;\n=]+>$/;
var JAVA_PRIMITIVE_DECLARED_TYPES = /* @__PURE__ */ new Set(["boolean", "byte", "char", "short", "int", "long", "float", "double"]);
var JAVA_DECLARED_NUMERIC_TYPES = /* @__PURE__ */ new Set(["Integer", "Long", "Double", "int", "long", "double"]);
var JAVA_BOXED_NUMERIC_TYPES = /* @__PURE__ */ new Set(["Integer", "Long", "Double"]);
var JAVA_SPLIT_RECEIVER_KINDS = /* @__PURE__ */ new Set([
  _typescript2.default.SyntaxKind.Identifier,
  _typescript2.default.SyntaxKind.PropertyAccessExpression,
  _typescript2.default.SyntaxKind.ElementAccessExpression,
  _typescript2.default.SyntaxKind.CallExpression,
  _typescript2.default.SyntaxKind.ParenthesizedExpression,
  _typescript2.default.SyntaxKind.StringLiteral,
  _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral
]);
var JAVA_LIST_BACKED_TS_CLASSES = /* @__PURE__ */ new Set([
  "ArrayCache",
  "ArrayCacheByTimestamp",
  "ArrayCacheBySymbolById",
  "ArrayCacheByOutcomeById",
  "ArrayCacheBySymbolBySide",
  "OrderBookSide",
  "IndexedOrderBookSide",
  "Asks",
  "Bids",
  "IndexedAsks",
  "IndexedBids",
  "IOrderBookSide"
]);
var JAVA_DECLARED_STRING_TYPE = /^(java\.util\.)?String$/;
var JAVA_NATIVE_PARAMETER_TYPES = {
  "Dict": "java.util.Map<String, Object>",
  "Market": "java.util.Map<String, Object>",
  "Currency": "java.util.Map<String, Object>",
  "Str": "String",
  "OrderType": "String",
  "OrderSide": "String",
  "Bool": "Boolean"
};
var JAVA_NATIVE_PARAMETER_TYPES_OPTIONAL = {
  "Dict": "java.util.Map<String, Object>",
  "Market": "java.util.Map<String, Object>",
  "Currency": "java.util.Map<String, Object>",
  "Str": "String",
  "OrderType": "String",
  "OrderSide": "String",
  "Int": "Long",
  "Strings": "java.util.List<String>"
};
var JAVA_STRINGS_OPTIONAL_PARAMETER_NAMES = /* @__PURE__ */ new Set(["symbols"]);
var JAVA_STRING_LIST_TYPE = "java.util.List<String>";
var JAVA_STRINGS_EXCLUDED_METHODS = /* @__PURE__ */ new Set(["marketSymbols", "marketIds", "marketsForSymbols", "getMarketFromSymbols"]);
var JAVA_NATIVE_PARAMETER_SOURCE_FILES = /(^|\/)ts\/src\/base\/types\.ts$/;
var JAVA_NATIVE_PARAMETER_EXCLUDED_POSITIONS = {
  // implicit endpoints pass fetch2/request/sign params through untyped (arrays for batch orders)
  "fetch2": [3],
  // the Exchange tier keeps fetchOrderBook's symbol Object; PredictionExchange overrides must erase alike
  "fetchOrderBook": [0],
  "request": [3],
  "sign": [3],
  "handleErrors": [4],
  "parseOrder": [0],
  "parseTrade": [0],
  "amountToPrecision": [0],
  "client": [0],
  "costToPrecision": [0],
  "handleOptionAndParams": [1],
  "market": [0],
  "marketSymbols": [1],
  "parseAccount": [0],
  "parseMarket": [0],
  "parseOrderBook": [1],
  "priceToPrecision": [0],
  "safeBalance": [0],
  "safeOpenInterest": [0],
  "safeSymbol": [0],
  // java/tests/src/main/java/tests/base/TestSafeTicker.java is generated by the
  // --baseTests stage, which the farm's java target never reruns: it calls
  // `exchange.safeTicker(<Object local>)` and cannot be regenerated with a checkcast
  "safeTicker": [0],
  "symbol": [0],
  "watch": [0, 1]
};
var JAVA_NATIVE_PARAMETER_GENERATED_FILES = /(^|\/)ts\/src\/(?:pro\/|prediction\/)?[a-z0-9_]+\.ts$/;
var JAVA_TRANSPILE_DELIMITER = "METHODS BELOW THIS LINE ARE TRANSPILED FROM TYPESCRIPT";
var JAVA_HANDWRITTEN_FULL_ARITY = /* @__PURE__ */ new Set(["loadMarkets", "fetchMarkets", "fetchCurrencies"]);
var JAVA_NATIVE_PARAMETER_BASE_FILES = /(^|[\\/])ts[\\/]src[\\/]base[\\/](Prediction)?Exchange(\.nooverloads[^/]*)?\.ts$/;
var JAVA_NATIVE_RETURN_MAP_TYPE = "java.util.Map<String, Object>";
var JAVA_STRING_RETURN_BASE_METHODS = /* @__PURE__ */ new Set([
  "safeString",
  "safeString2",
  "safeStringN",
  "safeStringUpper",
  "safeStringUpper2",
  "safeStringUpperN",
  "safeStringLower",
  "safeStringLower2",
  "safeStringLowerN",
  "decimalToPrecision",
  "numberToString",
  "formatNumber",
  "implodeParams",
  "implodeHostname"
]);
var JAVA_STRING_RETURN_BASE_FILES = /(^|[\\/])ts[\\/]src[\\/]base[\\/](?:functions[\\/](?:type|time)\.ts|Exchange(\.nooverloads[^/]*)?\.ts)$/;
var JAVA_NATIVE_PARAMETER_PREDICTION_FILES = /(^|\/)ts\/src\/prediction\/[a-z0-9_]+\.ts$/;
var JAVA_BOOLEAN_EXCLUDED_TYPE_FLAGS = _typescript2.default.TypeFlags.Any | _typescript2.default.TypeFlags.Unknown | _typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.Void | _typescript2.default.TypeFlags.Never | _typescript2.default.TypeFlags.TypeParameter | _typescript2.default.TypeFlags.Conditional | _typescript2.default.TypeFlags.Enum | _typescript2.default.TypeFlags.EnumLiteral;
var JAVA_NULLABLE_BOOLEAN_MEMBER_FLAGS = _typescript2.default.TypeFlags.Boolean | _typescript2.default.TypeFlags.BooleanLiteral | _typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.Void;
var JAVA_BOOLEAN_BOX_TUPLE_METHODS = /* @__PURE__ */ new Set([
  "handleParamBool",
  "handleParamBool2"
]);
var JavaTranspiler = class extends BaseTranspiler {
  constructor(config = {}) {
    config["parser"] = Object.assign({}, parserConfig5, _nullishCoalesce(config["parser"], () => ( {})));
    super(config);
    this._baseExchangeMethodsByName = /* @__PURE__ */ new WeakMap();
    this.varListFromObjectLiterals = {};
    // binary operators whose printed Java is a primitive boolean: Helpers.isEqual (and the
    // negated `!Helpers.isEqual` / `<` / `>` / `<=` / `>=` family), Helpers.inOp,
    // Helpers.isInstance and the native `&&` / `||`
    this.javaBooleanOperators = [
      _typescript2.default.SyntaxKind.EqualsEqualsToken,
      _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken,
      _typescript2.default.SyntaxKind.ExclamationEqualsToken,
      _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken,
      _typescript2.default.SyntaxKind.LessThanToken,
      _typescript2.default.SyntaxKind.LessThanEqualsToken,
      _typescript2.default.SyntaxKind.GreaterThanToken,
      _typescript2.default.SyntaxKind.GreaterThanEqualsToken,
      _typescript2.default.SyntaxKind.AmpersandAmpersandToken,
      _typescript2.default.SyntaxKind.BarBarToken,
      _typescript2.default.SyntaxKind.InKeyword,
      _typescript2.default.SyntaxKind.InstanceOfKeyword
    ];
    // Per-function analysis results. Populated by analyzeFinalVars at the start of
    // printFunctionBody and consumed during printing of the same function body.
    this.usageToFinalName = /* @__PURE__ */ new WeakMap();
    // Stack of emitted-finalName sets, one entry per enclosing block. Pushed on block
    // entry, popped on exit. Used by buildFinalVarDeclarations to dedup: skip if the
    // finalName is already in scope via any ancestor.
    this.finalVarScopeStack = [];
    // Identifiers rewritten in place to their finalXxx name during the current emit.
    // The parsed ts.SourceFile is cached and reused across transpile calls, so the
    // rewrite has to be undone when the emit ends — otherwise a second emit of the
    // same file reads `finalX` where the first read `x`, the symbol no longer
    // resolves, and the hoisted `final Object finalX = x;` declaration is dropped
    // while its usages remain.
    this.finalVarMutations = [];
    // Java expression passed as the second supplyAsync argument for async methods.
    // Empty (the default) emits the single-argument, common-pool supplyAsync form.
    this.asyncExecutor = "";
    // Static method emitted in place of java.util.concurrent.CompletableFuture.supplyAsync
    // for async methods. The callee owns the executor choice, so no second argument is emitted.
    this.asyncSupplier = "";
    // method declarations (first per name) of the `Exchange` class of ts/src/base/Exchange.ts, read off the
    // program the warp ran on. A prediction venue's method with one of these names overrides
    // the tier body javaTranspiler.ts injects into PredictionExchange.java.
    this._exchangeTierMethods = void 0;
    // the names the enclosing method body assigns with a compound operator (`x += ..`),
    // by method node; a plain assignment is handled by javaParameterAssignmentCast
    this.javaMethodAssignedNames = /* @__PURE__ */ new WeakMap();
    // D-09 memo/cycle guard for javaNativeReturnType (mutually recursive return chains)
    this.javaReturnTypeCache = /* @__PURE__ */ new WeakMap();
    this.javaReturnTypeInProgress = /* @__PURE__ */ new Set();
    // `file:method` of every async method whose body reassigns a parameter (unsupported in Java lambdas)
    this.javaReassigningMethods = [];
    // the embedding build turns a reassigned async parameter into a hard transpile error
    this.javaStrictEffectivelyFinal = false;
    this.csModifiers = {};
    // literals printed as `Helpers.newMap(k, v, ...)` (no anonymous class, so no capture rule)
    this.builderObjectLiterals = /* @__PURE__ */ new WeakSet();
    this.requiresParameterType = true;
    this.requiresReturnType = true;
    this.asyncTranspiling = true;
    this.implicitAsyncTranspiling = true;
    this.supportsFalsyOrTruthyValues = false;
    this.requiresCallExpressionCast = true;
    this.id = "Java";
    this.initConfig();
    this.applyUserOverrides(config);
    this.asyncExecutor = _nullishCoalesce(config["asyncExecutor"], () => ( ""));
    this.asyncSupplier = _nullishCoalesce(config["asyncSupplier"], () => ( ""));
  }
  // the embedding build layer (build/java-local-types.js) installs this: it names the
  // Java type of a local whose printed declaration line it rewrote (`Long`/`Double`).
  // The arithmetic rule reads it for identifier operands only.
  countRequiredParameters(declaration) {
    const params = _nullishCoalesce(_optionalChain([declaration, 'optionalAccess', _1098 => _1098.parameters]), () => ( []));
    let required = 0;
    for (const p of params) {
      if (p.initializer === void 0 && p.questionToken === void 0 && p.dotDotDotToken === void 0) {
        required++;
      }
    }
    return required;
  }
  printArgsForCallExpression(node, identation) {
    const fullArity = this.javaFullArityCallArguments(_nullishCoalesce(node.arguments, () => ( [])), node, identation);
    if (fullArity !== void 0) {
      return fullArity;
    }
    let args = _nullishCoalesce(node.arguments, () => ( []));
    const callee = node.expression;
    const isThisCall = _optionalChain([callee, 'optionalAccess', _1099 => _1099.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression && _optionalChain([callee, 'access', _1100 => _1100.expression, 'optionalAccess', _1101 => _1101.kind]) === _typescript2.default.SyntaxKind.ThisKeyword;
    if (isThisCall && args.length > 0) {
      const last = args[args.length - 1];
      const isNullish = last.kind === _typescript2.default.SyntaxKind.NullKeyword || last.kind === _typescript2.default.SyntaxKind.Identifier && last.escapedText === "undefined";
      let inOptionalTail = false;
      if (isNullish) {
        const signature = this.getChecker().getResolvedSignature(node);
        const declaration = _optionalChain([signature, 'optionalAccess', _1102 => _1102.declaration]);
        if (declaration !== void 0) {
          inOptionalTail = args.length > this.countRequiredParameters(declaration);
        }
      }
      if (isNullish && inOptionalTail) {
        args = args.slice(0, -1);
      }
    }
    return this.javaPrintCallArguments(args, node, identation);
  }
  // a call into a method whose fixed parameter prints a native type: generated locals are `Object`,
  // so the argument carries the same checkcast as native map/string reads. The checker proved the
  // argument assignable to the parameter, so the declared type describes the value received.
  javaPrintCallArguments(args, node, identation) {
    const superCore = this.javaSuperCoreCallArguments(args, node, identation);
    if (superCore !== void 0) {
      return superCore;
    }
    const scheduled = this.javaScheduledCallArguments(args, node, identation);
    if (scheduled !== void 0) {
      return scheduled;
    }
    const spawnTypes = this.javaSpawnCallParameterTypes(node);
    const parameterTypes = spawnTypes !== void 0 ? spawnTypes : this.javaNativeCallParameterTypes(node);
    return args.map((a, i) => {
      const parsedArg = this.printNode(a, identation).trim();
      const type = parameterTypes[i];
      if (type === void 0 || this.javaNativeArgumentAlreadyTyped(a, type)) {
        return parsedArg;
      }
      return `(${type}) (${parsedArg})`;
    }).join(", ");
  }
  // the implementation declaration of a resolved method (an overload signature has no body)
  javaMethodImplementation(declaration) {
    if (declaration === void 0 || !_typescript2.default.isMethodDeclaration(declaration)) {
      return void 0;
    }
    if (declaration.body !== void 0) {
      return declaration;
    }
    try {
      const symbol = this.getChecker().getSymbolAtLocation(declaration.name);
      return _optionalChain([symbol, 'optionalAccess', _1103 => _1103.declarations, 'optionalAccess', _1104 => _1104.find, 'call', _1105 => _1105((d) => _typescript2.default.isMethodDeclaration(d) && d.body !== void 0)]);
    } catch (e) {
      return void 0;
    }
  }
  // a method this printer emits with its single typed signature: hand-written Java declares
  // ts/src/base/** except the base tiers below their transpile delimiter
  javaIsPrintedMethod(method) {
    if (method === void 0 || !_typescript2.default.isMethodDeclaration(method) || method.body === void 0 || !_typescript2.default.isClassDeclaration(method.parent)) {
      return false;
    }
    const file = method.getSourceFile();
    const fileName = file.fileName.replace(/\\/g, "/");
    if (!/(^|\/)ts\/src\//.test(fileName)) {
      return true;
    }
    if (JAVA_NATIVE_PARAMETER_BASE_FILES.test(fileName)) {
      const delimiter = file.text.indexOf(JAVA_TRANSPILE_DELIMITER);
      return delimiter >= 0 && method.pos > delimiter || JAVA_HANDWRITTEN_FULL_ARITY.has(_optionalChain([method, 'access', _1106 => _1106.name, 'optionalAccess', _1107 => _1107.escapedText]));
    }
    return JAVA_NATIVE_PARAMETER_GENERATED_FILES.test(fileName);
  }
  javaHasOptionalParameter(method) {
    return (_nullishCoalesce(_optionalChain([method, 'optionalAccess', _1108 => _1108.parameters]), () => ( []))).some((p) => p.initializer !== void 0 || p.questionToken !== void 0);
  }
  // a call into a printed method with optional parameters passes every parameter: omitted or
  // `undefined` slots take the TS default, fixed slots keep the native checkcast
  javaFullArityCallArguments(args, node, identation) {
    const callee = node.expression;
    if (_optionalChain([callee, 'optionalAccess', _1109 => _1109.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([callee, 'access', _1110 => _1110.expression, 'optionalAccess', _1111 => _1111.kind]) === _typescript2.default.SyntaxKind.SuperKeyword) {
      return void 0;
    }
    let method;
    try {
      method = _nullishCoalesce(this.javaMethodImplementation(_optionalChain([this, 'access', _1112 => _1112.getChecker, 'call', _1113 => _1113(), 'access', _1114 => _1114.getResolvedSignature, 'call', _1115 => _1115(node), 'optionalAccess', _1116 => _1116.declaration])), () => ( this.javaAnyReceiverBaseMethod(callee)));
    } catch (e) {
      return void 0;
    }
    return this.javaFullArityArguments(method, args, identation);
  }
  // an `any` receiver (the tests' `exchange: any`) compiles against the Java Exchange class:
  // bind the base Exchange method of that name when it is implemented exactly once
  javaAnyReceiverBaseMethod(callee) {
    const type = this.getChecker().getTypeAtLocation(callee.expression);
    if ((type.flags & _typescript2.default.TypeFlags.Any) === 0) {
      return void 0;
    }
    const methods = this.baseExchangeMethodsByName().get(_optionalChain([callee, 'access', _1117 => _1117.name, 'optionalAccess', _1118 => _1118.escapedText]));
    return _optionalChain([methods, 'optionalAccess', _1119 => _1119.length]) === 1 ? methods[0] : void 0;
  }
  baseExchangeMethodsByName() {
    const program = this.getProgram();
    const cached = this._baseExchangeMethodsByName.get(program);
    if (cached !== void 0) {
      return cached;
    }
    const names = /* @__PURE__ */ new Map();
    const file = program.getSourceFiles().find((sf) => /(^|[\\/])ts[\\/]src[\\/]base[\\/]Exchange\.ts$/.test(sf.fileName));
    const exchange = _optionalChain([file, 'optionalAccess', _1120 => _1120.statements, 'access', _1121 => _1121.find, 'call', _1122 => _1122((s) => _typescript2.default.isClassDeclaration(s) && _optionalChain([s, 'access', _1123 => _1123.name, 'optionalAccess', _1124 => _1124.text]) === "Exchange")]);
    for (const member of _nullishCoalesce(_optionalChain([exchange, 'optionalAccess', _1125 => _1125.members]), () => ( []))) {
      if (_typescript2.default.isMethodDeclaration(member) && member.body !== void 0 && _typescript2.default.isIdentifier(member.name)) {
        const key = member.name.text;
        names.set(key, (_nullishCoalesce(names.get(key), () => ( []))).concat([member]));
      }
    }
    this._baseExchangeMethodsByName.set(program, names);
    return names;
  }
  javaFullArityArguments(method, args, identation) {
    if (!this.javaIsPrintedMethod(method) || !this.javaHasOptionalParameter(method)) {
      return void 0;
    }
    const params = method.parameters;
    if (args.length > params.length || params.some((p) => p.dotDotDotToken !== void 0) || args.some((a) => a.kind === _typescript2.default.SyntaxKind.SpreadElement)) {
      return void 0;
    }
    const types = this.javaCoreParameterTypes(method);
    return params.map((p, i) => {
      const arg = args[i];
      if (arg === void 0 || p.initializer !== void 0 && this.javaIsUndefinedLiteral(arg)) {
        return this.javaCoreDefaultArgument(p, types[i]);
      }
      const printed = this.printNode(arg, identation).trim();
      if (p.initializer !== void 0) {
        return this.javaArgumentHasType(arg, types[i]) || types[i] === "Object" ? this.javaIsUndefinedLiteral(arg) ? "null" : printed : this.javaConvertToCoreType(types[i], printed, arg);
      }
      const native = this.javaNativeParameterType(p);
      if (native === void 0 || this.javaNativeArgumentAlreadyTyped(arg, native)) {
        return printed;
      }
      return `(${native}) (${printed})`;
    }).join(", ");
  }
  javaIsUndefinedLiteral(node) {
    return _optionalChain([node, 'optionalAccess', _1126 => _1126.kind]) === _typescript2.default.SyntaxKind.Identifier && node.escapedText === "undefined";
  }
  // `this.spawn(this.m, a..)` / `this.delay(ms, this.m, a..)`: the ccxt post-pass turns the
  // reference into a lambda calling `this.m(a..)`, so those arguments take m's full arity
  javaScheduledCallArguments(args, node, identation) {
    const callee = node.expression;
    if (_optionalChain([callee, 'optionalAccess', _1127 => _1127.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([callee, 'access', _1128 => _1128.expression, 'optionalAccess', _1129 => _1129.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return void 0;
    }
    const at = _optionalChain([callee, 'access', _1130 => _1130.name, 'optionalAccess', _1131 => _1131.escapedText]) === "spawn" ? 0 : _optionalChain([callee, 'access', _1132 => _1132.name, 'optionalAccess', _1133 => _1133.escapedText]) === "delay" ? 1 : -1;
    const reference = at >= 0 ? args[at] : void 0;
    if (_optionalChain([reference, 'optionalAccess', _1134 => _1134.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([reference, 'access', _1135 => _1135.expression, 'optionalAccess', _1136 => _1136.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return void 0;
    }
    let method;
    try {
      const symbol = this.getChecker().getSymbolAtLocation(reference.name);
      method = this.javaMethodImplementation(_nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1137 => _1137.valueDeclaration]), () => ( _optionalChain([symbol, 'optionalAccess', _1138 => _1138.declarations, 'optionalAccess', _1139 => _1139[0]]))));
    } catch (e) {
      return void 0;
    }
    const forwarded = this.javaFullArityArguments(method, args.slice(at + 1), identation);
    if (forwarded === void 0) {
      return void 0;
    }
    const head = args.slice(0, at + 1).map((a) => this.printNode(a, identation).trim());
    return head.concat(forwarded.length > 0 ? [forwarded] : []).join(", ");
  }
  // `super.x(..)` into a split method must bind the typed core: an ancestor bridge re-dispatches
  // through `this`, which lands back in the overriding core (infinite recursion)
  javaSuperCoreCallArguments(args, node, identation) {
    const callee = node.expression;
    if (_optionalChain([callee, 'optionalAccess', _1140 => _1140.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([callee, 'access', _1141 => _1141.expression, 'optionalAccess', _1142 => _1142.kind]) !== _typescript2.default.SyntaxKind.SuperKeyword) {
      return void 0;
    }
    let declaration;
    try {
      declaration = this.javaMethodImplementation(_optionalChain([this, 'access', _1143 => _1143.getChecker, 'call', _1144 => _1144(), 'access', _1145 => _1145.getResolvedSignature, 'call', _1146 => _1146(node), 'optionalAccess', _1147 => _1147.declaration]));
    } catch (e) {
      return void 0;
    }
    if (declaration === void 0 || !this.hasDefaultedTail(declaration)) {
      return void 0;
    }
    const params = declaration.parameters;
    if (params.some((p) => p.dotDotDotToken !== void 0)) {
      return void 0;
    }
    if (args.length > params.length) {
      return void 0;
    }
    const types = this.javaCoreParameterTypes(declaration);
    return params.map((p, i) => i < args.length ? this.javaArgumentHasType(args[i], types[i]) ? this.printNode(args[i], identation).trim() : this.javaConvertToCoreType(types[i], this.printNode(args[i], identation).trim(), args[i]) : this.javaCoreDefaultArgument(p, types[i])).join(", ");
  }
  // an argument that is a parameter of the enclosing method already printed with this type
  javaArgumentHasType(arg, type) {
    if (_optionalChain([arg, 'optionalAccess', _1148 => _1148.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    let declaration;
    try {
      declaration = _optionalChain([this, 'access', _1149 => _1149.getChecker, 'call', _1150 => _1150(), 'access', _1151 => _1151.getSymbolAtLocation, 'call', _1152 => _1152(arg), 'optionalAccess', _1153 => _1153.valueDeclaration]);
    } catch (e) {
      return false;
    }
    if (declaration === void 0 || !_typescript2.default.isParameter(declaration)) {
      return false;
    }
    const method = declaration.parent;
    if (!_typescript2.default.isMethodDeclaration(method) && this.ReassignedVars[this.getVarKey(declaration)] && this.isAsyncFunction(method)) {
      return false;
    }
    const printed = declaration.initializer !== void 0 ? this.hasDefaultedTail(method) ? this.javaOptionalParameterJavaType(declaration) : "Object" : (this.printParameterType(declaration) || "Object").trim();
    return this.javaErasure(printed) === this.javaErasure(type);
  }
  // the printed Java type of every parameter of a split method's typed core
  javaCoreParameterTypes(method) {
    return method.parameters.map((p) => p.initializer !== void 0 ? this.javaOptionalParameterJavaType(p) : (this.printParameterType(p) || "Object").trim());
  }
  // an omitted parameter of a typed-core call: its TS default, typed like the front's reader
  javaCoreDefaultArgument(param, type) {
    const initializer = param.initializer;
    const value = initializer === void 0 || !this.isPureInitializer(initializer) ? "null" : this.printNode(initializer, 0);
    if (value === "null") {
      return `(${type}) null`;
    }
    if (type === "Long" && /^-?\d+$/.test(value)) {
      return value + "L";
    }
    if (type === JAVA_STRING_LIST_TYPE && _typescript2.default.isArrayLiteralExpression(initializer) && initializer.elements.length === 0) {
      return "new java.util.ArrayList<String>()";
    }
    if (type === "Object" || type === "String" && _typescript2.default.isStringLiteralLike(initializer) || type === "java.util.Map<String, Object>" && _typescript2.default.isObjectLiteralExpression(initializer)) {
      return value;
    }
    return this.javaConvertToCoreType(type, value, initializer);
  }
  // a value of any static type converted to a typed-core parameter, with the fronts' semantics
  javaConvertToCoreType(type, printed, node) {
    if (_optionalChain([node, 'optionalAccess', _1154 => _1154.kind]) === _typescript2.default.SyntaxKind.NullKeyword || _optionalChain([node, 'optionalAccess', _1155 => _1155.kind]) === _typescript2.default.SyntaxKind.Identifier && node.escapedText === "undefined") {
      return `(${type}) null`;
    }
    if (type === "Object") {
      return `(Object) (${printed})`;
    }
    if (type === "Long") {
      return /^-?\d+L$/.test(printed) ? printed : `Helpers.toLongOrNull(${printed})`;
    }
    if (type === "String") {
      return this.javaNativeArgumentAlreadyTyped(node, type) ? printed : `Helpers.toStringArg(${printed})`;
    }
    if (type === "java.util.Map<String, Object>") {
      return `Helpers.toMapArg(${printed})`;
    }
    if (type === JAVA_STRING_LIST_TYPE) {
      return `Helpers.toStringListArg(${printed})`;
    }
    return `(${type}) (${printed})`;
  }
  // Java erasure of a printed type, for override/bridge comparisons
  javaErasure(type) {
    return type.replace(/<.*>/, "").replace(/^java\.util\./, "").trim();
  }
  // An override whose typed core differs from an ancestor's (another parameter count, another
  // default position or type) no longer overrides it in Java: a bridge with the ancestor's
  // signature forwards to this method, so base code calling the ancestor core reaches it.
  printOverrideBridges(node, identation) {
    const ownTypes = this.javaCoreParameterTypes(node);
    const ownKey = ownTypes.map((t) => this.javaErasure(t)).join(",");
    const seen = /* @__PURE__ */ new Set();
    let out = "";
    let ancestor;
    try {
      ancestor = this.getMethodOverride(node);
    } catch (e) {
      return "";
    }
    while (ancestor !== void 0) {
      if (this.hasDefaultedTail(ancestor)) {
        const ancestorTypes = this.javaCoreParameterTypes(ancestor);
        const key = ancestorTypes.map((t) => this.javaErasure(t)).join(",");
        if (key !== ownKey && !seen.has(key)) {
          seen.add(key);
          out += this.printOverrideBridge(node, ancestor, ancestorTypes, ownTypes, identation);
        }
      }
      try {
        ancestor = this.getMethodOverride(ancestor);
      } catch (e) {
        ancestor = void 0;
      }
    }
    return out;
  }
  printOverrideBridge(node, ancestor, ancestorTypes, ownTypes, identation) {
    const name = this.transformMethodNameIfNeeded(node.name.escapedText);
    const ancestorNames = ancestor.parameters.map((p) => this.printNode(p.name, 0));
    const ancestorDef = this.printMethodDefinition(
      ancestor,
      identation,
      () => ancestorTypes.map((t, i) => `${t} ${ancestorNames[i]}`).join(", ")
    );
    const forwarded = node.parameters.map((p, i) => i < ancestorNames.length ? this.javaErasure(ownTypes[i]) === this.javaErasure(ancestorTypes[i]) ? ancestorNames[i] : this.javaConvertToCoreType(ownTypes[i], ancestorNames[i], void 0) : this.javaCoreDefaultArgument(p, ownTypes[i])).join(", ");
    const call = `this.${name}(${forwarded})`;
    const returnOf = (def) => _nullishCoalesce(_optionalChain([def, 'access', _1156 => _1156.match, 'call', _1157 => _1157(/(?:public|protected|private)\s+(?:static\s+)?(.+?)\s+\w+\s*\(/), 'optionalAccess', _1158 => _1158[1], 'optionalAccess', _1159 => _1159.trim, 'call', _1160 => _1160()]), () => ( "Object"));
    const returnType = returnOf(ancestorDef);
    const ownReturn = returnOf(this.printMethodDefinition(node, identation, () => ""));
    const body = returnType === "void" ? `${call};` : returnType === ownReturn ? `return ${call};` : `return (${returnType}) (Object) ${call};`;
    return "\n" + ancestorDef + this.getBlockOpen(identation) + this.getIden(identation + 1) + body + this.getBlockClose(identation);
  }
  // `this.spawn(this.someMethod, args...)`: the spawned work executes `this.someMethod(args)`
  // (the ccxt post-pass rewrites the reference into a lambda), so the arguments belong to the
  // referenced method's signature, not to spawn's `...args` - each one carries the checkcast
  javaSpawnCallParameterTypes(node) {
    const callee = node.expression;
    if (_optionalChain([callee, 'optionalAccess', _1161 => _1161.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([callee, 'access', _1162 => _1162.expression, 'optionalAccess', _1163 => _1163.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword || _optionalChain([callee, 'access', _1164 => _1164.name, 'optionalAccess', _1165 => _1165.escapedText]) !== "spawn") {
      return void 0;
    }
    const reference = (_nullishCoalesce(node.arguments, () => ( [])))[0];
    if (_optionalChain([reference, 'optionalAccess', _1166 => _1166.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([reference, 'access', _1167 => _1167.expression, 'optionalAccess', _1168 => _1168.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword || _optionalChain([reference, 'access', _1169 => _1169.name, 'optionalAccess', _1170 => _1170.escapedText]) === void 0) {
      return void 0;
    }
    let declaration;
    try {
      const symbol = this.getChecker().getSymbolAtLocation(reference.name);
      declaration = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1171 => _1171.valueDeclaration]), () => ( _optionalChain([symbol, 'optionalAccess', _1172 => _1172.declarations, 'optionalAccess', _1173 => _1173[0]])));
    } catch (e) {
      return void 0;
    }
    const parameters = _optionalChain([declaration, 'optionalAccess', _1174 => _1174.parameters]);
    if (parameters === void 0) {
      return void 0;
    }
    return (_nullishCoalesce(node.arguments, () => ( []))).map((a, i) => {
      if (i === 0) {
        return void 0;
      }
      const param = parameters[i - 1];
      if (param === void 0 || !_typescript2.default.isParameter(param)) {
        return void 0;
      }
      return this.javaNativeParameterType(param);
    });
  }
  // the argument is a literal (or a String the embedding build layer's resolver proves)
  // and the parameter declares exactly that type, so no cast is needed. Every other
  // argument prints from a local the printer declares `Object`, and needs the checkcast.
  javaNativeArgumentAlreadyTyped(arg, type) {
    if (arg.kind === _typescript2.default.SyntaxKind.NullKeyword) {
      return true;
    }
    if (type !== "String") {
      return false;
    }
    if (_typescript2.default.isStringLiteralLike(arg)) {
      return true;
    }
    return this.javaProvableString(arg);
  }
  // the native printed type of each argument position of a call, when the resolved
  // signature declares that parameter natively
  javaNativeCallParameterTypes(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return [];
    }
    const declaration = _optionalChain([checker, 'access', _1175 => _1175.getResolvedSignature, 'call', _1176 => _1176(node), 'optionalAccess', _1177 => _1177.declaration]);
    const parameters = _optionalChain([declaration, 'optionalAccess', _1178 => _1178.parameters]);
    if (parameters === void 0) {
      return [];
    }
    return (_nullishCoalesce(node.arguments, () => ( []))).map((a, i) => {
      const param = parameters[i];
      if (param === void 0 || !_typescript2.default.isParameter(param)) {
        return void 0;
      }
      return this.javaNativeParameterType(param);
    });
  }
  initConfig() {
    this.LeftPropertyAccessReplacements = {
      // 'this': '$this',
    };
    this.RightPropertyAccessReplacements = {
      // Java list/string methods (lowerCamelCase)
      push: "add",
      indexOf: "indexOf",
      toUpperCase: "toUpperCase",
      toLowerCase: "toLowerCase",
      toString: "toString"
    };
    this.FullPropertyAccessReplacements = {
      "JSON.parse": "parseJson",
      "console.log": "System.out.println",
      "Number.MAX_SAFE_INTEGER": "Long.MAX_VALUE",
      "Math.min": "Math.min",
      "Math.max": "Math.max",
      "Math.log": "Math.log",
      "Math.abs": "Math.abs",
      "Math.floor": "Math.floor",
      "Math.pow": "Math.pow"
      // 'Promise.all' handled via promiseAll wrapper
    };
    this.CallExpressionReplacements = {
      "parseInt": "Helpers.parseInt",
      "parseFloat": "Helpers.parseFloat"
      // Add ad-hoc function call rewrites here if you need them
    };
    this.ReservedKeywordsReplacements = {
      string: "str",
      object: "obj",
      params: "parameters",
      // base: "bs",
      internal: "intern",
      event: "eventVar",
      fixed: "fixedVar",
      final: "finalVar",
      native: "nativeVar"
      // add Java keywords if you need to avoid collisions (e.g., enum, assert)
    };
    this.VariableTypeReplacements = {
      string: "String",
      Str: "String",
      number: "double",
      Int: "long",
      Num: "double",
      Dict: "java.util.Map<String, Object>",
      Strings: "java.util.List<String>",
      List: "java.util.List<Object>",
      boolean: "boolean",
      object: "Object"
    };
    this.ArgTypeReplacements = {
      string: "String",
      Str: "String",
      number: "double",
      Int: "long",
      Num: "double",
      Dict: "java.util.Map<String, Object>",
      Strings: "java.util.List<String>",
      List: "java.util.List<Object>",
      boolean: "boolean",
      object: "Object"
    };
    this.binaryExpressionsWrappers = {
      [_typescript2.default.SyntaxKind.EqualsEqualsToken]: [
        this.EQUALS_EQUALS_WRAPPER_OPEN,
        this.EQUALS_EQUALS_WRAPPER_CLOSE
      ],
      [_typescript2.default.SyntaxKind.EqualsEqualsEqualsToken]: [
        this.EQUALS_EQUALS_WRAPPER_OPEN,
        this.EQUALS_EQUALS_WRAPPER_CLOSE
      ],
      [_typescript2.default.SyntaxKind.ExclamationEqualsToken]: [
        this.DIFFERENT_WRAPPER_OPEN,
        this.DIFFERENT_WRAPPER_CLOSE
      ],
      [_typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken]: [
        this.DIFFERENT_WRAPPER_OPEN,
        this.DIFFERENT_WRAPPER_CLOSE
      ],
      [_typescript2.default.SyntaxKind.GreaterThanToken]: [
        this.GREATER_THAN_WRAPPER_OPEN,
        this.GREATER_THAN_WRAPPER_CLOSE
      ],
      [_typescript2.default.SyntaxKind.GreaterThanEqualsToken]: [
        this.GREATER_THAN_EQUALS_WRAPPER_OPEN,
        this.GREATER_THAN_EQUALS_WRAPPER_CLOSE
      ],
      [_typescript2.default.SyntaxKind.LessThanToken]: [
        this.LESS_THAN_WRAPPER_OPEN,
        this.LESS_THAN_WRAPPER_CLOSE
      ],
      [_typescript2.default.SyntaxKind.LessThanEqualsToken]: [
        this.LESS_THAN_EQUALS_WRAPPER_OPEN,
        this.LESS_THAN_EQUALS_WRAPPER_CLOSE
      ],
      [_typescript2.default.SyntaxKind.PlusToken]: [
        this.PLUS_WRAPPER_OPEN,
        this.PLUS_WRAPPER_CLOSE
      ],
      [_typescript2.default.SyntaxKind.MinusToken]: [
        this.MINUS_WRAPPER_OPEN,
        this.MINUS_WRAPPER_CLOSE
      ],
      [_typescript2.default.SyntaxKind.AsteriskToken]: [
        this.MULTIPLY_WRAPPER_OPEN,
        this.MULTIPLY_WRAPPER_CLOSE
      ],
      [_typescript2.default.SyntaxKind.PercentToken]: [
        this.MOD_WRAPPER_OPEN,
        this.MOD_WRAPPER_CLOSE
      ],
      [_typescript2.default.SyntaxKind.SlashToken]: [
        this.DIVIDE_WRAPPER_OPEN,
        this.DIVIDE_WRAPPER_CLOSE
      ]
    };
  }
  getBlockOpen(identation) {
    return "\n" + this.getIden(identation) + this.BLOCK_OPENING_TOKEN + "\n";
  }
  getCustomClassName(node) {
    return this.capitalize(node.name.escapedText);
  }
  getClassModifier(node) {
    return "public ";
  }
  printSuperCallInsideConstructor(_node, _identation) {
    return "";
  }
  printNumericLiteral(node) {
    const javaMax = 2147483647;
    const nodeText = node.text;
    if (Number(nodeText) > javaMax && Number.isInteger(Number(nodeText)) && node.text.indexOf("e") === -1) {
      return `${nodeText}L`;
    }
    return node.text;
  }
  printIdentifier(node) {
    let idValue = _nullishCoalesce(node.text, () => ( node.escapedText));
    if (this.ReservedKeywordsReplacements[idValue]) {
      idValue = this.ReservedKeywordsReplacements[idValue];
    }
    if (idValue === "undefined") {
      return this.UNDEFINED_TOKEN;
    }
    const isInsideNewExpression = _optionalChain([node, 'optionalAccess', _1179 => _1179.parent, 'optionalAccess', _1180 => _1180.kind]) === _typescript2.default.SyntaxKind.NewExpression;
    const isInsideCatch = _optionalChain([node, 'optionalAccess', _1181 => _1181.parent, 'optionalAccess', _1182 => _1182.kind]) === _typescript2.default.SyntaxKind.ThrowStatement;
    const isLeftSide = _optionalChain([node, 'optionalAccess', _1183 => _1183.parent, 'optionalAccess', _1184 => _1184.name]) === node || _optionalChain([node, 'optionalAccess', _1185 => _1185.parent, 'optionalAccess', _1186 => _1186.left]) === node;
    const isCallOrPropertyAccess = _optionalChain([node, 'optionalAccess', _1187 => _1187.parent, 'optionalAccess', _1188 => _1188.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([node, 'optionalAccess', _1189 => _1189.parent, 'optionalAccess', _1190 => _1190.kind]) === _typescript2.default.SyntaxKind.ElementAccessExpression;
    if (!isLeftSide && !isCallOrPropertyAccess && !isInsideCatch && !isInsideNewExpression) {
      const type = this.getChecker().getTypeAtLocation(node);
      const typeSymbol = _optionalChain([type, 'optionalAccess', _1191 => _1191.symbol]);
      if (typeSymbol !== void 0) {
        const decl = _nullishCoalesce(_optionalChain([typeSymbol, 'optionalAccess', _1192 => _1192.declarations]), () => ( []));
        let isBuiltIn = void 0;
        if (decl.length > 0) {
          isBuiltIn = decl[0].getSourceFile().fileName.indexOf("typescript") > -1;
        }
        if (isBuiltIn !== void 0 && !isBuiltIn) {
          const symbol = this.getChecker().getSymbolAtLocation(node);
          let isClassDeclaration = false;
          if (symbol) {
            const first = symbol.declarations[0];
            if (first.kind === _typescript2.default.SyntaxKind.ClassDeclaration) {
              isClassDeclaration = true;
            }
            if (first.kind === _typescript2.default.SyntaxKind.ImportSpecifier) {
              const importedSymbol = this.getChecker().getAliasedSymbol(symbol);
              if (_optionalChain([importedSymbol, 'optionalAccess', _1193 => _1193.declarations, 'access', _1194 => _1194[0], 'optionalAccess', _1195 => _1195.kind]) === _typescript2.default.SyntaxKind.ClassDeclaration) {
                isClassDeclaration = true;
              }
            }
          }
          if (isClassDeclaration) {
            return `${idValue}.class`;
          }
        }
      }
    }
    return this.transformIdentifier(node, idValue);
  }
  printConstructorDeclaration(node, identation) {
    const classNode = node.parent;
    const className = this.printNode(classNode.name, 0);
    const args = this.printMethodParameters(node);
    const constructorBody = this.printFunctionBody(node, identation);
    let superCallParams = "";
    let hasSuperCall = false;
    _optionalChain([node, 'access', _1196 => _1196.body, 'optionalAccess', _1197 => _1197.statements, 'access', _1198 => _1198.forEach, 'call', _1199 => _1199((statement) => {
      if (_typescript2.default.isExpressionStatement(statement)) {
        const expression = statement.expression;
        if (_typescript2.default.isCallExpression(expression)) {
          const expressionText = expression.expression.getText().trim();
          if (expressionText === "super") {
            hasSuperCall = true;
            superCallParams = expression.arguments.map((a) => {
              return this.printNode(a, identation).trim();
            }).join(", ");
          }
        }
      }
    })]);
    const header = this.getIden(identation) + className + "(" + args + ")";
    if (!hasSuperCall) {
      return header + constructorBody;
    }
    const injected = this.injectLeadingInBody(constructorBody, `super(${superCallParams});`);
    return header + injected;
  }
  injectLeadingInBody(body, firstLine) {
    const lines = body.split("\n");
    if (lines.length >= 2) {
      lines.splice(1, 0, this.getIden(1) + firstLine);
    }
    return lines.join("\n");
  }
  printDynamicCall(node, identation) {
    const elementAccess = node.expression;
    if (_optionalChain([elementAccess, 'optionalAccess', _1200 => _1200.kind]) === _typescript2.default.SyntaxKind.ElementAccessExpression) {
      const parsedArg = _optionalChain([node, 'access', _1201 => _1201.arguments, 'optionalAccess', _1202 => _1202.length]) > 0 ? node.arguments.map((n) => this.printNode(n, identation).trimStart()).join(", ") : "";
      const target = this.printNode(elementAccess.expression, 0);
      const propName = this.printNode(elementAccess.argumentExpression, 0);
      const argsArray = `new Object[] { ${parsedArg} }`;
      const open = this.DYNAMIC_CALL_OPEN;
      return `${open}${target}, ${propName}, ${argsArray})`;
    }
    return void 0;
  }
  // printElementAccessExpressionExceptionIfAny(node) {
  //     const tsKind = ts.SyntaxKind;
  //     if (node.expression.kind === tsKind.CallExpression) {
  //         const callExp = node.expression;
  //         const calleeText = callExp.expression.getText();
  //         if (calleeText.endsWith('.split') || calleeText.toLowerCase().includes('split')) {
  //             // print Split call normally (should already close with ))
  //             let splitCall = this.printNode(callExp, 0).trim();
  //             if (!splitCall.endsWith(')')) {
  //                 splitCall += ')';
  //             }
  //             const idxArg = this.printNode(node.argumentExpression, 0);
  //             return `GetValue(${splitCall}, ${idxArg})`;
  //         }
  //     }
  //     // default: no exception
  //     return undefined;
  // }
  printWrappedUnknownThisProperty(node) {
    const type = this.getChecker().getResolvedSignature(node);
    if (_optionalChain([type, 'optionalAccess', _1203 => _1203.declaration]) === void 0) {
      let parsedArguments = _optionalChain([node, 'access', _1204 => _1204.arguments, 'optionalAccess', _1205 => _1205.map, 'call', _1206 => _1206((a) => this.printNode(a, 0)), 'access', _1207 => _1207.join, 'call', _1208 => _1208(", ")]);
      parsedArguments = parsedArguments ? parsedArguments : "";
      const propName = _optionalChain([node, 'access', _1209 => _1209.expression, 'optionalAccess', _1210 => _1210.name, 'access', _1211 => _1211.escapedText]);
      const isAsyncDecl = _optionalChain([node, 'optionalAccess', _1212 => _1212.parent, 'optionalAccess', _1213 => _1213.kind]) === _typescript2.default.SyntaxKind.AwaitExpression;
      const argsArray = `new Object[] { ${parsedArguments} }`;
      const open = this.DYNAMIC_CALL_OPEN;
      const statement = `${open}this, "${propName}", ${argsArray})`;
      return statement;
    }
    return void 0;
  }
  printOutOfOrderCallExpressionIfAny(node, identation) {
    if (node.expression.kind === _typescript2.default.SyntaxKind.Identifier) {
      const callee = node.expression.escapedText;
      if (callee === "parseInt" || callee === "parseFloat") {
        const nativeParse = this.printNativeScalarParse(node, callee);
        if (nativeParse !== void 0) {
          return nativeParse;
        }
      }
    }
    if (node.expression.kind === _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      const expressionText = node.expression.getText().trim();
      const args = node.arguments;
      if (args.length === 1) {
        const parsedArg = this.printNode(args[0], 0);
        switch (expressionText) {
          case "Math.abs":
            return `Helpers.mathAbs(Double.parseDouble(${this.javaStringBoxText(args[0], parsedArg)}))`;
        }
      } else if (args.length === 2) {
        const parsedArg1 = this.printNode(args[0], 0);
        const parsedArg2 = this.printNode(args[1], 0);
        switch (expressionText) {
          case "Math.min":
          case "Math.max": {
            const native = this.printNativeMathMinMax(
              node,
              args[0],
              args[1],
              parsedArg1,
              parsedArg2,
              expressionText === "Math.min" ? "min" : "max"
            );
            if (native !== void 0) {
              return native;
            }
            const wrapper = expressionText === "Math.min" ? "Helpers.mathMin(" : "Helpers.mathMax(";
            return `${wrapper}${parsedArg1}, ${parsedArg2})`;
          }
          case "Math.pow":
            return `Math.pow(Double.parseDouble(${this.javaStringBoxText(args[0], parsedArg1)}), Double.parseDouble(${this.javaStringBoxText(args[1], parsedArg2)}))`;
        }
      }
      const leftSide = _optionalChain([node, 'access', _1214 => _1214.expression, 'optionalAccess', _1215 => _1215.expression]);
      const leftSideText = leftSide ? this.printNode(leftSide, 0) : void 0;
      if (leftSideText === this.THIS_TOKEN || leftSide.getFullText().indexOf("(this as any)") > -1) {
        const res = this.printWrappedUnknownThisProperty(node);
        if (res)
          return res;
      }
    }
    if (node.expression.kind === _typescript2.default.SyntaxKind.ElementAccessExpression) {
      return this.printDynamicCall(node, identation);
    }
    return void 0;
  }
  handleTypeOfInsideBinaryExpression(node, _identation) {
    const left = node.left;
    const right = node.right.text;
    const op = node.operatorToken.kind;
    const expression = left.expression;
    const isDifferentOperator = op === _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsToken;
    const notOperator = isDifferentOperator ? this.NOT_TOKEN : "";
    const target = this.printNode(expression, 0);
    switch (right) {
      case "string":
        return `${notOperator}(${target} instanceof String)`;
      case "number":
        return `${notOperator}(${target} instanceof Long || ${target} instanceof Integer || ${target} instanceof Float || ${target} instanceof Double)`;
      case "boolean":
        return `${notOperator}(${target} instanceof Boolean)`;
      case "object":
        return `${notOperator}(${target} instanceof java.util.Map)`;
      case "function":
        return `${notOperator}(${target} instanceof java.util.concurrent.Callable)`;
    }
    return void 0;
  }
  getVarMethodIfAny(node) {
    let current = _optionalChain([node, 'optionalAccess', _1216 => _1216.parent]);
    while (current) {
      if (_typescript2.default.isMethodDeclaration(current) || _typescript2.default.isFunctionDeclaration(current)) {
        return String(_nullishCoalesce(_optionalChain([current, 'access', _1217 => _1217.name, 'optionalAccess', _1218 => _1218.escapedText]), () => ( "")));
      }
      current = current.parent;
    }
    return "outsideAnyMethod";
  }
  getVarClassIfAny(node) {
    let current = _optionalChain([node, 'optionalAccess', _1219 => _1219.parent]);
    while (current) {
      if (_typescript2.default.isClassDeclaration(current)) {
        return String(_nullishCoalesce(_optionalChain([current, 'access', _1220 => _1220.name, 'optionalAccess', _1221 => _1221.escapedText]), () => ( "")));
      }
      current = current.parent;
    }
    return "";
  }
  getVarKey(node) {
    const varName = _nullishCoalesce(_optionalChain([node, 'optionalAccess', _1222 => _1222.escapedText]), () => ( _optionalChain([node, 'optionalAccess', _1223 => _1223.name, 'optionalAccess', _1224 => _1224.escapedText])));
    if (!varName) {
      return "";
    }
    return `${this.getVarClassIfAny(node)}-${this.getVarMethodIfAny(node)}-${varName}`;
  }
  // Static operand family of one side of an equality, undefined when the checker proves
  // nothing usable. Null/undefined union members are folded away: Objects.equals handles
  // those exactly like the helper, so `string | undefined` still counts as string.
  equalityOperandFamily(type) {
    if (type === void 0 || type === null) {
      return void 0;
    }
    const flags = type.flags;
    if (flags & _typescript2.default.TypeFlags.Intersection) {
      return void 0;
    }
    if (flags & _typescript2.default.TypeFlags.Union) {
      if (flags & _typescript2.default.TypeFlags.Boolean) {
        return "boolean";
      }
      const families = new Set((_nullishCoalesce(type.types, () => ( []))).map((t) => this.equalityOperandFamily(t)));
      families.delete(void 0);
      families.delete("null");
      return families.size === 1 ? families.values().next().value : void 0;
    }
    if (flags & (_typescript2.default.TypeFlags.String | _typescript2.default.TypeFlags.StringLiteral)) {
      return "string";
    }
    if (flags & (_typescript2.default.TypeFlags.Boolean | _typescript2.default.TypeFlags.BooleanLiteral)) {
      return "boolean";
    }
    if (flags & (_typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Void)) {
      return "null";
    }
    return void 0;
  }
  // The null/undefined literal: its Java text is `null`, so Objects.equals(x, null)
  // is literally the identity test Helpers.isEqual performs on that operand.
  isNullishLiteral(node) {
    return _optionalChain([node, 'optionalAccess', _1225 => _1225.kind]) === _typescript2.default.SyntaxKind.NullKeyword || _optionalChain([node, 'optionalAccess', _1226 => _1226.kind]) === _typescript2.default.SyntaxKind.Identifier && node.escapedText === "undefined";
  }
  // ---- numeric operand kinds (java-15) ----
  // Helpers.isEqual compares numeric operands by value (integers via toLong, Double/Float via
  // toDouble, class mismatch false), so two operands of the SAME kind compare natively.
  // the Java kind a decimal numeric literal prints with: integer literal as `N` (int) or `NL` (long),
  // a '.'/exponent literal as a Java double. TypeScript normalizes the literal text (`1e3` -> `1000`,
  // `0x10` -> `16`, `100.0` -> `100`), so node.text is exactly what prints.
  javaEqualityLiteralKind(node) {
    if (!node) {
      return void 0;
    }
    if (node.kind === _typescript2.default.SyntaxKind.PrefixUnaryExpression && node.operator === _typescript2.default.SyntaxKind.MinusToken) {
      return this.javaIntegerLiteralKind(node.operand) === "int" ? "int" : void 0;
    }
    if (!_typescript2.default.isNumericLiteral(node)) {
      return void 0;
    }
    return /[.eE]/.test(node.text) ? "double" : this.javaIntegerLiteralKind(node);
  }
  // `x.length` on a checker-proven String/List receiver prints a native int
  // (((String)x).length() / ((List<?>)x).size(), see javaLengthKind). Every other
  // receiver prints Helpers.getArrayLength, whose value is not an int.
  javaNativeLengthKind(node) {
    if (_optionalChain([node, 'optionalAccess', _1227 => _1227.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || node.name.escapedText !== "length") {
      return void 0;
    }
    return this.javaLengthKind(node.expression) !== void 0 ? "int" : void 0;
  }
  // `for (var i = <int literal>; ...; i++)`: javac infers a primitive int counter, so
  // every read of it — not only the loop condition the comparison rule sees — is a
  // native int. ++/-- keep it int and the counter cannot hold another box.
  isJavaPrimitiveCounterReference(node) {
    if (!node || node.kind !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const checker = this.getChecker();
    const symbol = checker.getSymbolAtLocation(node);
    const declaration = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1228 => _1228.valueDeclaration]), () => ( _optionalChain([symbol, 'optionalAccess', _1229 => _1229.declarations, 'optionalAccess', _1230 => _1230[0]])));
    if (!declaration || declaration.kind !== _typescript2.default.SyntaxKind.VariableDeclaration || !_typescript2.default.isIdentifier(declaration.name)) {
      return false;
    }
    const list = declaration.parent;
    const forStatement = _optionalChain([list, 'optionalAccess', _1231 => _1231.parent]);
    if (!list || !forStatement || forStatement.kind !== _typescript2.default.SyntaxKind.ForStatement || forStatement.initializer !== list) {
      return false;
    }
    if (_optionalChain([list, 'access', _1232 => _1232.declarations, 'optionalAccess', _1233 => _1233.length]) !== 1 || this.javaIntegerLiteralKind(declaration.initializer) === void 0) {
      return false;
    }
    const incrementor = forStatement.incrementor;
    if (!incrementor || incrementor.kind !== _typescript2.default.SyntaxKind.PostfixUnaryExpression && incrementor.kind !== _typescript2.default.SyntaxKind.PrefixUnaryExpression || incrementor.operator !== _typescript2.default.SyntaxKind.PlusPlusToken && incrementor.operator !== _typescript2.default.SyntaxKind.MinusMinusToken || _optionalChain([incrementor, 'access', _1234 => _1234.operand, 'optionalAccess', _1235 => _1235.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const incrementorSymbol = checker.getSymbolAtLocation(incrementor.operand);
    return incrementorSymbol === void 0 || symbol === void 0 || incrementorSymbol === symbol;
  }
  // the box a printed local carries: the kind of its declaration's initializer, kept
  // only while every write in the declaring function writes the same kind (D2 scan).
  // A `const` declaration has no write to scan.
  javaLocalNumberKind(node, depth) {
    if (!node || node.kind !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const symbol = this.getChecker().getSymbolAtLocation(node);
    const declaration = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1236 => _1236.valueDeclaration]), () => ( _optionalChain([symbol, 'optionalAccess', _1237 => _1237.declarations, 'optionalAccess', _1238 => _1238[0]])));
    if (!declaration || declaration.kind !== _typescript2.default.SyntaxKind.VariableDeclaration || !_typescript2.default.isIdentifier(declaration.name)) {
      return void 0;
    }
    const kind = this.javaPrintedNumberKind(declaration.initializer, depth + 1);
    if (kind === void 0) {
      return void 0;
    }
    const isConst = (_optionalChain([declaration, 'access', _1239 => _1239.parent, 'optionalAccess', _1240 => _1240.flags]) & _typescript2.default.NodeFlags.Const) !== 0;
    if (!isConst && !this.javaWritesKeepNumberKind(declaration, symbol, kind)) {
      return void 0;
    }
    return kind;
  }
  // true when every assignment to the symbol inside its function writes the same
  // numeric kind; a compound assignment (`x += 1` prints Helpers.add) changes the box.
  javaWritesKeepNumberKind(declaration, symbol, kind) {
    const owner = this.enclosingFunctionLike(declaration);
    if (owner === void 0) {
      return false;
    }
    let safe = true;
    const visit = (node) => {
      if (!safe) {
        return;
      }
      if (_typescript2.default.isBinaryExpression(node)) {
        const op = node.operatorToken.kind;
        if (this.isAssignmentOperator(op) && this.expressionReferencesSymbol(node.left, symbol)) {
          if (op !== _typescript2.default.SyntaxKind.EqualsToken || this.javaPrintedNumberKind(node.right, 0) !== kind) {
            safe = false;
            return;
          }
        }
      } else if ((node.kind === _typescript2.default.SyntaxKind.ForOfStatement || node.kind === _typescript2.default.SyntaxKind.ForInStatement) && this.expressionReferencesSymbol(node.initializer, symbol)) {
        safe = false;
        return;
      } else if ((_typescript2.default.isPrefixUnaryExpression(node) || _typescript2.default.isPostfixUnaryExpression(node)) && (node.operator === _typescript2.default.SyntaxKind.PlusPlusToken || node.operator === _typescript2.default.SyntaxKind.MinusMinusToken) && kind !== "int" && this.expressionReferencesSymbol(node.operand, symbol)) {
        safe = false;
        return;
      }
      _typescript2.default.forEachChild(node, visit);
    };
    _typescript2.default.forEachChild(owner, visit);
    return safe;
  }
  expressionReferencesSymbol(node, symbol) {
    if (node === void 0 || symbol === void 0) {
      return false;
    }
    if (node.kind === _typescript2.default.SyntaxKind.Identifier && this.getChecker().getSymbolAtLocation(node) === symbol) {
      return true;
    }
    let found = false;
    const visit = (child) => {
      if (found) {
        return;
      }
      if (child.kind === _typescript2.default.SyntaxKind.Identifier && this.getChecker().getSymbolAtLocation(child) === symbol) {
        found = true;
        return;
      }
      _typescript2.default.forEachChild(child, visit);
    };
    _typescript2.default.forEachChild(node, visit);
    return found;
  }
  enclosingFunctionLike(node) {
    let current = _optionalChain([node, 'optionalAccess', _1241 => _1241.parent]);
    while (current) {
      if (_typescript2.default.isFunctionLike(current)) {
        return current;
      }
      current = current.parent;
    }
    return void 0;
  }
  // box/primitive kind of an operand the printer prints as a native number, or undefined
  // when the printed value could be any box (a call, a parameter, an `any` local)
  javaPrintedNumberKind(node, depth = 0) {
    const literalKind = this.javaEqualityLiteralKind(node);
    if (literalKind !== void 0) {
      return literalKind;
    }
    const lengthKind = this.javaNativeLengthKind(node);
    if (lengthKind !== void 0) {
      return lengthKind;
    }
    if (this.isJavaPrimitiveCounterReference(node)) {
      return "int";
    }
    const arithmeticKind = this.javaNativeArithmeticKind(node);
    if (arithmeticKind !== void 0) {
      return arithmeticKind;
    }
    return depth < 2 ? this.javaLocalNumberKind(node, depth) : void 0;
  }
  // numeric equality operand: the checker proves a plain number and the printed Java
  // value carries a known kind. Aliases (Int/Num), unions and `any` stay with the
  // helper, like in the arithmetic rule.
  javaEqualityNumberKind(node) {
    if (this.javaScalarFamily(node) !== "number") {
      return void 0;
    }
    return this.javaPrintedNumberKind(node);
  }
  // ==/===/!=/!== become java.util.Objects.equals once the checker proves one operand is
  // a string, a boolean or the null/undefined literal: for those Helpers.isEqual reduces
  // to Objects.equals (value compare, class-strict, no numeric promotion). Two operands
  // the checker types as plain numbers compare natively when their printed Java kinds
  // match (java-15): the helper's numeric paths are the same value compare.
  printNativeEqualityIfProvable(node, leftText, rightText) {
    const op = node.operatorToken.kind;
    const negated = op === _typescript2.default.SyntaxKind.ExclamationEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken;
    if (!negated && op !== _typescript2.default.SyntaxKind.EqualsEqualsToken && op !== _typescript2.default.SyntaxKind.EqualsEqualsEqualsToken) {
      return void 0;
    }
    const checker = this.getChecker();
    const leftFamily = this.equalityOperandFamily(_optionalChain([checker, 'optionalAccess', _1242 => _1242.getTypeAtLocation, 'call', _1243 => _1243(node.left)]));
    const rightFamily = this.equalityOperandFamily(_optionalChain([checker, 'optionalAccess', _1244 => _1244.getTypeAtLocation, 'call', _1245 => _1245(node.right)]));
    const leftProved = leftFamily !== void 0 && (leftFamily !== "null" || this.isNullishLiteral(node.left));
    const rightProved = rightFamily !== void 0 && (rightFamily !== "null" || this.isNullishLiteral(node.right));
    if (leftProved || rightProved) {
      const equalCall = `java.util.Objects.equals(${leftText}, ${rightText})`;
      return negated ? `!${equalCall}` : equalCall;
    }
    if (this.javaDeclaredStringType(node.left) || this.javaDeclaredStringType(node.right)) {
      const equalCall = `java.util.Objects.equals(${leftText}, ${rightText})`;
      return negated ? `!${equalCall}` : equalCall;
    }
    if (this.javaOperandPrintsPrimitiveNumber(node.left) && this.javaOperandPrintsPrimitiveNumber(node.right)) {
      return `(${leftText} ${negated ? "!=" : "=="} ${rightText})`;
    }
    const leftKind = this.javaEqualityNumberKind(node.left);
    const rightKind = this.javaEqualityNumberKind(node.right);
    if (leftKind !== void 0 && leftKind === rightKind) {
      if (this.javaOperandPrintsPrimitiveNumber(node.left) && this.javaOperandPrintsPrimitiveNumber(node.right)) {
        return `(${leftText} ${negated ? "!=" : "=="} ${rightText})`;
      }
      if (leftKind !== "double") {
        const equalCall = `java.util.Objects.equals(${leftText}, ${rightText})`;
        return negated ? `!${equalCall}` : equalCall;
      }
    }
    const leftDeclared = this.javaDeclaredNumericFamily(node.left);
    const rightDeclared = this.javaDeclaredNumericFamily(node.right);
    if (leftDeclared !== void 0 || rightDeclared !== void 0) {
      const leftOk = leftDeclared !== void 0 || this.javaOperandPrintsPrimitiveNumber(node.left);
      const rightOk = rightDeclared !== void 0 || this.javaOperandPrintsPrimitiveNumber(node.right);
      if (leftOk && rightOk) {
        const boxes = [];
        if (leftDeclared !== void 0 && JAVA_BOXED_NUMERIC_TYPES.has(leftDeclared)) {
          boxes.push(leftText);
        }
        if (rightDeclared !== void 0 && JAVA_BOXED_NUMERIC_TYPES.has(rightDeclared)) {
          boxes.push(rightText);
        }
        const compare = `${leftText} ${negated ? "!=" : "=="} ${rightText}`;
        if (boxes.length === 0) {
          return `(${compare})`;
        }
        return negated ? `(${boxes.map((box) => `${box} == null`).join(" || ")} || ${compare})` : `(${boxes.map((box) => `${box} != null`).join(" && ")} && ${compare})`;
      }
    }
    return void 0;
  }
  // true when the printer prints the operand as a Java primitive (a decimal literal, a
  // native .length/.size(), a for counter or a nested native arithmetic node) rather
  // than as the Object-declared box every other local gets
  javaOperandPrintsPrimitiveNumber(node) {
    return this.javaEqualityLiteralKind(node) !== void 0 || this.javaNativeLengthKind(node) !== void 0 || this.isJavaPrimitiveCounterReference(node) || this.javaNativeArithmeticKind(node) !== void 0 || this.javaPrimitiveOperandKind(node) !== void 0;
  }
  // the numeric family the pass declared for a local (`Long`/`Double`/`Integer` boxed,
  // `int`/`long`/`double` primitive), undefined when nothing was declared or the declared
  // type is not numeric
  javaDeclaredNumericFamily(expression) {
    const type = this.javaDeclaredTypeOf(expression);
    if (type === void 0) {
      return void 0;
    }
    const trimmed = type.trim();
    return JAVA_DECLARED_NUMERIC_TYPES.has(trimmed) ? trimmed : void 0;
  }
  // `x[k] = v` prints the runtime helper by default. Helpers.addElementToObject
  // exists for receivers the printer cannot type (Lists, arbitrary objects via
  // reflection) and for ConcurrentHashMap null-removal, so the native Map.put is
  // printed only when the checker excludes all of those.
  elementWriteTargetsMap(container, base, keys) {
    const lastKey = keys[keys.length - 1];
    if (!_typescript2.default.isStringLiteral(lastKey) && !this.isJavaStringType(this.getChecker().getTypeAtLocation(lastKey))) {
      return false;
    }
    if (_typescript2.default.isPropertyAccessExpression(base) && base.expression.kind === _typescript2.default.SyntaxKind.ThisKeyword) {
      return false;
    }
    return this.isDictionaryType(container) || this.isPlainHashMapReceiver(container, keys);
  }
  // a key proven by the checker to be a string prints as a java String: the read is
  // the same expression, only the key needs the (String) cast the typed put demands
  elementWriteKeyText(key, keyText) {
    if (_typescript2.default.isStringLiteral(key)) {
      return keyText;
    }
    return `(String)${keyText}`;
  }
  // Receivers that are a plain java.util.HashMap at runtime, where ".put" and the helper's map branch
  // are the same write: a local initialized with an object literal or a call whose every return is
  // such a literal (this.account()). Lists, class instances and ConcurrentHashMaps stay helpers.
  isPlainHashMapReceiver(container, keys) {
    if (keys.length !== 1 || container === void 0 || container.kind !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const symbol = checker.getSymbolAtLocation(container);
    const declaration = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1246 => _1246.valueDeclaration]), () => ( _optionalChain([symbol, 'optionalAccess', _1247 => _1247.declarations, 'optionalAccess', _1248 => _1248[0]])));
    if (!declaration || declaration.kind !== _typescript2.default.SyntaxKind.VariableDeclaration || !declaration.initializer) {
      return false;
    }
    const initializer = this.unwrapPrintTransparentExpression(declaration.initializer);
    const proven = _typescript2.default.isObjectLiteralExpression(initializer) || _typescript2.default.isCallExpression(initializer) && this.callAlwaysReturnsPlainHashMap(initializer, 0);
    if (!proven) {
      return false;
    }
    return !this.javaLocalIsReassigned(container);
  }
  unwrapPrintTransparentExpression(node) {
    let current = node;
    while (current && (_typescript2.default.isParenthesizedExpression(current) || _typescript2.default.isAsExpression(current) || _typescript2.default.isTypeAssertionExpression(current) || current.kind === _typescript2.default.SyntaxKind.NonNullExpression)) {
      current = current.expression;
    }
    return current;
  }
  // Every write of the local in its enclosing function must be the element write
  // itself; an assignment could replace the HashMap with a List or a class instance.
  javaLocalIsReassigned(node) {
    let scope = node;
    while (scope && !_typescript2.default.isFunctionLike(scope) && !_typescript2.default.isSourceFile(scope)) {
      scope = scope.parent;
    }
    if (!scope) {
      return true;
    }
    const symbol = this.getChecker().getSymbolAtLocation(node);
    let reassigned = false;
    const walk = (current) => {
      if (reassigned || current === void 0) {
        return;
      }
      if (current.kind === _typescript2.default.SyntaxKind.BinaryExpression && current.operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken && current.left.kind === _typescript2.default.SyntaxKind.Identifier && this.getChecker().getSymbolAtLocation(current.left) === symbol) {
        reassigned = true;
        return;
      }
      if ((_typescript2.default.isForOfStatement(current) || _typescript2.default.isForInStatement(current)) && _typescript2.default.isIdentifier(current.initializer) && this.getChecker().getSymbolAtLocation(current.initializer) === symbol) {
        reassigned = true;
        return;
      }
      _typescript2.default.forEachChild(current, walk);
    };
    walk(scope);
    return reassigned;
  }
  // A call whose callee body returns object literals only, so the value it hands
  // back is always a freshly built HashMap on the Java side as well.
  callAlwaysReturnsPlainHashMap(node, depth) {
    if (depth > 3) {
      return false;
    }
    const checker = this.getChecker();
    let callee = this.unwrapPrintTransparentExpression(node.expression);
    if (callee !== void 0 && _typescript2.default.isPropertyAccessExpression(callee)) {
      if (callee.expression.kind !== _typescript2.default.SyntaxKind.ThisKeyword) {
        return false;
      }
      callee = callee.name;
    }
    if (callee === void 0 || callee.kind !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    let symbol;
    try {
      symbol = checker.getSymbolAtLocation(callee);
    } catch (e) {
      return false;
    }
    const declaration = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1249 => _1249.valueDeclaration]), () => ( _optionalChain([symbol, 'optionalAccess', _1250 => _1250.declarations, 'optionalAccess', _1251 => _1251[0]])));
    if (!declaration || declaration.kind !== _typescript2.default.SyntaxKind.MethodDeclaration && declaration.kind !== _typescript2.default.SyntaxKind.FunctionDeclaration) {
      return false;
    }
    if (!this.returnTypePrintsAsHashMap(declaration)) {
      return false;
    }
    const body = declaration.body;
    if (!body || !_typescript2.default.isBlock(body)) {
      return false;
    }
    let plain = true;
    let returns = 0;
    const walk = (current) => {
      if (!plain || current === void 0) {
        return;
      }
      if (_typescript2.default.isFunctionLike(current) && current !== declaration) {
        return;
      }
      if (_typescript2.default.isReturnStatement(current)) {
        returns += 1;
        const expression = current.expression;
        if (expression === void 0) {
          plain = false;
          return;
        }
        const value = this.unwrapPrintTransparentExpression(expression);
        if (_typescript2.default.isObjectLiteralExpression(value)) {
          return;
        }
        if (_typescript2.default.isCallExpression(value) && this.callAlwaysReturnsPlainHashMap(value, depth + 1)) {
          return;
        }
        plain = false;
        return;
      }
      _typescript2.default.forEachChild(current, walk);
    };
    walk(body);
    return plain && returns > 0;
  }
  // the declared return type must not be a class instance (those print as objects)
  // or an array (those print as Lists), so the object-literal returns above are what
  // the caller can rely on
  returnTypePrintsAsHashMap(declaration) {
    try {
      const checker = this.getChecker();
      const signature = checker.getSignatureFromDeclaration(declaration);
      const type = _optionalChain([signature, 'optionalAccess', _1252 => _1252.getReturnType, 'call', _1253 => _1253()]);
      if (!type || (type.flags & _typescript2.default.TypeFlags.Object) === 0) {
        return false;
      }
      if (checker.isArrayType(type) || checker.isTupleType(type)) {
        return false;
      }
      const declarations = _nullishCoalesce(_optionalChain([type, 'access', _1254 => _1254.getSymbol, 'call', _1255 => _1255(), 'optionalAccess', _1256 => _1256.declarations]), () => ( []));
      if (declarations.some((d) => d.kind === _typescript2.default.SyntaxKind.ClassDeclaration)) {
        return false;
      }
      return type.getCallSignatures().length === 0;
    } catch (e) {
      return false;
    }
  }
  isDictionaryType(node) {
    try {
      const checker = this.getChecker();
      const type = checker.getTypeAtLocation(node);
      return this.isDictionaryTsType(type, checker, 0);
    } catch (e) {
      return false;
    }
  }
  // A TS dictionary (`{ [key: string]: any }`, i.e. ccxt's Dict) is a Map on every
  // print and run path. Arrays, class instances and unknown types are not, so they
  // keep the helper.
  isDictionaryTsType(type, checker, depth) {
    if (!type || depth > 3) {
      return false;
    }
    const flags = type.flags;
    if (flags & _typescript2.default.TypeFlags.Union) {
      const parts = _nullishCoalesce(type.types, () => ( []));
      return parts.length > 0 && parts.every((t) => this.isDictionaryTsType(t, checker, depth + 1));
    }
    if (!(flags & _typescript2.default.TypeFlags.Object)) {
      return false;
    }
    try {
      if (checker.isArrayType(type) || checker.isTupleType(type)) {
        return false;
      }
      return checker.getIndexTypeOfType(type, _typescript2.default.IndexKind.String) !== void 0;
    } catch (e) {
      return false;
    }
  }
  // -------------------------------------------------------------------
  // helper removal: native emission when the checker proves the printed
  // operand is a Java numeric primitive / List / Map
  // -------------------------------------------------------------------
  // int/long kind of a literal as printNumericLiteral emits it. Fraction and
  // exponent forms are Java doubles and a double can hold NaN, which the
  // comparison helpers order differently (`NaN < x` is true there), so those
  // never become a native comparison.
  javaIntegerLiteralKind(node) {
    if (!node || !_typescript2.default.isNumericLiteral(node)) {
      return void 0;
    }
    const text = node.text;
    if (text.indexOf(".") !== -1 || text.indexOf("e") !== -1 || text.indexOf("E") !== -1) {
      return void 0;
    }
    return Number(text) > 2147483647 ? "long" : "int";
  }
  // A rest parameter is a Java varargs array, not a List, so a List cast on it
  // would throw ClassCastException; simple identifier aliases are followed too.
  isVarargsArrayReference(node, depth = 0) {
    if (!node || depth > 4 || node.kind !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const symbol = this.getChecker().getSymbolAtLocation(node);
    const declaration = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1257 => _1257.valueDeclaration]), () => ( _optionalChain([symbol, 'optionalAccess', _1258 => _1258.declarations, 'optionalAccess', _1259 => _1259[0]])));
    if (!declaration) {
      return false;
    }
    if (declaration.dotDotDotToken !== void 0) {
      return true;
    }
    const initializer = declaration.initializer;
    if (initializer && _typescript2.default.isIdentifier(initializer)) {
      return this.isVarargsArrayReference(initializer, depth + 1);
    }
    return false;
  }
  // checker proof that the value is printed as a java.util.List: TS arrays and
  // tuples become ArrayList, ReadonlyArray only adds a readonly modifier
  isJavaListType(type) {
    if (!type) {
      return false;
    }
    const checker = this.getChecker();
    if (checker.isArrayType(type) || checker.isTupleType(type)) {
      return true;
    }
    return _optionalChain([type, 'access', _1260 => _1260.target, 'optionalAccess', _1261 => _1261.symbol, 'optionalAccess', _1262 => _1262.escapedName]) === "ReadonlyArray";
  }
  // `.length` is a Java int for exactly these receivers; every other receiver keeps
  // Helpers.getArrayLength, whose result type is not proven
  javaLengthKind(expression) {
    const type = this.getChecker().getTypeAtLocation(expression);
    if (this.isJavaStringType(type)) {
      return "String";
    }
    if (this.isJavaListValueType(type) && !this.isVarargsArrayReference(expression)) {
      return "List";
    }
    if (_typescript2.default.isIdentifier(expression)) {
      if (this.isJavaNullishUnion(type, (member) => this.isStringType(member.flags))) {
        return "StringOrNull";
      }
      if (this.isJavaNullishUnion(type, (member) => this.isJavaListValueType(member)) && !this.isVarargsArrayReference(expression)) {
        return "ListOrNull";
      }
    }
    return void 0;
  }
  // every union member is a List-printing type (TS array/tuple/ReadonlyArray, or an
  // Array-derived class whose hand-written java counterpart is an ArrayList)
  isJavaListValueType(type) {
    if (!type) {
      return false;
    }
    if ((type.flags & _typescript2.default.TypeFlags.Union) !== 0) {
      return type.types.length > 0 && type.types.every((member) => this.isJavaListValueType(member));
    }
    return this.isJavaListType(type) || this.isJavaListBackedClassType(type);
  }
  // TS class/interface whose java counterpart extends java.util.ArrayList<Object>:
  // the ws caches (ws/ArrayCache.java) and the order-book sides (ws/OrderBookSide.java)
  isJavaListBackedClassType(type) {
    if (!type || (type.flags & _typescript2.default.TypeFlags.Object) === 0) {
      return false;
    }
    let current = _nullishCoalesce(type.target, () => ( type));
    for (let depth = 0; current && depth < 8; depth++) {
      const name = _optionalChain([current, 'access', _1263 => _1263.symbol, 'optionalAccess', _1264 => _1264.escapedName]);
      if (name !== void 0 && JAVA_LIST_BACKED_TS_CLASSES.has(name)) {
        return true;
      }
      const bases = _nullishCoalesce(_optionalChain([current, 'access', _1265 => _1265.getBaseTypes, 'optionalCall', _1266 => _1266()]), () => ( []));
      current = bases.length > 0 ? _nullishCoalesce(bases[0].target, () => ( bases[0])) : void 0;
    }
    return false;
  }
  // union of one accepted member family plus null/undefined: the emitted guard is the
  // helper's answer for the nullish case and the native read otherwise
  isJavaNullishUnion(type, isMember) {
    if (!type || (type.flags & _typescript2.default.TypeFlags.Union) === 0 || type.types.length === 0) {
      return false;
    }
    return type.types.every((member) => isMember(member) || (member.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null)) !== 0);
  }
  // shared by printLengthProperty and transformPropertyAcessExpressionIfNeeded
  printJavaLength(expression, leftSide) {
    const kind = this.javaLengthKind(expression);
    if (kind === "String") {
      return `((String)${leftSide}).length()`;
    }
    if (kind === "List") {
      return `((java.util.List<?>)${leftSide}).size()`;
    }
    if (kind === "StringOrNull") {
      return `(${leftSide} == null ? 0 : ((String)${leftSide}).length())`;
    }
    if (kind === "ListOrNull") {
      return `(${leftSide} == null ? 0 : ((java.util.List<?>)${leftSide}).size())`;
    }
    const declared = this.javaDeclaredTypeOf(expression);
    if (declared !== void 0 && JAVA_DECLARED_LIST_TYPES.test(declared)) {
      return `(${leftSide} == null ? 0 : ${leftSide}.size())`;
    }
    return `${this.ARRAY_LENGTH_WRAPPER_OPEN}${leftSide}${this.ARRAY_LENGTH_WRAPPER_CLOSE}`;
  }
  // `for (var i = <int literal>; ...; i++)`: printForStatement rewrites the
  // Object initializer into `var`, so javac infers a primitive counter there,
  // and only ++/-- writes keep it primitive
  isJavaPrimitiveForCounter(node) {
    if (node.kind !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const comparison = node.parent;
    if (!comparison || comparison.kind !== _typescript2.default.SyntaxKind.BinaryExpression) {
      return false;
    }
    const forStatement = comparison.parent;
    if (!forStatement || forStatement.kind !== _typescript2.default.SyntaxKind.ForStatement || forStatement.condition !== comparison) {
      return false;
    }
    const initializer = forStatement.initializer;
    if (!initializer || initializer.kind !== _typescript2.default.SyntaxKind.VariableDeclarationList) {
      return false;
    }
    const declarations = _nullishCoalesce(initializer.declarations, () => ( []));
    if (declarations.length !== 1) {
      return false;
    }
    const declaration = declarations[0];
    if (!_typescript2.default.isIdentifier(declaration.name) || declaration.name.escapedText !== node.escapedText) {
      return false;
    }
    if (this.javaIntegerLiteralKind(declaration.initializer) === void 0) {
      return false;
    }
    const counterSymbol = this.getChecker().getSymbolAtLocation(node);
    const declarationSymbol = this.getChecker().getSymbolAtLocation(declaration.name);
    if (counterSymbol !== void 0 && declarationSymbol !== void 0 && counterSymbol !== declarationSymbol) {
      return false;
    }
    const incrementor = forStatement.incrementor;
    if (!incrementor || _optionalChain([incrementor, 'access', _1267 => _1267.operand, 'optionalAccess', _1268 => _1268.kind]) !== _typescript2.default.SyntaxKind.Identifier || incrementor.operand.escapedText !== node.escapedText) {
      return false;
    }
    return incrementor.kind === _typescript2.default.SyntaxKind.PostfixUnaryExpression || incrementor.kind === _typescript2.default.SyntaxKind.PrefixUnaryExpression;
  }
  // Java primitive kind of a comparison operand; undefined keeps the helper. Proof sources: literals
  // (int/long/double) and expressions the printer's own emitter pins: `var` for-counter, `.length`,
  // `x.indexOf(arg)`/`x.search(arg)` (int), Math.round (long), Math.floor/ceil/pow (double).
  javaPrimitiveOperandKind(node) {
    if (node === void 0 || node === null) {
      return void 0;
    }
    if (node.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.javaPrimitiveOperandKind(node.expression);
    }
    const literalKind = this.javaIntegerLiteralKind(node);
    if (literalKind !== void 0) {
      return literalKind;
    }
    if (this.isJavaFloatLiteral(node)) {
      return "double";
    }
    if (this.isJavaPrimitiveForCounter(node)) {
      return "int";
    }
    if (node.kind === _typescript2.default.SyntaxKind.PropertyAccessExpression && node.name.escapedText === "length") {
      return "int";
    }
    if (node.kind === _typescript2.default.SyntaxKind.CallExpression) {
      return this.javaPrintedCallKind(node);
    }
    return void 0;
  }
  // fractional / exponent literals print as Java double literals (printNumericLiteral)
  isJavaFloatLiteral(node) {
    if (!node || !_typescript2.default.isNumericLiteral(node)) {
      return false;
    }
    const text = node.text;
    if (/^0[xXbBoO]/.test(text)) {
      return false;
    }
    return text.indexOf(".") !== -1 || text.indexOf("e") !== -1 || text.indexOf("E") !== -1;
  }
  // Java kind of a call the printer emits itself, undefined otherwise. Mirrors printIndexOfCall /
  // printSearchCall / printMathRoundCall / printMathFloorCall / printMathCeilCall and Math.pow:
  // x.indexOf(a) / x.search(a) -> int; Math.round(x) -> long; Math.floor/ceil/pow -> double.
  javaPrintedCallKind(node) {
    const callee = node.expression;
    if (_optionalChain([callee, 'optionalAccess', _1269 => _1269.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      return void 0;
    }
    const name = _optionalChain([callee, 'access', _1270 => _1270.name, 'optionalAccess', _1271 => _1271.escapedText]);
    const argCount = _nullishCoalesce(_optionalChain([node, 'access', _1272 => _1272.arguments, 'optionalAccess', _1273 => _1273.length]), () => ( 0));
    const onMath = _optionalChain([callee, 'access', _1274 => _1274.expression, 'optionalAccess', _1275 => _1275.kind]) === _typescript2.default.SyntaxKind.Identifier && callee.expression.escapedText === "Math";
    switch (name) {
      case "indexOf":
      case "search":
        return argCount >= 1 ? "int" : void 0;
      case "round":
        return onMath && argCount === 1 ? "long" : void 0;
      case "floor":
      case "ceil":
        return onMath && argCount === 1 ? "double" : void 0;
      case "pow":
        return onMath && argCount === 2 ? "double" : void 0;
    }
    return void 0;
  }
  // operand usability for `>=` / `<` / `<=`, which route through the helper's isEqual. Two integral
  // operands are always exact; once a double is involved, toLong saturates and BigDecimal throws on
  // ±Infinity, so a double or long is accepted only as a finite literal within ±2^53.
  javaComparisonOperandsAreExact(left, leftKind, right, rightKind) {
    if (leftKind === "int" && rightKind === "int") {
      return true;
    }
    if (leftKind !== "double" && rightKind !== "double") {
      return true;
    }
    return this.javaComparisonOperandIsExactAgainstDouble(left, leftKind) && this.javaComparisonOperandIsExactAgainstDouble(right, rightKind);
  }
  javaComparisonOperandIsExactAgainstDouble(node, kind) {
    if (kind === "int") {
      return true;
    }
    if (kind !== "long" && kind !== "double") {
      return false;
    }
    if (!_typescript2.default.isNumericLiteral(node)) {
      return false;
    }
    const text = node.text;
    if (/^[0-9]+$/.test(text)) {
      return BigInt(text) <= 9007199254740992n;
    }
    const value = Number(text);
    return Number.isFinite(value) && Math.abs(value) <= 9007199254740992;
  }
  // checker proof that the value is printed as a java.util.HashMap: TS object
  // shapes (interfaces, object literals, aliases) become HashMaps, while class
  // instances are real Java objects and arrays/unions are not proven here
  isJavaMapType(type) {
    if (!type || (type.flags & _typescript2.default.TypeFlags.Object) === 0) {
      return false;
    }
    const checker = this.getChecker();
    if (checker.isArrayType(type) || checker.isTupleType(type)) {
      return false;
    }
    if (type.getCallSignatures().length > 0) {
      return false;
    }
    const declarations = _nullishCoalesce(_optionalChain([type, 'access', _1276 => _1276.getSymbol, 'call', _1277 => _1277(), 'optionalAccess', _1278 => _1278.declarations]), () => ( []));
    return !declarations.some((declaration) => declaration.kind === _typescript2.default.SyntaxKind.ClassDeclaration || declaration.getSourceFile().fileName.indexOf("typescript") > -1);
  }
  // string keys (plain, literal or a union of literals) print as Java Strings
  isJavaStringType(type) {
    if (!type) {
      return false;
    }
    if (this.isStringType(type.flags)) {
      return true;
    }
    if ((type.flags & _typescript2.default.TypeFlags.Union) === 0) {
      return false;
    }
    return type.types.every((member) => this.isStringType(member.flags));
  }
  // `Dictionary | undefined` (what a no-overload safe* signature widens to): the helper
  // answers false for the nullish arm and the guarded emission keeps exactly that; every
  // non-map member (arrays, classes, scalars) keeps the helper
  isJavaNullableMapType(type) {
    if (type === void 0 || (type.flags & _typescript2.default.TypeFlags.Union) === 0) {
      return false;
    }
    const members = _nullishCoalesce(type.types, () => ( []));
    let maps = 0;
    for (const member of members) {
      if ((member.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null)) !== 0) {
        continue;
      }
      if (!this.isJavaMapType(member)) {
        return false;
      }
      maps++;
    }
    return maps > 0;
  }
  // the guarded emission reads the receiver twice, so it is only printed for an operand
  // that cannot run anything twice: a name or a `this.` field. Everything else (calls,
  // element reads) keeps the helper so the operand is still evaluated once.
  javaRepeatableOperand(node) {
    if (node === void 0) {
      return false;
    }
    if (_typescript2.default.isIdentifier(node)) {
      return true;
    }
    if (_typescript2.default.isParenthesizedExpression(node)) {
      return this.javaRepeatableOperand(node.expression);
    }
    return _typescript2.default.isPropertyAccessExpression(node) && _optionalChain([node, 'access', _1279 => _1279.expression, 'optionalAccess', _1280 => _1280.kind]) === _typescript2.default.SyntaxKind.ThisKeyword;
  }
  // The declared Java type of a local/parameter is known to the pass that rewrites the declaration
  // text (build/java-local-types.js), which records every name it typed here. With no consumer
  // installed the table is empty and every read keeps the helper.
  javaDeclaredTypeOf(expression) {
    if (expression === void 0 || !_typescript2.default.isIdentifier(expression)) {
      return void 0;
    }
    const declaration = this.javaDeclarationOfIdentifier(expression);
    if (declaration === void 0 || expression.escapedText !== _optionalChain([declaration, 'access', _1281 => _1281.name, 'optionalAccess', _1282 => _1282.escapedText])) {
      return void 0;
    }
    return this.javaDeclaredTypeOfDeclaration(declaration);
  }
  // the declared Java type of a declaration: the embedding build layer names the
  // declarations it retypes itself, and a parameter the printer retypes
  // (javaNativeParameterType) answers for itself, so its element reads go native too
  javaDeclaredTypeOfDeclaration(declaration) {
    const resolver = this.javaDeclaredLocalTypeResolver;
    if (resolver !== void 0) {
      let type;
      try {
        type = resolver(declaration);
      } catch (e) {
        type = void 0;
      }
      if (typeof type === "string" && type.trim().length > 0) {
        return type.trim();
      }
    }
    if (_typescript2.default.isParameter(declaration)) {
      return this.javaNativeParameterType(declaration);
    }
    return void 0;
  }
  // The native Java type a parameter prints with when its TS annotation is a base/types.ts alias in
  // JAVA_NATIVE_PARAMETER_TYPES. Fixed parameters of generated-tier methods only, and every
  // declaration up the heritage chain must print the same type: Java overrides are invariant.
  javaNativeParameterType(node) {
    if (node === void 0 || !_typescript2.default.isParameter(node) || node.initializer !== void 0 || node.dotDotDotToken !== void 0) {
      return void 0;
    }
    const own = this.javaNativeParameterTypeOf(node);
    const type = own !== void 0 ? own : this.javaInheritedParameterType(node);
    if (type === void 0) {
      return void 0;
    }
    if (this.javaParameterIsCompoundAssigned(node)) {
      return void 0;
    }
    try {
      const method = node.parent;
      const index = method.parameters.indexOf(node);
      let override = this.getMethodOverride(method);
      if (override === void 0 && JAVA_NATIVE_PARAMETER_PREDICTION_FILES.test(node.getSourceFile().fileName) && method.name !== void 0) {
        const tier = this.exchangeTierMethods().get(method.name.getText().trim());
        if (tier !== void 0 && !this.javaParameterPrintsType(_optionalChain([tier, 'access', _1283 => _1283.parameters, 'optionalAccess', _1284 => _1284[index]]), type)) {
          return void 0;
        }
      }
      while (override !== void 0) {
        const baseParam = _optionalChain([override, 'access', _1285 => _1285.parameters, 'optionalAccess', _1286 => _1286[index]]);
        if (!this.javaParameterPrintsType(baseParam, type)) {
          return void 0;
        }
        override = this.getMethodOverride(override);
      }
    } catch (e) {
      return void 0;
    }
    return type;
  }
  exchangeTierMethods() {
    if (this._exchangeTierMethods !== void 0) {
      return this._exchangeTierMethods;
    }
    const names = /* @__PURE__ */ new Map();
    try {
      const file = this.getProgram().getSourceFiles().find((sf) => JAVA_NATIVE_PARAMETER_BASE_FILES.test(sf.fileName));
      const collect = (node) => {
        if (_typescript2.default.isClassDeclaration(node) && _optionalChain([node, 'access', _1287 => _1287.name, 'optionalAccess', _1288 => _1288.text]) === "Exchange") {
          for (const member of node.members) {
            const key = _optionalChain([member, 'access', _1289 => _1289.name, 'optionalAccess', _1290 => _1290.getText, 'call', _1291 => _1291(), 'access', _1292 => _1292.trim, 'call', _1293 => _1293()]);
            if (_typescript2.default.isMethodDeclaration(member) && key !== void 0 && !names.has(key)) {
              names.set(key, member);
            }
          }
        }
        _typescript2.default.forEachChild(node, collect);
      };
      if (file !== void 0) {
        collect(file);
      }
    } catch (e) {
    }
    this._exchangeTierMethods = names;
    return names;
  }
  // the same parameter position of an ancestor declaration prints this native type: its
  // own annotation names it, or - with no annotation of its own - it inherits the same
  // type from a declaration above it, exactly like this one does
  javaParameterPrintsType(baseParam, type) {
    if (baseParam === void 0 || !_typescript2.default.isParameter(baseParam) || baseParam.initializer !== void 0 || baseParam.dotDotDotToken !== void 0) {
      return false;
    }
    const own = this.javaNativeParameterTypeOf(baseParam);
    const printed = own !== void 0 ? own : this.javaInheritedParameterType(baseParam);
    return printed === type;
  }
  // the type a fixed parameter with no annotation of its own inherits: the nearest
  // declaration of the method up the heritage chain that prints a native type. An
  // unannotated root declaration prints `Object`, so nothing is inherited and the whole
  javaInheritedParameterType(node) {
    const method = node.parent;
    const index = _optionalChain([method, 'optionalAccess', _1294 => _1294.parameters, 'optionalAccess', _1295 => _1295.indexOf, 'call', _1296 => _1296(node)]);
    if (method === void 0 || index === void 0 || index < 0) {
      return void 0;
    }
    let override = this.getMethodOverride(method);
    while (override !== void 0) {
      const baseParam = _optionalChain([override, 'access', _1297 => _1297.parameters, 'optionalAccess', _1298 => _1298[index]]);
      const type = baseParam === void 0 ? void 0 : this.javaNativeParameterTypeOf(baseParam);
      if (type !== void 0) {
        return type;
      }
      override = this.getMethodOverride(override);
    }
    return void 0;
  }
  // Default-valued parameters are real parameters of the typed core that carries the body;
  // the untyped `Object... optionalArgs` front delegates to it.
  javaOptionalParameterJavaType(node) {
    return _nullishCoalesce(this.javaOptionalParameterType(node), () => ( "Object"));
  }
  javaOptionalParameterType(node) {
    if (node === void 0 || !_typescript2.default.isParameter(node)) {
      return void 0;
    }
    if (node.initializer === void 0 || node.dotDotDotToken !== void 0) {
      return void 0;
    }
    const own = this.javaOptionalParameterTypeOf(node);
    if (own === void 0) {
      return void 0;
    }
    if (this.javaParameterIsCompoundAssigned(node)) {
      return void 0;
    }
    if (own === JAVA_STRING_LIST_TYPE && this.javaParameterIsTypeofTested(node)) {
      return void 0;
    }
    const method = node.parent;
    try {
      const index = method.parameters.indexOf(node);
      let override = this.getMethodOverride(method);
      while (override !== void 0) {
        if (!this.javaOptionalParameterFamilyAgrees(method, override, index, own)) {
          return void 0;
        }
        override = this.getMethodOverride(override);
      }
    } catch (e) {
      return void 0;
    }
    return own;
  }
  // the annotation proof for an optional parameter (no initializer bail)
  javaOptionalParameterTypeOf(node) {
    if (node === void 0 || !_typescript2.default.isParameter(node) || node.dotDotDotToken !== void 0) {
      return void 0;
    }
    const method = node.parent;
    if (method === void 0 || !_typescript2.default.isMethodDeclaration(method) || !_typescript2.default.isClassDeclaration(method.parent)) {
      return void 0;
    }
    if (!JAVA_NATIVE_PARAMETER_BASE_FILES.test(node.getSourceFile().fileName) && !JAVA_NATIVE_PARAMETER_GENERATED_FILES.test(node.getSourceFile().fileName)) {
      return void 0;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const type = checker.getTypeAtLocation(node);
    if (type === void 0) {
      return void 0;
    }
    const symbol = this.javaParameterAliasSymbol(node, type, checker);
    const name = _optionalChain([symbol, 'optionalAccess', _1299 => _1299.name]);
    const excluded = JAVA_NATIVE_PARAMETER_EXCLUDED_POSITIONS[_optionalChain([method, 'access', _1300 => _1300.name, 'optionalAccess', _1301 => _1301.escapedText])];
    if (excluded !== void 0 && excluded.includes(method.parameters.indexOf(node))) {
      return void 0;
    }
    if (name === "Strings" || this.javaIsStringArrayType(checker, type)) {
      const listName = JAVA_STRINGS_OPTIONAL_PARAMETER_NAMES.has(_optionalChain([node, 'access', _1302 => _1302.name, 'optionalAccess', _1303 => _1303.escapedText])) && !JAVA_STRINGS_EXCLUDED_METHODS.has(_optionalChain([method, 'access', _1304 => _1304.name, 'optionalAccess', _1305 => _1305.escapedText]));
      return listName ? JAVA_STRING_LIST_TYPE : void 0;
    }
    if (name === void 0 || JAVA_NATIVE_PARAMETER_TYPES_OPTIONAL[name] === void 0) {
      return void 0;
    }
    const declaration = _optionalChain([symbol, 'optionalAccess', _1306 => _1306.declarations, 'optionalAccess', _1307 => _1307[0]]);
    const fileName = _optionalChain([declaration, 'optionalAccess', _1308 => _1308.getSourceFile, 'optionalCall', _1309 => _1309(), 'optionalAccess', _1310 => _1310.fileName]);
    return JAVA_NATIVE_PARAMETER_SOURCE_FILES.test(_nullishCoalesce(fileName, () => ( ""))) ? JAVA_NATIVE_PARAMETER_TYPES_OPTIONAL[name] : void 0;
  }
  // a `string[]` annotation (optionally `| undefined`), the unaliased spelling of `Strings`
  javaIsStringArrayType(checker, type) {
    const members = _optionalChain([type, 'access', _1311 => _1311.isUnion, 'optionalCall', _1312 => _1312()]) ? type.types : [type];
    let arrays = 0;
    for (const member of members) {
      if (member.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null)) {
        continue;
      }
      if (!checker.isArrayType(member)) {
        return false;
      }
      const element = _optionalChain([checker, 'access', _1313 => _1313.getTypeArguments, 'call', _1314 => _1314(member), 'optionalAccess', _1315 => _1315[0]]);
      if (element === void 0 || !(element.flags & _typescript2.default.TypeFlags.String)) {
        return false;
      }
      arrays++;
    }
    return arrays === 1;
  }
  // Java overrides are invariant: every ancestor declaration must match the parameter count,
  // the first default-valued index and this position's type, otherwise the position stays Object.
  javaOptionalParameterFamilyAgrees(method, override, index, type) {
    const baseParams = _optionalChain([override, 'optionalAccess', _1316 => _1316.parameters]);
    if (baseParams === void 0 || baseParams.length !== method.parameters.length) {
      return false;
    }
    if (this.firstDefaultParameterIndex(baseParams) !== this.firstDefaultParameterIndex(method.parameters)) {
      return false;
    }
    const baseParam = baseParams[index];
    if (baseParam === void 0 || !_typescript2.default.isParameter(baseParam) || baseParam.dotDotDotToken !== void 0) {
      return false;
    }
    const baseIsOptional = baseParam.initializer !== void 0 || baseParam.questionToken !== void 0;
    const ownIsOptional = method.parameters[index].initializer !== void 0 || method.parameters[index].questionToken !== void 0;
    if (baseIsOptional !== ownIsOptional) {
      return false;
    }
    const baseType = baseIsOptional ? this.javaOptionalParameterTypeOf(baseParam) : this.javaNativeParameterTypeOf(baseParam);
    return baseType === type;
  }
  firstDefaultParameterIndex(params) {
    for (let i = 0; i < params.length; i++) {
      if (params[i].initializer !== void 0 || params[i].questionToken !== void 0) {
        return i;
      }
    }
    return -1;
  }
  // a method with >=1 optional parameter prints one typed full signature (callers pass every slot).
  hasDefaultedTail(node) {
    if (node === void 0 || !_typescript2.default.isMethodDeclaration(node)) {
      return false;
    }
    return this.javaHasOptionalParameter(node);
  }
  // a typed default-valued method parameter is written in place, so its writes convert to the declared type
  javaSplitParameterWriteType(node) {
    if (_optionalChain([node, 'optionalAccess', _1317 => _1317.initializer]) === void 0 || !this.hasDefaultedTail(node.parent)) {
      return void 0;
    }
    return this.javaOptionalParameterType(node);
  }
  // the async body copy of a reassigned default-valued parameter keeps a `List<String>` type;
  // its writes convert like the sync in-place ones (other types keep the `Object` copy)
  javaAsyncParameterLocalType(node) {
    if (_optionalChain([node, 'optionalAccess', _1318 => _1318.initializer]) === void 0 || !this.hasDefaultedTail(node.parent) || !this.isAsyncFunction(node.parent)) {
      return void 0;
    }
    const type = this.javaOptionalParameterType(node);
    return type === JAVA_STRING_LIST_TYPE ? type : void 0;
  }
  javaParameterIsTypeofTested(node) {
    const method = node.parent;
    const name = _optionalChain([node, 'access', _1319 => _1319.name, 'optionalAccess', _1320 => _1320.escapedText]);
    let found = false;
    const visit = (n) => {
      if (found) {
        return;
      }
      if (_typescript2.default.isTypeOfExpression(n) && _typescript2.default.isIdentifier(n.expression) && n.expression.escapedText === name) {
        found = true;
        return;
      }
      _typescript2.default.forEachChild(n, visit);
    };
    if (_optionalChain([method, 'optionalAccess', _1321 => _1321.body]) !== void 0 && name !== void 0) {
      _typescript2.default.forEachChild(method.body, visit);
    }
    return found;
  }
  javaParameterIsCompoundAssigned(node) {
    const method = node.parent;
    const name = _optionalChain([node, 'access', _1322 => _1322.name, 'optionalAccess', _1323 => _1323.escapedText]);
    if (_optionalChain([method, 'optionalAccess', _1324 => _1324.body]) === void 0 || name === void 0) {
      return false;
    }
    let assigned = this.javaMethodAssignedNames.get(method);
    if (assigned === void 0) {
      assigned = /* @__PURE__ */ new Set();
      const collect = (n) => {
        if (_typescript2.default.isBinaryExpression(n) && n.operatorToken.kind !== _typescript2.default.SyntaxKind.EqualsToken && JAVA_ASSIGNMENT_OPERATOR_KINDS.has(n.operatorToken.kind)) {
          const left = n.left;
          if (_typescript2.default.isIdentifier(left) && left.escapedText !== void 0) {
            assigned.add(left.escapedText);
          }
        }
        _typescript2.default.forEachChild(n, collect);
      };
      _typescript2.default.forEachChild(method.body, collect);
      this.javaMethodAssignedNames.set(method, assigned);
    }
    return assigned.has(name);
  }
  // a write to a parameter this printer declared natively: the right side prints from `Object`
  // locals, so the assignment carries the same checkcast as the call sites. The checker proved the
  // right side assignable to the parameter, so the declared type describes the value received.
  javaParameterAssignmentCast(left, right, identation) {
    if (!_typescript2.default.isIdentifier(left)) {
      return void 0;
    }
    const declaration = this.javaDeclarationOfIdentifier(left);
    if (declaration === void 0 || !_typescript2.default.isParameter(declaration) || left.escapedText !== _optionalChain([declaration, 'access', _1325 => _1325.name, 'optionalAccess', _1326 => _1326.escapedText])) {
      return void 0;
    }
    const native = _nullishCoalesce(this.javaNativeParameterType(declaration), () => ( this.javaSplitParameterWriteType(declaration)));
    if (native === void 0) {
      return void 0;
    }
    const leftText = this.printNode(left, 0);
    if (this.javaNativeArgumentAlreadyTyped(right, native)) {
      return `${leftText} = ${this.printNode(right, identation)}`;
    }
    if (native === "Long") {
      return `${leftText} = Helpers.toLongOrNull(${this.printNode(right, identation)})`;
    }
    if (native === JAVA_STRING_LIST_TYPE) {
      return `${leftText} = Helpers.toStringListArg(${this.printNode(right, identation)})`;
    }
    return `${leftText} = (${native}) (${this.printNode(right, identation)})`;
  }
  // ===== native return types (D-09) =====
  //
  // The native Java return a generated method declaration prints with, when its TS
  javaNativeReturnType(node) {
    if (node === void 0 || node.kind !== _typescript2.default.SyntaxKind.MethodDeclaration || node.name === void 0 || node.type === void 0 || node.body === void 0 || !_typescript2.default.isClassDeclaration(node.parent)) {
      return void 0;
    }
    if (this.javaReturnTypeInProgress.has(node)) {
      return void 0;
    }
    if (this.javaReturnTypeCache.has(node)) {
      return this.javaReturnTypeCache.get(node);
    }
    this.javaReturnTypeInProgress.add(node);
    let result;
    try {
      result = this.javaNativeReturnTypeUncached(node);
    } finally {
      this.javaReturnTypeInProgress.delete(node);
    }
    this.javaReturnTypeCache.set(node, result);
    return result;
  }
  javaNativeReturnTypeUncached(node) {
    if (!JAVA_NATIVE_PARAMETER_GENERATED_FILES.test(node.getSourceFile().fileName)) {
      return void 0;
    }
    if (this.isAsyncFunction(node)) {
      return void 0;
    }
    if (this.getMethodOverride(node) !== void 0) {
      return void 0;
    }
    const target = this.javaNativeReturnTypeTarget(node);
    if (target === void 0) {
      return void 0;
    }
    if (!this.javaReturnSitesPrintType(node, target)) {
      return void 0;
    }
    return target;
  }
  javaNativeReturnTypeTarget(node) {
    let type;
    try {
      type = this.getChecker().getTypeAtLocation(node.type);
    } catch (e) {
      return void 0;
    }
    if (type === void 0) {
      return void 0;
    }
    const aliasSymbol = type.aliasSymbol;
    if (aliasSymbol !== void 0) {
      const name = aliasSymbol.name;
      const fileName = _optionalChain([aliasSymbol, 'access', _1327 => _1327.declarations, 'optionalAccess', _1328 => _1328[0], 'optionalAccess', _1329 => _1329.getSourceFile, 'optionalCall', _1330 => _1330(), 'optionalAccess', _1331 => _1331.fileName]);
      if (fileName !== void 0 && JAVA_NATIVE_PARAMETER_SOURCE_FILES.test(fileName)) {
        if (name === "Str") {
          return "String";
        }
        if (name === "Bool") {
          return "Boolean";
        }
      }
    }
    if (type.flags === _typescript2.default.TypeFlags.String) {
      return "String";
    }
    if (type.flags === _typescript2.default.TypeFlags.Boolean) {
      return "Boolean";
    }
    return this.isJavaMapStructureType(type) ? JAVA_NATIVE_RETURN_MAP_TYPE : void 0;
  }
  // every `return` of the method body (nested functions are separate scopes) prints the
  // target Java type
  javaReturnSitesPrintType(method, target) {
    const returns = [];
    const collect = (n) => {
      if (n === void 0) {
        return;
      }
      if (n !== method && _typescript2.default.isFunctionLike(n)) {
        return;
      }
      if (_typescript2.default.isReturnStatement(n)) {
        returns.push(n);
      }
      _typescript2.default.forEachChild(n, collect);
    };
    collect(method.body);
    if (returns.length === 0) {
      return false;
    }
    return returns.every((r) => r.expression !== void 0 && this.javaExpressionPrintsType(r.expression, target));
  }
  javaExpressionPrintsType(expression, target) {
    const node = this.javaUnwrapReturnExpression(expression);
    if (node === void 0) {
      return false;
    }
    if (node.kind === _typescript2.default.SyntaxKind.NullKeyword) {
      return true;
    }
    if (_typescript2.default.isIdentifier(node) && node.escapedText === "undefined") {
      return true;
    }
    if (node.kind === _typescript2.default.SyntaxKind.ConditionalExpression) {
      return this.javaExpressionPrintsType(node.whenTrue, target) && this.javaExpressionPrintsType(node.whenFalse, target);
    }
    if (target === JAVA_NATIVE_RETURN_MAP_TYPE) {
      if (_typescript2.default.isObjectLiteralExpression(node)) {
        return true;
      }
      if (this.javaReturnedParameterType(node) === target) {
        return true;
      }
      return this.javaReturnedCallType(node) === target;
    }
    if (target === "String") {
      if (_typescript2.default.isStringLiteralLike(node)) {
        return true;
      }
      if (this.javaReturnedParameterType(node) === target) {
        return true;
      }
      if (this.javaReturnedCallType(node) === target) {
        return true;
      }
      return this.javaStringCallReturn(node);
    }
    if (target === "Boolean") {
      if (this.javaPrintsBooleanValue(node, /* @__PURE__ */ new Set(), 0)) {
        return true;
      }
      if (this.javaReturnedParameterType(node) === target) {
        return true;
      }
      return this.javaReturnedCallType(node) === target;
    }
    return false;
  }
  javaUnwrapReturnExpression(expression) {
    if (expression === void 0) {
      return void 0;
    }
    let node = expression;
    while (node !== void 0 && (_typescript2.default.isParenthesizedExpression(node) || _typescript2.default.isAsExpression(node) || _typescript2.default.isNonNullExpression(node) || _typescript2.default.isTypeAssertionExpression(node) || node.kind === _typescript2.default.SyntaxKind.SatisfiesExpression)) {
      node = node.expression;
    }
    return node;
  }
  // an identifier bound to a parameter the printer itself declares with the target Java
  // type (javaNativeParameterType: Dict/Market/Currency/Str/Bool annotations)
  javaReturnedParameterType(node) {
    if (!_typescript2.default.isIdentifier(node)) {
      return void 0;
    }
    const declaration = this.javaDeclarationOfIdentifier(node);
    if (declaration === void 0 || !_typescript2.default.isParameter(declaration)) {
      return void 0;
    }
    return this.javaNativeParameterType(declaration);
  }
  // a `this.<name> (...)` / `super.<name> (...)` return is the callee's native return type
  // (fixpoint over javaNativeReturnType)
  javaReturnedCallType(node) {
    if (!_typescript2.default.isCallExpression(node)) {
      return void 0;
    }
    const callee = node.expression;
    if (!_typescript2.default.isPropertyAccessExpression(callee) || callee.expression.kind !== _typescript2.default.SyntaxKind.ThisKeyword && !(_typescript2.default.isIdentifier(callee.expression) && callee.expression.escapedText === "super")) {
      return void 0;
    }
    let declaration;
    try {
      declaration = _optionalChain([this, 'access', _1332 => _1332.getChecker, 'call', _1333 => _1333(), 'access', _1334 => _1334.getResolvedSignature, 'call', _1335 => _1335(node), 'optionalAccess', _1336 => _1336.declaration]);
    } catch (e) {
      return void 0;
    }
    if (declaration === void 0 || declaration.kind !== _typescript2.default.SyntaxKind.MethodDeclaration) {
      return void 0;
    }
    return this.javaNativeReturnType(declaration);
  }
  // a `this.<name> (...)` return of a hand-written base String producer: the resolved
  // signature must still live in the base tier (a venue override prints its own boxed
  // Object signature)
  javaStringCallReturn(node) {
    if (!_typescript2.default.isCallExpression(node)) {
      return false;
    }
    const callee = node.expression;
    if (!_typescript2.default.isPropertyAccessExpression(callee) || callee.expression.kind !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return false;
    }
    if (!JAVA_STRING_RETURN_BASE_METHODS.has(callee.name.escapedText)) {
      return false;
    }
    let declaration;
    try {
      declaration = _optionalChain([this, 'access', _1337 => _1337.getChecker, 'call', _1338 => _1338(), 'access', _1339 => _1339.getResolvedSignature, 'call', _1340 => _1340(node), 'optionalAccess', _1341 => _1341.declaration]);
    } catch (e) {
      return false;
    }
    if (declaration === void 0) {
      return false;
    }
    const fileName = _optionalChain([declaration, 'access', _1342 => _1342.getSourceFile, 'optionalCall', _1343 => _1343(), 'optionalAccess', _1344 => _1344.fileName]);
    return fileName !== void 0 && JAVA_STRING_RETURN_BASE_FILES.test(fileName);
  }
  // the alias a parameter's annotation names; `OrderType` ('limit' | 'market' | string) reduces
  // to plain `string` and keeps no aliasSymbol, so read the annotation's type reference instead
  javaParameterAliasSymbol(node, type, checker) {
    const symbol = _nullishCoalesce(type.aliasSymbol, () => ( type.symbol));
    if (symbol !== void 0 || node.type === void 0 || !_typescript2.default.isTypeReferenceNode(node.type)) {
      return symbol;
    }
    const referenced = checker.getSymbolAtLocation(node.type.typeName);
    const alias = referenced !== void 0 && referenced.flags & _typescript2.default.SymbolFlags.Alias ? checker.getAliasedSymbol(referenced) : referenced;
    return alias !== void 0 && alias.flags & _typescript2.default.SymbolFlags.TypeAlias ? alias : void 0;
  }
  // the annotation proof alone, without the heritage check
  javaNativeParameterTypeOf(node) {
    if (node === void 0 || !_typescript2.default.isParameter(node) || node.initializer !== void 0 || node.dotDotDotToken !== void 0) {
      return void 0;
    }
    const method = node.parent;
    if (method === void 0 || !_typescript2.default.isMethodDeclaration(method) || !_typescript2.default.isClassDeclaration(method.parent)) {
      return void 0;
    }
    if (!JAVA_NATIVE_PARAMETER_BASE_FILES.test(node.getSourceFile().fileName) && !JAVA_NATIVE_PARAMETER_GENERATED_FILES.test(node.getSourceFile().fileName)) {
      return void 0;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const type = checker.getTypeAtLocation(node);
    if (type === void 0) {
      return void 0;
    }
    const symbol = this.javaParameterAliasSymbol(node, type, checker);
    const name = _optionalChain([symbol, 'optionalAccess', _1345 => _1345.name]);
    if (name === void 0 || JAVA_NATIVE_PARAMETER_TYPES[name] === void 0) {
      return void 0;
    }
    const excluded = JAVA_NATIVE_PARAMETER_EXCLUDED_POSITIONS[_optionalChain([method, 'access', _1346 => _1346.name, 'optionalAccess', _1347 => _1347.escapedText])];
    if (excluded !== void 0 && excluded.includes(method.parameters.indexOf(node))) {
      return void 0;
    }
    const declaration = _optionalChain([symbol, 'optionalAccess', _1348 => _1348.declarations, 'optionalAccess', _1349 => _1349[0]]);
    const fileName = _optionalChain([declaration, 'optionalAccess', _1350 => _1350.getSourceFile, 'optionalCall', _1351 => _1351(), 'optionalAccess', _1352 => _1352.fileName]);
    return JAVA_NATIVE_PARAMETER_SOURCE_FILES.test(_nullishCoalesce(fileName, () => ( ""))) ? JAVA_NATIVE_PARAMETER_TYPES[name] : void 0;
  }
  // `k` where the consumer declares k as a Java String: the helper's String branch (the
  // only one that can answer true for a map) is the native lookup, and no null key can
  // reach containsKey
  javaDeclaredStringType(expression) {
    const type = this.javaDeclaredTypeOf(expression);
    return type !== void 0 && JAVA_DECLARED_STRING_TYPE.test(type);
  }
  printCustomBinaryExpressionIfAny(node, identation) {
    const left = node.left;
    const right = node.right;
    const op = node.operatorToken.kind;
    if (left.kind === _typescript2.default.SyntaxKind.Identifier) {
      this.ReassignedVars[this.getVarKey(left)] = true;
    }
    if (left.kind === _typescript2.default.SyntaxKind.TypeOfExpression) {
      const typeOfExpression = this.handleTypeOfInsideBinaryExpression(
        node,
        identation
      );
      if (typeOfExpression)
        return typeOfExpression;
    }
    if (op === _typescript2.default.SyntaxKind.EqualsToken && left.kind === _typescript2.default.SyntaxKind.Identifier) {
      const assignment = this.javaParameterAssignmentCast(left, right, identation);
      if (assignment !== void 0) {
        return assignment;
      }
    }
    if (op === _typescript2.default.SyntaxKind.EqualsToken && left.kind === _typescript2.default.SyntaxKind.ArrayLiteralExpression) {
      const arrayBindingPatternElements = left.elements;
      const parsedArrayBindingElements = arrayBindingPatternElements.map((e) => {
        this.ReassignedVars[this.getVarKey(e)] = true;
        return this.printNode(e, 0);
      });
      const syntheticName = parsedArrayBindingElements.join("") + "Variable";
      let arrayBindingStatement = `var ${syntheticName} = ${this.printNode(right, 0)};
`;
      parsedArrayBindingElements.forEach((e, index) => {
        let elementValue = `((java.util.List<Object>) ${syntheticName}).get(${index})`;
        const target = arrayBindingPatternElements[index];
        if (_typescript2.default.isIdentifier(target)) {
          const declaration = this.javaDeclarationOfIdentifier(target);
          const native = declaration !== void 0 && _typescript2.default.isParameter(declaration) ? _nullishCoalesce(this.javaNativeParameterType(declaration), () => ( this.javaSplitParameterWriteType(declaration))) : void 0;
          if (native === "Long") {
            elementValue = `Helpers.toLongOrNull(${elementValue})`;
          } else if (native !== void 0) {
            elementValue = `(${native}) ${elementValue}`;
          }
        }
        const statement = this.getIden(identation) + `${e} = ${elementValue}`;
        if (index < parsedArrayBindingElements.length - 1) {
          arrayBindingStatement += statement + ";\n";
        } else {
          arrayBindingStatement += statement;
        }
      });
      return arrayBindingStatement;
    }
    if (op === _typescript2.default.SyntaxKind.EqualsToken && left.kind === _typescript2.default.SyntaxKind.ElementAccessExpression) {
      const keys = [];
      let baseExpr = null;
      let cur = left;
      while (_typescript2.default.isElementAccessExpression(cur)) {
        keys.unshift(cur.argumentExpression);
        const expr = cur.expression;
        if (!_typescript2.default.isElementAccessExpression(expr)) {
          baseExpr = expr;
          break;
        }
        cur = expr;
      }
      const containerStr = this.printNode(baseExpr, 0);
      const keyStrs = keys.map((k) => this.printNode(k, 0));
      let acc = containerStr;
      let firstKey = 0;
      if (keyStrs.length > 1 && this.javaDeclaredMapReceiver(baseExpr) && _typescript2.default.isStringLiteralLike(keys[0])) {
        acc = `${containerStr}.get(${keyStrs[0]})`;
        firstKey = 1;
      } else if (keyStrs.length > 1) {
        const fieldRead = this.javaFieldMapReadText(baseExpr, keys[0]);
        if (fieldRead !== void 0) {
          acc = fieldRead;
          firstKey = 1;
        }
      }
      for (let i = firstKey; i < keyStrs.length - 1; i++) {
        acc = `${this.ELEMENT_ACCESS_WRAPPER_OPEN}${acc}, ${keyStrs[i]}${this.ELEMENT_ACCESS_WRAPPER_CLOSE}`;
      }
      const prefixes = "";
      const lastKey = keyStrs[keyStrs.length - 1];
      const rhs = this.printNode(right, 0);
      const keyArg = this.elementWriteKeyText(keys[keys.length - 1], lastKey);
      if (this.elementWriteTargetsMap(left.expression, baseExpr, keys)) {
        return `${prefixes}((${this.OBJECT_KEYWORD})${acc}).put(${keyArg}, ${rhs})`;
      }
      return `${prefixes}Helpers.addElementToObject(${acc}, ${lastKey}, ${rhs})`;
    }
    if (op === _typescript2.default.SyntaxKind.InKeyword) {
      const objectType = this.getChecker().getTypeAtLocation(right);
      const keyType = this.getChecker().getTypeAtLocation(left);
      const objText = this.printNode(right, 0);
      const keyText = this.printNode(left, 0);
      const keyOk = this.isJavaStringType(keyType) || this.javaDeclaredStringType(left);
      if (keyOk) {
        if (this.javaDeclaredMapReceiver(right)) {
          return `${objText}.containsKey(${keyText})`;
        }
        if (this.isJavaMapType(objectType)) {
          return `((java.util.Map<?, ?>)${objText}).containsKey(${keyText})`;
        }
        if (this.isJavaNullableMapType(objectType) && this.javaRepeatableOperand(right)) {
          return `(${objText} != null && ((java.util.Map<?, ?>)${objText}).containsKey(${keyText}))`;
        }
      }
      if (this.javaSideEffectFreeReference(left) && !this.javaOperandPrintsPrimitiveNumber(left) && !this.isNullishLiteral(left) && left.kind !== _typescript2.default.SyntaxKind.TrueKeyword && left.kind !== _typescript2.default.SyntaxKind.FalseKeyword) {
        const guarded = `${keyText} != null && `;
        if (this.javaDeclaredMapReceiver(right)) {
          return `(${guarded}${objText}.containsKey(${keyText}))`;
        }
        if (this.isJavaMapType(objectType)) {
          return `(${guarded}((java.util.Map<?, ?>)${objText}).containsKey(${keyText}))`;
        }
        if (this.isJavaNullableMapType(objectType) && this.javaRepeatableOperand(right)) {
          return `(${objText} != null && ${guarded}((java.util.Map<?, ?>)${objText}).containsKey(${keyText}))`;
        }
      }
      return `Helpers.inOp(${objText}, ${keyText})`;
    }
    if (op === _typescript2.default.SyntaxKind.LessThanToken || op === _typescript2.default.SyntaxKind.GreaterThanToken || op === _typescript2.default.SyntaxKind.LessThanEqualsToken || op === _typescript2.default.SyntaxKind.GreaterThanEqualsToken) {
      const leftKind = this.javaPrimitiveOperandKind(left);
      const rightKind = this.javaPrimitiveOperandKind(right);
      const orderingSafe = op === _typescript2.default.SyntaxKind.GreaterThanToken || this.javaComparisonOperandsAreExact(left, leftKind, right, rightKind);
      if (leftKind !== void 0 && rightKind !== void 0 && orderingSafe) {
        return `${this.printNode(left, 0)} ${this.SupportedKindNames[op]} ${this.printNode(right, 0)}`;
      }
    }
    if (op === _typescript2.default.SyntaxKind.PlusEqualsToken || op === _typescript2.default.SyntaxKind.MinusEqualsToken || op in this.binaryExpressionsWrappers) {
      const leftText = this.printNode(left, 0);
      const rightText = this.printNode(right, 0);
      const inlined = this.printInlineHelperArithmetic(left, right, leftText, rightText, op);
      if (inlined !== void 0) {
        return inlined;
      }
      if (op === _typescript2.default.SyntaxKind.PlusEqualsToken) {
        return `${leftText} = Helpers.add(${leftText}, ${rightText})`;
      }
      if (op === _typescript2.default.SyntaxKind.MinusEqualsToken) {
        return `${leftText} = Helpers.subtract(${leftText}, ${rightText})`;
      }
      const wrapper = this.binaryExpressionsWrappers[op];
      const nativeEquality = this.printNativeEqualityIfProvable(node, leftText, rightText);
      if (nativeEquality !== void 0) {
        return nativeEquality;
      }
      const open = wrapper[0];
      const close = wrapper[1];
      return `${open}${leftText}, ${rightText}${close}`;
    }
    return void 0;
  }
  // dict-shaped values are Map<String, Object> in the Java port: raw HashMap/ConcurrentHashMap
  // or a types.TypedMap view (AbstractMap<String, Object> over the raw payload). Proven by the
  // checker (string index signature, or an interface/alias declared in the base types file).
  isJavaMapStructureType(type) {
    if (type === void 0) {
      return false;
    }
    const excludedFlags = _typescript2.default.TypeFlags.Any | _typescript2.default.TypeFlags.Unknown | _typescript2.default.TypeFlags.Union | _typescript2.default.TypeFlags.Intersection | _typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.TypeParameter | _typescript2.default.TypeFlags.Conditional | _typescript2.default.TypeFlags.Never;
    if ((type.flags & excludedFlags) !== 0) {
      return false;
    }
    const checker = this.getChecker();
    if (checker.isArrayType(type) || checker.isTupleType(type)) {
      return false;
    }
    if (type.getStringIndexType() !== void 0) {
      return true;
    }
    const symbol = _nullishCoalesce(type.aliasSymbol, () => ( type.symbol));
    const declaration = _optionalChain([symbol, 'optionalAccess', _1353 => _1353.declarations, 'optionalAccess', _1354 => _1354[0]]);
    const fileName = _optionalChain([declaration, 'optionalAccess', _1355 => _1355.getSourceFile, 'optionalCall', _1356 => _1356(), 'optionalAccess', _1357 => _1357.fileName]);
    return fileName !== void 0 && /(^|\/)ts\/src\/base\/types\.ts$/.test(fileName);
  }
  // tuples are List<Object> in the Java port (types.TypedList). Only an index inside the
  // tuple's required elements goes native: .get(i) throws out of range where the helper
  // returns null, so non-tuple array reads (any[], string[], ...) keep the helper.
  isJavaListStructureType(type) {
    if (type === void 0) {
      return false;
    }
    const excludedFlags = _typescript2.default.TypeFlags.Any | _typescript2.default.TypeFlags.Unknown | _typescript2.default.TypeFlags.Union | _typescript2.default.TypeFlags.Intersection | _typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.TypeParameter | _typescript2.default.TypeFlags.Conditional | _typescript2.default.TypeFlags.Never;
    if ((type.flags & excludedFlags) !== 0) {
      return false;
    }
    return this.getChecker().isTupleType(type);
  }
  tupleRequiredElementCount(type) {
    const flags = _nullishCoalesce(_nullishCoalesce(_optionalChain([type, 'optionalAccess', _1358 => _1358.target, 'optionalAccess', _1359 => _1359.elementFlags]), () => ( _optionalChain([type, 'optionalAccess', _1360 => _1360.elementFlags]))), () => ( []));
    let required = 0;
    for (const flag of flags) {
      if (flag !== _typescript2.default.ElementFlags.Optional && flag !== _typescript2.default.ElementFlags.Rest) {
        required++;
      } else {
        break;
      }
    }
    return required;
  }
  // non-tuple array reads: the Java representation is java.util.List (the printer maps every
  // array type to one), but neither the receiver nor the index is bounded statically, so the
  // read keeps the helper's null / off-range outcomes.
  isJavaArrayStructureType(type) {
    if (type === void 0) {
      return false;
    }
    const excludedFlags = _typescript2.default.TypeFlags.Any | _typescript2.default.TypeFlags.Unknown | _typescript2.default.TypeFlags.Union | _typescript2.default.TypeFlags.Intersection | _typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.TypeParameter | _typescript2.default.TypeFlags.Conditional | _typescript2.default.TypeFlags.Never;
    if ((type.flags & excludedFlags) !== 0) {
      return false;
    }
    const checker = this.getChecker();
    if (checker.isTupleType(type)) {
      return false;
    }
    return checker.isArrayType(type);
  }
  // the null-safe element read prints its receiver once per guard; only a reference whose
  // printed Java has no side effects (identifier, `this`/`this.field` chain) may repeat it.
  javaSideEffectFreeReference(node) {
    if (node === void 0) {
      return false;
    }
    switch (node.kind) {
      case _typescript2.default.SyntaxKind.Identifier:
      case _typescript2.default.SyntaxKind.ThisKeyword:
        return true;
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
        return this.javaSideEffectFreeReference(node.expression);
      case _typescript2.default.SyntaxKind.PropertyAccessExpression:
        return this.javaSideEffectFreeReference(node.expression);
      default:
        return false;
    }
  }
  // receivers whose printed elements the ccxt post-pass types as String FROM the helper call
  // (`x.split(sep)` and string-literal array producers): those locals narrow to String only
  // while the read prints `Helpers.GetValue(`, so their reads keep the helper.
  javaStringElementsReceiver(node) {
    if (!_typescript2.default.isIdentifier(node)) {
      return false;
    }
    const declaration = _optionalChain([this, 'access', _1361 => _1361.getChecker, 'call', _1362 => _1362(), 'access', _1363 => _1363.getSymbolAtLocation, 'call', _1364 => _1364(node), 'optionalAccess', _1365 => _1365.valueDeclaration]);
    if (declaration === void 0 || !_typescript2.default.isVariableDeclaration(declaration)) {
      return false;
    }
    const initializer = declaration.initializer;
    if (initializer === void 0) {
      return false;
    }
    if (_typescript2.default.isCallExpression(initializer) && _typescript2.default.isPropertyAccessExpression(initializer.expression)) {
      return _optionalChain([initializer, 'access', _1366 => _1366.expression, 'access', _1367 => _1367.name, 'optionalAccess', _1368 => _1368.escapedText]) === "split";
    }
    if (_typescript2.default.isArrayLiteralExpression(initializer)) {
      return initializer.elements.length > 0 && initializer.elements.every((element) => element.kind === _typescript2.default.SyntaxKind.StringLiteral || element.kind === _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral);
    }
    return false;
  }
  isLeftSideOfAssignment(node) {
    const parent = node.parent;
    if (_optionalChain([parent, 'optionalAccess', _1369 => _1369.kind]) !== _typescript2.default.SyntaxKind.BinaryExpression || parent.left !== node) {
      return false;
    }
    return JAVA_ASSIGNMENT_OPERATOR_KINDS.has(parent.operatorToken.kind);
  }
  // the declaration node behind an identifier, when the checker resolves one
  javaDeclarationOfIdentifier(expression) {
    if (expression === void 0 || !_typescript2.default.isIdentifier(expression)) {
      return void 0;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const symbol = checker.getSymbolAtLocation(expression);
    const declaration = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1370 => _1370.valueDeclaration]), () => ( _optionalChain([symbol, 'optionalAccess', _1371 => _1371.declarations, 'optionalAccess', _1372 => _1372[0]])));
    if (declaration === void 0) {
      return void 0;
    }
    const kind = declaration.kind;
    if (kind !== _typescript2.default.SyntaxKind.VariableDeclaration && kind !== _typescript2.default.SyntaxKind.Parameter) {
      return void 0;
    }
    return declaration;
  }
  // `x["lit"]` where the consumer declares x as a Java map: the native read returns the
  // element or null, exactly what the helper's Map branch returns, and the declaration
  // already carries the type, so no cast is needed.
  javaDeclaredMapReceiver(expression) {
    if (expression === void 0) {
      return false;
    }
    const declaration = this.javaDeclarationOfIdentifier(expression);
    if (declaration === void 0) {
      return false;
    }
    if (expression.escapedText !== _optionalChain([declaration, 'access', _1373 => _1373.name, 'optionalAccess', _1374 => _1374.escapedText])) {
      return false;
    }
    const type = this.javaDeclaredTypeOfDeclaration(declaration);
    if (type === void 0) {
      return false;
    }
    return JAVA_DECLARED_MAP_TYPES.test(type);
  }
  // `x[i]` where x is a declared java List and i is the int counter of `for (var i = <int literal>`:
  // GetValue answers null for a null receiver or index outside [0, size) while List.get throws, so
  // the emission carries the same tests. `||` short-circuits over identifiers, so nothing runs twice.
  javaDeclaredListElementRead(node, isCounter) {
    if (_optionalChain([node, 'access', _1375 => _1375.parent, 'optionalAccess', _1376 => _1376.kind]) === _typescript2.default.SyntaxKind.ExpressionStatement) {
      return void 0;
    }
    if (_optionalChain([node, 'access', _1377 => _1377.parent, 'optionalAccess', _1378 => _1378.kind]) === _typescript2.default.SyntaxKind.VariableDeclaration && node.parent.initializer === node && _optionalChain([node, 'access', _1379 => _1379.parent, 'access', _1380 => _1380.name, 'optionalAccess', _1381 => _1381.escapedText]) === "client") {
      return void 0;
    }
    const declared = this.javaDeclaredTypeOf(node.expression);
    const declaredList = declared !== void 0 && JAVA_DECLARED_LIST_TYPES.test(declared);
    let target;
    let list;
    if (declaredList) {
      target = this.printNode(node.expression, 0);
      list = target;
    } else {
      const type = this.getChecker().getTypeAtLocation(node.expression);
      if (!this.isJavaListValueType(type) || this.isVarargsArrayReference(node.expression) || !this.javaSideEffectFreeReference(node.expression)) {
        return void 0;
      }
      target = this.printNode(node.expression, 0);
      list = `((java.util.List<?>)${target})`;
    }
    if (!isCounter && Number(node.argumentExpression.text) > 2147483647) {
      return void 0;
    }
    if (this.javaStringElementsReceiver(node.expression)) {
      return void 0;
    }
    const indexText = this.printNode(node.argumentExpression, 0);
    const lowerBound = isCounter ? `${indexText} < 0 || ` : "";
    return `(${target} == null || ${lowerBound}${indexText} >= ${list}.size() ? null : ${list}.get(${indexText}))`;
  }
  // `x[k]` where a consumer declares x a java Map and the key is not a literal: the
  // helper's Map branch is the map accessor behind its own tests — a null receiver, a
  // null key and a key that is not a String all answer null, and a String-keyed map's
  javaDeclaredMapElementRead(node) {
    if (_optionalChain([node, 'access', _1382 => _1382.parent, 'optionalAccess', _1383 => _1383.kind]) === _typescript2.default.SyntaxKind.ExpressionStatement) {
      return void 0;
    }
    if (this.printElementAccessExpressionExceptionIfAny(node) !== void 0 || this.isLeftSideOfAssignment(node)) {
      return void 0;
    }
    if (!this.javaDeclaredMapReceiver(node.expression)) {
      return void 0;
    }
    const key = node.argumentExpression;
    if (!_typescript2.default.isIdentifier(key) || !this.javaRepeatableOperand(key)) {
      return void 0;
    }
    const declared = this.javaDeclaredTypeOf(key);
    if (declared !== void 0 && JAVA_PRIMITIVE_DECLARED_TYPES.has(declared.trim())) {
      return void 0;
    }
    if (declared === void 0 && this.javaOperandPrintsPrimitiveNumber(key)) {
      return void 0;
    }
    let keyType;
    try {
      keyType = this.getChecker().getTypeAtLocation(key);
    } catch (e) {
      return void 0;
    }
    if (keyType === void 0 || this.getChecker().isArrayType(keyType) || this.getChecker().isTupleType(keyType)) {
      return void 0;
    }
    const target = this.printNode(node.expression, 0);
    const keyText = this.printNode(key, 0);
    if (keyType.aliasSymbol === void 0 && keyType.flags === _typescript2.default.TypeFlags.String) {
      return `(${target} == null || ${keyText} == null ? null : ${target}.get(${keyText}))`;
    }
    return `(${target} == null || !(${keyText} instanceof String) ? null : ${target}.get(${keyText}))`;
  }
  // the identifier of a `for (var i = <int literal>; …; i++)` counter that still prints as
  // `i`: the JN capture rename prints `finalI`, a `final Object` box, which is not an int
  javaPrimitiveCounterIndex(node) {
    if (!this.isJavaPrimitiveCounterReference(node)) {
      return false;
    }
    const declaration = this.javaDeclarationOfIdentifier(node);
    return declaration !== void 0 && node.escapedText === _optionalChain([declaration, 'access', _1384 => _1384.name, 'optionalAccess', _1385 => _1385.escapedText]);
  }
  // `this.<field>[k]` on a hand-written base map field (JAVA_FIELD_TYPES): the helper's Map branch is
  // the native `get`. The helper answers null for a null receiver or key, so both stay guarded; the
  // receiver and key are side-effect free. An unguarded read has no parens so a checkcast binds it.
  javaFieldMapReadText(receiver, key) {
    if (receiver === void 0 || receiver.kind !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([receiver, 'access', _1386 => _1386.expression, 'optionalAccess', _1387 => _1387.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return void 0;
    }
    const name = _optionalChain([receiver, 'access', _1388 => _1388.name, 'optionalAccess', _1389 => _1389.escapedText]);
    const field = typeof name === "string" ? JAVA_FIELD_TYPES[name] : void 0;
    if (field === void 0 || field.map !== true) {
      return void 0;
    }
    const keyText = this.printNode(key, 0);
    let keyGuarded = false;
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const keyType = checker.getTypeAtLocation(key);
    if (!this.isJavaStringType(keyType) && !this.javaDeclaredStringType(key)) {
      if (!this.javaRepeatableOperand(key)) {
        return void 0;
      }
      keyGuarded = true;
    }
    const target = this.printNode(receiver, 0);
    const read = `((java.util.Map<?, ?>)${target}).get(${keyText})`;
    if (!keyGuarded && field.nullable !== true) {
      return read;
    }
    const receiverGuard = field.nullable === true ? `${target} == null ? null : ` : "";
    return `(${keyGuarded ? `${keyText} == null ? null : ` : ""}${receiverGuard}${read})`;
  }
  javaFieldMapRead(node) {
    return this.javaFieldMapReadText(node.expression, node.argumentExpression);
  }
  // the read stands only where no exchange-specific override claims the site and the node
  // is not a write target
  javaFieldMapReadIfAllowed(node) {
    const read = this.javaFieldMapRead(node);
    if (read === void 0) {
      return void 0;
    }
    if (this.printElementAccessExpressionExceptionIfAny(node) !== void 0 || this.isLeftSideOfAssignment(node)) {
      return void 0;
    }
    return read;
  }
  // `x[k]` reads: emit the native container accessor when the checker proves the Java
  // representation of `x`, otherwise return undefined so the base prints Helpers.GetValue.
  printCheckerTypedElementAccessRead(node) {
    const key = node.argumentExpression;
    const isStringKey = _typescript2.default.isStringLiteralLike(key);
    const isNumberKey = _typescript2.default.isNumericLiteral(key);
    const isCounterKey = !isStringKey && !isNumberKey && this.javaPrimitiveCounterIndex(key);
    if (!isStringKey && !isNumberKey && !isCounterKey) {
      const field = this.javaFieldMapReadIfAllowed(node);
      if (field !== void 0) {
        return field;
      }
      return this.javaDeclaredMapElementRead(node);
    }
    if (this.printElementAccessExpressionExceptionIfAny(node) !== void 0) {
      return void 0;
    }
    if (this.isLeftSideOfAssignment(node)) {
      return void 0;
    }
    const type = this.getChecker().getTypeAtLocation(node.expression);
    if (isStringKey) {
      if (!this.isJavaMapStructureType(type)) {
        if (!this.javaDeclaredMapReceiver(node.expression)) {
          return this.javaFieldMapReadIfAllowed(node);
        }
        return `${this.printNode(node.expression, 0)}.get(${this.printNode(key, 0)})`;
      }
      const target2 = this.printNode(node.expression, 0);
      return `((java.util.Map<String, Object>)${target2}).get(${this.printNode(key, 0)})`;
    }
    if (isCounterKey) {
      return this.javaDeclaredListElementRead(node, true);
    }
    const index = Number(key.text);
    if (!Number.isInteger(index) || index < 0) {
      return void 0;
    }
    if (this.isJavaListStructureType(type)) {
      if (index >= this.tupleRequiredElementCount(type)) {
        return void 0;
      }
      const target2 = this.printNode(node.expression, 0);
      return `((java.util.List<Object>)${target2}).get(${this.printNode(key, 0)})`;
    }
    if (!this.isJavaArrayStructureType(type)) {
      return this.javaDeclaredListElementRead(node, false);
    }
    if (!this.javaSideEffectFreeReference(node.expression) || this.javaStringElementsReceiver(node.expression)) {
      return void 0;
    }
    const target = this.printNode(node.expression, 0);
    const list = `((java.util.List<?>)${target})`;
    const keyText = this.printNode(key, 0);
    return `(${target} == null || ${keyText} >= ${list}.size() ? null : ${list}.get(${keyText}))`;
  }
  printElementAccessExpression(node, identation) {
    const native = this.printCheckerTypedElementAccessRead(node);
    if (native !== void 0) {
      return native;
    }
    return super.printElementAccessExpression(node, identation);
  }
  // ---- helper-family inlining: `+ - * / += -=` ---------------------------
  // `x + y` normally prints Helpers.add, `- * /` print Helpers.subtract/multiply/
  // divide, and `+=`/`-=` print `x = Helpers.add/subtract(...)`. They print native
  // Java when BOTH operands are checker-typed in the same non-nullable scalar family
  // and the printed operands carry the matching Java kind, so the native expression
  // has the helper's boxed result kind and value on every path.
  // the TypeScript scalar family of a binary operand: plain `string`/`number` and
  // their literals only. The nullable aliases (Str/Int/Num/Bool), unions and `any`
  // can hold undefined at runtime, which the helpers absorb.
  javaScalarFamily(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const type = checker.getTypeAtLocation(node);
    if (type === void 0 || type.aliasSymbol !== void 0) {
      return void 0;
    }
    const flags = type.flags;
    if (flags === _typescript2.default.TypeFlags.String || flags === _typescript2.default.TypeFlags.StringLiteral) {
      return "string";
    }
    if (flags === _typescript2.default.TypeFlags.Number || flags === _typescript2.default.TypeFlags.NumberLiteral) {
      return "number";
    }
    return void 0;
  }
  // true when the printed Java for this operand is statically a String: a string
  // literal, a nested `+` this rule prints as a native concat, or a form the
  // embedding build layer's javaExpressionTypeResolver names `String` (a local whose
  // emitted declaration is `String <name> = `, a call to a hand-written `public
  // String` runtime method). Java compiles `+` only when at least one operand is
  // statically a String, so a String local anchors the concat like a literal does.
  javaProvableString(node) {
    if (node === void 0) {
      return false;
    }
    switch (node.kind) {
      case _typescript2.default.SyntaxKind.StringLiteral:
      case _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral:
        return true;
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
        return this.javaProvableString(node.expression);
      case _typescript2.default.SyntaxKind.BinaryExpression:
        return this.javaNativeConcat(node);
    }
    return this.javaResolvedString(node);
  }
  // the embedding build layer's proof of the concrete printed Java type of an
  // expression (installed like csharpExpressionTypeResolver); only `String` is
  // consumed here, and a missing resolver proves nothing
  javaResolvedString(node) {
    if (node === void 0 || this.javaExpressionTypeResolver === void 0) {
      return false;
    }
    try {
      return this.javaExpressionTypeResolver(node) === "String";
    } catch (e) {
      return false;
    }
  }
  // does this `+` node print as a native concat (both sides plain string, one side a
  // provable String)? Mirrors printInlineHelperArithmetic so callers can reason about
  // the printed text of a nested concat.
  javaNativeConcat(node) {
    if (_optionalChain([node, 'optionalAccess', _1390 => _1390.operatorToken, 'optionalAccess', _1391 => _1391.kind]) !== _typescript2.default.SyntaxKind.PlusToken) {
      return false;
    }
    if (this.javaScalarFamily(node.left) !== "string" || this.javaScalarFamily(node.right) !== "string") {
      return false;
    }
    return this.javaProvableString(node.left) || this.javaProvableString(node.right);
  }
  // B-13: the `+` / `+=` concat drops the helper when one side's printed Java is
  // provably a String and the other side is an operand Helpers.add's String branch
  // converts exactly like javac's `+` does
  javaStringConcatIsProvable(left, right, leftFamily, rightFamily) {
    const leftProvable = this.javaProvableString(left);
    const rightProvable = this.javaProvableString(right);
    if (leftFamily === "string" && rightFamily === "string" && (leftProvable || rightProvable)) {
      return true;
    }
    return leftProvable && this.javaConcatOtherOperandIsSafe(right) || rightProvable && this.javaConcatOtherOperandIsSafe(left);
  }
  // the non-anchor operand of a concat: a value StringBuilder.append and Helpers.add's
  // `String.valueOf` branch turn into the same text. Inlined when it is a printed String, when the
  // checker proves a plain non-nullable `string`, or when the type cannot be a boxed Double.
  javaConcatOtherOperandIsSafe(node) {
    if (node === void 0) {
      return false;
    }
    if (this.javaProvableString(node)) {
      return true;
    }
    if (this.javaScalarFamily(node) === "string") {
      return true;
    }
    return this.javaConcatOperandPrintsAsValue(node) && !this.javaConcatOperandCanBeDouble(node);
  }
  // a bare value an infix operator can take without extra parentheses: a ternary,
  // assignment or comma expression printed here would re-parse (`(x + c ? a : b)`),
  // and an optional-chain call prints a guarded shape that is not an operand
  javaConcatOperandPrintsAsValue(node) {
    if (node === void 0) {
      return false;
    }
    if (_typescript2.default.isParenthesizedExpression(node)) {
      return this.javaConcatOperandPrintsAsValue(node.expression);
    }
    switch (node.kind) {
      case _typescript2.default.SyntaxKind.Identifier:
      case _typescript2.default.SyntaxKind.StringLiteral:
      case _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral:
      case _typescript2.default.SyntaxKind.NumericLiteral:
      case _typescript2.default.SyntaxKind.TrueKeyword:
      case _typescript2.default.SyntaxKind.FalseKeyword:
      case _typescript2.default.SyntaxKind.NullKeyword:
      case _typescript2.default.SyntaxKind.ThisKeyword:
      case _typescript2.default.SyntaxKind.NewExpression:
      case _typescript2.default.SyntaxKind.ArrayLiteralExpression:
        return true;
      case _typescript2.default.SyntaxKind.PropertyAccessExpression:
      case _typescript2.default.SyntaxKind.ElementAccessExpression:
      case _typescript2.default.SyntaxKind.CallExpression:
        return node.questionDotToken === void 0;
    }
    return false;
  }
  // a Double operand makes Helpers.add take its `instanceof Double` branch and return a NUMBER, while
  // javac's `+` concatenates, so an operand that can be a boxed Double keeps the helper. Only types
  // that can never hold a number are accepted, plus integer literals that print as a Java `long`.
  javaConcatOperandCanBeDouble(node) {
    if (this.javaProvableNumericKind(node) === "long") {
      return false;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return true;
    }
    const type = checker.getTypeAtLocation(node);
    if (type === void 0) {
      return true;
    }
    const notNumber = _typescript2.default.TypeFlags.String | _typescript2.default.TypeFlags.StringLiteral | _typescript2.default.TypeFlags.TemplateLiteral | _typescript2.default.TypeFlags.Boolean | _typescript2.default.TypeFlags.BooleanLiteral | _typescript2.default.TypeFlags.Object | _typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Void | _typescript2.default.TypeFlags.Never;
    const notNumeric = (t) => {
      if (t === void 0 || t.flags === 0) {
        return false;
      }
      if (_optionalChain([t, 'access', _1392 => _1392.isUnion, 'optionalCall', _1393 => _1393()])) {
        return t.types.every(notNumeric);
      }
      return (t.flags & ~notNumber) === 0;
    };
    return !notNumeric(type);
  }
  // the Java kind a `this.<name>(...)` call provably prints with (JAVA_THIS_RETURN_TYPES), or
  // undefined. The signature must resolve to the base tier or the Date.now lib signature of the
  // functions/time.ts mixin; a venue override prints its own (usually Object) signature.
  javaThisCallNumericKind(node) {
    if (_optionalChain([node, 'optionalAccess', _1394 => _1394.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return void 0;
    }
    const callee = node.expression;
    if (_optionalChain([callee, 'optionalAccess', _1395 => _1395.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([callee, 'access', _1396 => _1396.expression, 'optionalAccess', _1397 => _1397.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return void 0;
    }
    const name = _optionalChain([callee, 'access', _1398 => _1398.name, 'optionalAccess', _1399 => _1399.escapedText]);
    if (typeof name !== "string") {
      return void 0;
    }
    const kind = JAVA_THIS_RETURN_TYPES[name];
    if (kind === void 0) {
      return void 0;
    }
    let declaration;
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      declaration = void 0;
    }
    declaration = _optionalChain([checker, 'access', _1400 => _1400.getResolvedSignature, 'call', _1401 => _1401(node), 'optionalAccess', _1402 => _1402.declaration]);
    const fileName = _optionalChain([declaration, 'optionalAccess', _1403 => _1403.getSourceFile, 'optionalCall', _1404 => _1404(), 'access', _1405 => _1405.fileName]);
    if (typeof fileName !== "string") {
      return void 0;
    }
    if (!JAVA_THIS_RETURN_TYPES_BASE_FILE.test(fileName) && !JAVA_THIS_RETURN_TYPES_LIB_FILE.test(fileName)) {
      return void 0;
    }
    return kind;
  }
  // the Java kind a numeric operand provably prints with: decimal integer literal -> 'long',
  // fractional literal -> 'double', a nested native `+ - * /` -> its kind, a local retyped
  // `Long`/`Double` -> that kind. Hex/binary literals, negatives, calls, untyped locals: undefined.
  javaProvableNumericKind(node, allowDeclaredLocals = true) {
    if (node === void 0) {
      return void 0;
    }
    if (node.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.javaProvableNumericKind(node.expression, allowDeclaredLocals);
    }
    if (_typescript2.default.isNumericLiteral(node)) {
      const text = node.text;
      if (/^0[xXbBoO]/.test(text)) {
        return void 0;
      }
      return /[.eE]/.test(text) ? "double" : "long";
    }
    if (node.kind === _typescript2.default.SyntaxKind.Identifier) {
      return allowDeclaredLocals ? this.javaDeclaredNumericLocalKind(node) : void 0;
    }
    if (node.kind === _typescript2.default.SyntaxKind.CallExpression) {
      if (this.javaBaseTimeLongCall(node)) {
        return "long";
      }
      return this.javaBaseIntCall(node) ? "int" : void 0;
    }
    if (node.kind === _typescript2.default.SyntaxKind.BinaryExpression) {
      return this.javaNativeArithmeticKind(node, allowDeclaredLocals);
    }
    return void 0;
  }
  // a bare identifier the embedding layer declares `Long`/`Double` prints as a boxed numeric, but a
  // null box would NPE where the helpers return null, so the checker must see a plain non-nullable
  // number here (nullable aliases and `any` excluded; a narrowed `number` is a real guard in Java).
  javaDeclaredNumericLocalKind(node) {
    if (!this.javaOperandIsNonNullNumber(node)) {
      return void 0;
    }
    if (!this.javaIdentifierKeepsDeclaredName(node)) {
      return void 0;
    }
    if (!this.javaIdentifierPrintsDeclaredName(node)) {
      return void 0;
    }
    const declaration = this.javaDeclarationOfIdentifier(node);
    let javaType;
    if (declaration !== void 0) {
      try {
        javaType = this.javaDeclaredTypeOfDeclaration(declaration);
      } catch (e) {
        javaType = void 0;
      }
    }
    if (javaType === void 0 && typeof this.javaExpressionTypeResolver === "function") {
      try {
        javaType = this.javaExpressionTypeResolver(node);
      } catch (e) {
        javaType = void 0;
      }
    }
    return this.javaDeclaredNumericTypeKind(javaType);
  }
  // the numeric kind a printed Java declaration type names, or undefined for every other
  // type (`Object`, `String`, a boxed collection)
  javaDeclaredNumericTypeKind(javaType) {
    if (javaType === void 0) {
      return void 0;
    }
    const type = String(javaType).replace(/^final\s+/, "").trim();
    if (type === "Long" || type === "long") {
      return "long";
    }
    if (type === "Double" || type === "double") {
      return "double";
    }
    if (type === "Integer" || type === "int") {
      return "int";
    }
    return void 0;
  }
  // the use prints the declaration's own name: a local or parameter the printer renamed
  // (the `finalX` object-literal capture, the async parameter wrapper) prints against a
  // different declaration, whose recorded type does not describe it
  javaIdentifierPrintsDeclaredName(node) {
    try {
      return this.printNode(node, 0) === String(node.escapedText);
    } catch (e) {
      return false;
    }
  }
  // a use the printer rewrote to its `finalX` anonymous-class capture prints against its
  // own `Object finalX = x;` local, so the recorded type of the declaration no longer
  // holds (`(finalTime - 8L)` on an Object local does not compile)
  javaIdentifierKeepsDeclaredName(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const declaration = _optionalChain([checker, 'access', _1406 => _1406.getSymbolAtLocation, 'call', _1407 => _1407(node), 'optionalAccess', _1408 => _1408.valueDeclaration]);
    if (declaration === void 0 || declaration.kind !== _typescript2.default.SyntaxKind.VariableDeclaration && !_typescript2.default.isParameter(declaration)) {
      return false;
    }
    return String(node.escapedText) === String(_optionalChain([declaration, 'access', _1409 => _1409.name, 'optionalAccess', _1410 => _1410.escapedText]));
  }
  // the checker type is the plain non-nullable `number` (TypeFlags.Number/NumberLiteral, no
  // alias): `Int`/`Num`/`any` and unions hold undefined at runtime, which the helpers absorb
  javaOperandIsNonNullNumber(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const type = checker.getTypeAtLocation(node);
    return type !== void 0 && type.aliasSymbol === void 0 && (type.flags === _typescript2.default.TypeFlags.Number || type.flags === _typescript2.default.TypeFlags.NumberLiteral);
  }
  // the kind of the native arithmetic this rule prints for `+ - * /`, or undefined when
  // the node keeps the helper. Mirrors printInlineHelperArithmetic operand-for-operand
  // so callers can reason about the printed text of a nested arithmetic operand;
  // `allowDeclaredLocals=false` asks for a kind that does not rely on a retyped local
  // anywhere in the subtree (`+` never consumes one — java-13/14 own Add).
  javaNativeArithmeticKind(node, allowDeclaredLocals = true) {
    const op = _optionalChain([node, 'optionalAccess', _1411 => _1411.operatorToken, 'optionalAccess', _1412 => _1412.kind]);
    const isPlus = op === _typescript2.default.SyntaxKind.PlusToken;
    const isMinus = op === _typescript2.default.SyntaxKind.MinusToken;
    const isMultiply = op === _typescript2.default.SyntaxKind.AsteriskToken;
    const isDivide = op === _typescript2.default.SyntaxKind.SlashToken;
    if (!isPlus && !isMinus && !isMultiply && !isDivide) {
      return void 0;
    }
    if (this.javaScalarFamily(node.left) !== "number" || this.javaScalarFamily(node.right) !== "number") {
      return void 0;
    }
    const childAllows = allowDeclaredLocals && !isPlus;
    const leftKind = this.javaProvableNumericKind(node.left, childAllows);
    const rightKind = this.javaProvableNumericKind(node.right, childAllows);
    return this.javaNativeArithmeticPairKind(isPlus, isMultiply, isDivide, leftKind, rightKind);
  }
  // the native operator kind for a proven pair, or undefined. The helper's branch IS this operator:
  // `/` is always double division; `-` boxes long for long/long and double otherwise; `*` only on
  // long pairs (multiply re-boxes an integral double product as Long); `+` keeps the equal-kind rule.
  javaNativeArithmeticPairKind(isPlus, isMultiply, isDivide, leftKind, rightKind) {
    if (leftKind === void 0 || rightKind === void 0) {
      return void 0;
    }
    if (isDivide) {
      return "double";
    }
    const hasDouble = leftKind === "double" || rightKind === "double";
    if (isMultiply) {
      return hasDouble ? void 0 : "long";
    }
    if (isPlus) {
      return leftKind === rightKind ? leftKind : void 0;
    }
    return hasDouble ? "double" : "long";
  }
  // ---- widened native add (`+` only) ----
  // Helpers.add normalizes Integer to Long, boxes Long for integral operands and Double otherwise
  // (null in -> null out), so native `+` over proven numeric non-null operands gives the same box.
  // `this.milliseconds()` / `this.seconds()`: the hand-written Java declares both `public Long` over
  // a primitive time value, so the box is never null. Only a signature resolving into the base time
  // mixin or the Date.now lib chain qualifies; an unresolved call or venue override keeps the helper.
  javaBaseTimeLongCall(node) {
    if (_optionalChain([node, 'optionalAccess', _1413 => _1413.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return false;
    }
    const callee = node.expression;
    if (_optionalChain([callee, 'optionalAccess', _1414 => _1414.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || callee.expression.kind !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return false;
    }
    const name = _optionalChain([callee, 'access', _1415 => _1415.name, 'optionalAccess', _1416 => _1416.escapedText]);
    if (name !== "milliseconds" && name !== "seconds") {
      return false;
    }
    let declaration;
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      declaration = void 0;
    }
    declaration = _optionalChain([checker, 'access', _1417 => _1417.getResolvedSignature, 'call', _1418 => _1418(node), 'optionalAccess', _1419 => _1419.declaration]);
    if (declaration === void 0) {
      return false;
    }
    const fileName = _nullishCoalesce(_optionalChain([declaration, 'access', _1420 => _1420.getSourceFile, 'optionalCall', _1421 => _1421(), 'optionalAccess', _1422 => _1422.fileName]), () => ( ""));
    return /(^|[\\/])ts[\\/]src[\\/]base[\\/]functions[\\/]time\.ts$/.test(fileName) || /(^|[\\/])lib\.[^\\/]*\.d\.ts$/.test(fileName);
  }
  // `this.parseTimeframe(..)`: the hand-written java BaseExchange declares `public int
  // parseTimeframe(Object)` (java/lib/.../BaseExchange.java:1513) over the primitive-int
  // ts/src/base/functions/misc.ts arrow, so the call is a never-null Java int. A venue's
  javaBaseIntCall(node) {
    if (_optionalChain([node, 'optionalAccess', _1423 => _1423.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return false;
    }
    const callee = node.expression;
    if (_optionalChain([callee, 'optionalAccess', _1424 => _1424.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || callee.expression.kind !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return false;
    }
    if (_optionalChain([callee, 'access', _1425 => _1425.name, 'optionalAccess', _1426 => _1426.escapedText]) !== "parseTimeframe") {
      return false;
    }
    let declaration;
    try {
      declaration = _optionalChain([this, 'access', _1427 => _1427.getChecker, 'call', _1428 => _1428(), 'access', _1429 => _1429.getResolvedSignature, 'call', _1430 => _1430(node), 'optionalAccess', _1431 => _1431.declaration]);
    } catch (e) {
      declaration = void 0;
    }
    if (declaration === void 0) {
      return false;
    }
    const fileName = _nullishCoalesce(_optionalChain([declaration, 'access', _1432 => _1432.getSourceFile, 'optionalCall', _1433 => _1433(), 'optionalAccess', _1434 => _1434.fileName]), () => ( ""));
    return /(^|[\\/])ts[\\/]src[\\/]base[\\/]functions[\\/]misc\.ts$/.test(fileName);
  }
  // `for (var i = <int literal>; ...; i++)`: printForStatement rewrites the emitted
  // `Object i = 0` initializer to `var i = 0`, so javac types the counter int. The
  // counter is widened explicitly by javaPrintWidenedOperand, and no `=`/compound
  javaIntForCounter(node) {
    if (_optionalChain([node, 'optionalAccess', _1435 => _1435.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const symbol = this.getChecker().getSymbolAtLocation(node);
    const declaration = _optionalChain([symbol, 'optionalAccess', _1436 => _1436.valueDeclaration]);
    if (declaration === void 0 || !_typescript2.default.isVariableDeclaration(declaration)) {
      return false;
    }
    const declarationList = declaration.parent;
    if (declarationList === void 0 || !_typescript2.default.isVariableDeclarationList(declarationList) || declarationList.declarations.length !== 1) {
      return false;
    }
    const forStatement = declarationList.parent;
    if (forStatement === void 0 || forStatement.kind !== _typescript2.default.SyntaxKind.ForStatement || forStatement.initializer !== declarationList) {
      return false;
    }
    if (this.javaIntegerLiteralKind(declaration.initializer) !== "int") {
      return false;
    }
    return this.javaCounterHasNoBoxWrite(node, symbol);
  }
  // no `=`/compound assignment anywhere in the enclosing function writes this counter;
  // `++`/`--` keep the primitive int, any other operator would not
  javaCounterHasNoBoxWrite(node, symbol) {
    let scope = node.parent;
    while (scope !== void 0 && !_typescript2.default.isFunctionLike(scope) && scope.kind !== _typescript2.default.SyntaxKind.SourceFile) {
      scope = scope.parent;
    }
    if (scope === void 0) {
      return false;
    }
    let safe = true;
    const visit = (current) => {
      if (!safe || current === void 0) {
        return;
      }
      if (_typescript2.default.isIdentifier(current) && this.getChecker().getSymbolAtLocation(current) === symbol) {
        const parent = current.parent;
        if (parent !== void 0 && _typescript2.default.isBinaryExpression(parent) && parent.left === current && JAVA_ASSIGNMENT_OPERATOR_KINDS.has(parent.operatorToken.kind)) {
          safe = false;
          return;
        }
      }
      _typescript2.default.forEachChild(current, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return safe;
  }
  // `x.length` on a String/List receiver prints `((String)x).length()` /
  // `((java.util.List<?>)x).size()` — a Java int on every path printJavaLength takes
  javaLengthIntRead(node) {
    if (_optionalChain([node, 'optionalAccess', _1437 => _1437.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([node, 'access', _1438 => _1438.name, 'optionalAccess', _1439 => _1439.escapedText]) !== "length") {
      return false;
    }
    return this.javaLengthKind(node.expression) !== void 0;
  }
  // the Java numeric kind of one `+` operand: the literal proofs above plus the
  // non-null base-tier Long accessors, primitive int for-counters and String/List
  // length reads. Undefined keeps the helper.
  javaWidenedNumericKind(node) {
    if (node === void 0) {
      return void 0;
    }
    if (node.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.javaWidenedNumericKind(node.expression);
    }
    const literalKind = this.javaIntegerLiteralKind(node);
    if (literalKind !== void 0) {
      return literalKind;
    }
    if (_typescript2.default.isNumericLiteral(node)) {
      if (/^0[xXbBoO]/.test(node.text)) {
        return void 0;
      }
      return /[.eE]/.test(node.text) ? "double" : void 0;
    }
    if (this.javaBaseTimeLongCall(node)) {
      return "long";
    }
    if (this.javaBaseIntCall(node)) {
      return "int";
    }
    if (this.javaIntForCounter(node)) {
      return "int";
    }
    if (this.javaLengthIntRead(node)) {
      return "int";
    }
    if (node.kind === _typescript2.default.SyntaxKind.Identifier) {
      return this.javaDeclaredNumericLocalKind(node);
    }
    if (node.kind === _typescript2.default.SyntaxKind.BinaryExpression && node.operatorToken.kind === _typescript2.default.SyntaxKind.PlusToken) {
      return this.javaWidenedAddKind(node);
    }
    return this.javaNativeArithmeticKind(node);
  }
  // the kind of a nested native `+` this rule prints, or undefined when it keeps the helper
  javaWidenedAddKind(node) {
    const leftKind = this.javaWidenedNumericKind(node.left);
    const rightKind = this.javaWidenedNumericKind(node.right);
    if (leftKind === void 0 || rightKind === void 0) {
      return void 0;
    }
    return leftKind === "double" || rightKind === "double" ? "double" : "long";
  }
  // the native form of a numeric `+`, or undefined to keep Helpers.add
  printWidenedNativeAdd(left, right, leftText, rightText) {
    const leftKind = this.javaWidenedNumericKind(left);
    const rightKind = this.javaWidenedNumericKind(right);
    if (leftKind === void 0 || rightKind === void 0) {
      return void 0;
    }
    const resultKind = leftKind === "double" || rightKind === "double" ? "double" : "long";
    const leftOperand = this.javaPrintWidenedOperand(leftKind, resultKind, left, leftText);
    const rightOperand = this.javaPrintWidenedOperand(rightKind, resultKind, right, rightText);
    return `(${leftOperand} + ${rightOperand})`;
  }
  // an int operand is widened to long explicitly: `i + 1` would box an Integer where
  // Helpers.add hands back a Long, and an all-int sum wraps where the helper's long
  // does not. Integer literals print long; a Double result needs no widening.
  javaPrintWidenedOperand(kind, resultKind, node, text) {
    if (kind === "double") {
      return text;
    }
    if (kind === "long" || _typescript2.default.isNumericLiteral(node)) {
      return this.javaPrintOperandAsLong(node, text);
    }
    if (resultKind === "long") {
      return `((long) ${text})`;
    }
    return text;
  }
  // true when the receiver's printed Java would be a `cond ? a : b` (a parenthesised
  // conditional): those keep the helper so no added line carries a `?`
  javaSplitTernaryReceiver(node) {
    if (node.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.javaSplitTernaryReceiver(node.expression);
    }
    return node.kind === _typescript2.default.SyntaxKind.ConditionalExpression;
  }
  // `s.split (<literal>)` -> the printer's own TS-array shape around Java's
  // String.split(Pattern.quote(...)): the plain-string receiver makes the helper's
  // String.valueOf/null branch unreachable and the result stays readable as List<Object>.
  javaNativeSplitCall(node, name, parsedArg) {
    if (node === void 0 || name === void 0 || parsedArg === void 0) {
      return void 0;
    }
    const callee = node.expression;
    if (callee === void 0 || callee.kind !== _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      return void 0;
    }
    if (_optionalChain([callee, 'access', _1440 => _1440.name, 'optionalAccess', _1441 => _1441.escapedText]) !== "split" || _optionalChain([node, 'access', _1442 => _1442.arguments, 'optionalAccess', _1443 => _1443.length]) !== 1) {
      return void 0;
    }
    const receiver = callee.expression;
    const separator = node.arguments[0];
    if (receiver === void 0 || !JAVA_SPLIT_RECEIVER_KINDS.has(receiver.kind)) {
      return void 0;
    }
    if (this.javaSplitTernaryReceiver(receiver)) {
      return void 0;
    }
    if (separator.kind !== _typescript2.default.SyntaxKind.StringLiteral && separator.kind !== _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral) {
      return void 0;
    }
    if (this.javaScalarFamily(receiver) !== "string") {
      return void 0;
    }
    return `${this.ARRAY_OPENING_TOKEN}((String)${name}).split(java.util.regex.Pattern.quote(${parsedArg}))${this.ARRAY_CLOSING_TOKEN}`;
  }
  // integer literals print as Java `int`; the helpers normalize Integer to Long before
  // the arithmetic, so native integer arithmetic is emitted in long to keep the boxed
  // result identical
  javaPrintOperandAsLong(node, text) {
    if (!_typescript2.default.isNumericLiteral(node) || /[.eE]/.test(node.text)) {
      return text;
    }
    return /L$/.test(text) ? text : text + "L";
  }
  // the native form of a helper-family binary operator, or undefined to keep the helper
  // `for (var i = <int literal>; ...; i++)` prints a primitive `var` counter (see
  // isJavaPrimitiveForCounter). Only ++/-- may write it: any other assignment in the
  // loop would print a different kind into the same slot (D2), so the proof bails.
  javaProvableCounterInt(node) {
    if (!node || node.kind !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const symbol = this.getChecker().getSymbolAtLocation(node);
    const declaration = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1444 => _1444.valueDeclaration]), () => ( _optionalChain([symbol, 'optionalAccess', _1445 => _1445.declarations, 'optionalAccess', _1446 => _1446[0]])));
    if (!declaration || declaration.kind !== _typescript2.default.SyntaxKind.VariableDeclaration) {
      return false;
    }
    if (this.javaIntegerLiteralKind(declaration.initializer) !== "int") {
      return false;
    }
    const list = declaration.parent;
    const forStatement = _optionalChain([list, 'optionalAccess', _1447 => _1447.parent]);
    if (!forStatement || forStatement.kind !== _typescript2.default.SyntaxKind.ForStatement || forStatement.initializer !== list) {
      return false;
    }
    const incrementor = forStatement.incrementor;
    if (!incrementor || _optionalChain([incrementor, 'access', _1448 => _1448.operand, 'optionalAccess', _1449 => _1449.kind]) !== _typescript2.default.SyntaxKind.Identifier || incrementor.operand.escapedText !== node.escapedText) {
      return false;
    }
    if (incrementor.kind !== _typescript2.default.SyntaxKind.PostfixUnaryExpression && incrementor.kind !== _typescript2.default.SyntaxKind.PrefixUnaryExpression) {
      return false;
    }
    const name = node.escapedText;
    let safe = true;
    const scan = (n) => {
      if (!safe || !n || n === incrementor) {
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.BinaryExpression && JAVA_ASSIGNMENT_OPERATOR_KINDS.has(n.operatorToken.kind) && _optionalChain([n, 'access', _1450 => _1450.left, 'optionalAccess', _1451 => _1451.kind]) === _typescript2.default.SyntaxKind.Identifier && n.left.escapedText === name) {
        safe = false;
        return;
      }
      if ((n.kind === _typescript2.default.SyntaxKind.PostfixUnaryExpression || n.kind === _typescript2.default.SyntaxKind.PrefixUnaryExpression) && _optionalChain([n, 'access', _1452 => _1452.operand, 'optionalAccess', _1453 => _1453.kind]) === _typescript2.default.SyntaxKind.Identifier && n.operand.escapedText === name) {
        return;
      }
      _typescript2.default.forEachChild(n, scan);
    };
    scan(forStatement.statement);
    if (!safe) {
      return false;
    }
    let enclosing = forStatement.parent;
    while (enclosing && !_typescript2.default.isFunctionLike(enclosing)) {
      enclosing = enclosing.parent;
    }
    if (enclosing) {
      const body = _nullishCoalesce(enclosing.body, () => ( enclosing));
      const scanOuter = (n) => {
        if (!safe || !n || n === forStatement || _typescript2.default.isFunctionLike(n)) {
          return;
        }
        if (n.kind === _typescript2.default.SyntaxKind.BinaryExpression && JAVA_ASSIGNMENT_OPERATOR_KINDS.has(n.operatorToken.kind) && _optionalChain([n, 'access', _1454 => _1454.left, 'optionalAccess', _1455 => _1455.kind]) === _typescript2.default.SyntaxKind.Identifier && n.left.escapedText === name) {
          safe = false;
          return;
        }
        _typescript2.default.forEachChild(n, scanOuter);
      };
      scanOuter(body);
    }
    return safe;
  }
  printInlineHelperArithmetic(left, right, leftText, rightText, op) {
    const isPlus = op === _typescript2.default.SyntaxKind.PlusToken || op === _typescript2.default.SyntaxKind.PlusEqualsToken;
    const isMinus = op === _typescript2.default.SyntaxKind.MinusToken || op === _typescript2.default.SyntaxKind.MinusEqualsToken;
    const isMultiply = op === _typescript2.default.SyntaxKind.AsteriskToken;
    const isDivide = op === _typescript2.default.SyntaxKind.SlashToken;
    const isMod = op === _typescript2.default.SyntaxKind.PercentToken;
    if (!isPlus && !isMinus && !isMultiply && !isDivide && !isMod) {
      return void 0;
    }
    const leftFamily = this.javaScalarFamily(left);
    const rightFamily = this.javaScalarFamily(right);
    if (isPlus && this.javaStringConcatIsProvable(left, right, leftFamily, rightFamily)) {
      const concat = `(${leftText} + ${rightText})`;
      return op === _typescript2.default.SyntaxKind.PlusEqualsToken ? `${leftText} = ${concat}` : concat;
    }
    if (op === _typescript2.default.SyntaxKind.PlusEqualsToken || op === _typescript2.default.SyntaxKind.MinusEqualsToken) {
      return void 0;
    }
    if (op === _typescript2.default.SyntaxKind.MinusToken) {
      const anchoredKind = this.javaThisCallNumericKind(left);
      if (anchoredKind === "long" && this.javaProvableNumericKind(right) === "long") {
        return `(${leftText} - ${this.javaPrintOperandAsLong(right, rightText)})`;
      }
    }
    if (leftFamily !== "number" || rightFamily !== "number") {
      return void 0;
    }
    if (op === _typescript2.default.SyntaxKind.PlusToken) {
      return this.printWidenedNativeAdd(left, right, leftText, rightText);
    }
    if (isMod) {
      if (!this.javaProvableNumericDoubleOperand(left) || !this.javaProvableNumericDoubleOperand(right)) {
        return void 0;
      }
      return `(((double) ${leftText}) % ((double) ${rightText}))`;
    }
    const childAllows = !isPlus;
    const leftKind = this.javaProvableNumericKind(left, childAllows);
    const rightKind = this.javaProvableNumericKind(right, childAllows);
    const pairKind = this.javaNativeArithmeticPairKind(isPlus, isMultiply, isDivide, leftKind, rightKind);
    if (pairKind === void 0) {
      return void 0;
    }
    if (isDivide) {
      return `(((double) ${leftText}) / ((double) ${rightText}))`;
    }
    const operator = isPlus ? "+" : isMinus ? "-" : "*";
    const widenedLeft = this.javaPrintArithmeticOperand(leftKind, left, leftText);
    const widenedRight = this.javaPrintArithmeticOperand(rightKind, right, rightText);
    return `(${widenedLeft} ${operator} ${widenedRight})`;
  }
  // an Integer/int operand is widened to long before the operator: Helpers normalizes an
  // Integer to Long first, so the native long arithmetic reproduces both the value (no
  // int-width wrap) and the Long box the helper hands back
  javaPrintArithmeticOperand(kind, node, text) {
    if (kind === "int") {
      return `((long) ${text})`;
    }
    return this.javaPrintOperandAsLong(node, text);
  }
  // Helpers.mathMin/mathMax take Object, tolerate null and return the ORIGINAL box; Math.min/max take
  // primitives, so the native call needs both operands primitive of one family: literals, `for`
  // counters, `.length`/`.size()`, native long arithmetic; the only NaN-free double is a literal.
  javaNativeMathMinMaxOperandKind(node) {
    if (this.javaIntegerLiteralKind(node) !== void 0) {
      return "integral";
    }
    if (this.isJavaPrimitiveForCounter(node)) {
      return "integral";
    }
    if (_typescript2.default.isPropertyAccessExpression(node) && node.name.escapedText === "length" && this.javaLengthKind(node.expression) !== void 0) {
      return "integral";
    }
    if (_typescript2.default.isNumericLiteral(node)) {
      return this.javaProvableNumericKind(node) === "double" ? "double" : void 0;
    }
    return this.javaProvableNumericKind(node) === "long" ? "integral" : void 0;
  }
  // a primitive has no members, so a receiver position (and the printer's cast wrappers) keep the
  // helper - every other position boxes the primitive exactly like the helper's own box (Jackson,
  // isEqual and toString all read an Integer and a Long the same way)
  javaNativeMathMinMaxResultIsPlainValue(node) {
    let parent = node.parent;
    while (parent !== void 0 && _typescript2.default.isParenthesizedExpression(parent)) {
      parent = parent.parent;
    }
    if (parent === void 0) {
      return false;
    }
    if ((_typescript2.default.isPropertyAccessExpression(parent) || _typescript2.default.isElementAccessExpression(parent) || _typescript2.default.isCallExpression(parent)) && parent.expression === node) {
      return false;
    }
    return parent.kind !== _typescript2.default.SyntaxKind.AsExpression && parent.kind !== _typescript2.default.SyntaxKind.TypeAssertionExpression && parent.kind !== _typescript2.default.SyntaxKind.NonNullExpression;
  }
  // the native form of Math.min/Math.max, or undefined to keep the helper
  printNativeMathMinMax(node, left, right, leftText, rightText, name) {
    if (!this.javaNativeMathMinMaxResultIsPlainValue(node)) {
      return void 0;
    }
    const kind = this.javaNativeMathMinMaxOperandKind(left);
    if (kind === void 0 || kind !== this.javaNativeMathMinMaxOperandKind(right)) {
      return void 0;
    }
    return `Math.${name}(${leftText}, ${rightText})`;
  }
  // ---- helper-family inlining: `parseInt/parseFloat/toString/padStart` ----
  // parseInt catches NumberFormatException into null, parseFloat into 0.0, Helpers.toString maps
  // null to null, Helpers.padStart truncates: native form only where the fallback is unreachable.
  // `Helpers.toString(x)` is `x == null ? null : x.toString()`, so `String.valueOf(x)`
  // is exact for every argument that cannot be null. Only a numeric literal or a
  // nested `+ - * /` this rule prints natively qualifies: both are Java primitives.
  javaStringBoxText(node, text) {
    return this.javaProvableNumericKind(node) !== void 0 ? `String.valueOf(${text})` : `Helpers.toString(${text})`;
  }
  // The runtime uses Long.parseLong (parseInt) / Double.parseDouble (parseFloat) inside a catch; a
  // literal the native parser ACCEPTS cannot reach the catch, so the native call cannot change the
  // answer (parseInt additionally needs the value in long range, else the helper answers null).
  javaScalarParseAccepts(callee, text) {
    if (callee === "parseInt") {
      if (!/^[+-]?[0-9]+$/.test(text)) {
        return false;
      }
      const value = BigInt(text.replace(/^\+/, ""));
      return value >= BigInt("-9223372036854775808") && value <= BigInt("9223372036854775807");
    }
    return /^[+-]?([0-9]+(\.[0-9]*)?|\.[0-9]+)([eE][+-]?[0-9]+)?$/.test(text) || /^[+-]?Infinity$/.test(text) || text === "NaN";
  }
  // `parseInt(x)` / `parseFloat(x)` -> the native parse, or undefined to keep the helper
  printNativeScalarParse(node, callee) {
    const args = _optionalChain([node, 'optionalAccess', _1456 => _1456.arguments]);
    if (args === void 0 || args.length !== 1 || !_typescript2.default.isStringLiteral(args[0])) {
      return void 0;
    }
    if (!this.javaScalarParseAccepts(callee, args[0].text)) {
      return void 0;
    }
    const nativeName = callee === "parseInt" ? "Long.parseLong" : "Double.parseDouble";
    return `${nativeName}(${this.printNode(args[0], 0)})`;
  }
  // The printed receiver of a `padStart` this rule can inline: already cast (`((String)x)`) by the
  // printer or the ccxt local-typing pass, or the checker proves a plain TS string, so the accessor
  // cast this rule adds cannot fire.
  javaPadStartReceiverText(receiver, name) {
    if (/^\(+\(String\)/.test(name)) {
      return name;
    }
    if (this.javaScalarFamily(receiver) === "string") {
      return `((String)${name})`;
    }
    return void 0;
  }
  // `x.padStart(n, 'c')` with a non-negative integer literal length and single-char literal pad.
  // Helpers.padStart pads then answers the LAST `n` chars, so the native form keeps both halves:
  // String.format builds the pad from an empty `%<k>s` and the >= arm reproduces the truncation.
  printNativePadStart(node, name) {
    const args = _optionalChain([node, 'optionalAccess', _1457 => _1457.arguments]);
    if (args === void 0 || args.length !== 2 || name === void 0) {
      return void 0;
    }
    if (this.javaIntegerLiteralKind(args[0]) !== "int" || !/^[0-9]+$/.test(args[0].text)) {
      return void 0;
    }
    if (!_typescript2.default.isStringLiteral(args[1]) || args[1].text.length === 0) {
      return void 0;
    }
    const receiver = _optionalChain([node, 'access', _1458 => _1458.expression, 'optionalAccess', _1459 => _1459.expression]);
    if (!this.sideEffectFreeReceiver(receiver)) {
      return void 0;
    }
    const receiverText = this.javaPadStartReceiverText(receiver, name);
    if (receiverText === void 0) {
      return void 0;
    }
    const length = args[0].text;
    const pad = `'${this.javaCharLiteral(args[1].text[0])}'`;
    const lengthCall = `${receiverText}.length()`;
    return `(${lengthCall} >= ${length} ? ${receiverText}.substring(${lengthCall} - ${length}) : String.format("%" + (${length} - ${lengthCall}) + "s", "").replace(' ', ${pad}) + ${receiverText})`;
  }
  // one char literal for the `String.format(...).replace(' ', c)` pad, escaped
  javaCharLiteral(character) {
    if (character === "'" || character === "\\") {
      return `\\${character}`;
    }
    return character;
  }
  // ---- typed locals for native arithmetic ----
  // A local whose initializer prints as native arithmetic holds a String / Long / Double box, so the
  // declaration carries that type; every other use is scanned first (D2) as it changes resolution.
  javaUnwrapParentheses(node) {
    let current = node;
    while (_optionalChain([current, 'optionalAccess', _1460 => _1460.kind]) === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      current = current.expression;
    }
    return current;
  }
  // the Java type of the native arithmetic this printer prints for a `+ - * / +=`
  // node, or undefined when the node keeps the helper (its printed value is an
  // Object box). Mirrors printInlineHelperArithmetic operand-for-operand.
  javaNativeArithmeticType(node) {
    const value = this.javaUnwrapParentheses(node);
    if (_optionalChain([value, 'optionalAccess', _1461 => _1461.kind]) !== _typescript2.default.SyntaxKind.BinaryExpression) {
      return void 0;
    }
    const op = value.operatorToken.kind;
    if (op === _typescript2.default.SyntaxKind.PlusToken && this.javaNativeConcat(value)) {
      return "String";
    }
    if (op === _typescript2.default.SyntaxKind.PlusEqualsToken) {
      const isStringPair = this.javaScalarFamily(value.left) === "string" && this.javaScalarFamily(value.right) === "string" && (this.javaProvableString(value.left) || this.javaProvableString(value.right));
      return isStringPair ? "String" : void 0;
    }
    const kind = this.javaNativeArithmeticKind(value);
    if (kind === "long") {
      return "Long";
    }
    if (kind === "double") {
      return "Double";
    }
    return void 0;
  }
  // the enclosing function-like node: the D2 scan scope of a typed local
  javaEnclosingFunction(node) {
    let current = _optionalChain([node, 'optionalAccess', _1462 => _1462.parent]);
    while (current !== void 0) {
      const kind = current.kind;
      if (kind === _typescript2.default.SyntaxKind.MethodDeclaration || kind === _typescript2.default.SyntaxKind.FunctionDeclaration || kind === _typescript2.default.SyntaxKind.FunctionExpression || kind === _typescript2.default.SyntaxKind.ArrowFunction || kind === _typescript2.default.SyntaxKind.GetAccessor || kind === _typescript2.default.SyntaxKind.SetAccessor || kind === _typescript2.default.SyntaxKind.Constructor) {
        return current;
      }
      current = current.parent;
    }
    return void 0;
  }
  // a reassignment may keep the narrowed declaration only when its printed value is the
  // same Java type (the helper's box stays an Object, so it keeps the box)
  javaArithmeticWriteIsSafe(right, javaType) {
    const value = this.javaUnwrapParentheses(right);
    if (value === void 0) {
      return false;
    }
    if (value.kind === _typescript2.default.SyntaxKind.NullKeyword) {
      return true;
    }
    if (value.kind === _typescript2.default.SyntaxKind.Identifier && value.escapedText === "undefined") {
      return true;
    }
    if (javaType === "String" && (value.kind === _typescript2.default.SyntaxKind.StringLiteral || value.kind === _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral)) {
      return true;
    }
    return this.javaNativeArithmeticType(value) === javaType;
  }
  // the innermost block that scopes a declaration (Java locals live to the end of
  // their block; sibling blocks may reuse the name, nested ones may not)
  javaScopingBlock(node) {
    let current = _optionalChain([node, 'optionalAccess', _1463 => _1463.parent]);
    let last = void 0;
    while (current !== void 0) {
      if (current.kind === _typescript2.default.SyntaxKind.Block || current.kind === _typescript2.default.SyntaxKind.SourceFile) {
        return current;
      }
      last = current;
      current = current.parent;
    }
    return last;
  }
  javaNodeContains(outer, inner) {
    return outer !== void 0 && inner !== void 0 && outer.pos <= inner.pos && inner.end <= outer.end;
  }
  // two same-named bindings may both keep their own printed type only in disjoint
  // blocks: a nested one would already be an illegal Java shadowing of the Object
  // declaration the corpus compiles with
  javaBindingsAreDisjoint(declaration, other) {
    const mine = this.javaScopingBlock(declaration);
    const theirs = this.javaScopingBlock(other);
    if (mine === void 0 || theirs === void 0) {
      return false;
    }
    return !this.javaNodeContains(mine, theirs) && !this.javaNodeContains(theirs, mine);
  }
  // is this occurrence of the local compatible with the narrowed declaration?
  javaArithmeticLocalUseIsSafe(node, declaration, javaType) {
    const sourceName = declaration.name.escapedText;
    const parent = node.parent;
    if (parent === void 0) {
      return false;
    }
    if ((parent.kind === _typescript2.default.SyntaxKind.PropertyAccessExpression || parent.kind === _typescript2.default.SyntaxKind.PropertyAssignment) && parent.name === node) {
      return true;
    }
    if ((parent.kind === _typescript2.default.SyntaxKind.VariableDeclaration || parent.kind === _typescript2.default.SyntaxKind.Parameter || parent.kind === _typescript2.default.SyntaxKind.BindingElement) && parent.name === node) {
      return this.javaBindingsAreDisjoint(declaration, parent);
    }
    if (parent.kind === _typescript2.default.SyntaxKind.PostfixUnaryExpression || parent.kind === _typescript2.default.SyntaxKind.PrefixUnaryExpression) {
      return false;
    }
    if (parent.kind === _typescript2.default.SyntaxKind.SpreadElement || parent.kind === _typescript2.default.SyntaxKind.DeleteExpression) {
      return false;
    }
    if (parent.kind === _typescript2.default.SyntaxKind.ForOfStatement || parent.kind === _typescript2.default.SyntaxKind.ForInStatement) {
      return false;
    }
    if (parent.kind === _typescript2.default.SyntaxKind.TypeOfExpression) {
      return javaType === "String";
    }
    if (parent.kind === _typescript2.default.SyntaxKind.AsExpression || parent.kind === _typescript2.default.SyntaxKind.TypeAssertionExpression) {
      return javaType === "String" && _optionalChain([parent, 'access', _1464 => _1464.type, 'optionalAccess', _1465 => _1465.kind]) === _typescript2.default.SyntaxKind.StringKeyword;
    }
    if (parent.kind === _typescript2.default.SyntaxKind.ConditionalExpression) {
      return javaType === "String";
    }
    if (parent.kind === _typescript2.default.SyntaxKind.ElementAccessExpression && parent.expression === node) {
      const grand = parent.parent;
      if (_optionalChain([grand, 'optionalAccess', _1466 => _1466.kind]) === _typescript2.default.SyntaxKind.DeleteExpression) {
        return false;
      }
      if (_optionalChain([grand, 'optionalAccess', _1467 => _1467.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && grand.left === parent && JAVA_ASSIGNMENT_OPERATOR_KINDS.has(grand.operatorToken.kind)) {
        return false;
      }
    }
    if (parent.kind === _typescript2.default.SyntaxKind.ArrayLiteralExpression) {
      const grand = parent.parent;
      if (_optionalChain([grand, 'optionalAccess', _1468 => _1468.kind]) === _typescript2.default.SyntaxKind.BinaryExpression && grand.left === parent && JAVA_ASSIGNMENT_OPERATOR_KINDS.has(grand.operatorToken.kind)) {
        return false;
      }
    }
    if (parent.kind === _typescript2.default.SyntaxKind.BinaryExpression && parent.left === node) {
      const op = parent.operatorToken.kind;
      if (op === _typescript2.default.SyntaxKind.EqualsToken) {
        return this.javaArithmeticWriteIsSafe(parent.right, javaType);
      }
      if (op === _typescript2.default.SyntaxKind.PlusEqualsToken) {
        return this.javaNativeArithmeticType(parent) === javaType;
      }
      if (JAVA_ASSIGNMENT_OPERATOR_KINDS.has(op)) {
        return false;
      }
      if (op === _typescript2.default.SyntaxKind.PlusToken && javaType === "String") {
        return this.javaNativeArithmeticType(parent) === "String";
      }
    }
    return true;
  }
  // D2: the narrowed declaration needs every later use of the local in the enclosing
  // function to still compile and resolve the way the Object declaration did
  javaArithmeticLocalIsSafeToType(scope, declaration, javaType) {
    if (scope === void 0) {
      return false;
    }
    const sourceName = declaration.name.escapedText;
    let safe = true;
    const visit = (n) => {
      if (!safe) {
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.Identifier && n.escapedText === sourceName && n !== declaration.name) {
        if (!this.javaArithmeticLocalUseIsSafe(n, declaration, javaType)) {
          safe = false;
          return;
        }
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return safe;
  }
  // the Java type a declaration can carry because its initializer prints as native
  // arithmetic, or undefined to keep the Object declaration
  javaArithmeticLocalType(declaration) {
    if (!_typescript2.default.isIdentifier(declaration.name) || declaration.initializer === void 0) {
      return void 0;
    }
    const javaType = this.javaNativeArithmeticType(declaration.initializer);
    if (javaType === void 0) {
      return void 0;
    }
    const scope = this.javaEnclosingFunction(declaration);
    return this.javaArithmeticLocalIsSafeToType(scope, declaration, javaType) ? javaType : void 0;
  }
  // an operand `(double) <text>` can be applied to without changing what Helpers.mod
  // computes: a numeric literal or a native arithmetic node (javaProvableNumericKind,
  // already printed as a java number) or a primitive int loop counter
  javaProvableNumericDoubleOperand(node) {
    return this.javaProvableNumericKind(node) !== void 0 || this.javaProvableCounterInt(node);
  }
  getFinalVarName(varName) {
    if (this.ReservedKeywordsReplacements[varName]) {
      varName = this.ReservedKeywordsReplacements[varName];
    }
    if (varName.startsWith("final")) {
      return varName;
    }
    return `final${this.capitalize(varName)}`;
  }
  getOriginalVarName(name) {
    if (this.ReservedKeywordsReplacements[name]) {
      name = this.ReservedKeywordsReplacements[name];
    }
    return name;
  }
  // Resolves the trio of names used to wrap a reassigned async-method param
  // so the lambda body sees an effectively-final local. We base every name on
  // the keyword-remapped Java identifier — using the raw TS name when remapped
  // (e.g. params -> parameters) makes the wrapper text and the body diverge,
  // because identifier emission already routes through ReservedKeywordsReplacements.
  getAsyncParamWrapperNames(paramName) {
    const javaName = this.getOriginalVarName(paramName);
    return {
      sigName: `${javaName}2`,
      snapName: `${javaName}3`,
      localName: javaName
    };
  }
  isAssignmentOperator(op) {
    return op === _typescript2.default.SyntaxKind.EqualsToken || op === _typescript2.default.SyntaxKind.PlusEqualsToken || op === _typescript2.default.SyntaxKind.MinusEqualsToken || op === _typescript2.default.SyntaxKind.AsteriskEqualsToken || op === _typescript2.default.SyntaxKind.AsteriskAsteriskEqualsToken || op === _typescript2.default.SyntaxKind.SlashEqualsToken || op === _typescript2.default.SyntaxKind.PercentEqualsToken || op === _typescript2.default.SyntaxKind.LessThanLessThanEqualsToken || op === _typescript2.default.SyntaxKind.GreaterThanGreaterThanEqualsToken || op === _typescript2.default.SyntaxKind.GreaterThanGreaterThanGreaterThanEqualsToken || op === _typescript2.default.SyntaxKind.AmpersandEqualsToken || op === _typescript2.default.SyntaxKind.BarEqualsToken || op === _typescript2.default.SyntaxKind.CaretEqualsToken || op === _typescript2.default.SyntaxKind.BarBarEqualsToken || op === _typescript2.default.SyntaxKind.AmpersandAmpersandEqualsToken || op === _typescript2.default.SyntaxKind.QuestionQuestionEqualsToken;
  }
  isIncDecOperator(op) {
    return op === _typescript2.default.SyntaxKind.PlusPlusToken || op === _typescript2.default.SyntaxKind.MinusMinusToken;
  }
  // Walk the function body in source order and assign a version-aware finalName to
  // every identifier that appears inside an object literal property and refers to a
  // variable that's reassigned anywhere in this function. Each reassignment bumps the
  // per-variable version counter; usages of the same (var, version) share one finalName.
  //
  // Naming:
  //   - If only one version of a var is ever used in object literals, use `final<Var>`
  //     (matches prior behavior).
  //   - If multiple versions are used, name them `final<Var>`, `final<Var>_2`,
  //     `final<Var>_3` in source-order of the version they correspond to.
  analyzeFinalVars(fnBody) {
    this.usageToFinalName = /* @__PURE__ */ new WeakMap();
    if (!fnBody)
      return;
    const symbolIdOf = (n) => {
      try {
        const checker = this.getChecker();
        const sym = _optionalChain([checker, 'optionalAccess', _1469 => _1469.getSymbolAtLocation, 'optionalCall', _1470 => _1470(n)]);
        const decl = _nullishCoalesce(_optionalChain([sym, 'optionalAccess', _1471 => _1471.declarations, 'optionalAccess', _1472 => _1472[0]]), () => ( _optionalChain([sym, 'optionalAccess', _1473 => _1473.valueDeclaration])));
        if (decl)
          return `s:${decl.pos}:${decl.end}`;
      } catch (e5) {
      }
      return `n:${n.escapedText}`;
    };
    const reassignedSyms = /* @__PURE__ */ new Set();
    const discoverPotentialReassignments = (node) => {
      if (!node)
        return;
      if (node.kind === _typescript2.default.SyntaxKind.BinaryExpression) {
        if (_optionalChain([node, 'access', _1474 => _1474.left, 'optionalAccess', _1475 => _1475.kind]) === _typescript2.default.SyntaxKind.Identifier) {
          reassignedSyms.add(symbolIdOf(node.left));
        } else if (node.operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken && _optionalChain([node, 'access', _1476 => _1476.left, 'optionalAccess', _1477 => _1477.kind]) === _typescript2.default.SyntaxKind.ArrayLiteralExpression) {
          for (const elem of _nullishCoalesce(node.left.elements, () => ( []))) {
            if (_optionalChain([elem, 'optionalAccess', _1478 => _1478.kind]) === _typescript2.default.SyntaxKind.Identifier) {
              reassignedSyms.add(symbolIdOf(elem));
            }
          }
        }
      }
      if ((node.kind === _typescript2.default.SyntaxKind.PrefixUnaryExpression || node.kind === _typescript2.default.SyntaxKind.PostfixUnaryExpression) && this.isIncDecOperator(node.operator) && _optionalChain([node, 'access', _1479 => _1479.operand, 'optionalAccess', _1480 => _1480.kind]) === _typescript2.default.SyntaxKind.Identifier) {
        reassignedSyms.add(symbolIdOf(node.operand));
      }
      if (node.kind === _typescript2.default.SyntaxKind.Identifier) {
        const name = node.escapedText;
        if (name && name !== "undefined" && !_optionalChain([name, 'access', _1481 => _1481.startsWith, 'optionalCall', _1482 => _1482("null")])) {
          if (this.ReassignedVars[this.getVarKey(node)]) {
            reassignedSyms.add(symbolIdOf(node));
          }
        }
        return;
      }
      _typescript2.default.forEachChild(node, discoverPotentialReassignments);
    };
    discoverPotentialReassignments(fnBody);
    if (reassignedSyms.size === 0)
      return;
    const events = [];
    const visitExprInObjLit = (n) => {
      if (!n)
        return;
      if (n.kind === _typescript2.default.SyntaxKind.Identifier) {
        const name = n.escapedText;
        if (name && name !== "undefined" && !_optionalChain([name, 'access', _1483 => _1483.startsWith, 'optionalCall', _1484 => _1484("null")])) {
          const sym = symbolIdOf(n);
          if (reassignedSyms.has(sym)) {
            events.push({ kind: "use", sym, name, node: n });
          }
        }
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.BinaryExpression) {
        visitExprInObjLit(n.left);
        visitExprInObjLit(n.right);
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
        visitExprInObjLit(n.expression);
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.ConditionalExpression) {
        visitExprInObjLit(n.condition);
        visitExprInObjLit(n.whenTrue);
        visitExprInObjLit(n.whenFalse);
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.CallExpression) {
        _optionalChain([n, 'access', _1485 => _1485.arguments, 'optionalAccess', _1486 => _1486.forEach, 'call', _1487 => _1487(visitExprInObjLit)]);
        if (_optionalChain([n, 'access', _1488 => _1488.expression, 'optionalAccess', _1489 => _1489.kind]) === _typescript2.default.SyntaxKind.PropertyAccessExpression) {
          visitExprInObjLit(n.expression.expression);
        }
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.PrefixUnaryExpression) {
        visitExprInObjLit(n.operand);
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.ElementAccessExpression) {
        let left = n.expression;
        while (_optionalChain([left, 'optionalAccess', _1490 => _1490.kind]) === _typescript2.default.SyntaxKind.ElementAccessExpression) {
          left = left.expression;
        }
        visitExprInObjLit(left);
        visitExprInObjLit(n.argumentExpression);
        return;
      }
      if (n.kind === _typescript2.default.SyntaxKind.ObjectLiteralExpression) {
        _optionalChain([n, 'access', _1491 => _1491.properties, 'optionalAccess', _1492 => _1492.forEach, 'call', _1493 => _1493((p) => {
          if (p.initializer)
            visitExprInObjLit(p.initializer);
        })]);
        return;
      }
      _typescript2.default.forEachChild(n, visitExprInObjLit);
    };
    const walk = (node) => {
      if (!node)
        return;
      if (node.kind === _typescript2.default.SyntaxKind.BinaryExpression && this.isAssignmentOperator(node.operatorToken.kind)) {
        walk(node.right);
        if (_optionalChain([node, 'access', _1494 => _1494.left, 'optionalAccess', _1495 => _1495.kind]) === _typescript2.default.SyntaxKind.Identifier) {
          const sym = symbolIdOf(node.left);
          if (reassignedSyms.has(sym)) {
            events.push({ kind: "reassign", sym });
          }
        } else {
          walk(node.left);
        }
        return;
      }
      if ((node.kind === _typescript2.default.SyntaxKind.PrefixUnaryExpression || node.kind === _typescript2.default.SyntaxKind.PostfixUnaryExpression) && this.isIncDecOperator(node.operator) && _optionalChain([node, 'access', _1496 => _1496.operand, 'optionalAccess', _1497 => _1497.kind]) === _typescript2.default.SyntaxKind.Identifier) {
        const sym = symbolIdOf(node.operand);
        if (reassignedSyms.has(sym)) {
          events.push({ kind: "reassign", sym });
        }
        return;
      }
      if (node.kind === _typescript2.default.SyntaxKind.ObjectLiteralExpression) {
        _optionalChain([node, 'access', _1498 => _1498.properties, 'optionalAccess', _1499 => _1499.forEach, 'call', _1500 => _1500((prop) => {
          if (prop.initializer) {
            visitExprInObjLit(prop.initializer);
            walk(prop.initializer);
          }
        })]);
        return;
      }
      const walkBlockAndBump = (subNode) => {
        const usedInBlock = /* @__PURE__ */ new Set();
        if (!subNode)
          return usedInBlock;
        const eventsBefore = events.length;
        walk(subNode);
        for (let i = eventsBefore; i < events.length; i++) {
          const e = events[i];
          if (e.kind === "use")
            usedInBlock.add(e.sym);
        }
        for (const sym of usedInBlock) {
          events.push({ kind: "reassign", sym });
        }
        return usedInBlock;
      };
      if (node.kind === _typescript2.default.SyntaxKind.IfStatement) {
        walk(node.expression);
        walkBlockAndBump(node.thenStatement);
        walkBlockAndBump(node.elseStatement);
        return;
      }
      if (node.kind === _typescript2.default.SyntaxKind.TryStatement) {
        walkBlockAndBump(node.tryBlock);
        walkBlockAndBump(_optionalChain([node, 'access', _1501 => _1501.catchClause, 'optionalAccess', _1502 => _1502.block]));
        walkBlockAndBump(node.finallyBlock);
        return;
      }
      _typescript2.default.forEachChild(node, walk);
    };
    walk(fnBody);
    const counters = /* @__PURE__ */ new Map();
    const perSym = /* @__PURE__ */ new Map();
    for (const e of events) {
      if (e.kind === "reassign") {
        counters.set(e.sym, (_nullishCoalesce(counters.get(e.sym), () => ( 0))) + 1);
      } else {
        const v = _nullishCoalesce(counters.get(e.sym), () => ( 0));
        if (!perSym.has(e.sym))
          perSym.set(e.sym, { name: e.name, byVer: /* @__PURE__ */ new Map() });
        const entry = perSym.get(e.sym);
        if (!entry.byVer.has(v))
          entry.byVer.set(v, []);
        entry.byVer.get(v).push(e.node);
      }
    }
    for (const { name, byVer } of perSym.values()) {
      const versions = [...byVer.keys()].sort((a, b) => a - b);
      const baseName = this.getFinalVarName(name);
      if (versions.length === 1) {
        for (const n of byVer.get(versions[0])) {
          this.usageToFinalName.set(n, baseName);
        }
      } else {
        versions.forEach((v, idx) => {
          const finalName = idx === 0 ? baseName : `${baseName}_${idx + 1}`;
          for (const n of byVer.get(v)) {
            this.usageToFinalName.set(n, finalName);
          }
        });
      }
    }
  }
  // Undo every in-place identifier rewrite made during the current emit, newest
  // first so repeated rewrites of one node unwind to the original value.
  restoreFinalVarMutations() {
    for (let i = this.finalVarMutations.length - 1; i >= 0; i--) {
      const mutation = this.finalVarMutations[i];
      mutation.node.escapedText = mutation.escapedText;
      if (mutation.ownGetFullText) {
        mutation.node.getFullText = mutation.getFullText;
      } else {
        delete mutation.node.getFullText;
      }
    }
    this.finalVarMutations = [];
  }
  printNode(node, identation = 0) {
    if (node && node.kind === _typescript2.default.SyntaxKind.SourceFile) {
      this.restoreFinalVarMutations();
      this.ReassignedVars = {};
      this.varListFromObjectLiterals = {};
      this.usageToFinalName = /* @__PURE__ */ new WeakMap();
      this.finalVarScopeStack = [];
      try {
        return super.printNode(node, identation);
      } finally {
        this.restoreFinalVarMutations();
      }
    }
    return super.printNode(node, identation);
  }
  // true when a property value reads a local the body reassigns (or the analyzer saw
  // reassigned ahead): a double-brace anonymous class could not capture it
  objectLiteralCapturesReassigned(node) {
    return this.objectLiteralCapturedKeys(node).length > 0;
  }
  // ReassignedVars keys of the reassigned locals a literal's property values read
  objectLiteralCapturedKeys(node) {
    const keys = [];
    const walk = (n) => {
      if (!n)
        return;
      if (n.kind === _typescript2.default.SyntaxKind.Identifier) {
        const name = n.escapedText;
        const key = this.getVarKey(n);
        if (name && name !== "undefined" && !name.startsWith("null") && (this.usageToFinalName.has(n) || this.ReassignedVars[key])) {
          keys.push(key);
        }
        return;
      }
      _typescript2.default.forEachChild(n, walk);
    };
    for (const prop of node.properties) {
      walk(prop.initializer);
    }
    return keys;
  }
  printVariableDeclarationList(node, identation) {
    const declaration = node.declarations[0];
    if (this.removeVariableDeclarationForFunctionExpression && _optionalChain([declaration, 'optionalAccess', _1503 => _1503.initializer]) && _typescript2.default.isFunctionExpression(declaration.initializer)) {
      return this.printNode(declaration.initializer, identation).trimEnd();
    }
    if (_optionalChain([declaration, 'optionalAccess', _1504 => _1504.name, 'access', _1505 => _1505.kind]) === _typescript2.default.SyntaxKind.ArrayBindingPattern) {
      const arrayBindingPattern = declaration.name;
      const arrayBindingPatternElements = arrayBindingPattern.elements;
      const parsedArrayBindingElements = arrayBindingPatternElements.map(
        (e) => this.printNode(e.name, 0)
      );
      const syntheticName = parsedArrayBindingElements.join("") + "Variable";
      let arrayBindingStatement = `${this.getIden(identation)}var ${syntheticName} = ${this.printNode(
        declaration.initializer,
        0
      )};
`;
      parsedArrayBindingElements.forEach((e, index) => {
        const statement = this.getIden(identation) + `var ${e} = ((java.util.List<Object>) ${syntheticName}).get(${index})`;
        if (index < parsedArrayBindingElements.length - 1) {
          arrayBindingStatement += statement + ";\n";
        } else {
          arrayBindingStatement += statement;
        }
      });
      return arrayBindingStatement;
    }
    const isNew = _optionalChain([declaration, 'optionalAccess', _1506 => _1506.initializer]) && declaration.initializer.kind === _typescript2.default.SyntaxKind.NewExpression;
    let varToken = isNew ? "var " : this.VAR_TOKEN + " ";
    if (!isNew) {
      const arithmeticType = this.javaArithmeticLocalType(declaration);
      if (arithmeticType !== void 0) {
        varToken = arithmeticType + " ";
      }
    }
    if (!declaration.initializer) {
      return this.getIden(identation) + "Object " + this.printNode(declaration.name) + " = " + this.UNDEFINED_TOKEN;
    }
    const parsedValue = this.printNode(declaration.initializer, identation).trimStart();
    if (parsedValue === this.UNDEFINED_TOKEN) {
      let specificVarToken = "Object";
      if (this.INFER_VAR_TYPE) {
        const variableType = this.getChecker().typeToString(
          this.getChecker().getTypeAtLocation(declaration)
        );
        if (this.VariableTypeReplacements[variableType]) {
          specificVarToken = this.VariableTypeReplacements[variableType];
        }
      }
      return this.getIden(identation) + specificVarToken + " " + this.printNode(declaration.name) + " = " + parsedValue;
    }
    return this.getIden(identation) + varToken + this.printNode(declaration.name) + " = " + parsedValue;
  }
  printThisKeyword(node, identation) {
    let current = _optionalChain([node, 'optionalAccess', _1507 => _1507.parent]);
    while (current) {
      if (current.kind === _typescript2.default.SyntaxKind.PropertyAssignment && !this.builderObjectLiterals.has(current.parent)) {
        const className = this.currentClassName;
        return `${this.capitalize(className)}.this`;
      }
      current = _optionalChain([current, 'optionalAccess', _1508 => _1508.parent]);
    }
    return this.THIS_TOKEN;
  }
  transformPropertyAcessExpressionIfNeeded(node) {
    const expression = node.expression;
    const leftSide = this.printNode(expression, 0);
    const rightSide = node.name.escapedText;
    let rawExpression = void 0;
    switch (rightSide) {
      case "length": {
        const type = this.getChecker().getTypeAtLocation(
          expression
        );
        this.warnIfAnyType(node, type.flags, leftSide, "length");
        rawExpression = this.printJavaLength(expression, leftSide);
        break;
      }
      case "push":
        rawExpression = `((java.util.List<Object>)${leftSide}).add`;
        break;
    }
    return rawExpression;
  }
  printCustomDefaultValueIfNeeded(node) {
    if (_typescript2.default.isArrayLiteralExpression(node) || _typescript2.default.isObjectLiteralExpression(node) || _typescript2.default.isStringLiteral(node) || _typescript2.default.isBooleanLiteral(node)) {
      return this.UNDEFINED_TOKEN;
    }
    if (_typescript2.default.isNumericLiteral(node)) {
      return this.UNDEFINED_TOKEN;
    }
    if (_optionalChain([node, 'optionalAccess', _1509 => _1509.escapedText]) === "undefined" && _optionalChain([this, 'access', _1510 => _1510.getChecker, 'call', _1511 => _1511(), 'access', _1512 => _1512.getTypeAtLocation, 'call', _1513 => _1513(_optionalChain([node, 'optionalAccess', _1514 => _1514.parent])), 'optionalAccess', _1515 => _1515.flags]) === _typescript2.default.TypeFlags.Number) {
      return this.UNDEFINED_TOKEN;
    }
    return void 0;
  }
  // Unpacks one optional parameter. Native array access replaces Helpers.getArg
  // when the initializer is a pure literal; the null check keeps the helper's
  // contract that a null varargs array reads like an empty one.
  // The expression half is shared with the untyped front, which passes the same value to the
  // typed core as an argument instead of binding a local.
  printOptionalArgExpression(index, initializer) {
    const defaultValue = this.printNode(initializer, 0);
    if (!this.isPureInitializer(initializer)) {
      return `Helpers.getArg(optionalArgs, ${index}, ${defaultValue})`;
    }
    return `optionalArgs != null && optionalArgs.length > ${index} ? optionalArgs[${index}] : ${defaultValue}`;
  }
  printOptionalArgInit(paramName, index, initializer) {
    return `Object ${paramName} = ${this.printOptionalArgExpression(index, initializer)};`;
  }
  // Pure = evaluating the initializer has no effect and cannot throw, so
  // skipping it when the argument was supplied cannot change behavior.
  isPureInitializer(node) {
    switch (_optionalChain([node, 'optionalAccess', _1516 => _1516.kind])) {
      case _typescript2.default.SyntaxKind.NullKeyword:
      case _typescript2.default.SyntaxKind.TrueKeyword:
      case _typescript2.default.SyntaxKind.FalseKeyword:
      case _typescript2.default.SyntaxKind.StringLiteral:
      case _typescript2.default.SyntaxKind.NoSubstitutionTemplateLiteral:
      case _typescript2.default.SyntaxKind.NumericLiteral:
        return true;
      case _typescript2.default.SyntaxKind.Identifier:
        return node.escapedText === "undefined";
      case _typescript2.default.SyntaxKind.ArrayLiteralExpression:
        return node.elements.every((element) => this.isPureInitializer(element));
      case _typescript2.default.SyntaxKind.ObjectLiteralExpression:
        return node.properties.every((property) => _typescript2.default.isPropertyAssignment(property) && this.isPureInitializer(property.initializer));
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
      case _typescript2.default.SyntaxKind.AsExpression:
      case _typescript2.default.SyntaxKind.TypeAssertionExpression:
      case _typescript2.default.SyntaxKind.NonNullExpression:
        return this.isPureInitializer(node.expression);
      default:
        return false;
    }
  }
  printFunctionBody(node, identation) {
    const savedVarList = this.varListFromObjectLiterals;
    const savedUsageToFinalName = this.usageToFinalName;
    const savedScopeStack = this.finalVarScopeStack;
    this.varListFromObjectLiterals = {};
    this.analyzeFinalVars(node.body);
    this.finalVarScopeStack = [/* @__PURE__ */ new Set()];
    const funcParams = _nullishCoalesce(node.parameters, () => ( []));
    const bodyStatements = node.body.statements;
    const isAsync = this.isAsyncFunction(node);
    const splitCore = _typescript2.default.isMethodDeclaration(node);
    const initParams = [];
    const processedParts = [];
    try {
      for (let i = 0; i < bodyStatements.length; i++) {
        processedParts.push(this.printNode(bodyStatements[i], identation + 1));
      }
    } finally {
      this.varListFromObjectLiterals = savedVarList;
      this.usageToFinalName = savedUsageToFinalName;
      this.finalVarScopeStack = savedScopeStack;
    }
    let firstStatement = processedParts[0] || "";
    const remainingString = processedParts.slice(1).join("\n");
    let offSetIndex = 0;
    funcParams.forEach((param, i) => {
      const initializer = param.initializer;
      if (initializer) {
        if (splitCore) {
          if (!this.isPureInitializer(initializer)) {
            const name = this.printNode(param.name, 0);
            initParams.push(`if (${name} == null) { ${name} = ${this.printNode(initializer, 0)}; }`);
          }
          return;
        }
        const index = i + offSetIndex;
        const paramName = this.printNode(param.name, 0);
        initParams.push(this.printOptionalArgInit(paramName, index, initializer));
      } else {
        offSetIndex--;
      }
    });
    if (initParams.length > 0) {
      const defaultInitializers = initParams.map((l) => this.getIden(identation + 1) + l).join("\n") + "\n";
      const bodyParts = firstStatement.split("\n");
      const commentPart = bodyParts.filter((line) => this.isComment(line));
      const isComment = commentPart.length > 0;
      if (isComment) {
        const commentPartString = commentPart.map((c) => this.getIden(identation + 1) + c.trim()).join("\n");
        const firstStmNoComment = bodyParts.filter((line) => !this.isComment(line)).join("\n");
        firstStatement = commentPartString + "\n" + defaultInitializers + firstStmNoComment;
      } else {
        firstStatement = defaultInitializers + firstStatement;
      }
    }
    const blockOpen = this.getBlockOpen(identation);
    const blockClose = this.getBlockClose(identation);
    firstStatement = remainingString.length > 0 ? firstStatement + "\n" : firstStatement;
    if (isAsync && _typescript2.default.isMethodDeclaration(node) && this.javaReassignsParameter(node)) {
      const where = `${node.getSourceFile().fileName}:${node.name.escapedText}`;
      this.javaReassigningMethods.push(where);
      const message = `[Java] async method reassigns a parameter (not effectively final, the TS source must use a new local): ${where}`;
      if (this.javaStrictEffectivelyFinal) {
        throw new Error(message);
      }
      Logger.warning(message);
    }
    if (isAsync) {
      const finalWrapperVars = _typescript2.default.isMethodDeclaration(node) ? "\n" : this.printFinalOutsideMethodVariableWrappersIfAny(node, identation) + "\n";
      const insideWrappers = _typescript2.default.isMethodDeclaration(node) ? "\n" : this.printInsideMethodVariableWrappersIfAny(node, identation + 1) + "\n";
      const body = (firstStatement + remainingString).split("\n").map((line) => this.getIden(identation) + line).join("\n");
      const lastStatement = bodyStatements.length > 1 ? bodyStatements[bodyStatements.length - 1] : bodyStatements.length > 0 ? bodyStatements[0] : void 0;
      const lastStmtIsReturn = lastStatement && (_typescript2.default.isReturnStatement(lastStatement) || this.allBranchesTerminate(lastStatement));
      const returnNull = lastStmtIsReturn ? "" : this.getIden(identation + 2) + "return null;\n";
      const supplier = this.asyncSupplier || "java.util.concurrent.CompletableFuture.supplyAsync";
      const executorArg = !this.asyncSupplier && this.asyncExecutor ? `, ${this.asyncExecutor}` : "";
      const asyncBody = this.getIden(identation + 1) + `return ${supplier}(() -> {
` + insideWrappers + body + "\n" + returnNull + this.getIden(identation + 1) + `}${executorArg});
`;
      return blockOpen + finalWrapperVars + asyncBody + blockClose;
    }
    return blockOpen + firstStatement + remainingString + blockClose;
  }
  printBlock(node, identation, chainBlock = false) {
    const managed = this.finalVarScopeStack.length > 0;
    if (managed)
      this.finalVarScopeStack.push(/* @__PURE__ */ new Set());
    try {
      return super.printBlock(node, identation, chainBlock);
    } finally {
      if (managed)
        this.finalVarScopeStack.pop();
    }
  }
  printInstanceOfExpression(node, identation) {
    const right = node.right.escapedText;
    const left = this.printNode(node.left, 0);
    return this.getIden(identation) + `Helpers.isInstance(${left}, ${right}.class)`;
  }
  printAwaitExpression(node, identation) {
    const expression = this.printNode(node.expression, identation);
    return `(${expression}).join()`;
  }
  printAsExpression(node, identation) {
    const type = node.type;
    if (type.kind === _typescript2.default.SyntaxKind.AnyKeyword) {
      return `((${this.VariableTypeReplacements["object"]})${this.printNode(
        node.expression,
        identation
      )})`;
    }
    if (type.kind === _typescript2.default.SyntaxKind.StringKeyword) {
      return `((String)${this.printNode(node.expression, identation)})`;
    }
    if (type.kind === _typescript2.default.SyntaxKind.ArrayType) {
      if (type.elementType.kind === _typescript2.default.SyntaxKind.AnyKeyword) {
        return `(java.util.List<Object>)(${this.printNode(
          node.expression,
          identation
        )})`;
      }
      if (type.elementType.kind === _typescript2.default.SyntaxKind.StringKeyword) {
        return `(java.util.List<String>)(${this.printNode(
          node.expression,
          identation
        )})`;
      }
    }
    return this.printNode(node.expression, identation);
  }
  // every Java parameter is `Object` (base printParameterType), except the ones whose TS
  // annotation names a native-carriable alias (javaNativeParameterType)
  printParameterType(node) {
    const native = this.javaNativeParameterType(node);
    if (native !== void 0) {
      return native;
    }
    return super.printParameterType(node);
  }
  printParameter(node, defaultValue = true) {
    const name = this.printNode(node.name, 0);
    const initializer = node.initializer;
    let type = this.printParameterType(node) || "";
    if (defaultValue) {
      if (initializer) {
        const customDefaultValue = this.printCustomDefaultValueIfNeeded(initializer);
        const def = customDefaultValue ? customDefaultValue : this.printNode(initializer, 0);
        type = def === "null" && type !== "Object" ? type + " " : type + " ";
        return type + name + this.SPACE_DEFAULT_PARAM + "=" + this.SPACE_DEFAULT_PARAM + def;
      }
      return type + " " + name;
    }
    return name;
  }
  // the typed core signature: every parameter prints its Java type, the default-valued ones
  // included (a Java signature cannot carry a default - the front supplies it)
  printCoreMethodParameters(node) {
    return node.parameters.map((param) => param.initializer !== void 0 ? `${this.javaOptionalParameterJavaType(param)} ${this.printNode(param.name, 0)}` : this.printParameter(param)).join(", ");
  }
  // the async method body writes one of its parameters, or a non-pure default must be applied
  // by an `if (x == null) { x = init; }` inside the lambda (both break effective finality)
  javaReassignsParameter(node) {
    const params = _nullishCoalesce(node.parameters, () => ( []));
    if (params.some((p) => p.initializer !== void 0 && !this.isPureInitializer(p.initializer) && this.isAsyncFunction(node))) {
      return true;
    }
    const symbols = /* @__PURE__ */ new Set();
    params.forEach((p) => {
      const symbol = this.javaSymbolOf(p.name);
      if (symbol !== void 0) {
        symbols.add(symbol);
      }
    });
    return symbols.size > 0 && node.body !== void 0 && this.javaWritesSymbol(node.body, symbols);
  }
  javaSymbolOf(node) {
    return this.getChecker().getSymbolAtLocation(node);
  }
  // syntactic walk: assignment targets, ++/--, destructuring targets and for-in/of expression heads
  javaWritesSymbol(node, symbols) {
    if (_typescript2.default.isBinaryExpression(node)) {
      const op = node.operatorToken.kind;
      if (op >= _typescript2.default.SyntaxKind.FirstAssignment && op <= _typescript2.default.SyntaxKind.LastAssignment && this.javaTargetWritesSymbol(node.left, symbols, op === _typescript2.default.SyntaxKind.EqualsToken)) {
        return true;
      }
    } else if ((_typescript2.default.isPrefixUnaryExpression(node) || _typescript2.default.isPostfixUnaryExpression(node)) && (node.operator === _typescript2.default.SyntaxKind.PlusPlusToken || node.operator === _typescript2.default.SyntaxKind.MinusMinusToken) && this.javaTargetWritesSymbol(node.operand, symbols, false)) {
      return true;
    } else if ((_typescript2.default.isForInStatement(node) || _typescript2.default.isForOfStatement(node)) && !_typescript2.default.isVariableDeclarationList(node.initializer) && this.javaTargetWritesSymbol(node.initializer, symbols, true)) {
      return true;
    }
    return _typescript2.default.forEachChild(node, (child) => this.javaWritesSymbol(child, symbols) || void 0) === true;
  }
  javaTargetWritesSymbol(target, symbols, destructuring) {
    if (_typescript2.default.isParenthesizedExpression(target) || _typescript2.default.isNonNullExpression(target) || _typescript2.default.isAsExpression(target)) {
      return this.javaTargetWritesSymbol(target.expression, symbols, destructuring);
    }
    if (_typescript2.default.isIdentifier(target)) {
      const symbol = this.javaSymbolOf(target);
      return symbol !== void 0 && symbols.has(symbol);
    }
    if (!destructuring) {
      return false;
    }
    if (_typescript2.default.isArrayLiteralExpression(target)) {
      return target.elements.some((e) => this.javaTargetWritesSymbol(_typescript2.default.isSpreadElement(e) ? e.expression : e, symbols, true));
    }
    if (_typescript2.default.isObjectLiteralExpression(target)) {
      return target.properties.some((p) => {
        if (_typescript2.default.isShorthandPropertyAssignment(p)) {
          return this.javaTargetWritesSymbol(p.name, symbols, true);
        }
        if (_typescript2.default.isPropertyAssignment(p)) {
          return this.javaTargetWritesSymbol(p.initializer, symbols, true);
        }
        return _typescript2.default.isSpreadAssignment(p) && this.javaTargetWritesSymbol(p.expression, symbols, true);
      });
    }
    if (_typescript2.default.isBinaryExpression(target) && target.operatorToken.kind === _typescript2.default.SyntaxKind.EqualsToken) {
      return this.javaTargetWritesSymbol(target.left, symbols, true);
    }
    return false;
  }
  printMethodParameters(node) {
    const isAsyncMethod = this.isAsyncFunction(node);
    const params = node.parameters.map((param) => {
      const isReassignedVar = this.ReassignedVars[this.getVarKey(param)];
      let printedParam = this.printParameter(param);
      if (isAsyncMethod && isReassignedVar) {
        const paramName = param.name.escapedText;
        const { localName, sigName } = this.getAsyncParamWrapperNames(paramName);
        printedParam = printedParam.replace(localName, sigName);
      }
      return printedParam;
    });
    const hasOptionalParameter = node.parameters.some((p) => p.initializer !== void 0 || p.questionToken !== void 0);
    if (!hasOptionalParameter) {
      return params.join(", ");
    }
    const paramsWithOptional = params.filter((param) => param.indexOf("=") === -1);
    paramsWithOptional.push("Object... optionalArgs");
    return paramsWithOptional.join(", ");
  }
  printArrayLiteralExpression(node) {
    const elements = node.elements.map((e) => this.printNode(e)).join(", ");
    return `${this.ARRAY_OPENING_TOKEN}${elements}${this.ARRAY_CLOSING_TOKEN}`;
  }
  printFinalOutsideMethodVariableWrappersIfAny(node, identation) {
    const parameters = _optionalChain([node, 'optionalAccess', _1517 => _1517.parameters]);
    const finalVarWrappers = [];
    if (parameters) {
      const isAsyncMethod = this.isAsyncFunction(node);
      const splitCore = this.hasDefaultedTail(node);
      parameters.forEach((param) => {
        const isOptionalParam = param.initializer !== void 0 || param.questionToken !== void 0;
        if (!isOptionalParam || splitCore) {
          const isReassignedVar = this.ReassignedVars[this.getVarKey(param)];
          if (isAsyncMethod && isReassignedVar) {
            const paramName = param.name.escapedText;
            const { sigName, snapName } = this.getAsyncParamWrapperNames(paramName);
            const snapType = isOptionalParam && param.initializer !== void 0 ? this.javaOptionalParameterJavaType(param) : "Object";
            finalVarWrappers.push(this.getIden(identation + 1) + `final ${snapType} ${snapName} = ${sigName};`);
          }
        }
      });
    }
    return finalVarWrappers.join("\n");
  }
  printInsideMethodVariableWrappersIfAny(node, identation) {
    const parameters = _optionalChain([node, 'optionalAccess', _1518 => _1518.parameters]);
    const finalVarWrappers = [];
    if (parameters) {
      const isAsyncMethod = this.isAsyncFunction(node);
      const splitCore = this.hasDefaultedTail(node);
      parameters.forEach((param) => {
        const isOptionalParam = param.initializer !== void 0 || param.questionToken !== void 0;
        if (!isOptionalParam || splitCore) {
          const isReassignedVar = this.ReassignedVars[this.getVarKey(param)];
          if (isAsyncMethod && isReassignedVar) {
            const paramName = param.name.escapedText;
            const { localName, snapName } = this.getAsyncParamWrapperNames(paramName);
            const localType = _nullishCoalesce(this.javaAsyncParameterLocalType(param), () => ( "Object"));
            finalVarWrappers.push(this.getIden(identation + 1) + `${localType} ${localName} = ${snapName};`);
          }
        }
      });
    }
    return finalVarWrappers.join("\n");
  }
  printMethodDeclaration(node, identation) {
    const funcBody = this.printFunctionBody(node, identation);
    let methodDef = this.printMethodDefinition(node, identation, (n) => this.printCoreMethodParameters(n));
    methodDef += funcBody;
    methodDef += this.printOverrideBridges(node, identation);
    return methodDef;
  }
  printMethodDefinition(node, identation, paramsPrinter = void 0) {
    let name = node.name.escapedText;
    name = this.transformMethodNameIfNeeded(name);
    let returnType = this.printFunctionType(node);
    if (returnType === "java.util.concurrent.CompletableFuture") {
      returnType = "java.util.concurrent.CompletableFuture<Object>";
    }
    if (returnType === this.DEFAULT_RETURN_TYPE) {
      const native = this.javaNativeReturnType(node);
      if (native !== void 0) {
        returnType = native;
      }
    }
    const defaultAccess = this.METHOD_DEFAULT_ACCESS ? this.METHOD_DEFAULT_ACCESS + " " : "";
    const modifiers = defaultAccess;
    let parsedArgs = void 0;
    parsedArgs = parsedArgs ? parsedArgs : paramsPrinter ? paramsPrinter(node) : this.printMethodParameters(node);
    returnType = returnType ? returnType + " " : returnType;
    const methodToken = this.METHOD_TOKEN ? this.METHOD_TOKEN + " " : "";
    const signature = this.getIden(identation) + modifiers + returnType + methodToken + name + "(" + parsedArgs + ")";
    return this.printNodeCommentsIfAny(node, identation, signature);
  }
  // `Array.isArray(x)` prints `(x instanceof java.util.List)`: false for null, true for a List, same
  // as the helper for every Java-object operand. The helper stays where the operand prints as a Java
  // array (rest parameter: `getClass().isArray()` branch) or a final class not convertible to List.
  printArrayIsArrayCall(node, _identation, parsedArg = void 0) {
    const native = this.printNativeArrayIsArray(node, parsedArg);
    return native === void 0 ? `Helpers.isArray(${parsedArg})` : native;
  }
  printNativeArrayIsArray(node, parsedArg) {
    const operand = _optionalChain([node, 'optionalAccess', _1519 => _1519.arguments, 'optionalAccess', _1520 => _1520[0]]);
    if (operand === void 0 || parsedArg === void 0) {
      return void 0;
    }
    if (node.parent !== void 0 && _typescript2.default.isExpressionStatement(node.parent)) {
      return void 0;
    }
    if (_typescript2.default.isArrayLiteralExpression(operand)) {
      return this.javaArrayLiteralDropsNothing(operand) ? "true" : void 0;
    }
    if (!this.javaPrimaryIsArrayOperand(operand)) {
      return void 0;
    }
    const type = this.javaOperandType(operand);
    if (operand.kind === _typescript2.default.SyntaxKind.Identifier && this.javaNonArrayType(type)) {
      return "false";
    }
    if (this.isVarargsArrayReference(operand)) {
      return void 0;
    }
    if (this.javaScalarType(type)) {
      return void 0;
    }
    return `(${parsedArg} instanceof java.util.List)`;
  }
  // Operand shapes whose print is a primary expression: `instanceof` binds tighter than the
  // low-precedence operators, so a ternary/binary operand would re-parse, and a constructor
  // or cast print can be a final Java class on which `instanceof List` is not convertible.
  javaPrimaryIsArrayOperand(node) {
    const kind = node.kind;
    return kind === _typescript2.default.SyntaxKind.Identifier || kind === _typescript2.default.SyntaxKind.PropertyAccessExpression || kind === _typescript2.default.SyntaxKind.ElementAccessExpression || kind === _typescript2.default.SyntaxKind.CallExpression;
  }
  javaOperandType(operand) {
    return _optionalChain([this, 'access', _1521 => _1521.checkerOrUndefined, 'call', _1522 => _1522(), 'optionalAccess', _1523 => _1523.getTypeAtLocation, 'call', _1524 => _1524(operand)]);
  }
  // literals and identifiers have nothing an array-literal wrapper could skip by dropping
  javaArrayLiteralDropsNothing(node, depth = 0) {
    if (depth > 4) {
      return false;
    }
    return node.elements.every((element) => _typescript2.default.isStringLiteral(element) || _typescript2.default.isNumericLiteral(element) || element.kind === _typescript2.default.SyntaxKind.TrueKeyword || element.kind === _typescript2.default.SyntaxKind.FalseKeyword || element.kind === _typescript2.default.SyntaxKind.NullKeyword || element.kind === _typescript2.default.SyntaxKind.Identifier || _typescript2.default.isArrayLiteralExpression(element) && this.javaArrayLiteralDropsNothing(element, depth + 1));
  }
  // types whose Java print is a scalar final class (`instanceof java.util.List` is not
  // convertible on them): the scalar family, and unions made only of scalars/nulls
  javaScalarType(type, depth = 0) {
    if (type === void 0 || type === null || depth > 3) {
      return false;
    }
    const flags = type.flags;
    if (flags & _typescript2.default.TypeFlags.Union) {
      const parts = _nullishCoalesce(type.types, () => ( []));
      return parts.length > 0 && parts.every((part) => this.javaScalarType(part, depth + 1));
    }
    return (flags & JAVA_SCALAR_TYPE_FLAGS) !== 0;
  }
  // types that provably never hold a List: every scalar (Java String/Long/Double/Boolean
  // answer false) and the nullish types (JS Array.isArray(null) is false)
  javaNonArrayType(type, depth = 0) {
    if (type === void 0 || type === null || depth > 3) {
      return false;
    }
    const flags = type.flags;
    if (flags & _typescript2.default.TypeFlags.Union) {
      const parts = _nullishCoalesce(type.types, () => ( []));
      return parts.length > 0 && parts.every((part) => this.javaNonArrayType(part, depth + 1));
    }
    if ((flags & JAVA_NULLISH_TYPE_FLAGS) !== 0) {
      return true;
    }
    return (flags & JAVA_SCALAR_TYPE_FLAGS) !== 0;
  }
  // A checker-proven dict prints a Map on every path, so the key copy is native;
  // every other target keeps the helper — shared field maps need its synchronized
  // snapshot, the rest need its instanceof/null fallbacks.
  printObjectKeysCall(node, _identation, parsedArg = void 0) {
    const native = this.printNativeObjectKeysCall(node);
    if (native !== void 0) {
      return native;
    }
    return `Helpers.objectKeys(${parsedArg})`;
  }
  printNativeObjectKeysCall(node) {
    const argument = _optionalChain([node, 'optionalAccess', _1525 => _1525.arguments, 'optionalAccess', _1526 => _1526[0]]);
    if (argument === void 0 || _typescript2.default.isPropertyAccessExpression(argument)) {
      return void 0;
    }
    if (this.javaDeclaredMapReceiver(argument)) {
      return `new java.util.ArrayList<Object>(${this.printNode(argument, 0)}.keySet())`;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const type = checker.getTypeAtLocation(argument);
    if (!this.isJavaMapStructureType(type)) {
      return void 0;
    }
    return `new java.util.ArrayList<Object>(((java.util.Map<String, Object>)${this.printNode(argument, 0)}).keySet())`;
  }
  printObjectValuesCall(_node, _identation, parsedArg = void 0) {
    return `Helpers.objectValues(${parsedArg})`;
  }
  printJsonParseCall(_node, _identation, parsedArg = void 0) {
    return `Helpers.parseJson(${parsedArg})`;
  }
  printJsonStringifyCall(_node, _identation, parsedArg = void 0) {
    return `Helpers.json(${parsedArg})`;
  }
  // `await Promise.all ([e1, ..., en])` with every element checker-typed `Promise<...>`:
  // CompletableFuture.allOf waits for exactly those, so Helpers.promiseAll's reflective loop adds
  // nothing. Result used -> thenApply collecting values, only for `const` locals (no double runs).
  printNativePromiseAllCall(node) {
    const awaitNode = _optionalChain([node, 'optionalAccess', _1527 => _1527.parent]);
    if (_optionalChain([awaitNode, 'optionalAccess', _1528 => _1528.kind]) !== _typescript2.default.SyntaxKind.AwaitExpression) {
      return void 0;
    }
    const listNode = _optionalChain([node, 'access', _1529 => _1529.arguments, 'optionalAccess', _1530 => _1530[0]]);
    if (_optionalChain([listNode, 'optionalAccess', _1531 => _1531.kind]) !== _typescript2.default.SyntaxKind.ArrayLiteralExpression) {
      return void 0;
    }
    const elements = listNode.elements;
    const checker = this.getChecker();
    for (const element of elements) {
      if (element.kind === _typescript2.default.SyntaxKind.SpreadElement || !this.isPromiseType(checker.getTypeAtLocation(element))) {
        return void 0;
      }
    }
    const printed = elements.map((element) => this.printNode(element));
    const futureCast = `((${this.PROMISE_TYPE_KEYWORD}<?>) `;
    const casted = printed.map((element) => `${futureCast}${element})`);
    const allOf = `${this.PROMISE_TYPE_KEYWORD}.allOf(${casted.join(", ")})`;
    if (_optionalChain([awaitNode, 'access', _1532 => _1532.parent, 'optionalAccess', _1533 => _1533.kind]) === _typescript2.default.SyntaxKind.ExpressionStatement) {
      return allOf;
    }
    if (elements.length === 0 || !elements.every((element) => this.isConstBoundIdentifier(element)) || printed.indexOf("promiseAllValue") !== -1) {
      return void 0;
    }
    const collected = casted.map((element) => `${element}.join()`).join(", ");
    const collectList = `${this.ARRAY_OPENING_TOKEN}${collected}${this.ARRAY_CLOSING_TOKEN}`;
    return `${allOf}.thenApply(promiseAllValue -> ${collectList})`;
  }
  // a `const`-bound local is assigned exactly once, so the printed java local is
  // effectively final (a lambda can capture it) and re-reading it joins the same future
  isConstBoundIdentifier(node) {
    if (node.kind !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const declaration = _optionalChain([this, 'access', _1534 => _1534.getChecker, 'call', _1535 => _1535(), 'access', _1536 => _1536.getSymbolAtLocation, 'call', _1537 => _1537(node), 'optionalAccess', _1538 => _1538.valueDeclaration]);
    return _optionalChain([declaration, 'optionalAccess', _1539 => _1539.kind]) === _typescript2.default.SyntaxKind.VariableDeclaration && (_typescript2.default.getCombinedNodeFlags(declaration) & _typescript2.default.NodeFlags.Const) === _typescript2.default.NodeFlags.Const;
  }
  printPromiseAllCall(node, identation, parsedArg = void 0) {
    const nativeCall = this.printNativePromiseAllCall(node);
    if (nativeCall !== void 0) {
      return nativeCall;
    }
    return `Helpers.promiseAll(${parsedArg})`;
  }
  printMathFloorCall(node, _identation, parsedArg = void 0) {
    return `(Math.floor(Double.parseDouble(${this.javaStringBoxText(_optionalChain([node, 'optionalAccess', _1540 => _1540.arguments, 'optionalAccess', _1541 => _1541[0]]), parsedArg)})))`;
  }
  printMathRoundCall(node, _identation, parsedArg = void 0) {
    return `Math.round(Double.parseDouble(${this.javaStringBoxText(_optionalChain([node, 'optionalAccess', _1542 => _1542.arguments, 'optionalAccess', _1543 => _1543[0]]), parsedArg)}))`;
  }
  printMathCeilCall(node, _identation, parsedArg = void 0) {
    return `Math.ceil(Double.parseDouble(${this.javaStringBoxText(_optionalChain([node, 'optionalAccess', _1544 => _1544.arguments, 'optionalAccess', _1545 => _1545[0]]), parsedArg)}))`;
  }
  printNumberIsIntegerCall(_node, _identation, parsedArg = void 0) {
    return `((${parsedArg} instanceof Integer) || (${parsedArg} instanceof Long))`;
  }
  printArrayPushCall(_node, _identation, name = void 0, parsedArg = void 0) {
    return `((java.util.List<Object>)${name}).add(${parsedArg})`;
  }
  printIncludesCall(_node, _identation, name = void 0, parsedArg = void 0) {
    return `${name}.contains(${parsedArg})`;
  }
  // Helpers.getIndexOf(str, target) is List.indexOf for a List receiver, String.indexOf for a String
  // receiver with a String target, -1 otherwise. Locals are `Object`/`var`, so the native call takes
  // the same checkcast as `.length()` / `containsKey`; the target takes one unless a printed String.
  javaNativeIndexOfCall(node, name, parsedArg) {
    if (node === void 0 || name === void 0 || parsedArg === void 0) {
      return void 0;
    }
    const receiver = _optionalChain([node, 'optionalAccess', _1546 => _1546.expression, 'optionalAccess', _1547 => _1547.expression]);
    if (receiver === void 0) {
      return void 0;
    }
    const checker = this.getChecker();
    let receiverType;
    try {
      receiverType = checker.getTypeAtLocation(receiver);
    } catch (e) {
      return void 0;
    }
    if (this.isJavaListType(receiverType) && !this.isVarargsArrayReference(receiver)) {
      return `((java.util.List<?>)${name}).indexOf(${parsedArg})`;
    }
    if (receiverType.aliasSymbol !== void 0 || !this.isStringType(receiverType.flags)) {
      return void 0;
    }
    const arg = _optionalChain([node, 'access', _1548 => _1548.arguments, 'optionalAccess', _1549 => _1549[0]]);
    if (arg === void 0) {
      return void 0;
    }
    if (this.javaProvableString(arg)) {
      return `((String)${name}).indexOf(${parsedArg})`;
    }
    let argType;
    try {
      argType = checker.getTypeAtLocation(arg);
    } catch (e) {
      return void 0;
    }
    if (argType.aliasSymbol === void 0 && this.isStringType(argType.flags) && (_typescript2.default.isIdentifier(arg) || _typescript2.default.isPropertyAccessExpression(arg))) {
      return `((String)${name}).indexOf(((String)${parsedArg}))`;
    }
    return void 0;
  }
  printIndexOfCall(node, _identation, name = void 0, parsedArg = void 0) {
    const native = this.javaNativeIndexOfCall(node, name, parsedArg);
    if (native !== void 0) {
      return native;
    }
    return `${this.INDEXOF_WRAPPER_OPEN}${name}, ${parsedArg}${this.INDEXOF_WRAPPER_CLOSE}`;
  }
  printSearchCall(_node, _identation, name = void 0, parsedArg = void 0) {
    return `((String)${name}).indexOf(${parsedArg})`;
  }
  printStartsWithCall(_node, _identation, name = void 0, parsedArg = void 0) {
    return `((String)${name}).startsWith(((String)${parsedArg}))`;
  }
  printEndsWithCall(_node, _identation, name = void 0, parsedArg = void 0) {
    return `((String)${name}).endsWith(((String)${parsedArg}))`;
  }
  printTrimCall(_node, _identation, name = void 0) {
    return `((String)${name}).trim()`;
  }
  printJoinCall(_node, _identation, name = void 0, parsedArg = void 0) {
    return `String.join((String)${parsedArg}, (java.util.List<String>)${name})`;
  }
  printSplitCall(node, _identation, name = void 0, parsedArg = void 0) {
    const nativeSplit = this.javaNativeSplitCall(node, name, parsedArg);
    if (nativeSplit !== void 0) {
      return nativeSplit;
    }
    return `Helpers.split(${name}, ${parsedArg})`;
  }
  printConcatCall(_node, _identation, name = void 0, parsedArg = void 0) {
    return `Helpers.concat(${name}, ${parsedArg})`;
  }
  printToFixedCall(_node, _identation, name = void 0, parsedArg = void 0) {
    return `toFixed(${name}, ${parsedArg})`;
  }
  printToStringCall(_node, _identation, name = void 0) {
    return `String.valueOf(${name})`;
  }
  printToUpperCaseCall(_node, _identation, name = void 0) {
    return `((String)${name}).toUpperCase()`;
  }
  printToLowerCaseCall(_node, _identation, name = void 0) {
    return `((String)${name}).toLowerCase()`;
  }
  printShiftCall(_node, _identation, name = void 0) {
    return `((java.util.List<Object>)${name}).get(0)`;
  }
  printReverseCall(_node, _identation, name = void 0) {
    return `java.util.Collections.reverse((java.util.List<Object>)${name})`;
  }
  printPopCall(_node, _identation, name = void 0) {
    return `((java.util.List<Object>)${name}).get(((java.util.List<Object>)${name}).size()-1)`;
  }
  printAssertCall(_node, _identation, parsedArgs) {
    return `assert(${parsedArgs})`;
  }
  printSliceCall(node, _identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    const nativeCall = this.nativeSliceCallIfProvable(node, name);
    if (nativeCall !== void 0) {
      return nativeCall;
    }
    if (parsedArg2 === void 0) {
      parsedArg2 = "null";
    }
    return `Helpers.slice(${name}, ${parsedArg}, ${parsedArg2})`;
  }
  // Integer value of a slice bound that is an integer literal (`18`, `-64`); anything else keeps
  // the helper — the Java helper converts bounds with toInt, and a double cannot be clamped with the
  // integer Math.min/Math.max of the native form.
  javaSliceLiteralBound(node) {
    if (node === void 0) {
      return void 0;
    }
    if (_typescript2.default.isNumericLiteral(node)) {
      const text = String(node.text);
      if (text.indexOf(".") !== -1 || text.indexOf("e") !== -1 || text.indexOf("E") !== -1) {
        return void 0;
      }
      const value = Number(text);
      return value <= 2147483647 ? value : void 0;
    }
    if (_typescript2.default.isPrefixUnaryExpression(node) && node.operator === _typescript2.default.SyntaxKind.MinusToken) {
      const inner = this.javaSliceLiteralBound(node.operand);
      return inner === void 0 ? void 0 : -inner;
    }
    return void 0;
  }
  // JS slice clamps a literal bound into [0, length]: a non-negative bound is min
  // (bound, length), a negative one counts from the end (max (length - |bound|, 0)).
  // `0` stays `0` because the length of a String/List is never negative.
  javaSliceBoundExpression(value, length) {
    if (value === 0) {
      return "0";
    }
    return value > 0 ? `Math.min(${value}, ${length})` : `Math.max(${length} - ${-value}, 0)`;
  }
  // `x.slice (a, b)` -> substring/subList when the receiver provably prints as String/List AND every
  // bound is an integer literal: Java throws where JS clamps, so only literal bounds can be clamped.
  // The null guard keeps null -> null and needs a side-effect-free receiver (read up to three times).
  nativeSliceCallIfProvable(node, name) {
    const args = _nullishCoalesce(_optionalChain([node, 'optionalAccess', _1550 => _1550.arguments]), () => ( []));
    if (args.length < 1 || args.length > 2) {
      return void 0;
    }
    const start = this.javaSliceLiteralBound(args[0]);
    if (start === void 0) {
      return void 0;
    }
    const hasEnd = args.length === 2;
    const end = hasEnd ? this.javaSliceLiteralBound(args[1]) : void 0;
    if (hasEnd && end === void 0) {
      return void 0;
    }
    const receiverExpression = _typescript2.default.isPropertyAccessExpression(_optionalChain([node, 'optionalAccess', _1551 => _1551.expression])) ? node.expression.expression : void 0;
    if (!this.sideEffectFreeReceiver(receiverExpression)) {
      return void 0;
    }
    let kind;
    try {
      const type = this.getChecker().getTypeAtLocation(receiverExpression);
      if (this.isStringType(type.flags)) {
        kind = "String";
      } else if (this.isJavaListType(type) && !this.isVarargsArrayReference(receiverExpression)) {
        kind = "List";
      } else {
        return void 0;
      }
    } catch (e) {
      return void 0;
    }
    const cast = kind === "String" ? `((String)${name})` : `((java.util.List<Object>)${name})`;
    const length = kind === "String" ? `${cast}.length()` : `${cast}.size()`;
    const startText = this.javaSliceBoundExpression(start, length);
    const endText = hasEnd ? this.javaSliceBoundExpression(end, length) : length;
    const ordered = start === 0 || !hasEnd || start >= 0 && end >= 0 && start <= end || start < 0 && end < 0 && start <= end;
    const fromText = ordered ? startText : `Math.min(${startText}, ${endText})`;
    const argumentsText = hasEnd || kind === "List" ? `${fromText}, ${endText}` : fromText;
    const method = kind === "String" ? "substring" : "subList";
    return `(${name} == null ? null : ${cast}.${method}(${argumentsText}))`;
  }
  printReplaceCall(_node, _identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    return `Helpers.replace((String)${name}, (String)${parsedArg}, (String)${parsedArg2})`;
  }
  printReplaceAllCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    const pattern = this.stringLiteralArgument(_optionalChain([node, 'optionalAccess', _1552 => _1552.arguments, 'optionalAccess', _1553 => _1553[0]]));
    const replacement = this.stringLiteralArgument(_optionalChain([node, 'optionalAccess', _1554 => _1554.arguments, 'optionalAccess', _1555 => _1555[1]]));
    const receiver = this.sideEffectFreeReceiver(_optionalChain([node, 'optionalAccess', _1556 => _1556.expression]));
    if (pattern !== void 0 && replacement !== void 0 && receiver) {
      return `(${name} == null ? null : ((String)${name}).replace(${pattern}, ${replacement}))`;
    }
    return `Helpers.replaceAll((String)${name}, (String)${parsedArg}, (String)${parsedArg2})`;
  }
  // Printed form of a non-empty string-literal argument with no escapes, or undefined when the
  // argument is not a literal, prints with an escape sequence, or is the empty pattern.
  stringLiteralArgument(argument) {
    if (argument === void 0 || !_typescript2.default.isStringLiteral(argument)) {
      return void 0;
    }
    const printed = this.printNode(argument, 0);
    return /^"[^"\\]+"$/.test(printed) ? printed : void 0;
  }
  // True for receivers that read a value without calling anything: `x`, `x.y`, `this.x`,
  // `(x as string)` and parenthesised forms of those. Guards the double read of the ternary.
  sideEffectFreeReceiver(expression) {
    if (expression === void 0) {
      return false;
    }
    if (_typescript2.default.isParenthesizedExpression(expression) || _typescript2.default.isAsExpression(expression) || _typescript2.default.isTypeAssertionExpression(expression)) {
      return this.sideEffectFreeReceiver(expression.expression);
    }
    if (_typescript2.default.isIdentifier(expression) || expression.kind === _typescript2.default.SyntaxKind.ThisKeyword) {
      return true;
    }
    if (_typescript2.default.isPropertyAccessExpression(expression)) {
      return this.sideEffectFreeReceiver(expression.expression);
    }
    return false;
  }
  printPadEndCall(_node, _identation, name, parsedArg, parsedArg2) {
    return `Helpers.padEnd((String)${name}, ((Number)${parsedArg}).intValue(), ((String)${parsedArg2}).charAt(0))`;
  }
  printPadStartCall(node, _identation, name, parsedArg, parsedArg2) {
    const native = this.printNativePadStart(node, name);
    if (native !== void 0) {
      return native;
    }
    return `Helpers.padStart((String)${name}, ((Number)${parsedArg}).intValue(), ((String)${parsedArg2}).charAt(0))`;
  }
  printDateNowCall(_node, _identation) {
    return "System.currentTimeMillis()";
  }
  printLengthProperty(node, _identation, _name = void 0) {
    const leftSide = this.printNode(node.expression, 0);
    const type = this.getChecker().getTypeAtLocation(node.expression);
    this.warnIfAnyType(node, type.flags, leftSide, "length");
    return this.printJavaLength(node.expression, leftSide);
  }
  // For ++/--, prefer native Java operators rather than the C# ref-helpers
  printPostFixUnaryExpression(node, identation) {
    const { operand, operator } = node;
    const leftSide = this.printNode(operand, 0);
    const op = this.PostFixOperators[operator];
    if (op === "--") {
      return `${leftSide}--`;
    }
    return `${leftSide}++`;
  }
  // the identifier declared by a `for (var i = <int literal>; ...; i++)` header: the
  // printer writes that initializer as `var`, so javac infers a primitive int and the
  // ++/-- increment keeps it one
  javaPrimitiveCounter(node) {
    if (_optionalChain([node, 'optionalAccess', _1557 => _1557.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return false;
    }
    const symbol = this.getChecker().getSymbolAtLocation(node);
    const declaration = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1558 => _1558.valueDeclaration]), () => ( _optionalChain([symbol, 'optionalAccess', _1559 => _1559.declarations, 'optionalAccess', _1560 => _1560[0]])));
    if (declaration === void 0 || !_typescript2.default.isVariableDeclaration(declaration) || !_typescript2.default.isIdentifier(declaration.name)) {
      return false;
    }
    if (declaration.name.escapedText !== node.escapedText || declaration.initializer === void 0) {
      return false;
    }
    const list = declaration.parent;
    if (_optionalChain([list, 'optionalAccess', _1561 => _1561.kind]) !== _typescript2.default.SyntaxKind.VariableDeclarationList || list.declarations.length !== 1) {
      return false;
    }
    const forStatement = list.parent;
    if (_optionalChain([forStatement, 'optionalAccess', _1562 => _1562.kind]) !== _typescript2.default.SyntaxKind.ForStatement || forStatement.initializer !== list) {
      return false;
    }
    if (this.javaIntegerLiteralKind(declaration.initializer) === void 0) {
      return false;
    }
    const incrementor = forStatement.incrementor;
    return _optionalChain([incrementor, 'optionalAccess', _1563 => _1563.kind]) === _typescript2.default.SyntaxKind.PostfixUnaryExpression && _optionalChain([incrementor, 'access', _1564 => _1564.operand, 'optionalAccess', _1565 => _1565.kind]) === _typescript2.default.SyntaxKind.Identifier && incrementor.operand.escapedText === node.escapedText;
  }
  // `-x` prints as the plain Java operator when the operand is already primitive: a decimal literal,
  // a nested native `+ - * /`, a `for (var i = <int literal>` counter or a `.length` read.
  // Helpers.opNeg preserves the box and maps null to null, so every boxed local keeps the helper.
  javaNativeNegation(node) {
    if (node === void 0) {
      return false;
    }
    if (node.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.javaNativeNegation(node.expression);
    }
    if (_typescript2.default.isNumericLiteral(node)) {
      return this.javaProvableNumericKind(node) !== void 0;
    }
    if (node.kind === _typescript2.default.SyntaxKind.BinaryExpression) {
      return this.javaNativeArithmeticKind(node) !== void 0;
    }
    if (this.javaPrimitiveCounter(node)) {
      return true;
    }
    return node.kind === _typescript2.default.SyntaxKind.PropertyAccessExpression && node.name.escapedText === "length" && this.javaLengthKind(node.expression) !== void 0;
  }
  printPrefixUnaryExpression(node, identation) {
    const { operand, operator } = node;
    if (operator === _typescript2.default.SyntaxKind.ExclamationToken) {
      return this.PrefixFixOperators[operator] + this.printCondition(node.operand, 0);
    }
    const leftSide = this.printNode(operand, 0);
    if (operator === _typescript2.default.SyntaxKind.PlusToken) {
      return `+(${leftSide})`;
    } else if (operator === _typescript2.default.SyntaxKind.MinusToken) {
      if (this.javaNativeNegation(operand)) {
        return `-${leftSide}`;
      }
      return `Helpers.opNeg(${leftSide})`;
    }
    return super.printPrefixUnaryExpression(node, identation);
  }
  javaBooleanCondition(node) {
    if (node.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.javaBooleanCondition(node.expression);
    }
    if (node.kind === _typescript2.default.SyntaxKind.PrefixUnaryExpression) {
      return node.operator === _typescript2.default.SyntaxKind.ExclamationToken && this.javaBooleanCondition(node.operand);
    }
    if (node.kind !== _typescript2.default.SyntaxKind.BinaryExpression) {
      return false;
    }
    return this.javaBooleanOperators.includes(node.operatorToken.kind);
  }
  // `Precise.<relational>(a, b)` resolving to a static of the base `Precise` class: Precise.java
  // declares them `public static boolean`, so the call is a primitive boolean. The receiver name
  // alone is no proof (a shadowing local prints the same text), so the resolution is checked too.
  javaPreciseBooleanCall(node) {
    if (node.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.javaPreciseBooleanCall(node.expression);
    }
    if (node.kind === _typescript2.default.SyntaxKind.PrefixUnaryExpression) {
      return node.operator === _typescript2.default.SyntaxKind.ExclamationToken && this.javaPreciseBooleanCall(node.operand);
    }
    if (node.kind !== _typescript2.default.SyntaxKind.CallExpression || node.expression.kind !== _typescript2.default.SyntaxKind.PropertyAccessExpression || node.expression.expression.kind !== _typescript2.default.SyntaxKind.Identifier || node.expression.expression.escapedText !== "Precise" || !JAVA_PRECISE_BOOLEAN_STATICS.has(node.expression.name.escapedText)) {
      return false;
    }
    const declaration = _optionalChain([this, 'access', _1566 => _1566.getChecker, 'call', _1567 => _1567(), 'access', _1568 => _1568.getResolvedSignature, 'call', _1569 => _1569(node), 'optionalAccess', _1570 => _1570.declaration]);
    if (declaration === void 0 || declaration.kind !== _typescript2.default.SyntaxKind.MethodDeclaration || _optionalChain([declaration, 'access', _1571 => _1571.parent, 'optionalAccess', _1572 => _1572.kind]) !== _typescript2.default.SyntaxKind.ClassDeclaration || _optionalChain([declaration, 'access', _1573 => _1573.parent, 'access', _1574 => _1574.name, 'optionalAccess', _1575 => _1575.escapedText]) !== "Precise" || !_optionalChain([declaration, 'access', _1576 => _1576.modifiers, 'optionalAccess', _1577 => _1577.some, 'call', _1578 => _1578((modifier) => modifier.kind === _typescript2.default.SyntaxKind.StaticKeyword)])) {
      return false;
    }
    return (this.getChecker().getTypeAtLocation(node).flags & _typescript2.default.TypeFlags.Boolean) !== 0;
  }
  // the checker's view of a condition value: BooleanLike is the plain `boolean` (the `boolean |
  // undefined` of an accessor with a default is BooleanLiteral + Undefined and does not qualify),
  // while a union whose every member is boolean/nullish is the nullable box.
  javaBooleanValueKind(node) {
    const type = this.getChecker().getTypeAtLocation(node);
    const flags = _nullishCoalesce(_optionalChain([type, 'optionalAccess', _1579 => _1579.flags]), () => ( 0));
    if (flags & _typescript2.default.TypeFlags.BooleanLike) {
      return "boolean";
    }
    if ((flags & _typescript2.default.TypeFlags.Union) === 0) {
      return void 0;
    }
    const members = _nullishCoalesce(type.types, () => ( []));
    const booleanishMembers = _typescript2.default.TypeFlags.BooleanLike | _typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Void;
    const allBooleanish = members.length > 0 && members.every((member) => ((_nullishCoalesce(member.flags, () => ( 0))) & booleanishMembers) !== 0);
    return allBooleanish ? "nullableBoolean" : void 0;
  }
  // mirrors printWrappedUnknownThisProperty: a `this.<name>(...)` call the checker cannot
  // resolve prints `Helpers.callDynamically(this, "<name>", ...)`, whose Java return is Object
  javaCalleeResolves(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const signature = checker.getResolvedSignature(node);
    return _optionalChain([signature, 'optionalAccess', _1580 => _1580.declaration]) !== void 0;
  }
  // the boolean the printed Java of a `this.<name>(...)` call already carries, from the
  // hand-written base declarations in JAVA_THIS_BOOLEAN_METHODS / the box proof in
  // JAVA_THIS_BOOLEAN_BOX_METHODS. undefined: not a direct boolean call, keep the wrapper.
  javaCallBooleanKind(node) {
    if (_optionalChain([node, 'optionalAccess', _1581 => _1581.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return void 0;
    }
    const callee = node.expression;
    if (_optionalChain([callee, 'optionalAccess', _1582 => _1582.kind]) !== _typescript2.default.SyntaxKind.PropertyAccessExpression || _optionalChain([callee, 'access', _1583 => _1583.expression, 'optionalAccess', _1584 => _1584.kind]) !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return void 0;
    }
    const kind = this.javaBooleanValueKind(node);
    if (kind === void 0 || !this.javaCalleeResolves(node)) {
      return void 0;
    }
    const name = _optionalChain([callee, 'access', _1585 => _1585.name, 'optionalAccess', _1586 => _1586.escapedText]);
    if (JAVA_THIS_BOOLEAN_METHODS.has(name)) {
      return kind === "boolean" ? "boolean" : void 0;
    }
    const defaultArgumentIndex = JAVA_THIS_BOOLEAN_BOX_METHODS[name];
    if (defaultArgumentIndex === void 0) {
      return void 0;
    }
    const defaultArgument = _optionalChain([node, 'access', _1587 => _1587.arguments, 'optionalAccess', _1588 => _1588[defaultArgumentIndex]]);
    const defaultIsNullish = defaultArgument === void 0 || defaultArgument.kind === _typescript2.default.SyntaxKind.NullKeyword || defaultArgument.kind === _typescript2.default.SyntaxKind.Identifier && defaultArgument.escapedText === "undefined";
    const defaultIsBoolean = defaultIsNullish || _optionalChain([defaultArgument, 'optionalAccess', _1589 => _1589.kind]) === _typescript2.default.SyntaxKind.TrueKeyword || _optionalChain([defaultArgument, 'optionalAccess', _1590 => _1590.kind]) === _typescript2.default.SyntaxKind.FalseKeyword;
    return defaultIsBoolean ? "nullableBoolean" : void 0;
  }
  // the printer already emits these conditions as Java `boolean` (the comparison helpers,
  // `in`/`instanceof` and the logical operators all return/print primitive boolean), so
  // Helpers.isTrue would only re-test a value the checker proves is boolean
  javaConditionPrintsBoolean(node) {
    if (this.javaBooleanCondition(node)) {
      return (this.getChecker().getTypeAtLocation(node).flags & _typescript2.default.TypeFlags.Boolean) !== 0;
    }
    return this.javaPreciseBooleanCall(node);
  }
  // the checker proves this expression's TypeScript type is exactly `boolean` (or a boolean
  // literal type): no `any`, no `undefined`/`null` union, no nullable alias
  javaBooleanBoxType(type) {
    if (type === void 0) {
      return false;
    }
    if (type.aliasSymbol !== void 0) {
      return false;
    }
    const flags = _nullishCoalesce(type.flags, () => ( 0));
    if ((flags & JAVA_BOOLEAN_EXCLUDED_TYPE_FLAGS) !== 0) {
      return false;
    }
    return (flags & (_typescript2.default.TypeFlags.Boolean | _typescript2.default.TypeFlags.BooleanLiteral)) !== 0;
  }
  javaTypeOfNode(node) {
    return _optionalChain([this, 'access', _1591 => _1591.checkerOrUndefined, 'call', _1592 => _1592(), 'optionalAccess', _1593 => _1593.getTypeAtLocation, 'call', _1594 => _1594(node)]);
  }
  // the DECLARED type of a declaration, not the narrowed type at a use site: TypeScript
  // narrows a `const ok: boolean = true` to the literal `true` and can even reach `never`
  // after a guarding `if (ok) return;`, which says nothing about the box the local holds
  javaTypeOfDeclaration(decl) {
    if (decl === void 0) {
      return void 0;
    }
    return _optionalChain([this, 'access', _1595 => _1595.checkerOrUndefined, 'call', _1596 => _1596(), 'optionalAccess', _1597 => _1597.getTypeAtLocation, 'call', _1598 => _1598(decl)]);
  }
  // the printed Java of this expression is a primitive `boolean` (or Boolean box): boolean literals,
  // `!`, logical / comparison / `in` operators, `Array.isArray(x)` (Helpers.isArray, `public static
  // boolean`) and hand-written `public boolean` base methods. `seen` breaks `a = b; b = a;` cycles.
  javaPrintsBooleanValue(node, seen, depth = 0) {
    if (node === void 0) {
      return false;
    }
    switch (node.kind) {
      case _typescript2.default.SyntaxKind.TrueKeyword:
      case _typescript2.default.SyntaxKind.FalseKeyword:
        return true;
      case _typescript2.default.SyntaxKind.ParenthesizedExpression:
      case _typescript2.default.SyntaxKind.AsExpression:
      case _typescript2.default.SyntaxKind.NonNullExpression:
        return this.javaPrintsBooleanValue(node.expression, seen, depth);
      case _typescript2.default.SyntaxKind.PrefixUnaryExpression:
        return node.operator === _typescript2.default.SyntaxKind.ExclamationToken && this.javaPrintsBooleanValue(node.operand, seen, depth);
      case _typescript2.default.SyntaxKind.BinaryExpression:
        return JAVA_BOOLEAN_OPERATOR_KINDS.has(node.operatorToken.kind);
      case _typescript2.default.SyntaxKind.CallExpression:
        return this.javaPrintsBooleanCall(node, seen, depth);
      case _typescript2.default.SyntaxKind.ConditionalExpression:
        return this.javaPrintsBooleanValue(node.whenTrue, seen, depth) && this.javaPrintsBooleanValue(node.whenFalse, seen, depth);
      case _typescript2.default.SyntaxKind.PropertyAccessExpression:
        return this.javaBooleanBaseField(node) !== void 0;
      case _typescript2.default.SyntaxKind.Identifier:
        return this.javaBooleanBoxIdentifier(node, seen) !== void 0;
    }
    return false;
  }
  // the Java box of a `this.<name>(...)` call whose generated body returns a boolean on every path:
  // the TS return type is a boolean family and every `return` in the resolved declaration prints a
  // Java boolean or a proven Boolean-or-null box. `depth` and `seen` bound the recursion.
  javaCallReturnsBooleanBox(node, seen, depth) {
    if (_optionalChain([node, 'optionalAccess', _1599 => _1599.kind]) !== _typescript2.default.SyntaxKind.CallExpression || depth > 2) {
      return false;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const declaration = _optionalChain([checker, 'access', _1600 => _1600.getResolvedSignature, 'call', _1601 => _1601(node), 'optionalAccess', _1602 => _1602.declaration]);
    if (declaration === void 0 || declaration.kind !== _typescript2.default.SyntaxKind.MethodDeclaration || declaration.body === void 0 || seen.has(declaration)) {
      return false;
    }
    if (this.javaBooleanValueKind(node) === void 0) {
      return false;
    }
    const callee = node.expression;
    if (!_typescript2.default.isPropertyAccessExpression(callee) || callee.expression.kind !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return false;
    }
    const name = String(callee.name.escapedText);
    if (JAVA_THIS_BOOLEAN_METHODS.has(name) || JAVA_THIS_BOOLEAN_BOX_METHODS[name] !== void 0) {
      return false;
    }
    const next = new Set(seen);
    next.add(declaration);
    let returns = 0;
    let ok = true;
    const scan = (current) => {
      if (!ok) {
        return;
      }
      if (current !== declaration && _typescript2.default.isFunctionLike(current)) {
        return;
      }
      if (_typescript2.default.isReturnStatement(current)) {
        returns++;
        const expression = current.expression;
        if (expression === void 0 || !this.javaPrintsBooleanValue(expression, next, depth + 1)) {
          ok = false;
          return;
        }
      }
      _typescript2.default.forEachChild(current, scan);
    };
    scan(declaration.body);
    return ok && returns > 0;
  }
  // `Array.isArray(x)` prints `Helpers.isArray(x)` (`public static boolean`), the relational
  // Precise statics print `public static boolean` and the hand-written `public boolean` base
  // methods print a primitive boolean. Everything else - including the generated
  javaPrintsBooleanCall(node, seen = /* @__PURE__ */ new Set(), depth = 0) {
    if (this.isArrayIsArrayCall(node)) {
      return true;
    }
    if (this.javaPreciseBooleanCall(node)) {
      return true;
    }
    const callee = node.expression;
    if (_typescript2.default.isPropertyAccessExpression(callee) && callee.expression.kind === _typescript2.default.SyntaxKind.ThisKeyword && JAVA_BOOLEAN_BASE_CALLS.has(String(callee.name.escapedText))) {
      return true;
    }
    return this.javaCallReturnsBooleanBox(node, seen, depth);
  }
  isArrayIsArrayCall(node) {
    if (node === void 0 || node.kind !== _typescript2.default.SyntaxKind.CallExpression) {
      return false;
    }
    const callee = node.expression;
    return _typescript2.default.isPropertyAccessExpression(callee) && String(callee.name.escapedText) === "isArray" && callee.expression.kind === _typescript2.default.SyntaxKind.Identifier && String(callee.expression.escapedText) === "Array" && (_nullishCoalesce(_optionalChain([node, 'access', _1603 => _1603.arguments, 'optionalAccess', _1604 => _1604.length]), () => ( 0))) === 1;
  }
  // `this.<name>` read of a hand-written base field declared `boolean`, or undefined. The
  // TsChecker guard keeps a field the hand-written base declares Object (or String) out.
  javaBooleanBaseField(node) {
    if (!_typescript2.default.isPropertyAccessExpression(node) || node.expression.kind !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return void 0;
    }
    const printed = this.printNode(node, 0);
    if (!JAVA_BOOLEAN_BASE_FIELDS.has(printed)) {
      return void 0;
    }
    let declaration;
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      declaration = void 0;
    }
    declaration = _optionalChain([checker, 'access', _1605 => _1605.getSymbolAtLocation, 'call', _1606 => _1606(node.name), 'optionalAccess', _1607 => _1607.valueDeclaration]);
    const type = _nullishCoalesce(this.javaTypeOfDeclaration(declaration), () => ( this.javaTypeOfNode(node)));
    return this.javaBooleanBoxType(type) ? printed : void 0;
  }
  // the Java name of an identifier that provably holds a Boolean box or null, so `Helpers.isTrue(x)`
  // IS `Boolean.TRUE.equals(x)`. Proof: the TS type (`boolean`, never `any`/`undefined`-able) plus
  // the D2 write scan: every write in the enclosing function must print a Java boolean value.
  javaBooleanBoxIdentifier(node, seen) {
    if (_optionalChain([node, 'optionalAccess', _1608 => _1608.kind]) !== _typescript2.default.SyntaxKind.Identifier || seen.has(node)) {
      return void 0;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const symbol = checker.getSymbolAtLocation(node);
    const decl = _optionalChain([symbol, 'optionalAccess', _1609 => _1609.valueDeclaration]);
    if (decl === void 0) {
      return void 0;
    }
    if (decl.kind !== _typescript2.default.SyntaxKind.VariableDeclaration) {
      return void 0;
    }
    let type;
    try {
      type = this.javaTypeOfDeclaration(decl);
    } catch (e) {
      type = void 0;
    }
    if (!this.javaBooleanBoxType(type)) {
      return void 0;
    }
    if (!this.javaBooleanWritesAreBoxed(symbol, decl, node, seen)) {
      return void 0;
    }
    return this.printNode(node, 0);
  }
  // every value written to the symbol prints a Java boolean. The scan runs over the function
  // the DECLARATION lives in (a local can only be written inside it or inside a closure nested
  // in it), so a write in an enclosing scope is never missed.
  javaBooleanWritesAreBoxed(symbol, decl, node, seen) {
    const next = new Set(seen);
    next.add(node);
    let fn = decl.parent;
    while (fn !== void 0 && !_typescript2.default.isFunctionLike(fn)) {
      fn = fn.parent;
    }
    if (fn === void 0) {
      return false;
    }
    if (decl.initializer !== void 0 && !this.javaPrintsBooleanValue(decl.initializer, next)) {
      return false;
    }
    let ok = true;
    const scan = (current) => {
      if (!ok) {
        return;
      }
      if ((_typescript2.default.isForOfStatement(current) || _typescript2.default.isForInStatement(current)) && _typescript2.default.isIdentifier(current.initializer)) {
        let loop;
        const checker = this.checkerOrUndefined();
        if (checker === void 0) {
          loop = void 0;
        }
        loop = checker.getSymbolAtLocation(current.initializer);
        if (loop === symbol) {
          ok = false;
          return;
        }
      }
      if (_typescript2.default.isBinaryExpression(current) && JAVA_ASSIGNMENT_OPERATOR_KINDS.has(current.operatorToken.kind) && _typescript2.default.isIdentifier(current.left)) {
        let left;
        const checker = this.checkerOrUndefined();
        if (checker === void 0) {
          left = void 0;
        }
        left = checker.getSymbolAtLocation(current.left);
        if (left === symbol && !this.javaPrintsBooleanValue(current.right, next)) {
          ok = false;
          return;
        }
      }
      _typescript2.default.forEachChild(current, scan);
    };
    scan(fn);
    return ok;
  }
  // the declared Java type the ccxt-side declaration chain gave this local/param, via the
  // javaDeclaredLocalTypeResolver hook (build/java-local-types.js): `boolean` prints as the
  // primitive, so the identifier IS the condition; `Boolean` is the nullable box (isTrue = equals).
  javaDeclaredBooleanKind(node) {
    const declaration = this.javaDeclarationOfIdentifier(node);
    if (declaration === void 0 || node.escapedText !== _optionalChain([declaration, 'access', _1610 => _1610.name, 'optionalAccess', _1611 => _1611.escapedText])) {
      return void 0;
    }
    let declared;
    try {
      declared = this.javaDeclaredTypeOfDeclaration(declaration);
    } catch (e) {
      return void 0;
    }
    return declared === "boolean" || declared === "Boolean" ? declared : void 0;
  }
  // the DECLARED type of the local is a nullable boolean: `Bool` (`boolean | undefined`) or an
  // equivalent union. Every member must be boolean or nullish - an `Int`/`Str`/`any` member can
  // hold a box the isTrue helper tests with its runtime truthiness, so the helper must stay.
  javaNullableBooleanDeclaration(declaration) {
    if (_optionalChain([declaration, 'optionalAccess', _1612 => _1612.kind]) !== _typescript2.default.SyntaxKind.VariableDeclaration) {
      return false;
    }
    const type = this.javaTypeOfDeclaration(declaration);
    if (type === void 0) {
      return false;
    }
    const flags = _nullishCoalesce(type.flags, () => ( 0));
    if ((flags & (_typescript2.default.TypeFlags.Any | _typescript2.default.TypeFlags.Unknown)) !== 0) {
      return false;
    }
    if ((flags & _typescript2.default.TypeFlags.Boolean) !== 0) {
      return false;
    }
    if ((flags & _typescript2.default.TypeFlags.Union) === 0) {
      return false;
    }
    const members = _nullishCoalesce(type.types, () => ( []));
    if (members.length === 0) {
      return false;
    }
    const booleanish = (member) => ((_nullishCoalesce(_optionalChain([member, 'optionalAccess', _1613 => _1613.flags]), () => ( 0))) & JAVA_NULLABLE_BOOLEAN_MEMBER_FLAGS) !== 0;
    const hasBoolean = members.some((member) => ((_nullishCoalesce(_optionalChain([member, 'optionalAccess', _1614 => _1614.flags]), () => ( 0))) & (_typescript2.default.TypeFlags.Boolean | _typescript2.default.TypeFlags.BooleanLiteral)) !== 0);
    return hasBoolean && members.every(booleanish);
  }
  // a value the nullable-boolean write scan accepts: a Java boolean value this printer proves, a
  // nullish literal, or a safeBool-family accessor whose other paths return the caller's default
  // (Boolean or null). A Long/Int/String/List box would change what isTrue answers.
  javaPrintsBooleanBoxValue(node, seen) {
    if (node === void 0) {
      return false;
    }
    if (node.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression || node.kind === _typescript2.default.SyntaxKind.AsExpression || node.kind === _typescript2.default.SyntaxKind.NonNullExpression) {
      return this.javaPrintsBooleanBoxValue(node.expression, seen);
    }
    if (node.kind === _typescript2.default.SyntaxKind.NullKeyword) {
      return true;
    }
    if (node.kind === _typescript2.default.SyntaxKind.Identifier && node.escapedText === "undefined") {
      return true;
    }
    if (this.javaPrintsBooleanValue(node, seen)) {
      return true;
    }
    if (node.kind === _typescript2.default.SyntaxKind.Identifier && this.javaNullableBooleanBoxIdentifier(node, seen) !== void 0) {
      return true;
    }
    if (node.kind === _typescript2.default.SyntaxKind.CallExpression) {
      return this.javaCallBooleanKind(node) !== void 0;
    }
    return false;
  }
  // element `index` of a `[ x, params ] = this.handle*Bool (...)` destructure is a Boolean-or-null
  // box: those accessors return a safeBool result, never the raw member when not a Boolean. The
  // handleOptionAndParams family returns the raw member and is deliberately not a box proof.
  javaBooleanBoxTupleElement(node, index) {
    if (_optionalChain([node, 'optionalAccess', _1615 => _1615.kind]) !== _typescript2.default.SyntaxKind.CallExpression) {
      return false;
    }
    const callee = node.expression;
    if (!_typescript2.default.isPropertyAccessExpression(callee) || callee.expression.kind !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return false;
    }
    const name = String(callee.name.escapedText);
    if (!JAVA_BOOLEAN_BOX_TUPLE_METHODS.has(name)) {
      return false;
    }
    const declaration = _optionalChain([this, 'access', _1616 => _1616.getChecker, 'call', _1617 => _1617(), 'access', _1618 => _1618.getResolvedSignature, 'call', _1619 => _1619(node), 'optionalAccess', _1620 => _1620.declaration]);
    if (_optionalChain([declaration, 'optionalAccess', _1621 => _1621.name, 'optionalAccess', _1622 => _1622.escapedText]) !== name) {
      return false;
    }
    return index === 0;
  }
  // the D2 scan over a nullable boolean local: every write prints a Java boolean value or a
  // proven Boolean-or-null box, so no path can leave a truthy non-boolean box in it
  javaBooleanNullableWritesAreBoxed(symbol, declaration, node, seen) {
    const next = new Set(seen);
    next.add(node);
    next.add(declaration);
    let fn = declaration.parent;
    while (fn !== void 0 && !_typescript2.default.isFunctionLike(fn)) {
      fn = fn.parent;
    }
    if (fn === void 0) {
      return false;
    }
    if (declaration.initializer !== void 0 && !this.javaPrintsBooleanBoxValue(declaration.initializer, next)) {
      return false;
    }
    let ok = true;
    const scan = (current) => {
      if (!ok) {
        return;
      }
      if ((_typescript2.default.isForOfStatement(current) || _typescript2.default.isForInStatement(current)) && _typescript2.default.isIdentifier(current.initializer)) {
        let loop;
        const checker = this.checkerOrUndefined();
        if (checker === void 0) {
          loop = void 0;
        }
        loop = checker.getSymbolAtLocation(current.initializer);
        if (loop === symbol) {
          ok = false;
          return;
        }
      }
      if (_typescript2.default.isBinaryExpression(current) && JAVA_ASSIGNMENT_OPERATOR_KINDS.has(current.operatorToken.kind)) {
        if (_typescript2.default.isIdentifier(current.left)) {
          let left;
          const checker = this.checkerOrUndefined();
          if (checker === void 0) {
            left = void 0;
          }
          left = checker.getSymbolAtLocation(current.left);
          if (left === symbol && !this.javaPrintsBooleanBoxValue(current.right, next)) {
            ok = false;
            return;
          }
        } else if (_typescript2.default.isArrayLiteralExpression(current.left)) {
          const index = current.left.elements.findIndex((element) => {
            if (!_typescript2.default.isIdentifier(element)) {
              return false;
            }
            let elementSymbol;
            const checker = this.checkerOrUndefined();
            if (checker === void 0) {
              elementSymbol = void 0;
            }
            elementSymbol = checker.getSymbolAtLocation(element);
            return elementSymbol === symbol;
          });
          if (index !== -1 && !this.javaBooleanBoxTupleElement(current.right, index)) {
            ok = false;
            return;
          }
        }
      }
      _typescript2.default.forEachChild(current, scan);
    };
    scan(fn);
    return ok;
  }
  // `Helpers.isTrue(x)` where the DECLARED type of x is a nullable boolean and every write is a
  // proven Boolean-or-null box: `Boolean.TRUE.equals(x)` is exactly what the helper answers on
  // such a box (null and FALSE test false, TRUE tests true)
  javaNullableBooleanBoxIdentifier(node, seen = /* @__PURE__ */ new Set()) {
    if (_optionalChain([node, 'optionalAccess', _1623 => _1623.kind]) !== _typescript2.default.SyntaxKind.Identifier) {
      return void 0;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const symbol = checker.getSymbolAtLocation(node);
    const declaration = _optionalChain([symbol, 'optionalAccess', _1624 => _1624.valueDeclaration]);
    if (declaration === void 0 || _optionalChain([declaration, 'access', _1625 => _1625.name, 'optionalAccess', _1626 => _1626.escapedText]) !== node.escapedText) {
      return void 0;
    }
    if (seen.has(declaration)) {
      return void 0;
    }
    if (!this.javaNullableBooleanDeclaration(declaration)) {
      return void 0;
    }
    if (!this.javaBooleanNullableWritesAreBoxed(symbol, declaration, node, seen)) {
      return void 0;
    }
    return this.printNode(node, 0);
  }
  // the native Java a falsy wrapper around this condition prints, or undefined to keep
  // `Helpers.isTrue(...)`: a hand-written boolean field prints bare; `Array.isArray(x)` prints
  // `Helpers.isArray(x)` (null -> false like helper); a proven Boolean box: `Boolean.TRUE.equals(x)`
  javaBooleanWrapperFreeCondition(node) {
    if (node === void 0) {
      return void 0;
    }
    if (node.kind === _typescript2.default.SyntaxKind.ParenthesizedExpression) {
      return this.javaBooleanWrapperFreeCondition(node.expression);
    }
    if (this.isArrayIsArrayCall(node)) {
      return `(${this.printNode(node.arguments[0], 0)} instanceof java.util.List)`;
    }
    const field = this.javaBooleanBaseField(node);
    if (field !== void 0) {
      return field;
    }
    if (node.kind === _typescript2.default.SyntaxKind.CallExpression && this.javaCallBooleanKind(node) === void 0 && this.javaCallReturnsBooleanBox(node, /* @__PURE__ */ new Set(), 0)) {
      return `Boolean.TRUE.equals(${this.printNode(node, 0)})`;
    }
    if (node.kind === _typescript2.default.SyntaxKind.Identifier) {
      const declared = this.javaDeclaredBooleanKind(node);
      if (declared === "boolean") {
        return this.printNode(node, 0);
      }
      if (declared === "Boolean") {
        return `Boolean.TRUE.equals(${this.printNode(node, 0)})`;
      }
    }
    const identifier = this.javaBooleanBoxIdentifier(node, /* @__PURE__ */ new Set());
    if (identifier !== void 0) {
      return `Boolean.TRUE.equals(${identifier})`;
    }
    const nullableBox = this.javaNullableBooleanBoxIdentifier(node);
    if (nullableBox !== void 0) {
      return `Boolean.TRUE.equals(${nullableBox})`;
    }
    return void 0;
  }
  printCondition(node, identation) {
    if (this.javaConditionPrintsBoolean(node)) {
      return this.getIden(identation) + this.printNode(node, 0);
    }
    const wrapperFree = this.javaBooleanWrapperFreeCondition(node);
    if (wrapperFree !== void 0) {
      return this.getIden(identation) + wrapperFree;
    }
    const callKind = this.javaCallBooleanKind(node);
    if (callKind === "boolean") {
      return this.getIden(identation) + this.printNode(node, 0);
    }
    if (callKind === "nullableBoolean") {
      return this.getIden(identation) + `Boolean.TRUE.equals(${this.printNode(node, 0)})`;
    }
    return super.printCondition(node, identation);
  }
  printConditionalExpression(node, _identation) {
    const condition = this.printCondition(node.condition, 0);
    const whenTrue = this.printNode(node.whenTrue, 0);
    const whenFalse = this.printNode(node.whenFalse, 0);
    return `((${condition})) ? ${whenTrue} : ${whenFalse}`;
  }
  printDeleteExpression(node, _identation) {
    const object = this.printNode(node.expression.expression, 0);
    const key = this.printNode(node.expression.argumentExpression, 0);
    return `((java.util.Map<String,Object>)${object}).remove((String)${key})`;
  }
  printThrowStatement(node, identation) {
    if (node.expression.kind === _typescript2.default.SyntaxKind.Identifier) {
      const name = this.printNode(node.expression, 0);
      return this.getIden(identation) + `${this.THROW_TOKEN} (${name} instanceof RuntimeException ? (RuntimeException)${name} : new RuntimeException(${name}))${this.LINE_TERMINATOR}`;
    }
    if (node.expression.kind === _typescript2.default.SyntaxKind.NewExpression) {
      const expression = node.expression;
      const argumentsExp = _nullishCoalesce(_optionalChain([expression, 'optionalAccess', _1627 => _1627.arguments]), () => ( []));
      const parsedArg = _nullishCoalesce(argumentsExp.map((n) => this.printNode(n, 0)).join(","), () => ( ""));
      const newExpression = this.printNode(expression.expression, 0);
      if (expression.expression.kind === _typescript2.default.SyntaxKind.Identifier) {
        const id = expression.expression;
        const exceptionName = id.escapedText === "Error" ? "RuntimeException" : id.escapedText;
        const symbol = this.getChecker().getSymbolAtLocation(expression.expression);
        if (symbol) {
          const declarations = _nullishCoalesce(_optionalChain([this, 'access', _1628 => _1628.getChecker, 'call', _1629 => _1629(), 'access', _1630 => _1630.getDeclaredTypeOfSymbol, 'call', _1631 => _1631(symbol), 'access', _1632 => _1632.symbol, 'optionalAccess', _1633 => _1633.declarations]), () => ( []));
          const isClassDeclaration = declarations.find(
            (l) => l.kind === _typescript2.default.SyntaxKind.InterfaceDeclaration || l.kind === _typescript2.default.SyntaxKind.ClassDeclaration
          );
          if (isClassDeclaration) {
            return this.getIden(identation) + `${this.THROW_TOKEN} ${this.NEW_TOKEN} ${exceptionName}((String)${parsedArg}) ${this.LINE_TERMINATOR}`;
          } else {
            return this.getIden(identation) + `Helpers.throwDynamicException(${exceptionName}, ${parsedArg});return null;`;
          }
        }
        return this.getIden(identation) + `${this.THROW_TOKEN} ${this.NEW_TOKEN} ${exceptionName}(${parsedArg}) ${this.LINE_TERMINATOR}`;
      } else if (expression.expression.kind === _typescript2.default.SyntaxKind.ElementAccessExpression) {
        return this.getIden(identation) + `Helpers.throwDynamicException(${newExpression}, ${parsedArg});`;
      }
      return super.printThrowStatement(node, identation);
    }
  }
  printPropertyAccessModifiers(node) {
    let modifiers = this.printModifiers(node);
    if (modifiers === "") {
      modifiers = this.defaultPropertyAccess;
    }
    let typeText = "Object";
    if (node.type) {
      typeText = this.getType(node);
      if (!typeText) {
        if (node.type.kind === _typescript2.default.SyntaxKind.AnyKeyword) {
          typeText = this.OBJECT_KEYWORD + " ";
        }
      }
    }
    return modifiers + " " + typeText + " ";
  }
  printModifiers(node) {
    let modifiers = node.modifiers;
    if (modifiers === void 0) {
      return "";
    }
    modifiers = modifiers.filter((mod) => this.FuncModifiers[mod.kind]);
    modifiers = modifiers.filter((mod) => mod.kind !== _typescript2.default.SyntaxKind.AsyncKeyword);
    const res = modifiers.map((modifier) => this.FuncModifiers[modifier.kind]).join(" ");
    return res;
  }
  printObjectLiteralExpression(node, identation) {
    if (node.properties.length > 0 && node.properties.every((p) => _typescript2.default.isPropertyAssignment(p)) && this.objectLiteralCapturesReassigned(node)) {
      this.builderObjectLiterals.add(node);
      return this.printObjectLiteralBuilder(node, identation);
    }
    this.builderObjectLiterals.delete(node);
    const objectBody = this.printObjectLiteralBody(node, identation);
    const formattedObjectBody = objectBody ? "\n" + objectBody + "\n" + this.getIden(identation) : objectBody;
    return this.OBJECT_OPENING + formattedObjectBody + this.OBJECT_CLOSING;
  }
  printObjectLiteralBuilder(node, identation) {
    const keys = this.objectLiteralCapturedKeys(node);
    const saved = keys.map((key) => this.ReassignedVars[key]);
    try {
      return this.printObjectLiteralBuilderText(node, identation);
    } finally {
      keys.forEach((key, i) => {
        if (saved[i] === void 0) {
          delete this.ReassignedVars[key];
        } else {
          this.ReassignedVars[key] = saved[i];
        }
      });
    }
  }
  printObjectLiteralBuilderText(node, identation) {
    const props = node.properties;
    const lines = props.map((prop, i) => {
      const name = this.printNode(prop.name, 0);
      const custom = this.printCustomRightSidePropertyAssignment(prop.initializer, identation + 1);
      const value = (custom ? custom : this.printNode(prop.initializer, identation + 1)).trim();
      let comment = this.printTraillingComment(prop, identation + 1);
      comment = comment ? " " + comment : "";
      const sep = i < props.length - 1 ? "," : "";
      return this.getIden(identation + 1) + name + ", " + value + sep + comment;
    });
    return "Helpers.newMap(\n" + lines.join("\n") + "\n" + this.getIden(identation) + ")";
  }
  printObjectLiteralBody(node, identation) {
    const body = node.properties.map((p) => this.printNode(p, identation + 1)).join("\n");
    return body;
  }
  printForStatement(node, identation) {
    const initializer = this.printNode(node.initializer, 0).replace("Object ", "var ");
    const condition = this.printNode(node.condition, 0);
    const incrementor = this.printNode(node.incrementor, 0);
    const forStm = this.getIden(identation) + this.FOR_TOKEN + " " + this.CONDITION_OPENING + initializer + "; " + condition + "; " + incrementor + this.CONDITION_CLOSE + this.printBlock(node.statement, identation);
    return this.printNodeCommentsIfAny(node, identation, forStm);
  }
  printReturnStatement(node, identation) {
    const leadingComment = this.printLeadingComments(node, identation);
    let trailingComment = this.printTraillingComment(node, identation);
    trailingComment = trailingComment ? " " + trailingComment : trailingComment;
    let exp = node.expression;
    if (exp && exp.kind === _typescript2.default.SyntaxKind.AsExpression && (exp.expression.kind === _typescript2.default.SyntaxKind.ObjectLiteralExpression || _typescript2.default.SyntaxKind.CallExpression)) {
      exp = exp.expression;
    }
    let rightPart = exp ? " " + this.printNode(exp, identation) : "";
    rightPart = rightPart.trim();
    if (!rightPart) {
      let parent = node.parent;
      while (parent) {
        if (_typescript2.default.isFunctionDeclaration(parent) || _typescript2.default.isMethodDeclaration(parent) || _typescript2.default.isFunctionExpression(parent) || _typescript2.default.isArrowFunction(parent)) {
          if (this.isAsyncFunction(parent)) {
            rightPart = "null";
          }
          break;
        }
        parent = parent.parent;
      }
    }
    rightPart = rightPart ? " " + rightPart + this.LINE_TERMINATOR : this.LINE_TERMINATOR;
    return leadingComment + this.getIden(identation) + this.RETURN_TOKEN + rightPart + trailingComment;
  }
  allBranchesTerminate(node) {
    if (_typescript2.default.isReturnStatement(node) || _typescript2.default.isThrowStatement(node)) {
      return true;
    }
    if (_typescript2.default.isBlock(node)) {
      const stmts = node.statements;
      return stmts.length > 0 && this.allBranchesTerminate(stmts[stmts.length - 1]);
    }
    if (_typescript2.default.isIfStatement(node)) {
      if (!node.elseStatement) {
        return false;
      }
      return this.allBranchesTerminate(node.thenStatement) && this.allBranchesTerminate(node.elseStatement);
    }
    return false;
  }
};

// src/rustTranspiler.ts
init_cjs_shims();

var SyntaxKind4 = _typescript2.default.SyntaxKind;
var parserConfig6 = {
  "ELSEIF_TOKEN": "else if",
  "OBJECT_OPENING": "{",
  "OBJECT_CLOSING": "}",
  "ARRAY_OPENING_TOKEN": "Value::from(vec![",
  "ARRAY_CLOSING_TOKEN": "])",
  "PROPERTY_ASSIGNMENT_TOKEN": ":",
  "VAR_TOKEN": "let mut",
  "METHOD_TOKEN": "fn",
  "FUNCTION_TOKEN": "fn",
  "PROPERTY_ASSIGNMENT_OPEN": "",
  "PROPERTY_ASSIGNMENT_CLOSE": "",
  "SUPER_TOKEN": "super",
  "SUPER_CALL_TOKEN": "super",
  "FALSY_WRAPPER_OPEN": "is_true(&",
  "FALSY_WRAPPER_CLOSE": ")",
  "COMPARISON_WRAPPER_OPEN": "is_equal(&",
  "COMPARISON_WRAPPER_CLOSE": ")",
  "UKNOWN_PROP_WRAPPER_OPEN": "",
  "UNKOWN_PROP_WRAPPER_CLOSE": "",
  "UKNOWN_PROP_ASYNC_WRAPPER_OPEN": "",
  "UNKOWN_PROP_ASYNC_WRAPPER_CLOSE": "",
  "DYNAMIC_CALL_OPEN": "",
  "EQUALS_EQUALS_WRAPPER_OPEN": "is_equal(&",
  "EQUALS_EQUALS_WRAPPER_CLOSE": ")",
  "DIFFERENT_WRAPPER_OPEN": "!is_equal(&",
  "DIFFERENT_WRAPPER_CLOSE": ")",
  "GREATER_THAN_WRAPPER_OPEN": "is_greater_than(&",
  "GREATER_THAN_WRAPPER_CLOSE": ")",
  "GREATER_THAN_EQUALS_WRAPPER_OPEN": "is_greater_than_or_equal(&",
  "GREATER_THAN_EQUALS_WRAPPER_CLOSE": ")",
  "LESS_THAN_WRAPPER_OPEN": "is_less_than(&",
  "LESS_THAN_WRAPPER_CLOSE": ")",
  "LESS_THAN_EQUALS_WRAPPER_OPEN": "is_less_than_or_equal(&",
  "LESS_THAN_EQUALS_WRAPPER_CLOSE": ")",
  "PLUS_WRAPPER_OPEN": "add(&",
  "PLUS_WRAPPER_CLOSE": ")",
  "MINUS_WRAPPER_OPEN": "subtract(&",
  "MINUS_WRAPPER_CLOSE": ")",
  "ARRAY_LENGTH_WRAPPER_OPEN": "get_array_length(&",
  "ARRAY_LENGTH_WRAPPER_CLOSE": ")",
  "DIVIDE_WRAPPER_OPEN": "divide(&",
  "DIVIDE_WRAPPER_CLOSE": ")",
  "MULTIPLY_WRAPPER_OPEN": "multiply(&",
  "MULTIPLY_WRAPPER_CLOSE": ")",
  "INDEXOF_WRAPPER_OPEN": "get_index_of(&",
  "INDEXOF_WRAPPER_CLOSE": ")",
  "MOD_WRAPPER_OPEN": "mod_val(&",
  "MOD_WRAPPER_CLOSE": ")",
  "LINE_TERMINATOR": ";",
  "CONDITION_OPENING": "",
  "CONDITION_CLOSE": "",
  "AWAIT_TOKEN": "",
  "NULL_TOKEN": "Value::Null",
  "UNDEFINED_TOKEN": "Value::Null",
  "WHILE_TOKEN": "while",
  "ELEMENT_ACCESS_WRAPPER_OPEN": "get_value(&",
  "ELEMENT_ACCESS_WRAPPER_CLOSE": ")",
  "DEFAULT_PARAMETER_TYPE": "Value",
  "DEFAULT_RETURN_TYPE": "Value",
  "BLOCK_OPENING_TOKEN": "{",
  "TRUE_KEYWORD": "Value::Bool(true)",
  "FALSE_KEYWORD": "Value::Bool(false)"
};
var RUST_PARAM_SHADOWS = {
  MAP: "map",
  LIST: "list",
  /** `this.<safe*>` reads inlined natively against a shadowed dict param. */
  SAFE_READS: /* @__PURE__ */ new Set([
    "safeValue",
    "safeString",
    "safeInteger",
    "safeNumber",
    "safeBool",
    "safeDict",
    "safeList"
  ])
};
var RUST_DECLARED_DICT_LOCALS = {
  /** value the resolver answers for a proven Dict local */
  DICT: "dict",
  /** printed `self.<callee>` -> index of its `optionalArgs` parameter.
   *  `safe_dict*` returns `optional_args[0]` whenever the key holds a non-Dict. */
  SAFE_CALLEES: {
    safe_dict_k: 2,
    safe_dict: 2,
    safe_dict_n: 2,
    safe_dict2: 3
  },
  /** `&mut` receivers whose writes land inside the container, so a Dict local
   *  stays a Dict (`add_element_to_object` / `append_to_array` no-op on a
   *  non-container, `set_value` / `remove` write a key). */
  KIND_PRESERVING_MUTATORS: /* @__PURE__ */ new Set([
    "add_element_to_object",
    "append_to_array",
    "set_value",
    "remove",
    "get_value_mut"
  ])
};
function rustIsAssignmentOperator(kind) {
  return kind === _typescript2.default.SyntaxKind.EqualsToken || kind >= _typescript2.default.SyntaxKind.PlusEqualsToken && kind <= _typescript2.default.SyntaxKind.CaretEqualsToken;
}
var _RustTranspiler = class _RustTranspiler extends BaseTranspiler {
  constructor(config = {}) {
    config["parser"] = Object.assign({}, parserConfig6, _nullishCoalesce(config["parser"], () => ( {})));
    super(config);
    this.rustStringLocalDecisions = /* @__PURE__ */ new Map();
    // ── native `Option<String>` returns (`: Str` methods) ─────────────────────
    //
    // An internal, non-override, non-async method declared `: Str`
    this.rustNativeStrReturnDecisions = /* @__PURE__ */ new WeakMap();
    this.requiresParameterType = true;
    this.requiresReturnType = false;
    this.asyncTranspiling = true;
    this.supportsFalsyOrTruthyValues = false;
    this.id = "Rust";
    this.className = "undefined";
    this.methodSignatures = {};
    this.forLoopCounter = 0;
    this.initConfig();
    this.applyUserOverrides(config);
  }
  initConfig() {
    this.LeftPropertyAccessReplacements = {};
    this.RightPropertyAccessReplacements = {};
    this.FullPropertyAccessReplacements = {
      "console.log": "println_val",
      "Math.floor": "math_floor",
      "Math.ceil": "math_ceil",
      "Math.round": "math_round"
    };
    this.CallExpressionReplacements = {};
    this.ReservedKeywordsReplacements = {
      "type": "type_var",
      "move": "move_val",
      "ref": "ref_val",
      "str": "str_val",
      "use": "use_val",
      "mod": "mod_kw",
      "loop": "loop_val",
      "match": "match_val",
      "where": "where_val",
      "final": "final_val",
      "box": "box_val",
      "become": "become_val",
      "priv": "priv_val",
      "override": "override_val",
      "unsized": "unsized_val",
      "async": "async_val",
      "await": "await_val",
      "try": "try_val",
      "abstract": "abstract_val",
      "dyn": "dyn_val",
      "fn": "fn_val",
      "impl": "impl_val",
      "pub": "pub_val",
      "self": "self_val",
      "super": "super_val",
      "crate": "crate_val"
    };
    this.binaryExpressionsWrappers = {
      [SyntaxKind4.EqualsEqualsToken]: ["is_equal(", ")"],
      [SyntaxKind4.EqualsEqualsEqualsToken]: ["is_equal(", ")"],
      [SyntaxKind4.ExclamationEqualsToken]: ["!is_equal(", ")"],
      [SyntaxKind4.ExclamationEqualsEqualsToken]: ["!is_equal(", ")"],
      [SyntaxKind4.GreaterThanToken]: ["is_greater_than(", ")"],
      [SyntaxKind4.GreaterThanEqualsToken]: ["is_greater_than_or_equal(", ")"],
      [SyntaxKind4.LessThanToken]: ["is_less_than(", ")"],
      [SyntaxKind4.LessThanEqualsToken]: ["is_less_than_or_equal(", ")"],
      [SyntaxKind4.PlusToken]: ["add(", ")"],
      [SyntaxKind4.MinusToken]: ["subtract(", ")"],
      [SyntaxKind4.AsteriskToken]: ["multiply(", ")"],
      [SyntaxKind4.PercentToken]: ["mod_val(", ")"],
      [SyntaxKind4.SlashToken]: ["divide(", ")"]
    };
  }
  capitalize(str) {
    return str[0].toUpperCase() + str.slice(1);
  }
  // Escaped Rust string literal for the given TS literal text — shared by
  // the `Value::Str(..)` form and the `&str` key form below.
  quotedStringLiteral(text) {
    const backslashPlaceholder = "\0";
    text = text.replaceAll("\\", backslashPlaceholder);
    text = text.replaceAll("\b", "\\b");
    text = text.replaceAll("\f", "\\f");
    text = text.replaceAll("\n", "\\n");
    text = text.replaceAll("\r", "\\r");
    text = text.replaceAll("	", "\\t");
    text = text.replaceAll(backslashPlaceholder, "\\\\");
    text = text.replaceAll('"', '\\"');
    return `"${text}"`;
  }
  printStringLiteral(node) {
    const text = node.text;
    if (text in this.StringLiteralReplacements) {
      return this.StringLiteralReplacements[text];
    }
    return `Value::Str(${this.quotedStringLiteral(text)}.into())`;
  }
  printNumericLiteral(node) {
    const text = node.text;
    if (text.includes(".") || /[eE]/.test(text)) {
      return `Value::Float(${text})`;
    }
    return `Value::Int(${text})`;
  }
  printBooleanLiteral(node) {
    if (_typescript2.default.SyntaxKind.TrueKeyword === node.kind) {
      return "Value::Bool(true)";
    }
    return "Value::Bool(false)";
  }
  printNullKeyword(node, identation) {
    return "Value::Null";
  }
  // Primitive kind the checker proves for `type`; a union keeps the kind only
  // when every non-nullable member is that same primitive.
  primitiveKindOfType(type) {
    if (type === void 0) {
      return void 0;
    }
    const flags = type.flags;
    if (this.isStringType(flags)) {
      return "string";
    }
    if (flags === _typescript2.default.TypeFlags.Number || flags === _typescript2.default.TypeFlags.NumberLiteral) {
      return "number";
    }
    if (flags === _typescript2.default.TypeFlags.Boolean || flags === _typescript2.default.TypeFlags.BooleanLiteral) {
      return "boolean";
    }
    if (flags & _typescript2.default.TypeFlags.Union) {
      let kind = void 0;
      for (const member of _nullishCoalesce(type.types, () => ( []))) {
        if (member.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null)) {
          continue;
        }
        const memberKind = this.primitiveKindOfType(member);
        if (memberKind === void 0 || kind !== void 0 && kind !== memberKind) {
          return void 0;
        }
        kind = memberKind;
      }
      return kind;
    }
    return void 0;
  }
  // Native truthiness of a checker-proved boolean Value: `is_true(&v)` is `v.is_truthy()`
  // (`""`/`0`/`[]`/`{}`/Null are false). When the operand is proven `bool`/`undefined` only,
  // the runtime value is `Value::Bool(..)` or `Value::Null`, so `matches!(v, Value::Bool(true))`.
  /** `true` / `false` / `boolean` (a union of BooleanLiteral members too). */
  isBooleanValueType(type) {
    if (type === void 0) {
      return false;
    }
    if (type.flags & _typescript2.default.TypeFlags.Union) {
      const members = _nullishCoalesce(type.types, () => ( []));
      return members.length > 0 && members.every((member) => this.isBooleanValueType(member));
    }
    return (type.flags & (_typescript2.default.TypeFlags.Boolean | _typescript2.default.TypeFlags.BooleanLiteral)) !== 0;
  }
  /** `boolean | undefined`: `undefined`/`null` both print `Value::Null` (false
   *  for the helper and for the `matches!` alike), so they may join the union. */
  isBooleanOrUndefinedType(type) {
    if (type === void 0) {
      return false;
    }
    const members = type.flags & _typescript2.default.TypeFlags.Union ? _nullishCoalesce(type.types, () => ( [])) : [type];
    if (members.length === 0) {
      return false;
    }
    const onlyBooleanOrEmpty = members.every((member) => this.isBooleanValueType(member) || (member.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Void | _typescript2.default.TypeFlags.Null)) !== 0);
    return onlyBooleanOrEmpty && members.some((member) => this.isBooleanValueType(member));
  }
  /** Operands this unit owns: `safeBool`/`safeBool2`/`safeBoolN` calls (a
   *  `Value` in the port) and element accesses (printed as `get_value`). */
  isBooleanValueFamilyOperand(node) {
    const inner = this.unwrapParens(node);
    if (inner === void 0) {
      return false;
    }
    if (_typescript2.default.isElementAccessExpression(inner)) {
      return true;
    }
    if (_typescript2.default.isCallExpression(inner)) {
      const name = this.callExpressionName(inner);
      return name === "safeBool" || name === "safeBool2" || name === "safeBoolN";
    }
    return false;
  }
  /** The emitted `matches!` is a bare Rust `bool`: it is only valid where the
   *  whole enclosing boolean expression already sits in a bool slot. A logical
   *  expression stored in a `Value` slot gets its `Value::Bool(..)` box from the
   *  ccxt post-passes, which key on the leading helper token the operand would
   *  no longer provide. */
  isBareBoolEmissionSafe(node) {
    let current = node;
    let parent = current.parent;
    while (parent !== void 0) {
      if (_typescript2.default.isParenthesizedExpression(parent)) {
        current = parent;
        parent = parent.parent;
        continue;
      }
      if (parent.kind === SyntaxKind4.PrefixUnaryExpression && parent.operator === SyntaxKind4.ExclamationToken) {
        current = parent;
        parent = parent.parent;
        continue;
      }
      if (parent.kind === SyntaxKind4.BinaryExpression && (parent.operatorToken.kind === SyntaxKind4.AmpersandAmpersandToken || parent.operatorToken.kind === SyntaxKind4.BarBarToken)) {
        current = parent;
        parent = parent.parent;
        continue;
      }
      break;
    }
    return this.isBooleanPosition(current);
  }
  // B-26 extends the proof with the printer's own bool-typed sinks: a narrowed `let x: bool = …`
  // (getRustBoolLocalInitializer) and a logical boxed in `Value::Bool(…)`
  // (printCustomBinaryExpressionIfAny) both demand a `bool`, so their operands may print bare.
  rustConditionBoolSlot(node) {
    if (this.isBareBoolEmissionSafe(node)) {
      return true;
    }
    let current = node;
    let parent = current.parent;
    while (parent !== void 0) {
      if (_typescript2.default.isParenthesizedExpression(parent) || parent.kind === SyntaxKind4.PrefixUnaryExpression && parent.operator === SyntaxKind4.ExclamationToken || parent.kind === SyntaxKind4.BinaryExpression && (parent.operatorToken.kind === SyntaxKind4.AmpersandAmpersandToken || parent.operatorToken.kind === SyntaxKind4.BarBarToken)) {
        current = parent;
        parent = parent.parent;
        continue;
      }
      break;
    }
    const inner = this.unwrapParens(current);
    if (inner !== void 0 && inner.kind === SyntaxKind4.BinaryExpression && (inner.operatorToken.kind === SyntaxKind4.AmpersandAmpersandToken || inner.operatorToken.kind === SyntaxKind4.BarBarToken) && (this.hasNativeComparisonOperand(inner.left) || this.hasNativeComparisonOperand(inner.right))) {
      return true;
    }
    const declaration = current.parent;
    if (declaration === void 0 || !_typescript2.default.isVariableDeclaration(declaration) || declaration.initializer !== current || _optionalChain([declaration, 'access', _1634 => _1634.name, 'optionalAccess', _1635 => _1635.kind]) !== SyntaxKind4.Identifier) {
      return false;
    }
    return this.rustNodeIsBoolExpression(declaration.initializer) && this.rustTypeIsBoolean(declaration.initializer) && this.rustLocalUsesAcceptBool(declaration, String(declaration.name.escapedText));
  }
  /** Native truthiness text of the operand, or undefined to keep `is_true`. */
  printNativeTruthiness(node) {
    if (!this.isBooleanValueFamilyOperand(node)) {
      return void 0;
    }
    if (!this.printsValueExpression(node)) {
      return void 0;
    }
    if (!this.isBooleanOrUndefinedType(this.typeOfNodeIfAny(node))) {
      return void 0;
    }
    if (!this.isBareBoolEmissionSafe(node)) {
      return void 0;
    }
    return `matches!(${this.printNode(node, 0)}, Value::Bool(true))`;
  }
  // Kind of a literal operand whose printed Value variant is exactly known.
  literalKindOfNode(node) {
    if (node === void 0) {
      return void 0;
    }
    switch (node.kind) {
      case SyntaxKind4.StringLiteral:
        return "string";
      case SyntaxKind4.NumericLiteral:
        return "number";
      case SyntaxKind4.TrueKeyword:
      case SyntaxKind4.FalseKeyword:
        return "boolean";
      case SyntaxKind4.NullKeyword:
        return "null";
      case SyntaxKind4.Identifier:
        return node.escapedText === "undefined" ? "null" : void 0;
    }
    return void 0;
  }
  // Does printNode() render `node` as a Rust `Value` (and not a bare bool)?
  printsValueExpression(node) {
    if (node === void 0) {
      return false;
    }
    switch (node.kind) {
      case SyntaxKind4.ParenthesizedExpression:
        return this.printsValueExpression(node.expression);
      case SyntaxKind4.AwaitExpression:
        return this.printsValueExpression(node.expression);
      case SyntaxKind4.BinaryExpression:
        return !_RustTranspiler.BOOL_PRODUCING_OPERATORS.has(node.operatorToken.kind);
      case SyntaxKind4.PrefixUnaryExpression:
        return node.operator !== SyntaxKind4.ExclamationToken;
      case SyntaxKind4.CallExpression:
        return !_RustTranspiler.BOOL_PRODUCING_CALLS.has(this.callExpressionName(node));
      case SyntaxKind4.Identifier:
      case SyntaxKind4.PropertyAccessExpression:
      case SyntaxKind4.ElementAccessExpression:
        return !this.isClassInstanceType(this.getChecker().getTypeAtLocation(node));
      case SyntaxKind4.StringLiteral:
      case SyntaxKind4.NumericLiteral:
      case SyntaxKind4.TrueKeyword:
      case SyntaxKind4.FalseKeyword:
      case SyntaxKind4.NullKeyword:
      case SyntaxKind4.ArrayLiteralExpression:
      case SyntaxKind4.ObjectLiteralExpression:
      case SyntaxKind4.ConditionalExpression:
        return true;
    }
    return false;
  }
  // Class instances are emitted as their Rust struct (not a Value), so they
  // can neither be compared to Value::Null nor unwrapped with as_*().
  callExpressionName(node) {
    const expression = node.expression;
    if (_typescript2.default.isIdentifier(expression)) {
      return expression.escapedText;
    }
    if (_typescript2.default.isPropertyAccessExpression(expression)) {
      return expression.name.escapedText;
    }
    return "";
  }
  // Does a string (literal text or literal-type value) parse as a number?
  // is_equal() coerces those against numeric/bool operands, a plain string
  // compare does not.
  textCoercesToNumber(text) {
    if (/^[+-]?(\d+\.?\d*|\.\d+)([eE][+-]?\d+)?$/.test(text)) {
      return true;
    }
    return /^[+-]?(inf|infinity|nan)$/i.test(text);
  }
  stringLiteralCoercesToNumber(node) {
    return this.textCoercesToNumber(node.text);
  }
  // f64 literal text for a numeric literal; undefined when it is not a Rust
  // decimal/float literal (hex/octal/binary fall back to the helper).
  numericLiteralF64Text(node) {
    const text = node.text;
    if (text.startsWith("0x") || text.startsWith("0o") || text.startsWith("0b")) {
      return void 0;
    }
    if (text.startsWith(".")) {
      return `0${text}`;
    }
    if (text.includes(".") || text.includes("e") || text.includes("E")) {
      return text;
    }
    return `${text}.0`;
  }
  // The printer's own proof that a plain read prints as a Rust `Value`: `this.<field>` (every
  // declared field is `Value`) or an identifier bound to a local/param (a local is narrowed to
  // `bool` only when every use is a condition sink — never an is_equal argument).
  rustReadPrintsValue(node) {
    if (node === void 0) {
      return false;
    }
    if (node.kind === SyntaxKind4.PropertyAccessExpression && node.expression.kind === SyntaxKind4.ThisKeyword) {
      return true;
    }
    if (node.kind !== SyntaxKind4.Identifier) {
      return false;
    }
    const symbol = this.getChecker().getSymbolAtLocation(node);
    const declarations = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1636 => _1636.declarations]), () => ( []));
    if (declarations.length === 0) {
      return false;
    }
    return declarations.every((declaration) => _typescript2.default.isParameter(declaration) || _typescript2.default.isVariableDeclaration(declaration) && _optionalChain([declaration, 'access', _1637 => _1637.initializer, 'optionalAccess', _1638 => _1638.kind]) !== SyntaxKind4.NewExpression);
  }
  // Can the checked type only hold a Bool, Null/undefined or a non-numeric
  // string? Then `x.as_bool() == Some(b)` answers exactly what is_equal(x, b)
  // does: its f64 fallback (Str parse / Bool→0|1) can never fire.
  rustBooleanComparableType(type) {
    if (type === void 0) {
      return false;
    }
    if (type.flags & _typescript2.default.TypeFlags.Union) {
      const members = _nullishCoalesce(type.types, () => ( []));
      return members.length > 0 && members.every((member) => this.rustBooleanComparableType(member));
    }
    if (type.flags & (_typescript2.default.TypeFlags.Boolean | _typescript2.default.TypeFlags.BooleanLiteral)) {
      return true;
    }
    if (type.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null)) {
      return true;
    }
    if (type.flags & _typescript2.default.TypeFlags.StringLiteral) {
      return !this.textCoercesToNumber(String(_nullishCoalesce(type.value, () => ( ""))));
    }
    return false;
  }
  // Native `==`/`!=` on the unwrapped payload when the checker proves the
  // Value variants line up; undefined keeps the is_equal() helper.
  printNativeEqualityComparison(left, right, op) {
    const operator = op === SyntaxKind4.EqualsEqualsToken || op === SyntaxKind4.EqualsEqualsEqualsToken ? "==" : "!=";
    const leftLiteral = this.literalKindOfNode(left);
    const rightLiteral = this.literalKindOfNode(right);
    if (leftLiteral !== void 0 && rightLiteral !== void 0) {
      return void 0;
    }
    if (leftLiteral !== void 0 || rightLiteral !== void 0) {
      const literal = leftLiteral !== void 0 ? left : right;
      const literalKind = _nullishCoalesce(leftLiteral, () => ( rightLiteral));
      const other = leftLiteral !== void 0 ? right : left;
      if (!this.printsValueExpression(other) && (literalKind !== "null" || !this.rustReadPrintsValue(other))) {
        return void 0;
      }
      const otherType = this.getChecker().getTypeAtLocation(other);
      const otherKind = this.primitiveKindOfType(otherType);
      const typedStringLocal = this.rustStringLocalIdentifierIsTyped(other);
      if (literalKind === "null") {
        if (typedStringLocal) {
          return `${this.printNode(other, 0)}.${operator === "==" ? "is_none" : "is_some"}()`;
        }
        return `${this.printNode(other, 0)} ${operator} Value::Null`;
      }
      if (literalKind === "string") {
        if (literal.text in this.StringLiteralReplacements) {
          return void 0;
        }
        if (this.stringLiteralCoercesToNumber(literal) && otherKind !== "string") {
          return void 0;
        }
        const accessor2 = typedStringLocal ? "as_deref" : "as_str";
        return `${this.printNode(other, 0)}.${accessor2}() ${operator} Some(${this.quotedStringLiteral(literal.text)})`;
      }
      if (literalKind === "number") {
        if (otherKind !== "number") {
          return void 0;
        }
        const text = this.numericLiteralF64Text(literal);
        if (text === void 0) {
          return void 0;
        }
        return `${this.printNode(other, 0)}.as_f64() ${operator} Some(${text})`;
      }
      if (literalKind === "boolean") {
        if (otherKind !== "boolean" && !this.rustBooleanComparableType(otherType)) {
          return void 0;
        }
        const value = literal.kind === SyntaxKind4.TrueKeyword ? "true" : "false";
        return `${this.printNode(other, 0)}.as_bool() ${operator} Some(${value})`;
      }
      return void 0;
    }
    if (!this.printsValueExpression(left) || !this.printsValueExpression(right)) {
      return void 0;
    }
    const leftKind = this.primitiveKindOfType(this.getChecker().getTypeAtLocation(left));
    const rightKind = this.primitiveKindOfType(this.getChecker().getTypeAtLocation(right));
    if (leftKind === void 0 || leftKind !== rightKind) {
      return void 0;
    }
    const accessor = _RustTranspiler.PAYLOAD_ACCESSORS[leftKind];
    return `${this.printNode(left, 0)}.${accessor}() ${operator} ${this.printNode(right, 0)}.${accessor}()`;
  }
  // ── checker-typed helper elimination ─────────────────────────────────
  // Each predicate proves a static TS shape for which the native Rust form
  // is exactly what the runtime helper computes; only then is the helper
  // call dropped, anything unproven keeps the helper.
  typeOfNodeIfAny(node) {
    return _optionalChain([this, 'access', _1639 => _1639.checkerOrUndefined, 'call', _1640 => _1640(), 'optionalAccess', _1641 => _1641.getTypeAtLocation, 'call', _1642 => _1642(node)]);
  }
  // Arrays/tuples/strings: `.length` is exactly what `Value::len()` returns.
  // Other shapes (Dict) keep the helper — ArrayCache / OrderBookSide markers
  // hold their length in the marker dict, which get_array_length unwraps.
  isValueLengthType(type) {
    if (type === void 0) {
      return false;
    }
    if (type.flags & _typescript2.default.TypeFlags.Union) {
      const parts = _nullishCoalesce(type.types, () => ( []));
      return parts.length > 0 && parts.every((part) => this.isValueLengthType(part));
    }
    return (type.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.Void)) !== 0 || this.getChecker().isArrayType(type) || this.getChecker().isTupleType(type) || this.isStringType(type.flags);
  }
  printArrayLength(node, identation, leftExpr = void 0) {
    const receiver = _nullishCoalesce(leftExpr, () => ( this.printNode(node.expression, 0)));
    if (this.isValueLengthType(this.typeOfNodeIfAny(node.expression))) {
      return `Value::Int(${receiver}.len() as i64)`;
    }
    return `get_array_length(&${receiver})`;
  }
  // Native string search / slicing: `x.indexOf(y)` and `x.slice(a, b)` on a checker-proven
  // string receiver print native `str` code. The receiver is a `Value`, so the payload is reached
  // via `as_str()`; a `Value::Null` receiver takes the helper's `-1` / `Value::Null` branch.
  /** Literal integer bound of a `slice` call (`3`, `-64`), else undefined. */
  rustSliceLiteralBound(node) {
    if (node === void 0) {
      return void 0;
    }
    if (_typescript2.default.isNumericLiteral(node)) {
      const value = Number(node.text);
      return Number.isSafeInteger(value) ? value : void 0;
    }
    if (_typescript2.default.isPrefixUnaryExpression(node) && node.operator === SyntaxKind4.MinusToken && _typescript2.default.isNumericLiteral(node.operand)) {
      const value = Number(node.operand.text);
      return Number.isSafeInteger(value) ? -value : void 0;
    }
    return void 0;
  }
  // `slice` clamps like JS: a non-negative bound is capped at the length, a
  // negative one counts from the end and is floored at 0.
  rustSliceClampedIndex(value) {
    return value < 0 ? `(__l - ${-value}).max(0)` : `__l.min(${value})`;
  }
  // `x.indexOf("lit")` on a proven string receiver: `str::find` is exactly
  // the helper's `Value::Str` arm (byte index, `-1` when absent).
  printNativeStringIndexOf(node, receiverText) {
    if (node === void 0 || !_typescript2.default.isCallExpression(node) || !_typescript2.default.isPropertyAccessExpression(node.expression) || _optionalChain([node, 'access', _1643 => _1643.arguments, 'optionalAccess', _1644 => _1644.length]) !== 1) {
      return void 0;
    }
    if (this.primitiveKindOfType(this.typeOfNodeIfAny(node.expression.expression)) !== "string") {
      return void 0;
    }
    const needle = node.arguments[0];
    if (!_typescript2.default.isStringLiteral(needle) && !_typescript2.default.isNoSubstitutionTemplateLiteral(needle)) {
      return void 0;
    }
    if (typeof receiverText !== "string" || receiverText.includes("\n")) {
      return void 0;
    }
    const literal = this.escapeRustStringLiteral(needle.text);
    return `Value::Int(${receiverText}.as_str().and_then(|__s| __s.find("${literal}")).map(|__i| __i as i64).unwrap_or(-1))`;
  }
  // `x.slice(a)` / `x.slice(a, b)` with literal bounds on a proven string
  // receiver: the helper's char-vector clamps are inlined, so the emission
  // returns the same string (and `Value::Null` for a null receiver).
  printNativeStringSlice(node, receiverText) {
    if (node === void 0 || !_typescript2.default.isCallExpression(node) || !_typescript2.default.isPropertyAccessExpression(node.expression)) {
      return void 0;
    }
    const args = _nullishCoalesce(node.arguments, () => ( []));
    if (args.length === 0 || args.length > 2) {
      return void 0;
    }
    if (this.primitiveKindOfType(this.typeOfNodeIfAny(node.expression.expression)) !== "string") {
      return void 0;
    }
    if (typeof receiverText !== "string" || receiverText.includes("\n")) {
      return void 0;
    }
    const start = this.rustSliceLiteralBound(args[0]);
    if (start === void 0) {
      return void 0;
    }
    let end = "__l";
    if (args[1] !== void 0 && args[1].kind !== SyntaxKind4.NullKeyword && args[1].kind !== SyntaxKind4.UndefinedKeyword) {
      const bound = this.rustSliceLiteralBound(args[1]);
      if (bound === void 0) {
        return void 0;
      }
      end = this.rustSliceClampedIndex(bound);
    }
    const begin = this.rustSliceClampedIndex(start);
    return `${receiverText}.as_str().map(|__s| { let __c: Vec<char> = __s.chars().collect(); let __l = __c.len() as i64; let __i = ${begin}; let __j = ${end}; if __i <= __j { __c[__i as usize..__j as usize].iter().collect::<String>() } else { String::new() } }).map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null)`;
  }
  // An identifier bound by a local/param declaration: the printer declares
  // every one of them as `Value`. Imports, classes and function names print
  // as Rust items rather than as values, so they keep the helper.
  isDeclaredValueIdentifier(node) {
    const symbol = this.getChecker().getSymbolAtLocation(node);
    const declarations = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1645 => _1645.declarations]), () => ( []));
    if (declarations.length === 0) {
      return false;
    }
    return declarations.every((declaration) => _typescript2.default.isVariableDeclaration(declaration) || _typescript2.default.isParameter(declaration) || _typescript2.default.isBindingElement(declaration));
  }
  // A declared `Value` place: a local/param identifier, or a field/element
  // access rooted at `this` or at such an identifier. Those are the operands
  // whose printed text the helper already borrows as a `Value`.
  isDeclaredValuePlace(node) {
    const inner = this.unwrapParens(node);
    if (inner === void 0) {
      return false;
    }
    if (_typescript2.default.isIdentifier(inner)) {
      return this.isDeclaredValueIdentifier(inner);
    }
    if (_typescript2.default.isPropertyAccessExpression(inner) || _typescript2.default.isElementAccessExpression(inner)) {
      const root = this.unwrapParens(this.valuePlaceRoot(inner));
      return root !== void 0 && (root.kind === SyntaxKind4.ThisKeyword || this.isDeclaredValueIdentifier(root));
    }
    return false;
  }
  valuePlaceRoot(node) {
    let current = node;
    while (_typescript2.default.isPropertyAccessExpression(current) || _typescript2.default.isElementAccessExpression(current)) {
      current = current.expression;
    }
    return current;
  }
  nativeValuePredicateText(kind, operandNode, printedOperand) {
    const pattern = _RustTranspiler.RUST_TYPE_PREDICATE_PATTERNS[kind];
    if (pattern === void 0 || operandNode === void 0 || printedOperand === void 0 || !this.isDeclaredValuePlace(operandNode)) {
      return void 0;
    }
    return `matches!(${this.ensureRef(printedOperand)}, ${pattern})`;
  }
  // Object-typed values are Dicts at runtime, so `key in obj` is a plain
  // key lookup. Arrays keep the helper: `in_op` searches them element-wise.
  // A `Dict`/`Dictionary<T>` receiver is a Reference to its index-signature
  // interface, so the class/lib guards — not the Reference flag — decide it.
  isDictShapedType(type) {
    if (type === void 0) {
      return false;
    }
    if (type.flags & _typescript2.default.TypeFlags.Union) {
      const parts = _nullishCoalesce(type.types, () => ( []));
      const valueParts = parts.filter((part) => !this.rustTypeIsNullish(part));
      return parts.length > valueParts.length && valueParts.length > 0 && valueParts.every((part) => this.isDictShapedType(part));
    }
    if (!(type.flags & _typescript2.default.TypeFlags.Object)) {
      return false;
    }
    const checker = this.getChecker();
    if (checker.isArrayType(type) || checker.isTupleType(type) || checker.isArrayLikeType(type)) {
      return false;
    }
    if (type.getCallSignatures().length !== 0) {
      return false;
    }
    return !this.isClassInstanceType(type) && !this.isLibDeclaredType(type);
  }
  // `"key" in obj` → `matches!(&obj, Value::Dict(__d) if __d.contains_key("key"))`
  // In the TS AST `key` is the left operand and `obj` the right one.
  printNativeInOperator(key, obj) {
    if (!_typescript2.default.isStringLiteral(key)) {
      return void 0;
    }
    if (!this.isDictShapedType(this.typeOfNodeIfAny(obj))) {
      return void 0;
    }
    const printedKey = this.printStringLiteral(key);
    const keyLiteral = this.rustStringLiteralOf(printedKey);
    if (keyLiteral === void 0) {
      return void 0;
    }
    const objExpr = this.printNode(obj, 0);
    return `Value::Bool(matches!(&${objExpr}, Value::Dict(__d) if __d.contains_key(${keyLiteral})))`;
  }
  // The Rust string literal behind a printed TS string literal — the boxed
  // shapes the printer emits today (`Value::Str("k".to_string())`,
  // `Value::from("k")`) plus a bare `"k"` once an arg-shape unit drops the box.
  rustStringLiteralOf(printedKey) {
    const boxed = printedKey.match(/^(?:Value::Str|Value::from)\((.+)\)$/);
    const literal = (boxed ? boxed[1].replace(/\.(?:to_string|into)\(\)$/, "") : printedKey).trim();
    return /^"(?:[^"\\]|\\.)*"$/.test(literal) ? literal : void 0;
  }
  // `X["k"] = v` on a checker-proven plain Dict receiver: mutate the Dict
  // natively. The helper's tag hooks (book meta, ws subscriptions, cache
  // hashmap) only fire on tagged dicts, which a plain object type never is.
  printNativeDictInsert(baseExpr, keyNode, keyText, valueText) {
    const receiver = this.rustNativeInsertReceiver(baseExpr);
    if (receiver === void 0) {
      return void 0;
    }
    if (!this.rustReceiverStaysDict(baseExpr, receiver)) {
      return void 0;
    }
    if (!receiver.isField && this.rustLocalInitReadsTaggedContainer(baseExpr)) {
      return void 0;
    }
    const keyArg = this.rustNativeInsertKeyArg(receiver, keyNode, keyText);
    if (keyArg === void 0) {
      return void 0;
    }
    const name = receiver.text;
    let value = valueText;
    if (this.rustPrintedBoolArg(value)) {
      value = `Value::Bool(${value})`;
    }
    const insert = (val) => `if let Value::Dict(__d) = &mut ${name} { std::sync::Arc::make_mut(__d).insert(${keyArg}, ${val}); }`;
    const readsReceiver = receiver.isField ? /\bself\b/.test(value) : new RegExp(`\\b${name}\\b`).test(value);
    if (readsReceiver) {
      return `{ let __be_tmp = ${value}; ${insert("__be_tmp")} }`;
    }
    const trimmed = value.trim();
    if (/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(trimmed) && trimmed !== "self" && trimmed !== "true" && trimmed !== "false") {
      return insert(`${trimmed}.clone()`);
    }
    return insert(value);
  }
  /** The `insert` key argument. A literal becomes `"k".into()`; a proven
   *  string place becomes `crate::runtime::stringify_param(&k)` — the exact
   *  conversion the helper's dict branch applies to a non-string key, and
   *  `k.to_string()` for a string one. */
  rustNativeInsertKeyArg(receiver, keyNode, keyText) {
    if (_typescript2.default.isStringLiteral(keyNode)) {
      if (_RustTranspiler.RUST_BOOK_META_KEYS.has(keyNode.text) && !receiver.plain) {
        return void 0;
      }
      const keyLiteral = keyText.match(/^Value::Str\((.+)\.(?:to_string|into)\(\)\)$/);
      return keyLiteral === null ? void 0 : `${keyLiteral[1]}.into()`;
    }
    const key = keyText.trim();
    if (!/^[A-Za-z_][A-Za-z0-9_]*$/.test(key)) {
      return void 0;
    }
    if (receiver.isField ? key.includes("self") : new RegExp(`\\b${receiver.text}\\b`).test(key)) {
      return void 0;
    }
    return `crate::runtime::stringify_param(&${key})`;
  }
  // Receivers this unit may write natively: any local whose every path builds
  // a plain `Value::Map` (name-independent — rust-13's four names are the
  // batch-A subset of this proof), any parameter the checker proves is a
  rustNativeInsertReceiver(expr) {
    if (_typescript2.default.isIdentifier(expr)) {
      const plain = this.rustInsertIdentifierReceiver(expr);
      if (plain !== void 0) {
        return { text: expr.text, isField: false, plain, nameNode: expr };
      }
      return void 0;
    }
    if (_typescript2.default.isPropertyAccessExpression(expr) && expr.expression.kind === SyntaxKind4.ThisKeyword && _optionalChain([expr, 'access', _1646 => _1646.name, 'optionalAccess', _1647 => _1647.kind]) === SyntaxKind4.Identifier) {
      return {
        text: `self.${expr.name.text}`,
        isField: true,
        plain: _RustTranspiler.RUST_PLAIN_DICT_FIELDS.has(expr.name.text),
        nameNode: expr.name
      };
    }
    return void 0;
  }
  /** Element-write receiver proof for a local: the batch-A names (rust-13,
   *  `plain: false` — the whitelist is not a construction proof) or, for any
   *  other name, the dict-shape proof plus a plain-`Value::Map` build on
   *  every path (rust-12's proof, read off the checker), or a parameter whose
   *  annotation proves a plain dict and whose writes keep that shape. The
   *  returned flag is the *by-construction* plainness (literal-init locals,
   *  handler tuples, hand-written plain fields) that a book-meta key needs. */
  rustInsertIdentifierReceiver(ident) {
    const declaration = this.rustSingleLocalDeclaration(ident);
    if (declaration !== void 0 && _typescript2.default.isParameter(declaration)) {
      const admitted = this.rustParamStaysPlainDict(declaration) || declaration.initializer !== void 0;
      if (admitted) {
        return false;
      }
    } else if (declaration !== void 0 && _typescript2.default.isVariableDeclaration(declaration)) {
      if (this.rustInsertReceiverBuildsPlainDict(declaration)) {
        return true;
      }
      if (this.rustDeclaredLocalEntry(ident) !== void 0 && this.rustDeclaredInitIsTagFree(declaration)) {
        return false;
      }
    }
    if (_RustTranspiler.RUST_NATIVE_INSERT_RECEIVERS.has(ident.text)) {
      return false;
    }
    return void 0;
  }
  /** A literal initializer must carry no runtime tag key; a call initializer
   *  is the axiom the declared-Dict table itself rests on. */
  rustDeclaredInitIsTagFree(declaration) {
    let init = declaration.initializer;
    while (init !== void 0 && (_typescript2.default.isParenthesizedExpression(init) || _typescript2.default.isNonNullExpression(init) || _typescript2.default.isAsExpression(init))) {
      init = init.expression;
    }
    if (init === void 0 || !_typescript2.default.isObjectLiteralExpression(init)) {
      return true;
    }
    return this.rustPlainDictLiteral(init);
  }
  /** The local's single declaration is initialised from a call that reads
   *  `x.hashmap` / `x.subscriptions` / `x.futures` — element dicts the runtime
   *  tags with a backref so writes reach the shared store, not the COW copy. */
  rustLocalInitReadsTaggedContainer(ident) {
    const declaration = this.rustSingleLocalDeclaration(ident);
    if (declaration === void 0 || !_typescript2.default.isVariableDeclaration(declaration)) {
      return false;
    }
    let init = declaration.initializer;
    while (init !== void 0 && (_typescript2.default.isParenthesizedExpression(init) || _typescript2.default.isNonNullExpression(init) || _typescript2.default.isAsExpression(init))) {
      init = init.expression;
    }
    if (init === void 0 || !_typescript2.default.isCallExpression(init)) {
      return false;
    }
    return init.arguments.some((arg) => {
      let n = arg;
      while (n !== void 0 && (_typescript2.default.isParenthesizedExpression(n) || _typescript2.default.isAsExpression(n) || _typescript2.default.isNonNullExpression(n))) {
        n = n.expression;
      }
      return n !== void 0 && _typescript2.default.isPropertyAccessExpression(n) && _RustTranspiler.RUST_TAGGED_CONTAINER_FIELDS.has(n.name.text);
    });
  }
  /** True when every value the local can hold comes from an object literal:
   *  the runtime tags a dict (`__book_id`, `__ws_subs_url`, `__ws_sub_ref`,
   *  `__cache_backref`) only on handles its own store builds, so the helper's
   *  write-through branches are provably dead and `insert` is the whole
   *  helper. A `[ x, params ] = this.handle…(…)` tuple re-assigns the
   *  hand-written handler's own dict arguments. */
  rustInsertReceiverBuildsPlainDict(declaration) {
    const name = String(declaration.name.escapedText);
    if (!this.rustPlainDictLiteral(declaration.initializer)) {
      return false;
    }
    const scope = this.rustEnclosingFunction(declaration);
    if (scope === void 0) {
      return false;
    }
    const declarationSymbol = this.rustSymbolOf(declaration.name);
    let plain = true;
    const visit = (n) => {
      if (!plain || !_typescript2.default.isBinaryExpression(n) || n.operatorToken.kind !== SyntaxKind4.EqualsToken) {
        _typescript2.default.forEachChild(n, visit);
        return;
      }
      const left = n.left;
      if (_typescript2.default.isIdentifier(left) && String(left.escapedText) === name && (declarationSymbol === void 0 || this.rustSymbolOf(left) === declarationSymbol)) {
        if (!this.rustPlainDictLiteral(n.right) && !this.rustTypeIsUndefinedish(n.right)) {
          plain = false;
        }
      } else if (_typescript2.default.isArrayLiteralExpression(left) && left.elements.some((e) => _typescript2.default.isIdentifier(e) && String(e.escapedText) === name)) {
        if (!this.rustHandlerTupleCall(n.right)) {
          plain = false;
        }
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return plain;
  }
  /** An object literal with no runtime tag key — the transpiler built it, so
   *  it is a fresh plain `Value::Map` on every path. */
  rustPlainDictLiteral(node) {
    if (node === void 0) {
      return false;
    }
    if (_typescript2.default.isParenthesizedExpression(node)) {
      return this.rustPlainDictLiteral(node.expression);
    }
    if (!_typescript2.default.isObjectLiteralExpression(node)) {
      return false;
    }
    return node.properties.every((property) => {
      const key = property.name;
      if (key === void 0) {
        return false;
      }
      const text = _typescript2.default.isStringLiteral(key) ? key.text : _typescript2.default.isIdentifier(key) ? String(key.escapedText) : void 0;
      return text === void 0 || !text.startsWith("__");
    });
  }
  /** `this.handle…(…)` — the hand-written `handle*AndParams` / `handleUntil…`
   *  family; each returns its own request/params dict arguments. */
  rustHandlerTupleCall(node) {
    if (node === void 0 || !_typescript2.default.isCallExpression(node)) {
      return false;
    }
    const callee = node.expression;
    return _typescript2.default.isPropertyAccessExpression(callee) && callee.expression.kind === SyntaxKind4.ThisKeyword && _optionalChain([callee, 'access', _1648 => _1648.name, 'optionalAccess', _1649 => _1649.kind]) === SyntaxKind4.Identifier && /^handle[A-Z]/.test(callee.name.text);
  }
  /** A `null`/`undefined` write leaves the receiver a non-dict, which the
   *  emitted `if let Value::Dict` no-ops exactly like the helper. */
  rustTypeIsUndefinedish(node) {
    if (node.kind === SyntaxKind4.NullKeyword || _typescript2.default.isIdentifier(node) && node.text === "undefined") {
      return true;
    }
    const type = this.typeOfNodeIfAny(node);
    if (type === void 0) {
      return false;
    }
    const mask = _typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.Void;
    return (type.flags & mask) !== 0;
  }
  /** The single variable declaration a local identifier binds to, or
   *  undefined when the checker cannot answer / the binding is not a local. */
  rustSingleLocalDeclaration(ident) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const declarations = _nullishCoalesce(_optionalChain([checker, 'access', _1650 => _1650.getSymbolAtLocation, 'call', _1651 => _1651(ident), 'optionalAccess', _1652 => _1652.declarations]), () => ( []));
    if (declarations.length !== 1) {
      return void 0;
    }
    const declaration = declarations[0];
    if (!_typescript2.default.isVariableDeclaration(declaration) && !_typescript2.default.isParameter(declaration)) {
      return void 0;
    }
    return declaration;
  }
  // Dict-shape proof for a write receiver: an object type with no class, array or callable shape
  // — `Dictionary<T>` instantiations count (they resolve to their interface target). A union keeps
  // the proof when every member is a Dict or `undefined` (both untaggable at runtime).
  rustWriteDictShape(type) {
    if (type === void 0) {
      return false;
    }
    if (type.flags & _typescript2.default.TypeFlags.Union) {
      const parts = _nullishCoalesce(type.types, () => ( []));
      return parts.length > 0 && parts.every((part) => this.rustWriteDictShape(part));
    }
    if (type.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Void)) {
      return true;
    }
    if (!(type.flags & _typescript2.default.TypeFlags.Object)) {
      return false;
    }
    const checker = this.getChecker();
    if (checker.isArrayType(type) || checker.isTupleType(type) || checker.isArrayLikeType(type)) {
      return false;
    }
    const target = _nullishCoalesce(type.target, () => ( type));
    if (target.objectFlags & _typescript2.default.ObjectFlags.Class) {
      return false;
    }
    return type.getCallSignatures().length === 0 && type.getConstructSignatures().length === 0;
  }
  // The receiver is a plain Dict on every path: its declared/initializer type is object-shaped
  // (never a class or array handle), its initializer is not an element read (the get_value COW
  // write-back pass keys on the `&mut <name>` text), and no write in scope assigns another shape.
  rustReceiverStaysDict(baseExpr, receiver) {
    if (receiver.isField) {
      return this.rustFieldStaysDict(baseExpr, receiver.nameNode.text);
    }
    const ident = baseExpr;
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const declarations = _nullishCoalesce(_optionalChain([checker, 'access', _1653 => _1653.getSymbolAtLocation, 'call', _1654 => _1654(ident), 'optionalAccess', _1655 => _1655.declarations]), () => ( []));
    if (declarations.length !== 1) {
      return false;
    }
    const declaration = declarations[0];
    if (!_typescript2.default.isVariableDeclaration(declaration) && !_typescript2.default.isParameter(declaration)) {
      return false;
    }
    if (this.rustDeclaredLocalEntry(ident) !== void 0) {
      return true;
    }
    if (_typescript2.default.isParameter(declaration) && this.rustParamStaysPlainDict(declaration)) {
      return true;
    }
    const name = ident.text;
    const initializer = declaration.initializer;
    if (initializer === void 0 || _typescript2.default.isElementAccessExpression(initializer) || _optionalChain([declaration, 'access', _1656 => _1656.name, 'optionalAccess', _1657 => _1657.kind]) !== SyntaxKind4.Identifier) {
      return false;
    }
    const scope = this.rustEnclosingFunction(declaration);
    if (scope === void 0) {
      return false;
    }
    let safe = this.rustWriteDictShape(this.typeOfNodeIfAny(ident)) && this.rustWriteDictShape(this.typeOfNodeIfAny(initializer));
    const visit = (n) => {
      if (!safe) {
        return;
      }
      if (n !== declaration && this.rustBindsName(n, name)) {
        safe = false;
        return;
      }
      if (_typescript2.default.isBinaryExpression(n) && n.operatorToken.kind === SyntaxKind4.EqualsToken && _typescript2.default.isIdentifier(n.left) && n.left.text === name && !this.rustWriteDictShape(this.typeOfNodeIfAny(n.right))) {
        safe = false;
        return;
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return safe;
  }
  // `this.<field>` receivers: the field is a plain dict (object-shaped checker type, or held as one
  // by the hand-written base when the TS declaration is `any`) and no `this.<field> = …` in the
  // method assigns another shape. Handle fields (cache/client/subscriptions/order book) are excluded.
  rustFieldStaysDict(baseExpr, fieldName) {
    if (_RustTranspiler.RUST_TAGGED_HANDLE_FIELDS.has(fieldName)) {
      return false;
    }
    if (!this.rustWriteDictShape(this.typeOfNodeIfAny(baseExpr)) && !_RustTranspiler.RUST_PLAIN_DICT_FIELDS.has(fieldName)) {
      return false;
    }
    const scope = this.rustEnclosingFunction(baseExpr);
    if (scope === void 0) {
      return false;
    }
    let safe = true;
    const visit = (n) => {
      if (!safe) {
        return;
      }
      if (_typescript2.default.isBinaryExpression(n) && n.operatorToken.kind === SyntaxKind4.EqualsToken && _typescript2.default.isPropertyAccessExpression(n.left) && n.left.expression.kind === SyntaxKind4.ThisKeyword && _optionalChain([n, 'access', _1658 => _1658.left, 'access', _1659 => _1659.name, 'optionalAccess', _1660 => _1660.text]) === fieldName && !this.rustWriteDictShape(this.typeOfNodeIfAny(n.right))) {
        safe = false;
        return;
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return safe;
  }
  // A parameter the checker proves is a plain dict (`Dict`, `Dictionary<T>`,
  // a `Market`-style alias — the proof B-25's native reads use) whose every
  // write in the body keeps that shape: an object literal with no runtime tag
  rustParamStaysPlainDict(declaration) {
    if (declaration.type === void 0 || _optionalChain([declaration, 'access', _1661 => _1661.name, 'optionalAccess', _1662 => _1662.kind]) !== SyntaxKind4.Identifier) {
      return false;
    }
    const type = this.getCheckedTypeOf(declaration.type);
    if (type === void 0 || !this.isProvenMapType(type)) {
      return false;
    }
    const name = String(declaration.name.escapedText);
    const scope = this.rustEnclosingFunction(declaration);
    if (scope === void 0) {
      return false;
    }
    let plain = true;
    const visit = (n) => {
      if (!plain) {
        return;
      }
      if (_typescript2.default.isBinaryExpression(n) && rustIsAssignmentOperator(n.operatorToken.kind) && _typescript2.default.isIdentifier(n.left) && n.left.text === name && !this.rustPlainDictPreservingRhs(n.right, name)) {
        plain = false;
        return;
      }
      if (_typescript2.default.isBinaryExpression(n) && n.operatorToken.kind === SyntaxKind4.EqualsToken && _typescript2.default.isArrayLiteralExpression(n.left) && n.left.elements.some((e) => _typescript2.default.isIdentifier(e) && String(e.escapedText) === name) && !this.rustHandlerTupleCall(n.right)) {
        plain = false;
        return;
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return plain;
  }
  /** RHS of a write to a plain-dict parameter that keeps the shape. */
  rustPlainDictPreservingRhs(node, name) {
    if (this.rustPlainDictLiteral(node) || this.rustTypeIsUndefinedish(node)) {
      return true;
    }
    if (_typescript2.default.isIdentifier(node) && node.text === name) {
      return true;
    }
    if (this.rustHandlerTupleCall(node)) {
      return true;
    }
    if (_typescript2.default.isParenthesizedExpression(node) || _typescript2.default.isNonNullExpression(node) || _typescript2.default.isAsExpression(node)) {
      return this.rustPlainDictPreservingRhs(node.expression, name);
    }
    if (_typescript2.default.isConditionalExpression(node)) {
      return this.rustPlainDictPreservingRhs(node.whenTrue, name) && this.rustPlainDictPreservingRhs(node.whenFalse, name);
    }
    if (_typescript2.default.isBinaryExpression(node) && (node.operatorToken.kind === SyntaxKind4.BarBarToken || node.operatorToken.kind === SyntaxKind4.QuestionQuestionToken)) {
      return this.rustPlainDictPreservingRhs(node.left, name) && this.rustPlainDictPreservingRhs(node.right, name);
    }
    return false;
  }
  // Conservative "argument position expects a Value" bool test — the same set
  // ccxt's wrapBoolValueArgs pass uses on the helper call's value operand.
  rustPrintedBoolArg(raw) {
    let s = raw.trim();
    while (s.startsWith("(") && s.endsWith(")")) {
      let depth = 0;
      let balanced = true;
      for (let k = 0; k < s.length; k++) {
        if (s[k] === "(")
          depth++;
        else if (s[k] === ")") {
          depth--;
          if (depth === 0 && k < s.length - 1) {
            balanced = false;
            break;
          }
        }
      }
      if (!balanced || depth !== 0)
        break;
      s = s.slice(1, -1).trim();
    }
    if (s.startsWith("!"))
      s = s.slice(1).trim();
    return _RustTranspiler.RUST_BOOL_VALUE_HELPERS.some((fn) => s.startsWith(fn + "("));
  }
  // `negate(&Value::Int(n))` is `Value::Int(-n)` (same for Float) — fold the
  // literal so no helper call is needed. Runtime `negate` also coerces
  // strings/bools/floats, so only Int/Float literals can be folded.
  foldNegateLiteral(operandText) {
    const match = operandText.match(/^Value::(Int|Float)\((-?)(\d[\d_]*(?:\.\d+)?(?:[eE][+-]?\d+)?)\)$/);
    if (!match) {
      return void 0;
    }
    const digits = match[3].replaceAll("_", "");
    if (digits.replace(".", "").length > 18) {
      return void 0;
    }
    const sign = match[2] === "-" ? "" : "-";
    return `Value::${match[1]}(${sign}${match[3]})`;
  }
  // Ensure a & ref prefix — skip only if already a reference
  ensureRef(expr) {
    if (expr.startsWith("&")) {
      return expr;
    }
    return `&${expr}`;
  }
  // TS `number` / number-literal type proof for a comparison operand. Unions
  // (`number | undefined`) and `any` are rejected — those keep the helper.
  isNumberTyped(node) {
    const type = this.getChecker().getTypeAtLocation(node);
    if (type === void 0) {
      return false;
    }
    return (type.flags & (_typescript2.default.TypeFlags.Number | _typescript2.default.TypeFlags.NumberLiteral)) !== 0;
  }
  // Positions whose emitted Rust is a native `bool`: if/while/do/for
  // conditions, `? :` conditions, `!` operands and `&&` / `||` operands.
  // Parentheses are transparent.
  isBooleanPosition(node) {
    let current = node;
    let parent = current.parent;
    while (parent !== void 0 && _typescript2.default.isParenthesizedExpression(parent)) {
      current = parent;
      parent = parent.parent;
    }
    if (parent === void 0) {
      return false;
    }
    switch (parent.kind) {
      case SyntaxKind4.IfStatement:
      case SyntaxKind4.WhileStatement:
      case SyntaxKind4.DoStatement:
        return parent.expression === current;
      case SyntaxKind4.ForStatement:
        return parent.condition === current;
      case SyntaxKind4.ConditionalExpression:
        return parent.condition === current;
      case SyntaxKind4.PrefixUnaryExpression:
        return parent.operator === SyntaxKind4.ExclamationToken;
      case SyntaxKind4.BinaryExpression:
        return parent.operatorToken.kind === SyntaxKind4.AmpersandAmpersandToken || parent.operatorToken.kind === SyntaxKind4.BarBarToken;
    }
    return false;
  }
  // `is_less_than` & co. compare two `Value`s and answer `false` for
  // non-numbers; with both operands checker-typed numbers the same f64
  // comparison runs natively on `Value::as_f64()` unwraps.
  printNativeNumericComparison(node, operator, leftText, rightText) {
    const unwrap = (text) => `${text}.as_f64().unwrap_or(f64::NAN)`;
    const comparison = `${unwrap(leftText)} ${operator} ${unwrap(rightText)}`;
    return this.isBooleanPosition(node) ? comparison : `Value::Bool(${comparison})`;
  }
  // How numeric an ordered-comparison operand is: 'definite' (numeric
  // literal / `.length` / `indexOf` — Int/Float at runtime, never Null),
  // 'number' (checker-typed — Int, Float or Null, never a string), undefined.
  rustNumericOperandKind(node) {
    const inner = this.orderedComparisonOperand(node);
    if (inner.kind === SyntaxKind4.NumericLiteral) {
      return "definite";
    }
    if (_typescript2.default.isPropertyAccessExpression(inner) && inner.name.escapedText === "length") {
      return "definite";
    }
    if (inner.kind === SyntaxKind4.CallExpression && this.callExpressionName(inner) === "indexOf") {
      return "definite";
    }
    return this.isNumberTyped(node) ? "number" : void 0;
  }
  // Parens and `x as T` print as the operand itself (printAsExpression drops
  // the assertion), so the operand's own shape drives the emission.
  orderedComparisonOperand(node) {
    let inner = node;
    while (inner !== void 0 && (_typescript2.default.isParenthesizedExpression(inner) || _typescript2.default.isAsExpression(inner))) {
      inner = inner.expression;
    }
    return inner;
  }
  // Native text for an `is_less_than`-family call, undefined unless the
  // operands prove the helper's answer: one number pins `<`/`>`; `>=`/`<=`
  // OR `is_equal` in (TRUE for two Nulls) and need a 'definite' operand.
  printNativeOrderedComparison(node, op, left, right) {
    const operator = _RustTranspiler.NATIVE_COMPARISON_OPERATORS[op];
    if (operator === void 0) {
      return void 0;
    }
    if (!this.printsValueExpression(this.orderedComparisonOperand(left)) || !this.printsValueExpression(this.orderedComparisonOperand(right))) {
      return void 0;
    }
    const leftKind = this.rustNumericOperandKind(left);
    const rightKind = this.rustNumericOperandKind(right);
    const ordered = op === SyntaxKind4.LessThanToken || op === SyntaxKind4.GreaterThanToken;
    const proven = leftKind === "definite" || rightKind === "definite" || (ordered ? leftKind !== void 0 || rightKind !== void 0 : leftKind === "number" && rightKind === "number");
    if (!proven) {
      return void 0;
    }
    return this.printNativeNumericComparison(node, operator, this.printNode(left, 0), this.printNode(right, 0));
  }
  // ── native arithmetic (`+ - * /`) ────────────────────────────────────────
  // When the checker proves both operands are numbers (`Int`/`Float` at
  // runtime) or that one is always a string and the other a string or the
  // null the helper stringifies the same way, the helper call is replaced by
  // the arithmetic itself: a 4-arm `Value` match reproducing the helper's
  // Int/Float dispatch, `as_f64()` division, or a `format!` string concat.
  // Anything the checker cannot prove keeps the runtime helper.
  isNumberLikeType(type) {
    if (!type) {
      return false;
    }
    if (type.flags === _typescript2.default.TypeFlags.Number || type.flags === _typescript2.default.TypeFlags.NumberLiteral) {
      return true;
    }
    if (type.flags === _typescript2.default.TypeFlags.Union && Array.isArray(type.types)) {
      return type.types.length > 0 && type.types.every((member) => this.isNumberLikeType(member));
    }
    return false;
  }
  isStringLikeType(type) {
    if (!type) {
      return false;
    }
    if (type.flags === _typescript2.default.TypeFlags.String || type.flags === _typescript2.default.TypeFlags.StringLiteral) {
      return true;
    }
    if (type.flags === _typescript2.default.TypeFlags.Union && Array.isArray(type.types)) {
      return type.types.length > 0 && type.types.every((member) => this.isStringLikeType(member));
    }
    return false;
  }
  isStringOrNullishType(type) {
    if (!type) {
      return false;
    }
    if (_RustTranspiler.RUST_CONCAT_SAFE_FLAGS.has(type.flags)) {
      return true;
    }
    if (type.flags === _typescript2.default.TypeFlags.Union && Array.isArray(type.types)) {
      return type.types.length > 0 && type.types.every((member) => this.isStringOrNullishType(member));
    }
    return false;
  }
  // `Str` (`string | undefined`) operands concatenate natively only against an operand proven
  // ALWAYS a string: the helper then takes its string branch, which `format!` reproduces.
  // Without that anchor (`Str + Str`) the both-null case yields `Value::Null`, not "nullnull".
  isNativeStringConcatPair(leftType, rightType) {
    if (!this.isStringOrNullishType(leftType) || !this.isStringOrNullishType(rightType)) {
      return false;
    }
    return this.isStringLikeType(leftType) || this.isStringLikeType(rightType);
  }
  // `(+|-)` with the left operand of `+=`/`-=`: assignment plus the same
  // native emission as the plain binary form.
  printNativeAssignmentArithmetic(op, left, right, leftText, rightText) {
    let leftType, rightType;
    try {
      const checker = this.getChecker();
      leftType = checker.getTypeAtLocation(left);
      rightType = checker.getTypeAtLocation(right);
    } catch (e) {
      return void 0;
    }
    if (op === SyntaxKind4.PlusToken && this.isNativeStringConcatPair(leftType, rightType)) {
      return `${leftText} = ${this.printNativeStringConcat(leftText, rightText)}`;
    }
    if (!this.isNumberLikeType(leftType) || !this.isNumberLikeType(rightType)) {
      return void 0;
    }
    return `${leftText} = ${this.printNativeNumeric(op, leftText, rightText)}`;
  }
  printNativeArithmetic(op, left, right, leftText, rightText) {
    if (op !== SyntaxKind4.PlusToken && op !== SyntaxKind4.MinusToken && op !== SyntaxKind4.AsteriskToken && op !== SyntaxKind4.SlashToken) {
      return void 0;
    }
    let leftType, rightType;
    try {
      const checker = this.getChecker();
      leftType = checker.getTypeAtLocation(left);
      rightType = checker.getTypeAtLocation(right);
    } catch (e) {
      return void 0;
    }
    if (op === SyntaxKind4.PlusToken && this.isNativeStringConcatPair(leftType, rightType)) {
      return this.printNativeStringConcat(leftText, rightText);
    }
    if (!this.isNumberLikeType(leftType) || !this.isNumberLikeType(rightType)) {
      return void 0;
    }
    return this.printNativeNumeric(op, leftText, rightText);
  }
  printNativeStringConcat(leftText, rightText) {
    return `Value::Str(format!("{}{}", ${leftText}, ${rightText}).into())`;
  }
  // Both operands are `Int`/`Float` at runtime; `-> Value::Null` covers the
  // `Null`/non-numeric values the same way the helper's fallthrough does.
  // Always parenthesised so it composes under `&`, in argument position and
  // as an operand of another native match.
  printNativeNumeric(op, leftText, rightText) {
    const left = `(${leftText})`;
    const right = `(${rightText})`;
    if (op === SyntaxKind4.SlashToken) {
      return `(match (${left}.as_f64(), ${right}.as_f64()) { (Some(x), Some(y)) if y != 0.0 => Value::Float(x / y), _ => Value::Null })`;
    }
    const sign = op === SyntaxKind4.PlusToken ? "+" : op === SyntaxKind4.MinusToken ? "-" : "*";
    return `(match (&${left}, &${right}) { (Value::Int(x), Value::Int(y)) => Value::Int(x ${sign} y), (Value::Int(x), Value::Float(y)) => Value::Float(*x as f64 ${sign} *y), (Value::Float(x), Value::Int(y)) => Value::Float(*x ${sign} *y as f64), (Value::Float(x), Value::Float(y)) => Value::Float(x ${sign} y), _ => Value::Null })`;
  }
  printCustomBinaryExpressionIfAny(node, identation) {
    const left = node.left;
    const right = node.right;
    const op = node.operatorToken.kind;
    if (op === SyntaxKind4.EqualsToken && left.kind === SyntaxKind4.ArrayLiteralExpression) {
      const elements = left.elements;
      const rhs = this.printNode(right, 0);
      const tmpName = "__destr_tmp";
      const nativeList = this.rustNativeListSource(right);
      const assignments = elements.map((e, idx) => {
        const target = this.printNode(e, 0);
        if (nativeList) {
          return `${target} = ${this.printNativeListIndex(tmpName, idx)}`;
        }
        return `${target} = get_value(&${tmpName}, &Value::Int(${idx}))`;
      }).join("; ");
      return `{ let ${tmpName} = ${rhs}; ${assignments}; }`;
    }
    if (op === SyntaxKind4.EqualsToken && left.kind === SyntaxKind4.ElementAccessExpression) {
      const keys = [];
      let baseExpr = null;
      let cur = left;
      while (_typescript2.default.isElementAccessExpression(cur)) {
        keys.unshift(cur.argumentExpression);
        const expr = cur.expression;
        if (!_typescript2.default.isElementAccessExpression(expr)) {
          baseExpr = expr;
          break;
        }
        cur = expr;
      }
      const containerStr = this.printNode(baseExpr, 0);
      const keyStrs = keys.map((k) => this.printNode(k, 0));
      let acc = `&mut ${containerStr}`;
      for (let i = 0; i < keyStrs.length - 1; i++) {
        acc = `get_value_mut(${acc}, &${keyStrs[i]})`;
      }
      const lastKey = keyStrs[keyStrs.length - 1];
      const rhs = this.printNode(right, 0);
      if (keyStrs.length === 1) {
        const nativeInsert = this.printNativeDictInsert(baseExpr, keys[0], keyStrs[0], rhs);
        if (nativeInsert !== void 0) {
          return nativeInsert;
        }
      }
      return `add_element_to_object(${acc}, &${lastKey}, ${rhs})`;
    }
    if (left.kind === SyntaxKind4.TypeOfExpression) {
      const expression = left.expression;
      const rightText = right.text;
      const target = this.printNode(expression, 0);
      const isDiff = op === SyntaxKind4.ExclamationEqualsEqualsToken || op === SyntaxKind4.ExclamationEqualsToken;
      const not = isDiff ? "!" : "";
      const native = this.nativeValuePredicateText(rightText, expression, target);
      if (native !== void 0) {
        const negated = isDiff ? `!${native}` : native;
        return this.isBooleanPosition(node) ? negated : `Value::Bool(${negated})`;
      }
      switch (rightText) {
        case "string":
          return `${not}is_string(&${target})`;
        case "number":
          return `${not}is_number(&${target})`;
        case "boolean":
          return `${not}is_bool(&${target})`;
        case "object":
          return `${not}is_object(&${target})`;
        case "function":
          return `${not}is_function(&${target})`;
      }
    }
    if (op === SyntaxKind4.InKeyword) {
      const shadow = this.rustParamShadowOf(right);
      if (shadow !== void 0) {
        const native2 = this.printShadowInOperator(shadow, left);
        if (native2 !== void 0) {
          return native2;
        }
      }
      const native = this.printNativeInOperator(left, right);
      if (native !== void 0) {
        return native;
      }
      return `Value::Bool(in_op(&${this.printNode(right, 0)}, &${this.printNode(left, 0)}))`;
    }
    if (op === SyntaxKind4.PlusEqualsToken && left.kind !== SyntaxKind4.ElementAccessExpression) {
      const leftText = this.printNode(left, 0);
      const rightText = this.printNode(right, 0);
      const native = this.printNativeAssignmentArithmetic(SyntaxKind4.PlusToken, left, right, leftText, rightText);
      if (native !== void 0) {
        return native;
      }
      return `${leftText} = add(&${leftText}, &${rightText})`;
    }
    if (op === SyntaxKind4.MinusEqualsToken && left.kind !== SyntaxKind4.ElementAccessExpression) {
      const leftText = this.printNode(left, 0);
      const rightText = this.printNode(right, 0);
      const native = this.printNativeAssignmentArithmetic(SyntaxKind4.MinusToken, left, right, leftText, rightText);
      if (native !== void 0) {
        return native;
      }
      return `${leftText} = subtract(&${leftText}, &${rightText})`;
    }
    if (op === SyntaxKind4.EqualsEqualsToken || op === SyntaxKind4.EqualsEqualsEqualsToken || op === SyntaxKind4.ExclamationEqualsToken || op === SyntaxKind4.ExclamationEqualsEqualsToken) {
      const nativeEquality = this.printNativeEqualityComparison(left, right, op);
      if (nativeEquality) {
        return `Value::Bool(${nativeEquality})`;
      }
    }
    if (op === SyntaxKind4.AmpersandAmpersandToken || op === SyntaxKind4.BarBarToken) {
      if (this.hasNativeComparisonOperand(left) || this.hasNativeComparisonOperand(right)) {
        return `Value::Bool(${this.printLogicalInBooleanContext(node)})`;
      }
    }
    if (op in this.binaryExpressionsWrappers) {
      const nativeOperator = _RustTranspiler.NATIVE_COMPARISON_OPERATORS[op];
      if (nativeOperator !== void 0) {
        const native2 = this.printNativeOrderedComparison(node, op, left, right);
        if (native2 !== void 0) {
          return native2;
        }
      }
      const [fnName, close] = this.binaryExpressionsWrappers[op];
      const leftText = this.printNode(left, 0);
      const rightText = this.printNode(right, 0);
      const native = this.printNativeArithmetic(op, left, right, leftText, rightText);
      if (native !== void 0) {
        return native;
      }
      const leftRef = this.ensureRef(leftText);
      const rightRef = this.ensureRef(rightText);
      return `${fnName}${leftRef}, ${rightRef}${close}`;
    }
    return void 0;
  }
  printBinaryExpression(node, identation) {
    const custom = this.printCustomBinaryExpressionIfAny(node, identation);
    if (custom) {
      return custom;
    }
    return super.printBinaryExpression(node, identation);
  }
  // `Date.now()` → runtime helper returning current epoch millis.
  printDateNowCall(node, identation) {
    return "date_now()";
  }
  // `str.padStart(len, pad)` / `str.padEnd(len, pad)` → runtime helpers
  // (`pad_start` / `pad_end` take `(&Value, &Value, &Value)`).
  printPadStartCall(node, identation, name, parsedArg, parsedArg2) {
    return `pad_start(${this.ensureRef(name)}, ${this.ensureRef(parsedArg)}, ${this.ensureRef(parsedArg2)})`;
  }
  printPadEndCall(node, identation, name, parsedArg, parsedArg2) {
    return `pad_end(${this.ensureRef(name)}, ${this.ensureRef(parsedArg)}, ${this.ensureRef(parsedArg2)})`;
  }
  printVariableDeclarationList(node, identation) {
    const declaration = node.declarations[0];
    const isNew = declaration.initializer && declaration.initializer.kind === SyntaxKind4.NewExpression;
    if (_optionalChain([declaration, 'optionalAccess', _1663 => _1663.name, 'access', _1664 => _1664.kind]) === SyntaxKind4.ArrayBindingPattern) {
      const elements = declaration.name.elements;
      const parsedElements = elements.map((e) => this.printNode(e.name, 0));
      const syntheticName = parsedElements.join("") + "Variable";
      let stmt = `${this.getIden(identation)}let mut ${syntheticName} = ${this.printNode(declaration.initializer, 0)};
`;
      const nativeList = this.rustNativeListSource(declaration.initializer);
      parsedElements.forEach((e, idx) => {
        const access = nativeList ? this.printNativeListIndex(syntheticName, idx) : `get_value(&${syntheticName}, &Value::Int(${idx}))`;
        const line = `${this.getIden(identation)}let mut ${e}: Value = ${access}`;
        stmt += idx < parsedElements.length - 1 ? line + ";\n" : line;
      });
      return stmt;
    }
    const varName = this.printNode(declaration.name, 0);
    if (!declaration.initializer) {
      return `${this.getIden(identation)}let mut ${varName}: Value = Value::Null`;
    }
    const parsedValue = this.printNode(declaration.initializer, identation).trim();
    if (isNew) {
      return `${this.getIden(identation)}let mut ${varName} = ${parsedValue}`;
    }
    const boolValue = this.getRustBoolLocalInitializer(declaration, parsedValue);
    if (boolValue !== void 0) {
      return `${this.getIden(identation)}let mut ${varName}: bool = ${boolValue}`;
    }
    if (this.rustSafeStringLocalIsTyped(declaration)) {
      const suffix = this.rustNativeStrCalleeKind(declaration.initializer) === "str" ? "" : ".as_str().map(str::to_owned)";
      return `${this.getIden(identation)}let mut ${varName}: Option<String> = ${parsedValue}${suffix}`;
    }
    if (this.rustNativeStrCalleeKind(declaration.initializer) === "str") {
      return `${this.getIden(identation)}let mut ${varName}: Value = ${this.rustNativeStrValueBox(parsedValue)}`;
    }
    return `${this.getIden(identation)}let mut ${varName}: Value = ${parsedValue}`;
  }
  // Call to a hand-written `-> bool` fn: the checker must agree the TS call
  // is boolean-typed and the callee must be in the verified table above.
  rustCallPrintsBool(node) {
    if (_optionalChain([node, 'optionalAccess', _1665 => _1665.kind]) !== SyntaxKind4.CallExpression) {
      return false;
    }
    const callee = node.expression;
    let name;
    if (_optionalChain([callee, 'optionalAccess', _1666 => _1666.kind]) === SyntaxKind4.Identifier) {
      name = callee.escapedText;
    } else if (_optionalChain([callee, 'optionalAccess', _1667 => _1667.kind]) === SyntaxKind4.PropertyAccessExpression) {
      name = _optionalChain([callee, 'access', _1668 => _1668.name, 'optionalAccess', _1669 => _1669.escapedText]);
    }
    if (name === void 0 || !_RustTranspiler.RUST_BOOL_RESULT_CALLEES.has(name)) {
      return false;
    }
    return this.rustTypeIsBoolean(node);
  }
  // `<box><expr>)` spanning the whole printed value → `<expr>`. The payload is
  // only reachable this way: the printer prints the value, not its parts.
  peelValueBox(printedValue, prefix) {
    if (!printedValue.startsWith(prefix) || !printedValue.endsWith(")")) {
      return void 0;
    }
    let depth = 0;
    for (let i = prefix.length - 1; i < printedValue.length; i++) {
      const char = printedValue[i];
      if (char === '"') {
        i++;
        while (i < printedValue.length && printedValue[i] !== '"') {
          if (printedValue[i] === "\\")
            i++;
          i++;
        }
        continue;
      }
      if (char === "(")
        depth++;
      else if (char === ")") {
        depth--;
        if (depth === 0) {
          return i === printedValue.length - 1 ? printedValue.slice(prefix.length, i) : void 0;
        }
      }
    }
    return void 0;
  }
  // `Value::Bool(<expr>)` spanning the whole expression → `<expr>`.
  peelValueBoolBox(printedValue) {
    return this.peelValueBox(printedValue, "Value::Bool(");
  }
  // `Value::Str(<expr>)` spanning the whole expression → `<expr>` (a String).
  peelValueStrBox(printedValue) {
    return this.peelValueBox(printedValue, "Value::Str(");
  }
  // `((expr))` → `expr` — a redundant layer kept from the TS source; the
  // right-hand side of a declaration binds the whole expression anyway.
  stripOuterParens(printedValue) {
    let value = printedValue.trim();
    while (value.startsWith("(") && value.endsWith(")")) {
      let depth = 0;
      let closesAtEnd = true;
      for (let i = 0; i < value.length; i++) {
        const char = value[i];
        if (char === '"') {
          i++;
          while (i < value.length && value[i] !== '"') {
            if (value[i] === "\\")
              i++;
            i++;
          }
          continue;
        }
        if (char === "(")
          depth++;
        else if (char === ")") {
          depth--;
          if (depth === 0 && i !== value.length - 1) {
            closesAtEnd = false;
            break;
          }
        }
      }
      if (!closesAtEnd || depth !== 0)
        break;
      value = value.slice(1, -1).trim();
    }
    return value;
  }
  // `is_equal(...)` / `!is_true(...)` / `contains(...)` — bare bool helper calls.
  printedBoolHelperCall(printedValue) {
    const stripped = printedValue.startsWith("!") ? printedValue.slice(1).trim() : printedValue;
    const match = /^([a-z_][a-z0-9_]*)\(/.exec(stripped);
    return match !== null && _RustTranspiler.RUST_BOOL_RESULT_HELPERS.has(match[1]);
  }
  // Source shapes the printer turns into a bool: comparisons, `&&`/`||`
  // (each operand is is_true-wrapped), `!`, `in`, `instanceof`, true/false.
  rustNodeIsBoolExpression(node) {
    switch (_optionalChain([node, 'optionalAccess', _1670 => _1670.kind])) {
      case SyntaxKind4.TrueKeyword:
      case SyntaxKind4.FalseKeyword:
        return true;
      case SyntaxKind4.ParenthesizedExpression:
        return this.rustNodeIsBoolExpression(node.expression);
      case SyntaxKind4.PrefixUnaryExpression:
        return node.operator === SyntaxKind4.ExclamationToken;
      case SyntaxKind4.BinaryExpression:
        return _RustTranspiler.COMPARISON_OPS.has(node.operatorToken.kind) || node.operatorToken.kind === SyntaxKind4.AmpersandAmpersandToken || node.operatorToken.kind === SyntaxKind4.BarBarToken || node.operatorToken.kind === SyntaxKind4.InKeyword || node.operatorToken.kind === SyntaxKind4.InstanceOfKeyword;
    }
    return false;
  }
  rustTypeIsBoolean(node) {
    try {
      const type = this.getChecker().getTypeAtLocation(node);
      if ((type.flags & _typescript2.default.TypeFlags.BooleanLike) !== 0) {
        return true;
      }
      return this.getChecker().typeToString(type).trim() === "boolean";
    } catch (e) {
      return false;
    }
  }
  rustTypeIsString(node) {
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    return this.isStringLikeType(checker.getTypeAtLocation(node));
  }
  rustEnclosingFunction(node) {
    let current = _optionalChain([node, 'optionalAccess', _1671 => _1671.parent]);
    while (current) {
      switch (current.kind) {
        case SyntaxKind4.MethodDeclaration:
        case SyntaxKind4.FunctionDeclaration:
        case SyntaxKind4.FunctionExpression:
        case SyntaxKind4.ArrowFunction:
        case SyntaxKind4.Constructor:
        case SyntaxKind4.SourceFile:
          return current;
      }
      current = current.parent;
    }
    return void 0;
  }
  rustBindsName(node, name) {
    switch (_optionalChain([node, 'optionalAccess', _1672 => _1672.kind])) {
      case SyntaxKind4.VariableDeclaration:
      case SyntaxKind4.Parameter:
      case SyntaxKind4.FunctionDeclaration:
      case SyntaxKind4.ClassDeclaration:
      case SyntaxKind4.PropertyDeclaration:
      case SyntaxKind4.FunctionExpression:
      case SyntaxKind4.ArrowFunction:
        return _optionalChain([node, 'access', _1673 => _1673.name, 'optionalAccess', _1674 => _1674.kind]) === SyntaxKind4.Identifier && node.name.escapedText === name;
    }
    return false;
  }
  // Only these uses compile against a native `bool` local today: `is_true(&x)`
  // (under any depth of `(...)`, `!`, `&&`/`||`), and the condition slot of
  // if/while/for/ternary — all printed is_true-wrapped.
  rustIdentifierUseIsCondition(node) {
    let current = node;
    let parent = current.parent;
    while (parent) {
      switch (parent.kind) {
        case SyntaxKind4.ParenthesizedExpression:
          if (parent.expression !== current)
            return false;
          break;
        case SyntaxKind4.PrefixUnaryExpression:
          if (parent.operator !== SyntaxKind4.ExclamationToken || parent.operand !== current)
            return false;
          break;
        case SyntaxKind4.BinaryExpression:
          if (parent.operatorToken.kind !== SyntaxKind4.AmpersandAmpersandToken && parent.operatorToken.kind !== SyntaxKind4.BarBarToken)
            return false;
          break;
        case SyntaxKind4.IfStatement:
        case SyntaxKind4.WhileStatement:
        case SyntaxKind4.DoStatement:
          return parent.expression === current;
        case SyntaxKind4.ForStatement:
        case SyntaxKind4.ConditionalExpression:
          return parent.condition === current;
        default:
          return false;
      }
      current = parent;
      parent = current.parent;
    }
    return false;
  }
  rustLocalUsesAcceptBool(declaration, sourceName) {
    const scope = this.rustEnclosingFunction(declaration);
    if (scope === void 0 || sourceName === void 0) {
      return false;
    }
    let safe = true;
    const visit = (n) => {
      if (!safe)
        return;
      if (n !== declaration && this.rustBindsName(n, sourceName)) {
        safe = false;
        return;
      }
      if (n.kind === SyntaxKind4.Identifier && n.escapedText === sourceName && n !== declaration.name) {
        if (!this.rustIdentifierUseIsCondition(n)) {
          safe = false;
          return;
        }
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return safe;
  }
  // `let x = this.safeString(..)` / `safeString(..)` — the whole initializer;
  // a call whose callee already returns `Option<String>` (a native-`Str`
  // method) is the same payload without the unwrap.
  rustSafeStringLocalInitializer(declaration) {
    const initializer = declaration.initializer;
    if (_optionalChain([declaration, 'access', _1675 => _1675.name, 'optionalAccess', _1676 => _1676.kind]) !== SyntaxKind4.Identifier || _optionalChain([initializer, 'optionalAccess', _1677 => _1677.kind]) !== SyntaxKind4.CallExpression) {
      return false;
    }
    if (this.rustNativeStrCalleeKind(initializer) === "str") {
      return true;
    }
    const callee = initializer.expression;
    if (_optionalChain([callee, 'optionalAccess', _1678 => _1678.kind]) === SyntaxKind4.PropertyAccessExpression) {
      return _optionalChain([callee, 'access', _1679 => _1679.expression, 'optionalAccess', _1680 => _1680.kind]) === SyntaxKind4.ThisKeyword && _RustTranspiler.RUST_STRING_LOCAL_HELPERS.has(callee.name.escapedText);
    }
    if (_optionalChain([callee, 'optionalAccess', _1681 => _1681.kind]) === SyntaxKind4.Identifier) {
      return _RustTranspiler.RUST_STRING_LOCAL_HELPERS.has(callee.escapedText);
    }
    return false;
  }
  // The two uses that compile against an `Option<String>` local and print
  // natively: `x ==/!= null|undefined` and `x ==/!= "lit"`.
  rustStringLocalUseIsNative(node) {
    const parent = node.parent;
    if (parent === void 0) {
      return false;
    }
    if (parent.kind !== SyntaxKind4.BinaryExpression) {
      return false;
    }
    const op = parent.operatorToken.kind;
    if (op !== SyntaxKind4.EqualsEqualsToken && op !== SyntaxKind4.EqualsEqualsEqualsToken && op !== SyntaxKind4.ExclamationEqualsToken && op !== SyntaxKind4.ExclamationEqualsEqualsToken) {
      return false;
    }
    const other = parent.left === node ? parent.right : parent.right === node ? parent.left : void 0;
    if (other === void 0) {
      return false;
    }
    const otherLiteral = this.literalKindOfNode(other);
    if (otherLiteral === "null") {
      return true;
    }
    return otherLiteral === "string" && !(other.text in this.StringLiteralReplacements);
  }
  // Every use compiles against `Option<String>`, and at least one native sink
  // consumes it (otherwise the retype buys nothing).
  rustSafeStringLocalIsTyped(declaration) {
    const cached = this.rustStringLocalDecisions.get(declaration);
    if (cached !== void 0) {
      return cached;
    }
    this.rustStringLocalDecisions.set(declaration, false);
    const decision = this.rustSafeStringLocalIsTypedUncached(declaration);
    this.rustStringLocalDecisions.set(declaration, decision);
    return decision;
  }
  rustSafeStringLocalIsTypedUncached(declaration) {
    if (!this.rustSafeStringLocalInitializer(declaration)) {
      return false;
    }
    if (this.primitiveKindOfType(this.typeOfNodeIfAny(declaration.name)) !== "string") {
      return false;
    }
    const name = declaration.name.escapedText;
    const scope = this.rustEnclosingFunction(declaration);
    if (scope === void 0) {
      return false;
    }
    let nativeUses = 0;
    let safe = true;
    const visit = (n) => {
      if (!safe) {
        return;
      }
      if (n !== declaration && this.rustBindsName(n, name)) {
        safe = false;
        return;
      }
      if (n.kind === SyntaxKind4.Identifier && n.escapedText === name && n !== declaration.name && !this.rustIdentifierIsPropertyName(n)) {
        if (!this.rustStringLocalUseIsNative(n)) {
          safe = false;
          return;
        }
        nativeUses++;
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return safe && nativeUses > 0;
  }
  // `x.foo` / `{ foo: 1 }` — a property name is not a use of the local.
  rustIdentifierIsPropertyName(node) {
    const parent = node.parent;
    if (parent === void 0) {
      return false;
    }
    switch (parent.kind) {
      case SyntaxKind4.PropertyAccessExpression:
      case SyntaxKind4.PropertyAssignment:
      case SyntaxKind4.PropertySignature:
      case SyntaxKind4.ShorthandPropertyAssignment:
        return parent.name === node;
    }
    return false;
  }
  // Is this identifier occurrence bound to a typed string local?
  rustStringLocalIdentifierIsTyped(node) {
    if (_optionalChain([node, 'optionalAccess', _1682 => _1682.kind]) !== SyntaxKind4.Identifier) {
      return false;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return false;
    }
    const symbol = checker.getSymbolAtLocation(node);
    const declaration = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1683 => _1683.valueDeclaration]), () => ( _optionalChain([symbol, 'optionalAccess', _1684 => _1684.declarations, 'optionalAccess', _1685 => _1685[0]])));
    if (_optionalChain([declaration, 'optionalAccess', _1686 => _1686.kind]) !== SyntaxKind4.VariableDeclaration) {
      return false;
    }
    return this.rustSafeStringLocalIsTyped(declaration);
  }
  // `let x = <bool expr>` → the printed bool expression, or undefined.
  getRustBoolLocalInitializer(declaration, printedValue) {
    const initializer = declaration.initializer;
    if (initializer === void 0 || _optionalChain([declaration, 'access', _1687 => _1687.name, 'optionalAccess', _1688 => _1688.kind]) !== SyntaxKind4.Identifier) {
      return void 0;
    }
    const inner = this.stripOuterParens(printedValue);
    const peeled = this.peelValueBoolBox(inner);
    if (peeled === void 0 && !this.printedBoolHelperCall(inner) && !this.rustNodeIsBoolExpression(initializer)) {
      return void 0;
    }
    if (!this.rustTypeIsBoolean(initializer)) {
      return void 0;
    }
    if (!this.rustLocalUsesAcceptBool(declaration, declaration.name.escapedText)) {
      return void 0;
    }
    return peeled !== void 0 ? peeled : inner;
  }
  /** `'str'` when the method is emitted `-> Option<String>`, else undefined. */
  rustNativeStrReturnKind(node) {
    if (node === void 0 || node.kind !== SyntaxKind4.MethodDeclaration) {
      return void 0;
    }
    const cached = this.rustNativeStrReturnDecisions.get(node);
    if (cached !== void 0) {
      return cached ? "str" : void 0;
    }
    this.rustNativeStrReturnDecisions.set(node, false);
    const decision = this.rustNativeStrReturnDecisionUncached(node);
    this.rustNativeStrReturnDecisions.set(node, decision);
    return decision ? "str" : void 0;
  }
  rustNativeStrReturnDecisionUncached(node) {
    if (node.type === void 0 || this.isAsyncFunction(node)) {
      return false;
    }
    if (this.getMethodOverride(node) !== void 0) {
      return false;
    }
    if (_RustTranspiler.RUST_BASE_TIER_FILE.test(node.getSourceFile().fileName)) {
      return false;
    }
    let type;
    try {
      type = this.getChecker().getTypeFromTypeNode(node.type);
    } catch (e) {
      return false;
    }
    if (this.primitiveKindOfType(type) !== "string") {
      return false;
    }
    return this.rustStrReturnPathsConvert(node.body);
  }
  /** Every `return` of the method's own body converts, and the body's last
   *  statement is one of them (so Rust sees no `()`-valued tail the
   *  `-> Value` post-passes would have patched with `Value::Null`). */
  rustStrReturnPathsConvert(body) {
    if (body === void 0) {
      return false;
    }
    const statements = body.statements;
    const last = statements[statements.length - 1];
    if (last === void 0 || !_typescript2.default.isReturnStatement(last)) {
      return false;
    }
    let ok = true;
    const visit = (n) => {
      if (!ok) {
        return;
      }
      if (n !== body && _typescript2.default.isFunctionLike(n)) {
        return;
      }
      if (_typescript2.default.isReturnStatement(n) && !this.rustStrReturnValueConverts(n.expression)) {
        ok = false;
        return;
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(body, visit);
    return ok;
  }
  /** A `return` value of a native-`Str` method: a nullish literal, an
   *  expression already printing an `Option<String>` (a nested retyped call
   *  or a typed string local), or a `Value`-printing expression the checker
   *  types `string | undefined`. */
  rustStrReturnValueConverts(expression) {
    const inner = this.unwrapParensNode(expression);
    if (inner === void 0) {
      return false;
    }
    if (this.literalKindOfNode(inner) === "null") {
      return true;
    }
    if (this.rustStrNativeExpression(inner)) {
      return true;
    }
    if (!this.printsValueExpression(inner)) {
      return false;
    }
    return this.primitiveKindOfType(this.getCheckedTypeOf(inner)) === "string";
  }
  /** An expression that already prints an `Option<String>` in a `: Str`
   *  method's return position. */
  rustStrNativeExpression(expression) {
    const inner = this.unwrapParensNode(expression);
    if (inner === void 0) {
      return false;
    }
    if (_typescript2.default.isCallExpression(inner)) {
      return this.rustNativeStrCalleeKind(inner) === "str";
    }
    if (_typescript2.default.isIdentifier(inner)) {
      return this.rustStringLocalIdentifierIsTyped(inner);
    }
    return false;
  }
  unwrapParensNode(node) {
    let current = node;
    while (current !== void 0 && _typescript2.default.isParenthesizedExpression(current)) {
      current = current.expression;
    }
    return current;
  }
  /** The callee declaration behind `self.<method>(..)` when it is emitted
   *  `-> Option<String>`; undefined otherwise (no proof → keep the box). */
  rustNativeStrCalleeKind(node) {
    if (node === void 0 || node.kind !== SyntaxKind4.CallExpression) {
      return void 0;
    }
    let declaration;
    try {
      declaration = _optionalChain([this, 'access', _1689 => _1689.getChecker, 'call', _1690 => _1690(), 'access', _1691 => _1691.getResolvedSignature, 'call', _1692 => _1692(node), 'optionalAccess', _1693 => _1693.declaration]);
    } catch (e) {
      return void 0;
    }
    if (declaration === void 0 || declaration.kind !== SyntaxKind4.MethodDeclaration) {
      return void 0;
    }
    return this.rustNativeStrReturnKind(declaration);
  }
  /** `Option<String>` → `Value` (exact inverse of the return conversion). */
  rustNativeStrValueBox(text) {
    return `${text}.map(|__s| Value::Str(__s.into())).unwrap_or(Value::Null)`;
  }
  /** True when a call to a native-`Str` callee must be boxed back to a
   *  `Value` at this position; the declaration and return printers run the
   *  conversion themselves. */
  rustNativeStrCallNeedsBox(node) {
    let current = node;
    let parent = current.parent;
    while (parent !== void 0 && _typescript2.default.isParenthesizedExpression(parent) && parent.expression === current) {
      current = parent;
      parent = parent.parent;
    }
    if (parent === void 0) {
      return true;
    }
    if (_typescript2.default.isVariableDeclaration(parent) && parent.initializer === current && _typescript2.default.isIdentifier(parent.name)) {
      return false;
    }
    if (_typescript2.default.isReturnStatement(parent) && parent.expression === current) {
      const fn = _typescript2.default.findAncestor(parent.parent, _typescript2.default.isFunctionLike);
      return this.rustNativeStrReturnKind(fn) !== "str";
    }
    return true;
  }
  /** Wrap a call text when the callee returns a native `Option<String>`
   *  and the position still needs a `Value`. */
  rustBoxNativeStrCallIfNeeded(node, text) {
    if (this.rustNativeStrCalleeKind(node) !== "str") {
      return text;
    }
    if (!this.rustNativeStrCallNeedsBox(node)) {
      return text;
    }
    return this.rustNativeStrValueBox(text);
  }
  /** All `let x: Value = <dict-proven initialiser>` declarations of the current
   *  source file, keyed by local name in declaration order. */
  rustDeclaredDictLocals() {
    const src = this.getSrc();
    if (this.declaredDictLocalsCache === void 0 || this.declaredDictLocalsCache.src !== src) {
      let table;
      try {
        table = this.collectRustDeclaredDictLocals(src);
      } catch (e) {
        table = /* @__PURE__ */ new Map();
      }
      this.declaredDictLocalsCache = { src, table };
    }
    return this.declaredDictLocalsCache.table;
  }
  /** Printer hook for the helper-removal units: the proven kind of a declared
   *  local, or undefined when the local is not proven Dict at every use.
   *  Accepts the receiver node of the helper call (identifier, `x['k']` chain,
   *  `this.x` chain) or the declaration itself. */
  rustDeclaredLocalTypeResolver(node) {
    const entry = this.rustDeclaredLocalEntry(node);
    return entry === void 0 ? void 0 : entry.kind;
  }
  /** The table entry a use site resolves to (the declaration whose binding the
   *  use refers to, proven), or undefined. */
  rustDeclaredLocalEntry(node) {
    if (node === void 0)
      return void 0;
    const name = _typescript2.default.isVariableDeclaration(node) ? node.name.text : this.rootPlaceText(node);
    if (name === void 0)
      return void 0;
    const entries = this.rustDeclaredDictLocals().get(name);
    if (entries === void 0)
      return void 0;
    const start = node.getStart();
    const identifier = this.rustDeclaredLocalIdentifier(node);
    const symbol = identifier === void 0 ? void 0 : this.rustSymbolOf(identifier);
    let best;
    for (const entry of entries) {
      if (entry.start > start || !entry.alwaysDict || !entry.stable)
        continue;
      const scope = this.rustEnclosingFunction(entry.declaration);
      if (scope !== void 0 && !this.isNodeInsideNode(node, scope))
        continue;
      if (identifier !== void 0) {
        const entrySymbol = this.rustSymbolOf(entry.declaration.name);
        if (symbol !== void 0 && entrySymbol !== void 0 && symbol !== entrySymbol)
          continue;
      }
      if (best === void 0 || entry.start > best.start)
        best = entry;
    }
    return best;
  }
  /** The identifier at the head of a place (`x`, `x['k']`, `this.x` is not a
   *  local) — the node the resolver matches against the table. */
  rustDeclaredLocalIdentifier(node) {
    let current = node;
    while (current !== void 0) {
      if (_typescript2.default.isIdentifier(current))
        return current;
      if (_typescript2.default.isElementAccessExpression(current) || _typescript2.default.isPropertyAccessExpression(current)) {
        if (current.expression.kind === SyntaxKind4.ThisKeyword)
          return void 0;
        current = current.expression;
        continue;
      }
      if (_typescript2.default.isParenthesizedExpression(current) || _typescript2.default.isNonNullExpression(current)) {
        current = current.expression;
        continue;
      }
      return void 0;
    }
    return void 0;
  }
  /** Binding symbol of an identifier, or undefined when the checker cannot
   *  answer (ByContent probes without a class context, for instance). */
  rustSymbolOf(node) {
    return _optionalChain([this, 'access', _1694 => _1694.checkerOrUndefined, 'call', _1695 => _1695(), 'optionalAccess', _1696 => _1696.getSymbolAtLocation, 'call', _1697 => _1697(node)]);
  }
  /** True when this identifier is a use of the given declaration's binding.
   *  Without a checker answer the callers stay conservative (reject). */
  rustIdentifierRefersToDeclaration(node, declaration) {
    const symbol = this.rustSymbolOf(node);
    const declarationSymbol = this.rustSymbolOf(declaration.name);
    if (symbol === void 0 || declarationSymbol === void 0)
      return false;
    return symbol === declarationSymbol;
  }
  /** Census of the current source file's table, for reports and tests. */
  rustDeclaredDictLocalCensus() {
    let declarators = 0, dict = 0, alwaysDict = 0, kindUnstable = 0, retypeEligible = 0;
    for (const entries of this.rustDeclaredDictLocals().values()) {
      for (const entry of entries) {
        declarators++;
        if (entry.alwaysDict)
          alwaysDict++;
        if (entry.alwaysDict && entry.stable)
          dict++;
        if (entry.alwaysDict && !entry.stable)
          kindUnstable++;
        if (entry.alwaysDict && entry.stable && entry.uses.other === 0)
          retypeEligible++;
      }
    }
    return { declarators, dict, alwaysDict, kindUnstable, retypeEligible };
  }
  collectRustDeclaredDictLocals(src) {
    const candidates = [];
    const collect = (node) => {
      if (_typescript2.default.isVariableDeclaration(node) && node.initializer !== void 0 && node.name.kind === SyntaxKind4.Identifier) {
        const info = this.rustDictInitializerInfo(node.initializer);
        if (info !== void 0) {
          candidates.push({ declaration: node, name: String(node.name.escapedText), source: info.source, defaultNode: info.defaultNode });
        }
      }
      _typescript2.default.forEachChild(node, collect);
    };
    _typescript2.default.forEachChild(src, collect);
    candidates.sort((a, b) => a.declaration.getStart() - b.declaration.getStart());
    const table = /* @__PURE__ */ new Map();
    for (const candidate of candidates) {
      const declaration = candidate.declaration;
      const start = declaration.getStart();
      const alwaysDict = this.rustDictProvenExpression(candidate.defaultNode, table, start);
      const scan = this.rustDictLocalWriteScan(declaration, candidate.name, table);
      const entries = _nullishCoalesce(table.get(candidate.name), () => ( []));
      entries.push({
        kind: RUST_DECLARED_DICT_LOCALS.DICT,
        name: candidate.name,
        source: candidate.source,
        alwaysDict,
        stable: scan.stable,
        uses: scan.uses,
        declaration,
        start
      });
      table.set(candidate.name, entries);
    }
    return table;
  }
  /** The Dict-proven initialiser shape of a declaration, or undefined. */
  rustDictInitializerInfo(node) {
    if (_typescript2.default.isObjectLiteralExpression(node)) {
      return { source: "value_map", defaultNode: node };
    }
    if (_typescript2.default.isParenthesizedExpression(node) || _typescript2.default.isAsExpression(node) || _typescript2.default.isNonNullExpression(node)) {
      return this.rustDictInitializerInfo(node.expression);
    }
    if (!_typescript2.default.isCallExpression(node))
      return void 0;
    const callee = this.rustSafeDictCallee(node);
    if (callee === void 0)
      return void 0;
    return { source: callee, defaultNode: node.arguments[RUST_DECLARED_DICT_LOCALS.SAFE_CALLEES[callee]] };
  }
  /** Printed `safe_dict*` callee name of `self.<name>(..)`, or undefined. */
  rustSafeDictCallee(node) {
    const expression = node.expression;
    if (!_typescript2.default.isPropertyAccessExpression(expression) || expression.expression.kind !== SyntaxKind4.ThisKeyword) {
      return void 0;
    }
    const printed = this.toSnakeCaseName(expression.name.text);
    return RUST_DECLARED_DICT_LOCALS.SAFE_CALLEES[printed] === void 0 ? void 0 : printed;
  }
  /** True when the expression can only be a Dict at run time: an object
   *  literal, a `safe_dict*` call with a Dict-proven default, an element of a
   *  one-element literal default, or an already-proven local. */
  rustDictProvenExpression(node, table, useStart) {
    if (node === void 0)
      return false;
    if (_typescript2.default.isObjectLiteralExpression(node))
      return true;
    if (_typescript2.default.isParenthesizedExpression(node) || _typescript2.default.isAsExpression(node) || _typescript2.default.isNonNullExpression(node)) {
      return this.rustDictProvenExpression(node.expression, table, useStart);
    }
    if (_typescript2.default.isArrayLiteralExpression(node)) {
      return node.elements.length === 1 && this.rustDictProvenExpression(node.elements[0], table, useStart);
    }
    if (_typescript2.default.isIdentifier(node)) {
      const entries = _nullishCoalesce(table.get(node.text), () => ( []));
      return entries.some((entry) => {
        if (!entry.alwaysDict || entry.start > useStart)
          return false;
        const scope = this.rustEnclosingFunction(entry.declaration);
        return scope === void 0 || this.isNodeInsideNode(node, scope);
      });
    }
    const info = this.rustDictInitializerInfo(node);
    return info !== void 0 && this.rustDictProvenExpression(info.defaultNode, table, useStart);
  }
  /** D2 scan over the enclosing function: an assignment of a non-Dict-proven
   *  value would let the kind change. Every other write path the printer emits
   *  for a local is kind-preserving (`x['k'] = v` -> `add_element_to_object`,
   *  `x.push(v)` -> `append_to_array`, `delete x[k]` -> `remove`, nested
   *  `x['a']['b'] = v` -> `get_value_mut`/`set_value`). A *different* binding of
   *  the same name (sibling block, parameter) is not this local and does not
   *  count; when the checker cannot separate the two bindings the scan stays
   *  conservative and rejects. */
  rustDictLocalWriteScan(declaration, name, table) {
    const uses = { elementAccess: 0, mutHelper: 0, other: 0 };
    let stable = true;
    const scope = this.rustEnclosingFunction(declaration);
    if (scope === void 0)
      return { stable, uses };
    const declarationSymbol = this.rustSymbolOf(declaration.name);
    const visit = (node) => {
      if (!stable)
        return;
      if (node !== declaration && this.rustBindsName(node, name)) {
        const otherSymbol = this.rustSymbolOf(node.name);
        if (declarationSymbol === void 0 || otherSymbol === void 0 || otherSymbol === declarationSymbol) {
          stable = false;
          return;
        }
      }
      if (node.kind === SyntaxKind4.Identifier && node.escapedText === name && node !== declaration.name && this.rustIdentifierRefersToDeclaration(node, declaration)) {
        this.rustDictLocalClassifyUse(node, uses);
      }
      if (_typescript2.default.isBinaryExpression(node) && rustIsAssignmentOperator(node.operatorToken.kind) && this.rustAssignmentWritesWholeLocal(node.left, declaration) && !this.rustDictProvenExpression(node.right, table, declaration.getStart())) {
        stable = false;
        return;
      }
      if ((_typescript2.default.isForOfStatement(node) || _typescript2.default.isForInStatement(node)) && this.rustAssignmentWritesWholeLocal(node.initializer, declaration)) {
        stable = false;
        return;
      }
      _typescript2.default.forEachChild(node, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return { stable, uses };
  }
  /** True when this assignment target writes the local ITSELF (`x = ..`,
   *  `[x, y] = ..`, `({x} = ..)`), as opposed to a write *into* it
   *  (`x['k'] = ..`, kind-preserving). */
  rustAssignmentWritesWholeLocal(left, declaration) {
    if (_typescript2.default.isIdentifier(left)) {
      return this.rustIdentifierRefersToDeclaration(left, declaration);
    }
    if (_typescript2.default.isParenthesizedExpression(left)) {
      return this.rustAssignmentWritesWholeLocal(left.expression, declaration);
    }
    if (_typescript2.default.isArrayLiteralExpression(left)) {
      return left.elements.some((element) => this.rustAssignmentWritesWholeLocal(element, declaration));
    }
    if (_typescript2.default.isObjectLiteralExpression(left)) {
      return left.properties.some((property) => {
        if (!_typescript2.default.isShorthandPropertyAssignment(property))
          return false;
        return this.rustAssignmentWritesWholeLocal(property.name, declaration);
      });
    }
    return false;
  }
  /** One use of a dict-proven local: an element-access chain (`x['k']`, also
   *  the `x['k'] = v` write), a kind-preserving mutator (`x.push(v)`,
   *  `delete x[k]`), or something that would need the local to still be a
   *  `Value`. */
  rustDictLocalClassifyUse(node, uses) {
    let current = node;
    const parent = current.parent;
    if (parent !== void 0 && (_typescript2.default.isElementAccessExpression(parent) || _typescript2.default.isPropertyAccessExpression(parent)) && parent.expression === current) {
      current = parent;
      while (current.parent !== void 0 && (_typescript2.default.isElementAccessExpression(current.parent) || _typescript2.default.isPropertyAccessExpression(current.parent)) && current.parent.expression === current) {
        current = current.parent;
      }
      if (_typescript2.default.isElementAccessExpression(current)) {
        uses.elementAccess++;
        return;
      }
      uses.other++;
      return;
    }
    if (parent !== void 0 && _typescript2.default.isCallExpression(parent) && _typescript2.default.isPropertyAccessExpression(parent.expression) && parent.expression.expression === current && parent.expression.name.text === "push") {
      uses.mutHelper++;
      return;
    }
    if (parent !== void 0 && _typescript2.default.isDeleteExpression(parent)) {
      uses.mutHelper++;
      return;
    }
    uses.other++;
  }
  printPropertyDeclaration(node, identation) {
    const name = this.printNode(node.name, 0);
    if (node.initializer) {
      const init = this.printNode(node.initializer, 0);
      return `${this.getIden(identation)}${name}: Value, // default: ${init}`;
    }
    return `${this.getIden(identation)}${name}: Value,`;
  }
  // Collect property declarations with their initializers for use in new()
  getStructFields(node) {
    const propDecls = node.members.filter((m) => m.kind === SyntaxKind4.PropertyDeclaration);
    return propDecls.map((p) => {
      const name = this.printNode(p.name, 0);
      const init = p.initializer ? this.printNode(p.initializer, 0) : "Value::Null";
      return { name, init };
    });
  }
  printStruct(node, identation) {
    const fields = this.getStructFields(node);
    const fieldLines = fields.map((f) => `${this.getIden(identation + 1)}pub ${f.name}: Value,`).join("\n");
    return `#[derive(Debug, Clone)]
pub struct ${this.className} {
${fieldLines}
}`;
  }
  printNewMethod(node, identation) {
    const fields = this.getStructFields(node);
    const fieldInits = fields.map((f) => `${this.getIden(identation + 2)}${f.name}: ${f.init},`).join("\n");
    return `
impl ${this.className} {
${this.getIden(identation + 1)}pub fn new() -> Self {
${this.getIden(identation + 2)}${this.className} {
${fieldInits}
${this.getIden(identation + 2)}}
${this.getIden(identation + 1)}}
}`;
  }
  printClass(node, identation) {
    this.className = node.name.escapedText;
    const methods = node.members.filter((m) => m.kind === SyntaxKind4.MethodDeclaration);
    methods.forEach((method) => {
      const name = method.name.escapedText;
      const params = method.parameters;
      const requiredCount = params.filter((p) => !p.initializer && !p.questionToken).length;
      const hasOptional = params.some((p) => p.initializer !== void 0 || p.questionToken !== void 0);
      if (hasOptional) {
        this.methodSignatures[name] = { requiredCount };
      }
    });
    const struct = this.printStruct(node, identation);
    const newMethod = this.printNewMethod(node, identation);
    const classMethods = methods.map((m) => this.printMethodDeclaration(m, identation)).join("\n\n");
    const implBlock = `
impl ${this.className} {
${classMethods}
}`;
    return struct + newMethod + implBlock;
  }
  printMethodDefinition(node, identation) {
    const name = node.name.escapedText;
    const params = node.parameters;
    const hasOptional = params.some((p) => p.initializer !== void 0 || p.questionToken !== void 0);
    const requiredParams = params.filter((p) => !p.initializer && !p.questionToken);
    const optionalParams = params.filter((p) => p.initializer !== void 0 || p.questionToken !== void 0);
    let parsedArgs = "&self";
    if (requiredParams.length > 0) {
      const reqArgs = requiredParams.map((p) => `${this.printNode(p.name, 0)}: Value`).join(", ");
      parsedArgs += ", " + reqArgs;
    }
    if (hasOptional) {
      parsedArgs += ", optional_args: &[Value]";
    }
    const returnType = this.printRustFunctionType(node);
    const retStr = returnType ? ` -> ${returnType}` : "";
    return `${this.getIden(identation + 1)}pub fn ${name}(${parsedArgs})${retStr}`;
  }
  printRustFunctionType(node) {
    if (this.rustNativeStrReturnKind(node) === "str") {
      return "Option<String>";
    }
    try {
      const type = this.getChecker().getReturnTypeOfSignature(this.getChecker().getSignatureFromDeclaration(node));
      if (type.flags === _typescript2.default.TypeFlags.Void) {
        return "";
      }
    } catch (e) {
    }
    return "Value";
  }
  printMethodDeclaration(node, identation) {
    const methodDef = this.printMethodDefinition(node, identation);
    const params = node.parameters;
    const optionalParams = params.filter((p) => p.initializer !== void 0 || p.questionToken !== void 0);
    let optionalInits = "";
    if (optionalParams.length > 0) {
      const requiredCount = params.filter((p) => !p.initializer && !p.questionToken).length;
      optionalInits = optionalParams.map((p, idx) => {
        const pname = this.printNode(p.name, 0);
        const defaultVal = p.initializer ? this.printNode(p.initializer, 0) : "Value::Null";
        return `${this.getIden(identation + 2)}let ${pname} = get_arg(optional_args, ${idx}, ${defaultVal});`;
      }).join("\n") + "\n";
    }
    const blockOpen = this.getBlockOpen(identation);
    const blockClose = this.getBlockClose(identation);
    const shadows = this.rustParamShadowLines(node, identation + 2);
    const shadowPlan = this.rustProHandlerShadowPlan(node, identation);
    const savedShadowParam = this.rustProHandlerShadowParam;
    if (shadowPlan !== void 0) {
      this.rustProHandlerShadowParam = shadowPlan.param;
    }
    const statements = node.body.statements.map((s) => this.printNode(s, identation + 2)).join("\n");
    this.rustProHandlerShadowParam = savedShadowParam;
    const shadow = shadowPlan === void 0 ? "" : shadowPlan.lines;
    const body = blockOpen + optionalInits + shadows + shadow + statements + blockClose;
    return this.printNodeCommentsIfAny(node, identation, methodDef + body);
  }
  printFunctionDefinition(node, identation) {
    const name = _nullishCoalesce(_optionalChain([node, 'access', _1698 => _1698.name, 'optionalAccess', _1699 => _1699.escapedText]), () => ( ""));
    const params = node.parameters;
    const parsedArgs = params.map((p) => `${this.printNode(p.name, 0)}: Value`).join(", ");
    const returnType = this.printRustFunctionType(node);
    const retStr = returnType ? ` -> ${returnType}` : "";
    return `${this.getIden(identation)}fn ${name}(${parsedArgs})${retStr}`;
  }
  printFunctionDeclaration(node, identation) {
    if (_typescript2.default.isArrowFunction(node)) {
      const parameters = node.parameters.map((p) => `${this.printNode(p.name, 0)}: Value`).join(", ");
      const body = this.printNode(node.body);
      return `|${parameters}| ${body}`;
    }
    const funcDef = this.printFunctionDefinition(node, identation);
    const funcBody = super.printFunctionBody(node, identation);
    return this.printNodeCommentsIfAny(node, identation, funcDef + funcBody);
  }
  printOutOfOrderCallExpressionIfAny(node, identation) {
    if (node.expression.kind !== SyntaxKind4.PropertyAccessExpression) {
      return void 0;
    }
    const expr = node.expression;
    const args = node.arguments;
    if (expr.expression.kind === SyntaxKind4.ThisKeyword) {
      const methodName = expr.name.escapedText;
      const sig = this.methodSignatures[methodName];
      if (sig) {
        const requiredArgs = args.slice(0, sig.requiredCount).map((a) => this.printNode(a, 0)).join(", ");
        const optionalArgsList = args.slice(sig.requiredCount).map((a) => this.printNode(a, 0)).join(", ");
        const optSlice = optionalArgsList ? `&[${optionalArgsList}]` : "&[]";
        const reqPart = requiredArgs ? `${requiredArgs}, ` : "";
        return `self.${methodName}(${reqPart}${optSlice})`;
      }
    }
    return void 0;
  }
  printCallExpression(node, identation) {
    const expression = node.expression;
    const shadowSafeRead = this.printShadowSafeReadCall(node);
    if (shadowSafeRead !== void 0)
      return shadowSafeRead;
    const shadowRead = this.printProHandlerShadowRead(node);
    if (shadowRead !== void 0)
      return shadowRead;
    if (expression.kind === SyntaxKind4.PropertyAccessExpression) {
      const exprText = expression.getText().trim();
      if (exprText === "console.log") {
        const args = node.arguments;
        if (args.length === 1) {
          const argText = this.printNode(args[0], 0).trim();
          const ref = argText.startsWith("Value::") ? `&${argText}` : argText.startsWith("&") ? argText : `&${argText}`;
          return `println_val(${ref})`;
        }
        const argParts = Array.from(args).map((a) => {
          const t = this.printNode(a, 0).trim();
          return t.startsWith("Value::") || !t.startsWith("&") ? `&${t}` : t;
        }).join(", ");
        return `println_val(${argParts})`;
      }
    }
    const outOfOrder = this.printOutOfOrderCallExpressionIfAny(node, identation);
    if (outOfOrder) {
      return this.rustBoxNativeStrCallIfNeeded(node, outOfOrder);
    }
    const nativeParse = this.printNativeParseCall(node);
    if (nativeParse !== void 0)
      return nativeParse;
    if (expression.kind === SyntaxKind4.PropertyAccessExpression && expression.expression.kind === SyntaxKind4.ThisKeyword && expression.name.escapedText === "json" && node.arguments.length === 1) {
      const argText = this.printNode(node.arguments[0], 0).trim();
      if (!argText.includes("self.")) {
        const withoutClone = argText.replace(/\.clone\(\)$/, "");
        return `json_stringify(${this.ensureRef(withoutClone)})`;
      }
    }
    return this.rustBoxNativeStrCallIfNeeded(node, super.printCallExpression(node, identation));
  }
  printThisKeyword(node, identation) {
    return "self";
  }
  // A string literal prints as the bare `"lit"` (`&str` — no String
  // allocation); a checker-proven string drops the redundant `Value::Str` box.
  printErrorConstructorArg(name, index, node, identation) {
    const kind = _optionalChain([_RustTranspiler, 'access', _1700 => _1700.RUST_ERROR_CONSTRUCTOR_ARGS, 'access', _1701 => _1701[name], 'optionalAccess', _1702 => _1702[index]]);
    if (kind === void 0) {
      return this.printNode(node, identation);
    }
    if (_typescript2.default.isStringLiteral(node) && !(node.text in this.StringLiteralReplacements)) {
      return this.quotedStringLiteral(node.text);
    }
    const printed = this.printNode(node, identation).trim();
    if (kind !== "msg" || !this.rustTypeIsString(node)) {
      return printed;
    }
    const payload = _nullishCoalesce(_nullishCoalesce(this.peelValueStrBox(printed), () => ( this.peelValueStrBox(this.stripOuterParens(printed)))), () => ( printed));
    return payload.replace(/\.into\(\)$/, "");
  }
  // `BadRequest` → `bad_request`: the class-to-runtime-fn name the ccxt
  // post-pass applies to `X::new(..)` calls.
  rustErrorConstructorName(className) {
    return className.replace(/([A-Z]+)([A-Z][a-z])/g, "$1_$2").replace(/([a-z\d])([A-Z])/g, "$1_$2").toLowerCase();
  }
  printNewExpression(node, identation) {
    let expression = _optionalChain([node, 'access', _1703 => _1703.expression, 'optionalAccess', _1704 => _1704.escapedText]);
    expression = expression ? expression : this.printNode(node.expression);
    if (expression === "Error") {
      const args2 = node.arguments.map((a) => this.printNode(a, identation)).join(", ");
      return args2 || "Value::Null";
    }
    const errorClassPattern = /^(?:[A-Z][a-zA-Z]*(?:Error|Required|Found|Failed|Rejected|Available|Exceeded|Limit|Pending|Funds|Address|Order|Cached|Fillable|Closed|Maintenance|Nonce|Timeout|Response|Settings|User|Supported|Implemented|Denied|Enabled|Suspended|Symbol|Change|Unavailable|Proxy|Set|Needed))$/;
    if (typeof expression === "string" && errorClassPattern.test(expression)) {
      const snake = this.rustErrorConstructorName(expression);
      const args2 = node.arguments.map((a, index) => this.printErrorConstructorArg(snake, index, a, identation)).join(", ");
      return `crate::exchange_errors::${snake}(${args2})`;
    }
    const args = typeof expression === "string" ? node.arguments.map((a, index) => this.printErrorConstructorArg(this.rustErrorConstructorName(expression), index, a, identation)).join(", ") : node.arguments.map((a) => this.printNode(a, identation)).join(", ");
    return `${expression}::new(${args})`;
  }
  printPropertyAccessExpression(node, identation) {
    const transformedProperty = this.transformPropertyAcessExpressionIfNeeded(node);
    if (transformedProperty) {
      return this.getIden(identation) + transformedProperty;
    }
    const rightSide = node.name.escapedText;
    const rawExpression = node.getText().trim();
    if (this.FullPropertyAccessReplacements.hasOwnProperty(rawExpression)) {
      return this.FullPropertyAccessReplacements[rawExpression];
    }
    const leftExpr = this.printNode(node.expression, 0);
    if (rightSide === "length") {
      return this.printArrayLength(node, 0, leftExpr);
    }
    if (_typescript2.default.isIdentifier(node.name) && this.isShallowValueReceiver(node.expression) && this.isNativeAccessPositionSafe(node) && !this.isNativeWriteTargetBase(node)) {
      const native = this.printNativeMapAccess(leftExpr, node.expression, String(rightSide));
      if (native)
        return native;
    }
    return `${leftExpr}.${rightSide}`;
  }
  toSnakeCaseName(name) {
    return name.replace(/([A-Z]+)([A-Z][a-z])/g, "$1_$2").replace(/([a-z\d])([A-Z])/g, "$1_$2").toLowerCase();
  }
  escapeRustStringLiteral(text) {
    return String(text).replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/\t/g, "\\t");
  }
  getCheckedTypeOf(node) {
    return _optionalChain([this, 'access', _1705 => _1705.checkerOrUndefined, 'call', _1706 => _1706(), 'optionalAccess', _1707 => _1707.getTypeAtLocation, 'call', _1708 => _1708(node)]);
  }
  typeSymbolOf(type) {
    if (type === void 0 || type === null)
      return void 0;
    return _nullishCoalesce(_nullishCoalesce(_optionalChain([type, 'access', _1709 => _1709.getSymbol, 'optionalCall', _1710 => _1710()]), () => ( type.symbol)), () => ( type.aliasSymbol));
  }
  /** Types declared outside ts/src (Date, Response, Array, Promise, …) are never
   *  backed by a plain `Value` map in the rust port. */
  isLibDeclaredType(type) {
    const declarations = _nullishCoalesce(_optionalChain([this, 'access', _1711 => _1711.typeSymbolOf, 'call', _1712 => _1712(type), 'optionalAccess', _1713 => _1713.declarations]), () => ( []));
    return declarations.some((d) => {
      const file = _nullishCoalesce(_optionalChain([d, 'optionalAccess', _1714 => _1714.getSourceFile, 'optionalCall', _1715 => _1715(), 'optionalAccess', _1716 => _1716.fileName]), () => ( ""));
      return /[\\/]lib\.[^\\/]*\.d\.ts$/.test(file) || /[\\/]node_modules[\\/]typescript[\\/]/.test(file);
    });
  }
  isClassInstanceType(type) {
    if (type === void 0)
      return false;
    if (type.flags & (_typescript2.default.TypeFlags.Union | _typescript2.default.TypeFlags.Intersection)) {
      return (_nullishCoalesce(type.types, () => ( []))).some((member) => this.isClassInstanceType(member));
    }
    const symbol = _nullishCoalesce(this.typeSymbolOf(type), () => ( type.aliasSymbol));
    if (_optionalChain([symbol, 'optionalAccess', _1717 => _1717.flags]) & _typescript2.default.SymbolFlags.Class)
      return true;
    const declarations = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1718 => _1718.declarations]), () => ( []));
    return declarations.some((d) => _typescript2.default.isClassDeclaration(d) || _typescript2.default.isClassExpression(d));
  }
  hasCallableShape(type) {
    const checker = this.getChecker();
    return checker.getSignaturesOfType(type, _typescript2.default.SignatureKind.Call).length > 0 || checker.getSignaturesOfType(type, _typescript2.default.SignatureKind.Construct).length > 0;
  }
  isProvenListType(type) {
    if (!(type.flags & _typescript2.default.TypeFlags.Object))
      return false;
    const objectFlags = (_nullishCoalesce(type.objectFlags, () => ( 0))) | (_nullishCoalesce(_optionalChain([type, 'access', _1719 => _1719.target, 'optionalAccess', _1720 => _1720.objectFlags]), () => ( 0)));
    if (objectFlags & _typescript2.default.ObjectFlags.Tuple)
      return true;
    const name = _optionalChain([this, 'access', _1721 => _1721.typeSymbolOf, 'call', _1722 => _1722(type), 'optionalAccess', _1723 => _1723.getName, 'optionalCall', _1724 => _1724()]);
    if (name === "Array" || name === "ReadonlyArray")
      return true;
    const targetName = _optionalChain([this, 'access', _1725 => _1725.typeSymbolOf, 'call', _1726 => _1726(type.target), 'optionalAccess', _1727 => _1727.getName, 'optionalCall', _1728 => _1728()]);
    return targetName === "Array" || targetName === "ReadonlyArray";
  }
  /** True only for object types the rust port represents as `Value::Dict`
   *  (plain interfaces / index-signature / literal types — never classes). */
  isProvenMapType(type) {
    if (type === void 0)
      return false;
    if (type.flags & _typescript2.default.TypeFlags.Union) {
      const parts = _nullishCoalesce(type.types, () => ( []));
      const nullish = parts.filter((p) => this.rustTypeIsNullish(p));
      const valueParts = parts.filter((p) => !this.rustTypeIsNullish(p));
      return nullish.length > 0 && valueParts.length > 0 && valueParts.every((p) => this.isProvenMapType(p));
    }
    if (!(type.flags & _typescript2.default.TypeFlags.Object))
      return false;
    if (this.isProvenListType(type))
      return false;
    if (this.hasCallableShape(type))
      return false;
    if (this.isClassInstanceType(type))
      return false;
    if (this.isLibDeclaredType(type))
      return false;
    const hasStringIndex = this.getChecker().getIndexTypeOfType(type, _typescript2.default.IndexKind.String) !== void 0;
    return hasStringIndex || this.typeSymbolOf(type) !== void 0;
  }
  /** `undefined` / `null` / `void` / `never` — a union member that carries no
   *  runtime value; `Value::Null` is the only box these ever get. */
  rustTypeIsNullish(type) {
    return type !== void 0 && (type.flags & (_typescript2.default.TypeFlags.Undefined | _typescript2.default.TypeFlags.Null | _typescript2.default.TypeFlags.Void | _typescript2.default.TypeFlags.Never)) !== 0;
  }
  isProvenMapExpression(node) {
    const type = this.getCheckedTypeOf(node);
    return type !== void 0 && this.isProvenMapType(type);
  }
  isProvenListExpression(node) {
    const type = this.getCheckedTypeOf(node);
    return type !== void 0 && this.isProvenListType(type);
  }
  /** RHS of a generator destructure that provably holds a `Value::Arr`: the
   *  checker-proven list, or a call whose callee returns an array literal on
   *  every path. */
  rustNativeListSource(node) {
    return this.isProvenListExpression(node) || this.rustCallReturnsProvenList(node);
  }
  /** `x.split(sep)` → the runtime `split`, which yields an array on every
   *  path (a non-string receiver gives the empty array, never a dict). */
  rustCallPrintsRuntimeSplit(node) {
    if (!_typescript2.default.isCallExpression(node) || node.arguments.length === 0)
      return false;
    const expression = node.expression;
    if (!_typescript2.default.isPropertyAccessExpression(expression))
      return false;
    if (expression.expression.kind === SyntaxKind4.ThisKeyword)
      return false;
    return String(expression.name.escapedText) === "split";
  }
  /** True when the call's value is always a runtime array: the `handle*AndParams`
   *  family and its exchange overrides declare `any`, so the checker cannot
   *  prove the `[T, Dict]` tuple the body always builds — walk the resolved
   *  callee instead. */
  rustCallReturnsProvenList(node) {
    if (!_typescript2.default.isCallExpression(node))
      return false;
    return this.rustProvenListCall(node, /* @__PURE__ */ new Set());
  }
  rustProvenListCall(node, stack) {
    if (this.rustCallPrintsRuntimeSplit(node))
      return true;
    const declaration = this.rustCalleeDeclaration(node);
    if (declaration === void 0)
      return false;
    return this.rustFunctionReturnsArrayLiteral(declaration, stack);
  }
  /** Implementation of a `x.y(..)` call, when the checker resolves one. */
  rustCalleeDeclaration(node) {
    if (!_typescript2.default.isCallExpression(node))
      return void 0;
    if (!_typescript2.default.isPropertyAccessExpression(node.expression))
      return void 0;
    try {
      const signature = this.getChecker().getResolvedSignature(node);
      return _nullishCoalesce(_optionalChain([signature, 'optionalAccess', _1729 => _1729.declaration]), () => ( void 0));
    } catch (e) {
      return void 0;
    }
  }
  /** Every `return` in the function's own body builds an array literal, or
   *  delegates to a call that does. `throw` and fall-through (the printer's
   *  `Value::Null`) read the same through both forms. */
  rustFunctionReturnsArrayLiteral(declaration, stack) {
    if (stack.has(declaration))
      return false;
    const body = declaration.body;
    if (body === void 0 || !_typescript2.default.isBlock(body))
      return false;
    stack.add(declaration);
    try {
      let returns = 0;
      let all = true;
      const visit = (node) => {
        if (!all)
          return;
        if (node !== body && _typescript2.default.isFunctionLike(node))
          return;
        if (_typescript2.default.isReturnStatement(node)) {
          returns++;
          const expression = node.expression;
          if (expression === void 0 || !_typescript2.default.isArrayLiteralExpression(expression)) {
            if (expression !== void 0 && _typescript2.default.isCallExpression(expression) && this.rustProvenListCall(expression, stack)) {
              return;
            }
            if (expression !== void 0 && _typescript2.default.isParenthesizedExpression(expression) && _typescript2.default.isArrayLiteralExpression(expression.expression)) {
              return;
            }
            all = false;
            return;
          }
          return;
        }
        _typescript2.default.forEachChild(node, visit);
      };
      _typescript2.default.forEachChild(body, visit);
      return all && returns > 0;
    } finally {
      stack.delete(declaration);
    }
  }
  /** Native list-index read of a generator temp (`__destr_tmp.as_array()…`). */
  printNativeListIndex(receiverText, index) {
    return `${receiverText}.as_array().and_then(|__arr| __arr.get(${index})).cloned().unwrap_or(Value::Null)`;
  }
  /** Native read for one chain level, or undefined to keep `get_value`. */
  printNativeContainerAccess(receiverText, receiverNode, keyNode) {
    const shadow = this.rustParamShadowOf(receiverNode);
    if (shadow !== void 0) {
      const native = this.printShadowContainerRead(shadow, keyNode);
      if (native !== void 0)
        return native;
    }
    if (_typescript2.default.isStringLiteralLike(keyNode)) {
      return this.printNativeMapAccess(receiverText, receiverNode, keyNode.text);
    }
    if (_typescript2.default.isNumericLiteral(keyNode)) {
      const index = Number(keyNode.text);
      if (!Number.isInteger(index) || index < 0)
        return void 0;
      if (!this.isProvenListExpression(receiverNode))
        return void 0;
      return this.printNativeListIndex(receiverText, index);
    }
    const dictRead = this.printNativeDynamicMapAccess(receiverText, receiverNode, keyNode);
    if (dictRead !== void 0)
      return dictRead;
    return this.printNativeDynamicListIndex(receiverText, receiverNode, keyNode);
  }
  /** `get_value(&X, &i)` for a checker-proven list `X` and a dynamic integer
   *  index local `i`: the runtime's own array branch, spelled natively.
   *  `get_value` reaches its array arm for an `Arr` receiver and its default
   *  (`Value::Null`) otherwise, so the emitted match reproduces both — an
   *  `Int` index by value (a negative or out-of-range index misses), a
   *  numeric string by parse, anything else a miss. */
  printNativeDynamicListIndex(receiverText, receiverNode, keyNode) {
    if (!this.isProvenListExpression(receiverNode))
      return void 0;
    if (!this.isRustValueIndexKey(keyNode))
      return void 0;
    if (this.isWriteBackBindRead(keyNode.parent))
      return void 0;
    const keyText = this.printNode(keyNode, 0).trim();
    if (!keyText || keyText.startsWith("&"))
      return void 0;
    return `${receiverText}.as_array().and_then(|__arr| match &${keyText} { Value::Int(__n) => __arr.get(*__n as usize), Value::Str(__s) => __s.parse::<usize>().ok().and_then(|__n| __arr.get(__n)), _ => None }).cloned().unwrap_or(Value::Null)`;
  }
  /** True when this read initialises a local that the very next statement in
   *  the same block mutates (`x['k'] = v` -> `add_element_to_object(&mut x…)`,
   *  `x.push(v)` -> `append_to_array(&mut x…)`). */
  isWriteBackBindRead(read) {
    const declaration = read === void 0 ? void 0 : read.parent;
    if (declaration === void 0 || !_typescript2.default.isVariableDeclaration(declaration) || declaration.initializer !== read)
      return false;
    if (!_typescript2.default.isIdentifier(declaration.name))
      return false;
    const name = String(declaration.name.escapedText);
    let statement = declaration;
    while (statement !== void 0 && !_typescript2.default.isStatement(statement))
      statement = statement.parent;
    const siblings = _nullishCoalesce(_optionalChain([statement, 'optionalAccess', _1730 => _1730.parent, 'optionalAccess', _1731 => _1731.statements]), () => ( []));
    const at = siblings.indexOf(statement);
    if (at < 0 || at + 1 >= siblings.length)
      return false;
    return this.rustStatementMutatesLocal(siblings[at + 1], name);
  }
  /** A statement containing a write into a local (`x['k'] = v`, `x.k = v`,
   *  `x.push(v)`). */
  rustStatementMutatesLocal(node, name) {
    let mutated = false;
    const visit = (n) => {
      if (mutated)
        return;
      if (_typescript2.default.isBinaryExpression(n) && rustIsAssignmentOperator(n.operatorToken.kind) && this.rootPlaceText(n.left) === name) {
        mutated = true;
        return;
      }
      if (_typescript2.default.isCallExpression(n) && _typescript2.default.isPropertyAccessExpression(n.expression) && n.expression.name.text === "push" && this.rootPlaceText(n.expression.expression) === name) {
        mutated = true;
        return;
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(node, visit);
    return mutated;
  }
  /** A dynamic index the printer emits as a `Value` number: a `let x = <numeric
   *  literal>` declaration of the same function (the C-style loop counter).
   *  Any other shape keeps the helper — the printed local could be a native
   *  `i64`/`f64`, which the `Value` match would not compile against. */
  isRustValueIndexKey(node) {
    let current = node;
    while (_typescript2.default.isParenthesizedExpression(current) || _typescript2.default.isAsExpression(current) || _typescript2.default.isNonNullExpression(current)) {
      current = current.expression;
    }
    if (!_typescript2.default.isIdentifier(current))
      return false;
    const type = this.getCheckedTypeOf(current);
    if (type === void 0 || !(type.flags & (_typescript2.default.TypeFlags.Number | _typescript2.default.TypeFlags.NumberLiteral)))
      return false;
    const declaration = this.rustDeclarationOfIdentifier(current);
    if (declaration === void 0 || !_typescript2.default.isVariableDeclaration(declaration) || declaration.initializer === void 0)
      return false;
    let initializer = declaration.initializer;
    while (_typescript2.default.isParenthesizedExpression(initializer) || _typescript2.default.isAsExpression(initializer) || _typescript2.default.isNonNullExpression(initializer)) {
      initializer = initializer.expression;
    }
    return _typescript2.default.isNumericLiteral(initializer);
  }
  // Typed-parameter dict reads: a checker-proven plain dict param (`Dict`, `Dictionary<T>`,
  // `Market`-style alias) holds the dict or `Value::Null`, so `get_value`'s marker routes cannot
  // fire and the read is `m.get(k)`. The key must be a proven `Str` box so `.as_str()` reproduces it.
  /** The parameter declaration behind a receiver when its *annotation* proves
   *  a plain dict; undefined otherwise (no proof → keep the helper). */
  rustProvenDictParameter(node) {
    const declaration = this.rustDeclarationOfIdentifier(node);
    if (declaration === void 0 || !_typescript2.default.isParameter(declaration))
      return void 0;
    if (declaration.type === void 0)
      return void 0;
    const type = this.getCheckedTypeOf(declaration.type);
    if (type === void 0 || !this.isProvenMapType(type))
      return void 0;
    return this.rustLocalIsReassigned(declaration, String(declaration.name.escapedText)) ? void 0 : declaration;
  }
  /** `Str` (`string | undefined`) — the key box is `Value::Str` or Null. */
  rustKeyIsProvenString(node) {
    const type = this.getCheckedTypeOf(node);
    if (type === void 0)
      return false;
    const parts = type.flags & _typescript2.default.TypeFlags.Union ? _nullishCoalesce(type.types, () => ( [])) : [type];
    let strings = 0;
    for (const part of parts) {
      if (part.flags & (_typescript2.default.TypeFlags.String | _typescript2.default.TypeFlags.StringLiteral)) {
        strings++;
        continue;
      }
      if (this.rustTypeIsNullish(part))
        continue;
      return false;
    }
    return strings > 0;
  }
  /** The local/parameter proof of a dynamic-key map read: a parameter whose
   *  annotation proves a plain dict (B-25), or any local whose checker type
   *  proves a plain map and which nothing in the enclosing function
   *  re-assigns (D2). Returns the proven declaration. */
  rustProvenDynamicMapReceiver(node) {
    const parameter = this.rustProvenDictParameter(node);
    if (parameter !== void 0)
      return parameter;
    if (!_typescript2.default.isIdentifier(node))
      return void 0;
    const declaration = this.rustDeclarationOfIdentifier(node);
    if (declaration === void 0 || !_typescript2.default.isVariableDeclaration(declaration))
      return void 0;
    if (declaration.initializer === void 0)
      return void 0;
    if (!this.isProvenMapExpression(node))
      return void 0;
    if (this.rustLocalIsReassigned(declaration, String(node.escapedText)))
      return void 0;
    return declaration;
  }
  /** The element-access read a key node belongs to (`x[k]`, `x[(k)]`). */
  rustElementReadOfKey(keyNode) {
    let current = keyNode;
    while (current !== void 0 && (_typescript2.default.isParenthesizedExpression(current) || _typescript2.default.isAsExpression(current) || _typescript2.default.isNonNullExpression(current))) {
      current = current.parent;
    }
    const parent = current === void 0 ? void 0 : current.parent;
    if (parent === void 0 || !_typescript2.default.isElementAccessExpression(parent) || parent.argumentExpression !== current)
      return void 0;
    return parent;
  }
  /** `x[k]` where `x` is a proven-dict parameter and `k` a proven string. */
  printNativeDynamicMapAccess(receiverText, receiverNode, keyNode) {
    if (this.rustProvenDynamicMapReceiver(receiverNode) === void 0)
      return void 0;
    if (!this.rustKeyIsProvenString(keyNode))
      return void 0;
    const read = this.rustElementReadOfKey(keyNode);
    if (read !== void 0 && this.isWriteBackBindRead(read))
      return void 0;
    const keyText = this.printNode(keyNode, 0).trim();
    if (!/^[A-Za-z_][A-Za-z0-9_]*$/.test(keyText))
      return void 0;
    if (this.rustNodeIsKeyUnsafePlace(keyText))
      return void 0;
    return `${receiverText}.as_map().and_then(|__m| ${keyText}.as_str().and_then(|__k| __m.get(__k))).cloned().unwrap_or(Value::Null)`;
  }
  /** Keys `get_value` serves from the book store / cache bucket / live
   *  snapshot instead of the dict itself — a dynamic read cannot prove the
   *  key away, so a place named after one stays boxed. */
  rustNodeIsKeyUnsafePlace(keyText) {
    return _RustTranspiler.RUST_DICT_LOCAL_UNSAFE_KEYS.has(keyText);
  }
  rustParamShadowEmittedSet() {
    const src = this.getSrc();
    if (this.paramShadowEmitted === void 0 || this.paramShadowEmitted.src !== src) {
      this.paramShadowEmitted = { src, fns: /* @__PURE__ */ new Set() };
    }
    return this.paramShadowEmitted.fns;
  }
  rustParamShadowTables() {
    const src = this.getSrc();
    if (this.paramShadowCache === void 0 || this.paramShadowCache.src !== src) {
      this.paramShadowCache = { src, tables: /* @__PURE__ */ new Map() };
    }
    return this.paramShadowCache.tables;
  }
  /** Emitted shadow lines for a function, or '' when no parameter qualifies.
   *  The caller must use this before printing the body statements (it both
   *  registers the function as shadowed and computes the lines). */
  rustParamShadowLines(fn, identation) {
    const table = this.rustParamShadowTable(fn);
    if (table.size === 0)
      return "";
    const idn = this.getIden(identation);
    const lines = [];
    for (const shadow of table.values()) {
      const empty = `__${shadow.name}_empty`;
      if (this.rustFunctionDeclaresName(fn, empty))
        continue;
      const map = shadow.kind === RUST_PARAM_SHADOWS.MAP;
      const accessor = map ? "as_map" : "as_array";
      lines.push(`${idn}let ${empty} = ${map ? "indexmap::IndexMap::new()" : "Vec::new()"};`);
      lines.push(`${idn}let ${shadow.name} = ${shadow.name}.${accessor}().unwrap_or(&${empty});`);
    }
    if (lines.length === 0)
      return "";
    this.rustParamShadowEmittedSet().add(fn);
    return lines.join("\n") + "\n";
  }
  rustParamShadowTable(fn) {
    const tables = this.rustParamShadowTables();
    let table = tables.get(fn);
    if (table === void 0) {
      try {
        table = this.collectRustParamShadows(fn);
      } catch (e) {
        table = /* @__PURE__ */ new Map();
      }
      tables.set(fn, table);
    }
    return table;
  }
  /** The shadow a read receiver resolves to, or undefined (no proof → helper). */
  rustParamShadowOf(node) {
    if (node === void 0)
      return void 0;
    let current = node;
    while (current !== void 0 && (_typescript2.default.isParenthesizedExpression(current) || _typescript2.default.isAsExpression(current) || _typescript2.default.isNonNullExpression(current))) {
      current = current.expression;
    }
    if (current === void 0 || !_typescript2.default.isIdentifier(current))
      return void 0;
    const name = String(current.escapedText);
    const declaration = this.rustDeclarationOfIdentifier(current);
    if (declaration === void 0 || !_typescript2.default.isParameter(declaration))
      return void 0;
    const emitted = this.rustParamShadowEmittedSet();
    let scope = current.parent;
    while (scope !== void 0) {
      if (_typescript2.default.isFunctionLike(scope)) {
        const entry = emitted.has(scope) ? this.rustParamShadowTable(scope).get(name) : void 0;
        if (entry !== void 0 && entry.declaration === declaration)
          return entry;
      }
      scope = scope.parent;
    }
    return void 0;
  }
  /** True when the enclosing function already binds this name somewhere. */
  rustFunctionDeclaresName(fn, name) {
    let found = false;
    const visit = (node) => {
      if (found)
        return;
      if (node !== fn && _typescript2.default.isFunctionLike(node))
        return;
      if (_typescript2.default.isVariableDeclaration(node) && _typescript2.default.isIdentifier(node.name) && node.name.text === name) {
        found = true;
        return;
      }
      if (_typescript2.default.isParameter(node) && _typescript2.default.isIdentifier(node.name) && node.name.text === name) {
        found = true;
        return;
      }
      _typescript2.default.forEachChild(node, visit);
    };
    _typescript2.default.forEachChild(fn, visit);
    return found;
  }
  collectRustParamShadows(fn) {
    const table = /* @__PURE__ */ new Map();
    const parameters = _nullishCoalesce(fn.parameters, () => ( []));
    const body = fn.body;
    if (body === void 0)
      return table;
    for (const param of parameters) {
      if (!_typescript2.default.isParameter(param) || !_typescript2.default.isIdentifier(param.name))
        continue;
      const name = String(param.name.escapedText);
      if (name === "optional_args")
        continue;
      if (param.type === void 0)
        continue;
      const type = this.getCheckedTypeOf(param.type);
      if (type === void 0)
        continue;
      let kind;
      if (this.isProvenMapType(type)) {
        kind = RUST_PARAM_SHADOWS.MAP;
      } else if (this.isProvenShadowListType(type)) {
        kind = RUST_PARAM_SHADOWS.LIST;
      }
      if (kind === void 0)
        continue;
      if (this.rustParameterIsClientHandle(param))
        continue;
      if (this.rustParamShadowUseCensus(fn, param, kind) === void 0)
        continue;
      table.set(name, { kind, name, declaration: param });
    }
    return table;
  }
  /** A checker-proven array parameter type (`Vec<Value>` on the rust side);
   *  tuples are excluded (their printed shape is not a plain `Vec`). */
  isProvenShadowListType(type) {
    if (type === void 0)
      return false;
    if (!(type.flags & _typescript2.default.TypeFlags.Object))
      return false;
    const objectFlags = (_nullishCoalesce(type.objectFlags, () => ( 0))) | (_nullishCoalesce(_optionalChain([type, 'access', _1732 => _1732.target, 'optionalAccess', _1733 => _1733.objectFlags]), () => ( 0)));
    if (objectFlags & _typescript2.default.ObjectFlags.Tuple)
      return false;
    if (this.hasCallableShape(type))
      return false;
    if (this.isClassInstanceType(type))
      return false;
    const name = _optionalChain([this, 'access', _1734 => _1734.typeSymbolOf, 'call', _1735 => _1735(type), 'optionalAccess', _1736 => _1736.getName, 'optionalCall', _1737 => _1737()]);
    if (name === "Array" || name === "ReadonlyArray")
      return true;
    const targetName = _optionalChain([this, 'access', _1738 => _1738.typeSymbolOf, 'call', _1739 => _1739(type.target), 'optionalAccess', _1740 => _1740.getName, 'optionalCall', _1741 => _1741()]);
    return targetName === "Array" || targetName === "ReadonlyArray";
  }
  /** Every reference to the parameter must be a printable read, and at least
   *  one must exist; anything else (a write, a `Value` pass-through, a Null
   *  comparison, a marker key) answers undefined and the parameter keeps its
   *  box. */
  rustParamShadowUseCensus(fn, param, kind) {
    const name = String(param.name.escapedText);
    const paramSymbol = this.rustSymbolOf(param.name);
    if (paramSymbol === void 0)
      return void 0;
    let reads = 0;
    let ok = true;
    const visit = (node) => {
      if (!ok)
        return;
      if (_typescript2.default.isIdentifier(node) && node.text === name && node !== param.name) {
        if (this.rustSymbolOf(node) !== paramSymbol || !this.rustParamUseIsRead(node, kind)) {
          ok = false;
          return;
        }
        reads++;
      }
      _typescript2.default.forEachChild(node, visit);
    };
    _typescript2.default.forEachChild(fn.body, visit);
    return ok && reads > 0 ? { reads } : void 0;
  }
  /** One reference of a shadow candidate: true only for a read the shadow can
   *  print exactly (same proofs the emitted forms re-check). */
  rustParamUseIsRead(id, kind) {
    const parent = id.parent;
    if (parent === void 0)
      return false;
    if (_typescript2.default.isElementAccessExpression(parent) && parent.expression === id) {
      if (this.isNativeWriteTargetBase(id))
        return false;
      if (!this.isNativeAccessPositionSafe(id))
        return false;
      if (this.isWriteBackBindRead(parent))
        return false;
      return this.rustShadowKeyIsReadable(parent.argumentExpression, kind);
    }
    if (_typescript2.default.isPropertyAccessExpression(parent) && parent.expression === id) {
      return kind === RUST_PARAM_SHADOWS.LIST && String(parent.name.escapedText) === "length";
    }
    if (_typescript2.default.isBinaryExpression(parent) && parent.operatorToken.kind === SyntaxKind4.InKeyword && parent.right === id) {
      if (kind !== RUST_PARAM_SHADOWS.MAP)
        return false;
      return this.rustShadowKeyIsReadable(parent.left, RUST_PARAM_SHADOWS.MAP);
    }
    if (_typescript2.default.isCallExpression(parent) && kind === RUST_PARAM_SHADOWS.MAP) {
      if (this.rustShadowSafeCallee(parent) === void 0)
        return false;
      const args = parent.arguments;
      if (args[0] !== id || args.length < 2 || args.length > 3)
        return false;
      return this.rustShadowKeyIsLiteral(args[1]);
    }
    return false;
  }
  /** A key a shadow read can print: dicts take a bare string literal or a
   *  proven-string place, lists a literal non-negative index; the `safe_*`
   *  inline takes the literal key form only. Marker-route key names and keys
   *  whose text needs escaping are excluded. */
  rustShadowKeyIsReadable(key, kind) {
    if (key === void 0)
      return false;
    if (kind === RUST_PARAM_SHADOWS.MAP) {
      if (_typescript2.default.isStringLiteralLike(key))
        return this.rustShadowKeyIsLiteral(key);
      if (_typescript2.default.isIdentifier(key)) {
        return this.rustKeyIsProvenString(key) && !this.rustNodeIsKeyUnsafePlace(String(key.escapedText));
      }
      return false;
    }
    if (_typescript2.default.isNumericLiteral(key)) {
      const index = Number(key.text);
      return Number.isInteger(index) && index >= 0;
    }
    return false;
  }
  rustShadowKeyIsLiteral(key) {
    if (key === void 0 || !_typescript2.default.isStringLiteralLike(key))
      return false;
    const text = String(key.text);
    return this.rustShadowKeyLiteral(text) && !this.rustNodeIsKeyUnsafePlace(text);
  }
  /** Keys whose text is safe to inline into a rust string literal. */
  rustShadowKeyLiteral(text) {
    return /^[A-Za-z0-9_./-]*$/.test(text) && text.length > 0;
  }
  /** `this.safeString`-style callee of a call, or undefined. */
  rustShadowSafeCallee(node) {
    const callee = node.expression;
    if (callee === void 0 || !_typescript2.default.isPropertyAccessExpression(callee))
      return void 0;
    if (callee.expression.kind !== SyntaxKind4.ThisKeyword)
      return void 0;
    const name = String(callee.name.escapedText);
    return RUST_PARAM_SHADOWS.SAFE_READS.has(name) ? name : void 0;
  }
  /** `x['k']` / `x[i]` / `'k' in x` on a shadowed parameter: the native read,
   *  or undefined to keep the helper (the census guarantees it never happens
   *  for an emitted shadow). */
  printShadowContainerRead(shadow, keyNode) {
    const key = keyNode;
    if (shadow.kind === RUST_PARAM_SHADOWS.MAP) {
      if (_typescript2.default.isStringLiteralLike(key)) {
        const text = String(key.text);
        if (!this.rustShadowKeyLiteral(text) || this.rustNodeIsKeyUnsafePlace(text))
          return void 0;
        return `${shadow.name}.get("${text}").cloned().unwrap_or(Value::Null)`;
      }
      if (_typescript2.default.isIdentifier(key) && this.rustKeyIsProvenString(key) && !this.rustNodeIsKeyUnsafePlace(String(key.escapedText))) {
        const keyText = this.printNode(key, 0).trim();
        if (!/^[A-Za-z_][A-Za-z0-9_]*$/.test(keyText))
          return void 0;
        return `${keyText}.as_str().and_then(|__k| ${shadow.name}.get(__k)).cloned().unwrap_or(Value::Null)`;
      }
      return void 0;
    }
    if (_typescript2.default.isNumericLiteral(key)) {
      const index = Number(key.text);
      if (!Number.isInteger(index) || index < 0)
        return void 0;
      return `${shadow.name}.get(${index}).cloned().unwrap_or(Value::Null)`;
    }
    return void 0;
  }
  /** `'k' in x` on a shadowed dict parameter. */
  printShadowInOperator(shadow, keyNode) {
    const key = keyNode;
    if (shadow.kind !== RUST_PARAM_SHADOWS.MAP)
      return void 0;
    if (_typescript2.default.isStringLiteralLike(key)) {
      const text = String(key.text);
      if (!this.rustShadowKeyLiteral(text) || this.rustNodeIsKeyUnsafePlace(text))
        return void 0;
      return `Value::Bool(${shadow.name}.contains_key("${text}"))`;
    }
    if (_typescript2.default.isIdentifier(key) && this.rustKeyIsProvenString(key) && !this.rustNodeIsKeyUnsafePlace(String(key.escapedText))) {
      const keyText = this.printNode(key, 0).trim();
      if (!/^[A-Za-z_][A-Za-z0-9_]*$/.test(keyText))
        return void 0;
      return `Value::Bool(${keyText}.as_str().map(|__k| ${shadow.name}.contains_key(__k)).unwrap_or(false))`;
    }
    return void 0;
  }
  /** `x.length` on a shadowed list parameter — `get_array_length` natively. */
  printShadowLength(shadow) {
    if (shadow.kind !== RUST_PARAM_SHADOWS.LIST)
      return void 0;
    return `Value::Int(${shadow.name}.len() as i64)`;
  }
  /** `this.safe<Type>(x, 'k'[, default])` on a shadowed dict parameter: the
   *  runtime helper's exact semantics over `.get(..)`. */
  printShadowSafeReadCall(node) {
    const callee = this.rustShadowSafeCallee(node);
    if (callee === void 0)
      return void 0;
    const args = _nullishCoalesce(node.arguments, () => ( []));
    if (args.length < 2 || args.length > 3)
      return void 0;
    const shadow = this.rustParamShadowOf(args[0]);
    if (shadow === void 0 || shadow.kind !== RUST_PARAM_SHADOWS.MAP)
      return void 0;
    const key = args[1];
    if (!_typescript2.default.isStringLiteralLike(key))
      return void 0;
    const text = String(key.text);
    if (!this.rustShadowKeyLiteral(text) || this.rustNodeIsKeyUnsafePlace(text))
      return void 0;
    const fallback = args.length === 3 ? this.printNode(args[2], 0).trim() : "Value::Null";
    const get = `${shadow.name}.get("${text}")`;
    const wrap = (text2) => `(${text2})`;
    switch (callee) {
      case "safeValue":
        return wrap(`match ${get} { Some(__v) if !matches!(__v, Value::Null) && !matches!(__v, Value::Str(__s) if __s.is_empty()) => __v.clone(), _ => ${fallback} }`);
      case "safeString":
        return wrap(`match ${get} { Some(Value::Str(__s)) if !__s.is_empty() => Value::Str(__s.clone()), Some(Value::Int(__n)) => Value::Str(__n.to_string().into()), Some(Value::Float(__f)) => Value::Str(__f.to_string().into()), _ => ${fallback} }`);
      case "safeInteger":
        return wrap(`match ${get} { Some(Value::Int(__n)) => Value::Int(*__n), Some(Value::Float(__f)) => Value::Int(*__f as i64), Some(Value::Str(__s)) => match __s.parse::<i64>() { Ok(__n) => Value::Int(__n), Err(_) => match __s.parse::<f64>() { Ok(__f) if __f.is_finite() => Value::Int(__f as i64), _ => ${fallback} } }, _ => ${fallback} }`);
      case "safeNumber":
        return wrap(`match ${get} { Some(Value::Float(__f)) => Value::Float(*__f), Some(Value::Int(__n)) => Value::Float(*__n as f64), Some(Value::Str(__s)) => match __s.parse::<f64>() { Ok(__n) => Value::Float(__n), Err(_) => ${fallback} }, _ => ${fallback} }`);
      case "safeBool":
        return wrap(`match ${get} { Some(Value::Bool(__b)) => Value::Bool(*__b), _ => ${fallback} }`);
      case "safeDict":
        return wrap(`match ${get} { Some(__v) if matches!(__v, Value::Dict(_)) => __v.clone(), _ => ${fallback} }`);
      case "safeList":
        return wrap(`match ${get} { Some(__v) if matches!(__v, Value::Arr(_)) => __v.clone(), _ => ${fallback} }`);
    }
    return void 0;
  }
  /** The `message: Dict` parameter of a WS handler method (2nd param of a
   *  `handle*` method), undefined when unproven or written (D2). */
  rustProHandlerMessageParam(node) {
    if (node === void 0 || !_typescript2.default.isMethodDeclaration(node) || node.body === void 0)
      return void 0;
    const params = _nullishCoalesce(node.parameters, () => ( []));
    if (params.length < 2)
      return void 0;
    const methodName = node.name;
    if (methodName === void 0 || !/^handle[A-Z]/.test(String(_nullishCoalesce(methodName.escapedText, () => ( "")))))
      return void 0;
    const param = params[1];
    if (param === void 0 || !_typescript2.default.isIdentifier(param.name) || param.type === void 0)
      return void 0;
    if (param.initializer !== void 0 || param.questionToken !== void 0)
      return void 0;
    const clientParam = params[0];
    if (clientParam === void 0 || clientParam.type === void 0)
      return void 0;
    const clientType = this.getCheckedTypeOf(clientParam.type);
    if (clientType === void 0 || !this.isClassInstanceType(clientType))
      return void 0;
    const type = this.getCheckedTypeOf(param.type);
    if (type === void 0 || !this.isProvenMapType(type))
      return void 0;
    const name = String(param.name.escapedText);
    return this.rustProHandlerParamIsWritten(param, name) ? void 0 : param;
  }
  /** D2: a write rooted at the parameter (reassignment, element/property
   *  write, a merge/splice onto it) can reshape the dict after the shadow is
   *  taken — the shadow is skipped and every read keeps the helper. */
  rustProHandlerParamIsWritten(param, name) {
    if (this.rustLocalIsReassigned(param, name))
      return true;
    const scope = this.rustEnclosingFunction(param);
    if (scope === void 0)
      return true;
    const merging = ["deepExtend", "extend", "addElementToObject", "remove"];
    let written = false;
    const visit = (n) => {
      if (written)
        return;
      if (_typescript2.default.isBinaryExpression(n) && rustIsAssignmentOperator(n.operatorToken.kind) && this.rootPlaceText(n.left) === name) {
        written = true;
        return;
      }
      if (_typescript2.default.isCallExpression(n) && n.arguments.length > 0 && _typescript2.default.isIdentifier(n.arguments[0]) && n.arguments[0].escapedText === name) {
        const callee = n.expression;
        const calleeName = _typescript2.default.isPropertyAccessExpression(callee) ? String(_nullishCoalesce(_optionalChain([callee, 'access', _1742 => _1742.name, 'optionalAccess', _1743 => _1743.escapedText]), () => ( ""))) : _typescript2.default.isIdentifier(callee) ? String(_nullishCoalesce(callee.escapedText, () => ( ""))) : "";
        if (merging.includes(calleeName)) {
          written = true;
          return;
        }
      }
      if (_typescript2.default.isCallExpression(n) && _typescript2.default.isPropertyAccessExpression(n.expression) && _optionalChain([n, 'access', _1744 => _1744.expression, 'access', _1745 => _1745.name, 'optionalAccess', _1746 => _1746.text]) === "push" && this.rootPlaceText(n.expression.expression) === name) {
        written = true;
        return;
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return written;
  }
  /** Shadow plan for a handler: the parameter plus the two binding lines,
   *  present only when some body read actually turns native (no dead shed). */
  rustProHandlerShadowPlan(node, identation) {
    const param = this.rustProHandlerMessageParam(node);
    if (param === void 0)
      return void 0;
    if (this.rustParamShadowTable(node).has(String(param.name.escapedText)))
      return void 0;
    const saved = this.rustProHandlerShadowParam;
    this.rustProHandlerShadowParam = param;
    let hasRead = false;
    const scope = this.rustEnclosingFunction(param);
    const visit = (n) => {
      if (hasRead)
        return;
      if (_typescript2.default.isCallExpression(n) && this.printProHandlerShadowRead(n, true) !== void 0) {
        hasRead = true;
        return;
      }
      _typescript2.default.forEachChild(n, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    this.rustProHandlerShadowParam = saved;
    if (!hasRead)
      return void 0;
    const name = String(param.name.escapedText);
    const view = _RustTranspiler.PRO_HANDLER_SHADOW_NAME;
    const arc = `${view}_arc`;
    const map = "indexmap::IndexMap<String, Value>";
    const ind = this.getIden(identation + 2);
    const lines = `${ind}let ${arc}: std::sync::Arc<${map}> = (match &${name} { Value::Dict(__d) => __d.clone(), _ => std::sync::Arc::new(indexmap::IndexMap::new()) });
${ind}let ${view}: &${map} = &${arc};
`;
    return { param, lines };
  }
  /** The `safe_*` call on the shadowed parameter prints as a native
   *  `.get("k")` match, or undefined when the call is not one. In `probe`
   *  mode the shape is checked without printing (the pre-scan must not print
   *  a node twice). */
  printProHandlerShadowRead(node, probe = false) {
    const param = this.rustProHandlerShadowParam;
    if (param === void 0 || node === void 0 || !_typescript2.default.isCallExpression(node))
      return void 0;
    const callee = node.expression;
    if (callee === void 0 || !_typescript2.default.isPropertyAccessExpression(callee))
      return void 0;
    if (callee.expression.kind !== SyntaxKind4.ThisKeyword)
      return void 0;
    const kind = _RustTranspiler.PRO_HANDLER_SHADOW_SAFE_READS[String(_nullishCoalesce(_optionalChain([callee, 'access', _1747 => _1747.name, 'optionalAccess', _1748 => _1748.escapedText]), () => ( "")))];
    if (kind === void 0)
      return void 0;
    const args = _nullishCoalesce(node.arguments, () => ( []));
    if (args.length < 2 || args.length > 3)
      return void 0;
    const receiver = args[0];
    if (receiver === void 0 || receiver.kind !== SyntaxKind4.Identifier)
      return void 0;
    if (this.rustDeclarationOfIdentifier(receiver) !== param)
      return void 0;
    const key = args[1];
    if (key === void 0 || !_typescript2.default.isStringLiteral(key))
      return void 0;
    const keyText = key.text;
    if (keyText === "" || _RustTranspiler.RUST_DICT_LOCAL_UNSAFE_KEYS.has(keyText))
      return void 0;
    if (args.length === 3 && !this.rustProHandlerShadowDefaultShape(args[2]))
      return void 0;
    if (probe)
      return "native";
    const dflt = args.length === 3 ? this.rustProHandlerShadowDefault(args[2]) : "Value::Null";
    if (dflt === void 0)
      return void 0;
    return this.rustProHandlerShadowReadText(kind, this.escapeRustStringLiteral(keyText), dflt);
  }
  /** A miss-arm default the match can hold: absent (`Value::Null`) or a
   *  literal; a computed default keeps the helper (its Value is not
   *  re-printable inside an arm without re-evaluating it twice). */
  rustProHandlerShadowDefaultShape(node) {
    return node.kind === SyntaxKind4.StringLiteral || node.kind === SyntaxKind4.NumericLiteral || node.kind === SyntaxKind4.TrueKeyword || node.kind === SyntaxKind4.FalseKeyword || node.kind === SyntaxKind4.NullKeyword || node.kind === SyntaxKind4.ArrayLiteralExpression || node.kind === SyntaxKind4.ObjectLiteralExpression || _typescript2.default.isIdentifier(node) && node.escapedText === "undefined";
  }
  rustProHandlerShadowDefault(node) {
    if (!this.rustProHandlerShadowDefaultShape(node))
      return void 0;
    const text = this.printNode(node, 0).trim();
    return text === "" ? void 0 : text;
  }
  /** Exact native form of the runtime `_k` helper: same value kinds, same
   *  empty-string-is-missing rule, same default (verified against
   *  `exchange_stubs.rs`). `.cloned()` keeps the emitted line clone-free for
   *  the ccxt clone-pruning passes; the parenthesised `match` keeps the
   *  driver's `};` trailing-block replacement off the statement's `;`. */
  rustProHandlerShadowReadText(kind, key, dflt) {
    const m = _RustTranspiler.PRO_HANDLER_SHADOW_NAME;
    const at = `${m}.get("${key}").cloned()`;
    switch (kind) {
      case "value":
        return `(match ${at} { Some(Value::Str(__s)) if __s.is_empty() => ${dflt}, Some(__v) => __v, None => ${dflt} })`;
      case "string":
        return `(match ${at} { Some(Value::Str(__s)) if !__s.is_empty() => Value::Str(__s), Some(Value::Int(__n)) => Value::Str(__n.to_string().into()), Some(Value::Float(__f)) => Value::Str(__f.to_string().into()), _ => ${dflt} })`;
      case "integer":
        return `(match ${at} { Some(Value::Int(__n)) => Value::Int(__n), Some(Value::Float(__f)) => Value::Int(__f as i64), Some(Value::Str(__s)) if !__s.is_empty() => match __s.parse::<i64>() { Ok(__n) => Value::Int(__n), Err(_) => match __s.parse::<f64>() { Ok(__f) if __f.is_finite() => Value::Int(__f as i64), _ => ${dflt} } }, _ => ${dflt} })`;
      case "float":
        return `(match ${at} { Some(Value::Float(__f)) => Value::Float(__f), Some(Value::Int(__n)) => Value::Float(__n as f64), Some(Value::Str(__s)) if !__s.is_empty() => match __s.parse::<f64>() { Ok(__f) => Value::Float(__f), Err(_) => ${dflt} }, _ => ${dflt} })`;
      case "dict":
        return `(match ${at} { Some(__v) if matches!(__v, Value::Dict(_)) => __v, _ => ${dflt} })`;
      case "list":
        return `(match ${at} { Some(__v) if matches!(__v, Value::Arr(_)) => __v, _ => ${dflt} })`;
      case "bool":
        return `(match ${at} { Some(__v) if matches!(__v, Value::Bool(_)) => __v, _ => ${dflt} })`;
    }
    return void 0;
  }
  printNativeMapAccess(receiverText, receiverNode, keyText) {
    if (!this.isProvenMapExpression(receiverNode)) {
      if (!this.rustIsDeclaredDictLocal(receiverNode))
        return void 0;
      if (_RustTranspiler.RUST_DICT_LOCAL_UNSAFE_KEYS.has(keyText))
        return void 0;
      if (keyText === "" || /^\d+$/.test(keyText))
        return void 0;
    }
    const key = this.escapeRustStringLiteral(keyText);
    return `${receiverText}.as_map().and_then(|__m| __m.get("${key}")).cloned().unwrap_or(Value::Null)`;
  }
  rustDeclarationOfIdentifier(node) {
    if (!_typescript2.default.isIdentifier(node))
      return void 0;
    try {
      const symbol = this.getChecker().getSymbolAtLocation(node);
      return _optionalChain([symbol, 'optionalAccess', _1749 => _1749.valueDeclaration]);
    } catch (e) {
      return void 0;
    }
  }
  /** Initializer shapes that construct or return a plain dict. */
  rustDictProducingInitializer(node, seen) {
    if (node === void 0 || node === null || seen.has(node))
      return false;
    seen.add(node);
    if (_typescript2.default.isObjectLiteralExpression(node))
      return true;
    if (_typescript2.default.isParenthesizedExpression(node) || _typescript2.default.isAsExpression(node) || _typescript2.default.isNonNullExpression(node) || _typescript2.default.isTypeAssertionExpression(node)) {
      return this.rustDictProducingInitializer(node.expression, seen);
    }
    if (_typescript2.default.isIdentifier(node)) {
      const declaration = this.rustDeclarationOfIdentifier(node);
      if (declaration === void 0 || !_typescript2.default.isVariableDeclaration(declaration))
        return false;
      return this.rustDictProducingInitializer(declaration.initializer, seen);
    }
    if (_typescript2.default.isElementAccessExpression(node)) {
      const containerType = this.getCheckedTypeOf(node.expression);
      if (containerType === void 0)
        return false;
      const elementType = this.getChecker().getIndexTypeOfType(containerType, _typescript2.default.IndexKind.String);
      return elementType !== void 0 && this.isProvenMapType(elementType);
    }
    if (!_typescript2.default.isCallExpression(node))
      return false;
    const callee = node.expression;
    if (!_typescript2.default.isPropertyAccessExpression(callee) || callee.expression.kind !== _typescript2.default.SyntaxKind.ThisKeyword) {
      return false;
    }
    const name = String(callee.name.escapedText);
    if (name === "safeDict" || name === "safeMarketStructure" || name === "market" || name === "currency" || name === "safeMarket" || name === "safeCurrency") {
      return true;
    }
    if (name === "client") {
      return true;
    }
    if (name === "extend" || name === "deepExtend") {
      return this.rustDictProducingInitializer(node.arguments[0], seen);
    }
    return false;
  }
  /** D2: the proof holds only while nothing re-assigns the local. */
  rustLocalIsReassigned(declaration, name) {
    let scope = declaration;
    while (scope !== void 0 && !_typescript2.default.isFunctionLike(scope) && !_typescript2.default.isSourceFile(scope)) {
      scope = scope.parent;
    }
    if (scope === void 0)
      return true;
    let reassigned = false;
    const visit = (node) => {
      if (reassigned)
        return;
      if (_typescript2.default.isBinaryExpression(node)) {
        const operator = node.operatorToken.kind;
        if (operator >= SyntaxKind4.FirstAssignment && operator <= SyntaxKind4.LastAssignment && _typescript2.default.isIdentifier(node.left) && node.left.text === name) {
          reassigned = true;
          return;
        }
      }
      _typescript2.default.forEachChild(node, visit);
    };
    _typescript2.default.forEachChild(scope, visit);
    return reassigned;
  }
  /** True when the receiver is a local declared as (or provably holding) a
   *  plain dict — `get_value(_k)` and this read agree on every key the
   *  runtime does not route elsewhere. */
  rustIsDeclaredDictLocal(node) {
    const declaration = this.rustDeclarationOfIdentifier(node);
    if (declaration === void 0)
      return false;
    const name = _optionalChain([declaration, 'access', _1750 => _1750.name, 'optionalAccess', _1751 => _1751.text]);
    if (typeof name !== "string")
      return false;
    if (_typescript2.default.isParameter(declaration)) {
      if (!this.rustParameterIsClientHandle(declaration)) {
        const fallback = declaration.initializer;
        if (fallback === void 0 || !this.rustDictProducingInitializer(fallback, /* @__PURE__ */ new Set()))
          return false;
      }
    } else if (_typescript2.default.isVariableDeclaration(declaration)) {
      const initializer = declaration.initializer;
      if (initializer === void 0)
        return false;
      if (!this.rustDictProducingInitializer(initializer, /* @__PURE__ */ new Set()))
        return false;
    } else {
      return false;
    }
    return !this.rustLocalIsReassigned(declaration, name);
  }
  /** A parameter declared as the ws `Client` class (or a union with it). The
   *  class is the default export of `ts/src/base/ws/Client.ts`, so its type
   *  symbol is named `default`; the declaration itself carries the name. */
  rustParameterIsClientHandle(declaration) {
    const named = (type2) => {
      if (type2 === void 0)
        return false;
      const symbol = this.typeSymbolOf(type2);
      const declarations = _nullishCoalesce(_optionalChain([symbol, 'optionalAccess', _1752 => _1752.declarations]), () => ( []));
      return declarations.some((d) => {
        if (!_typescript2.default.isClassDeclaration(d) || d.name === void 0 || d.name.text !== "Client")
          return false;
        const file = String(d.getSourceFile().fileName).replace(/\\/g, "/");
        return file.endsWith("/ws/Client.ts") || file.endsWith("/ws/Client.d.ts");
      });
    };
    const type = this.getCheckedTypeOf(declaration.name);
    if (named(type))
      return true;
    return (_nullishCoalesce(_optionalChain([type, 'optionalAccess', _1753 => _1753.types]), () => ( []))).some((member) => named(member));
  }
  /** Constant string argument of `parseInt`/`parseFloat` folded the way rust's
   *  `str::parse` would; undefined when the fold is not obviously exact. */
  foldParsedStringLiteral(name, text) {
    const t = text.trim();
    if (name === "parseInt") {
      if (!/^[+-]?[0-9]+$/.test(t))
        return void 0;
      const value2 = BigInt(t.replace(/^\+/, "") || "0");
      if (value2 < -9223372036854775808n || value2 > 9223372036854775807n)
        return void 0;
      return `Value::Int(${value2.toString()})`;
    }
    if (!/^[+-]?[0-9]+(?:\.[0-9]+)?$/.test(t))
      return void 0;
    const value = Number(t);
    if (!Number.isFinite(value) || Object.is(value, -0))
      return void 0;
    if (!/^-?[0-9]+(?:\.[0-9]+)?$/.test(String(value)))
      return void 0;
    return `Value::Float(${String(value)})`;
  }
  /** `parseInt(x)` / `parseFloat(x)` with a single checker-proven string argument
   *  become the runtime helper's own match with native `str::parse`; every other
   *  argument shape keeps the helper call the ccxt post-pass rewrites. */
  printNativeParseCall(node) {
    const callee = node.expression;
    if (!_typescript2.default.isIdentifier(callee))
      return void 0;
    const name = String(callee.escapedText);
    const helper = _RustTranspiler.RUST_PARSE_HELPERS[name];
    if (helper === void 0)
      return void 0;
    if (node.arguments.length !== 1)
      return void 0;
    const arg = node.arguments[0];
    const type = this.getCheckedTypeOf(arg);
    if (type === void 0 || !this.isStringType(type.flags))
      return void 0;
    if (_typescript2.default.isStringLiteralLike(arg)) {
      const folded = this.foldParsedStringLiteral(name, arg.text);
      if (folded !== void 0)
        return folded;
    }
    const argText = this.printNode(arg, 0).trim();
    if (!argText || argText.startsWith("&"))
      return void 0;
    if (helper === "i64") {
      return `(match &${argText} { Value::Str(__parse_s) => __parse_s.trim().parse::<i64>().map(Value::Int).unwrap_or(Value::Null), Value::Int(__parse_n) => Value::Int(*__parse_n), Value::Float(__parse_f) => Value::Int(*__parse_f as i64), _ => Value::Null })`;
    }
    return `(match &${argText} { Value::Str(__parse_s) => __parse_s.trim().parse::<f64>().map(Value::Float).unwrap_or(Value::Null), Value::Float(__parse_f) => Value::Float(*__parse_f), Value::Int(__parse_n) => Value::Float(*__parse_n as f64), _ => Value::Null })`;
  }
  isNodeInsideNode(node, container) {
    return node.pos >= container.pos && node.end <= container.end;
  }
  /** Root place of an access chain (`x` for `x['a']['b']`, `this.balance` for
   *  `this.balance['usdt']`), or undefined for a temporary. */
  rootPlaceText(node) {
    let current = node;
    while (current) {
      if (_typescript2.default.isPropertyAccessExpression(current) || _typescript2.default.isElementAccessExpression(current)) {
        if (current.expression.kind === _typescript2.default.SyntaxKind.ThisKeyword)
          return current.getText().trim();
        current = current.expression;
        continue;
      }
      if (_typescript2.default.isParenthesizedExpression(current)) {
        current = current.expression;
        continue;
      }
      if (_typescript2.default.isIdentifier(current) || current.kind === _typescript2.default.SyntaxKind.ThisKeyword) {
        return current.getText().trim();
      }
      return void 0;
    }
    return void 0;
  }
  /** The ccxt post-passes hoist `get_value(...)` reads out of `&mut` calls by
   *  matching their text; the native form is invisible to them, so it is only
   *  emitted where no such hoist is needed. */
  isNativeAccessPositionSafe(node) {
    const parent = node.parent;
    if (parent && _typescript2.default.isPropertyAccessExpression(parent) && parent.expression === node)
      return false;
    if (parent && _typescript2.default.isCallExpression(parent) && parent.expression === node)
      return false;
    const root = this.rootPlaceText(node);
    let current = node.parent;
    while (current) {
      if (_typescript2.default.isStatement(current) || _typescript2.default.isSourceFile(current) || _typescript2.default.isFunctionLike(current))
        break;
      if (_typescript2.default.isBinaryExpression(current) && this.isNodeInsideNode(node, current.right)) {
        const op = current.operatorToken.kind;
        const isAssign = op === _typescript2.default.SyntaxKind.EqualsToken || op >= _typescript2.default.SyntaxKind.PlusEqualsToken && op <= _typescript2.default.SyntaxKind.CaretEqualsToken;
        if (isAssign && root !== void 0 && this.rootPlaceText(current.left) === root)
          return false;
      }
      if (_typescript2.default.isCallExpression(current) && _typescript2.default.isPropertyAccessExpression(current.expression) && this.isNodeInsideNode(node, current) && current.arguments.some((a) => this.isNodeInsideNode(node, a))) {
        const callee = current.expression;
        if (root !== void 0 && this.rootPlaceText(callee.expression) === root)
          return false;
        if (callee.expression.kind === _typescript2.default.SyntaxKind.ThisKeyword && _RustTranspiler.MUT_SELF_METHODS.has(this.toSnakeCaseName(String(callee.name.escapedText))) && (root === void 0 || root === "this" || root.startsWith("this.")))
          return false;
      }
      current = current.parent;
    }
    return true;
  }
  /** Receiver shapes whose printed text is a single `Value` place (`x`, `this.x`). */
  isShallowValueReceiver(node) {
    if (_typescript2.default.isIdentifier(node))
      return true;
    return _typescript2.default.isPropertyAccessExpression(node) && node.expression.kind === _typescript2.default.SyntaxKind.ThisKeyword;
  }
  /** True when this read is the receiver of an element-access chain that is
   *  written (`x['a'] = v`, `x['a']['b'] = v`, `delete x['a']['b']`), or a
   *  property write itself (`x.k = v`, `delete x.k`). The ccxt write passes
   *  match the `get_value(&…)` / `x.k` text to reach the real container, so a
   *  native read would write into a discarded clone. */
  isNativeWriteTargetBase(node) {
    const parent = node.parent;
    if (parent === void 0)
      return false;
    if (_typescript2.default.isBinaryExpression(parent) && parent.left === node && rustIsAssignmentOperator(parent.operatorToken.kind))
      return true;
    if (_typescript2.default.isDeleteExpression(parent))
      return true;
    if (!_typescript2.default.isElementAccessExpression(parent) || parent.expression !== node)
      return false;
    let current = parent;
    while (current.parent !== void 0 && _typescript2.default.isElementAccessExpression(current.parent) && current.parent.expression === current) {
      current = current.parent;
    }
    const top = current.parent;
    if (top === void 0)
      return false;
    if (_typescript2.default.isDeleteExpression(top))
      return true;
    return _typescript2.default.isBinaryExpression(top) && top.left === current && rustIsAssignmentOperator(top.operatorToken.kind);
  }
  transformPropertyAcessExpressionIfNeeded(node) {
    const rightSide = node.name.escapedText;
    const leftExpr = this.printNode(node.expression, 0);
    if (rightSide === "length") {
      const shadow = this.rustParamShadowOf(node.expression);
      if (shadow !== void 0) {
        const native = this.printShadowLength(shadow);
        if (native !== void 0)
          return native;
      }
      return this.printArrayLength(node, 0, leftExpr);
    }
    return void 0;
  }
  // `crate::value::get_value_k` is `get_value` for a `&str` key — the same
  // dict lookup minus the per-read `Value::Str` allocation. Three literal-key
  // families reach branches only `get_value` has (numeric cache/side indices,
  // the cache `hashmap` bucket, live client `subscriptions`/`futures`) and a
  // `this`/class receiver is not a `Value`, so those keep `get_value`.
  staticKeyLookup(node, container) {
    if (!_typescript2.default.isStringLiteral(node)) {
      return void 0;
    }
    const source = node.getSourceFile ? node.getSourceFile().fileName : "";
    if (typeof source === "string" && /\/test\//.test(source)) {
      return void 0;
    }
    if (container.kind === SyntaxKind4.ThisKeyword) {
      return void 0;
    }
    const text = node.text;
    if (text === "" || text in this.StringLiteralReplacements) {
      return void 0;
    }
    if (/^\d+$/.test(text) || text === "hashmap" || text === "subscriptions" || text === "futures") {
      return void 0;
    }
    const checker = this.checkerOrUndefined();
    if (checker === void 0) {
      return void 0;
    }
    const type = checker.getTypeAtLocation(container);
    if (type !== void 0 && type.objectFlags & _typescript2.default.ObjectFlags.Class) {
      return void 0;
    }
    return this.quotedStringLiteral(text);
  }
  printElementAccessExpression(node, identation) {
    const special = this.printElementAccessExpressionExceptionIfAny(node);
    if (special)
      return special;
    const parent = node.parent;
    const isAssignmentTarget = parent !== void 0 && _typescript2.default.isBinaryExpression(parent) && parent.left === node && parent.operatorToken.kind >= SyntaxKind4.FirstAssignment && parent.operatorToken.kind <= SyntaxKind4.LastAssignment;
    const isCallOrPropertyTarget = parent !== void 0 && (_typescript2.default.isPropertyAccessExpression(parent) && parent.expression === node || _typescript2.default.isCallExpression(parent) && parent.expression === node);
    const keys = [];
    const receivers = [];
    const containers = [];
    let baseExpr = null;
    let current = node;
    while (_typescript2.default.isElementAccessExpression(current)) {
      keys.unshift(current.argumentExpression);
      receivers.unshift(current.expression);
      containers.unshift(current.expression);
      const expr = current.expression;
      if (!_typescript2.default.isElementAccessExpression(expr)) {
        baseExpr = expr;
        break;
      }
      current = expr;
    }
    const nativeAllowed = this.isNativeAccessPositionSafe(node) && !this.isNativeWriteTargetBase(node);
    let acc = this.printNode(baseExpr, 0);
    keys.forEach((key, index) => {
      const native = nativeAllowed ? this.printNativeContainerAccess(acc, receivers[index], key) : void 0;
      if (native !== void 0) {
        acc = native;
        return;
      }
      const staticKey = isAssignmentTarget || isCallOrPropertyTarget ? void 0 : this.staticKeyLookup(key, containers[index]);
      if (staticKey !== void 0) {
        acc = `crate::value::get_value_k(&${acc}, ${staticKey})`;
        return;
      }
      const kRef = `&${this.printNode(key, 0)}`;
      acc = `get_value(&${acc}, ${kRef})`;
    });
    return acc;
  }
  printForStatement(node, identation) {
    const initNode = node.initializer;
    const condNode = node.condition;
    const incrNode = node.incrementor;
    const idn = this.getIden(identation);
    const idn1 = this.getIden(identation + 1);
    const initStr = initNode ? this.printNode(initNode, identation + 1) + ";\n" : "";
    const condStr = condNode ? this.printComparisonInBooleanContext(condNode, 0).trim() : "true";
    const incrStr = incrNode ? this.printNode(incrNode, 0) : "";
    const statements = node.statement.statements.map((s) => this.printNode(s, identation + 1)).join("\n");
    const body = `{
${statements}
${idn}}`;
    if (incrStr !== "") {
      const flag = `__for_first_${this.forLoopCounter++}`;
      const cond = `{ if !${flag} { ${incrStr}; } ${flag} = false; ${condStr} }`;
      return `${idn}{
${idn1}${initStr}${idn1}let mut ${flag}: bool = true;
${idn1}while ${cond} ${body}
${idn}}`;
    }
    return `${idn}{
${idn1}${initStr}${idn1}while ${condStr} ${body}
${idn}}`;
  }
  printCondition(node, identation) {
    if (node.kind === SyntaxKind4.BinaryExpression) {
      const opKind = node.operatorToken.kind;
      if (_RustTranspiler.COMPARISON_OPS.has(opKind)) {
        return this.printComparisonInBooleanContext(node, identation);
      }
      if (opKind === SyntaxKind4.AmpersandAmpersandToken || opKind === SyntaxKind4.BarBarToken) {
        if (this.hasNativeComparisonOperand(node.left) || this.hasNativeComparisonOperand(node.right)) {
          return `${this.getIden(identation)}${this.printLogicalInBooleanContext(node)}`;
        }
        return `${this.getIden(identation)}${this.printNode(node, 0)}`;
      }
    }
    if (node.kind === SyntaxKind4.PrefixUnaryExpression && node.operator === SyntaxKind4.ExclamationToken) {
      return this.printPrefixUnaryExpression(node, identation);
    }
    if (this.rustCallPrintsBool(node)) {
      return `${this.getIden(identation)}${this.printNode(node, 0)}`;
    }
    const nativeTruthiness = this.printNativeTruthiness(node);
    if (nativeTruthiness !== void 0) {
      return `${this.getIden(identation)}${nativeTruthiness}`;
    }
    const nativeCondition = this.printNativeParenthesizedCondition(node);
    if (nativeCondition !== void 0 && this.rustConditionBoolSlot(node)) {
      return `${this.getIden(identation)}${nativeCondition}`;
    }
    const expression = this.printNode(node, 0);
    const peeled = this.peelValueBoolBox(this.stripOuterParens(expression));
    if (peeled !== void 0 && this.rustConditionBoolSlot(node) && !this.printedBoolHelperCall(this.stripOuterParens(peeled))) {
      return `${this.getIden(identation)}(${peeled})`;
    }
    return `${this.getIden(identation)}is_true(&${this.printTruthyArgument(expression)})`;
  }
  // B-26: `is_true(&(…)` on an operand the printer already emits as a native Rust `bool` (payload
  // compares, `matches!` predicates, `&&`/`||` of those) is the identity (`IsTruthy for bool`);
  // native text has no helper token, so it is emitted only in a bool slot (`isBareBoolEmissionSafe`).
  /** Bool-slot text of a parenthesised native comparison/predicate, else undefined. */
  printNativeParenthesizedCondition(node) {
    const inner = this.unwrapParens(node);
    if (inner === void 0 || inner === node || inner.kind !== SyntaxKind4.BinaryExpression) {
      return void 0;
    }
    const op = inner.operatorToken.kind;
    if (op === SyntaxKind4.AmpersandAmpersandToken || op === SyntaxKind4.BarBarToken) {
      if (!this.hasNativeComparisonOperand(inner.left) && !this.hasNativeComparisonOperand(inner.right)) {
        return void 0;
      }
      return `(${this.printLogicalInBooleanContext(inner)})`;
    }
    if (!_RustTranspiler.COMPARISON_OPS.has(op)) {
      return void 0;
    }
    if (inner.left.kind === SyntaxKind4.TypeOfExpression) {
      const native2 = this.nativeValuePredicateText(
        inner.right.text,
        inner.left.expression,
        this.printNode(inner.left.expression, 0)
      );
      if (native2 === void 0) {
        return void 0;
      }
      const isDiff = op === SyntaxKind4.ExclamationEqualsEqualsToken || op === SyntaxKind4.ExclamationEqualsToken;
      return `(${isDiff ? "!" : ""}${native2})`;
    }
    if (op === SyntaxKind4.EqualsEqualsToken || op === SyntaxKind4.EqualsEqualsEqualsToken || op === SyntaxKind4.ExclamationEqualsToken || op === SyntaxKind4.ExclamationEqualsEqualsToken) {
      const native2 = this.nativeEqualityText(inner);
      return native2 === void 0 ? void 0 : `(${native2})`;
    }
    const native = this.printNativeOrderedComparison(inner, op, inner.left, inner.right);
    return native === void 0 ? void 0 : `(${native})`;
  }
  // The argument of an `is_true(&…)` sink with the printer's `Value::Bool(…)` box peeled when it
  // spans the whole argument: `IsTruthy for Value` unboxes `Value::Bool(b)` to `b`, so the bool
  // prints bare. `is_true` is the one sink taking a native `bool`; the marker stays for post-passes.
  printTruthyArgument(expression) {
    const inner = this.peelValueBoolBox(this.stripOuterParens(expression));
    return inner === void 0 ? expression : `(${inner})`;
  }
  // `assert(<cond>, <msgs>)` — the test harness' `assert<T: Truthy>` takes a
  // native `bool` (`impl Truthy for bool`), so a whole-argument
  // `Value::Bool(…)` box prints bare there too. The message list is untouched.
  printAssertCall(node, identation, parsedArgs) {
    const [first, rest] = this.splitFirstArgument(parsedArgs);
    const inner = this.peelValueBoolBox(this.stripOuterParens(first));
    return inner === void 0 ? `assert(${parsedArgs})` : `assert((${inner})${rest})`;
  }
  // Splits an already-printed argument list after its first top-level `,`.
  splitFirstArgument(parsedArgs) {
    let depth = 0;
    for (let i = 0; i < parsedArgs.length; i++) {
      const char = parsedArgs[i];
      if (char === '"') {
        i++;
        while (i < parsedArgs.length && parsedArgs[i] !== '"') {
          if (parsedArgs[i] === "\\")
            i++;
          i++;
        }
        continue;
      }
      if (char === "(" || char === "[" || char === "{")
        depth++;
      else if (char === ")" || char === "]" || char === "}")
        depth--;
      else if (char === "," && depth === 0) {
        return [parsedArgs.slice(0, i), parsedArgs.slice(i)];
      }
    }
    return [parsedArgs, ""];
  }
  // Bool-position text for a comparison: the native payload compare (already
  // bool) when the checker proves it, the is_equal() helper otherwise.
  printComparisonInBooleanContext(node, identation) {
    const native = this.nativeEqualityText(node);
    if (native) {
      return `${this.getIden(identation)}(${native})`;
    }
    return `${this.getIden(identation)}${this.printNode(node, 0)}`;
  }
  // Native equality text of `node` (parens unwrapped), if the checker proves it.
  nativeEqualityText(node) {
    const inner = this.unwrapParens(node);
    if (inner === void 0 || inner.kind !== SyntaxKind4.BinaryExpression) {
      return void 0;
    }
    const op = inner.operatorToken.kind;
    if (op !== SyntaxKind4.EqualsEqualsToken && op !== SyntaxKind4.EqualsEqualsEqualsToken && op !== SyntaxKind4.ExclamationEqualsToken && op !== SyntaxKind4.ExclamationEqualsEqualsToken) {
      return void 0;
    }
    return this.printNativeEqualityComparison(inner.left, inner.right, op);
  }
  unwrapParens(node) {
    let inner = node;
    while (inner !== void 0 && inner.kind === SyntaxKind4.ParenthesizedExpression) {
      inner = inner.expression;
    }
    return inner;
  }
  // Does `node` carry a native payload compare in a position where the old
  // text started with a bool helper? The post-pass types locals by that token.
  hasNativeComparisonOperand(node) {
    const inner = this.unwrapParens(node);
    if (inner === void 0) {
      return false;
    }
    if (inner.kind === SyntaxKind4.PrefixUnaryExpression && inner.operator === SyntaxKind4.ExclamationToken) {
      return this.hasNativeComparisonOperand(inner.operand);
    }
    if (this.nativeEqualityText(inner) !== void 0) {
      return true;
    }
    if (inner.kind === SyntaxKind4.BinaryExpression) {
      const op = inner.operatorToken.kind;
      if (op === SyntaxKind4.AmpersandAmpersandToken || op === SyntaxKind4.BarBarToken) {
        return this.hasNativeComparisonOperand(inner.left) || this.hasNativeComparisonOperand(inner.right);
      }
    }
    return false;
  }
  // Bare `&&`/`||` text of a logical expression (its operands are bools).
  printLogicalInBooleanContext(node) {
    const token = node.operatorToken.kind === SyntaxKind4.AmpersandAmpersandToken ? "&&" : "||";
    const left = this.printCondition(node.left, 0).trim();
    const right = this.printCondition(node.right, 0).trim();
    return `${left} ${token} ${right}`;
  }
  printWhileStatement(node, identation) {
    const expr = this.printCondition(node.expression, 0);
    const body = this.printBlock(node.statement, identation);
    return `${this.getIden(identation)}while ${expr}${body}`;
  }
  printIfStatement(node, identation) {
    const expression = this.printCondition(node.expression, 0);
    const elseExists = node.elseStatement !== void 0;
    const ifBody = this.printBlock(node.thenStatement, identation, elseExists);
    let ifComplete = `${expression}${ifBody}`;
    const isElseIf = node.parent.kind === SyntaxKind4.IfStatement;
    if (isElseIf) {
      ifComplete = `else if ${ifComplete}`;
    } else {
      ifComplete = `${this.getIden(identation)}if ${ifComplete}`;
    }
    const elseStatement = node.elseStatement;
    if (_optionalChain([elseStatement, 'optionalAccess', _1754 => _1754.kind]) === SyntaxKind4.Block) {
      ifComplete += ` else${this.printBlock(elseStatement, identation)}`;
    } else if (_optionalChain([elseStatement, 'optionalAccess', _1755 => _1755.kind]) === SyntaxKind4.IfStatement) {
      ifComplete += " " + this.printIfStatement(elseStatement, identation);
    }
    return this.printNodeCommentsIfAny(node, identation, ifComplete);
  }
  printPostFixUnaryExpression(node, identation) {
    const { operand, operator } = node;
    const operandText = this.printNode(operand, 0);
    if (operator === SyntaxKind4.PlusPlusToken) {
      const native = this.printNativeIncrement(SyntaxKind4.PlusToken, operand, operandText);
      if (native !== void 0) {
        return `${this.getIden(identation)}${operandText} = ${native}`;
      }
      return `${this.getIden(identation)}${operandText} = add(&${operandText}, &Value::Int(1))`;
    }
    if (operator === SyntaxKind4.MinusMinusToken) {
      const native = this.printNativeIncrement(SyntaxKind4.MinusToken, operand, operandText);
      if (native !== void 0) {
        return `${this.getIden(identation)}${operandText} = ${native}`;
      }
      return `${this.getIden(identation)}${operandText} = subtract(&${operandText}, &Value::Int(1))`;
    }
    return super.printPostFixUnaryExpression(node, identation);
  }
  // `x++` / `x--` on a checker-typed number: native `+`/`-` with `Value::Int(1)`.
  printNativeIncrement(op, operand, operandText) {
    try {
      if (!this.isNumberLikeType(this.getChecker().getTypeAtLocation(operand))) {
        return void 0;
      }
    } catch (e) {
      return void 0;
    }
    return this.printNativeNumeric(op, operandText, "Value::Int(1)");
  }
  printPrefixUnaryExpression(node, identation) {
    const { operand, operator } = node;
    if (operator === SyntaxKind4.ExclamationToken) {
      return this.getIden(identation) + "!" + this.printCondition(node.operand, 0);
    }
    if (operator === SyntaxKind4.MinusToken) {
      const operandText = this.printNode(operand, 0);
      const folded = this.foldNegateLiteral(operandText);
      if (folded !== void 0) {
        return this.getIden(identation) + folded;
      }
      return this.getIden(identation) + `negate(&${operandText})`;
    }
    return this.getIden(identation) + this.PrefixFixOperators[operator] + this.printNode(operand, 0);
  }
  printObjectLiteralExpression(node, identation) {
    if (node.properties.length === 0) {
      return "Value::Map({\n" + this.getIden(identation + 1) + "let mut m = std::collections::HashMap::new();\n" + this.getIden(identation + 1) + "m\n" + this.getIden(identation) + "})";
    }
    const escapeKey = (s) => {
      return String(s).replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/\t/g, "\\t");
    };
    const lines = node.properties.map((p) => {
      if (_typescript2.default.isShorthandPropertyAssignment(p)) {
        const name2 = p.name.escapedText;
        return `${this.getIden(identation + 2)}m.insert("${escapeKey(name2)}".to_string(), ${name2}.clone());`;
      }
      const { name, initializer } = p;
      const keyText = _typescript2.default.isStringLiteral(name) ? name.text : name.escapedText;
      const valText = this.printNode(initializer, 0);
      return `${this.getIden(identation + 2)}m.insert("${escapeKey(keyText)}".to_string(), ${valText});`;
    }).join("\n");
    return `Value::Map({
${this.getIden(identation + 1)}let mut m = std::collections::HashMap::new();
${lines}
${this.getIden(identation + 1)}m
${this.getIden(identation)}})`;
  }
  printArrayLiteralExpression(node, identation) {
    const elements = node.elements.map((e) => this.printNode(e, 0)).join(", ");
    return `Value::from(vec![${elements}])`;
  }
  printDeleteExpression(node, identation) {
    const object = this.printNode(node.expression.expression, 0);
    const key = this.printNode(node.expression.argumentExpression, 0);
    const keyRef = key.startsWith("Value::") ? `&${key}` : `&${key}`;
    return `remove(&mut ${object}, ${keyRef})`;
  }
  printInstanceOfExpression(node, identation) {
    const left = this.printNode(node.left, 0);
    const right = this.printNode(node.right, 0);
    return `${this.getIden(identation)}is_instance(&${left}, &${right})`;
  }
  printConditionalExpression(node, identation) {
    const condition = this.printCondition(node.condition, 0);
    const whenTrue = this.printTernaryArm(node.whenTrue);
    const whenFalse = this.printTernaryArm(node.whenFalse);
    return `(if ${condition} { ${whenTrue} } else { ${whenFalse} })`;
  }
  static isBoolValueExpression(text) {
    let value = text.trim();
    for (; ; ) {
      if (!(value.startsWith("(") && value.endsWith(")"))) {
        break;
      }
      let depth = 0;
      let wrapsWhole = true;
      for (let i = 0; i < value.length; i++) {
        if (value[i] === "(") {
          depth++;
        } else if (value[i] === ")") {
          depth--;
          if (depth === 0 && i < value.length - 1) {
            wrapsWhole = false;
            break;
          }
        }
      }
      if (!wrapsWhole) {
        break;
      }
      value = value.slice(1, -1).trim();
    }
    if (value.startsWith("!")) {
      value = value.slice(1).trim();
    }
    return _RustTranspiler.BOOL_VALUE_PREFIXES.some((fn) => value.startsWith(fn + "("));
  }
  // An if-expression arm keeps the type `ternary()`'s `Value` parameters gave
  // it: box bool expressions, and clone bare identifiers so the arm does not
  // move a local the caller still uses.
  printTernaryArm(node, identation = 0) {
    const text = this.printNode(node, identation);
    const trimmed = text.trim();
    if (_RustTranspiler.isBoolValueExpression(trimmed)) {
      return `Value::Bool(${trimmed})`;
    }
    if (/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(trimmed) && trimmed !== "self" && trimmed !== "true" && trimmed !== "false") {
      return `${trimmed}.clone()`;
    }
    return text;
  }
  // Built-in method call overrides
  printArrayIsArrayCall(node, identation, parsedArg = void 0) {
    const native = this.nativeValuePredicateText("array", _optionalChain([node, 'optionalAccess', _1756 => _1756.arguments, 'optionalAccess', _1757 => _1757[0]]), parsedArg);
    if (native !== void 0) {
      return `Value::Bool(${native})`;
    }
    return `Value::Bool(is_array(&${parsedArg}))`;
  }
  printObjectKeysCall(node, identation, parsedArg = void 0) {
    return `object_keys(&${parsedArg})`;
  }
  printObjectValuesCall(node, identation, parsedArg = void 0) {
    return `object_values(&${parsedArg})`;
  }
  printJsonParseCall(node, identation, parsedArg = void 0) {
    return `json_parse(&${parsedArg})`;
  }
  printJsonStringifyCall(node, identation, parsedArg = void 0) {
    return `json_stringify(&${parsedArg})`;
  }
  printMathFloorCall(node, identation, parsedArg = void 0) {
    return `math_floor(&${parsedArg})`;
  }
  printPromiseAllCall(node, identation, parsedArg = void 0) {
    return `promise_all(&${parsedArg})`;
  }
  // Rust uses postfix `.await`; the base transpiler defaults to prefix.
  printAwaitExpression(node, identation) {
    const expr = this.printNode(node.expression, identation);
    return `${expr}.await`;
  }
  printMathRoundCall(node, identation, parsedArg = void 0) {
    return `math_round(&${parsedArg})`;
  }
  printMathCeilCall(node, identation, parsedArg = void 0) {
    return `math_ceil(&${parsedArg})`;
  }
  printNumberIsIntegerCall(node, identation, parsedArg = void 0) {
    return `is_integer(&${parsedArg})`;
  }
  printArrayPushCall(node, identation, name = void 0, parsedArg = void 0) {
    return `append_to_array(&mut ${name}, ${parsedArg})`;
  }
  printIncludesCall(node, identation, name = void 0, parsedArg = void 0) {
    const pRef = _optionalChain([parsedArg, 'optionalAccess', _1758 => _1758.startsWith, 'call', _1759 => _1759("Value::")]) ? `&${parsedArg}` : `&${parsedArg}`;
    return `Value::Bool(contains(&${name}, ${pRef}))`;
  }
  printIndexOfCall(node, identation, name = void 0, parsedArg = void 0) {
    const native = this.printNativeStringIndexOf(node, name);
    if (native !== void 0) {
      return native;
    }
    return `get_index_of(&${name}, &${parsedArg})`;
  }
  printStartsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    return `Value::Bool(starts_with(&${name}, &${parsedArg}))`;
  }
  printEndsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    return `Value::Bool(ends_with(&${name}, &${parsedArg}))`;
  }
  printTrimCall(node, identation, name = void 0) {
    return `trim(&${name})`;
  }
  printJoinCall(node, identation, name = void 0, parsedArg = void 0) {
    return `join(&${name}, &${parsedArg})`;
  }
  printSplitCall(node, identation, name = void 0, parsedArg = void 0) {
    return `split(&${name}, &${parsedArg})`;
  }
  printConcatCall(node, identation, name = void 0, parsedArg = void 0) {
    return `concat(${name}.clone(), ${parsedArg}.clone())`;
  }
  printToFixedCall(node, identation, name = void 0, parsedArg = void 0) {
    return `to_fixed(&${name}, &${parsedArg})`;
  }
  printToStringCall(node, identation, name = void 0) {
    return `to_string_val(&${name})`;
  }
  printToUpperCaseCall(node, identation, name = void 0) {
    return `to_upper(&${name})`;
  }
  printToLowerCaseCall(node, identation, name = void 0) {
    return `to_lower(&${name})`;
  }
  printShiftCall(node, identation, name = void 0) {
    return `shift(${name}.clone())`;
  }
  printReverseCall(node, identation, name = void 0) {
    return `${name} = reverse(${name}.clone())`;
  }
  printPopCall(node, identation, name = void 0) {
    return `pop(${name}.clone())`;
  }
  printSliceCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    const native = this.printNativeStringSlice(node, name);
    if (native !== void 0) {
      return native;
    }
    const arg2 = _nullishCoalesce(parsedArg2, () => ( "Value::Null"));
    return `slice(&${name}, &${parsedArg}, &${arg2})`;
  }
  printReplaceCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    return `replace_str(&${name}, &${parsedArg}, &${parsedArg2})`;
  }
  printReplaceAllCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    return `replace_all_str(&${name}, &${parsedArg}, &${parsedArg2})`;
  }
  printThrowStatement(node, identation) {
    const expression = this.printNode(node.expression, 0);
    return `${this.getIden(identation)}panic!("{}", ${expression});`;
  }
  printTryStatement(node, identation) {
    const tryBody = node.tryBlock.statements.map((s) => this.printNode(s, identation + 1)).join("\n");
    const catchBody = node.catchClause.block.statements.map((s) => this.printNode(s, identation + 1)).join("\n");
    const rawName = _optionalChain([node, 'access', _1760 => _1760.catchClause, 'optionalAccess', _1761 => _1761.variableDeclaration, 'optionalAccess', _1762 => _1762.name, 'optionalAccess', _1763 => _1763.escapedText]);
    const errorName = rawName ? `_${rawName}` : "_e";
    const iden = this.getIden(identation);
    return `${iden}let _try_result = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
${tryBody}
${iden}}));
${iden}if let Err(${errorName}) = _try_result {
${catchBody}
${iden}}`;
  }
  printReturnStatement(node, identation) {
    const exp = node.expression;
    if (!exp) {
      return `${this.getIden(identation)}return;`;
    }
    const fn = _typescript2.default.findAncestor(node.parent, _typescript2.default.isFunctionLike);
    if (this.rustNativeStrReturnKind(fn) === "str" && this.rustStrReturnValueConverts(exp)) {
      const inner = this.unwrapParensNode(exp);
      if (this.literalKindOfNode(inner) === "null") {
        return `${this.getIden(identation)}return None;`;
      }
      const text = this.printNode(exp, 0).trim();
      const suffix = this.rustStrNativeExpression(exp) ? "" : ".as_str().map(str::to_owned)";
      return `${this.getIden(identation)}return ${text}${suffix};`;
    }
    const rightPart = this.printNode(exp, 0).trim();
    return `${this.getIden(identation)}return ${rightPart};`;
  }
  printBreakStatement(node, identation) {
    return `${this.getIden(identation)}break;`;
  }
  printContinueStatement(node, identation) {
    return `${this.getIden(identation)}continue;`;
  }
  printConstructorDeclaration(node, identation) {
    return "";
  }
  printSpreadElement(node, identation) {
    const expression = this.printNode(node.expression, 0);
    return `${this.getIden(identation)}${expression}`;
  }
};
// ── native equality emission ────────────────────────────────────────────
// When the checker proves both operands hold the same primitive payload
// (string / number / boolean, or one side is a matching literal) the
// unwrapped payloads are compared with `==`/`!=` instead of is_equal().
// Operators whose printed form is a bare Rust `bool` (not a Value).
_RustTranspiler.BOOL_PRODUCING_OPERATORS = /* @__PURE__ */ new Set([
  SyntaxKind4.EqualsEqualsToken,
  SyntaxKind4.EqualsEqualsEqualsToken,
  SyntaxKind4.ExclamationEqualsToken,
  SyntaxKind4.ExclamationEqualsEqualsToken,
  SyntaxKind4.LessThanToken,
  SyntaxKind4.LessThanEqualsToken,
  SyntaxKind4.GreaterThanToken,
  SyntaxKind4.GreaterThanEqualsToken,
  SyntaxKind4.AmpersandAmpersandToken,
  SyntaxKind4.BarBarToken,
  SyntaxKind4.InstanceOfKeyword
]);
// Method names whose Rust helper returns a bare `bool` (not a Value).
_RustTranspiler.BOOL_PRODUCING_CALLS = /* @__PURE__ */ new Set([
  "isInteger",
  "isSafeInteger",
  "some",
  "every",
  "test"
]);
// Receiver locals whose element writes may go native (rust-13). `request`
// is rust-12's family; every other receiver keeps the helper.
_RustTranspiler.RUST_NATIVE_INSERT_RECEIVERS = /* @__PURE__ */ new Set([
  "result",
  "account",
  "params",
  "fee"
]);
// Scalar book fields the shared order-book store keeps instead of the Dict
// (value.rs `is_book_meta_key` plus the `cache` replacement write).
_RustTranspiler.RUST_BOOK_META_KEYS = /* @__PURE__ */ new Set([
  "timestamp",
  "datetime",
  "nonce",
  "symbol",
  "checksum",
  "cache"
]);
// `this.<field>` receivers whose Value may be a tagged live handle (order
// book / cache / ws client / subscriptions snapshot) — these keep the helper
// so its store write-through stays reachable.
_RustTranspiler.RUST_TAGGED_HANDLE_FIELDS = /* @__PURE__ */ new Set([
  "cache",
  "client",
  "subscriptions",
  "orderbook"
]);
// Fields the hand-written base keeps as a plain dict: `rust/ccxt-base/src/exchange.rs`
// initialises each to `Value::Map(HashMap::new())` and never tags it, so an element write
// is the whole helper even though the TS declaration is `any` (no checker shape to read).
_RustTranspiler.RUST_PLAIN_DICT_FIELDS = /* @__PURE__ */ new Set([
  "balance",
  "orderbooks",
  "trades",
  "tickers",
  "bidsasks",
  "ohlcvs",
  "clients",
  "transactions",
  "headers",
  "currencies",
  "has",
  "tokenBucket"
]);
// Free helpers whose call prints a bare `bool` — an argument position that
// expects a `Value` has to box them (ccxt's wrapBoolValueArgs set).
_RustTranspiler.RUST_BOOL_VALUE_HELPERS = [
  "is_equal",
  "is_true",
  "is_greater_than",
  "is_less_than",
  "is_greater_than_or_equal",
  "is_less_than_or_equal",
  "is_array",
  "is_object",
  "in_op",
  "is_number",
  "is_string"
];
// Payload accessor used to compare each primitive kind natively.
_RustTranspiler.PAYLOAD_ACCESSORS = {
  "string": "as_str",
  "number": "as_f64",
  "boolean": "as_bool"
};
// Native value predicates (`Array.isArray` / `typeof … === '…'`): each runtime predicate is a
// single `matches!` over the `Value` variants, so on a declared `Value` place the call is
// replaced by that same match — no helper call, operand still read exactly once.
// `matches!` pattern of the runtime predicate, keyed by the `typeof` word.
_RustTranspiler.RUST_TYPE_PREDICATE_PATTERNS = {
  "array": "Value::Arr(_)",
  "string": "Value::Str(_)",
  "number": "Value::Int(_) | Value::Float(_)",
  "boolean": "Value::Bool(_)",
  "object": "Value::Dict(_)"
};
_RustTranspiler.RUST_TAGGED_CONTAINER_FIELDS = /* @__PURE__ */ new Set(["hashmap", "subscriptions", "futures"]);
// Types whose runtime value the `add` helper stringifies exactly as `format!` does: a string,
// or `undefined`/`null` boxed as `Value::Null` (`stringify_simple(Value::Null)` and `Display`
// both give "null"). `any` is absent — the Precise-dict branch has no `Display` equivalent.
_RustTranspiler.RUST_CONCAT_SAFE_FLAGS = /* @__PURE__ */ new Set([
  _typescript2.default.TypeFlags.String,
  _typescript2.default.TypeFlags.StringLiteral,
  _typescript2.default.TypeFlags.Undefined,
  _typescript2.default.TypeFlags.Null
]);
// ── native-typed locals ───────────────────────────────────────────────────
//
// A local is declared `bool` (instead of `Value`) when its initializer is
// already a bool-valued Rust expression and every use is a condition sink
// (`is_true(&x)`) — the one sink that accepts a native bool today.
//
// `is_true` is generic over `IsTruthy` (impl for `bool`/`&bool` in
// runtime.rs); every other sink takes `&Value`, so any other use keeps the
// local boxed. Bools come in two shapes: helpers whose Rust return type is
// already `bool` (below), and the printer's own `Value::Bool(...)` box,
// which the declaration drops at the init site.
_RustTranspiler.RUST_BOOL_RESULT_HELPERS = /* @__PURE__ */ new Set([
  "is_true",
  "is_equal",
  "is_greater_than",
  "is_greater_than_or_equal",
  "is_less_than",
  "is_less_than_or_equal",
  "is_array",
  "is_object",
  "is_string",
  "is_number",
  "is_bool",
  "is_integer",
  "is_function",
  "is_instance",
  "starts_with",
  "ends_with",
  "in_op",
  "contains"
]);
// Hand-written Rust fns outside `runtime.rs` with a `-> bool` signature, keyed by the TS callee
// name (verified in `rust/tests/src/tests_support.rs`). A call is already a Rust bool, so the
// condition printer's `is_true(&…)` wrapper is the identity (`IsTruthy for bool`) and is dropped.
_RustTranspiler.RUST_BOOL_RESULT_CALLEES = /* @__PURE__ */ new Set([
  "tickerExceptionNeedsOhlcv"
]);
// Typed string locals: `let x: Value = self.safeString(..)` is declared `Option<String>` when
// the checker proves a string and every use is a native sink (`x.is_none()`, `x.as_deref() ==
// Some("lit")`). The helper's Value is unwrapped with `.as_str()`; any other sink keeps the box.
_RustTranspiler.RUST_STRING_LOCAL_HELPERS = /* @__PURE__ */ new Set([
  "safeString",
  "safeString2",
  "safeStringN",
  "safeStringLower",
  "safeStringLower2",
  "safeStringLowerN",
  "safeStringUpper",
  "safeStringUpper2",
  "safeStringUpperN"
]);
// `ts/src/base/**` — the shared `Exchange` / `PredictionExchange` classes
// (the transpiler synthesises variants like `.__ExchangeNoOverloads.ts`).
_RustTranspiler.RUST_BASE_TIER_FILE = /ts[\\/]src[\\/]base[\\/]/;
// Native string args to the runtime error constructors, audited against
// rust/ccxt-base/src/exchange_errors.rs: `msg` is `impl ToErrorMessage` (`&str`/`String`/`Value`
// yield the same string) and `create_error`'s class name is `&str` (bare literal only).
_RustTranspiler.RUST_ERROR_CONSTRUCTOR_ARGS = {
  exchange_error: ["msg"],
  authentication_error: ["msg"],
  permission_denied: ["msg"],
  account_not_enabled: ["msg"],
  account_suspended: ["msg"],
  arguments_required: ["msg"],
  bad_request: ["msg"],
  bad_symbol: ["msg"],
  operation_rejected: ["msg"],
  no_change: ["msg"],
  margin_mode_already_set: ["msg"],
  market_closed: ["msg"],
  manual_interaction_needed: ["msg"],
  restricted_location: ["msg"],
  insufficient_funds: ["msg"],
  invalid_address: ["msg"],
  address_pending: ["msg"],
  invalid_order: ["msg"],
  order_not_found: ["msg"],
  order_not_cached: ["msg"],
  order_immediately_fillable: ["msg"],
  order_not_fillable: ["msg"],
  duplicate_order_id: ["msg"],
  contract_unavailable: ["msg"],
  not_supported: ["msg"],
  invalid_proxy_settings: ["msg"],
  exchange_closed_by_user: ["msg"],
  operation_failed: ["msg"],
  network_error: ["msg"],
  d_do_s_protection: ["msg"],
  rate_limit_exceeded: ["msg"],
  exchange_not_available: ["msg"],
  on_maintenance: ["msg"],
  invalid_nonce: ["msg"],
  checksum_error: ["msg"],
  request_timeout: ["msg"],
  bad_response: ["msg"],
  null_response: ["msg"],
  cancel_pending: ["msg"],
  unsubscribe_error: ["msg"],
  create_error: ["str", "msg"]
};
// ── native container access (`get_value(...)` -> `.get(...)`) ─────────────
// When the TypeScript checker proves the receiver is a plain Map/List value
// and the key is a literal, the runtime `get_value` key-marker / `__live_id`
// paths cannot apply, so the access is emitted natively. Missing keys still
// fall back to `Value::Null`.
/** Methods whose Rust counterpart takes `&mut self`: a `self.<field>` read in
 *  their args must keep the `get_value(...)` shape the ccxt post-pass hoists. */
_RustTranspiler.MUT_SELF_METHODS = /* @__PURE__ */ new Set([
  "watch",
  "watch_multiple",
  "fetch_order_book_snapshot",
  "un_watch",
  "client",
  "spawn",
  "delay",
  "fetch_tickers",
  "extend",
  "fetch",
  "send_evm_transaction"
]);
/** Global parse helpers that go native (`str::parse`) on a proven string arg,
 *  keyed to the rust integer/float type their runtime helper parses into. */
_RustTranspiler.RUST_PARSE_HELPERS = {
  parseInt: "i64",
  parseFloat: "f64"
};
// ── pro-tier WS handler `message` shadow (D-27) ───────────────────────────
// `handle_x (client: Client, message: Dict)` is dispatched by NAME with
// `Value` args, so the printed signature keeps `Value`; the annotation still
/** The borrowed view bound by the shadow. */
_RustTranspiler.PRO_HANDLER_SHADOW_NAME = "__pro_message";
/** TS helper name -> emitted match kind. */
_RustTranspiler.PRO_HANDLER_SHADOW_SAFE_READS = {
  "safeValue": "value",
  "safeString": "string",
  "safeInteger": "integer",
  "safeNumber": "float",
  "safeFloat": "float",
  "safeDict": "dict",
  "safeList": "list",
  "safeBool": "bool"
};
// Declared-Dict locals: the checker types many dict-holding locals `any`, losing the proof
// above, but the declaration still proves a plain dict (object literal, `getArg(.., {})` bag,
// `this.safeDict`, `extend` onto those, or an element of a map-typed container) at every read.
/** Keys `get_value(_k)` serves from the book store, a cache bucket or a
 *  live `__live_id` snapshot instead of from the dict itself: those routes
 *  are invisible to a plain map read, so they keep the helper. */
_RustTranspiler.RUST_DICT_LOCAL_UNSAFE_KEYS = /* @__PURE__ */ new Set([
  "timestamp",
  "datetime",
  "nonce",
  "symbol",
  "checksum",
  "cache",
  "hashmap",
  "subscriptions",
  "futures"
]);
_RustTranspiler.COMPARISON_OPS = /* @__PURE__ */ new Set([
  SyntaxKind4.EqualsEqualsToken,
  SyntaxKind4.EqualsEqualsEqualsToken,
  SyntaxKind4.ExclamationEqualsToken,
  SyntaxKind4.ExclamationEqualsEqualsToken,
  SyntaxKind4.LessThanToken,
  SyntaxKind4.LessThanEqualsToken,
  SyntaxKind4.GreaterThanToken,
  SyntaxKind4.GreaterThanEqualsToken
]);
// Comparison helpers that can be replaced by a native numeric operator.
_RustTranspiler.NATIVE_COMPARISON_OPERATORS = {
  [SyntaxKind4.LessThanToken]: "<",
  [SyntaxKind4.LessThanEqualsToken]: "<=",
  [SyntaxKind4.GreaterThanToken]: ">",
  [SyntaxKind4.GreaterThanEqualsToken]: ">="
};
// Free runtime functions that print as `bool` (not `Value`) — mirror of the
// Rust pipeline's `ternary()` bool-boxing list.
_RustTranspiler.BOOL_VALUE_PREFIXES = [
  "is_equal",
  "is_true",
  "is_greater_than",
  "is_less_than",
  "is_greater_than_or_equal",
  "is_less_than_or_equal",
  "is_array",
  "is_object",
  "in_op",
  "is_number",
  "is_string"
];
var RustTranspiler = _RustTranspiler;

// src/cppTranspiler.ts
init_cjs_shims();

var parserConfig7 = {
  "ELSEIF_TOKEN": "else if",
  "OBJECT_OPENING": "std::unordered_map<std::string, std::any> {",
  "OBJECT_CLOSING": "}",
  "ARRAY_OPENING_TOKEN": "std::vector<std::any>{",
  "ARRAY_CLOSING_TOKEN": "}",
  "PROPERTY_ASSIGNMENT_TOKEN": ",",
  "VAR_TOKEN": "std::any",
  // object
  "METHOD_TOKEN": "",
  "PROPERTY_ASSIGNMENT_OPEN": "{",
  "PROPERTY_ASSIGNMENT_CLOSE": "}",
  "SUPER_TOKEN": "base",
  "SUPER_CALL_TOKEN": "base",
  "FALSY_WRAPPER_OPEN": "isTrue(",
  "FALSY_WRAPPER_CLOSE": ")",
  "COMPARISON_WRAPPER_OPEN": "isEqual(",
  "COMPARISON_WRAPPER_CLOSE": ")",
  "UKNOWN_PROP_WRAPPER_OPEN": "this.call(",
  "UNKOWN_PROP_WRAPPER_CLOSE": ")",
  "UKNOWN_PROP_ASYNC_WRAPPER_OPEN": "this.callAsync(",
  "UNKOWN_PROP_ASYNC_WRAPPER_CLOSE": ")",
  "DYNAMIC_CALL_OPEN": "callDynamically(",
  "EQUALS_EQUALS_WRAPPER_OPEN": "isEqual(",
  "EQUALS_EQUALS_WRAPPER_CLOSE": ")",
  "DIFFERENT_WRAPPER_OPEN": "!isEqual(",
  "DIFFERENT_WRAPPER_CLOSE": ")",
  "GREATER_THAN_WRAPPER_OPEN": "isGreaterThan(",
  "GREATER_THAN_WRAPPER_CLOSE": ")",
  "GREATER_THAN_EQUALS_WRAPPER_OPEN": "isGreaterThanOrEqual(",
  "GREATER_THAN_EQUALS_WRAPPER_CLOSE": ")",
  "LESS_THAN_WRAPPER_OPEN": "isLessThan(",
  "LESS_THAN_WRAPPER_CLOSE": ")",
  "LESS_THAN_EQUALS_WRAPPER_OPEN": "isLessThanOrEqual(",
  "LESS_THAN_EQUALS_WRAPPER_CLOSE": ")",
  "PLUS_WRAPPER_OPEN": "add(",
  "PLUS_WRAPPER_CLOSE": ")",
  "MINUS_WRAPPER_OPEN": "subtract(",
  "MINUS_WRAPPER_CLOSE": ")",
  "ARRAY_LENGTH_WRAPPER_OPEN": "getArrayLength(",
  "ARRAY_LENGTH_WRAPPER_CLOSE": ")",
  "DIVIDE_WRAPPER_OPEN": "divide(",
  "DIVIDE_WRAPPER_CLOSE": ")",
  "MULTIPLY_WRAPPER_OPEN": "multiply(",
  "MULTIPLY_WRAPPER_CLOSE": ")",
  "INDEXOF_WRAPPER_OPEN": "getIndexOf(",
  "INDEXOF_WRAPPER_CLOSE": ")",
  "MOD_WRAPPER_OPEN": "mod(",
  "MOD_WRAPPER_CLOSE": ")",
  "FUNCTION_TOKEN": "",
  "DEFAULT_PARAMETER_TYPE": "std::any",
  "INFER_VAR_TYPE": false,
  "INFER_ARG_TYPE": false,
  "UNDEFINED_TOKEN": "std::any{}",
  // qualified with :: so class methods with the same name never shadow the helper
  "ELEMENT_ACCESS_WRAPPER_OPEN": "::getValue(",
  "ELEMENT_ACCESS_WRAPPER_CLOSE": ")",
  "DEFAULT_RETURN_TYPE": "std::any",
  "THIS_TOKEN": "this",
  "NEW_TOKEN": "",
  "CATCH_DECLARATION": "const std::exception&"
};
var CppTranspiler = class extends BaseTranspiler {
  constructor(config = {}) {
    config["parser"] = Object.assign({}, parserConfig7, _nullishCoalesce(config["parser"], () => ( {})));
    super(config);
    this.requiresParameterType = true;
    this.requiresReturnType = true;
    this.asyncTranspiling = true;
    this.supportsFalsyOrTruthyValues = false;
    this.requiresCallExpressionCast = false;
    this.id = "c++";
    this.initConfig();
    this.applyUserOverrides(config);
  }
  initConfig() {
    this.LeftPropertyAccessReplacements = {};
    this.RightPropertyAccessReplacements = {};
    this.FullPropertyAccessReplacements = {
      "JSON.parse": "parseJson",
      "JSON.stringify": "jsonStringify",
      "console.log": "consoleLog",
      "Number.MAX_SAFE_INTEGER": "INT_MAX",
      "Math.min": "mathMin",
      "Math.max": "mathMax",
      "Math.log": "mathLog",
      "Math.abs": "mathAbs",
      "Math.floor": "mathFloor",
      "Math.pow": "mathPow"
    };
    this.CallExpressionReplacements = {};
    this.ReservedKeywordsReplacements = {
      "union": "unionVar",
      "char": "charVar",
      "default": "defaultVar",
      "operator": "operatorVar",
      "new": "newVar",
      "delete": "deleteVar",
      "template": "templateVar"
    };
    this.VariableTypeReplacements = {};
    this.ArgTypeReplacements = {};
    this.binaryExpressionsWrappers = {
      [_typescript2.default.SyntaxKind.EqualsEqualsToken]: [this.EQUALS_EQUALS_WRAPPER_OPEN, this.EQUALS_EQUALS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.EqualsEqualsEqualsToken]: [this.EQUALS_EQUALS_WRAPPER_OPEN, this.EQUALS_EQUALS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.ExclamationEqualsToken]: [this.DIFFERENT_WRAPPER_OPEN, this.DIFFERENT_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken]: [this.DIFFERENT_WRAPPER_OPEN, this.DIFFERENT_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.GreaterThanToken]: [this.GREATER_THAN_WRAPPER_OPEN, this.GREATER_THAN_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.GreaterThanEqualsToken]: [this.GREATER_THAN_EQUALS_WRAPPER_OPEN, this.GREATER_THAN_EQUALS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.LessThanToken]: [this.LESS_THAN_WRAPPER_OPEN, this.LESS_THAN_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.LessThanEqualsToken]: [this.LESS_THAN_EQUALS_WRAPPER_OPEN, this.LESS_THAN_EQUALS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.PlusToken]: [this.PLUS_WRAPPER_OPEN, this.PLUS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.MinusToken]: [this.MINUS_WRAPPER_OPEN, this.MINUS_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.AsteriskToken]: [this.MULTIPLY_WRAPPER_OPEN, this.MULTIPLY_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.PercentToken]: [this.MOD_WRAPPER_OPEN, this.MOD_WRAPPER_CLOSE],
      [_typescript2.default.SyntaxKind.SlashToken]: [this.DIVIDE_WRAPPER_OPEN, this.DIVIDE_WRAPPER_CLOSE]
    };
  }
  getBlockOpen(identation) {
    return "\n" + this.getIden(identation) + this.BLOCK_OPENING_TOKEN + "\n";
  }
  printSuperCallInsideConstructor(node, identation) {
    return "";
  }
  printStringLiteral(node) {
    return `std::string(${super.printStringLiteral(node)})`;
  }
  printClass(node, identation) {
    const classDefinition = this.printClassDefinition(node, identation);
    const classBody = this.printClassBody(node, identation);
    const classClosing = this.getBlockClose(identation);
    return classDefinition + classBody + classClosing + ";";
  }
  printClassDefinition(node, identation) {
    const className = node.name.escapedText;
    const heritageClauses = node.heritageClauses;
    let classInit = "";
    if (heritageClauses !== void 0) {
      const classExtends = heritageClauses[0].types[0].expression.escapedText;
      classInit = this.getIden(identation) + "class " + className + " : public " + classExtends;
    } else {
      classInit = this.getIden(identation) + "class " + className;
    }
    return classInit + "\n" + this.getIden(identation) + this.BLOCK_OPENING_TOKEN + "\n" + this.getIden(identation) + "public:\n";
  }
  printConstructorDeclaration(node, identation) {
    const classNode = node.parent;
    const className = this.printNode(classNode.name, 0);
    const args = this.printMethodParameters(node);
    const constructorBody = this.printFunctionBody(node, identation);
    let superCallParams = "";
    let hasSuperCall = false;
    _optionalChain([node, 'access', _1764 => _1764.body, 'optionalAccess', _1765 => _1765.statements, 'access', _1766 => _1766.forEach, 'call', _1767 => _1767((statement) => {
      if (_typescript2.default.isExpressionStatement(statement)) {
        const expression = statement.expression;
        if (_typescript2.default.isCallExpression(expression)) {
          const expressionText = expression.expression.getText().trim();
          if (expressionText === "super") {
            hasSuperCall = true;
            superCallParams = expression.arguments.map((a) => {
              return this.printNode(a, identation).trim();
            }).join(", ");
          }
        }
      }
    })]);
    if (hasSuperCall) {
      const parentClassName = classNode.heritageClauses[0].types[0].expression.escapedText;
      return this.getIden(identation) + className + `(${args}) : ${parentClassName}(${superCallParams})` + constructorBody;
    }
    return this.getIden(identation) + className + "(" + args + ")" + constructorBody;
  }
  printThisElementAccesssIfNeeded(node, identation) {
    return void 0;
  }
  printDynamicCall(node, identation) {
    return void 0;
  }
  printAwaitExpression(node, identation) {
    const expression = this.printNode(node.expression, identation);
    if (this.asyncTranspiling) {
      return `awaitValue(${expression})`;
    }
    return expression;
  }
  printReturnStatement(node, identation) {
    if (this.asyncTranspiling && !node.expression) {
      let fn = node.parent;
      while (fn !== void 0 && !_typescript2.default.isFunctionLike(fn)) {
        fn = fn.parent;
      }
      if (fn !== void 0 && this.isAsyncFunction(fn)) {
        return this.getIden(identation) + "return std::any{}" + this.LINE_TERMINATOR;
      }
    }
    return super.printReturnStatement(node, identation);
  }
  printFunctionBody(node, identation) {
    if (this.asyncTranspiling && this.isAsyncFunction(node)) {
      const innerIdentation = identation + 2;
      const bodyStatements = node.body.statements;
      const statements = bodyStatements.map((s) => this.printNode(s, innerIdentation)).join("\n");
      const lastStatement = bodyStatements.length > 0 ? bodyStatements[bodyStatements.length - 1] : void 0;
      const endsWithReturn = lastStatement !== void 0 && lastStatement.kind === _typescript2.default.SyntaxKind.ReturnStatement;
      const fallbackReturn = endsWithReturn ? "" : this.getIden(innerIdentation) + "return std::any{};\n";
      return this.getBlockOpen(identation) + this.getIden(identation + 1) + "return std::async(std::launch::async, [=]() -> std::any {\n" + (statements ? statements + "\n" : "") + fallbackReturn + this.getIden(identation + 1) + "}).share();" + this.getBlockClose(identation);
    }
    return super.printFunctionBody(node, identation);
  }
  printWrappedUnknownThisProperty(node) {
    return void 0;
  }
  printOutOfOrderCallExpressionIfAny(node, identation) {
    if (node.expression.kind === _typescript2.default.SyntaxKind.PropertyAccessExpression) {
      const expressionText = node.expression.getText().trim();
      const args = node.arguments;
      if (args.length === 1) {
        const parsedArg = this.printNode(args[0], 0);
        switch (expressionText) {
          case "Math.abs":
            return `mathAbs(${parsedArg})`;
        }
      } else if (args.length === 2) {
        const parsedArg1 = this.printNode(args[0], 0);
        const parsedArg2 = this.printNode(args[1], 0);
        switch (expressionText) {
          case "Math.min":
            return `mathMin(${parsedArg1}, ${parsedArg2})`;
          case "Math.max":
            return `mathMax(${parsedArg1}, ${parsedArg2})`;
          case "Math.pow":
            return `mathPow(${parsedArg1}, ${parsedArg2})`;
        }
      }
    }
    return void 0;
  }
  handleTypeOfInsideBinaryExpression(node, identation) {
    const right = node.right.text;
    const op = node.operatorToken.kind;
    const expression = node.left.expression;
    const isDifferentOperator = op === _typescript2.default.SyntaxKind.ExclamationEqualsEqualsToken || op === _typescript2.default.SyntaxKind.ExclamationEqualsToken;
    const notOperator = isDifferentOperator ? this.NOT_TOKEN : "";
    const target = this.printNode(expression, 0);
    switch (right) {
      case "string":
        return notOperator + `isString(${target})`;
      case "number":
        return notOperator + `isNumber(${target})`;
      case "boolean":
        return notOperator + `isBool(${target})`;
      case "object":
        return notOperator + `isDictionary(${target})`;
      case "function":
        return notOperator + `isFunction(${target})`;
    }
    return void 0;
  }
  printCustomBinaryExpressionIfAny(node, identation) {
    const left = node.left;
    const right = node.right;
    const op = node.operatorToken.kind;
    if (left.kind === _typescript2.default.SyntaxKind.TypeOfExpression) {
      const typeOfExpression = this.handleTypeOfInsideBinaryExpression(node, identation);
      if (typeOfExpression) {
        return typeOfExpression;
      }
    }
    if (op === _typescript2.default.SyntaxKind.EqualsToken && left.kind === _typescript2.default.SyntaxKind.ArrayLiteralExpression) {
      const arrayBindingPatternElements = left.elements;
      const parsedArrayBindingElements = arrayBindingPatternElements.map((e) => this.printNode(e, 0));
      const syntheticName = parsedArrayBindingElements.join("") + "Variable";
      let arrayBindingStatement = `std::any ${syntheticName} = ${this.printNode(right, 0)};
`;
      parsedArrayBindingElements.forEach((e, index) => {
        const statement = this.getIden(identation) + `${e} = ::getValue(${syntheticName}, ${index})`;
        if (index < parsedArrayBindingElements.length - 1) {
          arrayBindingStatement += statement + ";\n";
        } else {
          arrayBindingStatement += statement;
        }
      });
      return arrayBindingStatement;
    }
    if (op === _typescript2.default.SyntaxKind.InKeyword) {
      return `inOp(${this.printNode(right, 0)}, ${this.printNode(left, 0)})`;
    }
    const leftText = this.printNode(left, 0);
    const rightText = this.printNode(right, 0);
    if (op === _typescript2.default.SyntaxKind.PlusEqualsToken) {
      return `${leftText} = add(${leftText}, ${rightText})`;
    }
    if (op === _typescript2.default.SyntaxKind.MinusEqualsToken) {
      return `${leftText} = subtract(${leftText}, ${rightText})`;
    }
    if (op === _typescript2.default.SyntaxKind.EqualsToken) {
      if (left.kind === _typescript2.default.SyntaxKind.ElementAccessExpression) {
        const elementAccess = left;
        const target = this.printNode(elementAccess.expression, 0);
        const propName = this.printNode(elementAccess.argumentExpression, 0);
        return `::setValue(${target}, ${propName}, ${rightText})`;
      }
    }
    if (op in this.binaryExpressionsWrappers) {
      const wrapper = this.binaryExpressionsWrappers[op];
      const open = wrapper[0];
      const close = wrapper[1];
      return `${open}${leftText}, ${rightText}${close}`;
    }
    return void 0;
  }
  printVariableDeclarationList(node, identation) {
    const declaration = node.declarations[0];
    if (_optionalChain([declaration, 'optionalAccess', _1768 => _1768.name, 'access', _1769 => _1769.kind]) === _typescript2.default.SyntaxKind.ArrayBindingPattern) {
      const arrayBindingPattern = declaration.name;
      const arrayBindingPatternElements = arrayBindingPattern.elements;
      const parsedArrayBindingElements = arrayBindingPatternElements.map((e) => this.printNode(e.name, 0));
      const syntheticName = parsedArrayBindingElements.join("") + "Variable";
      let arrayBindingStatement = `${this.getIden(identation)}std::any ${syntheticName} = ${this.printNode(declaration.initializer, 0)};
`;
      parsedArrayBindingElements.forEach((e, index) => {
        const statement = this.getIden(identation) + `std::any ${e} = ::getValue(${syntheticName}, ${index})`;
        if (index < parsedArrayBindingElements.length - 1) {
          arrayBindingStatement += statement + ";\n";
        } else {
          arrayBindingStatement += statement;
        }
      });
      return arrayBindingStatement;
    }
    const isNew = declaration.initializer && declaration.initializer.kind === _typescript2.default.SyntaxKind.NewExpression;
    let className = void 0;
    if (isNew) {
      className = declaration.initializer.expression.escapedText;
    }
    const varToken = isNew ? className + " " : this.VAR_TOKEN + " ";
    if (declaration.initializer === void 0) {
      return this.getIden(identation) + this.VAR_TOKEN + " " + this.printNode(declaration.name) + " = " + this.UNDEFINED_TOKEN;
    }
    const parsedValue = this.printNode(declaration.initializer, identation).trimStart();
    return this.getIden(identation) + varToken + this.printNode(declaration.name) + " = " + parsedValue;
  }
  printFunctionDefinition(node, identation) {
    let name = node.name.escapedText;
    name = this.transformFunctionNameIfNeeded(name);
    const parsedArgs = node.parameters.map((param) => this.printParameter(param)).join(", ");
    let returnType = this.printFunctionType(node);
    returnType = returnType ? returnType + " " : returnType;
    const functionDef = this.getIden(identation) + returnType + name + "(" + parsedArgs + ")";
    return functionDef;
  }
  printInstanceOfExpression(node, identation) {
    const left = this.printNode(node.left, 0);
    const right = node.right.escapedText;
    return this.getIden(identation) + `(dynamic_cast<const ${right}*>(&(${left})) != nullptr)`;
  }
  printAsExpression(node, identation) {
    return this.printNode(node.expression, identation);
  }
  printParameter(node, defaultValue = true) {
    const name = this.printNode(node.name, 0);
    const initializer = node.initializer;
    if (defaultValue) {
      if (initializer) {
        return this.DEFAULT_PARAMETER_TYPE + " " + name + this.SPACE_DEFAULT_PARAM + "=" + this.SPACE_DEFAULT_PARAM + this.printNode(initializer, 0);
      }
      return this.DEFAULT_PARAMETER_TYPE + " " + name;
    }
    return name;
  }
  printFunctionType(node) {
    if (!this.requiresReturnType) {
      return "";
    }
    if (this.asyncTranspiling && this.isAsyncFunction(node)) {
      return `std::shared_future<${this.DEFAULT_RETURN_TYPE}>`;
    }
    const typeText = this.getFunctionType(node);
    if (typeText === this.VOID_KEYWORD) {
      return this.VOID_KEYWORD;
    }
    return this.DEFAULT_RETURN_TYPE;
  }
  printMethodDefinition(node, identation) {
    let name = node.name.escapedText;
    name = this.transformMethodNameIfNeeded(name);
    let returnType = this.printFunctionType(node);
    const methodOverride = this.getMethodOverride(node);
    const isOverride = methodOverride !== void 0;
    const virtualPrefix = isOverride ? "" : "virtual ";
    const overrideSuffix = isOverride ? " override" : "";
    if (isOverride) {
      returnType = this.printFunctionType(methodOverride);
    }
    const parsedArgs = this.printMethodParameters(node);
    returnType = returnType ? returnType + " " : returnType;
    const methodToken = this.METHOD_TOKEN ? this.METHOD_TOKEN + " " : "";
    const methodDef = this.getIden(identation) + virtualPrefix + returnType + methodToken + name + "(" + parsedArgs + ")" + overrideSuffix;
    return this.printNodeCommentsIfAny(node, identation, methodDef);
  }
  printPropertyDeclaration(node, identation) {
    const name = this.printNode(node.name, 0);
    if (node.initializer) {
      const initializer = this.printNode(node.initializer, 0);
      return this.getIden(identation) + "std::any " + name + " = " + initializer + this.LINE_TERMINATOR;
    }
    return this.getIden(identation) + "std::any " + name + this.LINE_TERMINATOR;
  }
  printArrayLiteralExpression(node) {
    const elements = node.elements.map((e) => {
      return this.printNode(e);
    }).join(", ");
    return this.ARRAY_OPENING_TOKEN + elements + this.ARRAY_CLOSING_TOKEN;
  }
  printArrayIsArrayCall(node, identation, parsedArg = void 0) {
    return `isArray(${parsedArg})`;
  }
  printObjectKeysCall(node, identation, parsedArg = void 0) {
    return `getObjectKeys(${parsedArg})`;
  }
  printObjectValuesCall(node, identation, parsedArg = void 0) {
    return `getObjectValues(${parsedArg})`;
  }
  printJsonParseCall(node, identation, parsedArg = void 0) {
    return `parseJson(${parsedArg})`;
  }
  printJsonStringifyCall(node, identation, parsedArg = void 0) {
    return `jsonStringify(${parsedArg})`;
  }
  printPromiseAllCall(node, identation, parsedArg = void 0) {
    return `promiseAll(${parsedArg})`;
  }
  printMathFloorCall(node, identation, parsedArg = void 0) {
    return `mathFloor(${parsedArg})`;
  }
  printMathRoundCall(node, identation, parsedArg = void 0) {
    return `mathRound(${parsedArg})`;
  }
  printMathCeilCall(node, identation, parsedArg = void 0) {
    return `mathCeil(${parsedArg})`;
  }
  printNumberIsIntegerCall(node, identation, parsedArg) {
    return `isInteger(${parsedArg})`;
  }
  printArrayPushCall(node, identation, name = void 0, parsedArg = void 0) {
    return `arrayPush(${name}, ${parsedArg})`;
  }
  printIncludesCall(node, identation, name = void 0, parsedArg = void 0) {
    return `includes(${name}, ${parsedArg})`;
  }
  printIndexOfCall(node, identation, name = void 0, parsedArg = void 0) {
    return `${this.INDEXOF_WRAPPER_OPEN}${name}, ${parsedArg}${this.INDEXOF_WRAPPER_CLOSE}`;
  }
  printStartsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    return `startsWith(${name}, ${parsedArg})`;
  }
  printEndsWithCall(node, identation, name = void 0, parsedArg = void 0) {
    return `endsWith(${name}, ${parsedArg})`;
  }
  printTrimCall(node, identation, name = void 0) {
    return `trim(${name})`;
  }
  printJoinCall(node, identation, name = void 0, parsedArg = void 0) {
    return `join(${name}, ${parsedArg})`;
  }
  printSplitCall(node, identation, name = void 0, parsedArg = void 0) {
    return `split(${name}, ${parsedArg})`;
  }
  printConcatCall(node, identation, name = void 0, parsedArg = void 0) {
    return `concat(${name}, ${parsedArg})`;
  }
  printToFixedCall(node, identation, name = void 0, parsedArg = void 0) {
    return `toFixed(${name}, ${parsedArg})`;
  }
  printToStringCall(node, identation, name = void 0) {
    return `toString(${name})`;
  }
  printToUpperCaseCall(node, identation, name = void 0) {
    return `toUpperCase(${name})`;
  }
  printToLowerCaseCall(node, identation, name = void 0) {
    return `toLowerCase(${name})`;
  }
  printShiftCall(node, identation, name = void 0) {
    return `shift(${name})`;
  }
  printReverseCall(node, identation, name = void 0) {
    return `reverse(${name})`;
  }
  printPopCall(node, identation, name = void 0) {
    return `pop(${name})`;
  }
  printAssertCall(node, identation, parsedArgs) {
    return `assertTrue(${parsedArgs})`;
  }
  printSliceCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    if (parsedArg2 === void 0) {
      parsedArg2 = this.UNDEFINED_TOKEN;
    }
    return `slice(${name}, ${parsedArg}, ${parsedArg2})`;
  }
  printReplaceCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    return `replace(${name}, ${parsedArg}, ${parsedArg2})`;
  }
  printReplaceAllCall(node, identation, name = void 0, parsedArg = void 0, parsedArg2 = void 0) {
    return `replaceAll(${name}, ${parsedArg}, ${parsedArg2})`;
  }
  printPadEndCall(node, identation, name, parsedArg, parsedArg2) {
    return `padEnd(${name}, ${parsedArg}, ${parsedArg2})`;
  }
  printPadStartCall(node, identation, name, parsedArg, parsedArg2) {
    return `padStart(${name}, ${parsedArg}, ${parsedArg2})`;
  }
  printDateNowCall(node, identation) {
    return "getCurrentTimestamp()";
  }
  printLengthProperty(node, identation, name = void 0) {
    const leftSide = this.printNode(node.expression, 0);
    const type = this.getChecker().getTypeAtLocation(node.expression);
    this.warnIfAnyType(node, type.flags, leftSide, "length");
    return this.isStringType(type.flags) ? `getStringLength(${leftSide})` : `${this.ARRAY_LENGTH_WRAPPER_OPEN}${leftSide}${this.ARRAY_LENGTH_WRAPPER_CLOSE}`;
  }
  printPostFixUnaryExpression(node, identation) {
    const { operand, operator } = node;
    if (operand.kind === _typescript2.default.SyntaxKind.NumericLiteral) {
      return super.printPostFixUnaryExpression(node, identation);
    }
    const leftSide = this.printNode(operand, 0);
    const op = this.PostFixOperators[operator];
    if (op === "--") {
      return `postFixDecrement(${leftSide})`;
    }
    return `postFixIncrement(${leftSide})`;
  }
  printPrefixUnaryExpression(node, identation) {
    const { operand, operator } = node;
    if (operand.kind === _typescript2.default.SyntaxKind.NumericLiteral) {
      return super.printPrefixUnaryExpression(node, identation);
    }
    if (operator === _typescript2.default.SyntaxKind.ExclamationToken) {
      return this.PrefixFixOperators[operator] + this.printCondition(node.operand, 0);
    }
    const leftSide = this.printNode(operand, 0);
    if (operator === _typescript2.default.SyntaxKind.PlusToken) {
      return `prefixUnaryPlus(${leftSide})`;
    } else {
      return `prefixUnaryNeg(${leftSide})`;
    }
  }
  printConditionalExpression(node, identation) {
    const condition = this.printCondition(node.condition, 0);
    const whenTrue = this.printNode(node.whenTrue, 0);
    const whenFalse = this.printNode(node.whenFalse, 0);
    return `(${condition} ? std::any(${whenTrue}) : std::any(${whenFalse}))`;
  }
  printDeleteExpression(node, identation) {
    const object = this.printNode(node.expression.expression, 0);
    const key = this.printNode(node.expression.argumentExpression, 0);
    return `deleteKey(${object}, ${key})`;
  }
  printThrowStatement(node, identation) {
    if (node.expression.kind === _typescript2.default.SyntaxKind.Identifier) {
      return this.getIden(identation) + this.THROW_TOKEN + " " + this.printNode(node.expression, 0) + this.LINE_TERMINATOR;
    }
    if (node.expression.kind === _typescript2.default.SyntaxKind.NewExpression) {
      const expression = node.expression;
      const argumentsExp = _nullishCoalesce(_optionalChain([expression, 'optionalAccess', _1770 => _1770.arguments]), () => ( []));
      const parsedArg = _nullishCoalesce(argumentsExp.map((n) => this.printNode(n, 0)).join(", "), () => ( ""));
      const newExpression = this.printNode(expression.expression, 0);
      if (expression.expression.kind === _typescript2.default.SyntaxKind.Identifier) {
        const id = expression.expression;
        const symbol = this.getChecker().getSymbolAtLocation(expression.expression);
        if (symbol) {
          const declarations = _nullishCoalesce(_optionalChain([this, 'access', _1771 => _1771.getChecker, 'call', _1772 => _1772(), 'access', _1773 => _1773.getDeclaredTypeOfSymbol, 'call', _1774 => _1774(symbol), 'access', _1775 => _1775.symbol, 'optionalAccess', _1776 => _1776.declarations]), () => ( []));
          const isClassDeclaration = declarations.find((l) => l.kind === _typescript2.default.SyntaxKind.InterfaceDeclaration || l.kind === _typescript2.default.SyntaxKind.ClassDeclaration);
          if (isClassDeclaration) {
            return this.getIden(identation) + `${this.THROW_TOKEN} ${id.escapedText}(toString(${parsedArg}))${this.LINE_TERMINATOR}`;
          }
          return this.getIden(identation) + `throwDynamicException(${id.escapedText}, ${parsedArg})${this.LINE_TERMINATOR}`;
        }
        return this.getIden(identation) + `${this.THROW_TOKEN} ${newExpression}(${parsedArg})${this.LINE_TERMINATOR}`;
      } else if (expression.expression.kind === _typescript2.default.SyntaxKind.ElementAccessExpression) {
        return this.getIden(identation) + `throwDynamicException(${newExpression}, ${parsedArg})${this.LINE_TERMINATOR}`;
      }
      return super.printThrowStatement(node, identation);
    }
    return super.printThrowStatement(node, identation);
  }
  getExceptionalAccessTokenIfAny(node) {
    const leftSide = node.expression;
    const leftSideText = this.printNode(leftSide, 0);
    if (leftSideText === this.THIS_TOKEN) {
      return "->";
    }
    return void 0;
  }
};

// src/transpiler.ts
var __dirname_mock = import_dirname.default;
var fastCompilerOptions = {
  target: _typescript2.default.ScriptTarget.Latest,
  lib: ["lib.esnext.d.ts"],
  types: []
};
var globalsShim = `
declare var require: any;
declare var module: any;
declare var exports: any;
declare var console: any;
declare var process: any;
declare var Buffer: any;
declare var __dirname: string;
declare var __filename: string;
declare var setTimeout: any;
declare var clearTimeout: any;
declare var setInterval: any;
declare var clearInterval: any;
declare var setImmediate: any;
declare var fetch: any;
declare var URL: any;
declare var URLSearchParams: any;
declare var TextEncoder: any;
declare var TextDecoder: any;
declare var crypto: any;
declare var performance: any;
declare var AbortController: any;
declare var WebSocket: any;
declare var atob: any;
declare var btoa: any;
`;
var globalsShimPath = path2.resolve(path2.join(__dirname_mock, "__globals-shim.d.ts"));
function overrideHostForVirtualFiles(host, files) {
  const originalGetSourceFile = host.getSourceFile.bind(host);
  const originalReadFile = host.readFile.bind(host);
  const originalFileExists = host.fileExists.bind(host);
  host.getSourceFile = (fileName, languageVersion, onError, shouldCreateNewSourceFile) => {
    const virtual = files.get(path2.resolve(fileName));
    return virtual !== void 0 ? virtual : originalGetSourceFile(fileName, languageVersion, onError, shouldCreateNewSourceFile);
  };
  host.readFile = (fileName) => {
    const virtual = files.get(path2.resolve(fileName));
    return virtual !== void 0 ? virtual.text : originalReadFile(fileName);
  };
  host.fileExists = (fileName) => {
    return files.has(path2.resolve(fileName)) || originalFileExists(fileName);
  };
}
var NO_SYMBOL_SENTINEL = Symbol("noSymbol");
function memoizeCheckerCalls(checker) {
  if (checker.__astTranspilerMemoized) {
    return;
  }
  checker.__astTranspilerMemoized = true;
  const typeCache = /* @__PURE__ */ new WeakMap();
  const originalGetTypeAtLocation = checker.getTypeAtLocation.bind(checker);
  checker.getTypeAtLocation = (node) => {
    let type = typeCache.get(node);
    if (type === void 0) {
      type = originalGetTypeAtLocation(node);
      typeCache.set(node, type);
    }
    return type;
  };
  const symbolCache = /* @__PURE__ */ new WeakMap();
  const originalGetSymbolAtLocation = checker.getSymbolAtLocation.bind(checker);
  checker.getSymbolAtLocation = (node) => {
    const cached = symbolCache.get(node);
    if (cached !== void 0) {
      return cached === NO_SYMBOL_SENTINEL ? void 0 : cached;
    }
    const symbol = originalGetSymbolAtLocation(node);
    symbolCache.set(node, symbol === void 0 ? NO_SYMBOL_SENTINEL : symbol);
    return symbol;
  };
}
function getProgramAndTypeCheckerFromMemory(rootDir, text, options = {}, cache) {
  options = options || _typescript2.default.getDefaultCompilerOptions();
  const inMemoryFilePath = path2.resolve(path2.join(rootDir, "__dummy-file.ts"));
  const textAst = _typescript2.default.createSourceFile(inMemoryFilePath, text, options.target || _typescript2.default.ScriptTarget.Latest);
  const shimAst = _typescript2.default.createSourceFile(globalsShimPath, globalsShim, options.target || _typescript2.default.ScriptTarget.Latest);
  const host = _typescript2.default.createCompilerHost(options, true);
  overrideHostForVirtualFiles(host, /* @__PURE__ */ new Map([
    [inMemoryFilePath, textAst],
    [globalsShimPath, shimAst]
  ]));
  if (cache !== void 0) {
    const originalGetSourceFile = host.getSourceFile.bind(host);
    host.getSourceFile = (fileName, languageVersion, onError, shouldCreateNewSourceFile) => {
      const resolved = path2.resolve(fileName);
      if (resolved === inMemoryFilePath) {
        return textAst;
      }
      const cached = cache.sourceFiles.get(resolved);
      if (cached !== void 0 && !shouldCreateNewSourceFile) {
        return cached.sourceFile;
      }
      const sourceFile2 = originalGetSourceFile(fileName, languageVersion, onError, shouldCreateNewSourceFile);
      if (sourceFile2 !== void 0) {
        cache.sourceFiles.set(resolved, { mtimeMs: 0, sourceFile: sourceFile2 });
      }
      return sourceFile2;
    };
  }
  const program = _typescript2.default.createProgram({
    options,
    rootNames: [inMemoryFilePath, globalsShimPath],
    host,
    oldProgram: _optionalChain([cache, 'optionalAccess', _1777 => _1777.memoryOldProgram])
  });
  if (cache !== void 0) {
    cache.memoryOldProgram = program;
  }
  const typeChecker = program.getTypeChecker();
  memoizeCheckerCalls(typeChecker);
  const sourceFile = program.getSourceFile(inMemoryFilePath);
  return [program, typeChecker, sourceFile];
}
var Transpiler = class _Transpiler {
  // A program cache holds parsed typescript SourceFiles and the last program built
  // from them. Hand the same cache to several Transpiler instances to reuse one
  // parse/typecheck of the es lib chain and of every shared import across all of
  // them. Callers that need isolation simply omit it and get a private cache.
  //
  // Same-thread only: these are live V8 objects, so a cache cannot be posted to a
  // worker_threads isolate — give each worker its own long-lived cache instead.
  static createProgramCache() {
    return { sourceFiles: /* @__PURE__ */ new Map() };
  }
  constructor(config = {}, programCache) {
    this.config = config;
    this.programCache = _nullishCoalesce(programCache, () => ( _Transpiler.createProgramCache()));
    const phpConfig = config["php"] || {};
    const pythonConfig = config["python"] || {};
    const csharpConfig = config["csharp"] || {};
    const goConfig = config["go"] || {};
    const javaConfig = config["java"] || {};
    const rustConfig = config["rust"] || {};
    const cppConfig = config["cpp"] || {};
    if ("verbose" in config) {
      Logger.setVerboseMode(Boolean(config["verbose"]));
    }
    this.pythonTranspiler = new PythonTranspiler(pythonConfig);
    this.phpTranspiler = new PhpTranspiler(phpConfig);
    this.csharpTranspiler = new CSharpTranspiler(csharpConfig);
    this.goTranspiler = new GoTranspiler(goConfig);
    this.javaTranspiler = new JavaTranspiler(javaConfig);
    this.rustTranspiler = new RustTranspiler(rustConfig);
    this.cppTranspiler = new CppTranspiler(cppConfig);
  }
  setVerboseMode(verbose) {
    Logger.setVerboseMode(verbose);
  }
  // the cache this instance parses into, to hand to further Transpiler instances
  // that should reuse this one's parsed SourceFiles
  getProgramCache() {
    return this.programCache;
  }
  // a second Transpiler over the same parsed typescript state, with its own
  // printers and its own transpile context, so both can be driven independently
  // on this thread without either clobbering the other's program
  cloneSharingProgramCache(config = this.config) {
    return new _Transpiler(config, this.programCache);
  }
  createProgramInMemoryAndSetContext(content) {
    const [memProgram, memType, memSource] = getProgramAndTypeCheckerFromMemory(__dirname_mock, content, fastCompilerOptions, this.programCache);
    return this.setContext({
      src: memSource,
      checker: memType,
      program: memProgram
    });
  }
  getByPathCompilerHost(options) {
    if (this.programCache.byPathHost === void 0) {
      const host = _typescript2.default.createCompilerHost(options, true);
      const originalGetSourceFile = host.getSourceFile.bind(host);
      const cache = this.programCache.sourceFiles;
      host.getSourceFile = (fileName, languageVersion, onError, shouldCreateNewSourceFile) => {
        let mtimeMs = 0;
        try {
          mtimeMs = fs2.statSync(fileName).mtimeMs;
        } catch (e) {
        }
        const cached = cache.get(fileName);
        if (cached && cached.mtimeMs === mtimeMs && !shouldCreateNewSourceFile) {
          return cached.sourceFile;
        }
        const sourceFile = originalGetSourceFile(fileName, languageVersion, onError, shouldCreateNewSourceFile);
        if (sourceFile !== void 0) {
          cache.set(fileName, { mtimeMs, sourceFile });
        }
        return sourceFile;
      };
      const shimAst = _typescript2.default.createSourceFile(globalsShimPath, globalsShim, _typescript2.default.ScriptTarget.Latest);
      overrideHostForVirtualFiles(host, /* @__PURE__ */ new Map([[globalsShimPath, shimAst]]));
      this.programCache.byPathHost = host;
    }
    return this.programCache.byPathHost;
  }
  createProgramByPathAndSetContext(path3) {
    const options = fastCompilerOptions;
    const host = this.getByPathCompilerHost(options);
    const program = _typescript2.default.createProgram([path3, globalsShimPath], options, host, this.programCache.byPathOldProgram);
    this.programCache.byPathOldProgram = program;
    const sourceFile = program.getSourceFile(path3);
    const typeChecker = program.getTypeChecker();
    memoizeCheckerCalls(typeChecker);
    return this.setContext({
      src: sourceFile,
      checker: typeChecker,
      program
    });
  }
  // One program over N root files, instead of one program per file. Every
  // transpile*ByPath call pays for a full program: even with the SourceFile cache
  // making the ~340-file import closure parse-free, the binder/checker work behind
  // getPreEmitDiagnostics is redone per file. Batching N files into one program
  // pays it once for the whole set.
  //
  // Files that import each other (a derived exchange and its parent) are fine in
  // one batch — they are separate root files of the same program, exactly as
  // typescript would compile a project.
  //
  // The batch deliberately does not become the cache's byPathOldProgram: an N-file
  // program never structurally reuses a program built from a different root set, so
  // there is nothing to gain, and keeping the previous chunk's checker alive while
  // the next one is built would double peak memory — the opposite of why callers
  // chunk. The cross-batch saving comes from the shared host + SourceFile cache.
  createProgramBatch(paths) {
    const options = fastCompilerOptions;
    const host = this.getByPathCompilerHost(options);
    const program = _typescript2.default.createProgram([...paths, globalsShimPath], options, host);
    const checker = program.getTypeChecker();
    memoizeCheckerCalls(checker);
    return new TranspileProgramBatch(this, program, checker);
  }
  // the language printers read the typescript state (source file, checker, program)
  // off the context handed to them here, so two Transpiler instances never share
  // state and a nested transpile can restore whatever its caller was working on
  setContext(context) {
    this.context = context;
    this.pythonTranspiler.setContext(context);
    this.phpTranspiler.setContext(context);
    this.csharpTranspiler.setContext(context);
    this.goTranspiler.setContext(context);
    this.javaTranspiler.setContext(context);
    this.rustTranspiler.setContext(context);
    this.cppTranspiler.setContext(context);
    return context;
  }
  /** @deprecated renamed to createProgramInMemoryAndSetContext */
  createProgramInMemoryAndSetGlobals(content) {
    return this.createProgramInMemoryAndSetContext(content);
  }
  /** @deprecated renamed to createProgramByPathAndSetContext */
  createProgramByPathAndSetGlobals(path3) {
    return this.createProgramByPathAndSetContext(path3);
  }
  checkFileDiagnostics(context = this.context) {
    const diagnostics = _typescript2.default.getPreEmitDiagnostics(context.program, context.src);
    if (diagnostics.length > 0) {
      let errorMessage = "Errors found in the typescript code. Transpilation might produce invalid results:\n";
      diagnostics.forEach((msg) => {
        errorMessage += "  - " + msg.messageText + "\n";
      });
      Logger.warning(errorMessage);
    }
  }
  transpile(lang, mode, file, sync = false, createContext = true, handleImports = true) {
    if (createContext) {
      if (mode === 0 /* ByPath */) {
        this.createProgramByPathAndSetContext(file);
      } else {
        this.createProgramInMemoryAndSetContext(file);
      }
      this.checkFileDiagnostics();
    }
    const src = this.context.src;
    let transpiledContent = void 0;
    switch (lang) {
      case 0 /* Python */:
        this.pythonTranspiler.asyncTranspiling = !sync;
        transpiledContent = this.pythonTranspiler.printNode(src, -1);
        this.pythonTranspiler.asyncTranspiling = true;
        break;
      case 1 /* Php */:
        this.phpTranspiler.asyncTranspiling = !sync;
        transpiledContent = this.phpTranspiler.printNode(src, -1);
        this.phpTranspiler.asyncTranspiling = true;
        break;
      case 2 /* CSharp */:
        transpiledContent = this.csharpTranspiler.printNode(src, -1);
        break;
      case 3 /* Go */:
        transpiledContent = alignGoTrailingComments(this.goTranspiler.printNode(src, -1));
        break;
      case 4 /* Java */:
        transpiledContent = this.javaTranspiler.printNode(src, -1);
        break;
      case 5 /* Rust */:
        transpiledContent = this.rustTranspiler.printNode(src, -1);
        break;
      case 6 /* Cpp */:
        transpiledContent = this.cppTranspiler.printNode(src, -1);
        break;
    }
    let imports = [];
    let exports = [];
    if (handleImports) {
      imports = this.pythonTranspiler.getFileImports(src);
      exports = this.pythonTranspiler.getFileExports(src);
    }
    const methodsTypes = this.pythonTranspiler.getMethodTypes(src);
    Logger.success("transpilation finished successfully");
    return {
      content: transpiledContent,
      imports,
      exports,
      methodsTypes
    };
  }
  transpileDifferentLanguagesGeneric(mode, input, content) {
    let context;
    if (mode === 0 /* ByPath */) {
      context = this.createProgramByPathAndSetContext(content);
    } else {
      context = this.createProgramInMemoryAndSetContext(content);
    }
    this.checkFileDiagnostics(context);
    const files = [];
    input.forEach((inp) => {
      const async = inp.async;
      files.push({
        content: this.transpile(inp.language, mode, content, !async, false, false).content
      });
    });
    const methodsTypes = this.pythonTranspiler.getMethodTypes(context.src);
    const imports = this.pythonTranspiler.getFileImports(context.src);
    const exports = this.pythonTranspiler.getFileExports(context.src);
    const output = files.map((file) => {
      return {
        content: file.content,
        imports,
        exports,
        methodsTypes
      };
    });
    return output;
  }
  transpileDifferentLanguages(input, content) {
    const config = input.map((inp) => {
      return {
        language: this.convertStringToLanguageEnum(inp.language),
        async: inp.async
      };
    });
    return this.transpileDifferentLanguagesGeneric(1 /* ByContent */, config, content);
  }
  transpileDifferentLanguagesByPath(input, content) {
    const config = input.map((inp) => {
      return {
        language: this.convertStringToLanguageEnum(inp.language),
        async: inp.async
      };
    });
    return this.transpileDifferentLanguagesGeneric(0 /* ByPath */, config, content);
  }
  transpilePython(content) {
    return this.transpile(0 /* Python */, 1 /* ByContent */, content, !this.pythonTranspiler.asyncTranspiling);
  }
  transpilePythonByPath(path3) {
    return this.transpile(0 /* Python */, 0 /* ByPath */, path3, !this.pythonTranspiler.asyncTranspiling);
  }
  transpilePhp(content) {
    return this.transpile(1 /* Php */, 1 /* ByContent */, content, !this.phpTranspiler.asyncTranspiling);
  }
  transpilePhpByPath(path3) {
    return this.transpile(1 /* Php */, 0 /* ByPath */, path3, !this.phpTranspiler.asyncTranspiling);
  }
  transpileCSharp(content) {
    return this.transpile(2 /* CSharp */, 1 /* ByContent */, content);
  }
  transpileCSharpByPath(path3) {
    return this.transpile(2 /* CSharp */, 0 /* ByPath */, path3);
  }
  transpileJava(content) {
    return this.transpile(4 /* Java */, 1 /* ByContent */, content);
  }
  transpileJavaByPath(path3) {
    return this.transpile(4 /* Java */, 0 /* ByPath */, path3);
  }
  transpileGoByPath(path3) {
    return this.transpile(3 /* Go */, 0 /* ByPath */, path3);
  }
  transpileGo(content) {
    return this.transpile(3 /* Go */, 1 /* ByContent */, content);
  }
  transpileRust(content) {
    return this.transpile(5 /* Rust */, 1 /* ByContent */, content);
  }
  transpileRustByPath(path3) {
    return this.transpile(5 /* Rust */, 0 /* ByPath */, path3);
  }
  transpileCpp(content) {
    return this.transpile(6 /* Cpp */, 1 /* ByContent */, content);
  }
  transpileCppByPath(path3) {
    return this.transpile(6 /* Cpp */, 0 /* ByPath */, path3);
  }
  getFileImports(content) {
    const context = this.createProgramInMemoryAndSetContext(content);
    return this.phpTranspiler.getFileImports(context.src);
  }
  getFileExports(content) {
    const context = this.createProgramInMemoryAndSetContext(content);
    return this.phpTranspiler.getFileExports(context.src);
  }
  setPHPPropResolution(props) {
    this.phpTranspiler.propRequiresScopeResolutionOperator = props;
  }
  setPhpUncamelCaseIdentifiers(uncamelCase) {
    this.phpTranspiler.uncamelcaseIdentifiers = uncamelCase;
  }
  setPythonUncamelCaseIdentifiers(uncamelCase) {
    this.pythonTranspiler.uncamelcaseIdentifiers = uncamelCase;
  }
  setPhpAsyncTranspiling(async) {
    this.phpTranspiler.asyncTranspiling = async;
  }
  setPythonAsyncTranspiling(async) {
    this.pythonTranspiler.asyncTranspiling = async;
  }
  setPythonStringLiteralReplacements(replacements) {
    this.pythonTranspiler.StringLiteralReplacements = replacements;
  }
  convertStringToLanguageEnum(lang) {
    switch (lang) {
      case "python":
        return 0 /* Python */;
      case "php":
        return 1 /* Php */;
      case "csharp":
        return 2 /* CSharp */;
      case "go":
        return 3 /* Go */;
      case "java":
        return 4 /* Java */;
      case "rust":
        return 5 /* Rust */;
      case "cpp":
        return 6 /* Cpp */;
    }
  }
};
var TranspileProgramBatch = class {
  constructor(transpiler, program, checker) {
    this.transpiler = transpiler;
    this.program = program;
    this.checker = checker;
  }
  getProgram() {
    return this.program;
  }
  // point the owning Transpiler at one file of this batch, then run the same
  // diagnostics pass the single-file path runs — the printers read checker state
  // back from it, so it is not optional
  setContextForPath(filePath) {
    const src = _nullishCoalesce(this.program.getSourceFile(filePath), () => ( this.program.getSourceFile(path2.resolve(filePath))));
    if (src === void 0) {
      throw new Error(`ast-transpiler: "${filePath}" is not a file of this program batch`);
    }
    const context = this.transpiler.setContext({ src, checker: this.checker, program: this.program });
    this.transpiler.checkFileDiagnostics(context);
    return context;
  }
  transpileByPath(lang, filePath, sync = false) {
    this.setContextForPath(filePath);
    return this.transpiler.transpile(lang, 0 /* ByPath */, filePath, sync, false);
  }
  transpilePythonByPath(filePath) {
    return this.transpileByPath(0 /* Python */, filePath, !this.transpiler.pythonTranspiler.asyncTranspiling);
  }
  transpilePhpByPath(filePath) {
    return this.transpileByPath(1 /* Php */, filePath, !this.transpiler.phpTranspiler.asyncTranspiling);
  }
  transpileCSharpByPath(filePath) {
    return this.transpileByPath(2 /* CSharp */, filePath);
  }
  transpileGoByPath(filePath) {
    return this.transpileByPath(3 /* Go */, filePath);
  }
  transpileJavaByPath(filePath) {
    return this.transpileByPath(4 /* Java */, filePath);
  }
  transpileRustByPath(filePath) {
    return this.transpileByPath(5 /* Rust */, filePath);
  }
  transpileCppByPath(filePath) {
    return this.transpileByPath(6 /* Cpp */, filePath);
  }
};






exports.RUST_DECLARED_DICT_LOCALS = RUST_DECLARED_DICT_LOCALS; exports.TranspileProgramBatch = TranspileProgramBatch; exports.Transpiler = Transpiler; exports.alignGoTrailingComments = alignGoTrailingComments; exports.default = Transpiler;
//# sourceMappingURL=transpiler.cjs.map